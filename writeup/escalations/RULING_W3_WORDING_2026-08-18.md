# RULING — the W3 wording escalation of 2026-08-18

**The user ruled on 2026-08-18**, on `ESCALATION_W3_WORDING_2026-08-18.md` (raised by the Conductor
at `6320790` when `V3` / leg 399 graded `arXiv:2509.25116` `GRADE_A_FLUID`). **All three questions
answered, plus two consequential items.** This document is the authoritative record and is
**transcription work for the Conductor** — the exact wording is given below.

**`plan_of_record.py`, `WALLS.md`, `STATE.md` and `OPTIONS.md` were deliberately NOT edited by the
session recording this ruling.** They are Conductor-owned under §3g and three of them sit within
0.7% of their §3j caps. **§3j is amended in the same push to make room** — see §5.

---

## (Q1) WHICH WORDING GOVERNS W3 — **RULED: THE PROSE TEST.**

> **W3's prose test governs.** The wall requires a **genuine finite-time singularity**. **W3 STANDS.
> Lane V's premise survives. The cell-occupancy fact is recorded BESIDE the wall, not as a break of
> it.**

**The reasoning, recorded so the ruling can be audited rather than trusted.** Leg 174's two-clause
predicate was written to grade a **certification standard** — *is this enclosure Grade A, and is the
object a fluid* — not to define what W3 asserts. The wall's own headline has always carried
"blow-up". Where a grading predicate and the proposition it was recruited to support diverge, **the
proposition governs and the predicate is renamed to what it actually measures.**

**And the second reason, which is about behaviour and matters more.** `V3` found this row by applying
leg 174's criterion **unchanged**, having been told explicitly not to tighten it, and it recorded the
YES first and unsoftened with the disqualifying qualification beside it. **Letting the predicate
redefine the wall would convert that honesty into a wall-break it does not support; tightening the
predicate after seeing the row it admitted would be the mirror error.** Neither happens. The
predicate stays exactly as leg 174 wrote it, and it stops being described as W3's test.

**What this does NOT do.** It does not withdraw `V3`'s YES, which stands as graded. It does not
restore *"leg 242 confirms nobody filled it since"* — that over-read stays struck. It does not make
the cell empty: **on leg 174's own definitions the cell is OCCUPIED, and W3 is true anyway**, because
they are two different claims. Any unit writing otherwise is over-reading this ruling.

## (Q2) W3's TITLE — **RULED: RETITLE.**

> **W3's title becomes: `W3 — No certified blow-up exists for any fluid equation`.**
> The former title, *"The Grade-A × fluid cell is empty"*, is **struck, not deleted**, with the
> reason and this ruling's date.

**Required beside it, in W3's body, non-optional:**

> **THE CELL IS OCCUPIED AND THE WALL STILL STANDS — THESE ARE TWO DIFFERENT CLAIMS.**
> `arXiv:2509.25116` (Hou–Wang–Yang) satisfies **both** clauses of leg 174's Grade-A × fluid
> criterion — interval arithmetic (§7.3, p.55) enclosing a solution of a system carrying `−ΔŨ`
> (Prop. 1, eq. (1.15), p.5) for the unforced 3D incompressible Navier–Stokes equations (eq. (1.1)).
> **The certified object is NOT a finite-time singularity**: forward self-similar, from singular
> initial data, *"smooth for positive times"*, concluding **nonuniqueness of Leray–Hopf solutions**.
> The paper itself warns against confusing this with the backward setting (§1.2, p.2). **W3 asserts
> the absence of a certified SINGULARITY and is unrefuted; leg 174's cell asserts the absence of a
> Grade-A fluid ENCLOSURE and is refuted.** Graded by `V3` / leg 399, **UNVERIFIED and UNAUDITED**.

**Why the retitle is required whichever way Q1 went**, restated from the escalation because it is the
part that survives any answer: on leg 174's own definitions that cell is occupied, so the old title
is false as a matter of fact. **A wall whose title and whose test disagree is exactly how the next
over-read gets built** — that is the leg-348 shape, and it has now cost this programme two lanes'
worth of misdirection.

## (Q3) LEG 174's BANKED `the_empty_cell.meaning` — **RULED: CORRECTION RECORD, ARTEFACT UNTOUCHED.**

> `writeup/data/p2_route_vbs_v1_scoping.json::the_empty_cell.meaning` is **NOT edited.** A
> **correction record** is added in the established over-read-closure pattern, naming the field, the
> measurement that falsified it, and the unit that made it.

The sentence *"no published work applies interval arithmetic to a DISSIPATIVE fluid equation's own
self-similar object"* is **measured FALSE** by `2509.25116`, and the word *"self-similar"* is in it,
so no reading survives. **The Conductor already refused to edit it and was right**: *"rewriting a
banked datum to match a later finding is precisely how a record stops being a record."* This ruling
makes that refusal the standing rule for banked artefacts, not a one-off judgement.

## (Q4) `V3`'s ADVERSARIAL AUDIT — **RULED: DISPATCH IT. Wave 6, and it is not optional.**

> **The adversarial full-text audit of `arXiv:2509.25116`, to leg-309 depth, is QUEUED FOR WAVE 6**
> (≈4–8 h, `V3` §6 item (a)). Wave 5 is already dispatched and is not re-opened for it.

**Why it is required even though Q1 leaves W3 standing.** The YES is **UNAUDITED**: one database,
title-screened, Semantic Scholar banked as **a GAP, not a zero** (no key). It has already changed
what the record says about its own occupancy claim, and `V3` names this *"the unit that decides
whether the YES survives contact"* and rates it highest-value by a wide margin.

**Its scope, now that Q1 is answered:** enumerate the load-bearing constants **before** adjudicating;
recompute what is recomputable; and check the localisation step from the `ℝ³` self-similar profile to
a genuine Leray–Hopf solution (Remark 2, §1.3 + §2). **Pre-commit both branches** — a YES that
survives, and a YES that does not — before it starts.

**One question it must also answer, and nobody has asked it: does this row bear on W2?** `V3` was
gated on W3 and correctly stayed there. If Hou–Wang–Yang's certified profile is **genuinely
three-dimensional** rather than symmetry-reduced, it touches the census W2 rests on — and W2's own
pre-committed test is the arbiter. **Report it as a separate clause. Do not fold it into the W3
verdict, and do not claim it without the test.**

## (Q5) LANE L's NEXT UNIT — **RULED: W4 CLAUSE (b). ALREADY IN FLIGHT; NO CHANGE REQUIRED.**

The user ruled that Lane L should attack **W4 break-clause (b)** — whether a natively finite-energy
blow-up ansatz exists — now that `L2′` closed clause (a) by pinning the far-field decay exponent to
**exactly `α = 1`**. **The Conductor had already planned exactly that as `L5` (wave 5, `1e49a00`),
before the ruling existed.** The ruling **confirms** it and adds nothing. `L3` (§6(ii), persistence)
stays queued.

**Carried into `L5`'s reading, because `L2′` earned it:** clause (a) is closed not because the
literature lacks a technique but because **paying the bill destroys the object it is for** — any
`α > 1` puts `U` in `L³` and yields full regularity. **`L5` must state, in its own pre-registration,
what would distinguish a genuine natively-finite-energy ansatz from the same trap wearing a different
hat.**

---

## §5 — WHAT THIS RULING CHANGES ABOUT THE CEILING, STATED PLAINLY AND NOT OVERCLAIMED

`L2′` is the first result in this programme that constrains **the object** rather than **the search**.
Every prior negative named a realization, a budget or a seed pool. This one says the backward
`λ`-DSS profile's far-field decay **cannot be raised without turning the profile regular.**

**It closes exactly ONE of W4's three break clauses and route 4 is NOT closed** — `L2′` says so
itself and this ruling does not extend it. **But if `L5` returns with clause (b) also shut, the
Tier-2 ceiling stops looking circumstantial and starts looking structural**, and that is a report
the user is owed at that moment rather than at the end of the wave.

**Unchanged by all five items:** the three-tier win condition (**Tier 2 is never a proof**),
pre-committed gates, planted controls, lesson 91, §3d's `UNDER-RESOURCED`, `merge_gate.sh`, the
narrowed outreach hold (**read, do not contact**), and **no output described as movement toward Clay
unless a link of the `L1 → L4` chain actually moved. Ceiling TIER 2. Clay stays ~0.05%.** Nothing
here moved a link; this ruling moved wording and a queue.
