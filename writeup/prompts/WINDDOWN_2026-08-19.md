# WIND-DOWN DIRECTIVE — user, 2026-08-19

Delivered to the CONDUCTOR verbatim as a chat message. Recorded here so the restart document's
provenance is on `main` and the next session can see what ended the run and why.

---

**USER DIRECTIVE — GRACEFUL WIND-DOWN. This is `touch STOP` semantics under §9c, delivered in
chat, and it takes effect at the top of your next cycle.**

**DISPATCH NOTHING NEW.** Not a wave, not a single unit, not a "cheap" one, not a repair. From
this message forward you are an integrator only. If a returning unit reveals an obvious follow-on,
that follow-on goes into `OPTIONS.md` with its price and its re-open condition — it does not go
into a brief.

**LET EVERYTHING IN FLIGHT FINISH.** Do not `TaskStop` anything. `L6-b` (leg 406), `E-FE`
(leg 408) and whatever of wave 8 is still live run to their own pre-committed gates. Your live
list is authoritative, not mine. Gate each one, integrate it, push to `main`, answer §3i's seven
questions for it in the integration commit exactly as if the run were continuing. A wind-down is
not a licence to lower the bar on the last three landings — those are the ones a fresh session
will trust most and check least.

**DO NOT SELF-CHAIN.** §9e's successor trigger is suspended by this directive. Do not schedule a
successor session. If one is already scheduled, cancel it and say so in your final message. The
chain ends here on purpose; a quiet restart three minutes after you exit is a failure of this
directive, not a safety net.

**UPDATE THE AIMS BEFORE YOU STOP.** The standing goal is unchanged and is the reason for
everything below: **a full Clay solve, Fefferman (C)**. Before you write the restart document:

1. **Re-rank the lanes on what is measured NOW**, not on what was measured when they were last
   ranked. Put the ranking in `STATE.md` with one line of reason per lane. If a lane no longer
   earns its rank, demote it and say so — a demotion at wind-down costs nothing and saves the
   next session a wave.
2. **Name the single cheapest unit that could move an `L1 → L4` link**, with its price, and say
   plainly why it is not already dispatched. If the honest answer is "no such unit is known",
   write that sentence. **No `L1 → L4` link has moved in 409 legs** and the restart document must
   say so in its first screen.
3. **Plan wave 9 without dispatching it.** One committed plan under §3g step 1, subject to the
   wave-composition floor — at least one unit attacking a wall on the Clay chain directly. Mark
   it `PLANNED, NOT DISPATCHED`. The next session may re-plan it freely; it starts with a
   considered option rather than a blank page.
4. **Nothing may be declared broken, closed or discharged in the wind-down that was not measured
   by a unit that returned.** §3h binds through the last commit: a ban is superseded by a
   measurement, never by a decision, and tidiness is a decision.

**PUT THE OPEN ESCALATIONS ON THE USER'S DESK, PLAINLY.** They are the things only the user can
resolve and they are currently spread across the record. Collect them into one block at the top
of the final `reports/ORCH_STATE.md` and repeat it in the restart document. Each one gets: the
file, one sentence of what is at stake, and **a question phrased so it can be answered Y or N**.
Known open, at least — verify against your own record and add anything I have missed:

- `ESCALATION_W2_SCOPE_2026-08-18.md` — whether a NONUNIQUENESS theorem meets a wall whose
  statement names SINGULARITY theorems. Reading three would make Lane T's re-open condition (i)
  live.
- `ESCALATION_PUB0C_PUBLISHED_2026-08-18.md` — whether an unrefereed preprint with no journal-ref
  is a "published work" for `PUB_0C` §1.
- `ESCALATION_D_BUNDLING_2026-08-18.md` — statement **(D)** is deferred *with Lane T*, and Lane T
  was demoted for a reason that has nothing to do with (D). **W4's only surviving clause is (c),
  and clause (c) is statement (D).** This is the most direction-relevant open item on the board
  and it has never been ruled. If it is not in your escalation list, add it.
- `T1`'s ban-wording packet and the leg-257 lift-clause defect — recorded, not ruled.

**WRITE THE RESTART DOCUMENT.** This is the deliverable the user asked for by name. There is at
present **no CONDUCTOR start document in the repository** — `CONTINUATION_PROMPT.md` (25,324 B)
and `ORCHESTRATOR_PROMPT.md` (26,338 B) both describe the retired four-slot orchestrator/DM
contract, and a fresh session pasted either one would start the wrong machine.

- **Rewrite `CONTINUATION_PROMPT.md` as the CONDUCTOR restart document.** That is the filename
  the user will reach for.
- **Retire the existing text verbatim** to `writeup/prompts/CONTINUATION_PROMPT_FOURSLOT_2026-08-12.md`
  first, in its own commit, with a one-line header saying what it was and that §§2–5 remain
  available. Nothing is deleted.
- **It is pure instruction.** Second person, imperative. No sentence about the document itself, no
  "as discussed", no framing, no summary of its own structure. A fresh session must be able to
  paste it and start working without knowing this conversation happened.
- **It must be self-sufficient**: a session that reads only that file and the files it names can
  plan and dispatch a wave correctly. It must NOT duplicate `STATE.md` or `reports/ORCH_STATE.md`
  — it names them as the read surface and says which to read first and in what order.
- **Cap it at 16 KB.** If it does not fit, the fault is duplication, not the cap.
- **Numbers are cited by JSON field, never restated** (§3j). A restart document that restates
  measurements is a second source of truth and will go stale silently.

It must carry, at minimum:

1. **Mode and contract**: CONDUCTOR, `ORCHESTRATION.md` §3g, with §3h (the wall mandate), §3i (the
   direction check, seven questions, per unit, in the integration commit), §3j (headroom caps and
   retirement), §3k (the literature obligation), §3d (under-resourced is not a null).
2. **Goal, posture, ceiling, odds** — Clay solve, (C); the walls are the work; Tier 2 ceiling; and
   the `L1 → L4` sentence above.
3. **The standing bans and holds, in force**: C1 exemplar-free with its two-part naming
   requirement; A2; B1; and the narrowed outreach hold — *reading any published document is
   authorised; contacting an author, group, maintainer or list remains HELD*.
4. **The lane ranking as of the stop**, with the re-open conditions for anything deferred, and a
   pointer to `OPTIONS.md` for everything parked.
5. **State of play**: what landed, what was in flight when the run stopped and where it got to,
   what was abandoned — **branches, not handles** (§9d: subagent handles do not survive the
   session that spawned them).
6. **What is owed**: statement (D)'s conditions (8) and (9) still unread; leg 390's `check_A`
   re-run; `CORRECTIONS.md` §45 (evidence scripts that reference nothing outside their own unit's
   artefact); and anything else your record shows outstanding.
7. **The open escalations block**, as above.
8. **The wave-composition floor** and the ORCH_STATE-at-every-wave-boundary rule.
9. **The stop mechanism** — §9c's three files — so the next session can be stopped the same way.
10. **The first three things to do, in order**, ending with wave 9's planned-not-dispatched plan.

**THEN CLOSE OUT, in this order:**

- Final `reports/ORCH_STATE.md` per §9d. The **`## Environment notes`, `## Known flakes` and
  `## Incidents and root causes` sections are carried forward verbatim** — a wind-down that
  rewrites the file from scratch destroys the run's institutional memory. The live block says
  exactly where each in-flight unit stopped, its branch, and how far it got. Mark it
  `RUN STOPPED BY USER DIRECTIVE 2026-08-19 — NO SUCCESSOR SCHEDULED`.
- Refresh `PROGRESS.md` and write the run report per §9c.
- Regenerate `STATE.md` with the new ranking, under §3j's caps.
- Run `scripts/merge_gate.sh origin/main` and confirm it prints `MERGE GATE: PASS`.
- Push everything to `main`. Confirm `git status` is clean and nothing is unpushed.

**YOUR FINAL MESSAGE TO THE USER** is short and contains exactly four things: (1) the one-line
restart instruction — which file to paste; (2) what was still in flight and what happened to it;
(3) the escalations awaiting a Y/N; (4) one honest sentence on whether the run moved the Clay
chain. No summary of the run, no thanks, no framing.

**One last thing, and it matters more than the tidiness.** Do not use the wind-down to make the
board look finished. If a unit came back `UNDER-RESOURCED`, it stays `UNDER-RESOURCED`. If a wall
stands, it stands. If the honest answer to "did any of this move Clay" is no, write no. The
restart document's value is entirely in being true.
