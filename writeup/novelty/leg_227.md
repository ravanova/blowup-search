# Leg 227 — Route-EGMT — novelty / prior-art pass

**Committed BEFORE the one-field edit to `solver/literature_gates.py`.**

This leg changes no mathematics and cites no new paper. It re-tenses one prose field in a
shared ledger so that it stops asserting, in the present indicative, that five files in this
repository are wrong when they were repaired thirteen legs ago. The novelty question for a
leg like this is not "has anyone published this" — it is **"has any leg already fixed this,
and is the fix I am about to make the one that was prescribed?"**

## 1. The literature is not reopened, and why that is the right call

The underlying citation — Elgindi–Ghoul–Masmoudi, **arXiv:1906.05811**, *Stable self-similar
blowup for a family of nonlocal transport equations*, Prop. 2.1 — has been read at primary
source **three times** in this repository:

| leg | what it did with 1906.05811 | outcome |
|---|---|---|
| 141 | first full-text read; banked the `-1/2` gap at `a = 0`, `γ = 4` | banked, in leg artifacts only |
| 165 | re-used it against leg 111's admissible window | banked the `p = 2`, width-2.0 join |
| 190 | independent re-read from **both** the PDF and the arXiv LaTeX e-print | found the bracket sign-slope error; appended `EGM_PRIMARY_READ` to the shared ledger |
| 214 | fetched `OSWModel_Final.tex` **itself** (48494 bytes, line 356) rather than trusting leg 190 | corrected the five prose sites |

The mathematical content is therefore settled by two independent primary-source fetches (legs
190 and 214) that agree: the bracket is `-(1/2 - C|a|)` and the gap **degrades** with `|a|`;
the two readings coincide exactly at `a = 0` (both `-0.5`), which is the only value this
repository has ever evaluated. **This leg re-reads nothing and re-derives nothing**, because
nothing it touches is a number, a verdict or a citation. Re-fetching the e-print a third time
would be theatre; the correct discipline for a prose-tense fix is to verify the *file state*
the sentence describes, which §3 does.

## 2. Prior art *inside the repo* — has this already been fixed?

Checked, because a duplicate mechanical fix is the real failure mode available here.

- **Leg 214 (`2c901c4`, Route-EGMB)** fixed the five prose sites and stated explicitly that it
  left `solver/literature_gates.py` alone: *"leg 190's own row there is already correct."*
  That judgement is right about the row's **mathematics** and too coarse about its
  **bibliography** — the row's second sentence is a claim about the live tree, and leg 214's
  own commit is what falsified it. So the ledger field was never in leg 214's scope, and is
  still unfixed as of this leg.
- **Leg 213 (`bafe9ac`, Route-LGC2)** found the staleness in its post-append ledger audit,
  answered its gate **NO** on exactly this, and **deliberately did not reconcile it** —
  `literature_gates.py` was read-only to it under both branches. It recorded the repair as
  `suggested_repair_not_applied` in `writeup/data/p2_route_lgc2_v1_ledgeraudit.json` and
  handed it on. Its prescribed wording, quoted from its journal:

  > re-tense to *"…rendered the bracket as `(-1/2 - C|a|)` until leg 214 (`2c901c4`)
  > corrected all five"*, leaving every other word intact.

  This leg implements exactly that, and adds only the two provenance pointers (leg 213 found
  it, leg 227 re-tensed it) that let a later reader reconstruct the chain without this file.
- **Grep of the whole tree** for `sign_correction_leg_190` and for the phrase `render the
  bracket` outside `literature_gates.py`: **0 hits**. No test, runner or ledger asserts on this
  string, so no automated check pins the old wording and none is being evaded.

**Verdict: not duplicated, and not novel research.** It is the ledger-owner's half of a
two-part repair whose first half landed at leg 214 and whose diagnosis landed at leg 213.

## 3. What made the fix safe to make — the claim was checked, not assumed

The new sentence asserts a fact about five lines in this repository. That fact was verified
against the live worktree before the edit was committed, not inherited from leg 213's audit:

| site | renders today | new sentence holds? |
|---|---|---|
| `experiments/journal/leg_141.md:30` | `−(1/2 − C\|a\|)` | yes |
| `experiments/journal/leg_141.md:117` | `−(1/2 − C\|a\|)` | yes |
| `writeup/novelty/leg_141.md:233` | `−(1/2 − C\|a\|)` | yes |
| `experiments/journal/leg_165.md:120` | `−(1/2 − C\|a\|)` | yes |
| `experiments/journal/leg_165.md:128` | `−(1/2 − C\|a\|)` | yes |

**5/5 corrected.** Leg 213 measured the same five at 0/5 holding the *old* claim; this leg
re-measures them at 5/5 holding the *new* one, from a different worktree and a later commit.

## 4. What does not move

**0 banked numbers. 0 `CLAIM_LEDGER` verdicts. 0 bans lifted. 0 routes advanced. No figure
owed.** `novelty_verdict()` is untouched. EGM's `-1/2` at `a = 0`, `γ = 4` stands exactly as
legs 141, 165 and 190 banked it. The only thing that changes is which tense a reader of the
shared ledger encounters, and therefore whether they are sent to five correct files and told
they are wrong.
