# Novelty pass — leg 410, unit `PB2` (`ORCHESTRATION.md` §3k)

**What this unit builds: NOTHING.** `experiments/p2_route_pb2_v1.py` imports
`experiments/p2_route_l5_v1.py` **unchanged** and evaluates integrals of the field that module
already defines. §3k rule 3 ("before building, establish whether it is already built") is
discharged by there being no build: the only machinery is Gauss–Legendre quadrature on `S²` and
the trapezoid rule in `x = log r`, both textbook, neither novel, neither load-bearing on a
citation — every number is reproducible from the banked JSON by anyone with a quadrature rule.

**What this unit CLAIMS, and whether it is new.** It claims **nothing new about mathematics.**
The mathematics is Tsai 1998's, published, read here at FULL TEXT. What is new **to this
repository** is *which* of Tsai's two theorems carries W4 clause (b)'s `ṁ ≡ 0` exit, and the
measurement that the object meets that theorem's hypotheses. That is a fact about **the record**,
not about the literature.

## Sources touched, with DEPTH (§3k rule 1)

| source | obtained | DEPTH reached by this unit | verification |
|---|---|---|---|
| Tsai, *ARMA* **143** (1998) 29–51 | `Papers/TSAI1998.pdf`, already on disk (author's page, pre-arXiv) | **FULL TEXT** — Thm 1 (p.30), **Thm 2 (p.30)**, the hypothesis list for Thm 2 (§2 p.34), `(1.4)` (p.30–31), `(1.5)` (p.30), the weak-solution definition and smoothness (§2 p.34), **Rmk 5.3 and Rmk 5.4** (p.49), and the `[NRS]` bibliography entry (p.50) | sha256 `6d3182d53806ce82fa0a2d834b31b758f22399ff625b8c8d7025a65f83fb8182`, **matches** the pin held since leg 359; `pdftotext -layout` **1258 lines**, matches |
| Chae–Wolf `arXiv:1610.09464` | `Papers/1610.09464.pdf`, on disk | **NOT RE-READ** — already `FULL TEXT` (row 1, leg 359). Hash and line count **re-confirmed only**, per AMENDMENT 4 | sha256 `1f537bc2…` **matches**; md5 `f1d14db1…` **matches** (same file); `-layout` **1002 lines**, matches; `Pages: 19`, which resolves the `1021` residual as `1002 + 19` form feeds |
| Nečas–Růžička–Šverák, **Acta Math. 176 (1996) 283–294** | **NOT OBTAINED.** Two attempts (Springer → login wall, 247,202 B HTML, `sha256 04165b7d…`; Project Euclid → 1,165 B Incapsula stub, `sha256 d05e2720…`), with a **positive network control** (`arxiv.org/abs/1802.00038`, HTTP 200, 38,114 B) proving the refusal is the publisher's | **`UNREACHABLE`** at primary; hypothesis stays **`SECOND HAND`** via Tsai p.30 at FULL TEXT. Not even `ABSTRACT` is served — the landing page's `description` meta tags are empty | 4th independent reproduction (legs 253, 359, 364, 410) |
| Seregin `arXiv:math/0510396`, ESŠ 2003 | **NOT TOUCHED** — out of the re-scope; rows 4/5 unchanged | — | — |

**No source's depth was downgraded and none was silently promoted.** The one depth **change** in
the record from this leg is not a depth at all: it is the finding that `SOURCES.md` row 3's
`SECOND HAND` debt, while still real, is **not load-bearing for W4 clause (b)** — because the
theorem that carries clause (b) is Tsai's, at FULL TEXT, and NRŠ's `L³` hypothesis is measurably
**not satisfied** by the object.

## Prior art inside this repository, checked before claiming anything

- **leg 364 (`NRSV`)** — banked `(ii) DISCREPANCY-FOUND` on `solver/dssp_screen.py`'s
  `deciding_clause`. **Confirmed and narrowed here, not overturned.** Its verdict that the defect
  is *"a citation/attribution discrepancy rather than a computational or logical error"* stands.
- **leg 359** — first primary read of Tsai here; pinned the same sha256. Its own JSON already
  noted the `q = 3` vs `q > 3` split in passing.
- **leg 253** (`leg/253-nrsx-v1`, parked, never merged) — the hypothesis ladder with NRŠ and Tsai
  Thm 1 as **separate rows at different `q`**. Not read again this leg; leg 364 quotes it.
- **`L5` (leg 400)** — the object, the `ṁ ≡ 0` exit, and the joint NRŠ/Tsai citation this leg
  disambiguates. Its artefact is **read-only and unedited**.
- **`V-W4`** — four citation defects in this same neighbourhood (`WALLS_HISTORY.md` §W4-PROV2).
  The NRŠ journal/volume/page defect found here is a **fifth in the same file**, which is why it
  is banked as a pattern rather than shrugged off as a typo.

**Nothing here is claimed as new against the outside literature, so no external novelty search was
required or run.** **No author, group, maintainer or list was contacted; no paywall was
circumvented.**
