# Leg 263 — Route-WESC novelty pass

**Date:** 2026-08-07. **Agent:** LEG-C. **Branch:** `leg/263-wesc-v1`.
**Verdict: `NO MATHEMATICAL CLAIM IS AVAILABLE TO THIS LEG.`**

## What kind of leg this is

A **correction leg**, executing the user's Ruling 2 on leg 178 verbatim. It computes nothing,
imports no `solver/` module, runs no experiment, produces no JSON, produces no figure ("no
measurement, no figure" — legs 141/154/165/171/181's precedent), and touches no shared ledger.
Every magnitude it repeats is **read from a committed artifact** and re-stated with its source;
not one is re-derived.

**New to the world: nothing.** New to this repository: nothing either — the mathematical
content of the correction is leg 178's, landed at `ba1a22c`, and the classification it confirms
is leg 165's, landed at `84a5c7e`. This leg's product is **placement and pinning**: making a
correction visible that had been living in a `capabilities.py` `validated` string, and
resolving, per occurrence, which of two same-worded sets each "three realizations" sentence
means.

For that reason a literature pass in the usual sense (queries, links, located fragments) is not
the instrument here and is not faked. The corresponding due-diligence is the **occurrence
census** below, which is the thing that can actually come out wrong.

## The one thing that could go wrong, named before any edit

The user's Ruling 2 names the failure mode itself:

> *"Do not globally search-and-replace the phrase, and do not let a correction to journal prose
> silently weaken a live ban whose justification rests on a different set."*

`plan_of_record.py`'s re-posed stage-`V` ban rests on the **plan-triple** — `ℓ¹_w` (leg 54),
collocation (leg 56), origin-`H²` (legs 163/176/182). Leg 178's revival is on the
**weighted-energy** realization, which is **not** a member of that set. A `sed` over the phrase
would have taken the ban down with the journals. **Gate clause (c) is the tripwire:
`plan_of_record.py` must show a zero diff.** This leg does not open the file for writing at any
point.

## Pre-committed method

1. Repo-wide grep for the phrase family (`three realizations`, `third realization`,
   `measured-dead`, `all three`), **not** limited to the four files the DM named — the ruling's
   "occurrence by occurrence" is over the repository, and an occurrence outside territory still
   has to be *pinned*, even where it may not be *edited*.
2. For each hit, pin the triple **from local context first**, and where local context is silent,
   from a **datable** fact rather than an assumption: the plan-triple's wording entered
   `plan_of_record.py` at commit `4ff544a`; any artifact that landed before `4ff544a` and uses
   the phrase without local enumeration cannot be naming a set that did not yet exist.
3. Edit only occurrences that are (a) journal-triple, (b) reached by leg 178's revival, and
   (c) inside the declared territory. Occurrences failing (c) but passing (a) and (b) are
   **recorded, not edited**, and handed forward — a diff outside territory fails the merge gate,
   and quietly widening a territory is the same class of error as quietly widening a ban.
4. Mark, never hide: original wording stays standing; corrections are blockquoted blocks beside
   it (precedent: `TECHNICAL_P2_ROUTETN_V1.md` §39, banked lesson 35).
5. If any occurrence resists pinning at full context, **stop and escalate** — do not guess.

## Pre-registered prediction (recorded before the census was classified)

That the three journals the ruling names (141, 165, 183) all pin to the journal-triple, and that
at least one of them contains a *local* enumeration making the pin independent of the ruling.
**Held:** leg 141 enumerates it in its own ceiling paragraph (*"the coefficient-basis,
collocation and weighted-energy realizations alike"*), and leg 165 enumerates it in its own body
(*"the collocation basis (56) and the weighted-energy form (111/141)"*, beyond leg 127's `ℓ¹_w`).
Leg 183 carries no local enumeration and is pinned by the dating argument plus the fact that its
own body treats origin-`H²` as the realization where the operator is **alive**.

## Ceiling, pre-committed

Nothing in this leg lifts a ban, promotes a route, opens a lane, or moves a link of the
`L1 → L4` chain. **"Not dead" is not "open"** — the user's own bounding language at leg 178,
carried into every correction block this leg writes. Every magnitude cited is on the `a = 0` CLM
linearisation, whose `Y₀` is exactly zero for the banned degenerate reason; nothing is claimed
about `HL_S2_nonsymmetric`. Clause S7 unchanged. Clay `~0.05%`.
