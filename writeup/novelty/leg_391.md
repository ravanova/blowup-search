# Leg 391 novelty pass — T1, the ban-wording escalation packet

Committed alongside the journal, per standing practice.

## 0. Why this pass is INTERNAL and has no arXiv battery

`ORCHESTRATION.md` §3c requires a novelty pass **before construction**. **This leg constructs
nothing** — no solver, no module, no runner, no data, no figure, no measurement. Its entire output
is a §8 escalation packet that states two readings of three ban-wording questions and rules none of
them.

There is therefore no external claim to screen for prior art: the packet asserts no result about the
world. The standing **no-external-outreach hold** also bars the arXiv/Semantic-Scholar battery in any
case, and that hold is itself item (d) of the packet.

**What is screened instead, and what it is worth.** The one novelty question this unit genuinely
has is *internal*: **has any of these three questions already been asked and answered inside this
repository, such that the escalation is redundant?** That is answerable by grep over landed files
alone, with no network, and it is answered below. Its instrument is `git`/`grep` over the checked-out
worktree at `origin/main` + this branch — **not** the repository's standing arXiv-API novelty
instrument, and it is not a substitute for one. Disclosed rather than silently swapped.

## 1. Instrument, and its control

- **Instrument:** `git log -S<string> -- <path>`, `git show <sha> -- <path>`, and
  `grep -rn --include=*.md` over the worktree.
- **Positive control:** `grep -rn "needs L1 first" --include=*.md .`, excluding this leg's own two
  new files, returns **15 hits across 8 files** (`WALLS.md`, `experiments/JOURNAL.md`,
  `CONTINUATION_PROMPT.md`, `ORCHESTRATION.md`, `DIRECTION.md`, `CLAY_ROADMAP.md`, `STATE.md`,
  `writeup/novelty/leg_316.md`). Instrument alive on a phrase known to exist.
- **Negative control:** `git log -S "which needs L1 first" -- plan_of_record.py` returns **exactly
  one** commit, `561bb68`, and specifically **does not** return the 2026-08-06 ban-review commit
  `4ff544a`. The instrument can return a *narrow* answer, not just a wide one — which is the
  direction that mattered here, since the load-bearing finding is an **absence** of an edit.
- **Both controls fire in the direction that could have refuted the finding.** Had `4ff544a`
  appeared in the `-S` output, item (b)'s factual answer would have been the opposite one.

## 2. Result — none of the three questions is redundant, and one is closer than it looks

| question | already asked? | already answered? | evidence |
|---|---|---|---|
| **(a) Cadiot** | **YES** — raised and escalated 2026-08-11 by the annotation landed with leg 304 (`b319449`), which states the literal-vs-purposive conflict in terms. | **NO.** The annotation's own closing words: *"ESCALATED TO THE USER 2026-08-11 and pending. Until the user rules, the ban stands in force, unchanged."* | `plan_of_record.py` output; `experiments/journal/leg_304.md` |
| **(b) stage V's "needs L1 first"** | **YES, twice** — by the 2026-08-06 ban review, and again by `WALLS.md`/`STATE.md` on 2026-08-13. | **AMBIGUOUS, and that ambiguity is this leg's finding.** The review declared the wording retired but did not edit the entry that carries it. See journal §3. | `git show 4ff544a -- plan_of_record.py`; `git log -S` |
| **(c) the apparatus question** | **YES** — leg 348 §5 raised it precisely. | **NO, and explicitly refused:** *"I make no claim about whether that apparatus distinction matters to the ban's scope; that reading, if it is ever needed, is for the DM/user."* | `experiments/journal/leg_348.md:122-127` |

**So the escalation is not redundant on any of the three.** All three are live, none has a landed
ruling, and (a) and (c) were each explicitly routed to the user by the leg that raised them.

## 3. The one thing this pass found that was NOT already in the record

Item (b)'s ambiguity has never been stated with its artefacts. Prior statements of it are prose —
`WALLS.md` asserts the clause is live; `4ff544a`'s commit message asserts it was re-posed; leg 316
quotes it as live; leg 341 uses the same name for a different entry. **No landed file had checked
the diff.** Four facts, none previously banked:

1. `git log -S "which needs L1 first" -- plan_of_record.py` → **one commit, `561bb68` (2026-08-05)**.
   The V-as-posed entry has never been edited.
2. `4ff544a`'s hunk `@@ -739,7 +824,20 @@ BANNED = [` shows that entry as **unchanged context**.
3. The single line the review **deleted** was the V-rigorous entry, whose lift field was the bare
   string `"L1"` — it **did not contain** the phrase *"needs L1 first"*.
4. Two landed legs use the name *"the stage-V ban"* for **two different entries**: leg 341 (`:3-4`,
   `:113-123`) for the re-posed ℓ¹-Fourier entry, leg 316 (`writeup/novelty/leg_316.md:28-34`) for
   the V-as-posed entry.

This is the repository's own lesson applied to its own ban table: **a claim derived from prose is not
a measurement.** `WALLS.md` §R0 records the same lesson being learned twice in one paragraph one day
after being written; item (b) is the third instance, in a different file.

## 4. Ceiling and limits (lesson 91)

- **Realization named:** grep and `git log -S` over the Markdown and Python files present in this
  worktree at `a384d92` plus this branch. Not a semantic search; a phrase that was paraphrased rather
  than quoted would be missed.
- **Coverage gap, named:** **`DIRECTION.md` was not read.** Grep locates text bearing on item (b) at
  lines **755, 2262, 2275, 9798, 9997**. §3e and this unit's dispatch forbid reading that file, so
  those five entries are **unread artefacts**, named in the packet as such. A ruling on (b) should
  check them; item (b) as stated does not depend on them, but it is not informed by them either.
- **No external literature was screened at all**, and no claim in the packet requires it. If the user
  rules (c) toward C1, a scoping leg for the Galerkin-plus-tail apparatus **would** need a real
  external novelty pass — leg 348's own §6 already flags that its seven links were read at abstract
  level only, and that seven links is not exhaustive.
- **No control was run on the "has this been asked" question in the direction of over-return.** A
  question asked in wording sharing no phrase with mine would read as absent. The three questions were
  located by name from the dispatch, so this is a limit on *completeness* of the escalation's
  contents, not on the three items it carries.

## 5. Clay

No link of the `L1 → L4` chain moved. **Tier 2 ceiling. Clay odds ~0.05%, unmoved.** A novelty pass
over this repository's own ban table is bookkeeping about what to ask, never movement toward Clay.
