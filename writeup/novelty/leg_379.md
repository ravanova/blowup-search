# Leg 379 — ROUTE-LCB7 — novelty / prior-art pass

**Role: test-hygiene correction only. No new solver physics, no new measurement, no gate
answer touched. Both items were source-verified at entry (a pre-existing, currently-failing
`check_*` in each target file, confirmed by running it before any edit).**

## 0. What was already known (prior art inside the repo)

**(i) Leg 185 (Route-M2SD) and its continuation, leg 284 (Route-NU12).** Leg 185's own journal
(`experiments/journal/leg_185.md`, §D5, "genuine profile reachable?") self-flags its recovered
negative `nu` rows as diagnostic, not physics: *"The negative rows DOWNGRADED TO SIGN-ONLY. No
magnitude in `nu < 0` half [of the] table should [be] quoted [as] a measurement of anything."*
Leg 284's journal (`experiments/journal/leg_284.md`) is an explicit grid-refinement extension of
the same lineage (via leg 210's ladder), landing the same self-flagged sign-only status as "the
permanent honest state" for the scheme. Neither leg is this leg's discovery; both are cited, not
re-derived.

**(ii) The repo's own ULP-drift precedent for library-version FFT/BLAS re-runs.** Leg 147
(Route-NKB, `experiments/journal/leg_147.md`) measured 20 of 22 leaves moving at <= 3 ULP from
"environmental float drift... numpy 2.5.1, Python 3.12.3" on the SAME source re-run twice, with
6 of the 22 sitting on paths that never call the module under test at all — the repo's own
established diagnosis that a handful of ULP is a numpy-version artifact, not a defect. Leg 131
(`experiments/journal/leg_131.md:128`) independently measured "1-2 ULP BLAS reduction-order
drift" on a comparable same-source re-run. Neither leg is about `solver/gclm.py`; both are cited
as the pattern this leg's finding matches, not as direct prior measurements of it.

## 1. What this leg newly measured (small, source-verified)

* `check_banked_record_carries_no_out_of_domain_coefficient` (test_boussinesq_postrepair.py) was
  FAILING at entry (confirmed by running it before any edit): 295 out-of-domain `nu` rows,
  100% attributable by direct scan to exactly two files —
  `writeup/data/p2_route_m2sd_v1_diagnostic.json` (11 rows, leg 185) and
  `writeup/data/p2_route_nu12_v1_converge.json` (284 rows, leg 284). No third file contributes.
* Grepped both files' producing runner scripts
  (`experiments/p2_route_m2sd_v1_diagnostic.py`, `experiments/p2_route_nu12_v1_converge.py`) for
  the symbol `solve_boussinesq`: zero hits in either. Both import only
  `solver.dissipative_profile` and (nu12) `solver.line_hilbert`. This is the fact that licenses
  the exclusion: the flagged `nu` values were never Boussinesq coefficients, so defect 2 (an
  out-of-domain coefficient reaching `solve_boussinesq`) cannot be what produced them.
* `check_banked_stage1_5_t_stars_are_unmoved` (test_gclm_postrepair.py) was FAILING at entry: 2
  of 20 banked T* values fail `float.hex` bit-identity. Measured the drift directly:
  `bump(kappa=2)` at 3 ULP, `bump(kappa=5)` at 12 ULP (`np.spacing`-defined), reproduced
  identically with `OMP_NUM_THREADS`/`OPENBLAS_NUM_THREADS`/`MKL_NUM_THREADS`/
  `NUMEXPR_NUM_THREADS`/`VECLIB_MAXIMUM_THREADS` all pinned to 1 -- ruling out thread-count
  nondeterminism and leaving library-version FFT-kernel drift (`np.fft.rfft`/`irfft`, both
  reached through `clm_analytic_blowup_time`) as the only remaining explanation.

## 2. Disposition

Both are test-file corrections, gated on a demonstrated planted-failure control per the 361
lesson (see `experiments/journal/leg_379.md` for the verbatim run output). No gate answer for
leg 133's (Route-BOB) or leg 103's (Route-GLB) own YES verdict moves. No banked JSON is edited.
No solver file is touched. `plan_of_record.py`/`DIRECTION.md` untouched. Clay odds stay ~0.05%.
