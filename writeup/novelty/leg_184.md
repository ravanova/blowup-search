# Leg 184 — Route-GBW novelty pass (run and committed BEFORE the edit)

**Date:** 2026-08-06. **Branch:** `leg/184-gbw-v1`.
**Verdict: `PROCEED_NARROW` — one clause, one file, no claim of novelty of any kind.**

Links, not counts (lesson from leg 53's withdrawn count-only log). This leg makes no
mathematical claim, so the pass is not a literature pass in the usual sense: the object is
**this repository's own ban wording**, and the only things that can pre-empt it are (a) a prior
leg that already pinned the resolution, (b) a prior leg that already edited this clause, or (c)
a prior leg whose landed gate answer would be retroactively changed by the pinning. All three
are checked below against the git record, not against memory.

---

## 1. What this leg proposes to change, stated before the search

`plan_of_record.py`'s `BANNED` list carries, verbatim as of merge base `2d89233`:

```
("any GA compute on an unvalidated fitness -- C-PILOT's gate ANSWERED NO at leg 49 "
 "(4/6), so this ban did NOT lift when the stage closed",
 "never -- only a re-run of the six-property gate that PASSES on a repaired fitness"),
```

The **lift condition** (the second element) names no resolution. Leg 160 measured that this
makes the condition reachable without any repair at all. This leg edits that string and nothing
else in the file.

## 2. The measured loophole, read from leg 160's own artifacts (not from memory)

Read in full: `experiments/journal/leg_160.md`, `writeup/novelty/leg_160.md`.

Leg 160's frozen configuration — the one leg 49 and leg 59 were scored at — is
**`n = 201` coarse / `401` fine, `wall_model="2d"`, seed 0, `per_gene=9`, `refine=4`**
(`experiments/journal/leg_160.md`, "THE ANSWER: **NO.**" block). At that configuration the
unmodified leg-49 fitness scores **5/6**, with `P3` worst `|slope−1| = 0.3421493449940881`
against the frozen `0.05` floor and `P2 = 0.9750`.

Leg 160's parked finding, verbatim from its journal:

> **The unmodified, unrepaired leg-49 fitness PASSES the frozen six-property gate 6/6 at
> `n = 101/151`** (`P3 = 0.02007528568401651`, `P2 = 1.0000`).

with the mechanism it measured:

```
||rho||_inf = 4.790972900601766e-11      at n = 201   -- ABOVE the grid bottom 1e-11
||rho||_inf = 4.640571829290985e-13      at n = 101   -- BELOW it, by 21x
```

and its own reading:

> "`plan_of_record.py`'s ban lifts on 'a re-run of the six-property gate that PASSES on a
> repaired fitness'. The wording pins neither the resolution nor what 'repaired' means. As
> measured here, the lift condition is reachable **by coarsening the grid**, on a fitness
> nobody repaired. … Recommending whether the ban's wording should pin the resolution is the
> user's call."

Leg 160's control that makes this a mechanism and not an artifact: **WVR-1**, which drops `Y_0`
and is therefore defect-blind by construction, does **not** respond to the coarsening
(`1.0000325577018077 → 1.0000562750220603`, factor `0.99998`), while every defect-tracking mode
improves (identity `17.0×`, hiprec `34.9×`, WVR-2 `3.8×`). A knob that moves every
defect-tracking score and leaves the defect-blind one alone is measuring the defect's floor.

## 3. Pre-emption checks (a), (b), (c)

**(a) Has any prior leg already pinned the resolution in this clause?** No.
`git log -S"only a re-run of the six-property gate" --oneline -- plan_of_record.py` returns
exactly one commit, `9f33c42` ("Route-C-PILOT v0"), i.e. the clause has stood **unmodified since
leg 49 wrote it** and no leg has ever touched its lift condition.

**(b) How many times has `plan_of_record.py` been edited at all, and by whom?**
`git log --oneline -- plan_of_record.py` returns **14** commits, all of them either the
plan's own authoring commits or `Leg 0:` integration/DM commits (`fafae86` NG→DONE/B→NEXT,
`667e07b` integration cycle 1). **No exploration leg has ever edited this file** — which is why
`DIRECTION.md` calls this leg "the only leg in this file's history authorized to touch
`plan_of_record.py` directly, and only for this one clause", and why the precedent it cites is
leg 65's single-row `capabilities.py` annotation fix (nearest prior art also named in leg 145's
novelty log, `b8f1828`): a one-row correction to shared infrastructure, landed narrowly and
reported in full rather than folded in quietly.

**(c) Would the pinning retroactively alter any landed leg's gate answer?** No, checked against
each of the three legs that ran this gate:

| leg | scored at | result | effect of the pinning |
|---|---|---|---|
| 49 (C-PILOT v0) | `n = 201/401` | 4/6, ban did NOT lift | none — already the pinned configuration |
| 59 (weight repairs v2) | `n = 201/401` | 5/6, P3 `0.3421493449940881` | none — already the pinned configuration |
| 160 (WVR v1) | `n = 201/401` (gate read off this alone) | **NO** | **none — its NO stays NO** |

Leg 160's `n = 101/151` run was explicitly reported-and-refused, never used as a gate answer.
Pinning the resolution therefore ratifies what leg 160 already did; it does not relitigate it.
No landed leg's answer changes, and no ban is lifted, loosened, or satisfied by this leg.

## 4. What this leg is NOT

- It is **not** a lift of the GA ban. The ban stands, in force, after the edit.
- It is **not** a loosening: the edited condition is strictly harder to satisfy than the one it
  replaces (every configuration that satisfied the old text and also the new one is a subset).
- It is **not** a claim of novelty. "Pin the resolution of a convergence-sensitive acceptance
  test" is ordinary numerical hygiene, and this repository already carries it as standing
  **lesson 84** ("a known-answer probe has a WINDOW") and **lesson 86** ("a rigorous bound
  dominated by its own EVALUATION error is a statement about the code"). Leg 160 applied both to
  the probe's lower end. This leg only writes the consequence into the ban that depends on it.
- It runs **no GA compute** and **no solver compute**. Its runner reads text and asserts.

## 5. Verdict

`PROCEED_NARROW`. One clause of `plan_of_record.py`'s `BANNED` list — the GA ban's lift
condition — is edited to pin the resolution. Nothing else in the file is touched, no other ban,
stage or gate is read or written, and the diff is verified to be exactly that one clause before
the branch is pushed.
