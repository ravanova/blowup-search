# Leg 295 — Route-PUB3P: novelty / prior-art pass (committed before any document edit)

**Date** 2026-08-07 · **Role** LEG · **Branch** `leg/295-pub3p-v1`

## Dispatch thesis, as given

Leg 294's own report (`experiments/journal/leg_294.md:97`, and
`writeup/data/p2_route_v11x_v1_consolidated.json:157-159`) names exactly one live document
defect: `writeup/4_p2_lottery/TECHNICAL_P2_PUB3_V1.md:134` "quotes `a_max_machine=1.0` in a
consumer table," superseded by the two-independent-method-confirmed value 0.55 (leg 236's
row-exclusion, leg 226's D2/D3 repair, reconciled by leg 294).

## Site located — and a discrepancy found before any edit

Checked directly, not assumed from the dispatch:

1. **Line 134 today** (`origin/main`, unchanged since before leg 294 ran — confirmed by
   `git log -- writeup/4_p2_lottery/TECHNICAL_P2_PUB3_V1.md`, last touch `0aa9da9` Leg 283,
   which is an ancestor of leg 294's own landing commit `07c1098`) reads:
   `` `c(a = 1.50)` returns **0.20427 / 0.23717 / 0.97282** at `n = 101 / 201 / 301`, all three ``
   — unrelated to `a_max_machine`. The line-134 pointer in leg 294's report is stale/imprecise.
2. **The actual `a_max_machine` site is line 148**, the "Exposure" consumer table:
   `` | **Route-D v11** (`p2_route_d_v11_anchor.py`) — banks `a_max_machine`, `grid_converged_a_max`, `GA_boundary` on a `relres < 1e-8` verdict | **DIRECT AND MATERIAL.** ... | ``
   — this row **names** `a_max_machine` as an exposed banked quantity but does **not** state
   its numeric value anywhere. `grep -n -i "a_max"` over the whole 570-line file returns this
   one line only.
3. **A whole-file, wrap-immune search for the literal pattern `a_max_machine=1.0`** (regex
   `a_max_machine[^a-zA-Z0-9]{0,20}=?\s*1\.0\b`, case-insensitive, run against a
   whitespace-normalized single-line copy of the file so no line-wrap can hide a match) returns
   **zero hits** in `TECHNICAL_P2_PUB3_V1.md`. The literal string `a_max_machine=1.0` exists
   only in leg 294's own `experiments/journal/leg_294.md:97` and `writeup/novelty/leg_294.md`
   — i.e., leg 294 paraphrased the table row as if it asserted the stale value, but the row
   itself never prints a number for `a_max_machine`. This is leg 294's own citation being loose,
   not a second stale site.

## Plan, pre-committed before the edit

Because the row already grades `a_max_machine` **DIRECT AND MATERIAL** (i.e., it does not assert
the stale 1.0 as settled truth — it already flags exposure), there is no literal digit to swap.
The fix that actually satisfies the gate's intent — "does the site state 0.55 with a dated note
citing legs 236/226/294" — is **additive**: append one dated footnote sentence immediately after
the exposure table (after the existing "second consumer" sentence, before the next section),
stating the resolved value and citing legs 236/226/294, with **zero** change to the wording of
any existing sentence (table cells included). This keeps the fix single-site, non-argument-
touching, and closes the loop the dispatch actually wants closed.

**Sweep plan (mandatory before banking):** case-insensitive, wrap-immune (whitespace-normalized
whole-file), scan of every `writeup/**/*.md` and `experiments/**/*.md` for
`a_max_machine` co-located with `1.0` (the stale value) or `= *1\.0` patterns, to confirm no
sibling prose document repeats the same stale assertion. Already run once during this pass (see
above) — will be re-run after the edit lands, restricted to prose files (never the banked JSON,
which is leg 297/252's territory, read-only here).

## Territory

`writeup/4_p2_lottery/TECHNICAL_P2_PUB3_V1.md` (site at L148, footnote addition only),
`writeup/CORRECTIONS.md` (append), `writeup/novelty/leg_295.md`, `experiments/journal/leg_295.md`.
Read-only: `experiments/journal/leg_294.md`, `writeup/novelty/leg_294.md`,
`writeup/data/p2_route_v11x_v1_consolidated.json`, and legs 236/226's underlying reports.
