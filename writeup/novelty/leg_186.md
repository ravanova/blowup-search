# Leg 186 — ROUTE-PUB2 novelty pass, run and logged BEFORE the note was drafted

**Branch** `leg/186-pub2-v1`. **Route** PUB2 (exploration/writeup, not critical path).
**Verdict: `PROCEED_AS_SYNTHESIS_ONLY`.**

This leg claims **no mathematical novelty of its own**. Like leg 179 it is a bundling leg:
every mathematical statement in the note it produces belongs to an earlier leg of this
repository or to a published author. The pass therefore has the same two halves leg 179's
had:

1. **Provenance — this is the gate.** Every claim the note will make is read back from *its
   own banked source* (the producing leg's journal, its curated JSON, or the module it
   landed) and the number is *checked*, not remembered. DIRECTION.md §186's gate is a
   provenance gate ("no softening or strengthening"), so this half decides it.
2. **External novelty, carried not re-derived.** Legs 127, 163 and 182 each ran their own
   external-novelty pass. This leg carries those verdicts verbatim and re-runs none of them.
   Where a bundled leg's own pass forbade a claim, this note inherits the prohibition — in
   particular leg 163's standing ban on the sentence *"Xu proves the operator invertible,
   therefore a certificate is possible."*

Links, not counts (leg 53 logged counts, could not be audited, and was withdrawn).

---

## 0. Does this note already exist, and is it distinct from leg 179's?

**It does not exist.** `ls writeup/4_p2_lottery/ | grep -i pub` returns exactly
[`BLOG_P2_PUB1_V1.md`](../4_p2_lottery/BLOG_P2_PUB1_V1.md) and
[`TECHNICAL_P2_PUB1_V1.md`](../4_p2_lottery/TECHNICAL_P2_PUB1_V1.md) — leg 179's pair, and
nothing else. `grep -rln "space axis" --include=*.md .` returns
[`TECHNICAL_P2_ROUTEBX_V1.md`](../4_p2_lottery/TECHNICAL_P2_ROUTEBX_V1.md),
[`experiments/journal/leg_126.md`](../../experiments/journal/leg_126.md),
[`DIRECTION.md`](../../DIRECTION.md) and
[`experiments/journal/leg_182.md`](../../experiments/journal/leg_182.md) — three uses of the
phrase as one axis of stage `B`'s enumeration, plus leg 182's own report. **None is a note
about where a certificate for this operator can live.**

**Distinctness from leg 179's PUB1, stated precisely, because "two documents" is this leg's
gate-yes obligation.** PUB1 is organised as *four negative results from a CAP attempt that
failed* — an exponent-sum conservation law, a discrete-ball soundness trap, the `A₂₁`
inequality, and a closure audit — i.e. **why the method failed in `ℓ¹_w`**. Its §6.4 is a
**four-sentence currency addendum** noting that origin-`H²` was scoped and parked, with no
number from leg 163 in it and no mention of leg 182 at all (leg 182 landed after PUB1 was
drafted). PUB2's question is the complement: **where a certificate for this operator can and
cannot live**, with leg 127 as one endpoint, leg 163 as the other, and leg 182 as the
statement that the segment between them is empty. The only genuine overlap is leg 127's
`σ_min` ladder, which appears in PUB1 §3.1 as *the proof of a theorem* and in PUB2 as *the
measurement of one endpoint of a space axis*. **Two documents, not one, and the overlap is
one table used for two different purposes.**

---

## 1. Has leg 176 landed?

**NO.** Checked three ways, and the answer is load-bearing because DIRECTION.md §186 makes
folding 176 in *conditional* on its landing:

- `git log --oneline origin/main | grep "Leg 176:"` — **no hits**. Nothing from leg 176 is on
  `main`.
- `git diff --name-only origin/main...leg/176-h2c-v1` — exactly one path,
  `writeup/novelty/leg_176.md`. **No runner, no curated JSON, no journal, no gate answer.**
- The branch's only commit is `d7b4624`, *"Leg 176: LEG — Route-H2C novelty pass, committed
  BEFORE construction"* (2026-08-06 15:29). Its own text says it was written *"before a line
  of `solver/origin_h2_certificate.py` existed"*, and that file does not exist on any branch.

**Consequence for the note: leg 176 is reported as IN FLIGHT with no outcome, and nothing is
attributed to it.** What *is* quotable from it is only what its committed novelty log
forecloses in its own words, which is a statement about the leg's scope and not about any
result. Those two foreclosures are recorded in row (iv) below and appear in the note labelled
as leg 176's own pre-registration, never as a finding.

---

## 2. Provenance audit — every number the note will quote, read back from its own source

`reproduces` means: the value was read out of the named file **during this pass**, with the
key path given, not transcribed from prose about it.

Sources read in full this pass:

- Leg 127 — [`TECHNICAL_P2_ROUTENGX_V1.md`](../4_p2_lottery/TECHNICAL_P2_ROUTENGX_V1.md) (216
  lines) and [`writeup/data/p2_route_ngx_v1_general.json`](../data/p2_route_ngx_v1_general.json).
- Leg 163 — `experiments/journal/leg_163.md` and `writeup/data/p2_route_h2s_v1_scoping.json`,
  read via `git show origin/leg/163-h2s-v1:<path>` (parked branch, **read-only**, never
  edited — same access route leg 182 used).
- Leg 182 — [`experiments/journal/leg_182.md`](../../experiments/journal/leg_182.md) (304
  lines) and [`writeup/data/p2_route_h2i_v1_scoping.json`](../data/p2_route_h2i_v1_scoping.json).
- Leg 176 — `writeup/novelty/leg_176.md` via `git show leg/176-h2c-v1:<path>`.
- Leg 179 — both PUB1 artifacts, to establish distinctness (§0), not as a source of claims.

### (i) Leg 127 — `Z₁ ≥ 1` on `ℓ¹_w`

| claim the note makes | JSON key | value read | reproduces |
|---|---|---|---|
| fitted `σ_min` exponent at `s = 0`, `K = 4` | `NGX2_sigma_min_ladder[1].fitted_exponent_p_in_sigma_M_to_the_minus_p` | 0.9924789728516444 | ✅ |
| at `s = 0.3`, `K = 4` | same list, `s = 0.3` row | 0.6984888103734028 | ✅ |
| at `s = 0.7`, `K = 4` | same list, `s = 0.7` row | 0.32020… (banked 0.3202) | ✅ |
| at `s = 1.0`, `K = 4` | same list | 0.0788 | ✅ |
| at `s = 1.5`, `K = 4` | same list | 0.5000188728336935 | ✅ |
| max deviation from `1 − s` over `s < 1`, all `K` | `NGX2_max_deviation_from_1_minus_s_for_s_below_1` | 0.02186165145571506 | ✅ (note quotes 0.0219) |
| relative `K`-spread of `σ_min` for `s < 1` | `NGX2_K_independence` → 0.002928 / 0.002052 / 0.000915 | max **0.29%** | ✅ |
| `K`-spread at `s = 1.5` | same | 2.1407266870634354 | ✅ (note quotes 2.14) |
| explicit witness vs numerical optimum | `NGX3_max_ratio_over_optimum` | 1.0000000000044844 | ✅ |
| cosine with numerical optimum | `NGX3_min_cosine_with_optimum` | 0.9999999999999998 | ✅ |
| rows carrying the residual of `Lv` | `NGX3_max_rows_carrying_residual` | 1 | ✅ |
| `z_K` | `NGX3_max_abs_z_K` | 0.0 | ✅ |
| (T1) attained at `A = L⁻¹`, max slack | `NGX1_max_slack_at_the_exact_inverse` | 1.2366440968740889e−08 | ✅ |
| leg 54 battery coverage | `NGX6_rows_checked` / `NGX6_all_hold` / `NGX6_min_slack` | 196 / true / 7.734300317748637e−10 | ✅ |
| `μ > 0` positive control saturates | `NGX5_max_abs_exponent_for_mu_positive` vs `NGX5_exponent_at_mu_0` | 0.0026247852520269194 vs 0.698488810373405 | ✅ |
| bordering changes nothing at `μ = 0` | `NGX5_border_makes_no_difference_at_mu_0.max_relative_difference_at_mu_0` | 5.726114535073715e−15 | ✅ (note quotes 5.7e−15) |
| the same arms differ at `μ = 0.1` | same block, `same_quantities_at_mu_0p1.relative_difference` | 0.06130334906099269 | ✅ (note quotes 6.1e−02) |
| truncation-artifact audit | `NGX4_max_degradation_factor` | 1.3542912892527808 | ✅ |
| `‖A‖_w` floor growth per doubling | `NGX6_floor_growth_per_doubling_of_M` | flat 1.9896007725421134, algebraic 1.6228040524820584 | ✅ (note quotes 1.99× / 1.62×) |

**Two prohibitions carried from leg 127's own text, verbatim in force:** (a) **(T1) is
folklore and is NOT claimed** — the note says so where it states it; (b) *"No sentence in this
leg says the operator 'has no bounded approximate inverse'"* — no sentence in this note does
either.

**One superseded phrasing, checked so it cannot creep back in.** Leg 127's *corrected* reading
is that the singular sequence's far-field-amplitude component `z[K+1]` is **not** zero — it is
**6.5–6.8 % of `‖v‖₁` and growing with `M`** — and that bordering fails because the coupling
column `C[:,K+1]` is supported on a **single row**, the truncation edge. The earlier "exactly
zero far-field component" phrasing is superseded. The note uses the corrected mechanism only.
(Leg 163's journal records the same correction being applied to itself mid-leg; leg 163's JSON
`G4…ell1w_contrast.leg_127_bordering_effect` carries the corrected wording, read this pass.)

### (ii) Leg 163 — origin-`H²` structurally viable, capped at `a = 0`

| claim the note makes | JSON key | value read | reproduces |
|---|---|---|---|
| G1: Mellin symbol norm is **exact** `1/α` | `gates.G1_mellin_exact_norm.max_rel_err` | **0.0** | ✅ |
| G1 lower witness fraction of the bound, over `α = 0.05…1.5` | `G1…rows[*].witness_fraction_of_bound` | 0.9980059800697489 … 0.9999333399992594 | ✅ (note quotes 99.80–99.99 %) |
| G2: worst relative residual of the resolvent identity | `gates.G2_resolvent_identity.worst_rel_residual` | 2.8072372914100434e−14 | ✅ (note quotes 2.8e−14) |
| G2 rows | `G2…rows` | **18** (datum, `z`) combinations | ✅ |
| G2 resolution ladder, hardest case | `G2…resolution_ladder[0]` | 1.7748e−12 → 6.3508e−14 → 5.5437e−15 | ✅ |
| G3: two symmetry-mode residuals | `gates.G3_symmetry_modes` | 1.790180836524724e−15, 5.266250202865052e−16 | ✅ |
| G4: bordered-solve worst residual | `G4…worst_rel_residual` | 2.2201198984667287e−14 | ✅ |
| G4: `‖u‖_X/‖f‖_X` over three data | `G4…rows[*].ratio_uX_over_fX` | 1.39927667753796 / 1.2679622479822303 / 1.3769479925630155 | ✅ |
| G4: implied `σ_min` lower witness | `G4…implied_sigma_min_lower_witness` | **0.7146549471256172** | ⚠️ see below |
| G4: resolution-ladder spread | `G4…resolution_ladder_spread` | 0.000144433514507325 | ✅ (note quotes 1.444e−04) |
| G5: `α^{−3/2}` at the certificate point | `G5…rows[0].alpha_pow_minus_3_2` | 2.8284271247461903 | ✅ |
| G5: `c_G` | `G5…c_G_value` | 6.41227766016838 | ✅ (note quotes 6.4123) |
| border rank per Hardy block / on the odd real space | `G4…border_rank_per_hardy_block` / `…on_odd_real_space` | 1 / 2 | ✅ |
| `ℓ(f)` after projection | `G4…rows[*].ell_after_projection_abs` | 0.0 in all three | ✅ |
| the cancellation repair | `G2…cancellation_repair` | 1000077961.6959437 → 1.4182983223100105e−14, factor 7.051252518349574e+22 | ✅ |

> **⚠️ One rounding flagged rather than inherited.** Leg 163's journal states the lower witness
> as *"`σ_min ≥ 0.7147`"*. The JSON value is **0.7146549471256172**, which is *less* than
> 0.7147, so the journal's rounded form is very slightly **stronger** than its own data
> (by 4.5e−05). This is a rounding artifact, not a disputed result, and it does not touch any
> conclusion — but DIRECTION.md §186 forbids strengthening, so **the note quotes
> `σ_min ≥ 0.71465`** and records the journal's 0.7147 as a rounding. Escalating a 4.5e−05
> rounding would be disproportionate; stating it here and quoting the weaker true number is
> the discipline the gate actually asks for.

**Leg 163's ceiling, carried at full strength and not softened.** The obstruction census is
read from `obstruction_census` in the JSON and reproduced in the note as five items with their
own severity fields intact:

- **O1** `α^{−3/2}` blow-up — *"NOT the ell^1_w class"*, severity for a static certificate
  **NONE**.
- **O2** non-normality — severity for a static certificate **NONE**, `severity_downstream`
  **HIGH**. Xu's abstract, quoted through leg 163: *"we keep the two separate, since `L₀` is
  non-normal and a spectral gap does not by itself give a decay rate in the `X` norm."* An
  origin-`H²` certificate would certify **invertibility**, not **nonlinear stability**.
- **O3** `a = 0` exactness — `severity_for_transfer` **"FATAL"**, verbatim from the JSON. Its
  corollary, also verbatim: *"a certificate at `a = 0` in origin-`H²` would certify an object
  Xu already inverts IN CLOSED FORM."*
- **O4** realization coupling for `a > 0` — Xu §4.6: *"One cannot use the `H²` metric to empty
  the strip and the `L²` metric to close the origin channel."*
- **O5** the `ℓ¹_w` class obstruction — *"checked for, and it DOES NOT RECUR."*

**And leg 163's gate is quoted as what it was: a YES that is an ESCALATION, not a landing.**
Its journal's own words — *"This is a genuine yes/no split, not a clean win"* (DM update,
DIRECTION.md l. 1243) and *"Nothing was built. `main` was not touched"* — are carried. The
note does not say leg 163 built a certificate, does not say origin-`H²` gives one, and does
not omit O3.

### (iii) Leg 182 — no interpolating space avoids both obstructions

| claim the note makes | JSON key | value read | reproduces |
|---|---|---|---|
| gate answer | `gate_answer.answer` | **"NO"** | ✅ |
| gates | `gates_passed` / `gates_total` | 5 / 5 | ✅ |
| G1 Aitken limits at `s = 0 / 0.3 / 0.7` | `G1_control.rows[*].aitken_limit` | 1.0000082592823272 / 0.7002249765862092 / 0.30904586721386573 | ✅ |
| G1 max deviation of the Aitken limit from `1 − s` | derived from the same rows | 0.00905 (at `s = 0.7`) | ✅ |
| G1 dissipative negative control | `G1_control.dissipative_negative_control.aitken_limit` | −0.29042456505277525 at `μ = 1` vs +0.7002249765862092 at `μ = 0` | ✅ |
| G2 single-row share of `‖Th‖₁` | `G2_mechanism.single_row_share_of_residual_l1` | 0.9999999999998843 | ✅ |
| G2 rows above 1e−12 relative | `G2_mechanism.rows_above_1e-12_relative` | 1 | ✅ |
| G2 negative control (perturbed direction) | `G2…negative_control_perturbed_direction.rows_above_1e-12_relative` | **4088** | ✅ |
| G2 numerator relative spread across `p` | `G2_mechanism.numerator_relative_spread` | 5.653369903598577e−14 | ✅ |
| G2 denominator relative spread across `p` | `G2_mechanism.denominator_relative_spread` | 1.5707478242623005 | ✅ (note quotes 157 %) |
| G3 exponent table, `p = 1` and `p = 2` rows | `G3_interpolation_sweep.table` | `p=1`: 1.0000 / 0.7002 / 0.3090 / 0.1436 / 0.0836; `p=2`: 1.0000 / 0.7000 / 0.3000 / 0.1000 / 0.0000 | ✅ |
| G3 max deviation from `1 − s` for `s ≤ 0.7` | `max_abs_dev_from_1_minus_s_for_s_le_0.7` | 0.009045867213865688 | ✅ |
| G3 max spread across `p` for `s ≤ 0.7` | `max_spread_across_p_for_s_le_0.7` | 0.009036579560100677 | ✅ |
| **G3 rows where `p > 1` rescues a dead exponent** | `rows_where_p_gt_1_rescues_a_dead_exponent` | **0** | ✅ |
| G4 kernel / cokernel measured exponents | `G4_sigma_collapse.kernel_exponent_measured` / `cokernel_exponent_measured` | −2.00236292896265 / +1.0011766190838682 | ✅ |
| G4 `α` (leg 55's banked value, not re-measured here) | `G4…alpha_target` | 0.3973531116778231 | ✅ |
| G4 scoring | `G4…scoring.quantities_scored` / `…in_the_marginal_band` | 114 / 21 | ✅ |
| G4 worst deviation outside the band | `worst_abs_deviation_measured_vs_sigma_prediction_outside_the_band` | 0.01604850457188592 | ✅ |
| G4 mismatches above 0.02 outside the band | `mismatches_above_0.02_outside_the_band` | **0** | ✅ |
| G4 slowest row's ladder | `slowest_row_convergence_ladder.ladder[*].abs_dev` | 0.029546 → 0.021671 → 0.016049 → 0.012129 | ✅ |
| G4 log signature at `σ = 2` | `log_signature_at_the_crossing.max_over_min_increment_ratio` | 1.0111362497663532 | ✅ (note quotes 1.01114) |
| G5 window width | `G5_window.window_width_where_both_obstructions_vanish` | **0.0** | ✅ |
| G5 margin, identical at every `p` | `G5_window.per_p[*].margin_in_exponent_units` | −0.6026468883221769 at `p = 1, 1.25, 1.5, 1.75, 2, 3, 10, ∞` | ✅ |
| G5 crosscheck against leg 55's measured `p = 1` margin | `G5…leg51_55_p1_crosscheck` | measured −0.6062554687114012, predicted −0.6026468883221769, difference 0.003608580389224292 | ✅ (note quotes 0.6 %) |
| G5 crossing `s` per `p` | `G5…per_p[*].s_at_the_crossing` | 1.0 / 1.2 / 1.3333 / 1.4286 / 1.5 / 1.6667 / 1.9 / 2.0 | ✅ |

**Leg 182's own scope restrictions, carried and not dropped:**

- **MOVE B was not re-measured.** Leg 182's journal states it outright: *"this leg did NOT
  re-read Xu at primary source. Every Xu sentence here is quoted from leg 163's journal, which
  did."* The note repeats the attribution rather than presenting O4 as leg 182's own reading.
- **The Banach-algebra constraint is explicitly NOT the reason for the NO.** `σ > 1` holds
  throughout the region of interest, so it does not bind. Leg 182 recorded this so it could not
  later be mistaken for the reason; the note records it the same way.
- **The marginal band is excluded from scoring on stated instrument grounds** (`|σ −
  threshold| ≤ 0.15`, 21 of 114 quantities), and the note says so wherever it quotes the
  0.01605 figure.
- **Leg 182 does not contradict leg 163.** Its own words: *"Leg 163's origin-`H²` viability
  stands exactly as it stood, including its ceiling."* The note must not read leg 182 as
  weakening leg 163's YES, and does not.
- **Finding zero is a correction to the question, not an answer to it.** The two endpoints are
  not two points on one scale — they differ in **three** independent coordinates (domain:
  periodic circle vs. the line; index: `p = 1` vs. `p = 2`; side condition: algebraic weight
  vs. origin regularity). The note leads with this, as leg 182 did.

### (iv) Leg 176 — in flight, no outcome, nothing attributed

Only two things are quotable, both from leg 176's own committed novelty log, both
pre-registrations rather than findings:

- **What it may claim if it lands:** its discrete realization is method-new, because Xu's own
  numerics use compactified-grid Newton continuation, log-Mellin discretization and FFT grids,
  with *"no Laguerre basis, no matrix representation of `L₀⁺`, and no finite-section/Galerkin
  truncation"*. Its own log says this is *"a claim about the method, not the theorem — the
  result is Xu's."*
- **What it forecloses in advance:** Xu §3.2's three recorded CAP gaps (uniform
  large-imaginary-part bound, trace-ideal membership, quadrature-error bounds in trace norm)
  live in the Evans-determinant strand and *"this leg closes none of the three."* And leg
  163's O3 is *"confirmed rather than weakened"* by that pass.

**Nothing else from leg 176 enters the note, and no outcome is predicted for it.**

---

## 3. External novelty, carried verbatim from the bundled legs

Re-run: none. Carried:

- **Leg 127's pass** established that the trade-off inequality (T1) is **folklore and not
  claimable** (arXiv:1503.06315, arXiv:2505.03091, arXiv:2411.18361), and that
  [arXiv:2607.19762](https://arxiv.org/abs/2607.19762) (Xu, 2026) proves the same operator
  invertible on origin-`H²`, so *any* no-go is a statement about the `ℓ¹_w` realization only.
  Its later addendum adds Breden–Payan–Reisch–Tang
  [arXiv:2504.05066](https://arxiv.org/abs/2504.05066) Thm 2.6 as the nearest near-miss, whose
  compact-resolvent hypothesis leg 157 measured **failing** on this operator.
- **Leg 163's pass** established that no published certificate exists for this operator in any
  Hilbert realization, that Xu's abstract separates his spectral gap from any `X`-norm bound,
  and that [Cadiot–Lessard arXiv:2302.12877](https://arxiv.org/abs/2302.12877) is the nearest
  existing CAP machinery.
- **Leg 182's pass** pre-registered two constraints before its analysis: `ℓ^p` is not a Banach
  algebra for `p > 1`, and Xu §4.6's sentence as the *named candidate obstruction* for check
  (b). Both are reported as pre-registered, which is why its NO is not a convenient discovery.
  It also recorded zero in-repo grep hits for `besov` / `fourier-lebesgue` / `lorentz`.

The organising concept the note uses — that an obstruction can belong to the **realization**
rather than to the operator — is **published and named on this exact operator** (Xu, Prop. 2
§3.1) and is *used*, not claimed. This is the same handling PUB1 §6.1 gave it.

---

## 4. What this leg does not claim

- No mathematical result. The arrangement is the contribution, and even the arrangement is the
  shape leg 179 already demonstrated.
- No statement about `HL_S2_nonsymmetric` beyond its banked `α = 0.3973531116778231`.
- No claim that origin-`H²` is a viable route to anything the project needs. O3 says the
  opposite, and the note leads with O3 rather than burying it.
- **No link of the `L1 → L4` chain moved.** No ban lifted, and no ban's lift condition touched.
- Nothing here is interval-enclosed or rigorous: every bundled measurement is float64 at a
  stated truncation, including the one called a theorem, whose *proof* is exact but whose
  *verifying measurements* are floating-point.

## 5. Verdict

`PROCEED_AS_SYNTHESIS_ONLY`. Every bundled claim reproduces from its own banked source. One
rounding in leg 163's journal (0.7147 for 0.7146549…) is flagged and the **weaker** true value
is what the note quotes. Leg 176 has **not** landed and is reported as in flight with no
outcome. The note is drafted after this log is committed.
