# Leg 294 — Route-V11X novelty pass (light, internal-consolidation family)

**Committed BEFORE any synthesis.** Branch `leg/294-v11x-v1`, merge base `2c567ab`
(`origin/main`). Light pass: this leg synthesizes three already-landed/escalated internal
findings; it performs zero new measurement. The prior art that matters is internal — which
leg already measured which number, on which branch, in which environment — and that is what
this pass fixes before a single sentence of the consolidated report is drafted.

## 1. The question this leg is asking

Pre-committed gate (DIRECTION.md, "### 294"), verbatim in the dispatch: does the consolidated
report (a) re-base every leg 236 number against leg 252's regenerated anchor values, stating
the ~6.55e+11x stale-vs-regenerated gap explicitly; (b) carry an explicit provenance tier
(banked-stale / banked-regenerated / fresh-resolve) on every compared value, no exceptions;
(c) state leg 226's confirmation of `a_max_machine` and the opposite-direction
`grid_converged_a_max` behavior as two separately-signed findings; (d) record D1 falsified /
D3 real, correcting the record wherever D1 was cited as mechanism; (e) require movement past
leg 252's own measured noise floor before calling anything "moved"; (f) restate leg 247's
exoneration in the same document; (g) synthesize only, zero re-measurement.

## 2. State of the four inputs, verified directly rather than assumed

None of the four are on `main` as journal files — checked directly, not inferred from the
dispatch's framing:

| leg | branch(es) checked | on `origin/main`? | status as measured on the branch |
|---|---|---|---|
| 236 | `leg/236-rddep-v1-resume` | No (`git show origin/main:experiments/journal/leg_236.md` → fatal, path does not exist) | ESCALATED, branch pushed only |
| 226 | `leg/226-pnr-v1-resume` | No (same check) | ESCALATED, branch pushed only |
| 247 | `leg/247-vbr-v1` | No (same check) | ESCALATED, branch pushed only — **this corrects the dispatch's framing**, which described 247 as "already landed on main." It is not; `git log origin/main --grep="Leg 247:"` returns 0 commits and `origin/main`'s own `writeup/data/p2_route_d_v11_anchor.json` still carries the pre-repair stale `margin` (`1.0467862586e+10`), which would be impossible if 247 had landed. Recorded here because the consolidated report must be accurate about provenance, and getting this wrong would misstate every downstream tier. |
| 252 | `leg/252-vbrg-v1-wip2` (has the journal + regenerated data; `leg/252-vbrg-v1` and `-resume` lack `experiments/journal/leg_252.md` at the commit checked) | No | ESCALATED, branch pushed only |

`origin/main` HEAD at merge base `2c567ab` (DM's own dispatch commit for this leg) confirms
the banked anchor JSON on `main` is unchanged from its original 2026-07-31 state:
`a_max_machine=1.0`, `grid_converged_a_max=0.5`, `v5_budget.margin=1.0467862586e+10` — the
stale, pre-247, pre-252 values. This is the "banked-stale" tier's ground truth.

## 3. Prior art — who owns which number, confirmed by reading, not by title

| owner | branch | what it measured | is it prior art for THIS synthesis? |
|---|---|---|---|
| leg 236 | `leg/236-rddep-v1-resume` | dependency-trace exclusion against the **stale** anchor JSON: `a_max_machine` 1.0→0.55 (3 policies agree), `newton_weighted_defect_max` 640.1x, reproduces leg 247's 62.02x to 5 sig figs from the stale bank. Explicitly flags itself as reading the stale artifact ("leg 247's own reported 62.02373957× is used directly rather than the stale banked value") | Partially owns the exclusion-mechanism measurement; does **not** own re-basing against leg 252, which post-dates it and which 236's own journal could not have read (252 escalated after 236 ran). |
| leg 226 | `leg/226-pnr-v1-resume` | independent adversarial-repair confirmation of `a_max_machine` 1.0→0.55 via D2/D3, in its own environment; separately finds `grid_converged_a_max`'s pre-repair re-run gives **1.0** in that environment (not the banked 0.5) while the repaired verdict restores 0.5; falsifies D1 as a discriminator | Owns the two-method confirmation and the inversion. Does not attempt to reconcile its own environment's pre-repair `1.0` against leg 252's regenerated `0.5` — that reconciliation is unowned before this leg. |
| leg 247 | `leg/247-vbr-v1` | repairs `v5_budget`'s margin selection (worst-case, not min); banked→its-repaired-value-on-stale-rows 1.0467862586e+10 → 1.6122858874e-02 (62.02373957x violation); separately fresh-resolves `a=0.45` (1.532023e-02 vs banked 1.519582e-02, 0.82%) | Owns the repair logic and the banked-row headline. Does not touch the full regeneration — that is 252's. |
| leg 252 | `leg/252-vbrg-v1-wip2` | full regeneration of the anchor JSON with 247's repair ported in; finds regenerated margin 62.53156368658738x, exact float-identical to 247's own fresh re-solve (0.82% gap is banked-row-vs-fresh-row provenance, not a defect — 247 exonerated); finds 202/359 leaves move on regeneration, 46 by >10%, from float non-portability at Newton's basin boundary, NOT solver drift (solver unchanged, single commit); three scalars (`a_max_machine`, `last_machine_precision_a`, `grid_converged_a_max`) unchanged from stale bank | Owns the regenerated ground truth, the portability finding, the noise floor (202/359, 46/359 >10%), and the exoneration. Nothing else in the repository has regenerated this artifact. |

**Conclusion of the pass: the consolidation itself is unowned.** Leg 287's own novelty pass
(`writeup/novelty/leg_287.md` at `43354c5`) explicitly excludes the v11 anchor family from its
environment-portability census, "owned by the 236/226/252 consolidation rework" — an external
confirmation that no other leg has claimed this territory. No leg has re-based 236's exclusion
finding against 252's regenerated values, reconciled 226's two grid-verdict readings against
252's noise floor, or restated 247's exoneration alongside the exclusion/repair findings in one
document. That is this leg's job, and only this leg's.

## 4. One reconciliation this pass surfaces before construction, so it is not read as manufactured later

Leg 226 (its own environment, unrepaired `continuation`) measures `grid_converged_a_max`'s
pre-repair value as **1.0**, disagreeing with both the original bank (0.5) and leg 252's fresh
regeneration in a *different* environment (also 0.5, unrepaired). Leg 252 independently
documents that its own regenerated `v4_grids` rows at exactly `a=0.8` and `a=1.0` — the rows
that decide this scalar — are among the leaves that move across environments
(`grids_reaching_machine_precision` 1→2 at both). This is consistent, not contradictory: three
different environments (2026-07-31 bank, leg 226's machine, leg 252's machine) can each land on
either side of a near-boundary Newton basin decision without any of them being wrong about
their own run. The consolidated report treats this as the clearest illustration of leg 252's
noise floor in action, not as a fourth disagreement requiring escalation. This reading is
recorded here, before the report is drafted, so it is auditable as a pre-committed frame rather
than a convenient one reached while writing.

## 5. Scope discipline

This leg adds no new solver runs, no new Newton solves, no new classifier passes. Every number
in the consolidated report traces to one of the four branches above, cited by branch and file.
Where a number cannot be traced to one of the four inputs, it does not appear.
