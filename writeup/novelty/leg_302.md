# Leg 302 — ROUTE-P2T1: novelty / prior-art pass

**Committed BEFORE a line of `solver/p2_apparatus_term1.py` or the runner existed.**
Order of work: `plan_of_record.py` read in full → `CONTINUATION_PROMPT.md` standing discipline →
leg 285's **landed** spec on `main` (`experiments/journal/leg_285.md`,
`writeup/data/p2_route_p2s_v1_spec.json`) → `capabilities.py` grep → this pass → construction.

---

## 1. Which term, and why it is the cheapest LOAD-BEARING one

The leg builds **exactly one** of leg 285's sixteen records. The ranking is derived from 285's
own banked fields, not from my judgement:

| candidate | 285's `depth_of` | dependents (285's own `deps` edges) | 285's `existing` field |
|---|---|---|---|
| **A15a** ball arithmetic | 0 | A7, A15b | **`solver/interval.py` — the ONE absent term with a real substrate** |
| **A4** γ-law pressure | 0 | A5, A1 (→ transitively all 12 dependents) | `none` |
| **A6** radial geometry | 0 | A2, A3, A9, A12 | `none` |
| **A11** self-similar time `s` | 0 | A10, A12 | `none` |

285's journal §4 calls **A15a** *"the cheapest term in the list"*. **It is the cheapest and it is
not load-bearing**, for a reason 285 states itself: its substrate already exists
(`solver/interval.py`, leg 265's *"one genuine asset"*), and 285 puts it **off the critical path
entirely** (its only consumers are A7 and A15b, and **A15b is one of the two terms that
RESIST specification**). Building A15a would re-deliver a capability the repository already has.

**A4 is therefore the cheapest term that is load-bearing**, and it is load-bearing in 285's most
literal sense: it is the **head of the measured critical path** `A4 → A1 → A2 → A9 → A8 → A13 →
A14` (285's `dependency_graph.critical_path`, depth 7), and its own record says it computes
*"the two constants every other term is a function of"*. Of the four in-degree-0 terms it is the
only one on the critical path. It is also the cheapest of the three substrate-free layer-0 terms
by construction cost: A4 is scalar algebra, whereas A6 needs a spherical differential reduction
and A11 a change of variables with transported derivatives.

**Selected: A4 — adiabatic / γ-law pressure.** No other term is built, touched, or claimed.

## 2. Ban walk — every live ban in `plan_of_record.py`, and why none bites

Ban check is part of this leg's mandate, so it is done term-by-term rather than as a formality.

* **The re-posed stage-V ban (2026-08-06) — `ℓ¹`-Fourier / radii-polynomial machinery on ANY
  model.** **Does not bite, and the reason is checkable rather than asserted.** A4 is a scalar
  pressure law `p(ρ)=ρ^γ/γ` and the constant `α=(γ−1)/2`. It names **no radii polynomial, no
  Fourier coefficient space, no `ℓ¹_w` weight, no collocation basis, no approximate inverse and
  no Newton–Kantorovich bound** — there is no operator here at all, only a monotone scalar map
  and two constants. 285 §5 measured the same statement across the whole apparatus (*"of the 16
  records, **zero** name a radii polynomial"*), so the ban's lift condition (a namable fourth
  space) is not even engaged: **the ban forbids machinery this term does not use.** Per the
  leg's own instruction — *if the cheapest term is banned machinery, take the cheapest that is
  not* — no substitution is needed, and the escalated no-branch (*all 15 banned*) does **not**
  fire.
* **Another gCLM measurement leg.** Not contacted: the object is 3D isentropic compressible
  Euler/Navier–Stokes (BCG `arXiv:2208.09445`), not gCLM. No gCLM quantity is computed.
* **DSS cheap entrance (bifurcation off a fixed point of a rescaled flow) and the expensive
  entrance (global periodic-orbit search).** Not contacted: **no flow is integrated, no fixed
  point is located, no spectrum is computed, no orbit is searched.** This leg's only ODE-adjacent
  content is *closed-form scalar functions of `(γ, r)` transcribed from BCG*, evaluated
  pointwise.
* **Any GA compute on an unvalidated fitness.** Not contacted: no GA, no fitness, no search.
* **Re-measuring β on the 2D object / the 2D near-null direction / the scaling gauge as the
  near-null direction / Chen–Hou's 2D profile as a target.** Not contacted: no 2D object.
* **Closing the truncation gap by extending the domain; repairing `Z₁`'s block coupling; tuning
  the weight exponent `s`; re-claiming leg 51's methodological finding; the leg-53 readings.**
  Not contacted: no certificate, no weight family, no block matrix, no `Y₀`, no `Z₁`.
* **"Building a solver without grepping `capabilities.py` for the object first."** **Honoured,
  and recorded below in §3.**

**No ban is lifted, no stage is claimed, `plan_of_record.py` is untouched by this leg.**

## 3. Repo-internal instrument — is A4 actually absent, and what is already banked?

### 3a. `capabilities.py` (the index leg 265 censused)

Grepped for `gamma`, `pressure`, `adiabat`, `compressib`, `sound speed`, `barotropic`.
**0 rows carry a pressure law.** Every `gamma` hit is a *different* γ: Chen's `gamma=2` gCLM
candidate (l.119/122/132), the floating-point error constant `gamma_m` (l.209/211), and the
weight exponent `gamma` of the decay-grading family (l.311–325, l.372). **Homonyms, all three
families of them** — the same adjudication 285 made for `adiabaticity`, on a second vocabulary.

### 3b. Second instrument — direct grep over `solver/` and `experiments/`

* `adiabatic` in `solver/` + `experiments/*.py`: hits are `i4_adiabaticity` /
  `solver/marginal_flow.py:268` — the **continuation / slow-parameter** sense, plus 285's own
  spec runner quoting the term name. **0 pressure laws.** 285's adjudication reproduced
  independently.
* `barotropic`, `ideal gas`, `pressure law`, `p(rho)` as an implementation: **0 in `solver/`.**
* **`solver/` contains no file that computes `p(ρ)`, `α`, or `1/α` for any `γ`.** A4 is absent as
  a *module* on both instruments.

### 3c. What IS already banked — the part that changes this leg's shape

**This pass found prior internal work my first construction sketch would have duplicated, and
that is the point of running it first.** Three legs have already *measured* the constants A4
feeds:

1. **Leg 240 (Route-CNS2, `experiments/p2_route_cns2_v1_lit.py`)** — transcribed BCG
   `eq:delta:dis` and CGSS's own form and evaluated both on a 39×19 `(γ,r)` grid (max abs
   deviation **1.78e-15**); banked the γ=7/5 dominance window `r ∈ (1.1666667, 1.1909830)`,
   width **2.43e-02**, `δ_dis ≤ 0.1458980`; re-derived the two branches of `r*(γ)` agreeing at
   `γ=5/3` and the ceiling `γ < 1+2/√3` bisected to **2.154700538379** against published.
2. **Leg 265/266** — `k(7/6) = 16.3479210517` in closed form, *"giving exactly 7 odd branches
   (3,5,…,15) inside the target window"* (DIRECTION.md l.11548).
3. **Leg 275** — the ansatz `n=3`, `r^(3) ∈ (1.070374, 1.094975)`, `δ_dis ∈ (−0.5778, −0.4302)`.

**Consequence, stated before construction so it cannot be retrofitted:** the *numbers* those legs
banked are **NOT this leg's novelty and are not re-claimed**. They are this leg's **known
answers** — an independently derived, independently verified internal reference set that A4's
outputs must reproduce through a *different* code path. What is absent, and what this leg
supplies, is (i) **the term as a consumable module** with 285's I/O contract (no `experiments/`
literature runner exposes `p`, `α`, `1/α` to a downstream term), and (ii) **a sensitivity /
detection-threshold ledger for `α`** — how much precision A4 must carry for each of its
downstream consumers to survive — which **no leg has measured** and which is the only new
magnitude this leg claims.

## 4. External prior art — arXiv nets, run live

Instrument: arXiv API over https, 5-retry backoff. **Validated with two positive and one
negative control before any zero is read as evidence** — an all-zero net sheet is a broken
instrument, not a finding (this pass's *first* run returned 0/0/0/0/0/0/0 and a 301, and was
discarded as an artifact rather than banked).

| net | query | total |
|---|---|---|
| **N0-POSCTL** | `all:"Navier-Stokes"` | **10751** |
| **N0b-POSCTL** | `all:"imploding" AND all:"compressible"` | **58** |
| N1 | `all:"imploding" AND all:"compressible Navier-Stokes" AND all:"self-similar"` | **2** |
| N2 | `all:"gamma-law" AND all:"computer-assisted"` | 0 |
| N3 | `all:"dissipative forcing" AND all:"self-similar" AND all:"compressible"` | 0 |
| N4 | `all:"adiabatic exponent" AND all:"interval arithmetic"` | 0 |
| N5 | `all:"sensitivity" AND all:"adiabatic exponent" AND all:"self-similar"` | 0 |
| N6 | `all:"pressure law" AND all:"validated numerics"` | 0 |
| N7 | `abs:"implosion" AND abs:"branch" AND abs:"self-similar"` | 0 |
| N8 | `all:"barotropic" AND all:"self-similar profile" AND all:"stability"` | 0 |
| **N9-NEGCTL** | `all:"zzqxwv nonexistent phrase 12345"` | **0** |

**N1's two hits are both already in this repository's record**: BCG *Smooth imploding solutions
for 3D compressible fluids* (`2208.09445`, the target paper) and *Blow-up of the 3-D compressible
Navier–Stokes equations for monatomic gases* (`2501.15701` — the γ=5/3 SWWZ paper leg 240 used as
its independent-group control). **No third paper exists in that intersection.**

**What the zeros mean, stated narrowly.** The mathematics of A4 is textbook and **this leg claims
zero mathematical novelty for it** — `p(ρ)=ρ^γ/γ` is 19th-century thermodynamics and `α=(γ−1)/2`
is BCG l.180. The nets are not asked whether the γ-law is new; they are asked whether the
*artifact* is: **N2/N4/N6 = 0** says no published work packages a γ-law pressure term as
validated-numerics/interval-ready apparatus, and **N5 = 0** says no published work measures the
sensitivity of the self-similar implosion constants to the adiabatic exponent they hang on. That
is the shape of this leg's contribution: an **artifact and a sensitivity ledger**, not a theorem.

## 5. Provenance

BCG `arXiv:2208.09445` downloaded fresh by this leg into a **leg-private** scratch directory
(honouring leg 265's recorded shared-scratchpad defect): md5
**`45ea63c45a1a199ecfb4dc4a15431600`**, 6898 TeX lines — a **sixth** independent download,
bit-for-bit identical to legs 251, 266, 265, `verify_251`, `verify_265` and 285. Every BCG line
number cited by this leg was read from that file, not copied from another leg's journal:
`eq:Euler` l.134, the ideal gas law l.140–141, `σ=α⁻¹ρ^α, α=(γ−1)/2` l.180, `eq:rstar` l.353–357,
`eq:delta:dis` l.489–491, `eq:r:restriction` l.493–495, the ceiling `γ<1+2/√3` l.497–499,
`eq:R1` l.526–528, `eq:W1Z1`/`eq:def_R2` l.543–550, `eq:k_asquotientDZ1` l.600.

**Clay odds stay ~0.05%.** Building an apparatus term is not movement of any L1→L4 link, and this
leg's own thesis says so before it starts.
