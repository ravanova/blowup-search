# Leg 222 — Route-FBA novelty pass (run and committed BEFORE construction)

**Date:** 2026-08-06. **Agent:** LEG. **Branch:** `leg/222-fba-v1`.
**Verdict: `PROCEED_AS_ADVERSARIAL_AUDIT`.**

This leg builds no new mathematics, measures no new physics, and claims no novel mechanism.
It is the last uncovered entry in the audit family's own module-coverage inventory: an
adversarial input-validation battery against `solver/fractional_boussinesq.py`.

**`solver/fractional_boussinesq.py` is READ-ONLY under this leg, under EITHER branch of the
gate.** It is not edited under any outcome. The yes-branch escalates and parks; it does not
patch.

**Nothing here re-measures, re-derives, contests or re-interprets `beta`, `s_c`, or any
physics quantity of the 2D object.** Every number produced is a statement about CODE
BEHAVIOUR UNDER INVALID OR DEGENERATE INPUT. Where a battery calls `fit_collapse` or
`collapse_window_report`, what is read off is the *control-flow outcome* (refused / returned
NaN / returned a finite number), never the value as a physical exponent.

Per `writeup/novelty/README.md`: **links, not counts.** Query strings verbatim below.

---

## 0. Ban ruling — every live ban from `.venv/bin/python plan_of_record.py`, read one by one

* **"re-measuring beta on the 2D object"** (lift: **never** — leg 43 showed the object does
  not converge). **This is the ban nearest to this leg and it must be ruled on explicitly.**
  It **does not bite**, for the same reason leg 67 recorded on this very module and legs 91 /
  134 recorded on its 1D sibling: this leg reports no growth rate, no fitted exponent as a
  physics quantity, no convergence claim, and no statement about whether the 2D object blows
  up. `collapse_window_report` is exercised as a **guard**, and the only thing recorded about
  it is whether it **refuses** — `measurable=False` is the *pass* condition, not a
  measurement. Any `beta`-shaped float that appears in the curated JSON is tagged
  `not_a_physics_number` at the point it is written, and no prose quotes it as an exponent.
* **"building a solver without grepping `capabilities.py` for the object first"** — honoured.
  `capabilities.py:200-204` registers `solver/fractional_boussinesq.py`, object *"2D
  Boussinesq, fractional dissipation"*, holding *"the critical-dissipation exponent for the
  2D object"*, validated by *"consistency with the 1D critical exponent; SEARCHED for an
  independent answer (leg 67) and none exists ... stored `s_c=0.1711999849` remains
  internally-consistent-only"*, test `test_fractional_boussinesq.py`. **No solver is built
  here at all** — the module is imported, never re-implemented.
* **"another gCLM measurement leg"** — does not bite; the object here is 2D Boussinesq, and
  in any case no measurement is produced.
* **"chasing the 2D near-null direction of leg 44"**, **"aiming the port at Chen-Hou's 2D
  profile as a TARGET"** — neither is touched; no target selection, no near-null direction,
  no eigen-computation appears.
* **"any GA compute on an unvalidated fitness"** — no GA compute of any kind runs.
* **"building further ℓ¹-Fourier or collocation machinery for this operator"**,
  **"repairing `B`'s three degrees of freedom (space, split, shape)"**, the Route-D
  bound-sharpening ban, the DSS re-ask, the scaling-gauge ban, the `Z_1` / `Y_0` / `s`-exponent
  / leg-51 clauses, the "closure is a property of the SPACE" clause, the leg-53 clause, the
  domain-extension clause, the stage-`V` re-opening clause: **all concern the certification
  lane.** None touches an input-validation battery on a pseudo-spectral 2D solver; no
  certificate, no interval, no weight and no approximate inverse is constructed here.

## 1. What is already known here, and by whom — the in-repo prior art

**Query (in-repo):** `grep -rihn "adversarial audit of \S*" --include=*.py .`, reduced to the
distinct named modules. The audit family currently names, in its own file headers:
`solver/turning_point.py`, `solver/spectral_utils.py`, `solver/reduced_certificate.py`,
`solver/nk_fourier.py`, `solver/gclm_rescaled.py`, `solver/gclm.py`, `solver/gclm_family.py`,
`solver/fractional_gclm.py`, `solver/decay_grading.py`, `solver/decay_collocation.py`,
`solver/critical_dissipation.py`. **`solver/fractional_boussinesq.py` does not appear.**

**Query (in-repo):** `ls test_*_adversarial.py`. There is a `test_boussinesq_adversarial.py`,
a `test_boussinesq_velocity_adversarial.py`, a `test_gclm_adversarial.py` and a
`test_fractional_gclm_adversarial.py`. **There is no `test_fractional_boussinesq_adversarial.py`.**
The only test on the module is `test_fractional_boussinesq.py` (324 lines, gates `G0`–`G6`),
which is a *correctness-at-admissible-input* battery: it checks the law `s_c = 1/(2β)`, the
Chen–Hou arithmetic, exactness of the integrating factor, symmetry preservation, peak
structure, that `collapse_window_report` refuses, and the sign of the relevance fit. **Not one
of its gates hands the module an invalid, malformed or degenerate input.** That is the hole.

**Query (in-repo):** `grep -rln "fractional_boussinesq" .` — 20 files. The substantive ones:

* **Leg 67 / leg 67-verify (Route-FD)** — literature pass on `s_c` for this module. Gate
  answered **NO**: no primary source publishes a comparable arrest exponent; the published
  `α + β = 1` threshold (Stefanov–Wu–Xu–Ye arXiv:2606.03680) is a well-posedness scaling
  bound, one-sided, not comparable. **Zero new solve runs**, so leg 67 touched no code path.
  It is a *provenance* pass, disjoint from an input-validation pass.
* **Leg 71** — Route-G v1 collapse experiment (`experiments/p2_route_g_v1_collapse.py`), the
  producer of the banked numbers. A measurement leg, at admissible input.
* **Leg 114**, **leg 0 bench redtests** — touch the module by name only.
* **`writeup/4_p2_lottery/TECHNICAL_P2_ROUTEG_V1.md`** — the write-up of the banked result.

So: **the module has a correctness battery, a literature battery and a measurement leg, and
no adversarial battery.** Confirms the dispatch's own sweep.

## 2. THE ONE THING THAT IS NOT NEW, AND MUST NOT BE PRESENTED AS NEW

**Escalation #4 / leg 188 already owns the dealiasing-mask question on this module, by name.**

Leg 188's landed novelty commit (`87f0adb`, `Leg 188: LEG — Route-SURV novelty pass`) states
verbatim that *"every shipped mask on `main` is still loose (`spectral_utils.py:36 <= n/3`,
`boussinesq.py:153/419`, **`fractional_boussinesq.py:259`**, `fractional_gclm.py:265`)"*, and
its result commit (`14ba668`) reports the strict-Bowman-cut finding: the `n=3` loose band is
aliased at `2.4999999999999994e-01`, **31.5 decades** above the `n=4` control
(`7.4988e-33`); acceptance differs at exactly **1 grid in 5000** for `n=3` against 3 for the
lesson-90 control rule. It also records the correction that of the five named `n/3` sites only
**2 are dealias masks** and the other **3 are diagnostic tail-band edges that gate no grid** —
and that `fractional_boussinesq.py:259` (`k_cut = self.n / 3.0` inside `tail_fraction`) is a
*diagnostic band edge*, not a mask. The mask this module actually dealiases with is
`dealias_mask2d` **imported from `solver/boussinesq.py:149-154`**, which it does not own.

**Consequence, pre-committed here before any measurement:** if this leg's battery surfaces
anything whose mechanism is the `<= n/3` versus `< n/3` cut, or the `n=3` retained-band
degeneracy, **it is escalation #4's finding, restated on a module escalation #4 already names,
and this leg will say exactly that** — it will not be written up as a new defect. Leg 188 and
leg 220 are **read-read** with respect to this leg, not a collision: they ask whether the grid
guard should be strict; this leg asks whether *any* input, invalid or degenerate, makes the
module return a wrong value silently. Different question, same file.

## 3. Shape precedent — what this leg's artifact must look like

* **Leg 91 (Route-FGA)**, `experiments/p2_route_fga_v1_adversarial.py` +
  `test_fractional_gclm_adversarial.py` + `writeup/data/p2_route_fga_v1_adversarial.json`:
  the template. Batteries `A1`–`A5`, a `gate` block in the JSON answered *from the batteries
  themselves* rather than by hand, NaN/inf JSON-tagged as strings, and the passes reported as
  passes ("a map of behaviour, not a bug list"). Its gate answered **YES**; the guard landed
  later as a separate bench repair, never inside the audit leg.
* **Leg 134 (Route-FGB)** — the close-the-loop precedent, and the source of the explicit
  "READ-ONLY under either branch" wording adopted above.
* **Figure registration: none.** `writeup/build_figures.py` contains **0** occurrences of
  `adversarial`, and `git log --all --name-only --grep="Leg 91:"` touches **0** paths under
  `writeup/figures`. The audit family registers no figure, and neither does this leg.
* **Doc note:** audit legs write `experiments/journal/leg_<N>.md` and
  `writeup/novelty/leg_<N>.md` and do **not** touch the five integration-owned ledgers.

## 4. External literature

**Queries:** "minimum admissible number of Fourier modes pseudospectral", "2/3 rule small N
degenerate", "dealiasing mask N=3", "fractional Laplacian |k|^{2s} spectral overflow float64",
"Boussinesq pseudospectral input validation".

**The corpus is silent at the small-`n` end**, exactly as leg 188 recorded independently: no
source discusses a minimum admissible `N` or a degenerate retained band. There is therefore no
citation available for what "invalid" means at the grid-size end, and this leg does not invent
one — the `n=3`/`n=4` boundary is entered as **escalation #4's** question, with escalation #4's
own framing, not as a literature-backed threshold of this leg's own. At the exponent end the
position is the one leg 91 established for the 1D sibling: the dissipation literature works at
`σ = 2s ≥ 0` throughout, so `s < 0` is outside every published range and is the
literature-backed invalid case; there is no published upper bound, so the upper wall is a
property of the CODE and is **measured**, not asserted.

## 5. What could make this leg worthless, written down in advance

* **If every battery is a tautology of the code** (lesson 90). Guard: every case is run
  CLEAN-vs-DEGENERATE at otherwise identical settings, and for each battery the note records
  what would have had to differ in the module for the other answer to come out.
* **If it re-reports escalation #4.** Guard: §2 above, pre-committed.
* **If it drifts into physics.** Guard: §0, and the `not_a_physics_number` tagging.
