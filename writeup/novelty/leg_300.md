# Leg 300 — Route-P0TCV: novelty / prior-art pass

**Committed BEFORE construction**, per `CONTINUATION_PROMPT.md`. Route-P0TCV is the
independent verification of leg 266's GATE-YES correction to leg 251's certificate
obligation #1, followed (on a YES) by execution of 266's own yes-branch.

This leg's "prior art" is overwhelmingly *internal*: the question is not "has anyone in
the literature done this?" but "has this repository already checked what my gate asks me
to check, so that my measurement would be a re-run rather than a verification?" Both
halves are answered below. Every claim here is a repository fact I re-read, not a
transcription from another leg's report.

---

## 1. Has leg 266 ever been independently verified? **NO.**

`writeup/novelty/` holds 200 files, of which 14 are verification passes:
`leg_54_verify.md`, `leg_54_verify_v2.md`, `leg_58_verify.md`, `leg_64_verify.md`,
`leg_65_verify.md`, `leg_67_verify.md`, `verify_250.md`, `verify_251.md`,
`verify_255.md`, `verify_256.md`, `verify_265.md`, `verify_268.md`, `verify_271.md`,
`verify_276.md`.

**`verify_266.md` does not exist.** Leg 266 finished GATE YES on `leg/266-p0tc-v1`
(`873a15f`) in a session that died before any paired verification ran, and leg 275
(`2aac47a`) then *stacked its own correction on top of the unverified commit* — so the
current tip of `leg/251-p0t-v1` rests on an unverified rework. That is the gap this leg
closes, and it is genuinely open.

## 2. What HAS been independently confirmed already (so I do not claim it as mine)

Two prior verifiers touched adjacent ground, and I record exactly how far they reach so
that my own contribution is not overstated:

| prior check | what it confirmed | what it did NOT reach |
|---|---|---|
| `verify_251.md` §C | the *finding* that 266 implements — no stationary dissipative profile system at BCG's scaling; `F_dis` a non-autonomous forcing; real gap in the stability step | it verified **leg 251**; 266's *implementation* of the correction did not yet exist |
| `verify_265.md` (`7321005`) | `r* = (7−√5)/4 = 1.19098300562…` re-derived from `(eq:rstar)`; `δ_dis = 6r − 7` re-derived from `(eq:delta:dis)` with `α = (γ−1)/2` at l.180, so `δ_dis = 0` exactly at `7/6`; BCG e-print md5 reproduced on a **third** independent download | it was verifying **leg 265**'s `k(7/6)` chain. It never audited 266's diff, never checked byte-untouchedness of 251's report, and **never formed the width ratio at all** |

So: **both window endpoints already have one independent in-repo re-derivation**
(verify_265), and `writeup/data/p2_route_cns2_v1_lit.json:362` independently banks
`"hi": 1.1909830056250525`. `experiments/journal/leg_240.md:117` already carried the
window `r ∈ (1.1666667, 1.1909830)` at width `2.43e-02`.

**The width RATIO has never been re-derived by anyone in this repository.** A repo-wide
grep for `6.855` returns only downstream restatements of leg 266's single computation —
`experiments/journal/leg_266.md:70` (the source), `experiments/JOURNAL.md:3730`, and
`DIRECTION.md` — with **zero** independent derivations. That makes the ratio the one
magnitude in my gate's clause (c) with **no** prior in-repo check — and therefore the
load-bearing novel measurement of this leg.

> **Correction to this pass, made during the leg and left visible rather than rewritten.**
> At the time of writing, the count was three surfaces. `origin/main` then advanced
> (`80c0cc4 → b2d750b`, the fresh-session reroster) and the runner's own propagation
> census, re-run after rebasing, measures **8** surfaces, **7 on `main`** — six of them in
> `DIRECTION.md`, two of which are the title and thesis of drafted reserve leg 305. The
> novelty determination is unaffected (still zero independent derivations); the *scope of
> the repair* is larger than this pass anticipated, and the journal's §7 rework spec
> carries the corrected list.

## 3. Has the repository ever audited a rework leg's diff for byte-untouchedness? Partly.

`grep` over the journal set shows leg 275 performed exactly this audit against 266
(`Obligations 1-5 byte-identical … 0 outside territory`) — but **275 is the successor
leg auditing itself against its own predecessor**, i.e. the same authorial line, not an
independent party, and 275's audit takes 266's obligation-1 text as its *baseline*
rather than questioning it. No third party has diffed `bef1d5e → 873a15f`. Novel.

## 4. External prior art

None applies, and I state that plainly rather than padding. Clause (a) is a reading
question about two specific papers (BCG `arXiv:2208.09445`, CGSS `arXiv:2310.05325`);
clauses (b) and (c) are audits of this repository's own artifacts. The relevant external
object is BCG's LaTeX source itself, which this leg re-downloaded rather than inherited
(§ below). Leg 265's own novelty pass already established the decisive *forward*-looking
prior art (`arXiv:2509.12435`, Larson–Penston nonlinear stability with interval
arithmetic inside the mode-stability step) and I do not re-litigate it: it bears on what
Phase 1 would cost, not on whether 266's correction is right.

## 5. Provenance discipline for this leg

BCG `arXiv:2208.09445` was re-downloaded from `arxiv.org/e-print` into a **leg-private**
scratch directory (leg 265 recorded a provenance defect from a *shared* scratchpad, and
that lesson is honoured here). Tarball md5 `45ea63c45a1a199ecfb4dc4a15431600`, 1520724
bytes, extracting `RadialImplosion31_FinalArxiv.tex` at **6898 lines** — this is now the
**fourth** independent download agreeing bit-for-bit (verify_251, leg 266, verify_265,
leg 300). No magnitude in this leg's report is copied from 266's arithmetic; every one
is recomputed from BCG's own formulas at 50 decimal digits by
`experiments/p2_route_p0tcv_v1_verify.py`.

## 6. Determination

**Novel, and worth running.** Clause (b)'s independent diff audit and clause (c)'s width
ratio have no in-repo precedent; clause (a) and the two window endpoints have one prior
independent confirmation each, which I report as corroboration rather than re-claim.
