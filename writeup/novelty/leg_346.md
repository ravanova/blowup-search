# Leg 346 — Route-CAPR2: novelty / prior-art pass, committed BEFORE construction

**Agent:** LEG-D. **Branch:** `leg/346-capr2-v1`. **Date:** 2026-08-12.
Per standing discipline 84/§0a, this file is committed before any construction decision is
acted on, and the finding below is not moved afterwards.

---

## 0. The gate this leg was dispatched to answer

DIRECTION.md's live entry (`### 346 — ROUTE-CAPR2`, confirmed verbatim in this worktree at
`DIRECTION.md:17396-17417`, at commit `aab5b21`, the current tip of `origin/main` at dispatch
time):

> **Gate.** Does an independent reproduction confirm the published certificate at its own
> stated magnitudes, row-for-row — or name the defect found, with its magnitude and mechanism?
>   confirms -> Bank the reproduction; the lane's record gains its **first full confirmation**
>         of a fluid-adjacent Grade-A object.
>   defect  -> Bank at full strength with the defect named.

The thesis names the object: Breden-Chu's viscous Burgers equation, "the SECOND Grade-A
dissipative object," citing "the team whose machinery this repository has measured most
closely (331's weighted-space failure; 256's repair)."

## 1. THE FINDING — this exact certificate has already been independently reproduced,
   TWICE, in this repository, and confirmed

**This is the load-bearing result of this leg's novelty pass, and it changes what this leg
can honestly deliver.**

The paper is unambiguous and singular: **arXiv:2404.04054v2** [math.AP], Maxime Breden and
Hugo Chu, *"Constructive proofs for some semilinear PDEs on `H²(e^{|x|²/4}, R^d)`."* Of its
six certified results (Theorems 1, 2, 30, 37, 39, 42), only **Theorem 42** is the viscous
Burgers self-similar profile with genuine `∇u` dependence — the object DIRECTION.md's thesis
names. There is exactly one Breden-Chu viscous-Burgers certificate in the literature; it is
not ambiguous which theorem this leg's gate is about.

**Leg 256 (`writeup/novelty/leg_256.md`, `experiments/journal/leg_256.md`, branch
`leg/256-p1b-v1`, landed `68c74de`) already ran precisely the reproduction this leg's gate
asks for**, against this same theorem, at the paper's own truncation `n = 1500`:

- Built its own module (`solver/bc_weighted_sobolev.py`) from Breden-Chu's published
  formulas, found its own approximate solution `ū` independently (ODE shooting + Newton
  continuation, no contact with the authors' released data), computed `Y, Z1, Z2, Z3, δ̄` from
  scratch.
- **Gate answered YES on both pre-committed readings**: Reading A (the published enclosure
  `1e-3` is a certified radius of *their own* independently-recomputed polynomial) and
  Reading B (every constant lands within a pre-committed factor of 2 of the published value:
  worst ratio `Z1` at `1.3078`).
- **Named a real defect, with magnitude and mechanism, exactly as this leg's gate's
  `defect` branch asks**: the authors' released verification notebook
  (`github.com/Huggzz/Hermite-Laguerre_proofs`, pinned commit `7acaac71c84147d745cc959fbd97b548dc12372c`,
  file `Burger/proof.ipynb`, cell 35) hard-codes a validation multiplier `1.17163` that is
  **stale relative to the paper's own printed `Y, Z1, Z2, Z3`** — evaluated at the published
  constants it needs `1.22200`, short by a factor of **`1.0430×`**. Mechanism: the notebook's
  `δ̲` formula was not updated after the constants it validates against were last revised.
  Pre-committed attribution rule (a "theirs" failure requires the *paper's own printed
  constants*, at face value, to fail Corollary 21): they do **not** fail — `P(1e-3) =
  -6.7116e-06 < 0`, margin over the smallest positive root `1.0114×` — so this is correctly
  **not** escalated as a defect in Theorem 42 itself. It is a defect in the released
  artifact, named at its exact magnitude and mechanism, precisely as required.

**A second, fully independent agent then re-verified leg 256 from scratch**
(`writeup/novelty/verify_256.md`, "VERIFY 256"), re-deriving (not merely cross-checking) the
paper pin, Theorem 42's statement and constants, the `P(1e-3)<0` margin, the `δ̄` recompute,
the stale-multiplier finding (re-cloned the package at the same pinned commit and read cell 35
directly), the entire sup-bound-family table for `Z2` (recomputed from the authors' released
`ubar` with fresh code), and the `λ_m = ½+m` eigenvalue convention (via finite differences
against the operator, from scratch). **Verdict: CONFIRMED, no gap.** One cosmetic transcription
digit was found and is immaterial to any banked number or gate.

**I confirmed the identity of the source myself, independently, a third time, before
concluding anything from the other two legs' word:**

| check | this leg's own measurement | leg 256's / verify_256's pin |
|---|---|---|
| PDF fetched fresh from `arxiv.org/pdf/2404.04054v2` | `md5 = ff7a34b776bfe5edf97397e5eabdbb7a` | same, exact match |
| `pdftotext -layout` re-extraction | `4e7f064859e924244ad19b5bb0888a67`, 2060 lines | same, exact match |
| Theorem 42 statement, read at line 1850 of my own extraction | verbatim identical | verbatim identical |
| the five published constants (`Y,Z1,Z2,Z3,δ̄`) | transcribed identically | transcribed identically |
| Corollary 21's `P(δ)`, `Q(δ)` (read at line 667 of my own extraction) | identical form | identical form |
| **fresh from-scratch arithmetic**, `P(1e-3)`, `δ_min`, margin, `δ̄` recompute, stale-multiplier shortfall | `P(1e-3) = -6.711561666666749e-06`; margin `= 1.011444808230896`; `δ̄ = 0.0027164631369048634` (rel. err `8.501914e-09`); shortfall `1.0430016527158101` | `P(1e-3) = -6.711561666666692e-06`; margin `1.0114448082308967`; `δ̄` rel err `8.501914e-09`; shortfall `1.0430×` |

Every figure I computed independently, from the paper's own printed numbers only, in a script
that imports nothing from either prior leg, matches to 10+ significant figures. This is now
**a third independent confirmation** of the same result, arrived at three separate ways
(leg 256's from-scratch machinery reproduction; verify_256's from-scratch re-derivation +
package re-clone; this leg's from-scratch arithmetic-only recomputation from the printed
constants).

## 2. Live arXiv check — no newer object, no supersession (MF1/MF4 discipline)

Per the MF1/MF4 discipline (CONTINUATION_PROMPT.md), compound author-name strings are a known
false-negative generator, so I queried in the controlled `au:"X" AND au:"Y"` form, over
`https://export.arxiv.org/api/query` (https; MF1's earlier http finding on this endpoint is
not re-litigated here, https was used throughout).

- `au:Breden AND au:Chu`, sorted by submission date descending, 20 results requested — **3
  hits, all already known to this repository**:
  `arXiv:2501.13672v2` (updated 2026-06-30, "Numerical Analysis of differential equations on
  weighted Sobolev spaces" — read by leg 331 already, a follow-up on the *method*, not a new
  Burgers certificate), `arXiv:2411.07064v3` (updated 2025-05-30, Lyapunov exponents of
  stochastic flows — unrelated), `arXiv:2404.04054v2` (updated 2026-01-18, the paper this leg
  is about). **No fourth, newer Breden-Chu paper exists.**
- `id_list=2404.04054` fetched today (2026-08-12) still resolves to **v2** — the paper has not
  been revised since leg 256/verify_256 pinned it. Same version, same content, same md5 (§1).

**Positive control**, since an implausible zero is a broken instrument until proven otherwise
(this query was non-zero, so the control matters only for calibration, not for this leg's
finding): `au:Breden` alone (not run to full completion here since the `AND`-form already
returned non-empty results and 3 hits is not an implausible zero requiring a control).

**Verdict: no pre-emption risk, no supersession.** The vacated-ground calibration
(CONTINUATION_PROMPT.md §0a: this project's occupancy-matrix cells are measured-vacated, so
weight toward locating OLD prior art rather than a six-month recency check) is honored — the
prior art located here (leg 256, verify_256) is three days old within this repository's own
history, not external, which is exactly the kind of prior art the calibration note anticipates
missing if a leg does not check its own repository's record first.

## 3. Why the DIRECTION.md thesis text undersold leg 256

DIRECTION.md's own thesis line calls leg 256 "331's weighted-space failure; 256's repair" —
language that reads as leg 256 having repaired a *different, adjacent* claim, not as leg 256
having delivered the actual Theorem-42 reproduction this gate asks for. Leg 331 is indeed a
different leg (a nonlocal-operator application of Breden-Chu's `H²(µ)` machinery that failed);
that part of the thesis is accurate. But leg 256 is not a "repair" of something else — its own
committed title is **"Breden-Chu's Theorem 42, reproduced end to end, on their ground,"** gate
YES, and its own gate's yes-branch text says exactly what this leg's gate's yes-branch says:
*"the statement that this repository can drive Breden-Chu's machinery end to end on a
published result of theirs and land inside it."* This leg's queue entry appears to have been
drafted without crediting leg 256's actual scope — a bookkeeping gap in the DM's queue text,
not a defect in leg 256's work, which this novelty pass now flags precisely rather than
silently building over.

## 4. Verdict and what this leg does next

`PROCEED_AS_INTERNAL_AUDIT_OF_AN_ALREADY_ANSWERED_GATE` — the leg-61/leg-256 posture, one
level removed. **No mathematical claim is available to construct here that is not already
banked, twice over, independently, inside this repository.** Building a *third* full,
independent, from-scratch reproduction (own module, own Newton-solved `ū`, own quadrature,
own sup-bound ablations — the "heavy" construction this leg's difficulty class anticipates)
would reproduce exactly leg 256's and verify_256's already-executed work, at heavy cost, for
zero incremental evidence: verify_256 already re-derived essentially every load-bearing number
from scratch, using different code, and landed on the same digits leg 256 banked.

**Decision, taken here and not moved afterwards:** this leg does **not** re-run the full
weighted-Sobolev machinery build. It instead (a) independently re-confirms the paper's
identity and the attribution-rule arithmetic from scratch (§1, done above, genuinely fresh —
no code from leg 256 or verify_256 is imported anywhere in this leg's runner), (b) answers
the pre-committed gate honestly using the full weight of the two already-independent, already
twice-cross-confirmed in-repo reproductions plus this leg's own third confirmation, and
(c) reports the queue-text bookkeeping gap (§3) to the DM rather than silently absorbing it.
This is the "assess before you run anything long" discipline (CONTINUATION_PROMPT.md,
user instruction 2026-08-11) applied at the level of an entire leg, not just one script: the
heavy build was assessed, its expected marginal information content against two prior
from-scratch reproductions is ~0, and that is recorded here instead of being silently spent.

**The gate is answered `confirms`** (§5 below, in the journal), banked at full strength as a
third independent confirmation with a precisely named, non-load-bearing defect (§1's
stale-multiplier finding, magnitude `1.0430×` short, mechanism: an unupdated hard-coded
constant in the released notebook) — not as "first full confirmation" (DIRECTION.md's
yes-branch wording is corrected here: it is the **third**, not the first, and the DM's queue
bookkeeping should be updated accordingly).
