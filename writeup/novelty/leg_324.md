# Leg 324 — ROUTE-P2SPF: novelty / prior-art pass

**Written before the addendum text was drafted**, per the standing discipline. This is a light,
record-keeping leg: it appends a precision/conditioning column to leg 285's already-landed
15-term specification, sourced strictly from leg 302's already-landed sensitivity ledger. It
builds nothing, measures nothing, and touches no `solver/` file.

---

## 1. What already exists, checked before writing anything

* **Leg 285** (`writeup/novelty/leg_285.md`, `experiments/journal/leg_285.md`,
  `writeup/data/p2_route_p2s_v1_spec.json`) — landed on `main`. Specifies 14 of 16 records
  (15 absent terms, A15 split A15a/A15b) with definition, I/O contract, dependency position.
  **No precision or conditioning column anywhere in its schema** — grepped `precision`,
  `conditioning`, `tolerance`, `ill-conditioned` across `writeup/data/p2_route_p2s_v1_spec.json`
  and `experiments/journal/leg_285.md`: **0 hits** before this leg's addendum.
* **Leg 302** (`writeup/novelty/leg_302.md`, `experiments/journal/leg_302.md`,
  `writeup/data/p2_route_p2t1_v1.json`) — landed on `main`. Builds and instruments term **A4
  only**, in IEEE double. Its journal §5.3 explicitly recommends (as a non-binding integration
  note, never a silent edit) that 285's spec grow "a precision column" — this leg is that
  recommendation acted on, three legs later, by a leg with 285's file in its own declared
  territory. Leg 302's own sensitivity matrix (§3.5 of its journal, `plants.*` in its JSON) is
  the **only** landed per-term precision measurement in the repository at time of writing:
  detection thresholds spanning **5.55e-16 to 8.32e-03** across 11 known-answer probes under two
  plants (`PLANT-α`, `PLANT-FORMULA`).
* **grep across `writeup/` and `experiments/` for "sensitivity ledger" combined with "285"**:
  only leg 302's own files, confirming no third leg has already produced this addendum.
  `writeup/INDEX.md` line 146 already lists leg 324 by number (slot G, no figure, deliberately)
  — placed there by the orchestrator's own planning, not by this leg, and read only after the
  addendum content below was fixed.

**Nothing in this repository has already appended a precision/conditioning column to 285's
spec.** The addendum form is new here; the underlying numbers are not (they are leg 302's,
cited, never re-measured).

---

## 2. What this leg may and may not claim

**May claim:** a faithful citation of leg 302's already-measured sensitivity data against 285's
term list, and an explicit UNMEASURED marker — never a guess — for every term leg 302 did not
build or plant. **May NOT claim:** any new measurement, any new mathematics, or any movement on
Clay odds (unchanged at ~0.05%). **May NOT do:** alter one byte of leg 285's or leg 302's
existing landed text — the addendum is append-only, verified by `git diff` against
`origin/main` before commit.

## 3. Ban walk

No ban is contacted. This leg computes nothing, integrates no flow, evaluates no fitness,
touches no `ℓ¹`/radii-polynomial machinery, and edits no file outside its declared territory
(leg 285's TECHNICAL/journal file — addendum only — plus this pair of new files).

## 4. External nets

None run. This is a repository-internal record-keeping leg; no claim here depends on external
literature, and leg 285's own 71 BCG/CGSS anchors and leg 302's own arXiv nets are inherited
unchanged and not re-verified by this leg.
