# Leg 228 — Route-BHRV novelty / prior-art pass

**Committed BEFORE any verification code is written.** Role VERIFY, branch
`verify/228-bhrv-v1`. Object: leg 218's border-weight guard in `solver/bordered_hl.py`
(landed `d9a20fb`, journal `fe6aab0`), which repairs leg 198's BHA-1.

Verification legs owe a *light* novelty pass — the question is not "is this result new to
the literature" (it is a re-measurement of an intra-repo repair) but **"has anyone already
done this check, and what does the banked record already say that I must not simply
re-quote?"** Everything below is measured in this worktree, not inherited.

---

## 1. Prior art, intra-repo: has this repair already been verified?

`test_bordered_hl_postrepair.py` — **confirmed absent** across every ref:

```
git log --all --oneline -- test_bordered_hl_postrepair.py      -> 0 commits
ls test_bordered_hl_postrepair.py                              -> no such file
```

So the plain filename is available and the postrepair-family convention
(`test_boussinesq_postrepair.py`, `test_collocation_newton_postrepair.py`,
`test_first_integral_postrepair.py`, `test_target_norm_postrepair.py`, … 17 files) applies
without collision. Likewise `experiments/p2_route_bhrv_v1_postrepair.py` and
`writeup/data/p2_route_bhrv_v1_postrepair.json`: 0 commits, 0 files.

Route name **BHRV** grepped against `capabilities.py` and every `experiments/p2_route_*`
basename: clean (existing neighbours are `bhn`, `bha`, `bhr`).

**Nobody has verified leg 218.** Leg 223 (Route-PUB3, `6cb9bca`) is the nearest thing and
is explicit that it is *not* one: it records leg 218 as having "a novelty pass only" (its
own status snapshot predates `d9a20fb` landing) and prints leg 198's and leg 218's
disagreeing `Z_1` ratios side by side as an unresolved **banked-record disagreement**,
"neither adopted". That disagreement is this leg's item 3 and it is live.

## 2. The two things this leg must NOT do, both from the banked record

**(a) Do not re-quote leg 218's own differential as confirmation of leg 218.** Leg 218's
clause (b) is a 108,346-leaf byte differential run by leg 218. Re-running leg 218's runner
and reporting that it still passes is a **lesson-90 tautology** — a control that cannot
come out differently, since the same code decides both times. This leg re-derives the
caller set, the configurations and the comparison **from the callers themselves**, and
loads the pre-repair module from git independently (blob asserted, see below).

**(b) Do not resolve the `Z_1` disagreement by re-running either leg's arithmetic.** Both
legs computed the same quantity in the same way and got answers 5.24× apart. A third run
of the same code answers nothing. Leg 218's *explanation* — lesson 86, `Z_1` sits at the
round-off floor so the **ratio** is environment-dependent while the **verdict** is not —
is a falsifiable claim about **variance**, and it is tested here as one: perturb the
environment in ways that cannot change the mathematics, and measure the spread of the
ratio. If the spread brackets 5.24×, leg 218's explanation stands. If the ratio is stable
to a few percent under every such perturbation, leg 218's explanation is **wrong** and one
of the two measurements is a real defect.

## 3. Provenance, asserted not assumed

`solver/bordered_hl.py` has exactly **two** commits in the whole repository history:

| commit | blob | meaning |
|---|---|---|
| `0c075b8` | `a6ab17dfd84eb21d4b63f709fc7a464419d6bb51` | pre-repair, the module's original and only state for 172 legs |
| `d9a20fb` | `92e1c6700c17293d96f0c2f010ac96f69af151f3` | leg 218's guard, the state under verification |

Verified here with `git log --oneline -- solver/bordered_hl.py` and `git rev-parse`, not
taken from leg 218's journal. The runner re-asserts both hashes at startup and aborts
otherwise, so "pre-repair" cannot silently become "the same file twice".

## 4. THE DISPATCH'S CALLER SET IS FALSE — AND SO IS LEG 218's REPLACEMENT, BY FOUR

The dispatch names legs **54/58/127/192**. Leg 218 already measured that premise false and
re-aimed clause (b) at a caller set "enumerated **by import**": `port_v1`, `port_v2`,
`l1_v1`, `l1rh_v1`, `test_bordered_hl.py`, `test_interval_certificate.py` — **six**.

Re-enumerating independently (`grep -rn "bordered_hl" --include=*.py`, then reading every
hit to separate a real `from solver.bordered_hl import` from a prose mention) gives
**ten direct importers**, not five-plus-one:

| importer | in leg 218's list? |
|---|---|
| `experiments/p2_route_port_v1_bordered.py` | ✔ |
| `experiments/p2_route_l1_v1_interval.py` | ✔ |
| `experiments/p2_route_l1rh_v1_construction.py` | ✔ |
| `test_bordered_hl.py` | ✔ |
| `test_interval_certificate.py` | ✔ |
| `test_bordered_hl_adversarial.py` | ✗ **missed** |
| `experiments/p2_route_bhn_v1_adversarial.py` | ✗ **missed** |
| `experiments/p2_route_hlb_v1_contraction_lit.py` | ✗ **missed** |
| `experiments/p2_route_tn_v1_consistency.py` | ✗ **missed** |
| `experiments/p2_route_nb_v1_targetnorm.py` | ✗ **missed** |

`p2_route_port_v2_reach.py` does **not** import the module directly (it imports
`p2_route_port_v1_bordered`); leg 218's naming of it is correct as a *transitive* caller,
but then the enumeration was not purely "by import" and four direct importers fell through
the gap. This is **not** an impeachment of leg 218's clause (b) — its differential covers
the module's own public surface over 34 configurations, which is caller-agnostic — but the
**enumeration** is incomplete, and a verification leg that inherited the list would have
inherited the gap. All ten are checked here, at their own live configurations.

Whether any of the four missed importers touches the **weight surface** (the only surface
the guard changed) is a measurement this leg makes, not an assumption. Declared in advance:
if all four are weight-surface-free, the omission is **cosmetic** and clause (b)'s verdict
is untouched; if any one passes a weight, the omission is **material** and the gate's
"every one of their live values bit-identical" clause was answered on an incomplete set.

## 5. Leg 192's status, checked rather than assumed (dispatch item 5)

As of this pass, leg 192 has **one** commit anywhere in the repository (`483b0d7`, a
novelty pass, 1 file, 116 lines, `writeup/novelty/leg_192.md`), lives only on branch
`worktree-agent-a64cddd522d1387c5`, and has **no** journal, **no** runner and **no** JSON.
It has not landed on `main`. Independently: leg 192 is Route-H2CV, the verification of leg
176's **origin-H² certificate**, and its branch touches exactly one file — it does not
import `solver/bordered_hl.py` and never will. So leg 192 contributes **no live value** to
be bit-identical to, and the dispatch's inclusion of it is void for two independent reasons
(not landed; wrong object). Recorded explicitly, per the dispatch's own instruction not to
assume.

## 6. Leg 198's characterization gates: still uninverted, and NOT on `main`

Leg 218 flagged that leg 198's characterization gates "must start failing now". Measured:
`test_bordered_hl_adversarial.py` on `main` is **370 lines** and carries leg 80's 8 gates
only. Leg 198's appended battery (18 gates, 599 lines) exists solely on the **unmerged**
branch `leg/198-bha-v1`. There is therefore **nothing on `main` to invert**, and editing
that file is outside this leg's declared territory in any case. The two weight
characterization gates (`test_characterize_negative_weight_returns_a_negative_operator_norm`
at line 390, `test_characterize_negative_weight_fabricates_a_closing_certificate` at line
416) are read read-only off that branch and re-stated in **inverted** form, at leg 198's
own thresholds and never weakened, in `test_bordered_hl_postrepair.py` — which is the
correct home for them and is inside territory.

## 7. External literature

Light, per the verification-leg convention, and the substantive external question was
already settled by leg 218: the confirm-good polarity `all(isfinite(w) & (w > 0))` versus
detect-bad `any(w <= 0)` is **SEI CERT NUM07-J** (NaN compares false under every ordered
comparison, so detect-bad fails open). No new external claim is made by this leg; it makes
no claim about the literature at all, only about this repository's own code. Nothing here
touches the L1→L4 chain.

## 8. Bans

`plan_of_record.py` read in full. No ban bites: not a gCLM measurement leg, not a Route-D
bound-sharpening leg, not a DSS re-ask, no GA compute of any kind, no new ℓ¹-Fourier or
collocation machinery built (the module is read **only**, never edited), no re-claim of leg
51's methodological finding, no reading of leg 53's assembly as a statement about
`HL_S2_nonsymmetric`. `capabilities.py` grepped for the object first, per the standing ban.

**BANKED NUMBERS AT RISK: 0** — this leg edits no solver module and no shared ledger.

**Verdict: `PROCEED_AS_INDEPENDENT_VERIFICATION`.**
