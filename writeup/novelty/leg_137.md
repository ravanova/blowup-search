# Leg 137 — Route-JR3 novelty pass: the journal-coverage inventory, third window

**Run and committed BEFORE any construction.** Branch `leg/137-jr3-v1`.
Pass date 2026-08-06. **Pinned audit SHA: `34748624189c0a17a688a9195101bcfcaa2666a5`**
(`origin/main` HEAD at this leg's start; sibling legs 126–136 were landing in parallel, so
the window is frozen here and anything landing after it is out of scope by construction).

Following the JR1/JR2 precedent, for an audit leg the "novelty pass" is not a literature
search over the mathematics — this leg derives no mathematics. It is the **inventory** that
establishes (i) the audit is not redundant with the two prior passes, (ii) no other live or
reserve leg owns the ledger this pass reads, and (iii) the window is non-empty. Counts, not
booleans, throughout.

## 1. The plan of record was read first

`.venv/bin/python plan_of_record.py` run before anything else. `NG` is **DONE** (gate YES at
leg 58), `B` is **NEXT**. All **15** printed bans were read. **0 of 15 bind this leg**: it
imports no solver module, runs no GA compute, derives no number, builds no ℓ¹-Fourier or
collocation machinery, and re-opens no closed stage. It counts files and greps one Markdown
ledger, read-only.

## 2. Prior art, in-repo — the two earlier passes and the sibling ledger

| pass | leg | ledger | window | outcome |
|---|---|---|---|---|
| Route-JR  | 72  | `experiments/JOURNAL.md` + `journal/` | through leg 70's era | narrated 8 legs (60, 64–70); deferred 58, 59, 61, 62, 63, 71 as unanswered; found leg 60's 2 files missing |
| Route-JR2 | 102 | `experiments/JOURNAL.md` + `journal/` | `117a176..` (legs 73–99 era) | gate **NO**: 20 of 20 in-window legs unnarrated; 0 per-leg files missing in-window; leg 60's 2 files **still** absent — carried over |
| Route-IX2 | 108 | `writeup/INDEX.md` | ~105 era | different ledger; not this leg's territory |

Leg 102's own audit commit on `main` is **`12c0894`**. That is this leg's cut line: the
window is `12c0894..3474862`, which contains **125 commits**.

**Redundancy check.** JR2's window closed at `12c0894`; this leg's opens there. The two
windows are disjoint by construction, so the audit is not a repeat of an already-answered
question. `writeup/INDEX.md` (leg 138's territory this cycle) is a different file and is not
read or written here. **0 file-territory overlaps** with legs 126–136 or 138.

**Standing carry-over to re-check.** JR2 left exactly one: leg 60's `experiments/journal/leg_60.md`
and `writeup/novelty/leg_60.md`, 1 leg / 2 files. This leg's gate names it explicitly.

## 3. The window, enumerated

Method: `git log --format='%s' 12c0894..3474862`, filtered to subjects matching `^Leg N:`
with `N != 0` (leg 0 is repo-wide bench/orchestration work and carries no gate), deduplicated,
sorted numerically. Cross-checked against `ls experiments/journal/`, `ls writeup/novelty/`,
and a per-leg pointer grep over `experiments/JOURNAL.md`.

**35 legs landed on `main` inside the pinned window:**

    58, 60, 62, 71, 83, 85, 89, 92, 96, 98, 99, 100, 101, 103, 104,
    105, 106, 107, 108, 111, 112, 113, 114, 115, 116, 117, 119, 120,
    121, 123, 126, 131, 134, 136, 141

Eleven of these (58, 60, 62, 71, 83, 85, 89, 92, 96, 98, 99) carry leg numbers *below* JR2's
nominal 73–99 framing. They are in-window regardless: JR2's own §5 measured them as **not yet
on `main`** at its pass time (branch-only), and each landed after `12c0894`. The window is
defined by landing, not by leg number — which is why it is pinned to a SHA.

Leg 141's number sits above this leg's own, out of dispatch order; it landed at
`3474862`'s ancestry inside the window and is audited like any other.

## 4. Coverage counts, measured at the pinned SHA (magnitudes, not booleans)

| quantity | count |
|---|---|
| legs landed on `main` inside the window (gate answered, `N != 0`) | **35** |
| of those with `experiments/journal/leg_N.md` on `main` | **35 / 35** |
| of those with `writeup/novelty/leg_N.md` on `main` | **35 / 35** |
| of those with a `JOURNAL.md` narrative pointer line | **20 / 35** |
| of those **missing** a `JOURNAL.md` pointer line — the gap | **15** |
| leg 60's JR2 carry-over files still missing | **0 of 2** (closed at `e0eba8e`) |

The 15 legs with **zero** pointer lines:

    58, 71, 85, 98, 100, 101, 105, 107, 108, 123, 126, 131, 134, 136, 141

The pointer predicate is the ledger's own established line shape, `^\s*-\s*\*\*Legs? N\b`.
It was cross-checked against a looser predicate (`\bLeg N\b` anywhere on any line): for all
15, the loose count is **also 0**. `experiments/JOURNAL.md` is **3043** lines at the pinned
SHA. **See §7 — the runner widened this control and it then disagreed on one leg.**

## 5. Repo-wide cross-check, outside the window

Widening from the window to the whole of `main`'s history: **67** distinct legs with `N != 0`
have landed. Against those:

| quantity | count |
|---|---|
| landed legs with no `experiments/journal/leg_N.md` | **0 of 67** |
| landed legs with no `writeup/novelty/leg_N.md` | **0 of 67** |
| journal files for legs that never landed (orphans) | **0** |
| novelty files for legs that never landed (orphans) | **0** |

So the per-leg-file half of the ledger is complete **repo-wide**, not merely in-window — a
strictly stronger statement than the gate asks for, and the first time either has been true
since leg 72 opened this route.

## 6. Verdict

**PROCEED_AS_AUDIT.** The window is non-empty (35 legs), disjoint from both prior passes, and
the gate can come out either way on the evidence gathered — it does not come out the same on
its three clauses, which is itself the finding. Clauses (a) and (c) answer **YES** at 35/35
each, and the JR2 carry-over is **closed**; clause (b) answers **NO** at 15 missing pointers.
Per this leg's instructions the five shared ledgers are integration-owned, so — unlike JR2,
which owned `JOURNAL.md` — **this leg edits nothing**: the 15 missing pointer lines are
reported for the orchestrator to add, and no file is created on any other leg's behalf.

No number in this log is re-derived by this leg; every count is a file-existence or
line-match measurement at the pinned SHA.

## 7. Correction, added after the runner ran — the loose control DID come out differently

**This section is appended, not a rewrite; §4's original wording is edited only to point
here.** The pre-registration above is left standing so the record shows what changed and why.

§4's loose control used `\bLeg N\b`. The runner
(`experiments/p2_route_jr3_v1_journal_audit.py`) widened it by one character to
`\bLegs? N\b` — admitting the *plural* "Legs 58, 59, …" form the ledger actually uses in
prose. Under the widened control the strict and loose predicates **disagree on exactly one
leg of the 15: leg 58** (strict 0, loose 1). The other 14 remain 0 under both, so the count
of 15 is unchanged and still regex-independent.

That one disagreement is not a nuisance hit, and the correct response was to follow it rather
than tighten the regex until it went away. It is `experiments/JOURNAL.md:2830`, a surviving
sentence of leg 72's block:

> `Legs 58, 59, 61, 62, 63 and 71 have branch progress but no answered gate, so they are`
> `deliberately absent from this block.`

Of the **6** legs it names, **5 (58, 59, 61, 62, 71)** have since answered a gate and landed
on `main`; only **1 (63)** is still accurate. So the ledger's defect is not purely one of
silence: one sentence in it now **asserts something false** about 5 legs, and leg 58's is the
worst case — its gate answered **YES** (the `NG` theorem on the class `A21 = 0`, an escalation
the DM ruled mergeable), which is the single most consequential gate in the window, and the
only thing `JOURNAL.md` says about it is that it has no answered gate.

This is lesson 75 (two defects in the same problem are not the same defect) and lesson 90 (a
control that cannot come out differently is not a control) landing together on a documentation
leg. It is banked as check **A3b** in the runner and as `stale_assertion` in the JSON, and it
is reported to the orchestrator as a **correction** distinct from the 15 **additions** —
different remedies, so they are counted separately, not summed.
