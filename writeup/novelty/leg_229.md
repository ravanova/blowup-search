# Leg 229 — Route-PNRV: novelty/prior-art pass (committed before construction)

**Date** 2026-08-07 · **Role** LIT · **Branch** `leg/229-pnrv-v1` · **Difficulty** heavy

## Task

Post-repair verification of leg 226's `solver/profile_newton.py` repair (branch
`leg/226-pnr-v1-resume`, commit `d93b51f`). The gate: does an **independent, fresh
re-run** of leg 226's repaired module — fetched off its branch, not trusted from its
own reported numbers — reproduce leg 226's own reported before/after
`a_max_machine`/`last_machine_precision_a`/`grid_converged_a_max`/`GA_boundary` values
and its resolution of the three-grid `a=1.50` disagreement?

## Prior-art / distinctness check

- **Leg 226** (`d93b51f`, on `leg/226-pnr-v1-resume`, NOT on main) did the repair itself
  and ran its own reproduction (`experiments/p2_route_pnr_v1_repair.py` →
  `writeup/data/p2_route_pnr_v1_repair.json`). Its own numbers are the thing being
  checked here, not something to re-cite.
- **Leg 236** independently confirmed the same headline correction
  (`a_max_machine` 1.0→0.55) by a *different method* — dependency-trace exclusion
  analysis, not by running `profile_newton.py` at all.
- **Leg 294** (landed on main, `07c1098`/`c15936b`) is a **consolidation**: it
  synthesizes 236/226/247/252's already-reported numbers, provenance-tiers them, and
  explicitly does not re-run any code. Its own novelty pass (`c15936b`) states leg
  287 already reserves this exact re-execution territory for a leg like this one.
- Grep across `writeup/novelty/` and `experiments/journal/` for `PNRV`/`route_pnrv`/
  leg 229 finds no prior leg with this scope — the hits on bare "229" are line-number
  noise from unrelated files, not content matches. No leg has yet actually imported
  and executed the repaired `profile_newton.py` fresh from leg 226's branch and
  independently recomputed the four headlines plus the `a=1.50` three-grid case.

**Distinctness established.** This leg is the first true re-derivation: import
`solver/profile_newton.py` as it stands on `leg/226-pnr-v1-resume` (not main — the
repair is not yet merged), run it fresh in this leg's own process, and compare against
leg 226's reported table without importing or reusing leg 226's own runner script
verbatim (independent re-implementation of the same measurement, using the same
public definitions from `experiments/p2_route_d_v11_anchor.py` that both v11 and leg
226 use, read directly rather than transcribed from leg 226's summary).

## Plan, pre-committed

1. Fetch `solver/profile_newton.py` at `leg/226-pnr-v1-resume` via `git show` into a
   temp file, import it under a private module name (same discipline leg 226 itself
   used for its pre-repair comparison) — never edit the file, never check it out into
   this working tree.
2. Read v11's own test definitions straight from `experiments/p2_route_d_v11_anchor.py`
   (already on main, confirmed unedited): `a_max_machine` from V2 (`relres<1e-8`, N=801,
   the 17-point `a`-ladder), `last_machine_precision_a` from V3 (`relres<1e-8`, N=801,
   `a` in `[0.44,0.76)` step 0.02), `grid_converged_a_max` from V4 (`c_spread<1e-3` with
   `>=2` grids at `relres<1e-8`, ns=401/801/1601, a in {0,0.2,0.5,0.8,1.0}), `GA_boundary`
   from V3's hardcoded `[0.5,0.55]` literal.
3. Compute each headline twice: once under v11's own inline `relres<1e-8` test (should
   be identical whether or not the module is repaired — the repair only adds fields,
   the residual history is untouched), once under the repaired module's own `converged`
   verdict (`relres<1e-8` AND `converged` True, i.e. residual+gauge+decay all pass).
4. Independently re-run the `a=1.50` three-grid case (n=101/201/301) through the
   repaired module's `continuation()`, and separately through the module pinned at
   leg 226's own pre-repair reference commit `6a17ce6` (fetched fresh via `git show`,
   same discipline), to get pre-repair `c` values.
5. Compare every one of these six computed numbers plus the three-grid table against
   leg 226's reported table (banked in `writeup/data/p2_route_pnr_v1_repair.json` on
   its branch, and restated in `experiments/journal/leg_226.md`). Report relative
   error on every value, not a pass/fail flag.

**Territory respected**: only the five files this leg owns are written
(`test_profile_newton_postrepair.py`, `experiments/p2_route_pnrv_v1_postrepair.py`,
`writeup/data/p2_route_pnrv_v1_postrepair.json`, this file, `experiments/journal/leg_229.md`).
`solver/profile_newton.py`, `experiments/p2_route_d_v11_anchor.py`, and
`writeup/data/p2_route_d_v11_anchor.json` are read only, never edited, under either
outcome. No headline correction is applied to any banked file under this leg's own
authority regardless of result — confirm-or-flag only, per the standing gate contract.
