# Leg 188 — ROUTE-SURV novelty pass, run and logged BEFORE any analysis

**Date:** 2026-08-06. **Agent:** LEG (leg 188). **Branch:** `leg/188-surv-v1`.
**Route** SURV (exploration, not critical path). **Verdict: `PROCEED_AS_NECESSITY_AUDIT`.**

This leg claims **no mathematical novelty**. It asks a *derivational* question about work that
already exists on a parked branch: does leg 129's `n>=3 -> n>=4` floor correction follow
**necessarily** from the strict Bowman 2/3 rule, or is it a **judgment call**? The mathematics
of the 2/3 rule is published (Orszag 1971; Bowman 2013; restated 2026); the defect is measured
and banked in this repository (leg 120); leg 129's repair is written and parked. Nothing here
adds to any of those. What this pass must establish, before the analysis runs, is (a) that the
question has not already been answered in-repo, (b) what the *premise handed to this leg* is
actually worth, and (c) whether any published source admits a reading under which `n = 3`
survives — because that, and only that, is what separates NECESSARY from JUDGMENT CALL.

Per `writeup/novelty/README.md`: **links, not counts** (leg 53 logged counts, could not be
audited, and was withdrawn). Query strings verbatim.

---

## 0. THE PREMISE HANDED TO THIS LEG IS FALSE AS WRITTEN, AND THE PASS CAUGHT IT FIRST

`DIRECTION.md` §188 (l. 5676) and the DM note that drafted it (l. 1551) both say the strict
Bowman rule is *"already cited and **used** elsewhere in this repository, e.g. **leg 120's own
repair**, `k < n/3` strictly"*. **Leg 120 wrote no repair.** Its landed commit
[`750724b`](../../experiments/journal/leg_120.md) ends, verbatim:

> `0 of 107 banked energy_balance_residual records exposed — **escalated, not patched**`

Checked directly on `main`, not from prose about it:

- [`solver/spectral_utils.py:36`](../../solver/spectral_utils.py) — `return wavenumbers(n) <= n / 3.0` — **loose**.
- [`solver/boussinesq.py:153`](../../solver/boussinesq.py) — `cut = n / 3.0`, mask `<= cut` — **loose**.
- [`solver/boussinesq.py:419`](../../solver/boussinesq.py), [`solver/fractional_boussinesq.py:259`](../../solver/fractional_boussinesq.py), [`solver/fractional_gclm.py:265`](../../solver/fractional_gclm.py) — `k_cut = n / 3.0` — **loose**.

`grep -rn "// 3" solver/*.py` returns **nothing**. **No shipped module on `main` uses the
strict cut.** Where the strict rule *is* in force on `main` is in the *adversarial batteries* —
[`test_spectral_utils_adversarial.py:30`](../../test_spectral_utils_adversarial.py) and
[`experiments/p2_route_sua_v1_adversarial.py:120,665,754`](../../experiments/p2_route_sua_v1_adversarial.py) — which
**pin the loose cut as a measured defect** (D1) rather than adopt the strict one in code.

This is load-bearing for the gate, and it is recorded here *before* the analysis so it cannot
be presented later as a convenient discovery. It means the honest form of this leg's question
is **conditional**: *given* the strict cut (which leg 129's repair makes, and which leg 120
measured to be the correct boundary), is `n=3 -> rejected` forced? A reader who instead asks
"is the strict cut itself already adopted?" gets the answer **no, only measured**. Both halves
go in the report; conflating them would overstate necessity.

## 1. Has this question been asked in-repo?

**No.** Three searches, all on `main`:

- `grep -rln "n >= 4\|n>=4" --include=*.py --include=*.md .` → `DIRECTION.md`,
  [`solver/boussinesq_rescaled.py`](../../solver/boussinesq_rescaled.py),
  [`solver/line_hilbert.py`](../../solver/line_hilbert.py). The two solver hits are unrelated
  grid-size guards in other modules; **neither concerns the dealias floor**, and neither is
  reached by `solve_boussinesq`.
- `grep -n "resolve itself\|resolves itself" DIRECTION.md` → l. 103 and l. 5700, **both are
  §188's own text**. No earlier leg has attempted a self-resolution of escalation #4.
- `git log --all --grep "Leg 188"` → nothing prior.

**The nearest in-repo precedent is leg 164**, whose landed commit
[`c45cbc2`](../../experiments/journal/leg_164.md) read leg 162's parked branch, resolved its
ambiguity independently, and **left leg 162's report untouched and still parked**. That is the
template this leg follows in *shape* — read-only access to a parked branch, no merge, no
verdict flipped by the reading leg — and leg 164's file set (journal + runner + curated JSON,
**no** BLOG/TECHNICAL/figure) is the precedent for this leg's declared territory, which
likewise names no writeup or figure path. **Substantively the two legs share nothing**: leg 164
asked a functional-analysis question about compact support; this leg asks a derivational
question about an integer cut.

## 2. External half — is there any published reading under which `n = 3` survives?

Leg 120's own pass (`writeup/novelty/leg_120.md`) and leg 129's (`writeup/novelty/leg_129.md`,
read this pass off the parked branch via `git show`, **read-only**) already settled the prior
art for the *rule*. **Neither is re-run; both are carried**, with their two standing findings
intact: (i) **no published source prescribes the non-strict cut** — leg 129's (R1); (ii) the
repair *form* `(n-1)//3` is **UNPUBLISHED**, so it is justified arithmetically and not cited —
leg 129's (R2), which its runner proves exhaustively over `n = 1..5000` rather than asserting.

What neither pass asked, and this one must, is the **small-`n` end**: does the literature say
anything about a grid so small that the alias-free band is empty?

### Q1
`2/3 rule dealiasing minimum number of modes N=3 degenerate only mean mode retained pseudospectral`

- https://www.math.umd.edu/~tadmor/references/files/Hou%20&%20Li%20filter%20vs%202-3%20rule.pdf
  and https://www.sciencedirect.com/science/article/abs/pii/S0021999107001623 — Hou & Li,
  *Computing nearly singular solutions using pseudo-spectral methods*.
- https://fourierflows.github.io/FourierFlowsDocumentation/stable/aliasing/ — FourierFlows.jl, *Aliasing*.
- https://kth-nek5000.github.io/kthNekBook/_md/spectral/pseudo.html — KTH, *Pseudo-spectral method and aliasing errors*.
- https://arxiv.org/abs/math/0701337 — Hou & Li.
- https://arxiv.org/pdf/2603.08892 — *Aliasing and phase shifting in pseudo-spectral simulations of the incompressible Navier–Stokes equations*.
- https://www.researchgate.net/publication/290229199 — *The need for de-aliasing in a Chebyshev pseudo-spectral method*.

### Q2
`Orszag two-thirds rule alias-free condition N >= 3K+1 strict inequality smallest admissible grid`

- https://math.jhu.edu/~feilu/notes/DealiasingFFT.pdf — Fei Lu (JHU), *De-aliasing in Fast Fourier Transform*.
- https://publications.aston.ac.uk/id/eprint/16380/1/The_rule_for_dealiasing_in_the_split_step_Fourier_methods_for_n_wave_interactions.pdf
  — *The (n+1)/2 rule for dealiasing in split-step Fourier methods for n-wave interactions*.
- https://arxiv.org/pdf/2302.08694 — *A Fourier spectral immersed boundary method with exact translation invariance*.
- https://arxiv.org/pdf/2511.21891 — *GANDALF*.
- https://arxiv.org/pdf/2506.06200 — *Accurate N-body simulations with local primordial non-Gaussianities: initial conditions and aliasing*.
- https://www.cfd-online.com/Forums/main/104795-orszag-2-3-rule.html — CFD-Online, *Orszag 2/3 rule*.

**Finding (E1): the small-`n` end is UNADDRESSED in the corpus.** Not one source discusses a
minimum admissible `N`, a degenerate retained band, or what a solver should do when the
alias-free band contains only the mean mode. Every source states the rule as an asymptotic
resolution statement (*"retain 2/3 of the modes"*, *"effective resolution 2N/3"*). **So there
is no published authority to appeal to on the specific question of `n = 3`** — which means the
gate cannot be settled by citation and must be settled by derivation. That is recorded now so
the analysis is not later credited with a literature result it does not have.

**Finding (E2): the one recorded loose-wording codebase does NOT rescue `n = 3`.** Leg 129's
pass flagged FourierFlows.jl's looser wording as *unresolved rather than as a counter-authority*.
Fetched this pass: it specifies `dealias!` as *"zero-out the highest-`aliased_fraction`
wavenumber components"* with `aliased_fraction = 1/3` by default, and the docs **"do not
explicitly state the mathematical inequality"** (strict vs non-strict). It stays unresolved as
an inequality — but as a **fraction-of-modes** rule it is not a reading under which `n = 3`
keeps a non-constant mode either: one third of three modes is one, and the two non-constant
modes `k = ±1` are the highest. This is checked arithmetically in the runner, not asserted
here. arXiv:2603.08892's form (zero `|k| >= (2/3)k_Nyquist`) is likewise **strict-side**.

## 3. What this leg will NOT claim

- **It will not merge `leg/129-sur-v1`, edit any solver module, or flip any verdict.** Even a
  NECESSARY answer is reported as *information for the user's ruling*; the ruling stays the
  user's, procedurally, per §188's own yes-branch.
- **It will not restate leg 120's or leg 129's measurements as its own.** The 14.1-decade
  alias-error collapse, the 0-of-156 bitwise A/B, the 33/33 control and the per-family census
  are read back from their producing branch and attributed, never re-derived.
- **It will not claim the strict rule is in force on `main`.** §0 above; it is not.
- **No link of the `L1 -> L4` chain moves.** No ban lifted, no ban's lift condition touched.
  No GA compute.
- Nothing here is interval-enclosed: every number is float64 or exact integer arithmetic at a
  stated grid.

## 4. Verdict

`PROCEED_AS_NECESSITY_AUDIT`. The question is unasked in-repo, the shape-precedent (leg 164) is
recorded, the two carried novelty passes are attributed rather than re-run, and **the premise
handed to this leg is corrected before the analysis starts**: leg 120 escalated and did not
patch, so no shipped mask on `main` is strict, and this leg's necessity claim can only ever be
*conditional on adopting the strict cut*. The literature is silent at the small-`n` end, so the
gate is a derivation, not a citation.

---

# ADDENDUM — appended AFTER the analysis, recording two things the pass above did not cover

The pass above is unchanged; this addendum is additive, per the leg's instruction to extend
rather than overwrite. Two items, and only two: the analysis surfaced one **second premise
failure** the pass did not look for, and it **corrected one stated reason** in §2 (E2) while
confirming its conclusion. Both are logged because a novelty pass that is only ever confirmed
by its own leg is not evidence.

## A1. A SECOND FALSE PREMISE, in the same direction as the first

§0 above caught the framing's claim that the strict rule is "already used elsewhere". The
Decision Maker's **corrected** framing then introduced a second factual claim, which the pass
above had no reason to check because it postdates the pass: that leg 129's branch adopts the
strict rule for **"`solver/boussinesq.py`'s mask ALONE"**, leaving four other loose masks.

**Also false, and false twice over** (measured in `B1_site_census` / `B2_what_leg129_strictifies`):

- Of the five `n/3` sites named, only **2** are dealias masks. The other **3** —
  `boussinesq.py:419`, `fractional_boussinesq.py:259`, `fractional_gclm.py:265` — are
  **diagnostic reporting bands** feeding tail-energy ratios. They gate no grid and answer no
  admissibility question. "Four other shipped loose masks" is a miscount.
- The one remaining real mask, `spectral_utils.py:36`, is strictified **by leg 129's own diff**,
  in the same commit as `boussinesq.py:153`. `git diff main...leg/129-sur-v1` changes the cut in
  **2 of 2** masks and **0 of 3** diagnostics.

This matters for the novelty ledger because it changes what is prior art for any future leg:
**there is no "piecemeal adoption" state to reason about.** Leg 129's branch moves both masks
together or neither.

## A2. E2's CONCLUSION STANDS; ITS STATED REASON WAS WRONG

§2's finding (E2) argued that FourierFlows.jl's fraction-of-modes wording "is not a reading under
which `n = 3` keeps a non-constant mode either: one third of three modes is one, and the two
non-constant modes `k = ±1` are the highest."

**Implemented literally, that argument fails.** `ceil(3/3) = 1` component is zeroed, and `k = ±1`
are *two* components, so the literal rule zeroes only one of the pair and **does** leave a
non-constant mode. The pass promised this would be "checked arithmetically in the runner, not
asserted here" — the runner checked it, and it came out against the pass's stated reason. Logged
rather than quietly aligned.

**The conclusion survives on a stronger, measured ground.** The literal mask is **not
conjugate-symmetric**: it maps a real field to a complex one, leaving an **imaginary residue of
2.23e-01** where all five symmetric readings leave **0.00e+00**. Every consumer module
(`gclm`, `fractional_gclm`, `boussinesq`, `fractional_boussinesq`) evolves real fields, so this
is not a looser reading of the 2/3 rule — it is a mask no consumer can run. FourierFlows.jl's
wording therefore still does not rescue `n = 3`, and the count of readings that admit `n = 3`
without being the loose cut leg 120 measured defective stays at **0**.

## A3. Nothing else in the pass moved

The prior-art position (question unasked in-repo, leg 164 as shape-precedent, leg 120/129's
passes carried and attributed rather than re-run), finding **E1** (the corpus is silent at the
small-`n` end, so the gate is a derivation and not a citation), and the four "will not claim"
clauses in §3 all stand exactly as written, and the leg honoured all four.
