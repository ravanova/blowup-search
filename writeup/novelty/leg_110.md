# Leg 110 — Route-L1R novelty pass (run and committed BEFORE construction)

**Date:** 2026-08-06. **Agent:** exploration leg, Route-L1R. **Branch:** `leg/l1r-v1`.
**Verdict: `PROCEED_AS_REPRODUCTION_AUDIT`.**

This leg builds no new mathematics, measures no new physics, re-runs no expensive
computation, and claims no novel mechanism. It is a **prose-vs-stored-data reproduction
audit** of the two writeups that pronounced stage `L1` dead — leg 54 (Route-MM, coefficient
basis) and leg 56 (Route-TN, collocation basis). Every quantity it touches is already
banked; the only thing it can produce is a **magnitude of agreement or disagreement**
between a landed sentence and the JSON that sentence cites.

`solver/` is READ-ONLY under this leg, under either branch of the gate. So are the four
audited documents and the two audited JSONs — a discrepancy is **reported with its
magnitude and escalated**, never silently corrected (ORCHESTRATION.md §7b/§8).

## 1. Prior art inside this repository, read as source and not as description

| prior pass | what it actually covered | what it did **not** cover |
|---|---|---|
| `writeup/novelty/leg_54_verify.md` + `writeup/data/leg_54_verify_headline.json` (VER-A) | re-measured **leg 53's** headline before leg 54 built on it: closed forms, `Y₀`, the `μ`-ladder positive control, and the `MM-1` inequality — 5 clause groups | says nothing about leg 54's own prose; `serves_leg: 54`, `verifies_leg: **53**` |
| `writeup/novelty/leg_54_verify_v2.md` (VER-A2) | reviewed leg 54's **v1** construction; re-ran the runner (regenerated JSON differed in `elapsed_s` alone) and hand-built four numbers of the `K = 4` cell | its four hand-built numbers are the **superseded** ones (`32.7489`, `45.3628`, `1.3852`). The corrected headline (`8.9591` / `10.4584` / `1.167×`) was written *in response to* that review and has therefore **never** been checked against the shipped JSON by anyone |
| leg 68 (Route-IX), leg 102 (Route-JR2) | presence/absence of quartet rows in `INDEX.md`/`JOURNAL.md` | presence, not arithmetic — no number is re-derived |
| leg 60 (Route-PQ), parked on `origin/leg/pq-v1` | the same audit pattern, applied to **Route-PORT v1/v2** (legs 46/47): 114 quoted numbers, **111** re-derive, 3 do not | a different pair of legs; PORT, not MM/TN |

**Nobody has audited leg 56 (Route-TN) at all.** Grep over `writeup/novelty/` and
`experiments/journal/`: `leg_56.md` exists in both (the leg's own files), and the only
other files naming leg 56 are legs 69/84/85/94, none of which re-derive a TN number — they
cite `1.85e7×` / `2.04e11×` as settled inputs. `capabilities.py:188-189` cites the same two
numbers as a cleared threshold. There is no `verify/tn-*` branch on `origin`.

## 2. The gap, quantified before deciding the leg is worth its hour

| probe | count |
|---|---|
| numeric literals in the four audited documents | `TECHNICAL_MM` **289**, `BLOG_MM` **35**, `TECHNICAL_TN` **262**, `BLOG_TN` **93** |
| of those, ever checked against the shipped JSON by a prior pass | **4** (VER-A2's hand-build, and all four are now-superseded v1 values) |
| distinct downstream artifacts that quote leg 54's corrected headline | **3** (`CONTINUATION_PROMPT.md` ×3 sites, `DIRECTION.md` cycle table, `plan_of_record.py` ban text) |
| distinct downstream artifacts that quote leg 56's two exponents | **3** (`CONTINUATION_PROMPT.md` ×2 sites, `DIRECTION.md` cycle table, `capabilities.py`) |
| legs whose *stage* rests on `L1` being dead | `NG` (the current `NEXT`), the `M2` escalation, the `V` re-open ban |
| prior in-repo reproduction audits that found a landed writeup wrong | **1 of 1 attempted** (leg 60: 3 of 114) |

The base rate is what makes this worth running: **the one time this repository pointed a
prose-vs-data ledger at a banked negative result, it found three defects.**

## 3. External literature

**Not applicable, and stated rather than skipped.** This leg makes no mathematical claim,
so there is nothing for the literature to pre-empt. The mathematical content of legs 54 and
56 already carries its own novelty verdicts (`PROCEED_NARROW` in both,
`writeup/novelty/leg_54.md` and `writeup/novelty/leg_56.md`), and this leg does not touch,
extend, or re-open either. Zero external queries were run, deliberately — a literature
search here would be theatre.

## 4. Ceiling, pre-committed

Nothing this leg produces is a statement about `HL_S2_nonsymmetric`, about the far-field
gap, about whether `L1` should be reopened, or about any link of the `L1→L4` chain. A clean
audit means *the banked sentences match the banked data* — it does **not** independently
re-confirm that `L1` is dead, because it does not re-run the computation. A dirty audit
means *a landed sentence disagrees with its own data by X*, which is a documentation fact
until an escalation says otherwise. Clay stays ~0.05% behind Walls 1 and 2 either way.

## 5. Bans checked against this leg

* *build nothing without grepping `capabilities.py` for the object first* — done; the two
  relevant entries are the `interval.py` line (leg 56's two defects cited as cleared by 6.5
  and 10.5 decades) and the certificate entries. This leg builds no solver object.
* *closing the truncation gap by extending the domain* — this leg extends nothing; it reads
  stored JSON.
* *repairing `Z₁`'s block coupling by tuning `s` / weight family / split `K` / border
  direction* — this leg proposes no repair under either gate branch.
* *reading leg 53's assembled result as a statement about `HL_S2_nonsymmetric`* — §4 above.
* every remaining live ban names an object this leg does not touch.
