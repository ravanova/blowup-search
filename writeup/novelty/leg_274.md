# leg 274 — Route-J176R2 — novelty log

**Kind.** Light correction leg. **Nothing was computed.** Every number in this leg's artifacts is
either transcribed from an already-banked file (leg 176's JSON, leg 268's companion) or is an
arithmetic difference of numbers printed in the sentence being corrected. No new measurement, no
new claim, no route.

## Novelty question

Does this leg assert anything the repository has not already established?

**No.** Explicitly:

| what leg 274 wrote | where it already existed |
|---|---|
| `σ_min` is flat (`0.139 %` / 16-fold), not truncation-independent | leg 176's own `relative_spread` field; leg 268 `K3` for the `C2` twin; PUB2 §3.5 prose since leg 268 |
| the positive limit is float64 evidence, not a proof | leg 176's `reading` field's **own last sentence** |
| the `C1` `reading` field is the upstream regeneration source | `verify_271` §6b, verbatim |
| the §3.5 decrements belong to the reciprocal | `verify_271` §5, including the fix wording leg 274 used |
| `1.804e−2 → 9.269e−3 → 4.901e−3 → 2.623e−3`, ratios `0.514 / 0.529 / 0.535` | `verify_271` §5's table; re-derived here as differences of the printed ladder |
| `16.02× … 16.22×` | the only figure not printed elsewhere; it is `increment / decrement` rung by rung |

So the novelty content is **zero**, which is the correct answer for a correction leg. The
literature question does not arise: no claim is made about any object.

## Prior art / no-collision

No external source is used. The three internal artifacts read (never edited) are
`writeup/novelty/verify_271.md`, `writeup/data/p2_route_h2c_v1_construction.json`,
`writeup/data/p2_route_h2c_v1_construction_correction_leg268.json`.

## Ceiling

Leg 176's gate answer stands. The `ℓ¹_w` converges-vs-diverges contrast — the only thing the
`‖T⁻¹‖_X` number is in PUB2 for — is **unaffected**, because `0.510/0.527/0.534` and
`0.514/0.529/0.535` are the same geometric decay and support the same conclusion. `Z₁` still
fails at `140.72`. `a = 0` only. **No ban lifts, no route is promoted, no link of the
`L1 → L4` chain moves. Clay `~0.05%`. No GA compute.**
