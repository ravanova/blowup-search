# Leg 319 — Route-P0TCR: novelty / prior-art pass

**Committed BEFORE construction**, per `CONTINUATION_PROMPT.md`. This is a repair leg (no
curve to plot — see leg 128's own precedent for that phrasing) drafted directly from leg
300's journal §7 no-branch. It carries no new mathematics and claims no novel mechanism:
it corrects a transcribed digit on landed prose. Per the novelty-pass calibration steer
(2026-08-07 §0a, ORCHESTRATION.md-carried), this is vacated, purely-internal ground —
weight goes to locating what this repository has already said, not to an external
recency check.

## What this leg is *not* allowed to re-derive

Leg 300 (`experiments/journal/leg_300.md` §3–4, landed `5e30bf3`) already re-derived the
width ratio as the closed form `(7+3√5)/2 = 6.8541019662496845...` from BCG's own
`(eq:rstar)` and `(eq:r:restriction)` at γ=7/5, and showed re-dividing leg 266's own
rounded endpoints (`0.1666667 / 0.0243163`) independently yields `6.854` — so the defect
is a single transcribed digit, not an architectural or arithmetic error. This leg does not
re-run that derivation; it cites it.

## Internal prior art located (links, not counts)

- `experiments/journal/leg_300.md` §7 (`experiments/journal/leg_300.md:81-134`) — the
  no-branch table enumerating the propagation census: 8 surfaces total, 7 already on
  `main`. This is the leg's entire territory definition.
- `experiments/JOURNAL.md:4412-4434` — leg 300's landed-history entry, independently
  confirms the "8 surfaces, 7 on main" count and the closed-form value.
- `DIRECTION.md:14000-14060` (DM update, cycle 2) — the DM's own drafting of this leg,
  including Amendment 1 to reserve leg 305 (already superseded in the DM's *prose*, not
  yet in 305's actual dispatched text at `DIRECTION.md:13472-13488`, which is why 305's
  own lines are still in-scope here).
- `writeup/CORRECTIONS.md` §12 (`writeup/CORRECTIONS.md:867-914`) — the precedent for this
  leg's own shape: an append-only, cited, single-digit/single-scalar correction entry,
  with an explicit "ceiling" paragraph stating what was *not* touched. This leg's
  `CORRECTIONS.md` append follows that template.
- `writeup/novelty/leg_128.md` — precedent for "a repair leg with no curve to plot."

## Recency check (kept at low but nonzero weight per the calibration steer)

No external literature bears on a transcribed-digit correction to this repository's own
internal cross-reference of its own prose. Nothing to check outside the repo.

## Verdict

`PROCEED_AS_DIGIT_REPAIR`. Territory: exactly the 7 surfaces `experiments/journal/leg_300.md`
§7 names (listed at the start of `experiments/journal/leg_319.md`), plus
`writeup/CORRECTIONS.md` (append), this file, and `experiments/journal/leg_319.md`.
