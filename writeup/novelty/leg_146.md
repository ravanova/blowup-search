# Leg 146 — ROUTE-CSA novelty pass (run and committed BEFORE construction)

**Route.** ROUTE-CSA: adversarial audit of `solver/certificate_shapes.py`, plus an explicit
completeness check of the shape/class enumeration that legs 54 / 58 / 126 range over.

**Verdict: `PROCEED_AS_ADVERSARIAL_AUDIT`.** This leg claims **no mathematical novelty**. It
is an instrument audit of a module this repository already owns. The novelty question that
actually binds is therefore the *internal* one — has this module already been audited
adversarially, and does the battery duplicate an existing one — plus the standing external
check that the module's subject matter is bookkeeping and not a claim.

---

## Channel 1 — has `solver/certificate_shapes.py` ever been adversarially audited?

Every commit that has ever touched the module, `git log --all -- solver/certificate_shapes.py`:

- `e4072a4` Leg 57 — the shape dichotomy as an executable ledger + the measured dial (15/15)
- `d87a6d5` Leg 57 — BLOG + TECHNICAL + journal, label BDL assumption (4) a RECONSTRUCTION
- `d1ea41f` Leg 62 — Route-CP v1, Cadiot arXiv:2505.03091 settled from the full PDF
- `59daeaf` Leg 62 — Route-CP gate answers NO

Both authoring legs shipped **construction** batteries (`test_certificate_shapes.py` gates
1–15 from leg 57, gates 16–25 appended by leg 62 — 25 gates, all present-tense correctness
checks on well-formed input). Neither ran an adversarial pass: no malformed-descriptor,
degenerate-parameter, or silent-absorption probe exists for this module.

**No prior art in-repo. The battery is new.**

## Channel 2 — does the adversarial battery duplicate one of the 22 already banked?

The banked adversarial runners, `ls experiments/p2_route_*_adversarial.py`:

`bhn, boa, bva, cda, cna, dca, fga, fia, gca, gla, gra, hha, hna, hpa, hra, ica, lha, mfg,
nka, ola, sua, wsa` — 22 runners, 22 matching `writeup/data/p2_route_*_adversarial.json`,
22 `test_*_adversarial.py`. Target modules are the numerics modules (`bordered_hl`,
`boussinesq`, `first_integral`, `gclm`, `holder_norms`, `interval`, `nk_bounds`,
`op_lower`, `spectral_utils`, `target_norm`, `weight_search`, …).

`solver/certificate_shapes.py` is **not** among them. It is also structurally unlike them:
it is a **ledger / enumeration** module, not a floating-point kernel, so the standard
absorption probes (NaN census, domain guard, dealias off-by-one) do not transfer unmodified.
The audit template that does transfer is the **completeness** half — the question leg 126
(Route-BX) implicitly rests on.

## Channel 3 — the provenance question, checked before the audit rather than assumed

`DIRECTION.md` §146 states the module "supplies the shape/class enumeration that leg 54's
battery and leg 58's theorem both range over". **Checked, and it does not.** The seven-shape
enumeration is declared at

- `experiments/p2_route_mm_v1_shape.py:134` — `SHAPES = ("block_diag", "gs_lower",
  "gs_upper", "schur", "ff_lift", "oracle_pinv", "exact_inv")` (leg 54's own runner), and
- re-declared independently at `experiments/p2_route_bx_v1_stageb.py:298` (leg 126) and
  `experiments/p2_route_ng_v1_nogo.py:123` (leg 58's `OUT_OF_CLASS`).

`solver/certificate_shapes.py` holds a different object: leg 57's `SHAPE_LEDGER` (four
published certificates × three questions — multiplier-vs-shift, block-diagonal-or-not,
tail-inverse-decay) and leg 62's `CADIOT_*` clauses. `grep -rn "ff_lift" solver/` returns
only a comment in `solver/spectral_certificate.py:552`, never `certificate_shapes.py`.

Consequence recorded **before** measuring: the direction's stated threat model (a bug here
holes 126's 1,686-configuration count *through this module*) has no code path. The real
completeness risk is the **triplicated** enumeration drifting between the three runners.
The audit tests that instead, and says so.

## Channel 4 — external: is the module's subject matter a claim of ours?

Unchanged and re-verified against the banked scope, not re-litigated:
`capabilities.py` records for this module "**SCOPE: the dichotomy is FOLKLORE IN PRINT**
(Cadiot [arXiv:2505.03091](https://arxiv.org/abs/2505.03091) sections 2 and 3 state both
halves) — this module is BOOKKEEPING that makes it executable, NOT a finding of ours."
Ledger rows: [arXiv:1503.06315](https://arxiv.org/abs/1503.06315) (Breden–Desvillettes–
Lessard), [arXiv:2210.07191](https://arxiv.org/abs/2210.07191) (Chen–Hou),
[arXiv:2505.03091](https://arxiv.org/abs/2505.03091) (Cadiot),
[arXiv:2410.05480](https://arxiv.org/abs/2410.05480) (CGL branches, leg 48's).
An audit of bookkeeping makes no new external claim, so no new external clearance is owed.

## Channel 5 — the ban list, `.venv/bin/python plan_of_record.py`

Read in full at leg start. Binding here: **"building a solver without grepping
`capabilities.py` for the object first"** — done, the module's entry is
`capabilities.py:371–396`, and nothing is rebuilt (this leg edits no solver code under
either branch). **No GA compute** — none is run. **Do not build further ℓ¹-Fourier or
collocation machinery** — none is built. **Do not re-claim leg 51's methodological finding
at full strength** — not claimed.

---

**Conclusion.** Proceed. New battery, no duplicate, no external claim, one factual
correction to the direction's premise carried into the audit as a measured item rather than
silently.
