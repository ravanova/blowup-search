# Leg 286 — Route-CNRV novelty pass (run FIRST, committed BEFORE construction)

**Leg** 286 · **Route** CNRV · **Branch** `leg/286-cnrv-v1` · **Date** 2026-08-07
**Module under check** `solver/collocation_newton.py` (read-only; territory declares no edit
to it under either gate branch)

**Gate (pre-committed, `DIRECTION.md` line 12394-12399), verbatim.** *"Does an independent
re-run confirm (a) all 41 of leg 237's reachability-battery cases now correctly reject/flag,
(b) the 2 cases reproducing leg 202's banked numbers remain correctly flagged as genuine (no
overcorrection), and (c) no collocation_newton.py-dependent banked value moved (robust sweep
for dependents)?"*

The question this pass answers before any construction: **has anyone already independently
re-run leg 248's gauge_defect repair, and is the check this leg intends to build
distinguishable from leg 248's own self-report?**

---

## Channel 1 — is this postrepair check itself already banked?

`writeup/data/p2_route_cnrv_v1_postrepair.json` — absent. `experiments/p2_route_cnrv_v1_postrepair.py`
— absent. `writeup/novelty/leg_286.md` / `experiments/journal/leg_286.md` — absent before this
commit (this file). **Novel: yes**, at the file-existence level.

The precedent this leg belongs to is explicit in `DIRECTION.md`'s own dispatch text: *"same
family as legs 228/231-234"* — the standing postrepair-verification discipline, applied here
for the second time to this same module. The first time was leg 166 (Route-CNB), which
independently re-verified leg 150's `newton_gauged`/`critical_radius` repair. This leg
independently re-verifies leg 248's (Route-CNR2) **different** repair — `ACollocation.newton`'s
`converged` verdict, not `newton_gauged` or `critical_radius`, both of which leg 248's own
journal states, and this leg will re-confirm, are byte-for-byte untouched.

## Channel 2 — the module's commit history since leg 166's own audit

`git log --oneline -- solver/collocation_newton.py`:

```
f11728d4 (ORCH integration, JOURNAL pointer only — no code)
49eb889f Leg 248: LEG — Route-CNR2 gate YES ... (the repair under test here)
1ea4ecd  Leg 0: BENCH — critical_radius node-count isolation (leg 166's gap; different method)
ab5c4de  Leg 166: LEG — Route-CNB postrepair check (no repair; NO branch on clause a)
446805b  Leg 150: LEG — the newton_gauged/critical_radius repair
d4a6387  module's creating commit
```

Only **one** commit has touched `ACollocation.newton`'s verdict or `continuation`'s branch
logic since the module's creation: `49eb889f` (leg 248). `newton_gauged` and `critical_radius`
have not moved since `1ea4ecd`. So the differential this leg must check is fully identified
and singular: `f11728d4`'s parent tree (== `49eb889f`, since the integration commit touches
only `JOURNAL.md`) against `49eb889f`'s own parent — i.e. exactly leg 248's diff, nothing more.

## Channel 3 — what leg 248 already measured, and where an independent run differs

Read in full: `experiments/journal/leg_248.md`, `writeup/novelty/leg_248.md`,
`experiments/p2_route_cnr2_v1_repair.py` (the runner, re-read for its own construction, not
imported wholesale), `writeup/data/p2_route_cnr2_v1_repair.json`.

Leg 248's self-report: 41/41 reachability cases correct (17 converged before, 17 after, 0
verdicts changed), leg 202's calibration pair still flags 2/2 (c = −6127.935 /
−306421.259 against banked −6127.94 / −306421.26), 0/12 clean-input floats moved, 0/6 ladder
rungs moved, Section F holds both gate clauses at 8 tolerances spanning 1e-14..0.5.

Four places an independent re-run is a different measurement, not a repeat, following leg
166's own Channel-3 template against the equivalent situation one repair ago:

1. **Case provenance.** Leg 248's own battery is read from **leg 237's banked JSON**
   (`writeup/data/p2_route_sirc_v1_census.json`) via leg 237's own `g5_reachability` function,
   imported. This leg imports the SAME function fresh, in its own process, rather than trusting
   leg 248's report that it did so — the standing postrepair discipline's whole point (per
   ORCHESTRATOR_PROMPT.md: "not the repairing agent's own numbers").
2. **Independent gauge_defect computation.** Leg 248 computes `gauge_defect` via
   `col.newton(...)`'s own new return field. This leg additionally recomputes the gauge defect
   **from the returned `Omega`/`c` directly**, using the formula quoted in the module's own
   docstring (`max(|g0@Omega+1|, |Omega[i1]+0.5|)`) evaluated independently of the module's
   internal bookkeeping, so a bug in how the module computes-and-returns `gauge_defect` (as
   opposed to a bug in the gate itself) would be caught rather than silently trusted.
3. **The calibration control, re-run against `profile_newton.py` directly.** Leg 202's pair is
   re-derived from `solver/profile_newton.py`'s own `TwoScaleNewton`, not read from either leg
   202's or leg 248's JSON, so an accidental edit to `profile_newton.py` (outside this repair's
   territory, but worth checking since the calibration exists to prove the census methodology
   still fires a genuine positive) would be caught.
4. **Dependent sweep.** Gate clause (c) is new relative to leg 248's own gate (which only had
   clauses (a) and (b)) — leg 248's own artifact does not claim a dependent sweep by that name.
   This leg builds one: every direct importer of `collocation_newton.newton` or `continuation`
   (not `newton_gauged`, untouched by this repair) is enumerated by grep and re-run against
   both the pre-248 and post-248 module states.

## Channel 4 — the capabilities index (the mandated grep)

`grep -n "collocation_newton" capabilities.py` → line 399-402: module
`solver/collocation_newton.py`, object *"Newton in the bounds' own basis"*, test
`test_collocation_newton.py`. No entry mentions `gauge_defect` or leg 248's repair — the
registered validated line predates it. This leg runs the registered test (read-only,
production path `newton_gauged`, unaffected by this repair) as part of the dependent sweep
rather than treating the capabilities entry as settled by leg 248's landing.

## Channel 5 — cross-repo consumers, measured not asserted

`grep -rn "collocation_newton import\|import collocation_newton"` over `.py` gives 15
importing files. Of these, only files that call `.newton(` or `continuation(` (the two
functions leg 248 touched) are in this repair's blast radius — `newton_gauged` callers are
provably unaffected (leg 248's own diff, confirmed below, does not touch that method):

- **Direct `.newton()`/`continuation()` callers:** `experiments/p2_route_cna_v1_adversarial.py`
  (leg 114, adversarial only), `experiments/p2_route_cnr_v1_repair.py` (leg 150's own repair
  artifact), `experiments/p2_route_cnr2_v1_repair.py` (leg 248's own repair artifact),
  `experiments/p2_route_sirc_v1_census.py` (leg 237's census, the battery source),
  `test_collocation_newton_adversarial.py` (line 250/256/282/325, adversarial).
- **`newton_gauged`-only callers, out of this repair's direct blast radius but swept anyway for
  completeness (c)**: `solver/turning_point.py`, `experiments/p2_route_d_v12_defect.py`,
  `experiments/p2_route_d_v13_turning.py`, `experiments/p2_route_d_v14_first_integral.py`,
  `experiments/bench_first_integral_support_guard_check.py`, `test_collocation_newton.py`,
  `test_turning_point.py`, `test_turning_point_adversarial.py`, `test_first_integral.py`.

No production/claim-bearing runner calls `.newton()`/`continuation()` directly — this
reproduces leg 237's own Section 5 finding (0/14 claim-bearing call sites) rather than
re-deriving it, and this leg's dependent sweep (c) will still re-run every one of the above
against both module states to turn "should be unaffected" into a measured "is unaffected".

## Channel 6 — territory-filename collision with leg 166's banked suite

`test_collocation_newton_postrepair.py` already exists and is banked (leg 166 / bench
`1ea4ecd`), testing a **different** repair to a **different** function
(`critical_radius`'s node-count isolation guard, not `ACollocation.newton`'s verdict). This
leg's territory names the same filename — read as the DM continuing the same postrepair-file
convention across repairs to the same module, not as license to overwrite banked content.
**Resolution, stated before construction:** this leg APPENDS a new, clearly separated section
to that file (same convention the file already uses for the leg-0/BENCH addendum at its own
top), leaving every existing check, docstring and the `_checks` list's prior entries byte-for-
byte intact. `git diff` on this file will show only additions.

## Channel 7 — the bans

`.venv/bin/python plan_of_record.py` was run at this leg's merge base (`f11728d`) and its ban
list read in full (reproduced in `experiments/journal/leg_286.md`). None touches a read-only
regression check of a repair already landed: no gCLM measurement, no Route-D bound sharpening,
no DSS re-ask, no 2D beta re-measurement, no scaling-gauge re-test, no GA compute, no
ell^1-Fourier/collocation *machinery* (this leg builds no new solver machinery — it re-runs
existing methods and independently recomputes one already-defined formula), no domain-
extension fix, no leg-53 re-reading, no repair of B's degrees of freedom. The
capabilities-grep ban is discharged by Channel 4.

---

## Conclusion of the pass

**Novel: yes.** No independent re-run of leg 248's specific repair (gauge_defect on
`ACollocation.newton`/`continuation`) exists anywhere in the repository — leg 166's banked
suite in the same-named file covers a disjoint prior repair to a disjoint function. The
sharpest reason, mirroring leg 166's own Channel 2: leg 248 wrote the patch and its own
"independent" reachability re-run in the same commit (`49eb889f`), which is exactly the
situation the standing postrepair discipline exists to re-check with numbers nobody who wrote
the patch produced.

**No construction has been written at the time of this commit.**
