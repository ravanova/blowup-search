# Leg 373 (ROUTE-HSFM) -- Novelty Pass

## Queries / actions

| action | n | notable hits |
|---|---|---|
| `WebFetch` of `arxiv.org/abs/1308.3356` (abstract page, sanity check before full-text pull) | 1 | Confirmed identity: Lucas & Kerswell, "Spatiotemporal dynamics in 2D Kolmogorov flow over large domains", JFM (doi:10.1017/jfm.2014.270) |
| `curl https://arxiv.org/pdf/1308.3356` (direct PDF fetch, independent of leg 358's earlier fetch) | 1 | HTTP 200, 4.82MB, valid PDF, 2 nominal pages (dense two-column). Same working route leg 358 and 326 both banked (direct `arxiv.org/pdf/<id>` reliable; `export.arxiv.org` API separately known rate-limited under concurrent leg load -- not re-tested here, not needed, this leg located its target by known arXiv ID, not by search) |
| `pdftotext -layout` extraction + full-text `grep -n -i "hookstep\|newton\|gmres\|continuation\|converg"` scan | 1 (23 source-line matches) | Every match read in full surrounding context, not snippet-only. Two distinct hookstep-failure instances found (P1 continuation-branch endpoints, Re 15.03-26.68, alpha=1/4; long-period non-convergence, alpha=1/8, Re=22) |
| Re-fetch of `arxiv.org/pdf/1406.1820` (Lucas & Kerswell 2015, leg 358's actual target paper) to pin UPO37's domain/Re/period against 1308.3356's failure regime | 1 | Confirmed section III is the SQUARE TORUS (alpha=1) at Re=60 -- the exact target regime -- and that UPO37 (T=19.334, s=0.375) lives there, distinct from 1308.3356's extended-domain (alpha<=1/4) material |

## What was checked against prior repo state

Leg 358's own banked record (`writeup/data/p2_route_rpol_v1.json`, `experiments/journal/leg_358.md`)
was read first, per this leg's spec. It had already fetched 1308.3356's PDF (HTTP 200 confirmed in
its own JSON) but had extracted ONLY the two Re numbers (15.03, 26.68) as a parenthetical fact --
no domain, orbit-class, task-type, or period comparison against its own target regime was performed.
This leg is the first to have actually read the surrounding text: the saddle-node/continuation
mechanism, the alpha=1/4 domain, and the second (long-period, alpha=1/8) failure instance were all
new extractions, not present anywhere in the repo before this leg.

No prior leg (grep across `writeup/novelty/*.md` and `writeup/data/*.json` for `1308.3356`) had
quoted a single sentence of 1308.3356's body text. Leg 353's novelty pass located the arXiv ID by
citation search; leg 358 fetched the PDF and extracted two numbers; neither read the mechanism.
This leg closes that gap.

## Verdict

PROCEED / bank as new. This leg's question ("what exactly fails in 1308.3356, in which regime, and
does it intersect leg 358's own retry target") had not been asked or answered by any prior leg --
the citation existed only as an unread PDF plus two numbers. The full-text read produces a
DISJOINT classification (regime boundary on domain alpha, Re, and continuation-vs-single-shot task
structure; the one matching axis -- orbit class -- is not sufficient on its own, since the target
regime's own within-regime success/failure statistics were already sourced independently via
Chandler & Kerswell 2013, leg 358's residual risk #1). Full reasoning and quotes are in
`writeup/data/p2_route_hsfm_v1.json`.

No repo file previously quoted 1308.3356's saddle-node/continuation-endpoint sentence or its
long-period non-convergence sentence. Both are new to the repo's literature record via this leg.
