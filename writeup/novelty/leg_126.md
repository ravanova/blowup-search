# Leg 126 — Route-BX novelty pass (run and committed BEFORE construction)

**Date:** 2026-08-06. **Agent:** LEG-A (critical path, stage `B`). **Branch:** `leg/126-bx-v1`.
**Verdict: `PROCEED_AS_INTERNAL_AUDIT_ONLY`.**

Links, not counts (lesson from leg 53's withdrawn count-only log). Four queries run, every URL
below reachable as given. **Nothing in this leg is claimed as new mathematics.** The leg is a
*completeness audit* of one repository's own declared search space against its own banked
refutations. Its output is a bookkeeping verdict on a measured negative — it introduces no
object, no bound, no theorem, and no compute. Under **either** branch of the gate this leg
claims no new result; on the no-branch it applies stage `B`'s pre-committed no-branch, which
was written into `plan_of_record.py` before any of legs 49–111 ran.

## 1. Is the AUDIT SHAPE published? — Yes, as a known limitation, and that is the honest frame

The question "can an automated certificate search still succeed, or is the space exhausted?"
is a recognised one, and the literature's answer is a **soundness-without-completeness**
statement, not a completeness theorem.

| ref | what it is | verdict |
|---|---|---|
| [arXiv:2309.06090](https://arxiv.org/pdf/2309.06090) / [Annual Reviews in Control (2025)](https://www.sciencedirect.com/science/article/pii/S1367578825000422), *A general framework for verification and control of dynamical models via certificate synthesis* | states the exact epistemic asymmetry this leg lives inside: counterexample-guided certificate synthesis is **sound but not complete** — a found certificate proves the property, but a *failure to find one* licenses **no conclusion** about the model | **THE FRAME. NOT NEW, AND IT BINDS THIS LEG'S WORDING.** |
| [Converse Lyapunov theorems](https://www.emergentmind.com/topics/converse-lyapunov-theorems) (survey) and [Liu, *Converse barrier functions via Lyapunov functions*](https://www.math.uwaterloo.ca/~j49liu/papers/2022/liu2022converse.pdf) | where completeness *is* available it comes from a **converse theorem** for the certificate class, and those existence results are explicitly **non-constructive** | **NO CONVERSE THEOREM EXISTS FOR THE RADII-POLYNOMIAL CLASS HERE.** |
| [Ahmadi–Parrilo CDC 2014 tutorial](https://www.princeton.edu/~aaa/Public/Publications/CDC2014_Tutorial.pdf) | approximation/converse guarantees for the SDPs that search Lyapunov functions, via convex geometry | adjacent genre; the object is a convex SOS hierarchy, not a radii polynomial |

**Consequence, binding:** this leg may **not** write "no certificate exists". The strongest
honest statement its no-branch supports is *the declared search space of stage `B`, as this
repository declared it, is covered clause-by-clause by banked refutations* — an exhaustion of
a **named enumeration**, not of the mathematics. That distinction is written into the gate
answer and the prose, not left to the reader.

## 2. Is the SEARCH LANE itself novel? — No, and C-PILOT already logged that

`writeup/data/p2_route_c_pilot_v0.json` `C0_1_novelty` recorded verdict `PROCEED_NARROW` with
the note that *"automatic search for a stability certificate is a mature field"* (2 adjacent
entries, 14 queries). This pass re-confirms and extends it with two live links:

- [*Evolutionary search for polynomial Lyapunov functions: a genetic programming method for
  exponential stability certification*](https://doi.org/10.3390/axioms14050343) (Axioms, 2025)
  — a GA over a certificate's functional form, i.e. **exactly stage `B`'s lane**, published,
  on a different certificate class.
- [arXiv:2204.12846](https://arxiv.org/pdf/2204.12846), *Evolving generalizable multigrid-based
  Helmholtz preconditioners with grammar-guided genetic programming* — evolutionary search over
  the **approximate inverse**, i.e. stage `B`'s "split/shape" degree of freedom, published, for
  a non-rigorous solver.

So `B`'s premise ("these are search problems with a fitness that cannot lie") was never a novel
idea; what was ever repository-specific is the *fitness* — searching `Z₁`/`Y₀` of a radii
polynomial — and that remains **not found published** (see §3).

## 3. Is the OBSTRUCTION published? — Searched, not found, and that is a weak inference

A direct query for a lower bound / no-go on the radii-polynomial `Z₁` when the tail operator
has a kernel in the space
[returned nothing on the obstruction](https://arxiv.org/abs/2605.07500) — the hits are
*constructive* applications of the method (the Shimizu–Morioka heteroclinic case study states
the standard four-step recipe: zero-finding formulation, approximate zero, approximate inverse,
bound estimates), plus the method's originating literature
([the radii polynomial approach](https://www.researchgate.net/publication/274384127_Rigorous_numerics_for_analytic_solutions_of_differential_equations_The_radii_polynomial_approach)).
**Absence of search hits is not absence of literature**, and **leg 52's search-index flag
STANDS** — untested and uncleared by this pass. This is the same weak inference leg 58 already
banked for its own theorem; this leg adds no strength to it and does not re-claim it.

**One link surfaced that is NOT this leg's to resolve.** The standing ban on re-claiming leg
51's methodological finding names an open question — whether Cadiot's construction covers a
zero diagonal. This pass surfaced a
[habilitation thesis collecting that line of work](https://hal.science/tel-05142432v1/file/Habilitation_main.pdf)
alongside [arXiv:2505.03091](https://arxiv.org/pdf/2505.03091). **It is logged as a pointer and
nothing more**: reading it is outside this leg's territory and its gate, the ban is unaffected
either way, and this leg quotes no claim from it.

## 4. Ban check, explicitly, before construction

`.venv/bin/python plan_of_record.py` was run first and every ban read. Those that could touch
this leg:

- **"any GA compute on an unvalidated fitness"** — **NO GA RUNS ON EITHER BRANCH.** This leg
  imports no `ga/` module and calls neither `solver.ga_search` nor
  `solver.weight_search.grid_search`. The lift condition is the frozen six-property gate's
  PASS; leg 49 gave `4/6` and leg 59 gave `5/6` with P3 worst `|slope−1|` unmoved at `0.342`
  against the `0.05` ceiling. The condition is **unmet**, and if the audit's yes-branch names a
  corner needing GA compute, that sub-case **escalates to the user** by the gate's own wording.
- **"do not repair `B`'s three degrees of freedom (space, split, shape)"** — this leg **repairs
  nothing**. It *reads* the banked refutations of all three and checks the enumeration covers
  the declared space. No `s`, no `K`, no weight family, no border direction, no shape of `A` is
  varied by anything this leg runs.
- **"do not build further `ℓ¹`-Fourier or collocation machinery for this operator"** — none is
  built. Every number quoted is read from a banked JSON or recomputed **read-only** from a
  landed module. No new solver module, no edit to `solver/`.
- **"another Route-D bound-sharpening leg"** (lifted by `B`) — not taken; this leg sharpens no
  bound.
- **"re-claiming leg 51's methodological finding at full strength"** — not done; §3 above.
- **"building a solver without grepping `capabilities.py` for the object first"** — grepped
  (`GA`, `genetic`, `search`, `fitness`, `six-property`, `certificate`, `weight_search`,
  `ga_search`). The three rows that matter, all read before writing: `solver/weight_search.py`
  (holds `BorderedCLM`, the weight fitness, `FitnessEngine`, the **frozen six-property gate**,
  `grid_search`, `lower_wall`; `validated` field carries the FAIL `4/6` as part of the entry),
  `solver/ga_search.py` (a generic real-coded GA with **no fitness of its own**), and
  `solver/energy_coercivity.py` (leg 111's third realization, whose `validated` field carries
  its own NO: every admissible weight's gap negative, window width **zero**). **No module is
  built by this leg at all** — the runner assembles an audit table.

## 5. Ceiling, pre-committed

This is bookkeeping on a measured negative. **No link of the `L1 → L4` chain can move under
either branch**, and none has moved in 125 legs. The object throughout is the `a = 0` CLM
linearisation, whose `Y₀` is exactly zero for the banned degenerate reason (clause `S7`), so
every magnitude quoted bounds `HL_S2_nonsymmetric`'s difficulty **from below, not above**.
Clay odds unchanged at **~0.05%**. On the no-branch this leg applies `B`'s own pre-committed
no-branch and claims no new stage; on the yes-branch it names a corner and `B` stays `NEXT`.
