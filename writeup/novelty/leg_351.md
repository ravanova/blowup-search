# Novelty pass — leg 351, Route-DSSP brick B3 (DSSP-BS)

Committed BEFORE any construction, per ORCHESTRATION.md §0a. Links banked, not counts (MF2).

## Subject

B3's gate (TECHNICAL_P2_ROUTEDSSP_V1.md §5.1): does Biot–Savart recovery of the velocity `V`
from the vorticity `Ω` stay on the MULTIPLIER side of the nonlocal-output rule when both are
realised in the DSSP compactified radial variable and measured in the UNWEIGHTED `L²(R³)` space
B1 pinned — i.e. is `‖V‖_∞`, `‖∇V‖_∞` finite on a Type-I-enveloped `Ω`, and is leg 332's mapping
bound `‖(V·∇)Ω−(Ω·∇)V‖ ≤ ‖V‖_∞‖∇Ω‖ + ‖∇V‖_∞‖Ω‖` non-vacuous (ratio strictly inside `(0,1)`)?

## Instrument control (MF1/MF4 discipline)

arXiv export API over **https**: alive throughout, confirmed by a repeated positive control
(`all:electron`, 200/non-empty on every check). Over the run, three consecutive non-control
queries returned **503/429/503** in a row while the positive control kept returning 200 in
between — recorded as a **REFUSAL TO MEASURE** for those three specific calls (nothing banked
from them), not as absences; after ~45s extra backoff the endpoint recovered and every
subsequent query (including a plain `all:vortex` re-run) returned 200 with non-empty results.
No compound author-name query was run this leg (MF4 not triggered). Quoted-phrase AND-conjunction
zeros below are all controlled per the standing practice: each conjunct queried singly first,
both confirmed non-empty, before the conjunction's zero is banked as a measurement.

## Queries run (links/counts, not just counts)

1. `all:"Biot-Savart" AND all:"self-similar"` → **5**. All five are unrelated turbulence/space-
   weather papers (intermittency scaling, superfluid turbulence, wall-turbulence eddies, CME
   propagation) — none touch DSS Navier–Stokes vorticity recovery. No link banked as prior art.
2. `all:"Biot-Savart" AND all:"Navier--Stokes"` (LaTeX double-hyphen form, MF1) → **10** (capped
   by `max_results`). Ten titles returned, general Navier–Stokes/vortex-blob/propagation-of-chaos
   literature. One flagged for a closer look:
   `arXiv:1811.03304v2`, "Towards a finite-time singularity of the Navier-Stokes equations" —
   title alone suggested possible overlap; not opened in full (out of this leg's construction
   scope — B3 is a numerics brick, not a certification/prior-art-adjudication brick) but banked
   here as a link for a future leg to check, since it was not adjudicated.
3. `all:"discretely self-similar" AND all:"Biot-Savart"` → **0**.
   Controls: `all:"discretely self-similar"` alone → **129** (opensearch:totalResults); this
   leg's arithmetic-of-VORTICITY-recovery angle intersects nobody's a "Biot-Savart" (283, see
   below) is the arXiv-side result. Genuine absence: no paper combines the exact phrases
   "discretely self-similar" and "Biot-Savart".
4. `all:"Biot-Savart"` alone → **283** (opensearch:totalResults, control for #3/#5).
5. `all:"vorticity" AND all:"algebraically weighted"` → **1**, and the one hit ("Vortices in
   stably-stratified rapidly rotating Boussinesq convection") is unrelated (rotating-convection
   vortex dynamics, not a function-space/weight-tolerance question). No prior art banked.
6. `all:"Type-I" AND all:"Biot-Savart" AND all:"vorticity"` → **0**.
   Controls: `all:"Biot-Savart" AND all:"vorticity"` → **83** (opensearch:totalResults, non-empty
   pair); `all:"Biot-Savart"` alone → 283 (#4). Genuine absence: nobody's paper combines all
   three exact phrases "Type-I", "Biot-Savart", "vorticity".

## Verdict

**PROCEED.** The specific combination this brick measures — Biot–Savart recovery of velocity
from a Type-I-enveloped vorticity, checked for the multiplier-vs-summand shape of the resulting
nonlinearity in an UNWEIGHTED `L²(R³)`, expressed through the DSSP log/Boyd compactified radial
variable — returns no hits under any controlled query run. The one link flagged (`1811.03304`)
is recorded but not adjudicated as decisive prior art; it is a general finite-time-singularity
numerics paper whose title alone does not establish overlap with this leg's specific mapping-
bound measurement, and this leg's construction proceeds regardless since B3's territory is a
numerics brick, not a literature-adjudication brick. `capabilities.py` was grepped before any
line of `solver/` code was written (see journal): no existing instrument computes a 3D
Biot–Savart velocity, an unweighted `L²(R³)` norm, or anything DSSP-compactified-radial-variable
specific — `solver/dssp_basis.py` (leg 350) supplies only the 1-D far-field boundary-block basis
and boundary_block/boundary_block_v scalar functions, not a Biot–Savart operator — so this leg's
new `solver/` module is not a duplicate of anything already in the repo.

No ban lifted, narrowed or argued against. `plan_of_record.py` not read for this pass beyond the
standing rule that it is never edited. No L1→L4 link moved. Clay odds ~0.05%, unchanged.
