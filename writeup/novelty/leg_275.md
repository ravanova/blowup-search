# Leg 275 — Route-P0TC2: novelty / prior-art note

**This leg makes no novelty claim, and needs none.** It is the second correction leg on leg
251's parked report: it re-names the **ansatz** from `r^{(n)}`, `n` odd and *large*, to
`r^{(3)}` — the `n = 3` branch — so that leg 251's headline naming stops contradicting leg 251's
own obligation 4. It states no new result, discovers no new object, runs no new search, and
moves no gate.

## 1. What would need a novelty pass, and why none applies

| candidate claim | is it new here? |
|---|---|
| the named Phase-1 target object | **no** — leg 251's, unchanged: 3D isentropic compressible NS at `γ = 7/5` |
| that large `n` forces `r → r*`, contradicting obligation 4 | **not this leg's** — it is **leg 265's**, and it is independently re-derived in **`writeup/novelty/verify_265.md` §B**. Cited to both, in every place the correction appears |
| `k(7/6) = 16.3479210517`, hence `n ≥ 17` for BCG's own NS theorem | **not this leg's** — leg 265 evaluated it in closed form; `verify_265` §A re-derived it to 15 s.f. from hand-transcribed equations. Leg 265's own novelty file established the number is new *to the literature* (`grep` for it across `origin/main` returned 0 hits) |
| the seven odd branches `{3,5,7,9,11,13,15}` inside `(1, 7/6]` | **not this leg's** — leg 265's, with `verify_265`'s `n = 1` off-by-one check against BCG's *"odd `n ≥ 3`"* (l.372) |
| `n = 3`'s unconditional existence for all `γ > 1` | **not anyone's here** — it is **BCG Theorem 1.1**, l.214, quoted not derived |
| `r^{(3)} ∈ (1.070374, 1.094975)`, `δ_dis ∈ (−0.5778, −0.4302)` | **not this leg's** — leg 265's bisection, re-bisected independently by `verify_265` §A |
| `δ_dis = 6r − 7` at `γ = 7/5`, and the window `(7/6, 1.1909830)` | **no** — leg 240's line and leg 251/266's arithmetic, both already on the record |
| that `n = 15` is 31–41× cheaper but rests on an unquantified *"large enough"* | **not this leg's** — leg 265's, reported **open** there and reported open here |

The one thing this leg contributes is **consistency and attribution**: leg 251's ansatz now
says what its obligations already required, and the corrected text names **both** leg 265 and
`verify_265.md` §B as the source, dated `2026-08-07`, in the journal, in the runner and in the
banked JSON.

## 2. Source discipline

* `writeup/novelty/verify_265.md` (on `origin/main`) and `experiments/journal/leg_265.md` +
  `writeup/novelty/leg_265.md` (on `origin/leg/265-p2c-v1`) were read **in full** and **not
  edited** — read-only inputs.
* BCG Theorem 1.1's verbatim statement, the Lemma-`k` bijection, and the `r*` value are carried
  **through those two documents**, which read BCG at full LaTeX source. This leg did **not**
  re-download BCG. That is a deliberate scope choice for a rework leg, and it is stated rather
  than papered over: the reading now rests on **four** independent full-text passes over the
  same e-print md5 `45ea63c45a1a199ecfb4dc4a15431600` (leg 251, leg 266, `verify_251`,
  `verify_265`/leg 265), which is a stronger provenance base than a fifth download would add.
* `experiments/journal/leg_174.md:129–132` was read to adjudicate the `r = r*` line under gate
  clause (d), and confirmed to be BCG's own §1.3 wording. Not edited.

## 3. The process finding, recorded because the standing rule asked for it

Leg 251 has now had **two** internal-consistency defects, each caught by an independent
verifier, each of the same species — *the headline naming drifted from the leg's own
obligations, while the obligations themselves were right*:

1. `verify_251` §C → leg 266: obligation 1's wording named a non-existent object (a stationary
   dissipative profile system), while the leg's architecture prose was already correct.
2. `verify_265` §B → this leg: the ansatz named large `n`, while obligation 4 was already
   pointing at the correct locus.

The standing rule made a **third** instance an escalation rather than a patch. Gate clause (d)'s
targeted read over every `r`/window statement in all three of leg 251's files found **one**
species-instance (the ansatz) with three file surfaces and two inherited citation surfaces, and
**no third independent instance** — the full census is in `experiments/journal/leg_275.md` §4,
including the one borderline statement (*"…Taylor coefficient pairs at `r = r*`"*) that was
adjudicated **cleared** because it describes BCG's own computation verbatim, not this
certificate's `r`-coverage. So the escalation branch was **not** taken.

Worth the reader's attention anyway, and `verify_265` §B says so in its own words: in both
defects the leg's *obligations* were sound and its *naming* was not. Anyone assembling the
packet should read the obligations as the authoritative layer.

## 4. What this pass does NOT establish

* It does **not** establish that a certificate at `r^{(3)}` — or anywhere in `(1, 7/6]` — is
  feasible. Leg 265 costed it; this leg does not even cost. `F_dis` **grows** like `e^{+0.5 s}`
  there, against an architecture calibrated for decay at `≤ 0.1459`, and that price is stated in
  the corrected text.
* It does **not** settle BCG Thm 1.2's unquantified *"large enough"*, so it does not settle
  `n = 15`. Open, and left open.
* It does **not** re-verify leg 251's novelty §3 determination ("nobody has enclosed it"), and
  inherits `verify_251`'s recorded caveat that no forward-citation sweep of everything citing
  `2208.09445` has been run.
* It does **not** change leg 251's **escalated** status or grant any construction authority.
* **Clay odds ~0.05%, unchanged.** No link of the L1→L4 chain moves. Naming a different branch
  of a **compressible** target is not movement toward the incompressible Clay problem, and
  Wall 2 is not crossed.
