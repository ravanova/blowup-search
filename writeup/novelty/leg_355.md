# Leg 355 — novelty / prior-art pass (ROUTE-LCB3)

**Committed before any adjudication is treated as final.** Per `CONTINUATION_PROMPT.md` and
`ORCHESTRATION.md` §0a, the check runs first.

## 0. What kind of question this is

This leg makes **no external mathematical claim**. It is the third light corrections batch
(`writeup/CORRECTIONS.md` §20 / leg 338, §22 / leg 352, in leg 338's exact shape), applying two
already-located, already source-verified fixes:

* (i) a one-line `argv` fix to `experiments/p2_route_bvrr_v1_repair.py`'s `BANKED` registry
  entry for `spike1_stepC_gate`, flagged (not applied) by leg 335 at `CORRECTIONS.md` §23.4.
* (ii) a missing-PDF guard for `experiments/p2_route_cadx_v1_scope.py`, motivated by two
  independent legs' (322, 327) smoke tests catching and reverting the same accidental
  banked-JSON rewrite.

Both premises were verified independently by this leg before either fix was applied — this leg
does not simply trust the dispatch's "already source-verified" framing.

## 1. Repo-internal prior-art check

| query | scope | result |
|---|---|---|
| `grep -n "spike1_stepC_gate" experiments/p2_route_bvrr_v1_repair.py` | the one file | One `BANKED` entry, `argv=["--logged"]`, no `--steps`, unchanged since leg 221 authored it — confirmed at leg-start, before any edit. |
| `grep -c "spike1_stepC_gate" writeup/CORRECTIONS.md` | `CORRECTIONS.md` | 1 hit at leg-start: §23 (leg 335), which diagnoses the mechanism and explicitly declines to fix it ("outside this leg's territory... flagged, not corrected, for whoever owns that file"). No prior leg has applied the fix. |
| `git log --oneline -- experiments/p2_route_bvrr_v1_repair.py` since `2868f15` (leg 335's commit) | history | No commit touches the file's `BANKED` list between leg 335 and this leg's dispatch. |
| `grep -n "PDF.is_file\|NOT_AVAILABLE" experiments/p2_route_cadx_v1_scope.py` | the one file | `verify_quotes()` already guards its own quote-check sub-result correctly (`NOT_AVAILABLE`); `main()` had no guard before writing `OUT` — confirmed at leg-start. |
| `git log --oneline -- experiments/p2_route_cadx_v1_scope.py` since leg 304's landing | history | No commit adds a write-guard; the two prior catches (legs 322, 327) reverted their own accidental writes by hand rather than fixing the script — consistent with the dispatch's framing that this is a recurring, unfixed hazard. |

**No prior leg had applied either fix.** This leg is the first.

## 2. Both premises re-verified directly (not trusted from dispatch)

**Item (i):** Read `experiments/journal/leg_335.md` in full. Leg 335's own re-derivation table
(quoted verbatim in `CORRECTIONS.md` §23.2) already shows all four banked `alpha` values
reproduce to `rel_diff` between `0.0` and `4.64e-15` when run at `--steps 2500` — the CLI default
is confirmed `400` by reading `experiments/spike1_stepC_gate.py` line 137
(`ap.add_argument("--steps", type=int, default=400)`) directly, and the banked
`writeup/data/spike1_stepC_gate.json`'s own `runs[*].steps` field reads `2500` in all four
entries, read directly rather than assumed. This leg does not stop at reading leg 335's numbers —
it independently re-runs both the full harness (`rerun_one`, differential) and a direct
`spike1_stepC_gate.py --logged --steps 2500` invocation, after applying the fix, and reproduces
the same table to the same digit (`experiments/journal/leg_355.md` §2).

**Item (ii):** Read `experiments/p2_route_cadx_v1_scope.py` in full. Confirmed directly (not
assumed from the dispatch) that `Papers/2505.03091.pdf` was absent in this leg's own worktree at
leg-start (`ls Papers/` shows only `fetch.sh` and `MANIFEST.md`, both `!`-excepted from the
`Papers/*` gitignore rule), and that running the unguarded script in that state would silently
overwrite `writeup/data/p2_route_cadx_v1.json` with a `quote_verification.status: NOT_AVAILABLE`
result — reproduced live, pre-fix, by running the script and diffing the write
(`experiments/journal/leg_355.md` §3), then reverting before the fix was applied.

## 3. Ban-check

No solver logic touched in either file — item (i) is a one-line addition to a Python list
literal (`argv=[...]`); item (ii) is an early-return guard inside `main()`, before any of the
existing computation runs. No banned family (gCLM, DSS, GA, ℓ¹-Fourier collocation,
radii-polynomial) touched anywhere. `plan_of_record.py`, `DIRECTION.md`, `CLAY_ROADMAP.md` not
opened for editing. No banked JSON's *content* is changed by either fix — both
`writeup/data/spike1_stepC_gate.json` and `writeup/data/p2_route_cadx_v1.json` are confirmed
byte-identical (sha256, `git diff`) before this leg's commit.

## 4. Outcome

Gate: **YES, both items.** Item (i) applied and its regeneration verified, twice, against the
banked artifact to float64 precision. Item (ii) applied and demonstrated both ways the gate
requires — (a) PDF-absent run no longer writes the banked JSON (loud failure, exit 1, sha256
unchanged), (b) PDF-present run reproduces the pre-fix output field-for-field except the
non-claim-bearing `elapsed_s` wall-clock key, figure byte-identical. One batched
`writeup/CORRECTIONS.md` §24 entry records both. See `experiments/journal/leg_355.md` for the
full run log and diff-check.
