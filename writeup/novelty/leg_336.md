# Leg 336 — novelty / prior-art pass (ROUTE-C305)

**Committed before any adjudication is treated as final.** Per `CONTINUATION_PROMPT.md` and
`ORCHESTRATION.md` §0a, the check runs first.

## 0. What kind of question this is

This leg makes **no external mathematical claim** and does no literature search. It is a
correction leg: it re-derives two numbers from leg 305's own already-banked ledger JSON
(`writeup/data/p2_route_dwm_v1.json`) and compares them to leg 305's own landed prose. There is
no external prior-art question to ask — the object under adjudication is entirely internal (a
repo's JSON file versus that same repo's prose describing it), so an arXiv/literature sweep would
be spending cycles on the wrong question, per the 2026-08-07 novelty-pass calibration steer
recorded in prior legs' journals (e.g. leg 339 §0).

## 1. Repo-internal prior-art check — has this correction already been made?

Searched, at the start of this leg, before any edit was made:

| query | scope | result |
|---|---|---|
| `grep -rn "largest elasticity"` | whole tree, excluding `.claude/worktrees` | Three hits, all uncorrected at leg-start: `writeup/4_p2_lottery/TECHNICAL_P2_ROUTEDWM_V1.md`, `experiments/JOURNAL.md:5018`, `writeup/INDEX.md:124`. No hit is itself a correction of the claim. |
| `grep -rn "all seven capped"` | whole tree | One hit, `TECHNICAL_P2_ROUTEDWM_V1.md`, uncorrected at leg-start. |
| `grep -rn "single most sensitive knob"` | whole tree | One hit, `BLOG_P2_ROUTEDWM_V1.md`, uncorrected at leg-start. |
| `grep -n "C11_aR1\|inert" writeup/CORRECTIONS.md` | `CORRECTIONS.md` | No pre-existing entry for leg 305 at leg-start (last section was §17, over-read closure #6). |
| `git log --oneline -- writeup/4_p2_lottery/TECHNICAL_P2_ROUTEDWM_V1.md writeup/4_p2_lottery/BLOG_P2_ROUTEDWM_V1.md` | history | Single landing commit from leg 305; no subsequent correcting commit before this leg. |

**No prior leg has made either correction.** This leg is the first to compare leg 305's prose
against leg 305's own `ledger[]` array field-by-field for these two claims.

## 2. What was verified, against what

Both corrections are **arithmetic re-reads of an already-banked JSON file**, not new
measurements:

* Claim A (elasticity): compared `M2_pct_of_window_per_1pct` across all eleven `ledger[]` rows.
  `C9_a1 = −31.0579785116809336315876452723111951987553662928548882422155%` versus
  `C1_c_lap = −13.7082039324993690892275210061938287063218550788345771728339%`. `|C9| > |C1|`.
  No computation performed beyond reading and comparing decimal strings already present in the
  JSON.
* Claim B (all seven): compared `M1_linearity_rel_dev` and `scaling_exponent_up`/`_dn` across the
  seven `capped_r_star_at_a_maximum: true` rows. Six report `~0.899…` and one-sided exponents
  `~1.996…–1.9996`; `C11_aR1` reports `0E-54` and `"flat"`/`"flat"`. No computation performed
  beyond reading the JSON.

Both are exact string/decimal comparisons against digits already committed to the repository by
leg 305 (2026-08-11, `de64fa8` per `DIRECTION.md:15263`). Nothing here could be "scooped" by
external literature, and nothing here is a statement any literature search could confirm or
refute — it is a same-repo cross-check, the kind explicitly in scope for a correction leg.

## 3. Ban-check

No solver module read, written, or imported. No banned family (gCLM, DSS, GA, ℓ¹-Fourier
collocation) touched. `writeup/data/p2_route_dwm_v1.json` read, never edited (per territory).
`plan_of_record.py` not read for content beyond the standing prohibition already known; not
edited. `DIRECTION.md` not edited.

## 4. Outcome

Gate: **YES** — both statements are corrected at measured width with inline `[CORRECTED …]`
markers, `writeup/CORRECTIONS.md` §18 is appended, the `C11_aR1` all-zero row is adjudicated
**inert-by-construction** (not missing data — its term multiplies `R₁`, which is itself exactly
zero at the evaluation point `r*` by the same structural fact that defines `r*`), and the SHARP
verdict's dependence on each corrected claim is stated explicitly: **UNMOVED** by both (see
`experiments/journal/leg_336.md` and `writeup/CORRECTIONS.md` §18.3).
