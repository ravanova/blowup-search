# Leg 372 — Route-IDXB — novelty note

Bookkeeping/evidence-script leg (the same class as legs 322 and 327): closes the three
gap-list items `writeup/build_figures.py`'s own comment reserved for "a later batched
follow-up leg" (items 9, 10 and 13 of `writeup/INDEX.md`'s quartet-gap list), by writing
standalone `*_evidence.py` scripts that read already-banked JSON and, where a figure was
already assigned or genuinely owed, rebuild it. No new measurement, no new mathematics, no
new model, and no new literature search anywhere in this leg. Nothing here is a novelty
claim of any kind — it neither asserts nor implies anything about prior art, and it does
not touch `plan_of_record.py` or any BANNED item.

Item 9 (Weight-repairs v1) turned out to be already closed by leg 138; verified and cited,
not re-touched (recreating it would be a duplicate, and this leg's own gate treats a
duplicate as a FAIL). Item 10 (Route-KA v1) and the NKR half of item 13 are known-answer
audit / repair legs whose own TECHNICAL headers state, verbatim, that no figure is owed by
established convention — the same by-design class the gap list's own item 6 already
records for Route-D. This leg does not manufacture a figure or a figure number for those
two: `writeup/INDEX.md`'s own standing practice is that legs do not pick their own figure
numbers, and neither route's own header asked for one. The M2P half of item 13 does have
an assigned figure (`fig61`, from leg 125's own runner) and that figure IS rebuilt here,
from JSON alone, with the one live-solve panel of the original `build_figure()` swapped
for a closed-form-only rendering of the same underlying claim (leg 125's, adjudicated at
leg 125 and not re-opened here).

The only judgment calls made: (i) which banked scalars to assert as self-checks in each
evidence script (chosen to match each route's own pre-stated gate wording, not invented
here), and (ii) how to render Route-M2P v1's panel (a) without re-running Newton — both are
rendering/verification choices about already-adjudicated numbers, not new claims. No
banked JSON file (`writeup/data/*.json`) was read for anything other than plotting/
asserting its existing values, and none was edited.
