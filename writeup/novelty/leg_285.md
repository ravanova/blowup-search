# Leg 285 — Route-P2S novelty / prior-art pass

**Run and committed BEFORE a line of `experiments/p2_route_p2s_v1_spec.py` existed**, per the
standing discipline (*"Novelty/prior-art pass FIRST: list the 15 absent terms and the 3 existing
ones before doing any specification work"*). Date **2026-08-07**. Branch `leg/285-p2s-v1`.

Links, not counts. Every source below resolves, or is a repository-internal locator.

**This leg is a SPECIFICATION leg.** It builds nothing, writes no solver code, touches no
`solver/` file, and pre-empts no construction decision. It converts leg 265's **count** (15 of
18 apparatus terms absent) into a per-term **specification**: what each absent term must
compute, its mathematical definition traced to a named BCG/CGSS equation or section, its
input/output contract against the 3 terms that already exist, and its position in the
dependency graph. Clay odds stay **~0.05%**; no link of the L1→L4 chain moves.

---

## 0. The 18 terms, listed before any specification work

These are leg 265's, taken verbatim from its banked ledger
`writeup/data/p2_route_p2c_v1_costing.json` at `deliverable_3_capabilities.census`, on branch
`leg/265-p2c-v1` @ `8e592d4`. This leg re-read the ledger; it did not re-run leg 265's script.

### The 15 ABSENT (zero rows in a 48-row `capabilities.py` index)

| # | term |
|---|---|
| A1 | compressible fluid model |
| A2 | compressible / 3D Euler |
| A3 | Navier–Stokes |
| A4 | adiabatic / γ-law pressure |
| A5 | sound speed / density variable |
| A6 | radial / spherical geometry |
| A7 | ODE barrier argument |
| A8 | semigroup generation |
| A9 | maximally dissipative operator |
| A10 | non-autonomous forcing |
| A11 | self-similar time variable `s` |
| A12 | algebraically weighted high-order energy |
| A13 | bootstrap / continuity argument |
| A14 | topological / Brouwer degree argument |
| A15 | ball arithmetic (Arb) or interval ODE solver (VNODE-LP) |

### The 3 PRESENT (non-zero, each adjudicated by leg 265 by reading the row)

| # | term | rows | leg 265's adjudication |
|---|---|---|---|
| E1 | shooting argument | `solver/bc_weighted_sobolev.py`, `solver/viscous_novelty.py` | float RK4 seed-finders for scalar ODEs — *"the cheap half only"*, not rigorous interval barrier/shooting for a 2D system with a degenerate saddle |
| E2 | Frobenius / Taylor recurrence at a degenerate saddle | `solver/origin_h2_certificate.py` | the row is a **quadrature** Taylor technique, not a Frobenius recurrence — *"does not transfer"* |
| E3 | spectral gap of a non-self-adjoint operator | `solver/origin_h2_certificate.py` | the `a=0` CLM operator's, capped at `a=0` (legs 163/176) — *"does not transfer"* |

**The one genuine asset leg 265 names is `solver/interval.py`** (the interval-arithmetic core,
Route-D). It is not one of the 18 rows; it is the substrate three of them would sit on.

### An independent second instrument on the absence claim — run, and it holds with one homonym

Leg 265's census reads `capabilities.py`'s **index**. This leg ran a **different** instrument —
a direct `grep` over `solver/` and `experiments/` for the absent terms' own vocabulary — because
an index can be stale in a way the tree is not. Result: **5 hits, all adjudicated by reading the
line, 0 surviving.**

* `experiments/p2_route_i_v1_driven.py:205,208,265,373` and `solver/marginal_flow.py:268` match
  `adiabatic` — but every one is **`adiabaticity`** in the continuation/slow-parameter sense
  (*"the distance to it — the adiabaticity measurement"*), **not** A4's γ-law pressure
  `p(ρ) = ρ^γ/γ`. A homonym, not a capability.
* `maximally dissipative`, `semigroup generation`, `Brouwer`, `gamma-law`: **0** in either tree.

**So A4's absence survives a second instrument, and the other 14 are absent on both.** This is
worth recording because it is the kind of check that could have cheaply falsified leg 265 and
did not.

---

## 1. In-repository prior art — has this specification already been written here?

**No. Repo-wide grep for `bill of materials`, `specification dossier`, `input/output contract`
across `experiments/` and `writeup/`: 0 hits.** The adjacent legs and what each actually owns:

* **Leg 265** (`leg/265-p2c-v1` @ `8e592d4`, parked, PR #21) — owns the **count** and the
  adjudication of the 3 non-zero rows. Its §5 stops at *"the absent apparatus is not a tuning
  job, it is a from-scratch build"*. It states **no definition, no contract, and no dependency
  order** for any absent term. That is precisely this leg's gap to fill.
* **`verify_265.md`** (on `main`) — CONFIRMED, no gap found; re-derived `k(7/6)` independently
  (agreement to 15 s.f.) and caught that BCG's `(eq:def_R2)` is typographically ambiguous with
  reading **(A)** the correct one. This leg inherits reading (A) **with the citation**, and does
  not re-derive `k`.
* **Leg 251** (`leg/251-p0t-v1` @ `873a15f`) — names the target and the five obligations. Costs
  nothing, specifies nothing.
* **Leg 260** — the build-floor rate (`5.42` legs per delivered module). This leg cites the
  rate; it does not re-derive it and does not extend it into a schedule, which would be a
  construction decision.
* **Leg 174 / 242** — the empty Grade-A × fluid-adjacent cell.

**Nothing in this repository specifies these terms. The specification form itself is new here.**

---

## 2. The nets, run live

**14 arXiv API nets**, `https://export.arxiv.org/api/query`, `max_results=60–80`,
`sortBy=submittedDate&sortOrder=descending`, run **2026-08-07** in a **leg-private** directory
(`…/scratchpad/leg285_private/`) — honouring leg 265's recorded **provenance defect**, that the
session scratchpad is shared between concurrently-running legs. Raw output banked in the leg
JSON under `novelty.nets`, every id/date/title, so the set re-derives rather than being trusted.

| net | query | hits | what it means here |
|---|---|---|---|
| N1 | `all:"imploding" AND all:"computer-assisted"` | **1** | only `2509.12435` (LP) |
| N2 | `abs:"compressible Navier-Stokes" AND abs:"self-similar" AND abs:"stability"` | 3 | includes BCG `2208.09445` itself |
| N3 | `all:"interval arithmetic" AND all:"compressible"` | 2 | **both irrelevant** (a reputation-economics paper, a 65 nm unum ALU) — the fluid intersection is **empty** |
| N4 | `all:"maximally dissipative" AND all:"computer-assisted"` | **1** | again only LP |
| N5 | `all:"VNODE-LP"` | **0** | the interval-ODE tool has **no** arXiv abstract-level fluid literature |
| N6 | `all:"ball arithmetic" AND all:"blow-up"` | **0** | |
| N7 | `abs:"computer-assisted proof" AND abs:"software" AND abs:"fluid"` | **0** | **nobody publishes a software specification for a fluid CAP** |
| N8 | `all:"Larson-Penston"` | 29 | LP lineage, mostly astrophysics |
| N9 | `abs:"imploding" AND abs:"compressible"` | 58 | the target's live field |
| N10 | `au:"Cao-Labora"` | 11 | most recent `2608.05114` (2026-08-05), two days old |
| N11 | `abs:"self-similar" AND abs:"interval arithmetic"` | 2 | incl. `2604.09949`, the recorded **negative-control** citation |
| N12 | `all:"dissipative forcing" AND all:"self-similar"` | **0** | leg 265's finding reproduced on a fifth independent run |
| N13 | `all:"rigorous numerics" AND all:"semigroup"` | 2 | both numerical-analysis, neither a certificate |
| N14 | `abs:"implosion" AND abs:"polytropic"` | 4 | none computer-assisted |

**N7 = 0 is the decisive one for THIS leg's form.** A per-term specification of the apparatus a
fluid computer-assisted proof needs is not a published genre. That does **not** make this leg a
result — it makes it internal scoping that nobody has externalised, which is exactly how it is
claimed.

**N3 = 2-with-both-irrelevant and N5 = 0 sharpen leg 265's A15.** The tool it names (VNODE-LP /
Arb) is real and public, but its use inside a *compressible fluid* certificate has exactly one
instance in the literature — LP — and that instance is **inviscid**.

### New since leg 265's pass — checked, nothing displaces it

N9 surfaces `2606.12758` (2026-06-10, *Self-similar imploding solutions of the 1D compressible
Euler equations with a far field cutoff*) and `2606.18152` (*A new class of Euler explosions*),
both **after** leg 265's window and both **inviscid Euler**. `2603.10141v2` (degenerate-viscosity
implosion) is the one leg 265 explicitly read at **abstract depth only** and inferred nothing
from; this leg does the same and inherits that residue unchanged rather than quietly upgrading
it. **No 2026 work puts a certificate inside a viscous self-similar stability step.**

---

## 3. Primary sources, downloaded by this leg

Downloaded **by this leg**, from `arxiv.org/e-print`, into the leg-private directory:

| paper | md5 | TeX lines | agrees with |
|---|---|---|---|
| BCG `arXiv:2208.09445` | `45ea63c45a1a199ecfb4dc4a15431600` | **6898** | leg 251, leg 266, `verify_251`, leg 265, `verify_265` — a **fifth** independent match |
| CGSS `arXiv:2310.05325` | `04676de9e3262b0740f4938dd8241b79` | **3916** | leg 265, `verify_265` |

Line numbers below and in the journal are therefore **directly comparable** to leg 265's and
`verify_265`'s, which is what makes every specification citation checkable rather than trusted.

---

## 4. What this leg may and may not claim

**May claim:** that the 15 absent terms now carry a definition, a contract and a dependency
position, each traceable to a named BCG/CGSS equation or section; and any term that **resists**
specification, reported at full strength as a costing finding that raises leg 265's estimate.

**May NOT claim:** novelty in the mathematics — every definition below is BCG's or CGSS's, cited
to a line. Novelty in the architecture — LP `2509.12435` already demonstrated interval arithmetic
inside a radially-imploding compressible mode-stability step and ships public code; this leg
cites it as a template exactly as leg 265 did. Any movement toward Clay — **none**; specifying
what a build would need is not building it, and the four gap-to-Clay items of leg 265 §7 (wrong
equation, wrong mechanism, Wall 2 not crossed, blow-up already proved) are unchanged by anything
here.

**May NOT do:** authorize construction. That is the user's signature, not this leg's and not the
DM's. If the user declines, this dossier is banked scoping knowledge on the secondary-goal track,
which is the disposition its own dispatch pre-commits.
