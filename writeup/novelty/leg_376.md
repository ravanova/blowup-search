# Leg 376 — Route-R3SP: novelty / prior-art pass

**Scope note, stated first.** This leg makes no new mathematical claim that could enter the
literature-novelty space. It is a straightforward, textbook `f(r) = r^ell g(r)` centrifugal
substitution applied to an operator (`-Delta + (1/2)(y.grad) + 1`), a basis (leg 313's
compactified `X = r/(1+r)`), an enrichment map (leg 350's `solver/dssp_basis.py`), and a
control pattern (leg 343's planted-well / drift-0-null pair) that are ALL already banked and
already vetted by prior legs. The only thing new is running two more angular-momentum
channels (`ell=1`, `ell=2`) of an already-measured operator through already-built apparatus.
TIER-2 (apparatus information, not a PDE/Clay claim) — see `experiments/journal/leg_376.md`.

Query log below is deliberately narrow, matching that scope: it checks whether anyone has
already published the SPECIFIC thing this leg computes (a per-`ell` spectral decomposition of
this particular rescaled operator), not the surrounding folklore (block-diagonalisation by
`SO(3)`-symmetry / Schur's lemma is standard representation theory, cited as such in the
module docstring, not searched for as if it might be novel).

**Positive control** (`export.arxiv.org` endpoint alive, `all:"Navier-Stokes"`): 3 results
returned out of 10,756 total — endpoint confirmed functioning before any zero is trusted.

| query | total results | notes |
|---|---|---|
| `all:"Navier-Stokes"` (positive control) | 10756 (3 shown) | endpoint alive |
| `all:"self-similar" AND all:"angular momentum" AND all:"Navier-Stokes"` | 0 | controlled zero |
| `all:"spherical harmonic" AND all:"rescaled" AND all:"Navier-Stokes" AND all:"spectrum"` | 0 | controlled zero |
| `all:"angular momentum channel" AND all:"blow-up"` | 0 | controlled zero |

**Assessment.** No prior-art hit for the specific combination this leg measures. This is
consistent with the scope note above: the leg is not claiming a novel operator, basis, or
mathematical technique — every piece is either standard (the `r^ell` substitution, `SO(3)`
block-diagonalisation) or already banked by a prior leg (343's operator/controls, 350's basis,
313's compactified variable). Nothing here is offered as, or should be read as, a novel
mathematical result; it is the internal apparatus-extension the task specified. No claim in
`experiments/journal/leg_376.md` rests on an absence-of-prior-art argument — this pass exists
only so the discipline (query, don't assert) is on record.
