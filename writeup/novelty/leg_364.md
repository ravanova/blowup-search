# Leg 364 — Route-NRSV: novelty / prior-art / instrument-control log

**Gate:** does the ledger's encoded NRS 1996 Theorem-1 clause (`solver/dssp_screen.py`) survive a
clause-by-clause cross-check against every legitimately obtainable statement of NRS 1996's actual
hypothesis?

**Answer: (ii) DISCREPANCY-FOUND** — see `experiments/journal/leg_364.md` and
`writeup/data/p2_route_nrsv_v1.json` for the full adjudication. This file records the literature
instrument only: what was searched, what controls were run, what was independently re-downloaded
and re-hashed, and what was banked as a refusal.

## 1. Repo-internal novelty check

`grep -rn "q ∈ (3\|q in (3\|Theorem 1.*Tsai\|restating.*NRS\|generalising NRS"` across
`experiments/journal/*.md`, `writeup/novelty/*.md`, and `solver/dssp_screen.py` finds the
Theorem-1 quote (`q ∈ (3,∞]`, captioned "restating/generalising NRS 1996") in exactly two places
in `solver/dssp_screen.py` (the leg-362 header comment, lines 364–367, and the `EXCLUDED-BY-T1`
`deciding_clause` in `_ledger_nrs_tsai_three_way()`, lines 534–539) and nowhere else in the
repository's landed record. Leg 359's own JSON (`writeup/data/p2_route_l3bd_v1.json`,
`sub_question_1.deciding_clause`) quotes the SAME Tsai Theorem-1 text with the SAME caption
("restating and generalizing NRS 1996's U in L^3 hypothesis") — leg 362 inherited this caption from
leg 359 rather than inventing it, but neither leg noticed, or was asked to check, that the quoted
range `(3,∞]` is open at 3 and therefore does not literally include the `L^3` case being described.
This leg is the first to flag the boundary mismatch as a citation discrepancy rather than pass it
through as inherited text.

## 2. Primary/secondary-source acquisition, independently re-fetched and re-hashed

Three sources attempted, all three independently re-downloaded this leg (not reused from cache
without re-verification), matching the standing practice legs 253/359/330 established:

| source | obtained | url | sha256 (this leg) | cross-check |
|---|---|---|---|---|
| Tsai 1998, ARMA 143, 29–51 | YES, full text | `https://personal.math.ubc.ca/~ttsai/publications/leray.pdf` | `6d3182d53806ce82fa0a2d834b31b758f22399ff625b8c8d7025a65f83fb8182` | **byte-identical** to leg 359's own independent pin of the same file — three separate download sessions (legs 253, 359, 364) now agree |
| Bradshaw & Tsai survey, arXiv:1802.00038 | YES, full text | `https://arxiv.org/pdf/1802.00038` | `2f5cda6a6bc63e100d30673a2051860c8aff0c1bac79376c2535e02134fb07a0` | first hash recorded for this file in repo history (leg 253 read it, recorded no hash); content matches the cached `rotated.txt`/`survey.txt` extraction leg 253 pinned by line-locator, word-for-word once line-wraps are discounted |
| NRS 1996, Acta Math 176, 283–294 | **NO — refusal to obtain, banked** | `link.springer.com/content/pdf/10.1007/BF02551584.pdf`; `projecteuclid.org/.../10.1007/BF02551584.full` | n/a | same two URLs legs 253 and 359 both tried; same failure mode reproduced independently a third time (HTTP 303 to a Springer login wall; a 1157-byte Incapsula-gated Project Euclid stub) |

**Not load-bearing**: this leg's DISCREPANCY-FOUND verdict does not require NRS 1996's own text —
it is established from what two independently obtainable sources (Tsai's own paper and the
Bradshaw–Tsai survey) BOTH say NRS 1996 is (`U ∈ L³` exactly), which is enough on its own to show
the ledger's quoted Theorem-1 range (`q ∈ (3,∞]`, open at 3) is a distinct, non-overlapping
citation from NRS's actual `q=3` result — regardless of what NRS's own paper additionally says.

## 3. Instrument control — real refusals banked honestly, not laundered

Both NRS 1996 attempts returned genuine barriers (a login-wall redirect and a bot-mitigation
paywall stub), not empty results or timeouts that could be mistaken for "tried and found nothing."
No proxy, cache-bypass, sci-hub-style mirror, or other paywall-circumvention route was attempted —
per the dispatch's rule, a firewall/paywall hit is banked as a refusal-to-obtain, never worked
around. The two obtainable sources (Tsai 1998, Bradshaw–Tsai) were both fetched over plain HTTPS
from their authors'/arXiv's own hosting, no login or paywall encountered, both returning HTTP 200
and a genuine PDF (`file` confirms `PDF document` for both downloads, 10 pages and 25 pages
respectively — plausible for a 22-page journal article and a 25-page survey chapter).

## 4. What this leg is not

No outreach to authors. No forward-citation count gathered or treated as load-bearing (this leg is
a hypothesis-text cross-check, not a novelty/prior-art sweep in the sense legs 313/326/330 ran — no
`export.arxiv.org` query was needed since both obtainable sources were located by URL, inherited
from legs 253/359's own citation trail and independently re-verified). `plan_of_record.py`,
`DIRECTION.md`, `solver/dssp_screen.py`, `capabilities.py` — read-only, untouched.
