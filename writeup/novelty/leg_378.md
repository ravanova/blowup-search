# Leg 378 — ROUTE-LCB6 — novelty / prior-art pass

**Role: record-integrity correction only (accumulator item (10), applied). No new solver
physics, no new measurement, no re-derivation of anything the trilogy already established.**

## 0. What is already known (prior art inside this repo)

The claim under correction — that the discretized essential/continuum spectrum of
`p2_route_e_v1_spectrum`'s `a=0` linearization is "theoretically pinned to `Re(lambda)=0`
exactly" — originates in leg 356's own repair journal (`experiments/journal/leg_356.md` §2,
paraphrasing `solver/rescaled_spectrum.py`'s module docstring) and its
`writeup/CORRECTIONS.md` §25(a) entry. Three already-landed legs subsequently investigated that
specific ground, none of them touching leg 356's own files (each confirmed via `git status
--short` before/after, per their own journals):

- Leg 367 (ROUTE-E5P): dispatched explicitly to verify or downgrade the claim. Found no theorem
  located and no derivation constructed (six skew-symmetrization mechanisms tried, none worked);
  classified UNVERIFIED-ASSUMPTION, empirically corroborated with ~6.5 orders of margin.
- Leg 369 (ROUTE-BSE): dispatched to try the generic route — standard nonsymmetric eigensolver
  backward-error theory. Found the asymptotic bound not licensed (precondition violated by
  4-5 orders across the plausible constant range) and the unconditional bound licensed but
  vacuous (~19 orders too loose). GAP-NAMED: that specific route is foreclosed, not merely
  unexplored.
- Leg 371 (ROUTE-SFX, terminal): dispatched to try a structural, non-generic mechanism. Found
  realness channeled through LAPACK's specific real-Schur construction explains the observed
  tightness to within a factor 0.93-1.68x across all four of leg 367's measured points,
  independently cross-checked via a second LAPACK driver (`dgees` via `scipy.linalg.schur`).
  STRUCTURE-DERIVED — but explicitly NOT from any generic/abstract non-normal-matrix argument.
  Leg 371's own §7 finalized the exact marker text for "item (10)," citing all three legs.

A repo-wide check (`grep -rn "theoretically pinned\|Re(lambda)\s*=\s*0\|Re lambda = 0"` across
`experiments/journal/*.md` and `writeup/CORRECTIONS.md`) confirms leg 356's journal §2 and
`writeup/CORRECTIONS.md` §25(a) are the only two sites making the theorem-level claim in this
repository's standing record; no other leg's file asserts it independently. This leg corrects
both — the journal site directly (in territory), the CORRECTIONS.md site by non-destructive
annotation only (per the brief, since rewriting §25's own prose is out of territory).

## 1. What this leg is, and is not

This is bookkeeping: the DM's accumulator item (10) was already finalized by leg 371 before this
leg was dispatched; leg 378's only job is to apply that finalized text at the correct site,
accurately, without re-opening or re-litigating the trilogy's own findings. No new hypothesis was
tested. No number was recomputed — every figure quoted in the marker (the K*machine_epsilon
noise floor, the 4-5/19-order gap of the generic route, the 0.93-1.68x Schur-block match) is
copied verbatim from the three source legs' own journals, not re-derived here.

## 2. Consequence for the repair and for Clay

None. `canonical_order()`'s `round_dp=6` repair (leg 356) never depended on the "exactly 0"
claim being a theorem — only on the empirical floor sitting below `1e-6`, independently
re-confirmed by leg 367 with ~6.5 orders of margin and left untouched by legs 369 and 371. This
correction is purely textual: it brings the landed record's epistemic-status language into line
with what was actually established, closing the E5-floor investigation thread per leg 371's own
terminal-leg declaration. No gate answer, repair, or consumer verdict changes.

### Ceiling

TIER 2. Correction/record leg only. No proof, no certificate, no Clay claim. Clay stays ~0.05%.
