# Leg 217 — Route-PCR novelty pass (light; repair-family precedent, legs 150–154)

**Date:** 2026-08-06. **Agent:** LEG (leg 217). **Branch:** `leg/217-pcr-v1`.
**Verdict: `PROCEED_AS_REJECTION_LAYER_REPAIR`. Novelty claimed: NONE.**

This leg repairs four defects in `solver/port_certification.py` that leg 200 (Route-PCA)
**measured, named and escalated without patch authority**. It builds no new mathematics,
proposes no new bound, and makes no constant smaller. All four mechanisms were established
as **prior art by leg 200's own novelty pass**, before leg 200 measured them:
MGS-GMRES residual gaps (Greenbaum–Rozložník–Strakoš, BIT 37 (1997)), NumPy dtype
truncation (numpy#7730, numpy#8733), unchecked stated preconditions, and NaN slipping a
comparison (this repo's own `certificate_guards.NAN_HINT_GE_ONE`). This leg claims only
the repair and its regression evidence.

`capabilities.py` was grepped first (plan-of-record ban: *"building a solver without
grepping capabilities.py for the object first"*). No ban printed by
`.venv/bin/python plan_of_record.py` reaches this leg: every live ban concerns stage `B`'s
degrees of freedom, gCLM/2D measurement, DSS, the GA's frozen fitness gate, or re-claiming
leg 51's methodological finding. **Nothing here is a bound-sharpening leg.** Every
admissible outcome of the four repairs either **rejects** an input or **corrects a dtype**;
none returns a tighter number on any previously-passing input, and the previously-passing
surface is checked **bit-identical** rather than merely close.

## 1. Is the repair novel? No — it is the sixth instance of one in-repo template

| landed repair | commit | shape |
|---|---|---|
| `solver/target_norm.py` domain guard | `dc812c1` | named warning class, warn-not-raise, propagated into the return value |
| `solver/op_lower.py` lower-bound restoration | `147e8c2` | exact rescaling; may only move the bound down |
| `solver/first_integral.py` compact-support guard | `9c08287` | `on_outside=` policy, in-support values bit-identical |
| `solver/hilbert_pointwise.py` domain guard (leg 130) | `5905138` | policy parameter, default **raise**, accepted surface bit-identical |
| `solver/hilbert_holder.py` direction guard (leg 153) | leg 153 | the leg-130 shape, plus a truncation warning |
| **this leg** | — | **default `raise`**, on the two mechanisms where the input is not invertible as advertised (M1, M4); **sign selection** where it is (M1's uniform-negative branch); the shared guard **reused not reinvented** (M2); a one-token dtype cast (M3) |

## 2. Prior repairs of this exact module — and the half that was left

`git log -- solver/port_certification.py` returns four commits, two of which are repairs of
`radii_polynomial_status` specifically:

| commit | leg | what it repaired |
|---|---|---|
| `6fe8020` | Route-K v1 | the module's own construction |
| `d0f9280` | Route-L v1 | `line_sweep_solve` added |
| `3f187d8` | leg 79 / ORCH | the constants' **hypothesis** half: 11/25 fabricated inputs closed → 0/25 |
| `bfe6883` | leg 128 | the shared hypothesis guard; 17 of leg 116's 21 false-closing certificates reject, **4626/4626 clean-input values bit-identical at 0 ULP** |

Leg 200's sharpest observation is about exactly this row: legs 79 and 128 repaired the
**hypothesis** half of `radii_polynomial_status` and left the **radius** half, in the module
that was leg 128's own bit-identical comparison target. `certificate_guards.radius_violation`
was written verbatim for the class left behind and is called only by `nk_bounds.py:561`.
M2 is therefore not a new guard: it is **the existing one, finally asked**.

Leg 128's own standard is the standard this leg is measured against and is adopted verbatim:
a rejection layer *may* turn something previously accepted into a refusal, and **must never
move a clean-input number**. Hence this leg's bit-identity checks compare against the
**pre-repair implementations reproduced verbatim inside its runner**, with `np.array_equal`,
rather than against a tolerance.

## 3. What this leg deliberately did not do

Leg 200 pins three further silent mechanisms that are **not** among this leg's four named
ones — `leading_order_solve`'s sign-blindness in `c_l` (26.0×), `stall_verdict`'s positional
ladder read (16.0 → 0.0625), and `pack`/`unpack`'s shape-blindness (1.32). Repairing them
would put a fifth, sixth and seventh mechanism in a diff that was chartered as four. They are
**re-measured post-repair** in the runner so the residue is a magnitude. **The module is not
closed**, and this file says so as loudly as the journal does.

## 4. Chain status

**No link of the L1→L4 chain moved, and none could have.** This is an instrument repair, not
a mathematical result. Clay odds unchanged at ~0.05% behind Walls 1 and 2.
