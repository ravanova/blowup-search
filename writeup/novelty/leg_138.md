# Leg 138 — Route-IX3 novelty pass (committed BEFORE any edit)

**Leg.** IX3 — third freshness audit of `writeup/INDEX.md`, window = every leg landed since
leg 108's pass. Light / mechanical-documentation leg. Read-only on everything except
`writeup/INDEX.md`, which is this route's own declared territory (the leg-68 precedent,
re-affirmed in `DIRECTION.md` §138's no-branch).

**Audit window pinned at** `34748624189c0a17a688a9195101bcfcaa2666a5` (`origin/main` HEAD at
the moment this leg began, subject `Leg 134: LEG — Route-FGB post-repair regression check…`).
Every count below is `63dc163..3474862`. Pinning is not cosmetic here: legs 136 and 141 are
already in the range and sibling legs are landing concurrently, so an unpinned window would
not be reproducible.

**Bans checked.** `.venv/bin/python plan_of_record.py` printed **13** active bans (another gCLM
measurement leg; another Route-D bound-sharpening leg; DSS re-ask; re-measuring β on the 2D
object; re-testing the scaling gauge; GA compute on an unvalidated fitness; "closure is a
property of the SPACE"; re-opening stage V as posed; closing the truncation gap by extending
the domain; the three readings of legs 51/53; aiming the port at Chen–Hou; building a solver
without grepping `capabilities.py`). **0 of 13 bind this leg.** It produces no measurement,
builds no solver, runs no GA, and touches no numeric claim — `INDEX.md`'s own header forbids
the last one outright ("Do not add or edit numeric claims here — links and one-line labels
only"), and this leg holds to that: every row it may add is links and one-line labels.

## Is this audit already done?

Every prior pass over this file or its siblings, by commit, not by recollection:

| Prior pass | Commit | Target file | Window it closed | Overlap with leg 138's window |
|---|---|---|---|---|
| pre-convention `DOCS` | `55dbc5c` | `writeup/INDEX.md` | up to Route-PORT v2 | 0 legs |
| leg 68 | `b3ef49a` | `writeup/INDEX.md` | legs 53–57 | 0 legs |
| leg 108 (Route-IX2) | `63dc163` | `writeup/INDEX.md` | legs 58–102 as they stood at `63dc163` | **window boundary** — this leg starts where it stopped |
| leg 72 | `117a176` | `writeup/JOURNAL.md` | 8 gates since the 54–57 entry | different file |
| leg 102 | `12c0894` | `writeup/JOURNAL.md` | 20 gates since leg 72's pass | different file |

`git log --oneline -- writeup/INDEX.md` returns exactly **3** commits: `63dc163` (leg 108),
`b3ef49a` (leg 68), `55dbc5c` (DOCS). The most recent is leg 108's. So `INDEX.md` has received
**0 edits across every leg that landed after `63dc163`**, and this window has **0 prior
coverage**. The audit is not a repeat.

One thing leg 108 could not have known, and it is why this window is not merely "more of the
same": leg 108's own window overlaps this one in leg number but not in content. Legs **58**,
**60** and **62** appear in leg 108's list of legs that "produced no BLOG/TECHNICAL and no
figure, by design" — and all three have since landed BLOG/TECHNICAL prose and/or registered
figures on `main`, in commits (`50c1745`, `e0eba8e`, `59daeaf`) that post-date `63dc163`. Leg
108's classification was true when written and is false at the pinned SHA. That is exactly the
staleness mode this route exists to catch, and it is a *new* mode: legs 68 and 108 both found
missing rows, neither found a row that had gone from correct to incorrect underneath it.

## The window, measured

Commits in `63dc163..3474862` carrying a `Leg N:` subject, by `N`:

`0` (repo-wide integration/bench work, not a leg) and legs
`58, 60, 62, 71, 83, 85, 89, 92, 98, 99, 100, 101, 103, 104, 105, 106, 107, 111, 112, 113,
114, 115, 116, 117, 119, 120, 121, 123, 126, 131, 134, 136, 141` — **33 numbered legs**, 34
distinct `Leg N:` prefixes including `Leg 0:`.

Note the shape: the low numbers (58, 60, 62, 71, 83, 85, 89, 92, 98, 99) are legs whose
branches merged *after* leg 108's pass, not legs leg 108 missed. Ordering by leg number and
ordering by landing date are different orderings in this repository, and an audit that assumes
they agree will mis-scope its own window.

## What the 33 legs actually produced (on-disk, per leg, unioned over each leg's commits)

- **BLOG and/or TECHNICAL under `writeup/4_p2_lottery/`: 4 legs of 33** — leg 58
  (`BLOG_P2_ROUTENG_V1.md` + `TECHNICAL_P2_ROUTENG_V1.md`), leg 62 (`BLOG_P2_ROUTECP_V1.md` +
  `TECHNICAL_P2_ROUTECP_V1.md`), leg 126 (`BLOG_P2_ROUTEBX_V1.md` +
  `TECHNICAL_P2_ROUTEBX_V1.md`), and leg 60, which **edited** `TECHNICAL_P2_ROUTEPORT_V1.md`
  and `TECHNICAL_P2_ROUTEPORT_V2.md` rather than adding a route. So **3 routes are owed a new
  table row** (NG, CP, BX); leg 60 is owed **corrections to two existing rows**.
- **Registered figure under `writeup/figures/`: 3 legs of 33** — leg 58
  (`fig55_route_ng_v1_nogo.png`), leg 62 (`fig56_route_cp_v1_cadiot.png`), leg 60
  (`fig59_route_port_v1.png` **and** `fig60_route_port_v2.png`, 2 figures).
- **Curated data JSON under `writeup/data/`: 32 of the 33 legs** (plus `Leg 0:`).
- **`experiments/journal/leg_N.md` + `writeup/novelty/leg_N.md`: 33 of 33.** Unlike leg 108's
  window, there is no `_verify`-suffix exception here — every numbered leg files both.
- **29 of the 33 legs produced no BLOG/TECHNICAL and no figure** — the "no measurement, no
  figure" convention. This is not a quartet gap and no row is owed for them.

## Three convention questions this pass has to answer, and the evidence for each

**1. Is leg 126 (Route-BX) a figure gap?** No — it is the convention's cleanest instance yet,
and it says so itself. `TECHNICAL_P2_ROUTEBX_V1.md` line 21: *"**NO FIGURE, BY DESIGN.** The
yes-branch would have measured a corner and registered `fig62`. The no-branch measures none,
and the repository's Route-D convention ('no measurement, no figure') applies."*
`BLOG_P2_ROUTEBX_V1.md` line 4 repeats it. The row is owed a `none (by design)`, not a `GAP`.

**2. Is leg 63 / Route-M2 landed?** **No — parked, and it is not on `main` at all.**
`DIRECTION.md` line 863 marks it *"(LANDED: gate YES — escalation #1, parked on `leg/m2-v1`,
not merged)"*, and line 2620 re-states *"Branch `leg/m2-v1` stays parked: READ it via
`git show`, never merge it."* Checked on disk at the pinned SHA: `git log --grep="^Leg 63:"`
returns **0 commits**; `writeup/4_p2_lottery/BLOG_P2_ROUTEM2_V1.md`,
`TECHNICAL_P2_ROUTEM2_V1.md`, `writeup/data/p2_route_m2_v1_targets.json` and
`writeup/figures/fig57_route_m2_v1_targets.png` — **0 of 4 present**. `INDEX.md` contains
**0** occurrences of "M2" and **0** of "parked". So the file neither claims it landed nor
records that it did not; the parked-escalation convention is unrepresented. A parked row is
owed, and its figure number `fig57` is *reserved*, not missing.

**3. Does leg 58 / NG count as parked too?** No, and the distinction is the whole point.
`TECHNICAL_P2_ROUTENG_V1.md` line 12 says *"This branch is therefore parked, not merged"* —
but that sentence is about the **publication-scoping escalation**, and the DM subsequently
ruled the route mergeable as-is (`ee5b2f4`, quoted in `CONTINUATION_PROMPT.md`). The evidence
is decisive: `plan_of_record.py` prints `[x] NG` **DONE**, and the route's five quartet files
are on `main`. Leg 58 is **landed with a parked escalation attached** — a third state, and any
row that flattens it to either "landed" or "parked" is wrong. Recording this distinction, not
just the two rows, is what "correctly described" means for this window.

## What is already known to be wrong, before construction

Located during this pass, all verified on disk at the pinned SHA; the runner will enumerate
them formally and the JSON will list every corrected row:

- `INDEX.md`'s **Route-PORT v1** row carries `E = GAP: none` and `F = GAP: none`, and gap-list
  **item 1** says *"no `*_evidence.py` and no registered figure anywhere"*. Leg 60 (`e0eba8e`)
  landed `experiments/p2_route_port_v1_bordered_evidence.py` and
  `writeup/figures/fig59_route_port_v1.png`. **Both halves of item 1 are now false.**
- The **Route-PORT v2** row carries the same two `GAP: none` marks, and gap-list **item 2**
  names three missing pieces. Leg 60 landed `p2_route_port_v2_reach_evidence.py` and
  `fig60_route_port_v2.png`; `BLOG_P2_ROUTEPORT_V2.md` is still absent. **2 of item 2's 3
  clauses are closed, 1 stands.**
- The **Weight-repairs v1** row carries `E = GAP: none` and gap-list **item 9** says *"with no
  `*_evidence.py`"*. But `writeup/4_p2_lottery/p2_weight_repairs_v1_evidence.py` exists and
  landed in `9d9b7ea` — the very commit that created the route, which **pre-dates leg 108**.
  This one is not staleness: it was **incorrect when leg 108 wrote it**, and it is the only
  such case found.
- Gap-list **items 4 and 11** (`writeup/README.md` stale at entry #47). Still substantially
  true — the numbered list still ends at 47, still skips 45, and still contains **0** mentions
  of Weight-repairs or Route-KA/Kawahara — but item 11's implied "untouched" is no longer
  exact: leg 60 edited entries 46 and 47 (adding `*(fig 59)*` / `*(fig 60)*` and a
  reproduction-check block). Backlog grows by a further 33 legs.
- Gap-list **items 3, 5, 6, 7, 8, 10 re-verified and stand unchanged**, including item 10
  (Route-KA v1 still TECHNICAL-only: `BLOG_P2_ROUTEKA_V1.md`, an `*_evidence.py` and a figure
  are all still absent, 3 of 5 pieces present) and item 8 (`fig48` still assigned twice).
- The **"Index currency — second pass"** paragraph asserts currency through leg 108's window.
  It is stale by **33 legs**.
- **Figure-number registry**: 58 PNGs on disk. `fig53` and `fig54` name **0** files and are
  cited by **0** writeups — `reports/TECH_DEBT_REVIEW_2026-08-05.md` row A3 independently
  calls them *"an unallocated hole below DIRECTION's pre-allocated fig55–60"* — and `fig57` is
  pre-allocated to parked leg 63 (`DIRECTION.md` line 693). Both facts are unrecorded in
  `INDEX.md`; the same A3 row also confirms gap-list item 8's duplicated `fig48`.

## Verdict

**PROCEED, and the gate answers NO.** At the pinned SHA `INDEX.md` does **not** correctly
reflect the window: 33 legs landed and 0 edits followed; 3 routes are owed rows; 2 existing
rows carry 4 `GAP` marks that are now false; 1 gap-list item was false when written; 3
gap-list items are stale in whole or in part; the parked leg 63/M2 is absent entirely; and 3
legs are actively misclassified by leg 108's own convention block. No novelty claim is made
about the mathematics — this leg asserts only which files exist at one pinned commit.
