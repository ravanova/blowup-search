# Leg 310 — ROUTE-EPAP — novelty pass

**Prediction, registered before reading `writeup/CORRECTIONS.md` in full:** leg 287's own
yes-branch instruction ("Non-portable families get CORRECTIONS.md pointers... — the 252 lesson
generalized exactly as far as the data supports") would turn out to be UNEXECUTED — i.e. that
leg 310 would need to write N new pointer entries, one per non-portable family found by leg 287's
census.

**What was actually found (falsifies the prediction above):** leg 287's own landing commit
(`b0cf565`, message: "GATE ANSWERS YES...") already appended `writeup/CORRECTIONS.md` §24 in the
SAME commit that closed the leg — leg 287 executed its own yes-branch instruction itself, before
leg 310 was ever dispatched. §24's text is a pointer for the sole family leg 287 classified
NON-PORTABLE at the primary (banking-commit) measurement, `p2_route_e_v1_spectrum`, and its
wording ("banked leaf values are environment-local... compare its findings via a fresh re-solve
in the environment you are actually working in, not by reading the banked bytes as ground truth")
matches the instruction's own suggested language essentially verbatim. §24 also cites the
specific census row (`max_rel_move = 1.880`, `62` leaves >10%, the `E5` sweep index-swap
example) — satisfying "citing leg 287's specific census row."

So this leg's actual finding is negative: there is nothing left to append. Verified directly
from `writeup/data/p2_route_epa_v1_census.json`'s `classification_counts` that leg 287's census
found exactly **one** NON-PORTABLE family at the primary measurement (`p2_route_e_v1_spectrum`;
the other nine are PORTABLE, zero IRREPRODUCIBLE-AS-BANKED) — the HEAD-comparison numbers
(`classification_counts_at_head`, which show 2 NON-PORTABLE + 1 IRREPRODUCIBLE-AT-HEAD) are
explicitly excluded from this count by leg 287's own gate answer, which attributes those HEAD
movements to source-code change, not environment-portability, and §24's own text explicitly
withholds a pointer for `p2_route_nka_v1_adversarial`'s HEAD failure on exactly that
reasoning ("no CORRECTIONS pointer is warranted for it because it is not a claim about banked
VALUES being untrustworthy").

1 non-portable family, 1 existing pointer (§24). 1:1. Nothing appended.
