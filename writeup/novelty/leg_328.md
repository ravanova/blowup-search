# Leg 328 — novelty / prior-art pass (ROUTE-ORC5)

**Committed before any adjudication.** Per `CONTINUATION_PROMPT.md` DIRECTIVE 3 and
`ORCHESTRATION.md` §0a: the pass runs first. This leg's §0a load is light by design — it is
correction/record work, not new mathematics — but the check is still run rather than skipped,
per leg 339's precedent for the same class of leg (over-read closures).

## 0. What kind of question this is

No external mathematical claim is made. This leg adjudicates the repository's own banked
prose against its own banked facts (leg 174's occupancy matrix, leg 316's reproduction of
DF-CGL, the DM's own cycle-6 ruling text). There is no literature to search that could refute
or duplicate this — the only prior-art question that bites is *repo-internal*: has the
closure #5 register entry already been written, and have the two claim-bearing sites
(`CLAY_ROADMAP.md`, `CONTINUATION_PROMPT.md`) already been corrected by someone else?

## 1. Locating searches — run at this leg's worktree HEAD (`d797e44`)

- `grep -c "closure #5" writeup/CORRECTIONS.md` → **3** at HEAD, all three inside §17 (leg
  339's closure-#6 entry, which discusses closure #5's absence — `writeup/CORRECTIONS.md:1191,
  1222, 1225`). **No dedicated closure-#5 entry exists.** This matches leg 339's own §17.5
  finding (`grep -c` returned 0 when leg 339 wrote it) — the count moved from 0 to 3 only
  because leg 339's own prose mentions the string "closure #5" three times while explaining
  that no entry exists yet. Read in context, all three confirm rather than contradict "no
  entry yet."
- `grep -rn "No certified viscous blow-up exists in any model" --include=*.md --include=*.py .`
  → exactly two hits: `CLAY_ROADMAP.md:343` (the live over-wide sentence, unfixed) and
  `experiments/JOURNAL.md:4921` (a correction narrative quoting the sentence, not asserting
  it — already correctly framed as historical).
- `grep -n "Grade-A/fluid cell is empty" plan_of_record.py CONTINUATION_PROMPT.md
  CLAY_ROADMAP.md` → present, at the measured width, in `plan_of_record.py:47` and
  `CONTINUATION_PROMPT.md:68-80`. **Absent** (in favour of the older wider sentence) in
  `CLAY_ROADMAP.md:343` — confirms this is the one remaining live site.
- `git log --oneline -1 ce74d6b` → confirms the orchestrator's commit that fixed both
  `plan_of_record.py:47` and `CONTINUATION_PROMPT.md:68-80` in one edit, per the user's
  external-review packet, dated 2026-08-11.
- `grep -n "over-read closure #5" DIRECTION.md` → two DM sites: the directive-sentence block
  (`DIRECTION.md:9856` area) and leg 303's dispatched thesis (`DIRECTION.md:13722` area), both
  carrying inline `[CORRECTED …]` markers from DM cycle 6.
- `grep -n "Item 3" DIRECTION.md` (DM cycle 6) → the `6.5457e+11` label site is ruled
  dispatched gate text, immutable, carried as a pointer only — not an edit any leg may make,
  this one included.

## 2. Has this adjudication already been done? — No, in the specific sense that matters

The register entry (the thing this leg's territory is) does not exist (§1, first bullet). Two
of the six sites (`plan_of_record.py`, `CONTINUATION_PROMPT.md`) are already fixed by the
orchestrator, and two more (`DIRECTION.md`'s directive sentence and leg 303's thesis) are
already fixed by the DM — none of that duplicates this leg's work, because this leg's
territory was always the register entry plus the one remaining live site
(`CLAY_ROADMAP.md:343`), never those four. The cycle-6b amendment removing
`CONTINUATION_PROMPT.md` from this leg's territory is honoured (§21.5 of the CORRECTIONS
entry cites it) rather than re-litigated.

## 3. What this pass licenses

Licenses locating `CLAY_ROADMAP.md:343` as the one site this leg edits, and licenses the
CORRECTIONS.md §21 entry as the first-ever write of the closure-#5 register row. Licenses
nothing about the mathematics — the Grade-A/fluid cell's emptiness is untouched, no ban
lifted, no route promoted, no link of the `L1 → L4` chain moves. Clay stays **~0.05%**.
