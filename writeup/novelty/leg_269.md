# Leg 269 — ROUTE-J176P — novelty / prior-art pass

**Committed BEFORE the correction is made.** Branch `leg/269-j176p-v1`, cut from `origin/main`
at `06e96d2`. Date 2026-08-07.

**Role: DOCS correction.** One prose transcription slip in `experiments/journal/leg_176.md`.
No code, no computation, no banked JSON touched.

---

## 1. The claim under repair

| site | prose value | banked value | gap |
|---|---|---|---|
| `experiments/journal/leg_176.md` L92 — *"max relative residual of `(L₀⁺u − f)(y)`: **4.767e−15** (max absolute `1.29e−14`)"* | `1.29e−14` | `1.4295603459064552e−14` | prose low by **9.8 %** |

Source of truth: `writeup/data/p2_route_h2c_v1_construction.json`, key path
`C0_xu_closed_form.max_abs_residual`.

## 2. Prior art — has this already been fixed?

| leg | contact with this site | status |
|---|---|---|
| **176** | produced the slip | landed on `main` |
| **249** (`ROUTE-H2CV2`, verifier) | **found** it — §W7, *"10 agree, 1 does not … a **9.8 %** discrepancy"* — but declared it outside its own territory and edited nothing | landed on `main` |
| **268** | touched `writeup/4_p2_lottery/` only; never entered `experiments/journal/` | landed on `main` |

Repo-wide grep for the literal string `1.29e−14` (unicode minus) and `1.29e-14` (ASCII) at the
merge base: **one hit**, `leg_176.md` L92. The slip never propagated — PUB2's quoting sites
(`TECHNICAL_P2_PUB2_V1.md`, `BLOG_P2_PUB2_V1.md`) quote `σ_min` and `‖T⁻¹‖_X`, not this
residual. So this is a single-site repair with no downstream fan-out, and nobody has made it.

## 3. Independent confirmation, not trust in leg 249's characterization

The JSON was re-read directly rather than taken from leg 249's quote. Every other numeric claim
in the same bullet block was re-checked against the same JSON object at the same time:

| journal prose | JSON | verdict |
|---|---|---|
| max relative residual `4.767e−15` | `C0_xu_closed_form.max_rel_residual` = `4.766912579638455e−15` | agrees |
| ladder `kpan = 12 / 24 / 48`: `5.289e−15 / 4.767e−15 / 5.160e−15` | `5.289131…e−15 / 4.766912…e−15 / 5.160134…e−15` | agrees (3/3) |
| over six data, worst `5.135e−15` | `over_seeds` max = `5.134937812813957e−15` | agrees |
| leg 163's class `2.8e−14` | `leg163_established_class` = `2.8e−14` | agrees |
| **max absolute `1.29e−14`** | `max_abs_residual` = `1.4295603…e−14` | **disagrees, 9.8 %** |

Five of six agree; the sixth is the one leg 249 named. Leg 249's characterization is confirmed
independently, and no *additional* slip exists in the neighbourhood.

## 4. What does not change

The corrected value is still below leg 163's `2.8e−14` class, so the section heading (*"YES, and
a decade better than the target"*) and both gate conjuncts read the same after the fix as before.
`gate.conjunct_2_value` is the **relative** residual `4.766912…e−15`, which was never in doubt.
The repaired number is parenthetical and load-bearing for nothing.

## 5. Novelty

None claimed. This leg produces no new mathematical content. Its only output is that one
journal line now states what the run actually measured.
