# Leg 362 — Route-B7X: novelty / prior-art / instrument-control log

**Gate:** does the extended NRS/Tsai ledger entry in `solver/dssp_screen.py` distinguish
EXCLUDED-BY-T1 / EXCLUDED-BY-T2 / NOT-REACHED-BY-ANSATZ, passing all three required controls
(planted gap-then-closure, real-object ansatz classification, byte-level regression check)?

**Answer: YES**, all three controls pass (see `experiments/journal/leg_362.md`,
`writeup/data/p2_route_b7x_v1.json`).

## 1. Repo-internal novelty check

`grep -rln "EXCLUDED-BY-T2\|NOT-REACHED-BY-ANSATZ\|decays_to_zero_at_infinity"` across the
repository (excluding this leg's own new files) returns zero hits: no prior leg implemented
Theorem 2's decay-only route or an SS/DSS ansatz classifier in code. Leg 357's
`ledger_nrs_tsai()` implemented only the L³-convergence (Theorem 1) route; leg 359 read Tsai
1998's Theorem 2 at primary text and flagged, but per its own territory did not implement, the
gap this leg closes.

## 2. Source-of-truth handling

No new primary-source acquisition this leg. Every quoted clause in `solver/dssp_screen.py`'s
new module comment is copied verbatim from leg 359's own banked record
(`experiments/journal/leg_359.md`, `writeup/data/p2_route_l3bd_v1.json`), which itself
independently re-downloaded and re-hashed Tsai 1998 and its erratum from the author's current
publications page (`https://personal.math.ubc.ca/~ttsai/publications/leray.pdf`,
sha256 `6d3182d53806ce82fa0a2d834b31b758f22399ff625b8c8d7025a65f83fb8182`, pinned by leg 359,
not re-fetched here). This leg neither re-downloads nor re-derives the mathematics; it only
implements the machine-readable consequence of leg 359's adjudication in code, per this
leg's own dispatched territory ("extending landed apparatus... not doing new 3-D
construction").

One instrument note worth banking: while reading leg 359's banked JSON
(`writeup/data/p2_route_l3bd_v1.json`), this session's own Read tool silently truncated long
JSON string values behind `<<ccr:...>>` placeholders (a context-compression artifact, not a
content problem in the file itself). Re-reading the same file with `python3 -c
"json.load(open(path))"` and printing the fields directly recovered the full text, byte for
byte matching what `grep -o` on the raw file bytes shows. Banked here as an environment fact
for any leg that hits the same tool behaviour: prefer a direct-byte read (`python3`, `grep`,
`cat`) over the Read tool when a JSON value looks suspiciously short or hash-suffixed.

## 3. What this leg is not

No outreach to authors. No new arXiv/literature search (leg 359's search already covered the
relevant terrain and found no conflicting or duplicating result — see
`writeup/novelty/leg_359.md` section 3). No forward-citation count treated as load-bearing
(none consulted). No `plan_of_record.py` or `DIRECTION.md` touch. Leg 357's and leg 359's
banked JSON files were read and, in leg 357's case, diff-checked against a fresh in-process
re-measurement (control 3) and a full re-run of its own experiment script — both confirmed
zero verdict drift — but neither file was edited; `git status` shows only new files plus the
additive edits to `solver/dssp_screen.py`, `test_dssp_screen.py`, `capabilities.py`,
`test_capabilities.py` at the end of this leg's work.
