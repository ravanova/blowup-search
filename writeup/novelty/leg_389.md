# Leg 389 novelty pass — Route-CT2C: a SECOND, CERTIFIED T2 column in `screen_candidate()`, beside the fitted one

**Committed BEFORE any construction** (ORCHESTRATION.md §5a, CONTINUATION_PROMPT.md's standing
discipline). This file is the pass. The pre-registration, the module change, the additive
battery, the runner, the JSON, fig107 and the BLOG/TECHNICAL pair land in later commits on the
same branch, in that order.

Branch point: `origin/main` = `1df7d8fd970038f6566a7c5b3435999cb6d7c7dc`.

---

## 0. Disclosure up front, so nothing below is read as more than it is

**This leg claims no novel mathematics at all.** Every mathematical object it uses is already
landed in this repository and was already disclaimed as elementary by the leg that landed it:

* the Fourier–Motzkin elimination of the amplitude from an interval-enclosed log–log tube —
  leg 382, `solver/dssp_decay_enclosure.py`, whose own novelty pass states plainly that no
  novel mathematics is claimed;
* the relative-tolerance tube `f/(1+δ) ≤ C r^{-p} ≤ f(1+δ)` and its closed-form width law
  `4·log(1+δ)/log(R₁/R₀)` — leg 386, likewise disclaimed;
* the samples→cells adapter under a declared monotonicity/modulus hypothesis — leg 385,
  likewise disclaimed;
* the rigidity ledger and its four verdicts — legs 357/359/362/366/368/370/383.

What this leg contributes is **wiring plus refusal discipline**: the screen's report path,
which today reports a *fitted* exponent only, gains a second column whose exponent is a
certified enclosure, carrying δ and the hypothesis in force in the row, and **refusing** —
INCAPACITY / EMPTY recorded, never a guess — wherever the enclosure cannot certify. The fitted
column stays exactly where it is (`CLAY_OBLIGATIONS.md` §8 bullet 2, alongside-never-replacing).

**This is not movement toward Clay.** No L1→L4 link moves in either branch of this leg's gate.
CEILING: TIER 2, and it is not this leg's to relax.

## 1. What this pass had to establish

1. Does `solver/dssp_screen.py` already consume the certified enclosure, in any form? — §2.
2. Is there a second, competing "certified column" anywhere in the tree that this would
   duplicate (the standing "do not rebuild existing capability" ban, and the standing ban on
   building a solver without grepping `capabilities.py` first)? — §2, §3.
3. Does the work re-enter any clause of `plan_of_record.py`? — §4.
4. Is there published prior art that would make the build redundant? — §5.

## 2. In-repo prior art — SWEPT, and the answer is "the instrument exists, the screen has never called it"

Sweep run at the branch point.

* `grep -rln "certified_decay_interval\|certified_decay_from_cell_enclosures\|certified_decay_from_samples"`
  over the tracked tree returns **14 files**: `solver/dssp_decay_enclosure.py`,
  `solver/dssp_decay_samples.py`, their two test files, `experiments/p2_route_dexc_v1.py`,
  `experiments/p2_route_dtol_v1.py`, `experiments/p2_route_scel_v1.py`, three journals, three
  writeups, and `capabilities.py`. **`solver/dssp_screen.py` is not among them, and neither is
  `test_dssp_screen.py` or `test_dssp_screen_t2.py`.** The screen has never called the
  enclosure. There is exactly one implementation of the enclosure and one adapter, and this leg
  adds a caller, not a second instrument.
* `grep -rn "certified" solver/dssp_screen.py test_dssp_screen.py` → **zero hits.** The word
  does not occur in the screen or its main battery.
* `solver/dssp_screen.py:35–38` (`fitted_far_field_decay_exponent`) is a bare `np.polyfit` on
  12 log-spaced radii; `solver/dssp_decay_enclosure.py:4–11` names that routine explicitly as
  the thing it does **not** replace and says the intended use is "both columns are recorded
  side by side". This leg is the first unit to actually put them side by side in the report
  path. Leg 383 (`solver/dssp_screen.py:976–1007`) closed the report path with the fitted
  exponent **per its dispatch** and deliberately did not wire in leg 382's enclosure; its
  docstring records that choice.
* `capabilities.py:812` indexes `solver/dssp_screen.py` (grep run before any construction, per
  the standing ban). Its `holds` field lists the leg 357/362/370/383 columns and no certified
  one. `capabilities.py:959` and `:1030` index the enclosure and the adapter. **Only the
  `dssp_screen.py` row is this leg's to edit**, and it ships in the same commit as the module
  change (standing rule this cycle).

## 3. What is deliberately NOT rebuilt

* No second enclosure, no second Fourier–Motzkin, no second tolerance concept, no second
  samples→cells conversion. `solver/dssp_decay_enclosure.py` and `solver/dssp_decay_samples.py`
  are **imported read-only and edited nowhere** (territory rule).
* No second hypothesis vocabulary. The `HYP_*` strings are consumed as they arrive and are
  **passed through unmodified and un-restated** — the same discipline leg 386 applied to leg
  385's strings, for the same measured reason (leg 385's control X3: a certificate of width
  `7.438494264988549e-15`, bit-indistinguishable from the true certificate of a genuine planted
  known, and false about its profile; only the recorded hypothesis separates them).
* The fitted column, its `theorem2_decay_to_zero` reading, `machine_read_ledger()`'s signature
  and defaults, and the `ledger` dict's key set are **untouched**. Leg 383's eight checks must
  pass unchanged; that is a clause of this leg's own gate.

## 4. `plan_of_record.py` — no ban is entered, none is lifted

Run at the branch point and read in full. The relevant checks:

* **"building a solver without grepping `capabilities.py` for the object first"** — the grep
  is §2 above, run before construction. Satisfied.
* **The ℓ¹-Fourier / radii-polynomial ban (re-posed 2026-08-06)** — untouched. There is no
  function space here, no truncation, no approximate inverse, no Newton step and no radii
  polynomial. The instrument being wired in is two-variable linear programming on a log–log
  tube; `solver/dssp_decay_enclosure.py:145–147` states the same for itself.
* **Both DSS-entrance bans** — untouched. This leg builds no candidate, seeds no search, and
  performs no bifurcation or periodic-orbit trawl. It reads planted fields that already exist
  in `test_dssp_screen_t2.py` and one landed closed-form witness (`solver/dssp_biot_savart.py`).
* **The gCLM bans, the 2D-object bans, the weight-exponent and block-coupling bans** — no
  contact; different object entirely.
* Nothing here promotes a route into the committed sequence; `P0` remains `NEXT`. No escalation
  is triggered by the build itself.

## 5. External prior art — checked, and the honest answer is "the general technique is old, the specific object is not published anywhere and would not be worth publishing"

Queries run against the arXiv API at the branch point (`export.arxiv.org`, HTTPS, redirects
followed; the endpoint answered **HTTP 200** on every query below — no 429s this time, unlike
leg 386's disclosed hole):

| query | results |
|---|---|
| `all:"interval arithmetic" AND all:"self-similar"` | 2 hits, both singularity-construction papers (corotational harmonic map heat flow; a 5D-lifted axisymmetric NS reduction). Neither screens a candidate's decay exponent. |
| `all:"computer-assisted proof" AND all:"Navier-Stokes" AND all:"self-similar"` | 3 hits (Leray–Hopf nonuniqueness; the 5D-lifted reduction again; "Discovery of Unstable Singularities"). None reports a certified far-field **decay-exponent enclosure** as a screening column. |
| `all:"decay exponent" AND all:"enclosure"` | **0 hits.** |

Reading of that: computer-assisted proof in fluid PDE is a large, mature literature, and
verified enclosures of scalar quantities are entirely standard practice within it. What is
absent from the search is not a technique but a *product*: nobody publishes "a screening
pipeline that reports a certified decay exponent beside a fitted one", because on its own it is
a piece of software hygiene, not a theorem. **That is exactly this leg's own claim about
itself.** The novelty verdict is therefore: *no novel mathematics, no publishable claim, and no
redundancy either* — the build is not made pointless by prior art, it is made **modest** by it.

Disclosed limits of this check: three queries, title/abstract matching only, no full-text
retrieval, and no search of the interval-arithmetic software literature (INTLAB/Arb/kv release
notes) where a generic "fit a power law rigorously" utility could plausibly exist. If such a
utility exists it would be an alternative substrate for the same job, not a reason not to wire
this repository's own landed one into this repository's own screen.

## 6. Verdict of the pass

**PROCEED, with the novelty claim set to zero.** The work is a wiring and refusal-discipline
leg: it adds a caller for an instrument this repository already owns and already validated, in
the one place the instrument was built for and has never been used. `CLAY_OBLIGATIONS.md` §6
items 1 and 2 stay OPEN in every branch of the gate — this is at most the *exponent* half of
item 1, and **not** the cutoff half, which remains unattempted. §4 stays OPEN on the
admissible-cutoff half and the absent profile regardless of what is measured here. No L1→L4
link moves. Clay stays ~0.05%.
