# Leg 241 — Route-PCRC novelty pass (light; cleanup/characterisation family)

**Date:** 2026-08-06. **Agent:** LEG (leg 241). **Branch:** `leg/241-pcrc-v1`.
**Verdict: `PROCEED_AS_CHARACTERISATION`. Novelty claimed: NONE.**

This leg characterises three already-named defects and authors a two-line correction to two
stale artifacts. It builds no mathematics, proposes no bound, moves no constant, and touches
`solver/port_certification.py` not at all. Everything it measures was named as prior art
before it was measured — by leg 200's own novelty pass, and again by leg 217's.

`plan_of_record.py` was run first. **No live ban reaches this leg.** Every printed ban
concerns stage `B`'s degrees of freedom, gCLM/2D measurement, DSS, the GA's frozen fitness
gate, the Z_1 block coupling, or the reading of leg 51's finding. Nothing here is a
measurement leg, a bound-sharpening leg, or a solver build; `capabilities.py` was grepped
before any object was constructed (the standing ban), and no object was constructed — this
leg loads two existing module states and one existing battery.

## 1. Is the characterisation novel? No — it is leg 200's own instrument, re-aimed

| leg | shape | what it measured |
|---|---|---|
| leg 116 | audit | 14 degenerate "certificates" in `nk_bounds.budget` |
| leg 128 | repair + bit-identity | the shared hypothesis guard, 4626/4626 clean values at 0 ULP |
| leg 146 | audit | the same battery shape on a sibling module |
| **leg 200** | audit | four silent mechanisms in `port_certification.py`, escalated unpatched |
| **leg 217** | repair | those four, verified, escalated **unlanded** on its gate's last clause |
| **this leg** | **cleanup + characterisation** | the two artifacts leg 217 could not reach, and the three paths it left |

The three residual paths were all named — with a magnitude — in leg 200's journal and
re-measured in leg 217's. This leg claims only that it measured them **against the module
state that will actually be on main**, on the **live** fixtures, with the control column
that could report the other answer.

## 2. The three mechanisms are each documented prior art, not discoveries

* **`leading_order_solve` sign-blindness in `c_l`** — anti-upwind differencing of a
  first-order transport term. Textbook (Godunov's condition; LeVeque, *Finite Volume
  Methods*, §4.8). It is also M1's exact structural twin inside this repo, named as such by
  leg 217.
* **`stall_verdict`'s positional ladder read** — an ordering assumption on a sequence with
  no key. This repo has hit the same class before (leg 166's node-count isolation; leg 53's
  "a quantity you cite must be measured on the matrix you actually built").
* **`pack`/`unpack` total-size-only validation** — shape-vs-size conflation under
  `np.reshape`, documented NumPy semantics; the same hazard class as the dtype truncation
  (numpy#7730, numpy#8733) that leg 200 named for M3.

## 3. What this leg deliberately did not do

It did **not** repair any of the three. Characterisation is this leg's charter; repairing a
fifth, sixth or seventh mechanism is not, and would repeat exactly the scope creep leg 217
refused. It did **not** re-litigate whether leg 217's repair is correct — that stays leg
217's own claim, and this leg treats leg 217's blob as an oracle, not as a hypothesis.

It also did **not apply** the two-artifact correction it authored and verified, because
applying it on a main-based branch turns a landed test red. See the journal; the correction
is banked in `writeup/data/p2_route_pcrc_v1_cleanup.json` under `G7`, ready to apply inside
leg 217's own landing commit.

## 4. One correction this leg makes to a prior leg's report

Leg 217's journal records that **one** banked row moves. Five do. Only
`Y0_negative_zero`'s `closes`/`verdict` move — leg 217 is right about the verdict surface —
but four further rows gain `r_min` and/or `why` in their recorded `keys` list. Measured, not
argued: `G2_blast_radius` in this leg's JSON re-runs the whole 39-case battery under leg
217's module and diffs it row-by-row against the bank. The `verdict` block is byte-identical
and the gate answer stays `yes`, which is the half leg 217 checked and got right.

## 5. Chain status

**No link of the L1→L4 chain moved, and none could have.** This is bookkeeping on an
instrument, not a mathematical result. Clay odds unchanged at ~0.05% behind Walls 1 and 2.
