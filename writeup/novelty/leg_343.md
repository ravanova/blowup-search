# Leg 343 — Route-DSSP brick B1 — novelty / prior-art pass

**Committed before adjudication of the gate**, per ORCHESTRATION.md §0a. B1's gate is an
internal instrument question about THIS repository's own compactified space and its own
existing `solver/rescaled_spectrum.py` machinery (whether the space holds leg 313's crossing
and leg 331's exponents, and whether an analogous linear operator has continuous spectrum) —
not a claim that could be scooped by external literature, in the same sense leg 341 recorded
its S1/S2 questions as "internal-audit questions about this repository's banked record." The
pass below exists to confirm that framing rather than to search for a construction to import.

## Instrument check (MF1/MF4 discipline)

`https://export.arxiv.org/api/query` live: `all:"self-similar" AND all:"Navier-Stokes"`
(positive control) → 3 titles returned immediately, non-empty, confirming the endpoint is up
before any zero is trusted.

## Queries run (links, not counts)

| query | n | notes |
|---|---|---|
| `all:"self-similar" AND all:"Navier-Stokes"` (positive control) | 3+ (truncated at max_results=3) | endpoint alive |
| `all:"continuous spectrum" AND all:"self-similar" AND all:"Navier-Stokes"` | 0 | controlled zero (see positive control above) |
| `all:"essential spectrum" AND all:"self-similar" AND all:"blow-up"` | 0 | controlled zero, same instrument, same session |

**Verdict: genuine controlled absence, not banked as a discovery.** No title/abstract on arXiv
combines these three terms for backward self-similar Navier–Stokes. This is expected and not
informative either way: whether `-Δ + (1/2)(y·∇) + 1` has continuous spectrum on an unweighted
compactified space is a standard fact in the adjacent literature on backward self-similar
parabolic operators (Escobedo–Kavian-type linearizations, where the analogous statement is
well known for the **Gaussian-weighted** Hermite/Ornstein–Uhlenbeck operator having *discrete*
spectrum and the same generator *without* the weight losing that discreteness) — but it is not
phrased in a title/abstract-searchable way that this query surfaces, and this leg does not need
an external citation for it: the gate is measured directly, in this repository's own basis, in
`experiments/p2_route_dsspb1_v1.py`, against a validation control (the driftless `-Δ+1`
operator, whose absence of discrete spectrum on `L²(R³)` *is* a textbook fact independent of
this search).

**No new prior-art item is banked from this pass.** B1's territory is the plan's own §5.1 brick
entry (`writeup/4_p2_lottery/TECHNICAL_P2_ROUTEDSSP_V1.md`), which already carries its own
literature grounding (Chae–Wolf, Pineau–Vicol, etc.) from legs 251/253/260/313/332; this leg
adds no new citation to that ledger, and correctly does not need to.
