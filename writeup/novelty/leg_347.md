# Leg 347 — Route-DSSC novelty / prior-art pass

**Committed BEFORE any adjudication.** Branch `leg/347-dssc-v1`.

Gate (pre-committed, `DIRECTION.md`): *Does the screen yield a definite per-object verdict
ledger -- passes / fails-with-clause-named -- plus the technique-level statement (does the
Gauss-Newton/envelope line change 342's creation-path cost or risk, magnitudes where
available)?*

This leg's territory does not involve a new arXiv search. Leg 344 already located, controlled
the search instrument for, and fetched `arXiv:2509.14185` to full text in the prior leg of this
same literature line — re-running that search would not surface anything new, and the standing
discipline (links not counts, MF1/MF4) governs *search instruments*, not a direct HTTP fetch of
an already-known identifier. What this leg does independently is **re-fetch the paper's own
full HTML text** (not trust leg 344's paraphrase) to extract the object-level detail — the
individual list of twelve discovered solutions, their per-family precision figures, and the
exact symmetry-class and open-problem sentences — that neither 342 nor 344 needed for their own
paper-level gates but this leg's object-level gate requires.

---

## §1 instrument note

**No arXiv search run this leg.** Two direct fetches only:
- `https://arxiv.org/abs/2509.14185` (abstract page) — confirmed, consistent with leg 344's own
  finding, that the abstract truncates under the fetch tool's quoting limit and does not carry
  the object-level detail needed.
- `https://arxiv.org/html/2509.14185v1` (full HTML) — the load-bearing fetch. Read for: exact
  object enumeration (CCF ×3, IPM ×4, Boussinesq ×5 including the unresolved 4th), architecture
  and optimizer names, per-family precision quotes, the symmetry-class restriction sentence, and
  the boundary-free-3D-Euler/NS open-problem sentences.

Both fetches returned cleanly; no 429s, timeouts, or refusals this leg. `v2` was not
independently re-checked this leg — leg 344 already confirmed `arXiv:2509.14185v2` returns
HTTP 404 (no update exists) in its own session; re-spending an instrument call to re-confirm an
already-banked environment fact would not change any verdict here, and is recorded as a
deliberate non-repeat, not an unchecked assumption.

No `au:` compound-author query was run (none was needed — the paper's own author list, already
read and banked by leg 344, is not re-derived here). MF4 does not apply to this leg's method.

## §2 what changed between leg 344's paper-level read and this leg's object-level read

Leg 344's inventory entry for `2509.14185` (`writeup/data/p2_route_pklr_v1.json`) is a single
row: architecture, precision *range* (`O(1e-13)` to `O(1e-7)`/`O(1e-11)` to `O(1e-7)` depending
on system), one aggregate failure-mode list, one inherit/rebuild classification. That is
correct and sufficient for 344's own gate (a sourced *inventory*, not an object-by-object
*screen*). This leg's independent full-HTML re-fetch confirms every fact in that row and adds,
at the granularity leg 347's spec requires:

- the precise **count and identity** of discovered objects (3 CCF, 4 IPM, 5 Boussinesq = 12,
  not previously enumerated as a list anywhere in this repo);
- that CCF's precision **degrades within its own family** by instability order
  (`O(1e-13)` at stable/1st-unstable → `O(1e-7)` at 2nd-unstable) — a fact 344's aggregate range
  captured implicitly but did not call out as an order-dependent gradient;
- the **exact symmetry-class restriction sentence** ("restricted to Ψ that lie within the same
  symmetry class as the original solution"), which supplies this leg's axisymmetric-screen-way-1
  determination for the Boussinesq objects — not quoted in either 342's or 344's record;
- the **explicit unresolved status of the 4th Boussinesq candidate**, which 344's record already
  flagged in one sentence but this leg treats as a distinct, separately-ledgered object (O12)
  because the per-object screen this leg's spec asks for requires a verdict-or-no-verdict
  decision on it specifically, not a footnote on the Boussinesq row.

No fact here contradicts leg 342's or leg 344's banked record. This is a finer partition of the
same source, read independently rather than trusted secondhand, per this leg's own stated job
(screen at object level, not paper level).

## §3 what this pass does NOT claim

No new literature was searched for or located this leg — the source paper was already found and
banked by leg 344; this leg's only independent act is a full-text re-fetch of that one already-
known paper, to extract detail at the granularity its own screening gate requires. No claim here
rests on a search zero of any kind, confirmed or unconfirmed, since no search was run. The
`v2`-does-not-exist fact is attributed to leg 344, not re-verified, and is stated as such above.

No ban touched; `plan_of_record.py` read only (via the `Read` tool, byte-exact, at
`plan_of_record.py:807-808`), never edited. No outreach.
