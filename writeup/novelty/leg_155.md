# Leg 155 — Route-JR4 novelty pass: the journal-coverage inventory, fourth window

**Run and committed BEFORE any construction.** Branch `leg/155-jr4-v1`.
Pass date 2026-08-06. **Pinned audit SHA: `e832eadf18738c10911d0fe34930a4732afd8bff`**
(`origin/main` HEAD at this leg's start, verified equal to this worktree's start commit;
sibling legs are landing in parallel, so the window is frozen here and anything landing after
it is out of scope by construction).

Following the JR1/JR2/JR3 precedent, for an audit leg the "novelty pass" is not a literature
search over the mathematics — this leg derives no mathematics. It is the **inventory** that
establishes (i) the audit is not redundant with the three prior passes, (ii) no other live or
reserve leg owns the ledger this pass reads, and (iii) the window is non-empty. Counts, not
booleans, throughout.

## 1. The plan of record was read first

`.venv/bin/python plan_of_record.py` run before anything else. `NG` is **DONE** (gate YES at
leg 58), `B` is **NEXT**. All **15** printed bans were read. **0 of 15 bind this leg**: it
imports no solver module, runs no GA compute, derives no number, builds no ℓ¹-Fourier or
collocation machinery, and re-opens no closed stage. It counts files and greps one Markdown
ledger, read-only, out of the git object store at one frozen commit.

## 2. Prior art, in-repo — the three earlier passes on this ledger and the sibling ledger

| pass | leg | ledger | window | outcome |
|---|---|---|---|---|
| Route-JR  | 72  | `experiments/JOURNAL.md` + `journal/` | through leg 70's era | narrated 8 legs (60, 64–70); deferred 58, 59, 61, 62, 63, 71; found leg 60's 2 files missing |
| Route-JR2 | 102 | `experiments/JOURNAL.md` + `journal/` | `117a176..12c0894` | gate **NO**: **0 of 20** in-window legs had a pointer; 0 per-leg files missing in-window; leg 60's 2 files still absent |
| Route-JR3 | 137 | `experiments/JOURNAL.md` + `journal/` | `12c0894..3474862` | gate **NO** on clause (b) only: (a) 35/35, (c) 35/35, leg 60's carry-over **closed**; (b) **20/35**, 15 missing; plus 1 stale assertion at `JOURNAL.md:2830` |
| Route-IX3 | 138 | `writeup/INDEX.md` | ~137 era | different ledger; not this leg's territory (leg 156 owns IX4) |

Leg 137's own audit commit on `main` is **`e4199c2`**. That is this leg's cut line: the window
is **`e4199c2..e832ead`**, which contains **53 commits**.

**Redundancy check.** JR3's window closed at `e4199c2`; this leg's opens there. The two
windows are disjoint by construction, so the audit is not a repeat of an already-answered
question. `writeup/INDEX.md` (leg 156's territory) is a different file and is neither read nor
written here. **0 file-territory overlaps** with any sibling leg.

## 3. The orchestrator's manual catch-up commit is INSIDE this window — located and scoped

This leg's dispatch flagged a manual "pointer catch-up" integration commit applied after JR3's
pass. It is **`4301c7e`** — *"Leg 0: ORCH — integration: JOURNAL.md pointer catch-up (15 legs
flagged by leg 137/JR3) plus the leg-2830 correction; …"*.

**It is inside this window, not outside it**, and it sits **5th of the window's 53 commits**
(0-based index 4, oldest-first) — i.e. near the window's opening, with **48 commits landing
after it**. It touched **2 files** (`capabilities.py`, `experiments/JOURNAL.md`; +62/−3 lines,
of which `JOURNAL.md` is +52/−2).

Because it is in-window, this audit **re-measures rather than assumes** its effect, and both
JR3 remedies are confirmed discharged at the pin:

| JR3 remedy | count at this pin |
|---|---|
| the **15 additions** (legs 58, 71, 85, 98, 100, 101, 105, 107, 108, 123, 126, 131, 134, 136, 141) | **15 / 15** now carry a strict pointer line, 1 each |
| the **1 correction** at `JOURNAL.md:2830` | **applied** — the deferral sentence no longer asserts "no answered gate" for the 5 legs that landed; only leg 63 is still named as unanswered |

**Its scope was JR3's flagged list, not the ledger's state at catch-up time.** Of this
window's legs, **1 (leg 133)** had already landed before `4301c7e` and was not included in it;
the other **16** landed in the 48 commits after it. That distinction is the mechanism behind
this pass's finding and is banked as check A6 in the runner.

## 4. The window, enumerated

Method: `git log --format='%s' e4199c2..e832ead`, filtered to subjects matching `^Leg N:` with
`N != 0` (leg 0 is repo-wide bench/orchestration work and carries no gate — the same filter
JR1/JR2/JR3 used), deduplicated, sorted numerically.

**17 legs landed on `main` inside the pinned window:**

    122, 124, 127, 128, 133, 135, 138, 139, 140, 142, 144, 146, 147, 149, 157, 158, 161

Legs 157, 158 and 161 carry numbers above this leg's own, out of dispatch order; they landed
inside the pinned window and are audited like any other. **The window is defined by landing,
not by leg number** — which is why it is pinned to a SHA.

## 5. Coverage counts, measured at the pinned SHA (magnitudes, not booleans)

| quantity | count |
|---|---|
| legs landed on `main` inside the window (`N != 0`) | **17** |
| of those with `experiments/journal/leg_N.md` on `main` | **17 / 17** |
| of those with `writeup/novelty/leg_N.md` on `main` | **17 / 17** |
| of those with a `JOURNAL.md` narrative pointer line | **0 / 17** |
| of those **missing** a pointer line — the gap | **17** |
| JR3's 15 flagged pointers, re-checked | **15 / 15 present** (closed) |
| JR3's 1 stale assertion, re-checked | **applied** (closed) |
| standing carry-over files from prior passes still missing | **0** |

The pointer predicate is the ledger's own established line shape,
`^\s*-\s*\*\*Legs? N\b`. It is cross-checked against a deliberately looser control
(`\bLegs? N\b` anywhere on any line) that **can** report the other answer — lesson 90. For all
17 in-window legs the loose count is **also 0**, so **0 borderline legs**: the gap of 17 is
regex-independent. (The same control *did* come out differently at JR3, on exactly 1 leg, and
following it is what produced JR3's second defect — so its silence here is informative, not
vacuous.) `experiments/JOURNAL.md` is **3091** lines at the pin.

## 6. Repo-wide cross-check, outside the window

Widening from the window to the whole of `main`'s history: **87** distinct legs with `N != 0`
have landed (up from JR3's 67).

| quantity | count |
|---|---|
| landed legs with no `experiments/journal/leg_N.md` | **0 of 87** |
| landed legs with no `writeup/novelty/leg_N.md` | **0 of 87** |
| journal files for legs that never landed (orphans) | **0** |
| novelty files for legs that never landed (orphans) | **0** |

Both per-leg directories resolve to **exactly the 87-leg landed set**, in both directions.
`experiments/journal/` holds **93** entries and `writeup/novelty/` **95**; the surplus is
non-leg-numbered by name and accounted for, not unexplained — `README.md` and
`leg_0_bench_redtests.md` in both, plus `leg_54_verify_v2.md` in novelty. So the per-leg-file
half of the ledger is complete **repo-wide**, holding the property JR3 first established and
extending it across 20 more landed legs.

## 7. Verdict

**PROCEED_AS_AUDIT.** The window is non-empty (17 legs), disjoint from all three prior passes,
and the gate can come out either way on the evidence gathered — it does not come out the same
on its three clauses, which is itself the finding. Clauses (a) and (c) answer **YES** at 17/17
each and both JR3 carry-overs are **closed**; clause (b) answers **NO** at 17 missing pointers
of 17.

The shape worth recording is a **cadence**, not an endpoint (lesson 72): the narrative half of
this ledger is not maintained per-landing but repaired in **batches scoped to the previous
audit's flagged list**, so within one wave of landings it returns to a **total** gap. JR2
measured 0/20, JR3 measured 20/35 (partial, immediately after nothing), and JR4 measures 0/17
again — the 20/35 was the batch repair showing through, not a change in maintenance habit.

Per this leg's instructions the five shared ledgers are integration-owned, so — like JR3 and
unlike JR2, which owned `JOURNAL.md` — **this leg edits nothing**: the 17 missing pointer lines
are reported for the orchestrator to add, and no file is created on any other leg's behalf.

No number in this log is re-derived by this leg; every count is a file-existence or line-match
measurement at the pinned SHA, reproducible by re-running the runner at any later date.
