# Leg 307 — Route-TSCX novelty pass (run before construction)

**Date:** 2026-08-12. **Branch:** worked in an isolated detached worktree off `origin/main`.
**Verdict: `PROCEED_AS_VERIFICATION`.** This leg builds no new mathematics, sharpens no bound,
and claims no novel mechanism of its own. It is an independent reproduction leg: does leg
221's flagged two-scale counterexample in `solver/boussinesq_rescaled.py`'s
`odd_field_x_slope` (43.2% error / "86x tolerance", banked in
`writeup/data/p2_route_bvrr_v1_repair.json`'s `residual_margin_probe`) reproduce independently,
and if so, is its mechanism reachable from a banked result (genuine) or only from the probe's
own hand-built input (artifact)? Per `writeup/novelty/README.md`: **links, not counts.**

---

## In-repo questions (answered from the repository, not the web)

### R1. Has this specific flagged case been reproduced or adjudicated by any other leg already? **No.**

`grep -rl '43.2\|two-scale\|two_scale' writeup/novelty/*.md experiments/journal/*.md` over the
whole repository turns up exactly one other hit that even contains the string "two-scale":
`experiments/journal/leg_330.md` / `writeup/novelty/leg_330.md`, which is the unrelated
gCLM two-scale survival-boundary result (a different route, a different equation, a different
meaning of "two-scale" — a survival boundary in parameter space, not a two-radial-scale test
field). No file in the repository other than `experiments/journal/leg_221.md`,
`writeup/data/p2_route_bvrr_v1_repair.json`, and `solver/boussinesq_rescaled.py`'s own
docstring (the DEFECT B paragraph) mentions this specific case before this leg. `DIRECTION.md`
itself (leg 307's own spec, and the surrounding orchestrator/DM commentary at the lines
grepped for `43.2%/86x`) is the only other place it is discussed, and only as a flag awaiting
this leg, never as an adjudication.

### R2. Is this a web-literature question at all? **No — it is a pure numerical-reproduction question inside this repository's own code.**

The object under test is this repository's own `odd_field_x_slope` (Spike 1 Step B,
`solver/boussinesq_rescaled.py`), a heuristic origin-slope fit built for this repository's
rescaled-Boussinesq solver. There is no external literature to check: the question is whether
a specific Python function, called on a specific synthetic field on a specific grid, returns
what leg 221 says it returns, and whether the field that triggers the discrepancy is one this
repository's own physics ever actually produces. Both are settled by execution, not by
citation — this pass exists to record that no citation search is owed here, per the "links,
not counts" standard applied to a leg that generates none.

### R3. Does leg 221's own flag, read closely, already answer the gate itself (making this leg's dispatch moot)? **No — leg 221's own gate explicitly declines to answer it.**

`experiments/journal/leg_221.md` §4 states the two-scale case "is outside leg 205's battery
and outside this leg's gate" and is "handed to the post-repair-verification leg" (this leg,
per `DIRECTION.md`'s dispatch chain). §2b repeats this for the related `spike1_stepC_gate`
reproducibility finding. Leg 221's own gate (clause (a): leg 205's battery; clause (b): zero
contamination on banked calls) is scoped narrowly enough that the two-scale case never enters
either clause's yes/no — it is reported as a magnitude, explicitly not adjudicated. This leg's
own precondition (`DIRECTION.md`: "Preconditions: Leg 221 landed with the counterexample flag
unresolved by its own gate") is therefore confirmed true directly from leg 221's own text, not
merely from the orchestrator's summary of it.

### R4. Precedent shape for a pure-reproduction leg in this repository. **Legs 205→221 is the direct precedent**, and legs 129/130/150/151/152 (cited by leg 221's own novelty pass) are the wider family: escalate-with-magnitude, then a distinct leg independently re-derives before anyone treats the flag as settled either way. This leg follows the same shape one level further down the chain, and is explicit that leg 221's own diagnosis is *input*, not a substitute for its own fresh execution (`DIRECTION.md`'s own wording, restated in the DM's cycle-10f ruling).

---

## What this leg does NOT claim

No new mathematics, no bound, no movement on any link of the `L1→L4` chain, no statement
about the Chen–Hou constants, and no repair to `solver/boussinesq_rescaled.py` (read-only in
this leg's territory, confirmed unedited — see `experiments/journal/leg_307.md`). The only
claim is a measured one: the flagged number reproduces bit-for-bit, its mechanism is
independently re-derived (not merely quoted), and the mechanism's realization is measured to
require a synthetic input this repository's own banked physics does not produce — see the
runner's `verdict` block in `writeup/data/p2_route_tscx_v1.json` for the full reasoning.

## Files

* `experiments/p2_route_tscx_v1.py` — the runner: reproduction, tolerance-arithmetic check,
  mechanism re-derivation, reachability checks.
* `writeup/data/p2_route_tscx_v1.json` — every measured number.
* `experiments/journal/leg_307.md` — the leg's journal entry.

## Process notes

* `plan_of_record.py` run first; no ban in force touches this route.
* Worked in an isolated `git worktree --detach` off `origin/main`, per standing practice.
* `solver/boussinesq_rescaled.py` read-only throughout — grepped for edits before commit,
  none found.
