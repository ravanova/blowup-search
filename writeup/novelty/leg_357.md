# Novelty pass — leg 357, Route-DSSP brick B7 (DSSP-SCREEN)

Attempted BEFORE construction began, per ORCHESTRATION.md §0a, following leg 354's own
recorded precedent at the identical decision point: B7 is a numerics/diagnostics
construction brick (not a literature-adjudication brick), so a degraded external search
endpoint does not block construction once the internal, network-independent check is
decisive.

## Subject

B7's gate (`writeup/4_p2_lottery/TECHNICAL_P2_ROUTEDSSP_V1.md` §5.1): does every candidate
carry its admissibility screen — `‖V‖_{L³(R³)}`, the fitted far-field decay exponent, `λ`,
an axisymmetry diagnostic — computed at every step, with the rigidity ledger (NRS/Tsai;
Chae-Tsai; Pineau-Vicol) machine-read rather than transcribed?

## Instrument control (MF1/MF4 discipline)

The https export API's positive control (`all:electron`, no quoting, no AND) returned
`200` on **two separate, bracketing attempts** (before and after the substantive-query
attempts below, ~70s apart), confirming the endpoint was reachable in principle throughout
this leg's window.

Every substantive query attempted — three formulations, covering this brick's core
identifiers (`"admissibility screen" AND "self-similar"`, `"Newton-Krylov" AND
"Navier-Stokes" AND "screen"`, `"L3 norm" AND "self-similar" AND "axisymmetry"`) — returned
`429` on every attempt, spaced 20–25s apart with the positive control bracketing them on
both sides. This is a genuine **REFUSAL TO MEASURE** for every substantive query this leg
tried, in the same pattern leg 354 recorded at B4's identical decision point (positive
control alive throughout, every compound/quoted query refused). **Zero external prior-art
counts are banked below**, and none should be read as measured.

## What was checked instead (network-independent)

`capabilities.py` (53 entries before this leg's own addition) was grepped for `screen`,
`rigidity`, and `ledger` before any line of `solver/dssp_screen.py` was written. `screen`
and `rigidity` return no hits at all pre-existing in the index. `ledger` returns four
unrelated hits (`solver/literature_gates.py`, `solver/certificate_shapes.py`,
`solver/viscous_novelty.py`, `solver/target_selection.py`) — a published-results-as-gates
machinery, a CAP-certificate shape classifier, a dissipation-regime certification gate, and
a target-selection index respectively; none computes `‖V‖_{L³}`, a decay exponent, `λ`, or
an axisymmetry diagnostic, and none reads legs 326/330's landed JSON records
programmatically. This reconfirms leg 334's live-read finding (quoted at
`TECHNICAL_P2_ROUTEDSSP_V1.md` §3.2): the index holds no admissibility-screening machinery
for this equation family. `solver/dssp_biot_savart.py` (leg 351) and `solver/dssp_step.py`
(leg 354) were re-read in full (required background for this leg, both read-only) and
confirmed to hold no `L³` norm, no decay-exponent fit, no `λ`/period detector, and no
axisymmetry diagnostic of any kind — a Biot-Savart witness pair and a scalar-ODE stepper
respectively, neither screens anything against a rigidity theorem.

## Verdict

**PROCEED**, on the strength of the internal (network-independent) capability-index check
alone, for the same stated reason leg 354 gave at the identical decision point: B7 is a
numerics-construction brick, not a literature-adjudication brick, and the internal grep is
decisive — no admissibility-screening or machine-read rigidity-ledger machinery exists
in-repo for this equation family before this leg.

## What this leg is NOT claiming

No new arXiv prior-art count is banked (the refusal above is total for substantive
queries). This is a statement about this repository's own index, not about the published
literature at large — exactly the distinction leg 326's own GAP-326-A correction insists
on for absence claims.
