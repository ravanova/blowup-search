# Leg 345 — Route-PUB0C: novelty pass, run before assembling the §0c census-spine section

**Scope.** This is a record/publication assembly leg (not floor-eligible), so the novelty pass
applies loosely per the dispatch's own instruction: it checks that no other leg has already
assembled this exact spine section, rather than searching external literature (there is no new
mathematical claim in this leg for external prior art to bear on).

## Check 1 — does `writeup/PUB_0C_CENSUS_SPINE.md` (or an equivalent) already exist?

```
$ ls writeup/PUB_0C_CENSUS_SPINE.md
ls: cannot access 'writeup/PUB_0C_CENSUS_SPINE.md': No such file or directory
```

No file of that name exists anywhere in the repository (checked at the `origin/main` tip this
leg's worktree was cut from, commit `5c83f7e`).

## Check 2 — repo-wide grep for the assembled section's own name and near-spellings

```
$ grep -rn "PUB_0C\|PUB0C\|census spine\|census-spine" writeup/INDEX.md
(0 hits)
$ grep -rln "PUB_0C\|PUB0C\|census.spine\|census-spine" --include="*.md" writeup/ experiments/ \
    | grep -v DIRECTION.md
(0 hits)
```

`PUB0C`/`§0c` is discussed extensively as a **scheduling/gating token** in `DIRECTION.md` (the
Decision Maker's queue file — dozens of hits, all dispatch/ruling prose, none of them a drafted
section), but no `writeup/` or `experiments/` file contains an assembled census-spine document,
a `PUB_0C`-named data file, or a `PUB_0C`-named novelty pass predating this one.

## Check 3 — has any leg's territory ever included this filename?

`writeup/INDEX.md`'s existing PUB-track rows (PUB1 through PUB4, `writeup/4_p2_lottery/`) are a
different, already-published track — none names `PUB_0C` or a census-spine section. Leg 328's
territory (`CLAY_ROADMAP.md:343`, `writeup/CORRECTIONS.md` only) does not include
`writeup/PUB_0C_CENSUS_SPINE.md` either — confirmed by reading leg 328's own dispatch text in
`writeup/CORRECTIONS.md` §21 and its journal entry, neither of which claims this file.

## Verdict

**No other leg has assembled this exact spine section, or any equivalent under a different
name.** The precondition for drafting it fresh (leg 328 landed, closure #5 register entry
complete at §21) is independently confirmed at the top of the working journal
(`experiments/journal/leg_345.md`). Proceeding to assembly.
