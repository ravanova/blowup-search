# Leg 122 — Route-ASA v1 — novelty pass, RUN AND COMMITTED BEFORE CONSTRUCTION

**Date:** 2026-08-06. **Branch:** `leg/122-asa-v1`. **Route:** ASA (exploration, not critical
path; a recovered leg from the 110-series reserve). **Module under audit (READ-ONLY, edited
under neither branch of the gate):** `solver/advection_scope.py`.

Links, not counts (leg 53's withdrawal). Every claim below is a path, a SHA or a line number.

---

## 0. THE GATE, QUOTED VERBATIM FROM `DIRECTION.md`

Unlike legs 142/143/144, this leg's `DIRECTION.md` section **exists**: `### 122 — ROUTE-ASA:
ADVERSARIAL AUDIT OF advection_scope.py (RESERVE)`, at `DIRECTION.md:2778`. Quoted verbatim,
lines 2782–2785:

> **Gate.** Under an adversarial battery of degenerate or poisoned inputs, does
> solver/advection_scope.py ever silently return a wrong result instead of flagging the input?
>   yes -> Silent-corruption gap; report the exact failing case; escalate, do not patch.
>   no  -> Confirmed robust; bank the battery as a permanent regression test.

Territory, verbatim (`DIRECTION.md:2786-2789`): `test_advection_scope_adversarial.py`,
`experiments/p2_route_asa_v1_adversarial.py`, `writeup/data/p2_route_asa_v1_adversarial.json`,
`writeup/novelty/leg_122.md`, `experiments/journal/leg_122.md`. **Independence**, verbatim
(`DIRECTION.md:2791`): "Reads solver/advection_scope.py only; edits nothing."

Under **both** branches `solver/advection_scope.py` is byte-identical to this leg's merge base.
The gate is frozen at this commit and answered in exactly this wording.

## 1. Bans checked first

`/home/andy/projects/Unsolved/.venv/bin/python plan_of_record.py` was run before anything else
(the worktree has no `.venv` of its own; the repo root's interpreter is the one the plan
prescribes). Every ban in force was read. The ones that could touch this leg:

* **"building a solver without grepping `capabilities.py` for the object first"** — honoured.
  `grep -n "advection_scope" capabilities.py` → the entry at **lines 366–368**: object "where
  the advection term lives in a", holds "scope of the Route-D bound programme", validated
  "agrees with the analytic small-a and large-a limits of the advection coefficient",
  `"test": "test_advection_scope.py"`. **No solver is built by this leg**, and no file under
  `solver/` is edited.
* **"another Route-D bound-sharpening leg"** (lifted by `B`) — **not engaged.** This leg
  sharpens no bound and computes no new Route-D constant. It pushes degenerate and poisoned
  inputs through existing functions and records what comes back.
* **"another gCLM measurement leg"** — not engaged. No sweep, no new gCLM physics number. The
  Newton profiles are inputs to the battery, not its product.
* **"closing the truncation gap by extending the domain"** — **actively relevant and honoured.**
  Probe P1/P2 below concern what the module reports *beyond* its own grid. This leg does **not**
  extend any domain to close anything; it measures what the existing code says about points it
  does not have. The one large-radius run (control C2) exists to make the clamped value
  falsifiable, not to buy far-field accuracy.
* **Stage `B`'s three degrees of freedom (space, split, shape)**, **`ℓ¹`-Fourier/collocation
  machinery**, **leg 51's methodological finding** — none is a quantity in this file.

## 2. What is already banked, in this repository, on exactly this question

Prior art *inside* the repo is checked before prior art outside it, because the failure mode
this pass exists to prevent is re-measuring a banked number.

* **The module has exactly ONE commit and has never been modified since creation.**
  `git log --oneline -- solver/advection_scope.py` → `97ef772` ("Route-D: advection scope
  tooling -- the velocity is log-divergent, and what that costs"). One line of history. **No
  bench repair has ever touched it**, and no leg has audited it.
* **No adversarial battery exists for it.** The repo carries **27** `test_*_adversarial.py`
  files (`bordered_hl`, `boussinesq`, `boussinesq_velocity`, `collocation_newton`,
  `critical_dissipation`, `decay_collocation`, `decay_grading`, `energy_coercivity`,
  `first_integral`, `fractional_gclm`, `gclm`, `gclm_family`, `gclm_rescaled`, `hilbert_holder`,
  `hilbert_pointwise`, `hl_rescaled`, `holder_norms`, `interval_certificate`, `line_hilbert`,
  `marginal_flow`, `nk_bounds`, `nk_fourier`, `nk_seminorm`, `op_lower`, `spectral_utils`,
  `target_norm`, `weight_search`). **None** is for `advection_scope`. Its only test is
  `test_advection_scope.py` (182 lines, 6 pre-committed predicates) — §3 records, per probe,
  which of those 6 already covers it, so that nothing here re-measures a banked number.
* **Everything that reads it** (`grep -rln advection_scope --include=*.py .`):
  `test_advection_scope.py`, `capabilities.py`, `experiments/p2_route_cap_v1_audit.py`,
  and `solver/finite_support.py`. The last one is the stake: `finite_support.py` *re-reads this
  module's stagnation point as the edge of support* `X_c` (this module's own header §5,
  lines 71–84), so `stagnation_point`'s numerics are load-bearing for a second module's physics
  claim.
* **The escalation template, if the gate answers yes.** Leg 107 (`5e03e13`, Route-FIA): gate
  YES → "ESCALATED not patched", module byte-identical to `origin/main`, the battery pinned and
  banked. The later bench repair (`9c08287`) is the *repair* template, and is **not** this leg's
  job under either branch.
* **The nearest prior audits by shape**, so this is not a re-run: leg 107 (FIA,
  `first_integral.py` — out-of-support extrapolation + NaN-absorbing mask), leg 139 (DGA,
  `decay_grading.py`), leg 144 (ECA, `energy_coercivity.py` — weight-family domain edges).
  **None of them touches a `np.interp` far-field probe, a least-squares log-slope fit over a
  requested-vs-realized window, or a sign-change bracket search** — the three things this module
  is made of. The probe set is chosen on those three, i.e. on this module's own surface.

## 3. THE PROBES, NAMED AND FROZEN BEFORE A SINGLE NUMBER IS COMPUTED

Ten probes. There is no eleventh, and nothing found outside this list may be reported as this
leg's finding. Each names the *reachable* input, the documented claim it tests (module header
or docstring line), and what an honest answer would be.

| # | Probe | Documented claim it tests | Already banked? |
|---|---|---|---|
| **P1** | `advection_split(...)["transport_at"]` / `["stretch_at"]` at the keys `1e3` and `1e4` on the module's **default construction path** (`TwoScaleNewton(rho_max=8)` → sinh grid `X = 0.5 sinh(rho)`, so `X_max ≈ 0.5 sinh 8`). Both keys lie **beyond the last grid node**; `np.interp` (lines 184–187) clamps silently. | the dict is labelled "the value at `X = 1e3` / `1e4`". Is a clamped copy of the last node's value being returned under that label? | **no gate** — `test_advection_scope.py` runs `rho_max=12` throughout and never inspects `transport_at` beyond the grid |
| **P2** | The **requested vs realized fit window**: `_log_fit` (lines 109–115) selects `X < hi` and reports a slope; `advection_split` defaults `hi = 1e4`, `velocity_log_rate` returns `"window": [lo, hi]` — the *requested* pair — regardless of where the grid actually ends. | a fitted log rate is a statement about a window. Is the reported window the one the fit used? | not tested |
| **P3** | **NaN-poisoned `om`** (one interior node) through all six public entry points: `profile_mass`, `velocity`, `velocity_log_rate`, `advection_split`, `grading_comparison`, `stagnation_point`. | does any of them **absorb** the NaN and return a finite, plausible number, or does every one propagate/flag it? | not tested |
| **P4** | **Inf-poisoned `om`** (one node `+inf`, and one node `-inf`) through the same six. | same question; `inf - inf` inside the dense `Hmat`/`Vmat` products is the specific absorption risk | not tested |
| **P5** | `stagnation_point` with a **NaN in `U`**: `np.sign(nan) = nan` and `nan != 0` is **True**, so the sign-change search (lines 212–213) can bracket a *non-crossing*. Does it return a finite, plausible `X*`? | docstring: "Smallest X > 0 where the effective speed c + a U(X) changes sign" | not tested |
| **P6** | `stagnation_point` returning **`None`** for two physically opposite situations: `a = 0, c > 0` (documented: "no crossing … `c_eff == c > 0`") vs **`c < 0`**, where `c_eff` is negative *everywhere*. | is a single `None` standing for both "never reverses, always forward" and "always reversed"? | gate 5 checks only the `a = 0`, `c > 0` case |
| **P7** | `advection_split(..., grading="one_scale")["transport_predicted_rate"]`, **hard-coded `0.0`** at lines 182–183. | the sibling key under `two_scale` is a real prediction `a|M|α/π`. Is a placeholder being published under the label "predicted rate", and how far is it from the realized one-scale fit? | gate 4 checks the one-scale piece *decays*; **no gate reads `transport_predicted_rate` under `one_scale`** |
| **P8** | **Boundary-of-validity `alpha`**: `alpha = 2` exactly (the header's stretch piece "DECAYS for alpha < 2, which is the whole working range", lines 33–35) and `alpha = 2.5`, plus `alpha = 0` and `alpha < 0`. | does the module flag that its own stated working range has been left, or silently return the same dict shape? | gate 2/4 use `ALPHA = 1.4` only |
| **P9** | **Degenerate / zero-measure windows**: `lo >= hi`; `lo <= 0` (so `log X` of non-positive `X` enters the fit); a window containing exactly 7 and exactly 8 nodes (the `sel.sum() < 8` boundary, line 113). | line 114 returns `nan` — an honest refusal. Is it reached on every degenerate path, or is there a path that fits garbage instead? | not tested |
| **P10** | **Planted wrong-value pass-through in `h`**: `advection_split` with `h` of the wrong length, with `h` containing NaN, and with `a = nan`; plus `grading="two-scale"` (a plausible typo). | `GRADINGS[grading]` should raise; a wrong-length `h` should raise on the matvec. Which of these five is loud and which is silent? | not tested |

**Controls that can report the other answer** (lesson 90 — a control that cannot come out
differently is not a control):

* **C1 (negative control, for every probe):** the same battery at **`a = 0`**, where the module's
  own §2 and `test_advection_scope.py` gate 3 bank that both advection pieces are *identically*
  zero. If the battery flagged corruption there too, it would be flagging arithmetic.
* **C2 (positive control, for P1/P2 — and it can come out either way):** the same `transport_at`
  keys on a grid whose last node is genuinely **past `1e4`** (`rho_max = 12`, `X_max ≈ 0.5 sinh
  12 ≈ 8.1e4`). Then `1e3` and `1e4` are interior and honest. The finding is only a finding if
  the default-grid value and this value **differ by a factor this leg quotes as a number**; if
  they agree, P1 is arithmetic, not corruption.
* **C3 (loud-failure control):** at least one probe must make the module **raise**. A battery in
  which nothing raises has not reached the guards; one in which everything raises has not
  reached the reachable domain.

## 4. Outside the repository

An adversarial audit of one file's numerical-domain guards is not a literature question, and
**no literature claim is made by this leg**. The module's physics content (the log-divergent
velocity `U → (M/π) log X`, the two-scale/one-scale grading dichotomy) is banked by Route-D
legs v3–v11 and by `writeup/4_p2_lottery/TECHNICAL_P2_ADVECTION_SCOPE.md`; this leg re-derives
none of it and cites no paper. `np.interp`'s clamping and `np.polyfit`'s NaN behaviour are
documented NumPy semantics, verified in-process by this leg rather than quoted from memory.

## 5. Ceiling, pre-committed

Float64, one module, one process; the module's own header says "Nothing here is interval-
enclosed and nothing is rigorous". The object is the two-scale gCLM profile — **nothing here
can move a link of the `L1 → L4` chain**, and nothing here revises Route-D's scope conclusion
in the direction of *life*: a silent-corruption finding on this instrument can only widen the
error bars on a **negative** (the `a ≠ 0` scope obstruction), never turn it positive. The one
place a finding could propagate is `solver/finite_support.py`'s re-reading of `X*` as the edge
of support — and that propagation is reported as an *exposure*, not as a correction to that
module, which this leg does not touch. If any result reads like movement on the chain, that is
escalation #3 (`ORCHESTRATION.md` §8): parked, not written into prose.
