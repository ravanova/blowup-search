# Novelty pass — leg 354, Route-DSSP brick B4 (DSSP-STEP)

Attempted BEFORE construction began, per ORCHESTRATION.md §0a; construction proceeded
regardless once the external instrument was measured to be in sustained refusal (see below),
following leg 351's own recorded precedent that this brick's territory is numerics, not
literature-adjudication, and a numerics brick's construction does not wait on a degraded
external search endpoint.

## Subject

B4's gate (TECHNICAL_P2_ROUTEDSSP_V1.md §5.1): does a rescaled-vorticity time-stepper
reproduce a KNOWN answer inside a stated window — a decaying Leray solution relaxing to the
trivial state at a measured rate, with a planted non-trivial control that must fail?

## Instrument control (MF1/MF4 discipline) — SUSTAINED REFUSAL TO MEASURE

The https export API's plain positive control (`all:electron`, no quoting, no AND) returned
200/non-empty (184814 results) on **every isolated attempt** this leg made (4 separate
occasions, spaced minutes apart). This confirms the endpoint itself is reachable in principle.

Every substantive query this leg attempted — 7 distinct formulations, covering the core
identifiers of this brick's construction (`"rescaled vorticity"`, `"rescaled vorticity" AND
"self-similar"`, `"Ornstein-Uhlenbeck" AND "vorticity"`, `"single-mode Galerkin" AND
"Navier-Stokes"`, `"time-stepper" AND "self-similar" AND "Navier-Stokes"`, plus two encoding
variants of the first) — returned **429 or a bare connection timeout (curl exit 28, 0 bytes)**
on **every single attempt**, across roughly 20 attempts spread over ~25 minutes with
backoffs of 4–55 seconds between tries, including attempts made immediately after a successful
control call. This is recorded as a genuine, sustained **REFUSAL TO MEASURE** for every
substantive query this leg tried — not as an absence, and not papered over with a
partially-successful run dressed up as complete. No substantive arXiv query for this brick
returned a usable result this leg; **zero external prior-art counts are banked below**, and
none should be read as measured.

Unlike leg 326/351's transient refusals (a handful of consecutive 503/429s bracketed by
otherwise-successful runs), this leg's refusal was **total** for every non-trivial query
attempted, while the plain single-term control kept succeeding — consistent with the
compound/quoted queries specifically (or the accumulated request volume from repeated retries)
having tripped a rate limit whose cooldown outlasted this leg's patience budget. No query
result is fabricated or estimated to fill the gap.

## What was checked instead (network-independent)

`capabilities.py` (52 entries after this leg's own addition) was grepped for
`time-stepper|time stepper|galerkin|integrating factor|newton-krylov|periodic orbit|rk4|
runge-kutta` before any line of `solver/dssp_step.py` was written. The only hit besides this
leg's own new entry is an unrelated RK4 shooting solve for a Chen-Hou/CGL-NLS profile ODE (a
different equation, different application, no rescaled-vorticity or self-similar-NS content).
This reconfirms leg 334's live-read finding (quoted at TECHNICAL_P2_ROUTEDSSP_V1.md §3.2): 0 of
49 (now 52) indexed modules hold M1 (3D NS in similarity variables), M4 (periodic-orbit
search), or Newton-Krylov machinery. `solver/dssp_basis.py` (leg 350) and
`solver/dssp_biot_savart.py` (leg 351) were read in full (required background reading for this
leg) and confirmed to hold no time-stepper of any kind — a compactified basis and a static
Biot-Savart witness pair respectively, neither integrates anything in `s`.

## Verdict

**PROCEED**, on the strength of the internal (network-independent) capability-index check
alone, since B4 is a numerics-construction brick (not a literature-adjudication brick, per
leg 351's own recorded reasoning at the identical decision point) and the internal grep is
decisive: no rescaled-vorticity time-stepper, Galerkin-truncation-of-this-equation, or
scalar-ODE-verification-against-a-closed-form-Riccati-solution exists anywhere in this
repository's indexed solver modules. The external literature question this leg could not
measure — whether the specific single-mode-Galerkin-onto-a-closed-form-witness reduction used
here, or its confinement-sign-flip falsification control, has independent precedent in the
self-similar-Navier-Stokes numerics literature — is left OPEN and UNMEASURED, flagged here for
a future leg's arXiv access rather than silently dropped or guessed at.

No ban touched, lifted or argued against. `plan_of_record.py` and `DIRECTION.md` not edited
(sha256 confirmed unchanged before push). No L1→L4 link moved. Clay odds ~0.05%, unchanged.
