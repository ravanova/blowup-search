# Leg 136 — Route-MF2 v1 — novelty pass, RUN AND COMMITTED BEFORE CONSTRUCTION

**Date:** 2026-08-06. **Branch:** `leg/136-mf2-v1`. **Route:** MF2 (exploration).
**Module under audit (read-only):** `solver/marginal_flow.py`.

This file is committed **before** any experiment code exists, per the standing discipline
(`CONTINUATION_PROMPT.md`: "Run the novelty pass BEFORE the construction, and commit the query
log") and per leg 111's pre-naming discipline: **the family of candidate second criteria is
NAMED AND DEFINED IN THIS FILE, before a single number is measured.** Anything that does not
appear in §3 below cannot be reported as a success of this leg.

Links, not counts (leg 53's withdrawal).

---

## 1. Bans checked first

`.venv/bin/python plan_of_record.py` was run before anything else. Every ban in force was read.
The ones that could touch this leg:

* **"building a solver without grepping `capabilities.py` for the object first"** — honoured.
  `grep -n "marginal_flow" capabilities.py` → line 99–103, the entry for
  `solver/marginal_flow.py`, object "the augmented (Omega, mu) flow, driven", validated line
  quoting `integrate`'s `converged` predicate, test `test_marginal_flow.py`. That entry is the
  claim this leg's residual sits underneath. **No solver is built here at all.**
* **"another gCLM measurement leg"** — not engaged. This leg computes no profile, runs no
  sweep, and reads no new gCLM number. It pushes trajectories with closed-form exact solutions
  through the *existing* integrator's *existing* validity predicate, and evaluates *candidate
  predicates* on the recorded states. Same posture leg 83 recorded and the orchestrator
  accepted.
* **"do not build further ℓ¹-Fourier or collocation machinery for this operator"** — not
  engaged; nothing here is a certificate.
* **Stage `B`'s three degrees of freedom** — not engaged.

**Nothing under this leg's authority edits `solver/marginal_flow.py`.** The gate's yes-branch is
an ESCALATION, not a patch.

## 2. What is already banked, in this repository, on exactly this question

Prior art *inside the repo* is checked before prior art outside it, because the failure mode
this pass exists to prevent is re-measuring a banked number.

* **Leg 83 (Route-MFG), `experiments/journal/leg_83.md`, `writeup/novelty/leg_83.md`,
  `experiments/p2_route_mfg_v1_adversarial.py`, `writeup/data/p2_route_mfg_v1_adversarial.json`.**
  The 13-member synthetic battery, the `SyntheticFlow` duck-type, the fidelity gate, the two
  live controls. Gate answered NO: 1 of 9 scored divergent trajectories flagged.
* **The bench repair, commit `8572465`** ("Leg 0: ORCH — marginal_flow.py gate 11: add the state
  clause leg 83 measured missing (1/9 -> 5/9)"). Added the fourth clause `state_growth < 1e5`,
  scale-free (per-block, relative to each block's own initial magnitude). Its own commit body
  names the residual this leg inherits, and **those four names are the input to this leg, not an
  output of it**:
  - `mu_linear` — accepted at growth **2.01e2**
  - `b_quadratic` — accepted at growth **3.73e3**
  - `osc_sustained` — accepted at growth **1.00**
  - `osc_growing` — accepted at growth **1.99**
  plus the fifth open item, `osc_stiff_underresolved`, which is **excluded on fidelity** rather
  than missed (BDF2's own L-stability damped it to 3.5e-14 of its exact amplitude) and is
  carried by leg 83 as a *separate hazard* with no `dt`-vs-`max|Im|` guard anywhere.
  That is the "5 remaining open cases" of this leg's thesis: **4 scored misses + 1
  fidelity-excluded hazard.** This leg reports that decomposition as a magnitude rather than
  inheriting the round number.
* **The repair's own stated reason for stopping**, quoted verbatim from `8572465`:
  "below/too near the 4.06e2 legitimate ceiling for any safe growth bound" and "bounded, no
  limit — a magnitude clause is the wrong instrument; needs a tail test". **The present leg
  exists to test whether a tail test actually exists.** The repair asserted the shape of the
  fix without measuring it; this leg measures it.
* **`test_marginal_flow_adversarial.py`** already pins the four misses (`test_4b`, `test_5`).
  Those assertions are the pre-repair characterization; they are **not** re-derived here.
* **The legitimate ceiling, `4.06e2`**, comes from `test_marginal_flow.py::test_6` at `p = 5`,
  where `lambda_mu = +2` is *supposed* to grow `mu` from `2e-3` to `0.813` over `tau = 3`.
  This is the single most important object in the leg: **the module's own well-behaved battery
  contains a monotone, exponentially growing trajectory.** Any criterion proposed here must
  clear it.

Nothing in the repo has ever measured a *second* criterion against this residual. This leg is
not a re-run.

## 3. THE PRE-NAMED FAMILY (fixed here, before measurement)

Five criteria. Each is a **single scalar** computed from the accepted per-step states of one
trajectory, each is **scale-free** in the same per-block sense the landed `state_growth` clause
uses (`b` against `max|b0|`, `mu` against `|mu0|`, never a joint norm — `|b| ~ 1` while
`mu ~ 2e-3`), and each has its **threshold placed after measurement by the same rule the bench
repair used** (the geometric middle of the gap between the largest legitimate value and the
smallest true-positive value), so that the *definitions* are pre-committed and only the
*numbers* are measured.

Window convention, fixed now: let the accepted states be `y_0 … y_n`. `TAIL` is the last 25 % of
steps, `PRE` is the 25 % immediately before it. Per block `j ∈ {b, mu}`, `v_j(i)` is
`max_k |b_k(i)|` or `|mu(i)|`, and `s_j` is that block's initial scale as `integrate` defines it.

* **C1 `TAIL_GROWTH_RATIO`** — a windowed growth-rate estimate in ratio form.
  `C1 = max_j ( max_TAIL v_j / max_PRE v_j )`. Flag if `C1 > T1`.
  Rationale: a trajectory with a limit has `C1 → 1`; unbounded growth keeps `C1 > 1` forever,
  *however slowly it grows* — which is exactly what `state_growth`'s magnitude bound cannot see
  on `mu_linear`.
* **C2 `TAIL_DRIFT_OVER_INITIAL_SCALE`** — the residual-to-state-scale ratio.
  `C2 = max_j ( max_TAIL |v_j(i+1) − v_j(i)| / dt ) / s_j`. Flag if `C2 > T2`.
  Rationale: the right-hand side is the residual the flow is still generating at the end of the
  run; a converged trajectory's is at roundoff *relative to where it started*. This is the one
  criterion in the family that should see `osc_sustained` (drift `~2π` forever at amplitude 1).
* **C3 `MONOTONE_TAIL_FRACTION`** — a monotone-tail detector.
  `C3 = max_j (#{i ∈ TAIL : v_j(i+1) > v_j(i)} / |TAIL|)`. Flag if `C3 > T3`.
  Rationale: the cleanest possible statement of "still going up at the end", blind to magnitude.
* **C4 `TAIL_RANGE_OVER_INITIAL_SCALE`** — a non-decay detector for the oscillatory pair.
  `C4 = max_j ( max_TAIL v_j − min_TAIL v_j ) / s_j`. Flag if `C4 > T4`.
  Rationale: `osc_sustained` has no limit and no growth; what it has is a tail *range* that
  never shrinks. This is the "tail test" commit `8572465` asserted was needed.
* **C5 `TAIL_EFOLD_RATE`** — the windowed growth rate as a *rate*, not a ratio.
  `C5 = max_j log(max_TAIL v_j / max_PRE v_j) / (tau_TAIL − tau_PRE)`. Flag if `C5 > T5`.
  Rationale: C1 conflates "grows fast" with "runs long". C5 separates them, and is included
  specifically so the leg can report *which* of the two the residual actually turns on.

**The falsifiable prediction, recorded before measurement.** The module's own well-behaved
battery contains `test_6` at `p = 5`: monotone, exponential, growth `4.06e2` in `tau = 3`. On
C1/C3/C5 that legitimate trajectory should score **at or above** `mu_linear` (which grows
`2.01e2` over `tau = 60`, i.e. *slower per unit tau*), so those three criteria are predicted to
have **overlapping** populations — a false positive on `test_6` at any threshold that reaches
`mu_linear`. C2 and C4 are the two that can separate the oscillatory pair, and they are
predicted to be the ones that fail in the *other* direction, on `damped_oscillation` /
`test_6`. **If any single criterion clears all five open cases with zero false positives, this
prediction is falsified and the gate answers YES.** That is the point of writing it down.

## 4. External literature queried (verbatim, with links)

**Q1.** `numerical ODE integrator detecting divergent solutions blow-up criterion state growth monitor`
* [Numerical validation of blow-up solutions of ODEs (ScienceDirect)](https://www.sciencedirect.com/science/article/pii/S0377042716305027)
* [Numerical validation of blow-up solutions with quasi-homogeneous compactifications (arXiv:1707.05936)](https://arxiv.org/pdf/1707.05936)
* [same, Numerische Mathematik](https://link.springer.com/article/10.1007/s00211-020-01125-z)
* [Nonlinear problems with blow-up solutions: numerical integration based on differential and nonlocal transformations (ScienceDirect)](https://www.sciencedirect.com/science/article/abs/pii/S0096300318303977)

  **Reading.** The rigorous literature does not build a *detector* at all: it changes
  coordinates (compactification) so the divergent object becomes a finite equilibrium, then
  applies a Lyapunov function *around the equilibrium at infinity*. That is a construction on
  the vector field, not a predicate on a trajectory — it is not available to a validity clause
  inside a general-purpose `integrate`, and it presupposes knowing the divergent direction. This
  **confirms rather than pre-empts** the leg's framing: the criteria in §3 are engineering
  guards, and none of them is claimed as a mathematical blow-up criterion.

**Q2.** `SUNDIALS CVODE "too much work" solver diagnostics detect solution growing unbounded stopping criterion monitor`
* [SUNDIALS CVODE mathematical considerations](https://sundials.readthedocs.io/en/latest/cvodes/Mathematics_link.html)
* [CVODE usage documentation](https://sundials.readthedocs.io/en/latest/cvode/Usage/index.html)
* [sundials/src/cvode/cvode.c](https://github.com/LLNL/sundials/blob/main/src/cvode/cvode.c)

  **Reading.** CVODE's failure returns are all *solver* observables (convergence failure, error
  test failure, too-much-work, too-much-accuracy-requested); the state-side facility is a
  user-supplied **monitor function** called after successful steps — i.e. the production answer
  to "watch the state" is *"write the predicate yourself"*. This is the same separation leg 83
  recorded, and it means there is no off-the-shelf named criterion this leg is duplicating.

**Q3.** `steady state detection ODE simulation "monotone tail" OR "windowed growth rate" OR "residual to state ratio" convergence to equilibrium criterion`
* [Steady state detection for computational fluid dynamics (arXiv:1709.03158)](https://arxiv.org/pdf/1709.03158)
* [A steady-state detection (SSD) algorithm to detect non-stationary drifts in processes](https://www.researchgate.net/publication/255568863_A_steady-state_detection_SSD_algorithm_to_detect_non-stationary_drifts_in_processes)
* [The F-t-Pj-RG method: an adjacent-rolling-windows based steady-state detector (OSTI)](https://www.osti.gov/servlets/purl/1495967)
* [Abaqus steady-state detection](https://abaqus-docs.mit.edu/2017/English/SIMACAEANLRefMap/simaanl-c-steadystatedetection.htm)

  **Reading.** This is the closest external prior art and it is a real one: *adjacent rolling
  windows* with a local-trend test is precisely C1/C5's shape, and the SSD literature's own
  standing caveat — **"there does not exist a universal rule for the length of this window"** —
  is a warning this leg must answer rather than ignore. Consequence recorded before measurement:
  the 25 %/25 % window convention in §3 is **swept**, not asserted, and the sweep is reported.
  Note the direction of use is inverted here (that literature detects *arrival* at steady state;
  this leg detects *failure to arrive*), so the thresholds do not transfer.

**Novelty verdict.** The criteria in §3 are **standard instruments** — rolling-window trend
tests and a drift-to-scale ratio — deliberately so. **Nothing in this leg is claimed as novel
mathematics.** What is novel to this repository is only the *measurement*: whether any of them
separates this module's divergent residual from this module's own legitimate trajectories.
Presume the instruments known; the number is the finding.

---

*(Findings note appended below after measurement, per the leg 83 precedent.)*

---

# FINDINGS — written after measurement, against `writeup/data/p2_route_mf2_v1_residual.json`

**Gate answer: NO** — in its pre-committed wording, *"The residual gap is characterized, not
just counted."* `solver/marginal_flow.py` was not edited. Runner:
`experiments/p2_route_mf2_v1_residual.py` (66.1 s). Every number below is quoted from the JSON.

## BLOG — what happened, in one paragraph

Gate 11 still accepts five divergent trajectories, and the obvious next repair — a "tail test",
which the bench-repair's own commit message named as the missing instrument — **does not exist.**
Not "we could not tune it": the two populations *overlap*, and they overlap because of a
trajectory the module is supposed to keep. `test_marginal_flow.py`'s gate 6 at `p = 5` is a
legitimate, wanted run in which `mu` is *supposed* to grow — `lambda_mu = +2` is the number that
gate is measuring — and it grows by **4.063e2** over `tau = 3`, at an e-folding rate of
**+2.004** per unit `tau`. The mildest divergent case the leg has to catch, `mu_linear`
(`mu' = 1`, unbounded forever), grows by **2.01e2** at a rate of **+0.019**. So the legitimate
trajectory is **2.02× larger** in magnitude and **105× faster** in rate than the divergent one it
must be told apart from. Every instrument in the pre-named family is some form of "how much, or
how fast, at the end", and every one of them therefore reads the *legitimate* run as the more
divergent of the two. The best separation ratio anywhere in the family, across three window
lengths, is **0.548×** — and a criterion needs a ratio above 1 to have a threshold at all. The
gap is not a tuning gap; on this battery it is the wrong shape of question.

## TECHNICAL — the measurement

### 1. The five open cases, classified by mechanism (three mechanisms, not one)

| case | mechanism | `state_growth` | tail range / scale | tail e-fold rate |
|---|---|---|---|---|
| `mu_linear` | `SUB_THRESHOLD_UNBOUNDED_GROWTH` | 2.01e2 | 4.92e1 | +0.019 |
| `b_quadratic` | `SUB_THRESHOLD_UNBOUNDED_GROWTH` | 3.73e3 | 1.58e3 | +0.038 |
| `osc_sustained` | `BOUNDED_NON_CONVERGENT` | 1.00 | 2.78e-1 | +0.000 |
| `osc_growing` | `BOUNDED_NON_CONVERGENT` | 1.99 | 1.04 | +0.046 |
| `osc_stiff_underresolved` | `INTEGRATOR_ERASED_THE_DIVERGENCE` | 1.00 | 3.65e-14 | +0.004 |

The inherited "5 remaining open cases" decomposes as **4 scored misses + 1 fidelity-excluded
hazard**, and the fifth is a different object: BDF2's own L-stability damped
`osc_stiff_underresolved` to **3.479e-14** of its exact amplitude, so the trajectory the
predicate saw genuinely converged. **No state predicate can detect a divergence the stepper
removed.** It is a `dt`-vs-`max|Im|` hazard (leg 83's Finding 3), and it is *removed from this
leg's scoring*, not counted as a criterion failure — which is why the leg reports both a
4-case and a 5-case ratio throughout.

`mu_linear` carries the hardest fact in the leg: its growth **2.01e2 is 0.495× the largest
legitimate growth in the module, 4.063e2.** A magnitude clause that reaches it must first flag
gate 6. That is an ordering, not a threshold placement problem, and no choice of bound fixes it.

### 2. The pre-named family against the residual (window 0.25; §3 definitions, unchanged)

| criterion | weakest true positive | binding false positive | separation ratio (4 scored) | (all 5) |
|---|---|---|---|---|
| C1 `TAIL_GROWTH_RATIO` | `osc_sustained` 1.000 | `gate6_p5` 4.586 | **0.218×** | 0.218× |
| C2 `TAIL_DRIFT_OVER_INITIAL_SCALE` | `mu_linear` 3.333 | `gate6_p5` 798.7 | **4.17e-3×** | 1.85e-15× |
| C3 `MONOTONE_TAIL_FRACTION` | `osc_sustained` 0.496 | `gate6_p2` 1.000 | **0.496×** | 0.448× |
| C4 `TAIL_RANGE_OVER_INITIAL_SCALE` | `osc_sustained` 0.278 | `gate6_p5` 314.1 | **8.86e-4×** | 1.16e-16× |
| C5 `TAIL_EFOLD_RATE` | `osc_sustained` 0.000 | `gate6_p5` 2.004 | **0×** | 0× |

**Zero of five admit a threshold**, at any of the three swept window fractions (0.10 / 0.25 /
0.50 — swept because the steady-state-detection literature's own caveat is that no universal
window length exists, novelty log Q3). Best ratio anywhere: **0.548×** (C1 at window 0.10).
The binding false positive is `gate6_lambda_mu_p5` for C1/C2/C4/C5 and `gate6_p2`/`gate6_p4`
for C3 — i.e. **it is always a trajectory one of the 11 gates exists to keep.** Conjunctions of
two criteria were measured as well (not scored against the gate, which asks about a single
criterion): **0 of 10 pairs** admit a threshold pair at any window.

### 3. The prediction, and that it was falsifiable

`writeup/novelty/leg_136.md` §3, committed at `aeced70` before this file existed, predicted that
C1/C3/C5 would overlap on `gate6_p5` and that C2/C4 would fail in the other direction. **The
first half is confirmed and the second half is wrong**: C2 and C4 fail on `gate6_p5` too, not on
`damped_oscillation`, because normalizing by the *initial* scale (which the landed clause also
does) makes `mu0 = 2e-3` a very small denominator. The prediction was falsifiable, one half of it
was falsified by the measurement, and it is reported that way rather than quietly re-worded.

### 4. The positive control, which reports the other answer

A negative needs a control that can come out differently (banked lesson 90 — four identical
numbers are a bug, not a finding). The same five criteria were run against the **four cases the
landed `state_growth` clause already closed**, with the identical must-not-flag population:

| criterion | min over the closed 4 | max over must-not-flag | ratio | threshold? |
|---|---|---|---|---|
| C2 | 9.126e5 | 798.7 | **1.143e3×** | **YES** |
| C4 | 9.388e6 | 314.1 | **2.989e4×** | **YES** |
| C1 | 3.077 | 4.586 | 0.671× | no |
| C3 | 1.000 | 1.000 | 1.000× | no |
| C5 | 0.0749 | 2.004 | 0.0374× | no |

**C2 and C4 do admit thresholds — by three and four orders of magnitude — on the population the
state clause already handles, and fail by three and four orders on the open five.** The
instruments are not inert; the failure is a property of the residual, not of the family. Note
also that this control *inverts* on C2/C4 between the two populations, which is exactly the shape
a control has to have to be worth quoting.

### 5. The reproduction check is NOT quoted as a control, and why

All 27 trajectories report `reproduction_ok`, `mu_end` agreeing to `< 1e-12` relative between
`integrate`'s own record and the instrumented re-run. **That is close to a tautology of the
code** — the instrumented loop calls the module's own `_implicit_step` with the same arguments —
and lesson 90 says to ask what would have had to change for it to report otherwise. The honest
answer is "very little", so it is reported as a *plumbing check* (it would catch an
`AugmentedFlow` that carried step-to-step state into its own right-hand side, which it does not),
never as evidence for the finding.

### 6. What is banked, and what is explicitly NOT claimed

**Banked, permanently:** the three-mechanism classification of §1; the measured failure of each
of C1–C5 with its binding false positive and its ratio; the ordering fact that `mu_linear`'s
growth is **0.495×** the module's own legitimate ceiling; the positive control of §4.

**Not claimed:** that no detector exists. This leg measured *five named instruments of one
shape* — end-of-window magnitude and rate statistics — against *this* well-behaved battery. A
criterion of a different shape (a `dt`-vs-`max|Im|` resolution guard for the fifth case; a
model-aware predicate that knows `lambda_mu`'s predicted sign and so can tell a *wanted* growth
from an unwanted one; a compactification in the sense of the Q1 literature) is untouched here.
The gate's no-branch says *"nothing further without new information"*, and the shape of the new
information is named: **the separating variable is not on the trajectory, it is the caller's
expectation of it.** Gate 6 knows its run is supposed to grow at `+2`; `integrate` does not.

**No link of the L1→L4 chain moved.** Nothing here is rigorous, nothing is interval-enclosed,
and no banked number is shown to be wrong.
