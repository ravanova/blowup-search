# Leg 144 — Route-ECA v1 — novelty pass, RUN AND COMMITTED BEFORE CONSTRUCTION

**Date:** 2026-08-06. **Branch:** `leg/144-eca-v1`. **Route:** ECA (exploration, not critical
path). **Module under audit (READ-ONLY, edited under neither branch):**
`solver/energy_coercivity.py`.

Links, not counts (leg 53's withdrawal). Every claim below is a path, a SHA or a line number.

---

## 0. A DEFECT IN THE ASSIGNMENT, RECORDED BEFORE ANYTHING ELSE

The leg prompt says: read `DIRECTION.md` section `### 144 — ROUTE-ECA` and **quote the gate
verbatim**. **That section does not exist.** Measured, not asserted:

* `grep -c "^### [0-9]" DIRECTION.md` → **84** route sections; the highest three are
  `### 139 — ROUTE-DGA`, `### 140 — ROUTE-NFA`, `### 141 — ROUTE-WEL` (line 3152). There is
  no `### 142`, `### 143` or `### 144`.
* `grep -rIn "ROUTE-ECA" .` over the whole worktree (excluding `.git/`) → **0 hits**.
  `grep -rIl "leg_144\|Leg 144\|leg 144" .` → **0 files**.
* Not a stale-worktree artifact: `git show origin/main:DIRECTION.md | grep "^### 14[2-9]"` →
  0 hits at `e4199c2`, and a scan of every local ref's `DIRECTION.md` finds no section above
  141 anywhere in the repository.

**Precedent, and it is why this leg proceeds rather than stopping.** Two sibling legs were
dispatched into the same gap and did the work: `refs/heads/leg/142-nsa-v1` (commit `14bb3f7`,
"Leg 142: LEG — Route-NSA novelty pass, committed BEFORE construction", an adversarial battery
for `solver/nk_seminorm.py`) and `refs/heads/leg/143-vna-v1`. `git show
refs/heads/leg/142-nsa-v1:DIRECTION.md | grep "^### 14[0-9]"` shows 140 and 141 only, so leg
142 also had no written section and reconstructed its task from the route name plus the module
named in its prompt.

**What this leg does about it.** It does **not** invent a DIRECTION.md section (that file is an
integration-owned shared ledger; this leg writes zero bytes to it). It declares its gate **here,
in this file, before any number is computed**, in the two-branch wording the prompt itself
supplies. Per DIRECTIVE 3 of `CONTINUATION_PROMPT.md` — "a leg that discovers its gate along the
way has no gate" — the gate below is frozen at this commit and answered in exactly this wording.

### THE GATE (frozen here, before construction)

> **Does `solver/energy_coercivity.py` contain a silent-corruption site — an input inside its
> public API's reachable domain for which it returns a finite, plausible number instead of
> refusing, warning, or reporting the quantity as absent?**
>
> * **yes** → The module underwrites leg 111's zero-width-window finding, which feeds leg
>   141/WEL's publication scoping. Report the magnitude of every site (what the fabricated
>   value is, what the honest value is, and by how much they differ), **ESCALATE, do not
>   patch**, push the branch only, never `main`, and report the leg parked.
> * **no** → The module's public surface refuses or reports every reachable out-of-domain
>   input. Bank the battery as an executable regression (lesson 68: a check that is not
>   executable decays at the rate of memory) and land normally.

Under **both** branches `solver/energy_coercivity.py` is byte-identical to its merge base.

---

## 1. Bans checked first

`/home/andy/projects/Unsolved/.venv/bin/python plan_of_record.py` was run before anything else
(the worktree has no `.venv` of its own; the repo root's interpreter is the one the plan
prescribes). Every ban in force was read. The ones that could touch this leg:

* **"building a solver without grepping `capabilities.py` for the object first"** — honoured.
  `grep -n "energy_coercivity" capabilities.py` → the entry at **lines 224–253**: object "the
  WEIGHTED-ENERGY (Chen-Hou-shaped) coercivity form of the a=0 CLM linearisation — the THIRD
  realization (Route-WE, leg 111)", `"test": "test_energy_coercivity.py"`. Its `validated`
  field carries the ceiling in the entry itself (`gamma = 3`'s apparent `+4e-3` gap "collapses
  like `n^-1`… i.e. it is a statement about the code"). **No solver is built by this leg at
  all**, and no module under `solver/` is edited.
* **"another gCLM measurement leg"** — not engaged. This leg computes no profile, runs no
  sweep, and produces no new gCLM physics number. It pushes *out-of-domain inputs* through the
  *existing* functions and records what comes back.
* **"do not build further ℓ¹-Fourier or collocation machinery for this operator"** — not
  engaged; the module contains no `Y_0`/`Z_1`/`Z_2`/`A`/`ℓ¹_w` quantity (module header lines
  24–28), and neither does this leg.
* **Stage `B`'s three degrees of freedom (space, split, shape)** — not engaged; none of the
  three is a quantity in this file.
* **"re-claiming leg 51's methodological finding at full strength"** — not engaged.

## 2. What is already banked, in this repository, on exactly this question

Prior art *inside* the repo is checked before prior art outside it, because the failure mode
this pass exists to prevent is re-measuring a banked number.

* **The module has exactly ONE commit and has never been modified since creation.**
  `git log --oneline -- solver/energy_coercivity.py` → `41f4ac0` ("Leg 111: LEG — Route-WE: the
  weighted-energy realization is the THIRD one to die on the a=0 CLM linearisation, and the
  window has ZERO width"). One line of history. No bench repair has ever touched it.
* **No adversarial battery exists for it.** The repo carries **23** `test_*_adversarial.py`
  files (`bordered_hl`, `boussinesq`, `boussinesq_velocity`, `collocation_newton`,
  `critical_dissipation`, `decay_collocation`, `first_integral`, `fractional_gclm`, `gclm`,
  `gclm_family`, `gclm_rescaled`, `hilbert_holder`, `hilbert_pointwise`, `hl_rescaled`,
  `holder_norms`, `interval_certificate`, `line_hilbert`, `marginal_flow`, `nk_bounds`,
  `op_lower`, `spectral_utils`, `target_norm`, `weight_search`). **None** is for
  `energy_coercivity`. Its only test is `test_energy_coercivity.py` (190 lines, 14 gates) —
  see §3 for exactly which of its gates already covers which probe below, so that nothing here
  re-measures a banked number.
* **Everything that imports it** (`grep -rln energy_coercivity --include=*.py`):
  `experiments/p2_route_we_v1_coercivity.py` (leg 111's driver),
  `experiments/p2_route_wel_v1_lit.py` (leg 141/WEL's literature runner), `capabilities.py`,
  `test_energy_coercivity.py`. Four call sites, two of them the legs whose conclusions rest on
  it. That is the stake: this module is the *instrument* behind "the window has zero width".
* **The repair template, if the gate answers yes.** Two landed bench repairs define the shape:
  `bench/fix-target-norm-domain-guard` (the merged `target_norm` domain guard) and `9c08287`
  ("Leg 0: BENCH — `first_integral.py` compact-support guard + NaN census"), whose own log
  records leg 107's `96/96` fabricated out-of-support values becoming `96/96` exactly `0.0`
  with a warning. Leg 107 (`5e03e13`, Route-FIA) is also the escalation template: gate YES →
  "ESCALATED not patched", module byte-identical to `origin/main`.
* **The nearest prior audits by shape**, so this one is not a re-run: leg 107 (FIA,
  `first_integral.py` — branch-cut erasure + NaN-absorbing mask), leg 136 (MF2,
  `marginal_flow.py` — a validity *predicate*'s residual misses), legs 139/140 (DGA/NFA,
  `decay_grading.py` / `nk_fourier.py`). **None of them touches a weighted-`L^2` Gram matrix, a
  singular weight family, or a generalized eigenproblem** — the three things this module is
  made of. The probe set below is chosen on those three, i.e. on this module's own surface,
  not on the shape of a previous leg's finding.

## 3. THE PROBES, NAMED AND FROZEN BEFORE A SINGLE NUMBER IS COMPUTED

Ten probes. There is no eleventh, and nothing found outside this list may be reported as this
leg's finding. Each names the *reachable* input, the *documented* behaviour it is tested
against (module header or docstring line), and what the honest answer would be. For each, the
column "already banked?" says which existing gate in `test_energy_coercivity.py` covers it —
those probes exist to *bound* the new ones, not to be reported as findings.

| # | Probe | Documented claim it tests | Already banked? |
|---|---|---|---|
| **P1** | `coercivity_gap(n, "A", gamma)` for **INADMISSIBLE** `gamma = 3, 4` — the module's own header (lines 88–95) says `L^2_phi` contains the basis only for `gamma < 3`, and `coercivity_gap` never calls `admissibility` | does the gap function refuse, warn, or report *absent* on a weight whose space does not contain the trial basis — or return a plausible finite number? | test gate 10 checks the *ceiling* `<= 1/2` for all 7 members incl. `A3`/`A4`; **no gate asks whether an inadmissible member should return a number at all** |
| **P2** | The **`n`-ladder** of the P1 numbers at `gamma = 4` (power-divergent), `n = 16..256`, and their movement under the quadrature dial `n_grade` | lesson 86: a bound dominated by its own evaluation error is a statement about the code. `gamma=3`'s collapse is banked; `gamma=4`'s is not | gate 14 ladders only `gamma < 3`; capabilities.py banks the `gamma=3` ladder (`0.0426 → 0.000121`), **not `gamma=4`** |
| **P3** | `weight_values(theta, ...)` at **`theta = 0` and `theta = pi`** exactly, and at `theta < 0` / `theta > pi` | the family is defined on `(0, pi)` (header lines 75–83) | not tested at all |
| **P4** | `weight_values` / `damping_factor` with an **unknown family string** and with a **non-float `gamma`** | `raise ValueError(f"unknown weight family")` exists in both — is the guard reachable on every path? | not tested |
| **P5** | `graded_quadrature(n_unif=1, 2, 3)` — the panel construction takes `edges[1:-1]` and then *adds* the two end panels | the docstring (lines 267–278) says composite Gauss–Legendre **on `(0, pi)`**; the invariant is `sum(weights) == pi` | gate 5 checks `sum(qw) == pi` at the **default** `n_unif=128` only |
| **P6** | `graded_quadrature(grade=1.0)` and `grade > 1` — the geometric refinement ratio | `grade` is a ratio toward a singular endpoint; `grade >= 1` cannot refine | not tested |
| **P7** | `graded_quadrature(order=0, 1)` and `n_grade=0` | order is a Gauss rule size | not tested |
| **P8** | `coercivity_gap` with **`mu < 0`** (anti-dissipation) — the documented control is `mu > 0` (header lines 113–121) | running the positive control backwards should not silently produce a "measurement" | gate 12 uses `mu = 0, 0.5, 2.0` only |
| **P9** | `coercivity_gap(n, ...)` at **`n = 1, 2`** with `modulate=True` — modulation removes 2 published modes from an `n`-dimensional trial space | with `n <= 2` the modulated trial space is empty; the honest answer is "no referent" (lesson 73) | gate 11 checks `n = 32` only |
| **P10** | `admissibility` reports on **`sin theta` alone** (line 391) while `coercivity_gap` uses `span{sin 1..sin n}` | is a per-mode admissibility claim being read as a whole-basis claim? Measured as the `k`-dependence of the convergence ratio | not tested |

**Controls that can report the other answer** (lesson 90 — a control that cannot come out
differently is not a control):

* **C1 (negative control for P1/P2):** the same call at an **admissible** `gamma = 0, 1, 2`,
  where capabilities.py banks the honest answers `-1.499886, -1.006201, -0.499924`. If the
  battery flagged those too, it would be flagging arithmetic, not corruption.
* **C2 (positive control for P5):** the default `n_unif` must reproduce `sum(qw) - pi ≈ 0`
  while the flagged value differs by a factor this leg must *quote as a number*.
* **C3 (loud-failure control):** at least one probe must make the module **raise**. A battery
  in which nothing raises has not reached the guards; a battery in which everything raises has
  not reached the reachable domain.

## 4. Outside the repository

An adversarial audit of one file's numerical-domain guards is not a literature question, and no
literature claim is made by this leg. The one external input is already banked and is used only
as a ceiling, not re-derived: **Xu, arXiv:2607.19762** (point spectrum exactly `{0, 1}`,
essential spectrum on `Re λ = -1/2`, modulated gap `1/2`) — `KNOWN_ANSWER_CEILING = 0.5`,
module line 156. This leg does not re-check that paper (leg 111 did) and does not extend leg
141/WEL's literature pass.

## 5. Ceiling, pre-committed

Float64, one module, one process. The object is the `a = 0` CLM linearisation — the friendliest
object in the repository — so **nothing here can move a link of the `L1 → L4` chain**, and
nothing here revises leg 111's physics conclusion in the direction of *life*: a silent-
corruption finding on this instrument can only widen the error bars on a **negative**, never
turn it positive. If any probe's result reads like movement on the chain, that is escalation #3
(`ORCHESTRATION.md` §8) and it gets parked, not written into prose.
