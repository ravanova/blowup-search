# Leg 366 — Route-LCB5: novelty / prior-art pass

**Committed before adjudication treated final.** Per `CONTINUATION_PROMPT.md` / `ORCHESTRATION.md`
§0a, a repo-internal check runs first, for each of the three light-corrections items.

## 0. What kind of leg this is

Three independently source-verified light corrections, dispatched as the accumulator's
threshold-3 batch (`writeup/CORRECTIONS.md` §27's own pattern, repeated): (i) a grounds-vs-width
marker at leg 341's S2 site, sourced from leg 359's adjudication; (ii) a fix to leg 229's
already-flagged, already-diagnosed `per_row` zip bug in
`experiments/p2_route_pnrv_v1_postrepair.py`; (iii) a citation-text re-attribution in
`solver/dssp_screen.py`, sourced from leg 364's adjudication. None of the three items required new
derivation — each was located and source-verified by a prior leg before this leg was dispatched.

## 1. Repo-internal novelty check, item (i)

`grep -n "leg 359\|leg_359" experiments/journal/leg_341.md` at leg start: **no hits** — leg 341
predates leg 359 and never cites it. `grep -rn "GROUNDS CORRECTION\|grounds vs width"
experiments/journal/*.md writeup/CORRECTIONS.md` at leg start: no hits anywhere in the repo — no
prior leg has applied this specific marker. Leg 359's own journal (§"Where the object actually
escapes") already states the sharpening in its own words ("this changes leg 341's bottom line
`NOT EXCLUDED` stands but it changes WHY") but stops short of touching leg 341's file, consistent
with leg 359's own territory restriction (`writeup/data/p2_route_l3bd_v1.json`,
`writeup/novelty/leg_359.md`, its own journal only — `experiments/journal/leg_341.md` is
explicitly outside it). This leg is the first to land the pointer at leg 341's own site.

## 2. Repo-internal novelty check, item (ii)

`git log --oneline -- experiments/p2_route_pnrv_v1_postrepair.py` since leg 229's landing commit:
no commit touches the file between leg 229's landing and this leg's dispatch. `grep -n
"zip(v4_rows" experiments/p2_route_pnrv_v1_postrepair.py` at leg start: exactly one hit, the
positional zip leg 229 flagged at `experiments/journal/leg_229.md` §5a ("A real bug this leg's own
code shipped with... Recorded here rather than silently patched... a fix belongs to whoever next
touches this runner"). No prior leg applied that fix; this leg is the "whoever next" leg 229
addressed.

## 3. Repo-internal novelty check, item (iii)

`grep -n "restating/generalising NRS 1996\|Theorem 1.*Tsai" solver/dssp_screen.py` at leg start:
the same two locations leg 364 already located (header comment ~364–367, `deciding_clause`
~534–539 at leg 364's line numbers; both shifted after leg 366's own item-(i)-independent header
growth, re-located by this leg via the string search rather than trusted line numbers). `grep -rn
"leg 364\|leg_364\|NRS 1996.*re-attribut" solver/dssp_screen.py` at leg start: no hits — no prior
leg applied leg 364's flagged re-attribution. `git log --oneline -- solver/dssp_screen.py` since
leg 364's landing: no commit touches the file. This leg is the first to apply the fix leg 364
flagged (out of leg 364's own territory: "flagged here... not implemented as a code change;
`solver/dssp_screen.py` is untouched by this leg").

## 4. Instrument controls run

* **Item (ii):** the "cosmetic, not claim-bearing" claim was not merely re-cited from leg 229's
  prose — it was re-demonstrated live this leg, reconstructing both the OLD (buggy positional zip)
  and NEW (keyed on `(n,a)`) `per_row` blocks from the already-banked `v4_rows` and leg 226's own
  already-banked table (fetched fresh via `git show
  leg/226-pnr-v1-resume:writeup/data/p2_route_pnr_v1_repair.json`, the same fetch the runner
  itself performs). Result: 12/15 spurious mismatches (old) vs 0/15 (new), and the gate's
  `data["gate"]["answer"]` field (built from `v4_verdicts()`, never from `per_row`) is byte-
  identical in the banked JSON regardless — a control that could have failed (had `per_row` fed
  the gate) and did not.
* **Item (iii):** `test_dssp_screen.py`'s full battery (19 checks) was re-run after the citation
  edit, not merely eyeballed. All pass. `git status`/`git diff --stat` on `writeup/data/*.json`
  confirmed empty (no banked JSON touched) — a control that could have failed (had the edit
  accidentally touched operational logic or a test fixture) and did not.

## 5. What this leg is not

No new number derived beyond the two live demonstrations in §4 above (both re-derivations of
already-banked data, not new measurement). No figure (three citation/text corrections, nothing to
plot). No ban touched, no `plan_of_record.py`/`DIRECTION.md` edit. Clay odds unchanged, ~0.05%. No
link of the `L1 → L4` chain moved.
