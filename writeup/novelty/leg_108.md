# Leg 108 — Route-IX2 novelty pass (committed BEFORE any edit)

**Leg.** IX2 — second freshness audit of `writeup/INDEX.md`, window = every leg landed since
leg 68's pass. Light / mechanical-documentation leg.

**Bans checked.** `plan_of_record.py` printed 13 active bans (gCLM measurement, Route-D
bound-sharpening, DSS re-ask, 2D β, scaling gauge, GA-on-unvalidated-fitness, the "closure is a
property of the SPACE" explanation, re-opening stage V, closing the truncation gap by extending
the domain, three readings of leg 51/53, aiming the port at Chen–Hou, building a solver without
grepping `capabilities.py`). **0 of 13 bind this leg**: it produces no measurement, builds no
solver, and touches no numeric claim — `INDEX.md`'s own header forbids that in any case ("Do not
add or edit numeric claims here — links and one-line labels only").

## Is this audit already done?

Three prior passes exist over the same family of index/narrative files. Counted, not asserted:

| Prior pass | Target file | Window it closed | Overlap with leg 108 |
|---|---|---|---|
| pre-leg-68 `DOCS` commit `55dbc5c` | `writeup/INDEX.md` | up to Route-PORT v2 | 0 legs in this window |
| leg 68 (`b3ef49a`, `dbb71ee`) | `writeup/INDEX.md` | legs 53–57, 25/25 quartet pieces verified | 0 legs in this window |
| leg 72 (`117a176`) | `writeup/JOURNAL.md` | 8 gates since the 54–57 entry | different file |
| leg 102 (`12c0894`) | `writeup/JOURNAL.md` | 20 gates since leg 72's pass | different file |

`git log -- writeup/INDEX.md` returns exactly **2 commits**, the most recent being leg 68's
`b3ef49a`. So `INDEX.md` has received **0 edits across the 30 legs that landed after it**, and
this window has **0 prior coverage**. The audit is not a repeat.

## The window, measured

Legs with at least one commit on `main` carrying a `Leg N:` subject and `N > 57`:

`58, 59, 61, 64, 65, 66, 67, 69, 70, 72, 73, 74, 77, 78, 79, 80, 81, 82, 84, 86, 87, 88, 89, 90,
91, 92, 93, 94, 97, 102` — **30 legs**.

`INDEX.md`'s Arc 4 table ends at leg 57 (Route-XS v1) and its "Index currency" paragraph states
so explicitly. **0 of 30 legs in the window appear in `INDEX.md` in any form** — neither as a
quartet row nor as a "no figure by design" note.

## What the 30 legs actually produced (on-disk counts, per leg)

Counted by unioning the file lists of each leg's commits on `main`:

- **BLOG/TECHNICAL pair under `writeup/4_p2_lottery/`: 2 legs of 30** — leg 59 (both files) and
  leg 61 (TECHNICAL only, no BLOG).
- **Registered figure under `writeup/figures/`: 1 leg of 30** — leg 59 (`fig58_weight_repairs_v2.png`).
- **Curated data JSON under `writeup/data/`: 24 legs of 30.**
- **`*_evidence.py`: 1 leg of 30** — leg 59 (`experiments/p2_weight_repairs_v2_evidence.py`, i.e.
  the `Y†` placement drift already named in gap-list item 7).
- **`experiments/journal/leg_N.md` + `writeup/novelty/leg_N.md`: 29 legs of 30** (leg 58 is the
  verify lane and files its pair as `leg_58_verify.md`, not `leg_58.md`).

So **28 of 30** legs produced no BLOG/TECHNICAL and no figure — the "no measurement, no figure"
audit/literature convention the thesis describes. That convention is real and dominant, but
`INDEX.md` records it for exactly **2 routes** (Route-D advection scope and Route-D v15, gap-list
item 6) and for **0** of the 28 legs in this window. The convention is therefore *not* currently
reflected in `INDEX.md` for this window — it is assumed.

## One pre-existing gap found outside the window

`writeup/4_p2_lottery/BLOG_P2_WEIGHT_REPAIRS_V1.md` + `TECHNICAL_P2_WEIGHT_REPAIRS_V1.md` +
`experiments/p2_weight_repairs_v1.py` + `writeup/data/p2_weight_repairs_v1.json` +
`writeup/figures/fig48_weight_repairs_v1.png` all exist (commit `9d9b7ea`, pre-dating the
`Leg N:` subject convention, so it carries no leg number). This route has **4 of 5 quartet
pieces on disk and 0 rows in the Arc 4 table** — `INDEX.md` mentions it only obliquely, in
gap-list item 8, as the other half of the duplicated `fig48` number. Leg 68's pass did not catch
it because leg 68 scoped itself to legs 53–57 by number, and this route has no number.

## Verdict

**PROCEED, gate answers NO.** 30 legs unaccounted, 3 routes owed a table row (Weight-repairs v1,
Weight-repairs v2 / leg 59, Route-KA v1 / leg 61), 28 legs owed an explicit convention note, and
1 currency paragraph is stale by 30 legs. No novelty claim is made — this leg asserts nothing
about the mathematics, only about which files exist.
