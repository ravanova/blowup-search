# Novelty pass — leg 296, Route-ASR (reconcile `a*`'s pinning status across legs 125/210/283/284)

**Date:** 2026-08-07. Branch `leg/296-asr-v1`. Committed **before** any edit to any historical
file, per the standing discipline.

**Leg class.** Record reconciliation. No compute, no solver module imported, no profile solved,
no new number produced. Every quantitative fact this leg states is carried from legs 125, 210,
283, or 284's own journals, read in full. The only original work is the **method-by-method
comparison** (§2) and the **mechanistic explanation** it yields for why leg 210's exclusion and
leg 284's re-pin do not describe the same thing (§3).

**BANKED NUMBERS AT RISK: 0.** No shared ledger opened for writing (`plan_of_record.py`,
`CONTINUATION_PROMPT.md`, `experiments/JOURNAL.md`, `DIRECTION.md` — DM/orchestrator-owned).
No solver module touched. No test's assertions changed.

---

## 1. What each of the four reports actually measured, read before forming any conclusion

| leg | quantity computed | mechanism | precision / convergence evidence |
|---|---|---|---|
| **125** | `a*` where `Δ(a) := 2c_l/\|c_ω\| − 1` crosses zero, at `ν = 0` (a **steady-state scaling-consistency** condition on the profile equation's own coefficients, no Newton solve of `Ω` required at the crossing itself) | `Δ(a)` swept over `a ∈ [0.30, 0.70]`, every point converged to machine precision; crossing at `a* = 0.3864963972206034`, "determined two independent ways that agree" | relative residual **1.933e-15** (`TECHNICAL_P2_ROUTEM2P_V1.md` §3.5) |
| **210** | zero-crossing of `ν(a)` along the branch reached by **restarting Newton from Chen's profile at each fixed `a`** (verifying leg 185's reparametrized continuation) | a **scan**, not a bisection (repaired from an earlier bisection that silently stepped through non-convergent points, per its own lesson-67 note); bracket taken only **between converged scan points** | bracket **[0.36, 0.37]**; **`a = 0.3865` itself fails to converge** in this scheme, relative residual **6.0e-03**; single grid (`n = 401`), not refined at this `a` |
| **283** | no measurement of its own — carried leg 210's finding into the catalog and leg 185's journal, append-only | pure record-maintenance sweep (21 patterns, 1,239 files) | N/A — explicitly 0 numeric values changed |
| **284** | `a` at which the Object-B branch **folds** (turning point), by **pseudo-arclength continuation** in `(Ω_odd, ν, a)` from the independently-converged `a = 0.30` root — `a` is an *unknown*, not a fixed scan parameter, so the fold is traversable rather than a convergence failure | fold estimate on 4 grids: **0.39259 → 0.38626 → 0.38592 → 0.38568**, monotone convergence; amplitude at the fold stable (~1.506) | fold-location spread across the ladder **0.008719** (pre-registered pass threshold 0.02, P5 PASSES); re-pin **0.3857 ± 0.004** |

**The load-bearing structural fact, read directly off leg 284's own Finding 2 (singular Jacobian)
and Finding 4 (the fold):** the augmented Jacobian at fixed-`a` Newton solves collapses toward
singularity as `a` approaches the region where the branch turns around — `σ_min/σ_max` at Chen's
`a = 1/2` (well past the fold) is already **3.454e-19** at `n = 1601`, seven orders below the
control's drift. A fixed-`a` Newton scheme (which is what both leg 185's continuation and leg
210's independent re-derivation are — see §2 of leg 210's own report: "restarting Newton from the
Chen profile at every `(n, a)`") has no way to track a branch through a point where its own
Jacobian is singular. Pseudo-arclength continuation (leg 284's method) is the standard cure for
exactly this failure mode — it adds the fold direction to the unknown vector so the augmented
system stays nonsingular through the turn.

## 2. The hazard this leg must not commit

**Retrofitting the mechanistic explanation to match the desired conclusion.** The check against
this: leg 210's own report already states, independently of anything leg 284 later found, that
`a = 0.3865` fails to converge in its scheme (6.0e-03 residual) and that its amplitude
**collapses toward the trivial null** as `a` increases past its measured range (`a = 0.45`:
160× collapse; `a = 1/2`: further collapse) — this was written into leg 210's journal **before**
leg 284 existed, so the symptom of "something breaks down for this instrument exactly past
`a ≈ 0.37–0.39`" is not something this leg is inventing after the fact to explain away an
inconvenient result. Leg 284 independently supplies the *reason* (a fold with a singular
Jacobian) for a symptom leg 210 had already recorded without explaining it.

**Over-claiming: treating this as re-establishing leg 185's specific crossing claim.** It does
not. Leg 185's claim ("my `ν(a)` sign-crossing lands on leg 125's `a*`") stays refuted — leg
185's own two starts straddled zero at `a = 0.3865`, and that internal inconsistency is untouched
by anything below. What is newly supported is a *different* claim: that `a* ≈ 0.386` is a real
feature of Object B (a turning point), independently corroborated by 125 (algebraic crossing,
`ν = 0`) and by 284 (pseudo-arclength fold, `ν < 0` side), with leg 210's exclusion explained
rather than outvoted.

**Deleting or rewriting leg 283's record.** Leg 283's UNPINNED note was the correct reading of
the evidence available on 2026-08-07 at the time it was written (leg 284 had not landed). This
leg's edits are pointers appended after leg 283's text, not replacements.

## 3. Pre-registered gate reading, before any file is opened

Per DIRECTION.md §296's gate: does a careful read yield ONE defensible catalog state?

**Pre-registered expectation: (a) — a* IS pinned near 0.386**, on the strength of: three
independent methods (125's algebraic `Δ(a) = 0` crossing at machine precision; 284's
grid-converged pseudo-arclength fold, four-grid monotone convergence; and 210/185's `ν(a)`
sign-law, which itself independently establishes that a sign change *must* occur somewhere,
consistent with — not contradicting — a turning point) place the feature at `0.386 ± 0.004`,
and leg 210's exclusion bracket is explained by a named, evidenced mechanism (fixed-`a` Newton
scanning cannot resolve a fold; its own report shows exactly the predicted symptom — non-
convergence and amplitude collapse toward the trivial null in the same neighborhood) rather than
constituting a competing measurement of the same quantity.

This expectation is checked against the full texts in §1 above before any catalog file is
touched, and is stated here so it cannot be silently adjusted after the edit.
