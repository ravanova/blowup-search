# Leg 197 — Route-VNL novelty / literature pass (run FIRST, committed BEFORE the append)

**Date:** 2026-08-06. **Branch:** `lit/197-vnl-v1`. **Route:** VNL — bank `arXiv:2208.09445`
in `solver/viscous_novelty.py::PRECEDENTS`, append-only.

**This pass is deliberately light, and the reason is banked, not asserted.** Leg 174
(Route-VBS) already read `arXiv:2208.09445` at **primary-source, full-PDF depth** and banked
the result with three locators and three verbatim quotes in
`writeup/data/p2_route_vbs_v1_scoping.json` (ledger key `BCG-NS`). The novelty question for a
ledger-append leg is therefore **not** "has someone done this?" — it is the narrower and more
falsifiable one:

> **Am I transcribing leg 174's characterization faithfully, or am I trusting a secondhand
> summary of it?**

That question is answered in **code** (`experiments/p2_route_vnl_v1_ledger.py`, check `C2`),
not in prose: eleven load-bearing claims of the appended row are checked for substring
containment against leg 174's own JSON fields. A paraphrase that drifts off leg 174's wording
**fails** the runner. **11/11 traceable.**

---

## The one fresh check, and why it is the right size

Leg 174 read the PDF; this leg does **one** independent primary-source spot check, because the
NO branch of the gate is reserved for exactly the case where a fresh look contradicts leg 174.

### Q1 (2026-08-06) — direct fetch of the abstract page
`https://arxiv.org/abs/2208.09445`

* **Title returned:** *Smooth imploding solutions for 3D compressible fluids* — matches.
* **Authors returned:** Tristan Buckmaster, Gonzalo Cao-Labora, Javier Gómez-Serrano — matches.
* **The Navier–Stokes clause, verbatim:** *"we provide simplified proofs of linear stability
  and non-linear stability, which allow us to construct asymptotically self-similar imploding
  solutions to the compressible Navier-Stokes equations with density independent viscosity for
  the case γ=7/5"* — matches leg 174's transcription word for word.
* **Mentions of computer assistance in the abstract: 0. Of interval arithmetic: 0.** This
  reproduces leg 174's stated *reason for reading the PDF instead of the abstract* — the
  computer-assisted part is invisible at abstract depth. (The only abstract-page trace is the
  submission metadata's "16500 lines of code".)
* **The Grade-B shape is visible in the abstract independently of leg 174's reading:** exact
  self-similar imploding solutions are constructed for the **Euler** equations; Navier–Stokes
  is reached *from* them via stability proofs. That is the "theorem viscous, certificate
  inviscid" structure, stated by the authors themselves.

**No discrepancy. The NO branch does not fire.**

**What this spot check does NOT re-verify**, stated so the ceiling is honest: the interior
locators — system (1.5), §7's domination sentence, §1.3's 10000 coefficient pairs / 14
CPU-hours, Lemmas A.27/A.28, Appendix B — are **leg 174's full-text reading**, transcribed
here, not re-read. This leg transcribes; it does not re-derive.

---

## The prior art *inside this repository*, which is the part that matters here

| where | what it says about `2208.09445` | status |
|---|---|---|
| leg 113 (`experiments/p2_route_ms_v1_lit.py`) | read at full text; DOI + journal ref + 2 quotes; classified `clause_2_inviscid: True` | **correct** on leg 113's own definition (the *certified* equation carries no dissipative term) |
| leg 174 (`writeup/data/p2_route_vbs_v1_scoping.json`) | ledger key `BCG-NS`; Grade **B**; occupancy cell `fluid=True,grade=B` | the characterization this leg transcribes |
| leg 174's own audit | `arXiv_2208.09445_in_viscous_novelty_PRECEDENTS: false` | **the hole this leg closes** |
| `viscous_novelty.SEARCH_LOG` | 0 of 12 queries mention *compressible*, *implosion* or *imploding* | re-measured this leg: still 0, and **left unedited on purpose** |

The `SEARCH_LOG` is **not** rewritten. It is a record of what stage V actually asked on
2026-08-04, and back-filling it would destroy the evidence that stage V's gate *could not*
have found this paper.

---

## What the append does not claim

* **Stage V's gate answer does not move.** The appended row is `EXCLUSION` under this module's
  own vocabulary — `PRE_EMPTS` grades the **certified** object, and BCG's certified object is
  the inviscid Euler ODE. `novelty_verdict()` returns `YES` on `arXiv:2410.05480` alone,
  bit-identically, before and after (runner check `C3`).
* **Leg 174's occupancy matrix does not move.** *fluid-adjacent × Grade A* is still **EMPTY**.
* **No progress on this repository's own object.** Clay odds unchanged, **~0.05%**.

Reporting a ledger-hygiene fix as anything larger would be the move leg 42 deleted seven
claims for.

---

**Standing bans checked.** `plan_of_record.py` run first; every live ban read.
*"Re-opening stage V as posed"* — **not tripped**: this leg builds no certificate, floats
nothing against any margin, and runs no solve; it edits stage V's ledger *data*, not its
question. *"Another gCLM measurement leg"* — not tripped, no gCLM object touched. *"Build
nothing without grepping `capabilities.py`"* — grepped (`grep -n -i viscous_novelty
capabilities.py`, lines 493–506); no new module is built and the existing capability entry
stays accurate, so it is not edited (and is outside declared territory besides).
