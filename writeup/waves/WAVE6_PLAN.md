# WAVE 6 — PLANNED AND COMMITTED BEFORE DISPATCH (§3g step 1)

**Committed 2026-08-18, BEFORE any worker was dispatched.** Gates and pre-committed readings are in
their FINAL WORDING here. **Nothing in this file may be re-worded after dispatch** — including by a
user ruling that lands mid-wave, which is required at integration instead (that happened in wave 5
and was refused; see `reports/ORCH_STATE.md`).

**Why these three.** §3f rule 3: the wave **opens with construction** (`L6`) and the **verifier goes
last** (`V-W5`). §3i q7 has now fired **twice running** — the last six landed units were reading,
grading, verification, infrastructure, construction, verification — and wave 5's debt list made
construction **binding**, not conditional. `L6` is that construction, and it is not a small one.
`V5` discharges the obligation the user made **not optional** (ruling Q4) and carries the wave-4
repair with it, because it is the one unit that will have the same full text open in front of it.
`V-W5` discharges the standing fact that **every unit of wave 5 is `UNVERIFIED`** and that the
Conductor **may not verify a wave it planned**.

**A gate defect of my own, corrected before it repeats.** `L5`'s wave-5 gate said *"on route 4's
**banked** discrete profile"*. **Route 4 has no banked profile** — the gate as worded was
unsatisfiable, `L5` said so in its artefact and substituted leg 381's banked synthetic. Registered as
`writeup/CORRECTIONS.md` §34. **`L6` exists to remove the deficiency itself**, and no gate below names
an artefact I have not checked exists.

---

## `L6` — Lane L. **CONSTRUCTION. Dispatched FIRST.** ~10² agent-h. The wave's centre.

**The object.** Route 4 is the only live constructive route on the Clay chain, and **it has never had
a discrete profile banked**. Every route-4 measurement this programme has made — leg 381's cutoff
bill, `L5`'s clause-(b) bill — was made on a **synthetic** exactly-DSS poloidal field standing in for
an object nobody has built. That is why `L5`'s exponent is a **class fact** while its constant
`c_mod = 869.288` **is not route 4's number**, and it is why W4's two shut clauses are shut on a
stand-in rather than on the thing itself.

**GATE, final wording.** **Bank a discrete route-4 profile**: a divergence-free velocity field on a
named realization, in a named trial space and basis, solving route 4's profile system to a **measured**
residual, with the artefact carrying the residual, the realization, the trial space, the basis, the
resolution and the norm in which the residual is measured. Then answer, **with the number**: **what is
the smallest residual, in the norm route 4's own closure requires, that this construction attains at
its best affordable resolution — and does it decrease under refinement, YES or NO, at what measured
rate?**

**Pre-committed readings.**
(a) A **residual that decreases under refinement** is a **LEAD, NOT A CERTIFICATE and NOT A BLOW-UP.**
It must be reported with what certification would cost (§3d) before anything else is said about it,
and **Tier 2 is never a proof.**
(b) A residual that **stalls or grows** under refinement is a **real, landable result**: it prices
route 4's construction problem for the first time and tells the next unit whether the obstruction is
the ansatz, the space, or the resolution. **It must not be softened.**
(c) **IF THE CONSTRUCTION COLLAPSES TO AN EXACTLY-(D)SS OBJECT, SAY SO, STOP, AND SAY WHICH CHANGE OF
VARIABLES DID IT.** The `α = 1` pin then applies and the result is a `NO` about route 4 itself. That
would be the single most valuable thing this unit could find, and finding it early is a success.
(d) **A `NO` because the resolution was unaffordable is `UNDER-RESOURCED` WITH A COST (§3d), never a
bare `no`** — name the resolution reached, the wall-clock it cost, and the resolution the answer needs.
(e) **Lesson 91 is the gate's own wording here:** realization, trial space, basis. A residual without
all three is not a measurement and will not be landed.
(f) **C1 BINDS AND THE CONDUCTOR CANNOT WAIVE IT.** If this unit reaches for a `Y₀/Z₀/Z₁/Z₂`
radii-polynomial contraction **in any space**, it is inside the ban unless it, **in its own
pre-registration**, (1) names its apparatus with a citation and (2) shows that apparatus does not
construct a single bounded approximate inverse uniform in `M`. **C1 may not be cited as evidence that
any apparatus closes for any object class.** C1 stands **EXEMPLAR-FREE**.
(g) **A ban is superseded by a MEASUREMENT, never by a decision.** If a ban's *wording* obstructs,
that is a **user escalation** — record it, do not rule it, and do not stop.
(h) **Scale is not evidence.** A bigger run is not a result. **No output is movement toward Clay
unless a link of the `L1 → L4` chain actually moved**, and none has in 400 legs.

**Territory.** New files only, plus insert-only appends to its own journal. **No banked JSON may be
edited.** Pre-register the gate, the readings and the realization **before any result-producing code**,
in their own commit.

**COMMIT DURING THE RUN, NOT ONLY AT THE GATE.** A container is ephemeral; unpushed work is lost work.

---

## `V5` — Lane V. **OBLIGATORY (ruling Q4, not optional).** ~4–8 h, plus ~1–2 h for the repair.

**The object.** `arXiv:2509.25116` currently occupies the Grade-A × fluid cell of leg 174's census
and is what W3's *"no certified blow-up exists for any fluid equation"* now stands against on the
prose test. It has been **graded** but never **adversarially audited at leg-309 depth**. `V-W4` has
already found the first thing this audit must confront: **it carries no journal-ref.**

**GATE, final wording, TWO CLAUSES THAT MUST BE ANSWERED SEPARATELY.**
**Clause 1 — the audit.** Enumerate the paper's load-bearing constants **before** adjudicating
anything; recompute every recomputable one; check the localisation step (Remark 2, §1.3 together with
§2) at full text. **Does the certificate close as claimed — YES or NO — and if any constant fails to
recompute, name it, quote the failing clause verbatim, and give both numbers?**
**Clause 2 — three-dimensionality, ANSWERED ON ITS OWN.** **Is the certified profile genuinely 3D
against W2's own pre-committed test — YES or NO?** **W2's test is the arbiter and this answer MUST NOT
be folded into the clause-1 verdict.** Wave 2 measured that two certified rows elsewhere were **2D
lifts**; that is the failure mode this clause exists to catch.

**Pre-committed readings.**
(a) **A `YES` on clause 1 does not move W3 by itself** — ruling Q1 already put W3 on the prose test —
and **a `NO` does not vacate the cell**, which is occupied as a matter of grading. Say what changes and
what does not, in the wall's own words.
(b) **A `NO` on clause 2 is the more consequential answer of the two** and must be stated plainly even
if clause 1 returns `YES`: a 2D lift in the Grade-A × fluid cell is a **W2** finding, not a W3 one.
(c) **Branches are pre-committed before reading**, in this unit's own pre-registration, and the
adjudication is written against them afterwards — not the other way round.
(d) **Quote verbatim or do not quote.** `V-W4` found four fields in wave 4 presented as verbatim that
were not. **Character-exact, with location, or paraphrase and label it a paraphrase.**
(e) **The journal-ref finding is NOT this unit's to rule.** `PUB_0C` §1's *"any published work"* is an
**open user escalation** (`writeup/escalations/ESCALATION_PUB0C_PUBLISHED_2026-08-18.md`). Record what
you observe; **do not grade the criterion, and do not stop for it.**
(f) **Contacting the authors is PROHIBITED.** Reading published material is authorised.

**SECOND DELIVERABLE — the wave-4 repair (`V-W4`'s `D1`–`D6` and `N1`).** Ruling Q3 stands: **the
banked artefact is not edited.** Re-issue corrected artefacts as **new `_v2` files** — `v1` untouched,
each `v2` carrying a `supersedes` field, a `correction_record` pointer to `writeup/CORRECTIONS.md`
§33, and **only** the fields §33 names. `D1`–`D4` are quote/attribution fields of `V3`'s rows on this
very paper, which is why they belong to the unit that will have the full text open. `D5`/`D6` are
`L2′`'s pin provenance: the `≤ 1` direction runs through the **local** suitable-weak-solution form
(Seregin, `arXiv:math/0510396` §1), **not** the global Leray–Hopf form, and **ESŠ stays `UNREACHABLE`
at primary — banked as such, never as a zero.** `N1`: `326.875` per decade is **amplitude-dependent**
and must not be re-issued as a portable constant.

**Territory.** New files only. **No `v1` artefact may be edited.** Do not touch `L6`'s files, any
Conductor file (`STATE.md`, `WALLS.md`, `OPTIONS.md`, `ORCHESTRATION.md`, `reports/`), or wave 5's units.

**COMMIT DURING THE RUN, NOT ONLY AT THE GATE.**

---

## `V-W5` — verification. **DISPATCHED LAST. It verifies wave 5, which it did not plan.** ~3–6 h.

**The object.** Every unit of wave 5 — `D-REPAIR` `036e56d`, `L5` `4be46ef`, `V-W4` `b46ee4d` — is
`UNVERIFIED`, and **the Conductor may not verify a wave it planned.**

**GATE, final wording.** For each of the five items below, answer **REPRODUCES** or **DOES NOT
REPRODUCE**, with the number: (1) `L5`'s gate answer `NO` and its `ρ`-exponent `0.00010850007559945518`,
**re-derived from `writeup/data/p2_route_l5_finite_energy_v1.json` and the unit's own code, not from
its report**; (2) `L5`'s three positive controls — `−0.2498` at `α = 1.25`, `−0.5996` at `α = 1.6`,
`−2.000005` for the unmodulated `SS` control at `κ = a` — and **whether they license the claim that the
`NO` is about the object rather than the apparatus**; (3) `L5`'s control `C6`, which **did not fire as
planted** (`2.03e-02` against a `1e-02` tolerance): **was the tolerance moved at any point in the
branch's history, YES or NO**, and does anything the `NO` rests on depend on the constant `C6`
perturbs? (4) `L5`'s `self_hash` `0c5e0f827f526df6` and `V-W4`'s `e0171ac1e855f90e`, recomputed;
(5) `D-REPAIR`'s epoch correction, re-derived from the banked rows.

**Pre-committed readings.**
(a) **REPAIR NOTHING.** Report; do not fix. A verifier that repairs has destroyed the evidence it was
sent to gather. If something is broken, **say what, where, and by how much.**
(b) **A discrepancy that changes no verdict and no number is still a discrepancy** and is reported as
one, with *"what it does NOT change"* stated per item — the form `V-W4` used.
(c) **Exit non-zero if any item does not reproduce.** A green run that hid a disagreement is worse
than a red one.
(d) **Quote verbatim or do not quote** (see `V5` (d)).
(e) **You may not re-plan, re-rank, or pick your own next unit.** Self-terminate at the gate.

**Territory.** New files only. **Nothing belonging to `L5`, `D-REPAIR`, `V-W4`, `L6` or `V5` may be
edited**, and no Conductor file may be touched.

**COMMIT DURING THE RUN, NOT ONLY AT THE GATE.**

---

## What this wave cannot do, said in advance

**None of these three units can move a link of the `L1 → L4` chain**, and `L6` — the only one that
could ever contribute to one — would produce **Tier 2** evidence at best. **Clay stays ~0.05%.**
`W4`'s only unbroken clause is **(c)**, a target not imposing Clay condition (7), and it belongs to
**Lane T, deferred**. Nothing in this wave re-opens Lane T; what wave 6 owes Lane T is a **cost for
`T2″`**, and costing is not ranking.

**Debt carried forward, not attempted here:** `fig81_route_egmf_v1_evidence.py` and the **52**
cited-but-unrebuildable figures (`writeup/check_figure_coverage.py`). It is real and it is not the
priority while construction is the binding debt.
