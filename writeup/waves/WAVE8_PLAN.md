# WAVE 8 — PLANNED AND COMMITTED BEFORE DISPATCH. The paper pivot enters here.

**Written 2026-08-19 while wave 7 is still in flight, and NOT dispatched until wave 7 closes.**
The user directive of 2026-08-19 says the pivot enters at the next wave boundary and that no current
leg is interrupted. Wave 7's four units (`R-bank`, `R-prof`, `L6-b`, `V-W6`) run to their gates.

---

## §0. THE DIRECTIVE, AND WHAT IT DOES AND DOES NOT CHANGE

**TEMPORARY PIVOT TO PAPERS, user ruling 2026-08-19**, scaffold landed `7851899`
(`writeup/papers/README.md` + three `STATUS.md`, no drafts yet). Merged into `main` at `7608027`.

**Why it is not a detour, in the user's own terms:** a draft aimed at a referee is an **audit
instrument this repository does not have**. Every check we run asks *"is this number right?"*; a
draft asks *"can this claim be stated, in full, to someone who does not already believe it?"* — and
that catches a defect class the others cannot. **Leg 348's undischarged ceiling would not have
survived one honest paragraph of related work. The `C1` exemplar would not have survived a sentence
naming what had actually been demonstrated.**

**EACH PAPER HAS TWO OUTPUTS AND THE SECOND IS NOT THE PAPER.** Where a draft cannot state
something cleanly, that is **a finding about the record**: it goes to `FINDINGS.md`, then to
`CORRECTIONS.md` or an escalation. **It is never smoothed over in prose.** A paper that reads well
because its awkward parts were written around has done active harm. **Expect the findings to be
worth more than the drafts, and say so if they are.**

**THE BINDING RULE: A PAPER IS A VIEW OF THE RECORD, NEVER A SOURCE.** Every number cites the
banked JSON field it came from — not a journal, not a previous draft, not memory. A draft may not
state a claim at a confidence the record does not hold: **`UNVERIFIED` stays `UNVERIFIED` in the
draft, `UNDER-RESOURCED` is not written as a null result, and a control that did not fire as planted
is disclosed in the paper.** **NO UNIT MAY CITE A DRAFT** — drafts are downstream of everything and
load-bearing for nothing. **§3k binds**, and the related-work section is exactly where `SOURCES.md`
depth debt becomes visible.

**WHAT DOES NOT CHANGE.** `CLAY_ROADMAP.md` §7.6's posture, the lane rankings, every live gate, and
every live ban. **§3g's wave composition floor STANDS** — paper units are **ADDITIONAL, NOT A
SUBSTITUTE**, and a wave of pure paper units is out of contract. Tier 2 is never a proof. No output
is movement toward Clay unless a link actually moved. **Clay ~0.05%.** Outreach stays held:
**reading published material is authorised, contacting any author, group or maintainer list is
not.** **`P3` (the `2509.25116` audit) is NOT drafted** — its correct first route is to the authors,
outreach is held, and its Class A is unverified.

---

## §1. `L8` — THE CLAY-CHAIN SLOT. **OPENS THE WAVE (§3f rule 3), Lane L, construction.**

**The composition floor is met HERE, not by `PB2`.** `PB2` does attack W4 clause (b)'s citation
base, and the user is right that it is not a paper errand — but it is a **read**, and a wave whose
only wall unit is a read is the audit/instrument loop §3i q7 exists to catch. Wave 7's last three
units were instrument-kind. **Wave 8 opens with a measurement on the object.**

**WHICH unit this is depends on `L6-b`, which is in flight. The DECISION RULE IS PRE-COMMITTED HERE
so that the answer selects the unit and not the other way round.**

**BRANCH (i) — `L6-b` returns a material drop (smallest residual below `1.45`).** Then `L6`'s ladder
was measuring the OPTIMISER BUDGET, not the ansatz, and the refinement `NO` is not a statement about
route 4. **The unit is `L6-c`: re-run the refinement ladder at the budget.** GATE, final wording: at
rungs `J2`, `J3`, `J4` (`n_dof` unchanged from `L6`), branch B, realization and norm unchanged, with
**iteration cap 20,000 at every rung** and **at least one independent seed per rung in addition to
the continuation**, answer with numbers: **is `d log ρ / d log n_dof` materially negative over the
last three rungs — YES or NO?** Pre-committed reading: a `YES` re-opens `L7` and `L4` at prices that
must then be re-derived, not inherited; a `NO` restores `L6`'s conclusion **with the budget defect
discharged**, which is strictly stronger than what the record holds today.

**BRANCH (ii) — `L6-b` returns no material drop.** Then the stall is the CONSTRUCTION, the `NO`
hardens, and the cheapest thing that could still kill the priority lane is the question `L6` never
asked: **is it route 4 that has no profile, or is it this basis that cannot represent one?** The
unit is `L6-d`: same object, same norm, same `n_dof`, **a genuinely different trial space** (a
second, independently constructed basis for the poloidal–toroidal potentials — named before it is
run, per §3k rule 3). GATE, final wording: **what is the smallest residual the second trial space
reaches at cap 20,000, and is it materially below `1.6138` — YES or NO?** Pre-committed reading: a
`NO` from two independent trial spaces is a much harder statement about **route 4** than one from
one; a `YES` says `L6` measured its basis, and every route-4 number in the record is basis-bound —
which is `CORRECTIONS.md` §35's `1.476×` finding recurring one level up. **Either way no `L1→L4`
link moves.**

**Cost, both branches: ~10¹ core-h**, priced from `L6`'s banked 1,240 s / 800 iterations at
`n_dof = 6720`. Checkpoint above ~1 h to a tracked path.

---

## §2. `PB2` — READ BOTH JAWS OF P2's PINCER AT PRIMARY. **Lane L. Absorbs the queued `L7-src`.**

**THIS IS NOT A PAPER ERRAND, and the user said so: W4 CLAUSE (b) IS CURRENTLY RECORDED AS SHUT AND
VERIFIED ON TWO THEOREMS THIS REPOSITORY HAS NEVER OPENED.** `V-W5` called it the sharpest
undischarged item in the file. `§3k` rule 2 **forbids drafting `P2` past its claim statement** until
this is discharged. The previously queued `L7-src` (NRŠ 1996 alone) is **absorbed into this unit** —
same work, wider scope, one dispatch.

**GATE, final wording.** Obtain and read **at primary**: (a) **Chae–Wolf Thm 1.1 and Rmk 1.2**;
(b) the **ESŠ step** as actually used — noting the record already says ESŠ is `UNREACHABLE` at
primary and that `V-W4` re-routed the `≤ 1` direction through the **local** suitable-weak form
(Seregin, `arXiv:math/0510396` §1); (c) **Nečas–Růžička–Šverák**, ARMA 136 (1996) and (d) **Tsai**,
ARMA 143 (1998). For **each**, answer with the hypothesis quoted verbatim and located by page or
section: **does the theorem, as stated in its own text, apply to THIS object — a nontrivial backward
`λ`-DSS profile with `λ = 1.7`, `a = 0.5`, on `ℝ³` — YES or NO?** Then answer the pincer question:
**does the jaw close as `WALLS.md` cites it, YES or NO?**

**Pre-committed reading, both directions.** If every jaw closes as cited, W4(b) stands and the debt
is discharged — **that is a real result and it is `P2`'s precondition.** **If ANY jaw does not close
as cited, THAT IS THE FINDING**: it re-opens a wall closure the record currently calls **shut and
VERIFIED**, it goes to `CORRECTIONS.md` and to `WALLS.md`, and — per the directive — **it goes to
the user IMMEDIATELY, not at the end of the wave.** A source that cannot be obtained is banked as
`UNREACHABLE` **with the refusal itself recorded**; that is a result, not a failure, and it is
exactly leg 348's shape if it is left implicit.

**PROHIBITED IN THIS UNIT: contacting any author or group, and any paywall circumvention.** Reading
published material is authorised. `SOURCES.md` depth is updated **in the same commit** as the read.

---

## §3. `PB1` — RUN `P1`'s OWED NOVELTY CHECK. **Owed since wave 1. Cheap, and it can kill `P1`.**

**GATE, final wording.** Are **either** of `P1`'s two effects already in print: (a) that a **scalar
recurrence score used as an admission filter biases the recovered orbit set along any coordinate the
score is monotone in**, and (b) that **re-mining a fixed trajectory silently re-finds what the first
run already found**? Search the Chandler–Kerswell / Lucas–Kerswell / Cvitanović line and the wider
recurrence-mining literature. Answer, per effect: **is it in print — YES or NO — and if YES, in
which paper, at which page, stated how strongly?**

**Pre-committed reading, both directions.** **A `YES` KILLS `P1` AND IS A GOOD RESULT** — it is the
cheapest possible way to learn it, and it is worth more than a draft. A `NO` is a **controlled**
negative and only counts as one under **full leg-392 instrument discipline**: positive control,
topical positive control, nonsense negative control returning **exactly 0**, and AND-operator
control pairs, **all firing before any substantive verdict**; every query outcome labelled `OK` /
`THROTTLED` / `FAILED` and **`THROTTLED` NEVER RENDERED AS A ZERO ANYWHERE**; and the **opensearch
namespace actually served banked verbatim from the raw feed** (leg 387's exact failure — checked,
not assumed).

**A controlled zero is not evidence that nothing is in print** — leg 348's ceiling clause governs
this unit too, and the verdict must state which parts of the literature the instrument cannot reach
(pre-arXiv sources especially). **RUN THIS BEFORE `P1` IS DRAFTED PAST ITS TITLE.**

---

## §4. `V-W7` — THE VERIFIER. **DISPATCHED LAST. Verifies wave 7, which I planned.**

`R-bank`, `R-prof` and `L6-b` land `UNVERIFIED` and say so in their own gate answers. `V-W7`
verifies all three against the gates committed in `WAVE7_PLAN.md` **before** they ran, re-runs their
evidence scripts, checks at least one selftest by breaking it in a scratch copy, derives territory
from `git`, and reports what it could **not** check with a price. It also rules on **my** integration
commits, as `V-W6` was briefed to rule on mine for wave 6.

---

## §5. WHAT SLIPS, AND WHY — FLAGGED, NOT SILENTLY DROPPED

**`P4-METHODOLOGY`'s draft slips to wave 9.** The directive says P4 may proceed while `PB1`/`PB2`
run, and I would have run it, but **wave sizing is 2–4 workers (§3g)** and the four slots above are
each mandatory: the Clay-chain floor (§1), the two paper blockers the directive names (§2, §3), and
the verifier for a wave I planned (§4). Dropping any of those four to add a draft would breach a
standing rule to satisfy an ordering preference. **P4 is still the FIRST draft written** — `P1` and
`P2` cannot start until `PB1` and `PB2` clear, which is wave 8 at the earliest — so the user's
stated order is preserved even though the concurrency is not. **If the user wants P4 concurrent, say
so and I will drop `PB1` to wave 9 instead: it is the only one of the four that is not mandatory.**

**Also still queued:** `L5-nov` (~0.5 h, owed on the run's own output); `R4-a`, `R2`, `R3` in Lane R;
`E-FE` if `R-bank`'s 160/160 gate clears, which is a wave-7 decision, not a wave-8 one.

**P4's own blocker is carried forward verbatim so it is not lost:** the paper **needs an honest
account of what the discipline FAILED to catch and for how long** — leg 348's ceiling ran **45
legs**, and the `C1` ruling was made on a packet that inherited it. **A methodology paper that omits
its own misses is the failure mode it describes.** It is not a tour of the repository.

---

## §6. STANDING CLAUSES — IN EVERY BRIEF

**COMMIT DURING THE RUN, NOT ONLY AT THE GATE.** Checkpoint above ~1 h wall to a **tracked** path.
Explicit-paths-only commits while siblings share the tree; never `git add -A`, never `commit -a`.
**Tier 2 is never a proof. No output is movement toward Clay unless a link actually moved. Clay
~0.05%. §3k binds. A ban is superseded by a MEASUREMENT, never by a decision. Scale is not
evidence. I may not verify a wave I planned.** **PROHIBITED:** external contact of any kind; a
learned or evolved seed-scoring fitness (leg 349); more seed supply for `PROG-R4`; a grinder on the
current realization; screening as a unit of work; describing a retraction as progress; drafting
`P3`; **softening a caveat because it reads badly in a paper — in two of the three papers the
caveats ARE the result.**

---

## AMENDMENT 1 — 2026-08-19, WRITTEN BEFORE DISPATCH, ON `V-W6`'s RETURN. §1's slot changes.

**§3i q5 forced this and it is recorded in the same commit that landed `V-W6`**
(`writeup/waves/WAVE7_CLOSE.md`, `V-W6` §3i q5). §1's `L8` — the branch decision keyed to `L6-b` —
is **DEFERRED VERBATIM TO WAVE 9**, unchanged, still keyed to `L6-b`'s banked answer, which does not
expire and which `L6-b` will have banked by then. **§1's slot in wave 8 is taken by `L-JVER`.**

**Why.** `V-W6`'s single most important unchecked item is whether `J(c)` **as coded** is the
`L^{3/2}` norm of the curl of route 4's profile residual. The only internal evidence is selftest
`T_D` at `1.22e-4`, which compares two of `L6`'s **own** implementations. Every route-4 residual
number in the record is downstream of that one function: `L5`'s `c_mod = 869.288`, `L6`'s
`ρ = 1.613811231995397`, and `L6-b`'s answer whichever way it lands. **Either branch of `L8` would
spend a full wave refining a functional nobody outside its own author has ever computed.** Buying
the check first is strictly cheaper than buying it after another ladder. `L-JVER` is a Lane L
construction unit attacking W4/W5 directly, so the §3g composition floor is met and the wave now
**opens with construction** (§3f rule 3), which also repairs the 3:1 instrument skew flagged at §5.

### `L-JVER` — INDEPENDENTLY RE-IMPLEMENT `W[V]` AND `J(c)` IN A DIFFERENT BASIS

**Object.** The same banked profile `L6` banked: `writeup/data/p2_route_l6_profile_v1.json`,
`banked_profile.B` (and `.A` as the second case), `n_dof = 6720`, λ = 1.7, `a = 0.5`, the
Chandrasekhar poloidal–toroidal representation, `L5`'s norm `‖curl F‖_{L¹_t L^{3/2}_x}`.

**What the unit must NOT do.** It may not import, call, copy or transliterate
`experiments/p2_route_l6_v1.py`'s `W`, its quadrature, its basis, or its norm assembly. It may read
them — it must, to know what it is checking — and it must then build its own from the **written
mathematics**, in a genuinely different basis and a genuinely different quadrature. Reading the
existing code and re-typing it is the failure mode this unit exists to exclude, and it must state,
in its artefact, exactly which choices differ.

**THE GATE, PRE-COMMITTED, TWO-SIDED.**

> Evaluate the independently built `J` at `L6`'s banked coefficient vector for branch B at
> `n_dof = 6720`, and at branch A, and at a third point that is not a minimiser (`L6`'s coarsest-rung
> banked vector, re-expressed). **Report the relative difference `|J_new − J_L6| / J_L6` at each of
> the three points. Is it below `1e-3` at ALL THREE — YES or NO?**

**Pre-committed reading of each answer, written before the number exists.**

- **YES** ⟹ `J(c)` is confirmed as coded to the stated tolerance, at three points including a
  non-minimiser. `L5`'s `c_mod`, `L6`'s `ρ` and `L6-b`'s answer keep their standing. **This is the
  expected outcome and it is worth a wave slot anyway**, because it is the only thing that converts
  a self-comparison into a check. It does **not** move any `L1→L4` link and must not be written as
  progress.
- **NO at any of the three** ⟹ **the finding, and it is a large one.** Every route-4 residual number
  in the record is suspect, `L5`'s clause-(b) closure is resting on it, and it goes to the user
  **immediately**, not at the end of the wave. `L6-b` is then measuring an unknown functional and
  its answer must be held. Do **not** repair `L6`; report.
- **The unit cannot build the second implementation at all, or cannot reach `n_dof = 6720`** ⟹
  **`UNDER-RESOURCED` with a cost (§3d)**, never `NO`. Report the largest `n_dof` reached and the
  price of the rest.

**Discipline.** Controls before verdicts: evaluate both implementations on at least one field with a
**known closed-form** `curl` and a hand-computable `L^{3/2}` norm, and report that control **before**
the three-point comparison; if the control does not fire, say so and the verdicts are void. Bank the
disagreement at every one of `L6`'s five rungs, not only the top. Bank a `self_hash` under the
repository's rule. Do not edit any banked artefact. Do not edit `solver/` or
`experiments/p2_route_l6_v1.py`. **§3k binds**: name Chandrasekhar and Byrd–Lu–Nocedal–Zhu 1995 in
`SOURCES.md` with a DEPTH — `D-VW6-6` records that neither has a row, and that gap is the landing
commit's, so this unit closes it.

**Price.** ~1 unit-week of construction, 1–5 core-h of compute. **Checkpoint above ~1 h; commit
during the run, not only at the gate; explicit paths only; never `git add -A`, `git add .` or
`git commit -a`; no `checkout`, `stash`, `reset` or rebase; do not push.**

**Wave 8 is therefore: `L-JVER` ‖ `PB2` ‖ `PB1` ‖ `V-W7`** — construction first, verifier last, four
units, at the §3g cap. §2, §3, §4 and §6 are unchanged. **`L8` moves to wave 9 with its branch rule
intact.**
