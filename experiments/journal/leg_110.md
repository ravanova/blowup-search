# Leg 110 — Route-L1R: reproducing both `L1` death certificates from their own banked data

**Branch** `leg/l1r-v1`. **Exploration leg, audit, CLAIM-BEARING** (it can only confirm or
impeach numbers that are already banked). **Gate: pre-committed below, before construction.**

---

## The gate, pre-committed (written and committed BEFORE any measurement)

> **Does every headline number quoted in the prose of leg 54 (Route-MM, the coefficient-basis
> `L1` death certificate) and leg 56 (Route-TN, the collocation-basis one) re-derive from
> those legs' own banked curated JSON in `writeup/data/`, to the precision the prose states?**
>
> **yes** → Both `L1` death certificates reproduce from their own stored data. Report the
> ledger — rows checked, rows re-derived, worst residual in half-ulps of the prose's own last
> quoted digit — and close as a clean audit. Nothing banked changes; `L1` is *not* thereby
> re-confirmed dead, only its documentation confirmed faithful to its data (ceiling, novelty
> pass §4).
>
> **no** → Report the exact magnitude of every number that does not re-derive: absolute,
> relative, and in half-ulps of the prose's own last digit, with the direction of the error.
> **Do not correct the banked writeup** — that decision is the orchestrator's/DM's
> (ORCHESTRATION.md §7b). If any failing number is one that `L1`'s death, or the `NG` / `M2`
> / `V` escalations, depend on, this is escalation #4: **park the branch, push the branch
> only, never `main`**, and report it for the user.

**Territory, declared before construction and not widened afterwards:**
`experiments/p2_route_l1r_v1_repro.py`, `writeup/data/p2_route_l1r_v1_repro.json`,
`experiments/p2_route_l1r_v1_repro_evidence.py`,
`writeup/figures/fig<NN>_route_l1r_v1_repro.png` (+ its one registration line in
`writeup/build_figures.py`), `writeup/4_p2_lottery/{BLOG,TECHNICAL}_P2_ROUTEL1R_V1.md`,
`writeup/novelty/leg_110.md`, `experiments/journal/leg_110.md`.
**`solver/` is read-only. The four audited documents and the two audited JSONs are
read-only, under both branches of the gate.**

## Bookkeeping note, recorded at dispatch and not resolved by this leg

`DIRECTION.md` on `origin/main` (2102 lines, tenth refill, entries through leg 109) has **no
leg 110 entry** — `grep -c 'L1R' DIRECTION.md` is `0` on `main` and on all 50 remote
branches; `git log --all -S 'L1R' -- DIRECTION.md` is empty. Line 45 of that file reserves
`110` as "the next fresh leg number for any future candidate", which is consistent with the
dispatch, but the route's thesis/gate/territory were never written into the DM's file. The
gate above is therefore pre-committed **here**, from the dispatch brief's wording, before
any measurement — and `DIRECTION.md` is an integration-owned ledger this leg does not edit.

---

*(Everything below this line is written after the measurement. Nothing above it is.)*

---

## 0. The bookkeeping note above is superseded, and the DM's gate is the one answered

The pre-construction pass (killed mid-leg by an infrastructure interruption) recorded that
`DIRECTION.md` had no leg 110 entry. **It does.** The integration commit that is this
branch's own parent — `6535c6d`, *"Leg 0: ORCH — integration cycle: DM's fresh queue (legs
110-124…)"* — carries it at line 1998, `### 110 — ROUTE-L1R: L1 DEATH-CERTIFICATE
REPRODUCTION AUDIT (ASSIGNED, LEG-C)`. The earlier grep was run against a `main` from before
that refill landed. Nothing was widened to notice this; the entry was read before any
measurement in this session.

**Consequence, and it matters twice.** The DM's entry is the authoritative pre-commitment,
so this leg answers **its** gate, in its wording, not the reconstruction above — and the two
differ in the two places that decide the leg:

| | the reconstruction (sec above) | **`DIRECTION.md` 110, authoritative** |
|---|---|---|
| audited surface | the four `writeup/4_p2_lottery` documents of legs 54/56 | **`PHASE2_P2_NOTES.md` §§ for legs 54 and 56, the leg-51 window numbers they rest on (`+0.639`, `0.606`, the per-class tail-divergence exponents), *and everywhere those numbers are re-quoted*** |
| the no-branch | escalate *only if* a failing number is one `L1`'s death depends on | **"Report the exact discrepancy (file, field, quoted vs reproduced) and ESCALATE to the user; change no conclusion, lift no ban, and do not re-measure under this leg."** — unconditional |
| territory | + writeup quartet, figure, `build_figures.py` | **`experiments/p2_route_l1r_v1_repro.py`, `writeup/data/p2_route_l1r_v1_repro.json`, `writeup/novelty/leg_110.md`, `experiments/journal/leg_110.md`** — and nothing else |

The narrower territory is the one taken: **four files, no writeup quartet, no figure, no
`build_figures.py` line.** The audited surface is the wider one. Both readings are the
conservative one in their own direction.

## 1. What ran

`experiments/p2_route_l1r_v1_repro.py`, reading five banked JSONs and writing one:
`p2_route_mm_v1_shape.json` (leg 54), `p2_route_tn_v1_consistency.json` (leg 56),
`p2_route_l1_v2_spectral.json` (leg 51, the window), `p2_route_l1_v1_interval.json` (leg 46,
the budget leg 56 measures against), `leg_54_verify_headline.json` (VER-A, cross-reference).
No solver module is imported, nothing is re-measured, and **no audited file is written** —
the audited documents and JSONs stayed read-only under both branches, as pre-committed.

Each ledger row pairs a literal **as printed in prose** with a value **re-derived from the
JSON**, and scores the residual in **half-ulps of the prose's own last quoted digit**, so a
correctly-rounded literal scores ≤ 1.0. Where prose states a derived relationship rather
than a stored key — `τ = budget/‖A‖_w`, `budget = (1−Z₁)²/2Z₂`, `N = 2n+3`, the RHS as
`prefactor × weight-ratio × ‖A_tail e_{K+1}‖`, the headline as a re-minimisation over the
whole battery — the relationship is recomputed rather than read. Output is byte-identical
across runs.

## 2. The ledger

| | |
|---|---|
| rows | **302** |
| scored as exact (a printed digit that must round) | **262** |
| of those, re-derive | **256** |
| of those, **do not** | **6** |
| worst residual among the 256 that do | **0.9789 half-ulps** |
| rows carrying an explicit `~`/`about` qualifier | 24 (all within their qualifier) |
| verbal precision claims, scored as magnitudes | 5 |
| quoted in prose but absent from any curated JSON | 2 |

**The death itself re-derives, exactly, everywhere it is stated.** Re-minimised from the raw
battery rather than read from the headline key: best admissible `Z₁` = `8.959091169104095`
(`ff_lift`, algebraic `s = 0.3`, null gauge, `K = 2`), block-diagonal baseline
`10.458427031841403`, ratio `1.1673535668336368`. Leg 56's two exponents re-divide from
defect and `τ` independently of the stored ratio: `2.04033440973586e+11` and
`1.8536770035727e+07`. Leg 51's window: minimum `+0.6390773072841` over the whole curve,
gap `0.606` re-derived as `s_operator − s_max_object`. `r_max = 0` and `Y₀ = 0` in every row
of MM-6. Leg 56's cross-reference to leg 46 is bit-for-bit identical to leg 46's own file.

## 3. The six rows that do not re-derive — five distinct defects, the first at two sites

Ordered by what they touch, not by size. **None is corrected here.**

1. **`+1.27` at `s = 2`, the divergence curve** — `PHASE2_P2_NOTES.md` L1v2-4 (line 3321)
   **and** `writeup/4_p2_lottery/TECHNICAL_P2_ROUTEL1_V2.md` §9 (line 168), the same slip in
   two places. Stored: `S6_window.curve[s=2.0].divergence_exponent =
   1.264554318527888`, which prints as `+1.26`. Quoted **high** by `5.44568e-03` absolute,
   `4.306e-03` relative, **1.089 half-ulps** of the prose's own last digit. Every other value
   in the same sentence re-derives (`+0.639`, `+1.28`, `+1.00`, `0.606`), and the crude
   first-to-last-rung estimate (`1.2806`) is not what the other entries use — the stored
   fitted exponent is. **This is the one defect with a site inside `writeup/`**, so touching
   it is escalation #4 and it is left alone.
2. **"four orders of magnitude worse than the baseline it was supposed to beat"** —
   `BLOG_P2_ROUTEMM_V1.md`. The audit's own smallest `Z₁` (`10317.371485777867`) against the
   block-diagonal baseline the same post prints as `10.46` is **986.5×**, i.e. **2.994
   decades**. Against v1's superseded `45.3628` baseline it is `227.4×` (2.357 decades). No
   reading of "the baseline" gives four decades at the quoted `10⁴`. Overstated by **1.006
   decades**.
3. **"moving the derivative's by 1.11%"** — `BLOG_P2_ROUTETN_V1.md`. The ablation ratio is
   `1.1056×`, i.e. a **+10.56%** change; the same post says "eleven percent" correctly five
   lines earlier. A ratio rendered as a percentage: **off by 9.45 percentage points, a factor
   of 9.5**, and self-inconsistent within the document.
4. **"the artifact is the entire measured defect (99.99% of it)"** —
   `BLOG_P2_ROUTETN_V1.md`. Stored share is `1.0000939199963914`, i.e. **100.0094%** — the
   endpoint-zeroing term slightly **exceeds** the total, it does not fall just short of it.
   Off by `0.0194` percentage points and **on the wrong side of 100**. The TECHNICAL states
   the same quantity correctly as `1.00009`.
5. **"the `M_L = 1024` and `2048` rows agree to five digits"** — `TECHNICAL_P2_ROUTEMM_V1.md`
   §5. Five of the six `(M_A, class)` pairs agree to 5 or 6 significant digits; one agrees to
   **4** (`32820.993516454604` vs `32821.45989517439`). Worst relative gap over the audit is
   `3.375e-05`, so the sentence's actual claim — the cost is essentially independent of
   `M_L`, hence it lives at the seam — holds. The digit count is one too generous.

## 4. Five verbal precision claims the data does not support at the stated tightness

Reported as magnitudes, not as booleans; none changes a conclusion.

* **`MM1_second_factor_range` is a factor short of the quantity §1 defines.** The inequality
  displayed at `TECHNICAL_P2_ROUTEMM_V1.md:49` makes the second factor
  `(w_{K+1}/w_K)·‖A_tail e_{K+1}‖_w/w_{K+1}`. Re-derived from leg 54's *own* rows over
  `K = 4…64` that range is **`[0.941176470588235, 1.3873146915152630]`** — exactly what the
  prose says VER-A measured, and exactly what VER-A's own
  `V5_MM1_inequality.second_factor_range_leg53_sweep` stores. The key the next sentence
  points the reader at holds **`[0.941176470588235, 1.3318429055906214]`**: the min/max of
  `A_tail_e_Kplus1` *alone*, with the weight ratio dropped. Upper end **4.00% low**
  (`5.547e-02` absolute). The lower end coincides only because the flat class's weight ratio
  is exactly 1. Sharp edge: the value the stale key reproduces (`0.94 … 1.33`) is the
  `CONTINUATION_PROMPT.md` figure the very same paragraph calls *"not traceable to leg 53's
  shipped JSON"*. Nothing downstream uses it — the RHS table, the `K ≥ 6` / `K ≥ 4`
  restriction and the equality claim all come off the `rhs` column, which carries the weight
  ratio correctly (checked: `rhs = prefactor × weight_ratio × A_tail_e_Kplus1` at all 14
  rows).
* **"the geometric growth factor **is** `ν`, to three digits"** (`PHASE2_P2_NOTES.md`
  L1v2-3): measured `1.0450219483086232` against `ν = 1.05` and `1.194555708715214` against
  `ν = 1.20` — **4.7e-03 and 4.5e-03 relative**, i.e. two significant digits, not three.
* **"~5e−04"** for the dropped term of `schur`/`gs_upper` (`TECHNICAL_P2_ROUTEMM_V1.md` §4):
  the actual spread is `2.849e-04 … 1.470e-03`, the largest **2.9×** the quoted figure. The
  conclusion drawn from it (`0.000×` the floor) re-derives — worst admissible
  `term_over_floor` is `6.95e-05`.
* **"agree with each other to three decimal places"** (`BLOG_P2_ROUTEMM_V1.md`, of the v1
  ablation's two arms): worst **relative** gap `3.348e-03`; in absolute floor units the worst
  pair is `534.0078` vs `534.8182`. ~2 decimal places relative. The sentence's point — *that
  control varied nothing* — is correct and is the post's own self-criticism.
* **"the collapse matches `M/a = 1490.5` to within 1%"** (`TECHNICAL_P2_ROUTETN_V1.md` §8):
  true at the finest rung only — **n = 801: 0.83%, n = 401: 1.74%, n = 201: 4.69%**. The
  mechanism claim rests on `1503×` vs `1.11×`, which re-derives at every rung.

## 5. Two numbers quoted in prose that are in no curated JSON

`TECHNICAL_P2_ROUTETN_V1.md` line 10 says *"Every number quoted below is in the curated
JSON."* Two are not:

* **`h ≈ 14.8`** (§7, the far-field mesh spacing). Re-derives to **`14.7599`** from the grid
  definition alone — `X = c·sinh ρ`, `c = 0.5`, `ρ_max = 8`, `n = 801`, confirmed by
  `0.5·sinh 8 = 745.2394128947751` matching the stored `X_max` to all digits. Correctly
  rounded; a contract gap, not a wrong number.
* **`414 s`** (§10, the test-suite wall time). Not a measurement of the object; the JSON's
  `elapsed_s = 12.90` is the runner's own time. Unverifiable from banked data.

## 6. The gate, answered in the DM's wording

> *Does every quoted headline number in the two `L1` death records reproduce exactly from the
> banked JSON data alone?*

**NO** — 6 of 262 printed digits do not, at 1.089 to 1890 half-ulps of their own last digit.
The pre-committed no-branch fires verbatim: **report the exact discrepancy (file, field,
quoted vs reproduced) and ESCALATE to the user; change no conclusion, lift no ban, and do
not re-measure under this leg.** Accordingly this leg **pushes its branch only, never
`main`**, corrects nothing, and lifts nothing.

**And the death is not what failed.** Every number `L1`'s death and its dependents actually
rest on re-derives exactly: both gate answers (`8.9591` against the `1` it must be under;
`2.04e+11 τ` and `1.85e+07 τ`), the improvement factor `1.167×`, the floor `5.0444`, the
window minimum `+0.639` and gap `0.606`, `r_max = 0` throughout. One defect (`+1.27`) has a
site inside `writeup/`; the other five are a blog decade count, a blog ratio-as-percentage,
a blog percentage on the wrong side of 100, a digit count one too generous, and a stale JSON
key that the prose citing it silently corrects.

## 7. Ceiling, as pre-committed

A dirty audit means *landed sentences disagree with their own data by the magnitudes in §3–5*
— a documentation fact until an escalation says otherwise. It is **not** a statement about
`HL_S2_nonsymmetric`, the far-field gap, whether `L1` should reopen, or any link of the
`L1→L4` chain, and it does **not** independently re-confirm that `L1` is dead, because it
re-runs no measurement. No link moved. Clay stays ~0.05% behind Walls 1 and 2.
