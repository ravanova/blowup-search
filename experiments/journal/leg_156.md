# Leg 156 — Route-IX4: fourth freshness audit of `writeup/INDEX.md`

**Gate (pre-committed, `DIRECTION.md` §"156 — ROUTE-IX4"), verbatim:**

> At this leg's own pinned start SHA, does INDEX.md correctly reflect every leg
> landed since leg 138's window closed?
>   yes -> The index is current through the pinned SHA. Bank the dated completeness record;
>          merge this leg's report to `main` normally.
>   no  -> Report the exact stale or missing entries (the count is the finding); fix INDEX.md
>          directly per the leg 68/108/138 mechanical-fix precedent (INDEX.md freshness is its
>          own territory, not another leg's); merge regardless.

**Answer: NO.** The no-branch fires as written. `INDEX.md` was fixed directly, and all **7**
corrected rows are enumerated in `writeup/data/p2_route_ix4_v1_index_audit.json` under
`corrected_rows`. The count that is the finding: **3 missing route rows, 2 stale gap-list
items, 1 new gap item, 1 new currency block** — against **6 standing claims re-verified
unchanged**.

**Window pinned at** `9f6729e72a6ba804f09d6b213a4da2c8e938a702` — `origin/main` HEAD at the
moment this leg began. Range `3474862..9f6729e7`, where `3474862` is **leg 138's own pin**,
not its `INDEX.md` commit. That choice matters and is the one methodological difference from
leg 138's setup: leg 138's fix `b9b6398` landed *after* its own pin, so anything between the
two was audited by nobody. Basing this window on the pin rather than the fix closes that seam
at the cost of one bookkeeping subtlety, handled below.

**Artifacts.** Novelty pass `writeup/novelty/leg_156.md`, committed **before** any
construction (`e8e707c`). Runner `experiments/p2_route_ix4_v1_index_audit.py` → curated JSON
`writeup/data/p2_route_ix4_v1_index_audit.json`. **No figure, by design** — this leg produces
no measurement, so the repository's Route-D convention ("no measurement, no figure") applies,
the same one legs 126 and 138 invoked and the same one this audit spends most of its effort
*checking* on other legs.

## The window, measured

`81` commits. `25` distinct `Leg N:` prefixes, of which `Leg 0` is repo-wide integration work
and `138` is the previous auditor rather than an audited leg, leaving **23 audited legs**:
122, 124, 125, 127, 128, 130, 132, 133, 135, 137, 139, 140, 142, 144, 146, 147, 149, 152,
155, 157, 158, 159, 161. As leg 138 noted for its own window, leg *number* order and
*landing* order differ — the low numbers here are branches that merged late, not legs anyone
missed.

**The one bookkeeping subtlety, stated rather than smoothed over.** `git log 3474862..9f6729e7
-- writeup/INDEX.md` returns **1** commit, not 0: leg 138's own fix. Measured from that fix
instead (`b9b6398..9f6729e7`), the file has **0** edits. Both numbers are in the JSON under
distinct keys (`index_md_edits_within_window` = 1, `index_md_edits_since_leg138_fix` = 0) so
neither can be quoted as the other. My own novelty log's first draft got this wrong — it said
"0 edits across the 81 commits" — and the correction is recorded in that file rather than
silently amended out of it.

What the 23 produced, unioned over each leg's commits:

| Quantity | Count |
|---|---|
| legs that touched a `4_p2_lottery/` BLOG or TECHNICAL | **3** (125, 127, 128) |
| legs that added a figure | **2** (125 → fig61, 127 → fig63) |
| legs with neither, by design | **20** |
| legs filing both `journal/leg_N.md` and `novelty/leg_N.md` | **23 of 23** |
| legs filing a curated JSON under `writeup/data/` | **23 of 23** |
| new Arc 4 rows owed | **3** |
| new *parked* rows owed | **0** |

The hygiene line is the best of the four passes: leg 108's window filed a curated JSON for
24 of 30 legs, leg 138's for 32 of 33, this one for 23 of 23. That is a trend across three
windows, not a single reading.

## The three rows that were owed

| Route | Pieces on disk | What the row says |
|---|---|---|
| Route-M2P v1 (leg 125) | **5 of 6** — runner, data, BLOG, TECHNICAL, `fig61`; **no `*_evidence.py`** | gate answered NO on both clauses; `E` marked `GAP` |
| Route-NGX v1 (leg 127) | **6 of 6** — incl. `fig63` and an `evidence.py` in `experiments/` | gate answered YES (i); `E` marked `Y†` |
| Route-NKR v1 (leg 128) | **4 of 5** — runner, data, BLOG, TECHNICAL; **no `*_evidence.py`**; no figure **by design** | gate answered YES on (b)/(c), NO on (a); `F` = none, by design |

Two of the three are missing the same piece, which is why they became a single new gap item
(13) rather than two. NGX is the contrast case that keeps the item honest: a route in the same
window, under the same conventions, that has all six.

## The lesson-90 control, and why this pass is not a rubber stamp

Leg 138 had to **supersede** leg 108's "no BLOG/TECHNICAL and no figure, by design"
convention block, because 3 of the legs listed there (58, 60, 62) later landed prose. So the
obvious question for this pass is whether leg 138's own 29-leg block has gone the same way.

Running the identical predicate — did leg *N* touch any `writeup/4_p2_lottery/BLOG_*`,
`TECHNICAL_*` or `writeup/figures/*` inside this window? — over all 29 returns **0
violators**. Leg 138's block stands, unamended. This is the first pass at which the
predecessor's classification is *confirmed* rather than corrected.

Per lesson 90, a control that cannot come out differently is not a control, so: what would
have had to change for this check to report the other answer? Nothing hypothetical — the same
function, on the same window, reports `MISSING` three times (claims D1–D3), and that is
exactly how legs 125, 127 and 128 were found. The predicate discriminates inside this very
run. It is also the same predicate that returned FALSE one pass ago on real data.

## What was re-verified and did *not* move

Six standing claims were re-checked against the pinned tree and hold verbatim:
`BLOG_P2_ROUTEPORT_V2.md` still absent (item 2, as narrowed by leg 138);
`BLOG_P2_ROUTEKA_V1.md` still absent with no `ka_v1` figure (item 10); `fig48` still names 2
files (item 8); `writeup/README.md` still holds 46 numbered entries, still ends at 47, still
skips 45, still 0 mentions of Weight-repairs or Kawahara (items 4/11); exactly **1** parked
route with 0 artifacts on `main`, already rowed (leg 63 / Route-M2). That last one was
established mechanically rather than by memory: every
`writeup/4_p2_lottery/(BLOG|TECHNICAL)_*.md` path `DIRECTION.md` declares — 14 of them — was
tested against the pinned tree, and leg 63's pair is the only undelivered one.

## The two things that went stale

**Gap item 12, the figure registry.** Leg 138 recorded the hole set as `{fig53, fig54}` over
58 PNGs. At the pinned SHA there are **60 PNGs** and the hole set is `{fig53, fig54, fig62}`.
`fig62` is a new *kind* of entry: leg 126 reserved it for Route-BX's yes-branch, that branch
did not fire (the gate answered NO), and leg 127 then allocated `fig63` past it. So it is a
**released reservation** — permanently skipped, not pending — which is a different status from
`fig57`, still genuinely reserved by parked leg 63. The row now says which is which.

**Gap items 4/11, `writeup/README.md`.** True verbatim, and now larger: 0 mentions of M2P,
NGX or NKR either, so all three routes this pass added to the table are absent from the
narrative index as well. Backlog grows by 23 legs and 3 routes on top of the two windows
already recorded. Not fixed here for the standing reason — every entry in that list is a
numeric-prose paragraph, and this file's own header forbids adding numeric claims to it.

## Scope discipline

No numeric claim was added to `INDEX.md`: every number written into it is a count of files,
legs, commits or figure numbers, all of them recomputable by re-running the runner, and none
is a measurement of the mathematics. Territory touched is exactly the four declared paths.
No ban in force is engaged: no GA compute, no ℓ¹-Fourier or collocation machinery, no gCLM,
Route-D or DSS measurement. `.venv/bin/python plan_of_record.py` was run first, as required.

**Nothing here moves any link of the L1→L4 chain, and this leg claims no stage.**
