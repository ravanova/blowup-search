# Leg 155 — Route-JR4: the fourth freshness audit of `experiments/JOURNAL.md` and `journal/`

**Branch** `leg/155-jr4-v1`. **Exploration leg (light), mechanical/documentation.**
**Gate: NO — on one clause of three, the same clause as last time, and it has gone from
partial back to total.** Lands normally on `main`.

Same audit as legs 72 (JR), 102 (JR2) and 137 (JR3), fourth window. Like JR3 and unlike JR2,
this leg does **not** own `experiments/JOURNAL.md` — the five shared ledgers are
integration-owned — so it edits nothing and reports the gap instead.

---

## Order of work (the discipline, as executed)

1. `plan_of_record.py` run first. `NG` **DONE** (YES at leg 58), `B` **NEXT**. All 15 printed
   bans read; **0 of 15 bind this leg** — it imports no solver module, runs no GA compute,
   derives no number, builds no ℓ¹-Fourier or collocation machinery, re-opens no closed stage.
2. **Audit window pinned at this leg's start SHA before anything was read** (below).
3. **Novelty pass BEFORE construction, committed on its own** (`writeup/novelty/leg_155.md`,
   commit `b18e721`). For an audit leg the novelty pass *is* the coverage inventory.
4. Runner + curated JSON. **No figure** — "no measurement, no figure" is the standing
   convention, and `DIRECTION.md`'s leg-155 territory declares no figure path.
5. Finish protocol: territory check, rebase on `origin/main`, `scripts/merge_gate.sh`, push.

## The gate, verbatim

> At the pinned SHA, does every landed leg since JR3's window have (a) its
> `experiments/journal/leg_N.md` on main, (b) a `JOURNAL.md` pointer line, and (c) its
> `writeup/novelty/leg_N.md`?

**Answer: NO.** Clauses (a) and (c) answer **YES at 17/17 each**; clause (b) answers **NO at
0/17** — every leg that landed in this window is unnarrated.

## Why the SHA is pinned, and to what

Pinned SHA **`e832eadf18738c10911d0fe34930a4732afd8bff`** — this leg's start commit, verified
equal to `origin/main` HEAD at dispatch. Sibling legs were landing in parallel while this leg
ran; an audit reading the working tree would report a window nobody can reproduce. Every fact
in the JSON is read out of the git object store at that one commit via `git show` /
`git ls-tree`, never from the working tree, so the runner reproduces the same output at any
later date and anything landing after the pin is out of scope **by construction** rather than
by the accident of when the script ran.

Cut line: **`e4199c2`**, leg 137's own audit commit — where JR3's window closed. Window
`e4199c2..e832ead` contains **53 commits**.

## The window

**17 legs landed inside it** (subjects `^Leg N:`, `N != 0`; leg 0 is repo-wide bench work and
carries no gate — the same filter JR1/JR2/JR3 used):

    122, 124, 127, 128, 133, 135, 138, 139, 140, 142, 144, 146, 147, 149, 157, 158, 161

Legs 157, 158 and 161 carry numbers **above this leg's own**, out of dispatch order. They
landed inside the pinned window and are audited like any other. **The window is defined by
landing, not by leg number** — which is exactly why it is pinned to a SHA and not to a range
of integers.

## The result, by clause

| clause | answer | count |
|---|---|---|
| (a) `experiments/journal/leg_N.md` present | **YES** | **17 / 17** |
| (b) `JOURNAL.md` narrative pointer line present | **NO** | **0 / 17**, **17 missing** |
| (c) `writeup/novelty/leg_N.md` present | **YES** | **17 / 17** |

`experiments/JOURNAL.md` is **3091** lines at the pin (up 48 from JR3's 3043);
`experiments/journal/` holds **93** entries and `writeup/novelty/` **95**.

**All 17 in-window legs have no pointer line.** The pointer check ran **two** predicates —
the ledger's own line shape (STRICT, `^\s*-\s*\*\*Legs? N\b`) and a deliberately looser one
(`\bLegs? N\b` anywhere on any line) that **can** return a different answer, lesson 90's test
for whether a control is a control at all. At JR3 it **did**, on exactly 1 leg of 15, and
following that disagreement is what produced JR3's second defect. Here it agrees on all 17:
**0 borderline legs**, loose count 0 for every one. The gap of 17 is regex-independent, and
the control's silence is a measured result rather than a formality, because the same control
has demonstrably spoken before.

## The catch-up commit is INSIDE this window, and re-measured rather than assumed

Between JR3's pass and this one the orchestrator applied a **manual** integration commit
discharging JR3's two remedies. Resolving where it sits changes what this audit is measuring,
so it was resolved explicitly (check **A6**):

**`4301c7e`** — *"Leg 0: ORCH — integration: JOURNAL.md pointer catch-up (15 legs flagged by
leg 137/JR3) plus the leg-2830 correction; …"* — is a **descendant of the cut line and an
ancestor of the pin**, i.e. **inside this leg's window**. It is **commit 5 of the window's 53**
(oldest-first), with **48 commits landing after it**. It touched **2 files**, +62/−3 lines, of
which `experiments/JOURNAL.md` is **+52/−2**.

Both JR3 remedies are confirmed discharged at the pin, by re-measurement:

| JR3 remedy | result at this pin |
|---|---|
| the **15 pointer additions** (legs 58, 71, 85, 98, 100, 101, 105, 107, 108, 123, 126, 131, 134, 136, 141) | **15 / 15 closed**, 1 strict pointer line each, **0 still open** |
| the **1 stale-sentence correction** at `JOURNAL.md:2830` | **applied** — **0** surviving lines anywhere in the 3091-line ledger even contain the original wording "have branch progress but no answered gate"; the sentence now reads that legs 58, 59, 61, 62 and 71 *have* since answered and landed, leaving only leg 63 |

So this pass **retires JR3's entire remedy list**, evidenced rather than asserted. That is the
positive half of the finding and it is the reason the catch-up's position matters.

## The finding is a CADENCE, not a count — and it is the shape of a ladder, not its endpoint

The negative half is that the ledger's narrative coverage is back to **total** absence, and
the catch-up commit's position explains why. Splitting the window's legs by whether the
catch-up **could** have covered them (lesson 75 — two defects in the same problem are not the
same defect, and these take different remedies):

| | count | legs |
|---|---|---|
| landed **before** the catch-up — coverable, not covered | **1** | 133 |
| landed **after** the catch-up — not coverable | **16** | 122, 124, 127, 128, 135, 138, 139, 140, 142, 144, 146, 147, 149, 157, 158, 161 |

The catch-up was scoped to **JR3's flagged list**, not to the ledger's state at the moment it
was applied — leg 133 had already landed and was missed — and it landed at the window's
**opening**, so 48 commits and 16 further legs went in behind it with the per-landing step
still not running.

Across the four passes (lesson 72 — report the SHAPE of a ladder, not its endpoint):

| pass | narrative pointers | per-leg files |
|---|---|---|
| JR (72) | closed 8 | 1 leg missing (2 files) |
| JR2 (102) | **0 of 20** — total | 0 missing in-window; leg 60 open |
| JR3 (137) | **20 of 35** — partial | 0 missing, 0 repo-wide; leg 60 closed |
| **JR4 (155)** | **0 of 17** — total again | **0 missing, 0 repo-wide over 87 landed legs** |

Read as endpoints, JR3's 20/35 looks like a maintenance habit improving and JR4's 0/17 looks
like it regressing. Read as a shape, neither happened: **JR3's 20/35 was a batch repair
showing through**, and the per-landing step has produced **0 pointers in every window that
has ever been measured**. The narrative half of this ledger has only ever been written by
audit-triggered batches — three of them now (legs 72, 102, and the orchestrator's `4301c7e`)
— and each batch is scoped to the flagged list of the audit that triggered it, so the gap
returns to total within the next wave. Four passes is enough to say that this is the
mechanism, not a run of bad luck.

## The per-leg-file half is complete repo-wide, over 20 more legs than JR3 checked

Widening outside the gate (check A5): **87** distinct legs with `N != 0` have landed on `main`
(JR3 measured 67).

| quantity | count |
|---|---|
| landed legs with no `experiments/journal/leg_N.md` | **0 of 87** |
| landed legs with no `writeup/novelty/leg_N.md` | **0 of 87** |
| journal files for legs that never landed (orphans) | **0** |
| novelty files for legs that never landed (orphans) | **0** |

Both directories resolve to **exactly the 87-leg landed set in both directions**. The
directory entry counts (93 and 95) exceed the leg-number counts only by entries that are
enumerated by name rather than left as an unexplained surplus: `README.md` and
`leg_0_bench_redtests.md` in both, plus `leg_54_verify_v2.md` in novelty. The per-leg-file
discipline JR3 first established repo-wide has now held across **20 additional landed legs**
with **0** exceptions — the founding failure mode of this route (leg 72, leg 60's two missing
files) has not recurred once in three passes.

## What this leg did NOT do

- **Edited no shared ledger.** `experiments/JOURNAL.md` is integration-owned; the 17 missing
  pointer lines are reported for the orchestrator. **0 shared ledgers edited.**
- **Created 0 files on any other leg's behalf** — the JR1/JR2/JR3 precedent that the count
  *is* the finding.
- **Derived no number and imported no solver module.** Nothing here touches the L1→L4 chain,
  and nothing here is evidence about it either way.
- **No escalation.** None of `ORCHESTRATION.md`'s four escalations applies: the gate answered
  in its pre-committed wording on a mechanical audit, and no missing file belongs to a parked
  escalation — there are **0 missing files** of any kind, in-window or repo-wide.

## For the orchestrator

**17 pointer lines to add**, all additions, no corrections (JR3's single correction is
closed):

    122, 124, 127, 128, 133, 135, 138, 139, 140, 142, 144, 146, 147, 149, 157, 158, 161

Also worth noting, from A6: a catch-up scoped to the *previous audit's flagged list* rather
than to the ledger's state when it is applied will miss any leg that landed in between — it
missed exactly **1** (leg 133) this time. That is a small number, but it is a systematic one,
and it costs nothing to re-derive the list at apply time instead of copying it.

## Artifacts

- `experiments/p2_route_jr4_v1_journal_audit.py` — the runner (A1, A2, A3, A4, A5, A6)
- `writeup/data/p2_route_jr4_v1_journal_audit.json` — every number quoted above
- `writeup/novelty/leg_155.md` — novelty/inventory pass, committed before construction
- no figure, by the "no measurement, no figure" convention

No link of the L1→L4 chain moved. Clay unchanged at ~0.05%.
