# Leg 417 — unit `U1`, arc 6, SOLO MODE (§3f): ACQUIRE AND PIN

**Date:** 2026-09-09. **Role:** AUDIT unit. **Mode:** `ORCHESTRATION.md` **§3f SOLO** — one
instance, no subagents, no Decision Maker, no paired verifier.
**Brief:** arc 6 charter, `writeup/5_outpaced/TECHNICAL_OUTPACED.md` §5 (A6-AUDIT) as re-scoped
by the arc-6 unit list. **Territory:** `writeup/data/arc6_acquire_v1.json`,
`writeup/arc6_acquire_evidence.py`, this journal, `Papers/MANIFEST.md`, `Papers/fetch.sh`,
`writeup/novelty/leg_417.md`. `STATE.md`, `WALLS.md`, `CLAY_OBLIGATIONS.md`,
`plan_of_record.py` — **read-only, none edited.** `DIRECTION.md` — **not read.**

---

## §0. CEILINGS, WRITTEN BEFORE THE FILES WERE FETCHED

1. **This unit adjudicates NOTHING.** It fetches, hashes, page-counts, and quotes. Whether the
   manuscript's proof is correct is not asked here and is not answered here.
2. **UNVERIFIED, and it says so in its own gate answer.** §3f rule 1: verification is a fresh
   session or it is not verification. One session read the manuscript and wrote the gate answer
   about it. That is self-checking and it is labelled as such, not as verification.
3. **A Lean project that exists is not a theorem that is proved**, and this unit does not
   compile anything. `formalization.yaml`'s self-declared `sorry_count: 0` is banked as a
   **claim by the project about itself**, at that status. U2 measures it.
4. **Reading is authorised; contact is not.** Three PDFs and one git clone. No author, group,
   maintainer or mailing list was contacted, and none will be.
5. **No `L1 → L4` link can move here.** Clay stays ~0.05%. This is not Tier 2; it is not a
   measurement of any fluid equation at all.
6. **Arc 5 is a view of the record, never a source.** Where the manuscript and
   `TECHNICAL_OUTPACED.md` disagree, the manuscript governs and arc 5 is corrected.

---

## §1. WHAT WAS FETCHED

Everything is hashed in `writeup/data/arc6_acquire_v1.json` :: `artifacts_fetched`.

| artifact | bytes | sha256 (first 16) | pages / files |
|---|---|---|---|
| OpenAI, *Finite Time Blowup for Navier–Stokes* | 2,959,204 | `0e779481c4da40bd` | **166 pp** |
| OpenAI, *Finite Time Blowup for the Euler Equation* | 535,142 | `a0c234518e6c489e` | 57 pp |
| Fefferman, Clay official problem statement | 203,736 | `c1b5f27b1a64705c` | 6 pp |
| `github.com/openai/NavierStokesAndEuler` | — | HEAD `8937a8f4cbc7abaa` | 2,496 tracked, **2,486 `.lean`**, 32.4 MB |

Both manuscript PDFs carry `/CreationDate D:20260908…Z` and `pdfTeX-1.40.29 (TeX Live 2026)`.
Neither carries a `/Title` or `/Author` in metadata; the body gives `OPENAI` as author.

**The Euler manuscript was fetched deliberately, not opportunistically.** The Lean project's four
declared main results span **both** manuscripts (two Navier–Stokes, two Euler). U2 cannot
inventory the dependency graph of the project against only one of them.

**A network fact changed and is recorded, not repaired.** `Papers/fetch.sh` carries a banked
diagnosis — *"arxiv.org is not reachable … github.com IS allowlisted"*. Measured here, it is
**wrong in both directions**: `arxiv.org` returns 200; `github.com` over plain HTTPS returns
**403** while the *same repository* clones fine over the session's git proxy and reads fine over
`raw.githubusercontent.com`. `openai.com`'s announcement page is 403 (organisation egress
policy, not retried, reported); `cdn.openai.com` and `claymath.org` are 200. Full probe table:
JSON :: `network_fact_corrected`. **The script's conclusions elsewhere are not edited by this
unit** — a note is appended to the manifest and the probe is banked.

---

## §2. FEFFERMAN'S CONDITIONS, AT PRIMARY, INCLUDING (8) AND (9)

`STATE.md` **Open item 3**: *"Statement (D)'s data conditions (8) and (9) are **UNREAD** and
readable since the outreach narrowing."* **They are now read**, verbatim, from the hashed PDF:

- **(8)** `u°(x + e_j) = u°(x)`, `f(x + e_j, t) = f(x, t)` for `1 ≤ j ≤ n` — periodicity of the
  **datum and the force**.
- **(9)** `|∂ₓ^α ∂ₜ^m f(x,t)| ≤ C_{αmK}(1 + |t|)^{−K}`, *"In place of (4) and (5)"* — decay in
  **time only**. **(D) asks the force for no spatial decay whatsoever.**

This **confirms leg 390's machine-read at primary**: (D)'s solution conditions are
(1),(2),(3),(10),(11); **(7) is absent**; exactly one condition leaves and one arrives.

It also **corrects a wording** in `CLAY_OBLIGATIONS.md`. Leg 381's clause says statement (C)
permits a forcing *"satisfying its own decay conditions (4),(5)"*. At primary, **(4) is the
datum's decay and (5) is the force's**; (C) names the pair because it quantifies over both `u°`
and `f`. The substance of leg 381's clause is unaffected — this is a labelling slip, not a gate
defect, and it is recorded rather than escalated.

---

## §3. THE GATE

> **Is the theorem as reported in arc 5 the theorem the manuscript states?**

## **DIFFERS-AS-FOLLOWS.** *(UNVERIFIED — §3f rule 1; see §0.2.)*

**What agrees** (eight items, JSON :: `gate.agreements`): the Clay alternative is **(C)**,
verbatim — *"This establishes alternative (C) in the Millennium problem statement for
Navier–Stokes as stated by Fefferman in [13]"*; the domain is `ℝ³`; the datum is **rest**,
verbatim `u(·, 0) = 0`; the force is smooth and compactly supported in space and time, verbatim
`f ∈ C∞_c(ℝ³ × (0, ∞); ℝ³)`; the kinetic energy is uniformly bounded before the singular time;
the manuscript is **166 pages**; the Lean project is at the reported URL; and its review status
is **self-assessed**, verbatim in `formalization.yaml`.

**What differs — seven items.** Full text with both quotations: JSON :: `gate.differences`.

| id | severity | arc 5 said | the manuscript says |
|---|---|---|---|
| **D1** | MATERIAL | *"a … force `f` and a time `T`"*; no viscosity quantifier | *"**For every** `ν > 0`"*, and the singular time is **fixed at `T = 1`**. (C) needs one `ν`; the manuscript proves all of them, by the rescaling (10.22). |
| **D2** | **MATERIAL — and it is the one that touches our own wall** | silent | *"a compact set `K ⊂ ℝ³` … such that `supp u(·,t) ∪ supp p(·,t) ⊂ K` for every `0 ≤ t < 1`"*. **The solution and the pressure are compactly supported in space, uniformly in time.** |
| **D3** | MATERIAL | *"(A) and (B) are untouched. The unforced problem is open. **So is (D).**"* | *"Compact support also yields the corresponding construction on `T³ = ℝ³/ℤ³`, **establishing alternative (D)** in [13]; see Corollary 10.6."* Corollary 10.6 is stated and proved, pp. 125–126. |
| **D4** | PRECISION | *"`sup|u|` becomes unbounded"* | `lim sup_{t↑1} ‖u(t)‖_{L∞(ℝ³)} = ∞` — along an explicit path `x_τ = (√2 X_in τ, 0, 0)`, `t = 1 − τ`, (10.20)–(10.21). |
| **D5** | PRECISION | only the exhibition half | the operative clause is **non-existence**: *"there is no smooth solution `(u,P)` on `ℝ³ × [0,∞)` with the same force and initial datum whose kinetic energy is uniformly bounded"*. That clause, not the exhibition, **is** (C). |
| **D6** | MATERIAL, about a different paper | Euler companion = *"a separate 112-page preprint (Alpöge–Buckmaster) … **forced** 3D Euler"* | OpenAI's own companion is **57 pages**, *Finite Time Blowup for the Euler Equation*, and its Theorem 1.1 is **unforced**: *"There exists `u₀ ∈ C∞_{c,σ}(ℝ³)` such that `0 < T*(u₀) < ∞`."* |
| **D7** | corrects a reported criticism | *"critics note missing references including Chen–Hou"* | **Measured.** The Navier–Stokes bibliography has **22 entries and Chen–Hou is not among them**. The **Euler** manuscript **does** cite Chen–Hou ([6],[7]). Córdoba–Martínez-Zoroa **is** cited in the Navier–Stokes manuscript, three times ([6],[7],[8]). |

**D3 is the finding of this unit.** `WALLS.md` W4's **only unbroken break clause is (c)**, and
clause (c) **is statement (D)**. `TECHNICAL_OUTPACED.md` §5's A6-D charter calls (D) *"the
nearest **unclaimed** Fefferman statement"*. On the manuscript's own text it is **claimed**, as a
corollary, in the same document — and by a mechanism (periodize a compactly supported field by
lattice summation) that is **exactly** leg 390's *"cut off first and then wrap"* price. That
does not make the claim true. It makes arc 5's sentence **false as a report of the manuscript**,
and it means A6-D's premise needs re-reading before that lane is planned.

**D2 is the one that will govern U3.** The manuscript discharges Fefferman (4), (5), (7), (8)
and (9) by **containment** — compact spatial support of `u`, `p`, `f`, compact temporal support
of `f` — and **not one of the five by a decay estimate**. W4 is a statement about a **DSS profile
on `ℝ³`** having infinite global energy. The manuscript's object is a self-similar **core**
inside a **compactly supported** field, cut off in Section 10 with the residual paid to `f`.
Whether that is a route past W4 or merely a different object that never had W4's problem is
**U3's gate and U5's measurement**. It is not answered here, and this journal does not assert it.

---

## §4. WHAT THIS UNIT DID **NOT** ESTABLISH

- **Nothing about correctness.** 166 pages were fetched, hashed and quoted, not verified.
- **Nothing about the Lean project beyond its own metadata.** `formalization.yaml` declares
  `sorry_count: 0`, axioms `propext`/`Classical.choice`/`Quot.sound`, and four proved main
  results including `navier_stokes_breakdown_periodic`. **That is the project's claim about
  itself.** U2 builds it, counts, and diffs the top-level statement quantifier by quantifier.
- **Nothing about `W4`, `W2`, `W3` or any lane.** No wall moved. No `L1 → L4` link moved.
  Clay stays ~0.05%.

## §5. PRE-COMMITMENT OF THE NEXT UNIT (§3f rule 2)

The next unit is **U2 (leg 418, AUDIT) — LEAN, MEASURED NOT ASSUMED**, unchanged from the arc-6
list, with its four gate clauses (a)–(d) in their pre-committed wording. Nothing found in U1
re-ranks it. U1 and U2 are the **two** consecutive audit units §3f rule 3 permits; **U3 (leg
419) is a construction unit and is not optional.**
