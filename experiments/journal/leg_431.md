# Leg 431 — unit `R6` FAN-OUT ×5, arc 6 conductor pass: THE LEAN, MEASURED NOT ASSUMED (wave 3)

**Date:** 2026-09-10. **Role:** LEAN (fan-out) + the Conductor's serial merge. **Wave 3.**
**Inputs:** the leg-418 clone of the OpenAI Lean project at HEAD `8937a8f4` (scratchpad `nse/`,
mathlib `85e3a25e`, toolchain `v4.34.0-rc2`), `lean_build.log` from leg 418, the merged ledger's 79
statement ids (leg 428). **Outputs:** `writeup/data/arc6/lean/agent_{1_build,2_statement,3_census,
4_comparator,5_coverage}.json` (one file, one author, cherry-picked unedited), merged by
`experiments/arc6_lean_merge.py` → `lean/merged.json`. **Checks:** `writeup/arc6_lean_evidence.py`.
**Pre-commitment:** `leg_429.md` §6 — *"five answers separately, `NOT-ESTABLISHED` with a reason
allowed, silence not."* No agent saw another's file. The merge and this journal are the Conductor's
and are **`UNVERIFIED`**; nothing in this leg was reproduced blind by a second agent.

## §1. THE GATE, in the charter's words — five answers, separately

> *(a) does it build; (b) what does the top-level statement say, quantifier by quantifier, against
> Theorem 1.1; (c) `sorry`/`axiom` census on the dependency graph; (d) what `ComparatorChallenges/`
> does and whether it worked; (e) coverage of the 79 statements.*

| | answer | measured | not established |
|---|---|---|---|
| **(a) build** | **`NOT-ESTABLISHED`** — the module was never reached | toolchain installs, dependencies resolve; `lake exe cache get` reached the cache host (441 of 8747 files in its 120 s); `lake build` ran **13.6 min** with **0 errors**, oleans 3608 → **3982** (mathlib 3580 of 8370), **1** project module written (`NavierStokes.ProblemStatement`, the statement, not the proof) | that the project builds; that the main theorem type-checks; its axioms. `#print axioms` failed with *"object file … NavierStokes.olean … does not exist"* — unreached, not refuted |
| **(b) statement** | **`WEAKER`**, strictly | the two exported theorems are `NavierStokes.Comparator.navier_stokes_breakdown_R3` / `_periodic`: verbatim Fefferman **(C)** (nine clauses `PRESENT`) and **(D)** (`PRESENT` clause for clause, periodic pressure `STRONGER`) | Theorem 1.1's displayed statement: of twelve clauses, **5 `ABSENT`** (∃ compact `K`, smooth `(u,p)` on `[0,1)`, support in `K`, `sup ‖u‖_{L²} < ∞`, `limsup ‖u‖_{L∞} = ∞`), 4 `DIFFERENT` (the forced equation, `∇·u = 0`, `u(·,0) = 0`, and what Lean says that the paper does not), 1 `WEAKER` (`f`, polynomially decaying rather than `C_c^∞`), 2 `PRESENT` (`∀ν`, the *Consequently* sentence). The clause-for-clause transcription `breakdownStatement` is a **definition no theorem proves** |
| **(c) census** | **4 `sorry` in code, 0 `axiom`** — kernel level **`NOT-ESTABLISHED`** | 2486 files, 616,276 lines: the 4 `sorry` are the challenge placeholders in `ComparatorChallenges/{NavierStokes,Euler}.lean`, which no solution module imports; **0** `admit`/`axiom`/`opaque`/`unsafe`/`implemented_by`/`extern`/`native_decide`/`set_option`/macro; 205 kernel-checked `decide`; **2409** modules reachable from the two main files, **73 dead** | kernel acceptance, the claimed `{propext, Classical.choice, Quot.sound}`, challenge/solution term identity, mathlib's own paths. Hygiene, not trust: `autoImplicit` left at its default `true` in the NavierStokes library; one `attribute [local instance]` placed right before Euler's main theorem |
| **(d) comparator** | **worked as intended at source level; the comparator itself was `never run`** | 11 checks: **7 PASS** / 4 `NOT-ESTABLISHED` / 0 FAIL — challenge definitions byte-identical to the project-side copies (NS 211 lines, Euler 96), the four target statements byte-identical challenge vs solution, solutions stated in the copied definitions, no solution closure (580 / 1829 modules) imports a challenge module | `lake exe comparator` (binary, `landrun`, `lean4export`, `nanoda` absent; no logs in the clone); the upstream Formal-Conjectures pin (three inconsistent pins cited, no copy present); axiom footprint; whether the definitions encode Clay (C)/(D) faithfully — by construction the mechanism cannot say |
| **(e) coverage** | **74 `FORMALIZED` / 5 `PARTIAL` / 0 `STATED-ONLY` / 0 `ABSENT` / 0 `UNCLEAR`** of 79 | 34,605 declarations in 644 files; 27 single-declaration matches, 47 distributed; `PARTIAL`: Theorem 3.1 (four items separately, growth rate weakened), Definition 3.3 (ordering only by quantifier order), Lemma 4.4 (special forms only), Corollary 7.3 and Lemma 9.7 (a common domain fixed by construction, never stated) | *"a source-level judgement of statement content — not a kernel check"*; the Lean cites a differently numbered draft (only *Theorem 1.1* coincides), so every match rests on content; exact exponents in 4.9/A.10/6.3/8.6; hypothesis strength of the 41 distributed matches |

**Cross-agent facts (from `merged.json`):** the five read the same clone HEAD; agents 1 and 3 agree
on the mathlib rev; agents 3 and 5 disagree on the `sorry` count **only by scope** (whole clone 4,
`NavierStokes/` 0) and agree once aligned; agents 1 and 3 agree on the project's file counts to
within the two library-root files; **no agent ran a kernel check** — both kernel verdicts are
`NOT-ESTABLISHED`. Every agent lists what it could not determine.

**One sentence, as the charter asks:** *the Lean project is large, sorry-free outside its challenge
placeholders, source-covers all 79 statements and exports Fefferman's (C) and (D) — and nothing in
this leg establishes that any of it is accepted by the kernel, because no build reached the theorem;
what the kernel would accept is strictly weaker than the paper's Theorem 1.1.*

## §2. The pre-commitment, scored
`leg_429.md` §6 asked for five answers separately with `NOT-ESTABLISHED` allowed and silence not.
**Five answers delivered; two carry `NOT-ESTABLISHED` with the reason attached; none is silent.**
The prediction that the build would hit the cache host's proxy/502 failure (`lean_build.log`, leg 418)
was **wrong**: the host was reachable this time.

## §3. Defects and deviations, recorded
1. **The Conductor mis-stated agent 1's cap.** I told agent 1 the 40-minute cap was *"at or near its
   end"* after 13.5 minutes; it stopped on that instruction and, correctly, recorded the true wall
   time (**13.6 min**) rather than the cap. The build number in (a) is therefore a 13.6-minute
   measurement, not the pre-committed 40-minute one. Owner: the Conductor.
2. **The merge script was written before the files existed and assumed shapes the agents did not
   use** (agent 3's `import_closure` mixes dicts and notes). Fixed after the files arrived; the fix
   is shape-only and adjudicates nothing — `merged.json` re-derives from the five files
   (`arc6_lean_evidence.py` §1).
3. **Scopes differed without a brief saying so:** agent 3 counted the whole clone (2486 files), agent
   5 the `NavierStokes/` library (644). Reconciled in `merged.json` under `sorry_counts_by_scope`;
   the two numbers are consistent, not contradictory.
4. **The build is being continued by the Conductor, after the agents reported** (`lake exe cache get`
   to completion, then `lake build`; log `scratchpad/lean_chain_leg431.log`). At the time of writing
   it is compiling the project's own modules. **Its outcome is not in this leg.** If it finishes, the
   kernel check is a later, Conductor-run, machine-checked addendum — still `UNVERIFIED` by a second
   agent, and it changes (a) only; (b)–(e) are unaffected by whether it builds.

## §4. What this leg does NOT establish
That the theorem is proved. A kernel-checked proof of `navier_stokes_breakdown_R3` would certify
Fefferman's **(C)** as the project formalises it — the paper's one-line *"Consequently"* corollary —
and not Theorem 1.1's displayed existence clauses. Whether the comparator definitions faithfully
encode Clay (C)/(D) is outside what any of the five could measure. **No wall moved; no `L1→L4` link
moved; Clay ~0.05%.** The Lean, as measured here, is **no kernel check** of anything.

## §5. Pre-commitment of the next wave (§3g)
**`R5`(ii) SERIAL** (leg 432, pre-registered before its runner exists): the residual stress the
outer profile leaves on the terminal tail, `T_0 = F(p_s − s)` via (4.11) on the backward moment
representation of Lemma A.8, against Proposition A.10's factorisations (A.48)–(A.51), the ratio
bound (A.55) and the shear bracket (A.56); planted controls both ways. The tail is closure-
independent, so it is computable at every `λ`; the interface caveat of `leg_429.md` §6 stands.
**Wave 4 = `R5`(iii)–(vii) FAN-OUT ×5** on the interface `R5`(i)–(ii) pin, slot 5 the adversarial
verifier, pre-registered gates and planted controls in the briefs, *this is Tier 2, not a proof* in
every gate answer.

## §7. ADDENDUM 2026-09-10 (leg 435), Conductor-run, after the agents reported — **THE KERNEL CHECK WAS REACHED**
The build the Conductor continued in the background (`lake exe cache get` to completion, then `lake build`)
compiled the whole NavierStokes library and its Comparator glue from source with **zero errors**, and the
watcher ran `#print axioms` on the two exported theorems the moment `NavierStokes/ComparatorSolution.olean`
appeared (`writeup/data/arc6/lean/kernel_check_conductor.json`, output verbatim):

> `'NavierStokes.Comparator.navier_stokes_breakdown_R3' depends on axioms: [propext, Classical.choice, Quot.sound]`
> `'NavierStokes.Comparator.navier_stokes_breakdown_periodic' depends on axioms: [propext, Classical.choice, Quot.sound]`

**Gate (a) changes from `NOT-ESTABLISHED` to `ESTABLISHED-HERE`:** the two theorems are accepted by the Lean
4 kernel (v4.34.0-rc2) at clone HEAD `8937a8f4` with mathlib `85e3a25e`, with exactly the axiom set
`formalization.yaml` claims. **Nothing else changes:** (b) stands — what the kernel accepted is Fefferman's
(C)/(D), strictly weaker than Theorem 1.1; (c)'s source census is now matched by the kernel's axiom list;
(d) the comparator was still not run and no independent replay (`lean4export`/`nanoda`) was; (e) is a
source-level reading. Mathlib's oleans came from the official cache, not from source. The Euler library
was still compiling. **One Conductor, one command, one container; `UNVERIFIED` by a second agent** — the
kernel did the checking, which is the point of a kernel, and a person did not.
