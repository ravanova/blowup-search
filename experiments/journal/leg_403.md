# leg 403 — `V-W5`, VERIFIER of wave 5

**Unit:** `V-W5`. **Lane:** verification. **Branch:** `leg/403-vw5-verify-wave5`, cut from `main`
at `5c49486`. **Object:** wave 5 — `L5` (`4be46ef`), `D-REPAIR` (`036e56d`), `V-W4` (`b46ee4d`) —
all `UNVERIFIED`, and the Conductor may not verify a wave it planned.

**INSERT-ONLY.** Nothing above a `§` heading already written is ever edited; corrections are
appended as new `§` blocks.

---

## §0 — PRE-REGISTRATION. Committed BEFORE anything was run. NO RESULTS IN THIS SECTION.

### §0.1 The five gate items, copied verbatim from `writeup/waves/WAVE6_PLAN.md` (committed `e202653`)

> **GATE, final wording.** For each of the five items below, answer **REPRODUCES** or **DOES NOT
> REPRODUCE**, with the number: (1) `L5`'s gate answer `NO` and its `ρ`-exponent `0.00010850007559945518`,
> **re-derived from `writeup/data/p2_route_l5_finite_energy_v1.json` and the unit's own code, not from
> its report**; (2) `L5`'s three positive controls — `−0.2498` at `α = 1.25`, `−0.5996` at `α = 1.6`,
> `−2.000005` for the unmodulated `SS` control at `κ = a` — and **whether they license the claim that the
> `NO` is about the object rather than the apparatus**; (3) `L5`'s control `C6`, which **did not fire as
> planted** (`2.03e-02` against a `1e-02` tolerance): **was the tolerance moved at any point in the
> branch's history, YES or NO**, and does anything the `NO` rests on depend on the constant `C6`
> perturbs? (4) `L5`'s `self_hash` `0c5e0f827f526df6` and `V-W4`'s `e0171ac1e855f90e`, recomputed;
> (5) `D-REPAIR`'s epoch correction, re-derived from the banked rows.

### §0.2 What I will accept as a REPRODUCTION, item by item — fixed now, before any number is computed

**Item 1 — `L5`'s `NO` and `ρ`-exponent `0.00010850007559945518`.**
- I will locate, in `writeup/data/p2_route_l5_finite_energy_v1.json`, the raw `rows` the exponent is
  fitted from, and re-run the unit's OWN fitting code path on those rows, in a fresh process.
- ACCEPT as REPRODUCES iff the re-derived exponent equals `0.00010850007559945518` **bit-for-bit**
  (`repr()` equality of the float, equivalently `struct.pack('<d', ·)` equality). Anything short of
  bit-for-bit is DOES NOT REPRODUCE **with the disagreement stated in ULPs and in absolute value**.
- Separately, and reported separately, I will re-derive the `NO` **as a logical consequence of the
  artefact's own stated rule** (`Σ(S) = c_mod · S` diverges for any `c_mod > 0` that is
  `ρ`-independent), and check the artefact's `threshold_free` claim is what the arithmetic supports.
- I will ALSO recompute the exponent from the banked `rows` by an INDEPENDENT least-squares fit
  written by me, not by the unit, to separate "the unit's code is self-consistent" from "the number
  is right". A disagreement between the two is reported as a discrepancy even if it changes nothing.

**Item 2 — the three positive controls `−0.2498`, `−0.5996`, `−2.000005`.**
- ACCEPT as REPRODUCES iff each of the three re-derives bit-for-bit from the banked rows by the same
  route as item 1, AND each recovers, to the stated rounding, the exponent its planted `α` predicts:
  `α = 1.25 → 1 − α = −0.25`; `α = 1.6 → 1 − α = −0.6`; the unmodulated `SS` control at `κ = a` →
  `−2` (the artefact's `predicted_scale_invariant_sizes` for the surviving terms).
- I will state, as a separate sentence and NOT as an upgrade of the science, whether these three
  license the claim that the `NO` is about the object rather than the apparatus — i.e. whether the
  same apparatus, on planted objects with a KNOWN answer, returns the planted answer.

**Item 3 — was `C6`'s tolerance ever moved?**
- I will `git log -p` the file(s) that set `C6`'s tolerance across the WHOLE history of the `L5`
  branch (`e2f13c1 → 4be46ef`) and of `main`, and read every diff hunk touching that constant.
- ACCEPT as `NO, NEVER MOVED` iff the tolerance literal is introduced ONCE and never subsequently
  changed. If it changed at any commit I will name the commit, the old value and the new value.
- I will answer separately whether the `NO` depends on the constant `C6` perturbs (the cutoff basis /
  smoothstep degree), and whether the failure to fire is a defect in the control or a property of
  the object. I will NOT repair either way.

**Item 4 — both `self_hash` values.**
- Rule as stated: `sha256(json.dumps(doc_without_self_hash, sort_keys=True)).hexdigest()[:16]`.
- ACCEPT as REPRODUCES iff the recomputed 16-hex prefix equals `0c5e0f827f526df6` for
  `writeup/data/p2_route_l5_finite_energy_v1.json` and `e0171ac1e855f90e` for
  `writeup/data/p2_verify_wave4_v1.json`, under the rule EXACTLY as written above (default
  `json.dumps` separators, `ensure_ascii` default, key `self_hash` removed at TOP LEVEL only).
- If the literal rule fails I will report DOES NOT REPRODUCE and then, separately and labelled as
  such, report which variant rule (if any) does produce the banked digest. Finding the variant is
  NOT a reproduction of the stated rule.

**Item 5 — `D-REPAIR`'s epoch correction.**
- Source rows: `writeup/data/p2_prog_r4_e_v1.json` (`E`) and `writeup/data/p2_prog_r4_m3_v1.json`
  (`U5`). The claim under test is `experiments/journal/d_repair.md:95-100`, the like-for-like table.
- ACCEPT as REPRODUCES iff, from those banked rows alone: `E` `n_iters` total, `E` ledger-row total,
  `U5` `n_iters` total, `U5` ledger-row total, the four per-attempt figures `21.04`, `21.4375`,
  `21.95`, `22.3125`, the two ratios `1.0189` and `1.0165`, the core-seconds ratio `0.9958`, and the
  realised `95.389` s/epoch with ratio `1.0041`, all reproduce **to the precision printed in the
  journal** (i.e. rounding my computed value to the printed number of decimals gives the printed
  number). The convergence identity `2195 − 2104 = 91 = 100 − 9` and `357 − 343 = 14 = 16 − 2` must
  hold EXACTLY as integers.
- `0.9958` is the one the brief singles out: ACCEPT iff `round(computed, 4) == 0.9958`.

### §0.3 Reading (e) — the disclosed substitution, checked, not adjudicated

Additionally, per my brief's reading (e), and NOT as an adjudication of `CORRECTIONS.md` §34:
- check the artefact ITSELF discloses the substitution — the field
  `realization_lesson_91.route_4_has_no_banked_profile` must be present and `true` in the banked JSON;
- check control `C1` really reproduces leg 381 to `1.9961809982760315e-12` — recomputed as the max
  absolute disagreement between `C1`'s `reimplemented_rho_exponents` and its `banked_rho_exponents`
  over the keys they share, and cross-checked that the `banked_rho_exponents` really are leg 381's,
  i.e. equal to what `writeup/data/p2_route_cloc_v1.json` holds.

### §0.4 Pre-committed method, binding on me

(a) **REPAIR NOTHING.** I am not authorised to fix any defect I find. Record it with its number and
stop. A verifier who repairs has destroyed the evidence of the defect.
(b) **EXIT NON-ZERO ON ANY NON-REPRODUCTION.** `experiments/p2_verify_wave5_v1_evidence.py` must
fail loudly. Lesson 68: a check nobody can execute decays into a claim.
(c) **I MAY NOT RE-PLAN AND I MAY NOT RE-RANK.** If a finding implies a lane should be demoted I say
so in ONE sentence and stop; the ranking is the Conductor's.
(d) **A reproduction is not a validation of the SCIENCE, only of the ARITHMETIC.** I will not
upgrade `L5`'s `NO` on the strength of my own reproduction of it.
(e) **QUOTE VERBATIM OR LABEL IT A PARAPHRASE**, character-exact, with location.
(f) **A discrepancy that changes no verdict and no number is still a discrepancy** and is reported
with a *"what it does NOT change"* line, the form `V-W4` used.
(g) Tier 2 is never a proof; scale is not evidence; a ban is superseded by a MEASUREMENT, never by
a decision.

### §0.5 Territory, declared in advance

NEW FILES ONLY, plus this journal:
- `experiments/journal/leg_403.md` (this file, insert-only)
- `experiments/p2_verify_wave5_v1_evidence.py`
- `writeup/data/p2_verify_wave5_v1.json`

I will NOT edit any banked artefact, any `_v1` file, `STATE.md`, `WALLS.md`, `OPTIONS.md`,
`ORCHESTRATION.md`, `reports/`, `writeup/CORRECTIONS.md`, nor anything belonging to `L5`,
`D-REPAIR`, `V-W4`, `L6` (`leg/401-…`) or `V5` (`leg/402-…`), both of which are running concurrently.

**Ceiling declared in advance:** this unit verifies ARITHMETIC and PROVENANCE. It cannot and will not
move a link of the `L1 → L4` chain. Clay stays ~0.05%.

---

## §1 — WHAT RAN, AND WHAT SURVIVED IT

`experiments/p2_verify_wave5_v1_evidence.py`. **120 checks, 120 PASS, 0 FAIL, exit 0.**
Banked: `writeup/data/p2_verify_wave5_v1.json`, `self_hash da8a0d7cb9fb4896`, computed under the
same rule item 4 was sent to check.

**The script fails loudly, and this was TESTED, not asserted** (pre-committed reading (b),
lesson 68). A mutant copy with `GATE_EXPONENT` changed from `0.00010850007559945518` to
`0.00010850007559` exits **1** and names two failed checks by label. (A first attempt at a mutant —
changing the last digit `…18` → `…19` — exited **0**, correctly: those two decimal literals parse
to the *same* IEEE-754 double. That is a fact about the acceptance criterion, not a hole in it, and
it is why every float comparison in the script is done on `struct.pack('<d', ·).hex()` rather than
on the printed decimal.)

**Two run modes.** `--no-quad` re-derives every exponent from the banked rows (~3 s). The default
re-runs the float64 quadrature itself out of `L5`'s own code (~4 min). Both were run. Both green.

---

## §2 — ITEM 1. `L5`'s gate answer `NO` and its ρ-exponent. **REPRODUCES.**

**Answer: REPRODUCES, BIT-FOR-BIT.** `0.00010850007559945518`, bit pattern `21d8f11a51711c3f`.

**What the number actually is** (located from the artefact and the code, never from the report):
`fit_exponent(RHOS[-3:], curl_L32[-3:])` applied to `sweep["alpha=1|kappa=a_physical_frozen|DSS"]`,
i.e. the tail-3 log-log slope of `‖curl F‖_{L¹_t L^{3/2}_x}` over `ρ₀ ∈ {100, 300, 1000}`, where
`fit_exponent` is `experiments/p2_route_l5_v1.py:363`.

Three independent strengths of reproduction, all green:

| what was re-derived | result |
|---|---|
| `gate.rho_exponent` **is** `sweep[…].curl_L32_rho_exponent_tail3` | identical doubles |
| the unit's OWN `fit_exponent`, fresh process, on the **banked rows** | `21d8f11a51711c3f`, **bit-for-bit** |
| **MY OWN** pure-python OLS slope (not `numpy.linalg.lstsq`) | `0.00010850007559979301`, Δ = **3.378e-16** |
| the **float64 quadrature itself**, re-run out of `L5`'s code at `ρ₀ = 100, 300, 1000` | `curl_L32` and `L3` **bit-for-bit at every point** |
| the exponent re-fitted from those **freshly computed** rows | `21d8f11a51711c3f`, **bit-for-bit** |

The last two are the ones that matter: the measurement was **recomputed**, not re-read. `L5`'s
quadrature is deterministic to the last bit on this host.

**The `NO` itself, re-derived as a consequence rather than quoted.** From the artefact's own rule:
`c_mod = 869.2878218404479` (verified to be exactly `curl_L32_at_largest_rho`), `c_mod > 0`,
`c_mod_per_DSS_period = c_mod × 1.0612565021243408 = 922.5373531456809` (agrees to 1e-12 rel), and
each of the four `N_periods_affordable_by_threshold` entries recomputes exactly as
`ε / c_mod_per_period`. With the exponent `+1.085e-04` — **not strictly negative** — `Σ(S) = c_mod·S`
diverges, so the answer is `NO` for **every** `ε_close > 0` and the `threshold_free: true` flag is
what the arithmetic supports. `C1` never engages: no closure constant is computed.

**Reading (d), stated explicitly.** This reproduces `L5`'s ARITHMETIC. It does **not** upgrade the
`NO`, which remains a **Tier 2** statement about a **synthetic** float64 profile.

---

## §3 — ITEM 2. The three positive controls. **ALL THREE REPRODUCE, AND EACH RECOVERS WHAT IT PLANTED.**

**Answer: REPRODUCES.** Bit-for-bit on re-fit, bit-for-bit on a full quadrature re-run, and each
recovers the exponent its `α` plants:

| sweep key | reported | banked (full precision) | planted `1 − α` | \|error\| |
|---|---|---|---|---|
| `alpha=1.25\|kappa=a_physical_frozen\|DSS` | `−0.2498` | `−0.24977161426445738` | `−0.25` | `2.284e−04` |
| `alpha=1.6\|kappa=a_physical_frozen\|DSS` | `−0.5996` | `−0.5995717544426906` | `−0.60` | `4.282e−04` |
| `alpha=1\|kappa=a_physical_frozen\|SS` | `−2.000005` | `−2.000004894081206` | `−2.00` | `4.894e−06` |

**Do they license the claim that the `NO` is about the OBJECT rather than the APPARATUS?**
**YES, within Tier 2, with one stated limit.** The *same* code path, on three objects whose answer is
known in advance, returns the planted answer to `2.3e−04`, `4.3e−04` and `4.9e−06`. An apparatus that
manufactured a spurious zero exponent would have manufactured one at `α = 1.25` and `α = 1.6` too; it
did not. And the **unmodulated `SS` control at the SAME `κ = a`** — same cutoff, same quadrature,
same everything except `ṁ ≡ 0` — falls off at `−2.000005` with a constant **17544×** smaller than the
`DSS` case (`SS/DSS = 5.69995e−05` at the largest `ρ`). So what survives at `κ = a` is the
**modulation**, not the cutoff and not the quadrature. `C3′` closes the loop from the other side:
`c_mod` is linear in the modulation amplitude to `1.42e−04` and its value at amplitude `0` is
`0.5505` against `869.26` at amplitude `1`.

**THE LIMIT, said plainly.** These controls test the apparatus against analytically known
scale-invariant sizes of **the same synthetic poloidal ansatz**. They do **not** test it against a
real route-4 profile, which does not exist. Tier 2. Scale is not evidence.

---

## §4 — ITEM 3. **WAS `C6`'s TOLERANCE EVER MOVED? — `NO`.**

**Answer: `NO`. The tolerance was NEVER moved.**

**The file that sets it:** `experiments/p2_route_l5_v1_driver.py`, function `control_C6()`.
**The commit that shows it: `347676f`** (CHECKPOINT 1 — the driver written **before** the run).

`git log --all -p --follow` over that file returns **exactly three** diff lines carrying a `C6`
tolerance or criterion, across every ref in the repository. **All three are `+` lines. There is no
`-` line anywhere.** The literal is `1e-2` in all three:

| commit | line | when |
|---|---|---|
| `347676f` | `+    out["fired_as_planted"] = bool(max(d, dc) < 1e-2)` | driver written, **before the run** |
| `4be46ef` | `+    out["precommitted_tolerance"] = 1e-2` | landing — the same value, now a banked field |
| `4be46ef` | `+    out["fired_on_tail3_fit"] = bool(max(dt, dct) < 1e-2)` | landing — a **second** criterion, same tolerance, **different fit window** |

`L5`'s own pre-registration, **verbatim**, at `e2f13c1:experiments/journal/leg_400.md` line 274:

> `- **`C6` basis control.** Quintic `C²` vs degree-9 `C⁴` smoothstep: exponents must agree to `1e−2`.`

It fixes the **tolerance** at `1e−2` and **does not name a fit window**.

### §4.1 Is the failure to fire a DEFECT IN THE CONTROL or a REAL PROPERTY OF THE OBJECT?

**A defect in the CONTROL'S FIT WINDOW. Not a property of the object, and not a defect in the
tolerance.** The 5-point fit that fails spans `ρ₀ = 10`, which is pre-asymptotic and is exactly where
the two bases differ most. On the window the gate uses **everywhere else** — tail-3, `ρ₀ ≥ 100` —
the two bases agree to `5.408e−05`, i.e. **185× inside** the pre-committed tolerance, and both
tail-3 exponents sit within `1e−3` of zero (`C4`: `−6.180e−05`; leg 381's quintic `C²`:
`−7.718e−06`). The **object** — the exponent — is basis-independent. The control mis-specified its
own window.

**What it does NOT change: NOTHING.** The gate's exponent, the `NO`, and every number in §2 are
computed on the tail-3 window.

### §4.2 Does anything the `NO` rests on depend on the constant `C6` perturbs?

**`NO` for the `NO`. `YES` for the constant.**

- The `NO` is threshold-free and rests on the **exponent** not being strictly negative. Both bases
  give a tail-3 exponent within `1e−3` of zero. The `NO` survives the basis change.
- `c_mod` **is** basis-dependent: `constant_ratio_curl_C4_over_C2quintic = 1.476038407975093`. The
  degree-9 `C⁴` smoothstep gives a `c_mod` **47.6% larger** than leg 381's quintic `C²`.

### §4.3 DEFECTS LOCATED — RECORDED, **NOT REPAIRED**

**`D-VW5-1`** — *site:* `experiments/p2_route_l5_v1_driver.py::control_C6()`, commit `4be46ef`.
*What is wrong:* a **second pass-criterion**, `fired_on_tail3_fit`, was **added at the LANDING
commit** — after the failure on the pre-committed criterion was already known. The tolerance was not
moved and the failing result was not deleted: `fired_as_planted: false` is banked, `2.03e−02` is
banked, the `meaning` field discloses the reason, and the landing commit message says so in its own
voice. But **the criterion that passes is post-hoc.** *What it does NOT change:* **NIL** — no number
and no verdict. The tail-3 **window** is not itself post-hoc: `_rho_exponent_tail3` was already in
the driver's `sweep()` at `347676f`, before the run, and it is the window the gate uses.

**`D-VW5-2`** — *site:* `writeup/data/p2_route_l5_finite_energy_v1.json`,
`gate.c_mod_per_unit_s = 869.2878218404479`. *What is wrong:* the constant is quoted **unqualified**
in the gate block. `C6` measures a `1.476×` spread between the two smoothstep bases and the artefact
banks that ratio, but the gate block does not carry it. *What it does NOT change:* the `NO`. It
narrows the reach of `c_mod` **further than `CORRECTIONS.md` §34 already did** — §34 records that
`869.288` is *that synthetic profile's* number; this adds that it is also *that cutoff basis's*
number, to within a factor 1.48.

**NEITHER IS REPAIRED.** A verifier who repairs has destroyed the evidence of the defect.

---

## §5 — ITEM 4. Both `self_hash` values. **REPRODUCE, under the LITERAL rule.**

**Answer: REPRODUCES. `YES` for both.** No variant rule was needed.

Rule as stated: `sha256(json.dumps(doc_without_self_hash, sort_keys=True)).hexdigest()[:16]`,
`self_hash` removed at **top level only**, default separators, default `ensure_ascii`.

| file | unit | banked | recomputed | match |
|---|---|---|---|---|
| `writeup/data/p2_route_l5_finite_energy_v1.json` | `L5` | `0c5e0f827f526df6` | `0c5e0f827f526df6` | **YES** |
| `writeup/data/p2_verify_wave4_v1.json` | `V-W4` | `e0171ac1e855f90e` | `e0171ac1e855f90e` | **YES** |

**One difference between the stated rule and the implemented one, recorded because it is a
difference.** `L5`'s own helper at `experiments/p2_route_l5_v1.py:372` passes `default=str`, which
the stated rule does not. Verified to be a **no-op on this document** — both spellings give
`0c5e0f827f526df6` — because the artefact holds no non-JSON-native types. *What it does NOT change:*
**NIL.** It would matter only for a document containing a type `json` cannot serialise, and then
silently: `default=str` hashes a *repr*, not a value.

---

## §6 — ITEM 5. `D-REPAIR`'s epoch correction. **REPRODUCES.**

**Answer: REPRODUCES — every figure in the like-for-like table, and the cost model still stands at
`0.9958` of measured.**

The claim under test is `experiments/journal/d_repair.md:95-100`, `D-REPAIR`'s correction of
`V-W3`'s *"E used **2.3% fewer** epochs per attempt"*.

**Where the rows actually live.** `n_iters` is in the `_v1` artefacts; the **ledger-row** counts are
not — they are in `experiments/programme_r4/e_hhard_ledger.json` and
`experiments/programme_r4/u5_m3_ledger.json`, counted as `len(attempts[].ledger)`. Recounted from
those four sources:

| | `E` | `U5` |
|---|---|---|
| `n_iters` total | **343** | **2104** |
| ledger-row total | **357** | **2195** |
| attempts | **16** | **100** |
| converged | **2** | **9** |

**The convention identity holds, and it holds ATTEMPT BY ATTEMPT, not merely in aggregate.**
`2195 − 2104 = 91 = 100 − 9`; `357 − 343 = 14 = 16 − 2`. I checked the per-attempt rule — converged
⟹ `len(ledger) == n_iters`, non-converged ⟹ `n_iters + 1` — on **all 116 attempts**: **0 exceptions**.
`D-REPAIR`'s rule is exactly right.

**The like-for-like table, recomputed:**

| basis | `U5` | `E` | `E`/`U5` |
|---|---|---|---|
| epochs/attempt, `n_iters` convention | **21.04** | **21.4375** | **1.0189** |
| epochs/attempt, ledger-row convention | **21.95** | **22.3125** | **1.0165** |
| core-seconds/attempt | 2053.44 | 2044.8959049582481 | **0.9958** |
| realised s/epoch | (95.0 costed) | **95.38873026044307** | **1.0041** |

**The sign flips as `D-REPAIR` says:** `E` used **MORE** epochs per attempt in **both** conventions.
`V-W3`'s *"2.3% fewer"* is wrong in **size and direction**, and `D-REPAIR`'s conclusion survives —
the structural "overrun" explanation is contradicted by both ledgers in either convention.

**Does the banked cost model still stand at `0.9958` of measured? YES.** `0.9958391309014376`,
`E` **0.4% UNDER**, and `E`'s realised `95.389` s/epoch is within **0.409%** of the commissioned
`95` s model.

### §6.1 DEFECT LOCATED — RECORDED, **NOT REPAIRED**

**`D-VW5-3`** — *site:* the `U5` half of the `0.9958` ratio. *What is wrong:* the gate asked for the
correction to be re-derived **from the banked rows**. The `E` half is: `32718.334` s summed from
`p2_prog_r4_e_v1.json`'s own per-attempt `wall_seconds`, ÷ 16. **The `U5` half is not in any banked
JSON.** `0.0713` wall-h/attempt and the worker count `8` are **regex-scraped out of prose** at
`experiments/journal/prog_r4_u5.md:405` — `experiments/verify_wave3_rederive.py:159-172` does exactly
this, with a literal `re.search`. `U5`'s own artefact banks only
`resourcing.seconds_per_epoch_costed_at = 95.0`.

*What it does NOT change:* **the number.** `0.9958` is arithmetically exact given `0.0713` and 8
workers, and there is an **independent route with banked rows on both sides** — `E`'s realised
`95.389` s/epoch against `U5`'s **banked** `95.0` s/epoch model, ratio `1.0041` — which reaches the
same conclusion and needs no prose at all. The finding is about **provenance**, not arithmetic.

**NOT REPAIRED.**

---

## §7 — READING (e). `L5`'s disclosed gate deviation. **CHECKED, NOT ADJUDICATED.**

`CORRECTIONS.md` §34 owns the adjudication. I checked only the two things my brief named.

**(i) Is the substitution disclosed IN THE ARTEFACT ITSELF? — YES.**
`realization_lesson_91.route_4_has_no_banked_profile = true` is a **field in the banked JSON**, with
`route_4_no_profile_evidence` naming `experiments/journal/leg_382.md line 174` and
`experiments/journal/leg_397.md §1`, and `profile_used` saying **SYNTHETIC** in the field itself.
`L5` did not quietly substitute; the deviation is machine-readable in the artefact, not a footnote.

**(ii) Does `C1` really reproduce leg 381 to `1.9961809982760315e-12`? — YES, BIT-FOR-BIT.**
I re-fitted `L5`'s `fit_exponent` over `C1`'s own banked rows for all **7** magnitude keys at
`α = 1` and `α = 1.25` — **14 re-fits, every one bit-for-bit** — and took the max absolute
difference against `writeup/data/p2_route_cloc_v1.json`'s `measured_rho_exponents`. Recomputed
`1.9961809982760315e-12`, bit pattern `00000000008f813d`, identical to the banked field. And the
baseline **is** leg 381's: `C1`'s `banked_rho_exponents` equal `p2_route_cloc_v1.json`'s
`check_C_cutoff_magnitudes.by_alpha[…].measured_rho_exponents` **key by key**, and `L5` cites that
file's `self_hash 58c57b62c0cbc80d` correctly.

---

## §8 — THE FIVE ANSWERS, AND WHAT THIS UNIT DID NOT DO

| # | item | verdict |
|---|---|---|
| **1** | `L5`'s `NO` and ρ-exponent `0.00010850007559945518` | **REPRODUCES** — bit-for-bit, including a full quadrature re-run |
| **2** | the three positive controls `−0.2498`, `−0.5996`, `−2.000005` | **REPRODUCES** — all three, each recovering its planted exponent |
| **3** | was `C6`'s tolerance ever moved? | **`NO`** — `347676f`; never moved, never removed; the failure is a defect in the control's fit window, not a property of the object |
| **4** | `self_hash` `0c5e0f827f526df6` and `e0171ac1e855f90e` | **REPRODUCES** — `YES` for both, under the literal rule |
| **5** | `D-REPAIR`'s epoch correction | **REPRODUCES** — and the cost model still stands at `0.9958` of measured |

**Three defects located. ZERO repaired.** `D-VW5-1` (post-hoc second criterion in `C6`),
`D-VW5-2` (`c_mod` quoted without its 1.476× basis dependence), `D-VW5-3` (the `U5` half of `0.9958`
is scraped from prose, not banked). **None changes a verdict. None changes a number.** One —
`D-VW5-2` — narrows the reach of one constant.

**What this unit did NOT do.** It did not repair anything (reading (a)). It did not re-plan and did
not re-rank (reading (c)). It did **not** upgrade `L5`'s `NO` on the strength of reproducing it
(reading (d)) — a reproduction validates the **arithmetic**, never the **science**. It did not
adjudicate `CORRECTIONS.md` §34 (reading (e)).

**Nothing in these findings implies any lane should be demoted**, so there is no ranking sentence to
offer; the ranking is the Conductor's.

**Ceiling.** Tier 2 throughout, float64, synthetic realization. **No link of the `L1 → L4` chain
moved. Clay stays ~0.05%.** Wave 5's three units are now verified **as arithmetic** — which is what
`UNVERIFIED` was blocking, and no more than that.

**Banked:** `writeup/data/p2_verify_wave5_v1.json`, `self_hash da8a0d7cb9fb4896`, 120/120 checks.
**Executable:** `experiments/p2_verify_wave5_v1_evidence.py`, exit 0 clean and exit 1 under mutation.
**0 banked artefacts edited. 0 files belonging to another unit touched.**
