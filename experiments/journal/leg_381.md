# Leg 381 — Route-CLOC: is the localisation gap exactly as `CLAY_OBLIGATIONS.md` §4 states it?

**Gate: YES — "the reviewer's arithmetic survives (i) with the DSS modulation handled, AND (ii)
confirms the bounded-energy reading against the primary text" — with one REFUTED side-clause, one
labelling correction, and one REPAIRED derivation step**, all routed to integration verbatim
(TECHNICAL §6; integration's edit, not this leg's).

Novelty pass: `writeup/novelty/leg_381.md`, commit `638e056`, **committed before the runner
existed**. Runner: `experiments/p2_route_cloc_v1.py`. Ledger: `writeup/data/p2_route_cloc_v1.json`.
**Self-tests: ALL PASS.** Writeups: `writeup/4_p2_lottery/{BLOG,TECHNICAL}_P2_ROUTECLOC_V1.md`.
`plan_of_record.py` run first and read. **No solver module edited, no obligations file edited, no
localisation attempted. No link of the `L1 → L4` chain moved. Clay stays ~0.05 %.** No figure drawn
(fig99 was reserved for this leg and is left unused).

---

## 0. The answer in one paragraph

The `(T*−t)^{1/2}` **survives** an independent derivation for genuine `λ`-DSS with the log-periodic
modulation carried, and **the DSS exponent equals the SS exponent exactly**: measured ratio
`0.9999998844`, i.e. **no factor**. The reason is structural, not numerical — `λ^ℤ` is a subgroup of
the same scaling group that fixes the SS exponents, so it can only convert the constant `∫|U|²dy`
into a log-periodic `G(s)` of period `2 log λ`. The modulation is *not* negligible on this object
(`G` swings by a factor 2.99 within one period) and dropping it biases a naive fit by **2.47 %** —
which is why the gate demanded it be handled. Against the primary text, **bounded energy is confirmed
verbatim** as Fefferman's numbered condition (7), so §4's premise holds. Two corrections fall out:
**"with `f ≡ 0`" is REFUTED** (statement (C) permits a smooth forcing satisfying (4),(5); `f ≡ 0`
belongs to the *existence* statements (A),(B)), and the breakdown statement on `ℝ³` is labelled
**(C)**, not "(b)". One derivation step needed repair rather than confirmation: the reviewer's
identity is between two `+∞` exactly in the case of interest, and must be run on a fixed ball
instead — same conclusion, valid derivation.

## 1. What was actually run

An exactly-`λ`-DSS, exactly-divergence-free, non-axisymmetric poloidal synthetic field with tunable
algebraic decay `α` and a strong prescribed log-periodic modulation, checked to be what it claims:

* DSS symmetry `u(x,t) = λu(λx, T*−λ²(T*−t))`, `λ = 1.7`: max rel error **1.14e-15** (tol 1e-12).
* divergence-free, 4th-order FD: max rel **8.87e-12** (tol 1e-6).
* the change of variables itself, checked in **physical `x`-space** against the similarity-variable
  prediction — a different integral on a different grid in a different variable, so not tautological:
  max rel disagreement **4.19e-15** (tol 1e-8).

## 2. The magnitudes that decided the gate

| quantity | value |
|---|---|
| SS energy exponent | `0.500000000000` (pure power law, residual 8.9e-16) |
| DSS energy exponent, **modulation handled** | `0.499999942` |
| **DSS/SS exponent ratio — THE FACTOR** | **`0.9999998844`** |
| DSS energy exponent, modulation dropped (naive OLS) | `0.487628` — **biased 2.47 %** |
| `G(s)` oscillation amplitude over one period | **2.99** (299 % swing) |
| residual period in `s` vs `2 log λ` | `1.0574` vs `1.0613` — **0.36 %** |
| fixed-ball energy exponent at `α = 1` (predicted `α−1 = 0`) | **`−0.00026`** |
| `L²` shell ratio over 3 decades at `α = 1` | **`1000.32`** (linear divergence, reproducing leg 260) |
| `L²` threshold vs banked Type-I `α` | needs `α > 3/2`, has `α = 1`: **deficit 0.5, ratio 1.5×** |
| critical `L³` tail at `α = 1`, growth per decade of window | **326.875, constant to 7.4e-10** (log-divergent) |
| cutoff residuals at `α = 1`: nonlinear / viscous | `ρ^{−1.4993}` / `ρ^{−1.4999}` (**identical scaling, exactly at `α = 1`**) |
| divergence defect (Bogovskii source) | `ρ^{−0.4996}` |
| pressure perturbation at origin | `ρ^{−1.9997}` — vanishing relative to the profile's `(T*−t)^{−1}` |
| Clay clause ledger | **4 CONFIRMED, 1 CORRECTED, 1 REFUTED** of 6 |

## 3. What I deliberately did not do

* **No localisation attempted.** Cutting off changes the solution; §4's own sentence transferring the
  obligation to §5 is verified as correct, and §3 of the TECHNICAL is the *bill*, not a payment.
* **No certified decay enclosure built.** That is leg 382's instrument (slot C, Route-DEXC). This leg
  neither builds it nor depends on its landing: every magnitude is a function of `α`, so leg 382's
  enclosure `[α_lo, α_hi]` substitutes directly, and every bound depends on `α_lo` alone.
* **No obligations file edited.** All six requested amendments are routed to integration in
  TECHNICAL §6.

## 4. Territory deviation, disclosed

The spec's territory list names four files. The standing documentation contract (ORCHESTRATION §6,
restated in this leg's own instructions as "BLOG + TECHNICAL pair required even for a NO") requires
two more, and the merge gate enforces the pair. This leg therefore also added
`writeup/4_p2_lottery/BLOG_P2_ROUTECLOC_V1.md` and `TECHNICAL_P2_ROUTECLOC_V1.md` — new files under
this leg's own route name, colliding with no other slot's territory (380 is a programme, 382 creates
new solver modules, 233 creates postrepair files, 383 is undispatched). Flagged rather than assumed.
**No figure was drawn**, so `writeup/build_figures.py` is untouched and **fig99 remains unused and
available**.

## 5. Left to a successor

* **§7 of `CLAY_OBLIGATIONS` was not checked.** This leg read the *problem statement*; §7 is about
  the *Millennium Prize rules* (refereed journal of worldwide repute, two-year wait, general
  acceptance). §7's own instruction to check them against the Clay Institute's published rules still
  stands, unexecuted.
* **§1, §2, §3, §5, §6 were not verified.** Only §4 was in this leg's gate. Any header update is
  scoped to §4.
* **Statement (D), the `ℝ³/ℤ³` breakdown, carries no decay and no bounded-energy condition** — a
  torus target would make §4 vacuous by construction. Recorded as an option with its own cost (the
  route-4 object is not periodic; re-targeting re-opens §2's rigidity screen, the programme's
  strongest position). **No leg is authorised by this note**; it is integration's and the user's call
  whether it is worth a scoping question.
