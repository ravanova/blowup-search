# Leg 425 — unit `R2`, arc 6 second pass, SOLO MODE (§3f): READ ALL 166 PAGES

**Date:** 2026-09-09. **Role:** AUDIT (second of two consecutive audit units; `R3` is construction).
**Banked:** `writeup/data/arc6/ledger.json` (**139,889 B**), one entry per statement in
`statement_index.json`, with the nine charter fields `id, kind, section, page, hypotheses,
conclusion, constants, cites, role`, plus `cursor` and `hard_pages`.
**Machinery:** `experiments/arc6_ledger.py` (load / append / validate — the gate is computed, never
asserted). **Checks:** `writeup/arc6_ledger_evidence.py`.

**Every answer below is `UNVERIFIED` under §3f rule 1.** One session read the manuscript and wrote
its own ledger. Nothing here has been checked by a fresh session.

## §0. How the read was done, mechanically

Twelve section-sized passes over `manuscript_pages.txt` (§§1–3, §4, §5, §6, §7, §8, §9, §10, A, B,
C), every page in order, in ≤6-page reads. After each pass the section's statements were appended
through `append()`, which **refuses** an identifier not in the index, a duplicate, or a page that
disagrees with the index, and the cursor was pushed to `origin/leg/425-read-all-pages` — ten
cursor commits, so a mid-read handoff would have lost at most one section. Cursor history:
p.23 → 44 → 61 → 72 → 87 → 99 → 115 → 125 → 143 → 166.

## §1. THE GATE

> **(a)** one entry per statement (79)? **(b)** every entry non-empty? **(c)** hardest pages named?

### (a) **`YES` — 79 / 79.** Kinds 3/26/38/7/5, sections 1:1 3:3 4:11 5:5 6:6 7:8 8:8 9:9 10:6 A:10 B:9 C:3 — the charter's counts, recomputed from the ledger by the evidence script. Every page equals the index page.

### (b) **`YES` on the charter's three required fields, with one honest qualification.** All 79 entries have non-empty `hypotheses`, `conclusion` and `cites` (**158** hypothesis strings, **696** citation strings). `constants` is empty for exactly two entries — **`Definition 3.2`** (regular at the axis) and **`Definition 3.3`** (the `≪` convention) — because those definitions introduce no constant; the other 77 carry **270** constant strings. I did not pad those two to make the count round.

### (c) **`YES` — thirteen hard-page notes, each with pages and a reason.** The three hardest, ranked:

| rank | pages | what makes it hard |
|---|---|---|
| 1 | **107–111** (Prop 9.6, the inductive step) | Four ordered corrections, four exponent tables, and the cycle closes only through five inequalities on p.111 whose margins are **0.1 exactly** at the `0.4` row and `0.17 > 0.1` from the `W_0.68` threshold in (9.9). A slip of `0.02` in either threshold breaks the induction. |
| 2 | **74–77** (§7.1 frame and phase) | The hinge of the mechanism is one unlabelled line: `g₀ = F₀(−a, b_s)`, `λ₀² = 2aF₀²(1 − 2/v_s)`. It is the only place the cone condition `v_s > 2` becomes a positive wave growth rate. Verifiable only by recomputing the reference matrix by hand. |
| 3 | **129–137** (Prop A.4, the outer profile) | A dozen logarithmic stages, parameters in the strict order (A.6), a pulse amplitude that is the root of (A.19) on `[.9, 1.2]`, and cone margins `.74` / `1.68` from explicit quadratic maxima. The most numerically reproducible object in the paper and the easiest to port wrongly. |

The other ten: pp. 35–38 (Lemma 4.8 / Prop 4.10 constants justified only in the appendices),
39–44 (the twelve-deep parameter chain of Thm 4.6), 47–49 (Lemma 5.1's non-shrinking radius),
57–59 (Lemma 5.4's abstract summation), 62–64 (the torus `J_g = [[3,1],[1,5]]` and `κ_s = 10⁻⁵`),
82–83 (Prop 7.5's positivity via the reference cone), 90–92 & 97–99 (Lemma 8.2's flatness, Lemma
8.7's Vandermonde), 121–123 (Lemma 10.5's uniqueness with unrestricted pressure), 146–150 (Prop
B.2's analytic space and the complex-neighbourhood bound (B.16)), 158–160 (Lemma C.1's loop).

## §2. What the ledger holds that the gate did not ask for

**196 statement-to-statement edges** (from `cites`), **none dangling** — every `Kind S.n` a cite names
is in the index. **304** distinct displayed-equation labels cited. Sixteen entries cite no other
statement (the leaves: `Definition 3.3`, `Lemma 4.1`, `Lemma 4.3`, `Lemma 6.1`–`6.3`, `Definition
6.5`, `Lemma 7.4`, `Proposition 8.1`, `Lemma 9.8`, `Lemma A.1`, `A.2`, `A.6`, `A.9`, `Lemma B.1`,
`Lemma C.1`). External references cited from a statement: Fefferman `[13]` (Lemma 10.3, Cor 10.6)
and Stein `[20]` (Lemma 10.5). **This is the input R3 builds the DAG from.**

**Three things the read settled that the first pass (legs 417–422) had only inferred:**

1. **Where compact support is paid for.** Lemma 8.2's cutoff remainder `A_e f` is flat exactly when
   the weighted auxiliary-mean integral vanishes; Prop 8.3 isolates the single scalar obstruction
   `P`; Lemma 8.7's five-moment map removes `P, J_θ, J_z` while preserving the two moments (8.2).
   Lemma 10.3 then discharges Fefferman's (5) with a Borel-type extension — the "3 lines" of `U3`'s
   78.9 % figure are Lemma 10.3; the 78.9 % is Sections 4–9.
2. **Where non-existence comes from.** Theorem 1.1 is a *non-existence* statement only because of
   Lemma 10.5 — uniqueness against a bounded-energy competitor whose pressure has no growth
   condition, via a Riesz-transform identification of `∇π` and an expanding-ball commutator bound.
3. **Where the numbers live.** The only explicit numerical constants in the spine are
   `κ_s = 10⁻⁵`, `h < 1/100` (`h ≤ 10⁻²` in App. B), `j₀ ≤ .05`, `X_b = 100`, `X_i = 110`, `X_R =
   110 (CP_*)¹⁰`, the reserved-patch offsets after (A.9), `T_w = 60 log(1/λ)`, the pulse bracket
   `[.9, 1.2]`, the thresholds `0.68 / 0.9 / 1.9` in (9.9) and the gain `1/10` per cycle.

## §3. Not established
Nothing about correctness. The ledger records what each statement **says** and what it **cites**;
it does not say any of them is true. That is `R4`. No wall moved; no `L1→L4` link moved; Clay
odds unchanged.

## §4. Pre-commitment of the next unit (§3f rule 2)
**R3 (leg 426, CONSTRUCTION) — DAG AND SPINE.** Build the directed graph from the ledger's 196
edges (cites → cited), check it is acyclic, and extract the **spine**: every statement on a path
from `Theorem 1.1` down to a leaf that passes through `Theorem 3.1`. **Pre-committed expectation,
written before the graph is computed:** the spine passes through **§4** (`Theorem 4.6` via
`Proposition C.3`), **§7** (`Proposition 7.5`'s positive representation and `Lemma 7.1`'s
`λ₀² = 2aF₀²(1 − 2/v_s)`), and **§9** (`Proposition 9.6`'s `+1/10` cycle) — and that the longest
`Thm 1.1 → leaf` chain has length **≥ 8**. Gate: (i) acyclic `YES`/`NO`; (ii) spine contains
§4, §7 and §9 `YES`/`NO-AND-WHICH-IS-MISSING`; (iii) longest chain length, with the chain named.
Artefact `writeup/data/arc6/dag.json`; runner `experiments/arc6_dag.py`; evidence script.
