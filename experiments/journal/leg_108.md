# Leg 108 — Route-IX2: second freshness audit of `writeup/INDEX.md`

**Gate.** "Does `writeup/INDEX.md` correctly account for every leg landed since leg 68's pass —
either with a full quartet row (for legs that produced one) or an explicit note that the leg
falls under the 'no figure by design' audit/literature convention (for legs that did not)?"

**Answer: NO.** It accounted for 0 of 30.

## Method

Mechanical, no measurement produced. Three steps, all reproducible from `git` and `ls`:

1. Enumerate legs with at least one commit on `main` whose subject matches `Leg N:` and `N > 57`
   (leg 68's pass declared `INDEX.md` current through leg 57).
2. For each such leg, union the file lists of its commits and count how many touch
   `writeup/4_p2_lottery/{BLOG,TECHNICAL}*`, `writeup/figures/`, `writeup/data/`,
   `*_evidence.py`, `experiments/`, `experiments/journal/leg_N.md`, `writeup/novelty/leg_N.md`.
3. Cross-check the Arc 4 table's route list against `ls writeup/4_p2_lottery/` for routes that
   exist on disk but have no row at all.

## What the window is

**30 legs**: 58, 59, 61, 64, 65, 66, 67, 69, 70, 72, 73, 74, 77, 78, 79, 80, 81, 82, 84, 86, 87,
88, 89, 90, 91, 92, 93, 94, 97, 102.

`git log -- writeup/INDEX.md` returns **2 commits total**, the newest being leg 68's `b3ef49a`.
`INDEX.md` therefore received **0 edits across all 30 legs**, and its "Index currency" paragraph
was stale by 30 legs (45 leg-numbers, counting the 15 that never landed).

## What the 30 legs produced

| Quartet piece | Legs of 30 that produced one |
|---|---|
| BLOG **and** TECHNICAL under `4_p2_lottery/` | 1 (leg 59) |
| TECHNICAL only | 1 (leg 61) |
| registered figure in `writeup/figures/` | 1 (leg 59, `fig58_weight_repairs_v2.png`) |
| curated JSON in `writeup/data/` | 24 |
| `*_evidence.py` | 1 (leg 59, in `experiments/` → `Y†` placement drift, gap item 7) |
| `experiments/journal/leg_N.md` + `writeup/novelty/leg_N.md` | 29 (leg 58 uses `leg_58_verify.md`) |
| runner under `experiments/` | 30 |

So **28 of 30** legs are "no BLOG/TECHNICAL, no figure" — the audit / adversarial-robustness /
literature-watch / known-answer / hygiene convention. The thesis's guess about the convention's
dominance is confirmed at 28/30, but its *reflection* in `INDEX.md` was not: the file recorded
the convention for exactly 2 routes (Route-D advection scope, Route-D v15) and for 0 of the 28.
Assumed, not recorded — which is what the gate's "correctly reflected, not just assumed" clause
asks about.

## One gap found outside the stated window

**Weight-repairs v1** has 4 of 5 quartet pieces on disk (`experiments/p2_weight_repairs_v1.py`,
`writeup/data/p2_weight_repairs_v1.json`, both BLOG and TECHNICAL, `fig48_weight_repairs_v1.png`;
no `*_evidence.py`) and had **0 rows** in the Arc 4 table. Its landing commit `9d9b7ea` pre-dates
the `Leg N:` subject convention, so it carries no leg number and leg 68's by-number pass over
legs 53–57 could not reach it. `INDEX.md` referenced it only obliquely, as the other half of the
duplicated `fig48` in gap item 8. This is the audit's one genuine surprise: a route with a BLOG,
a TECHNICAL and a figure that no index row pointed at.

## Edits made to `writeup/INDEX.md` (append-only)

- **3 rows appended** to the Arc 4 table: Weight-repairs v1 (E `GAP`, F duplicated number),
  Weight-repairs v2 / leg 59 (full quartet, `Y†` evidence placement), Route-KA v1 / leg 61
  (B/T `GAP: T only`, E `GAP`, F `GAP`).
- **A second "Index currency" paragraph appended**, superseding leg 68's as a currency statement
  while leaving leg 68's record intact, and naming all 28 convention legs explicitly so the
  no-figure-by-design status is recorded rather than inferred.
- **Gap-list items 9, 10, 11 appended**: Weight-repairs v1's absent row + missing evidence
  script; Route-KA v1's missing BLOG/evidence/figure (3 of 5 pieces — and this is the
  repository's one *external* published-answer check of the interval pipeline, so lesson 76
  applies at full weight); and `writeup/README.md`'s numbered index now stale by a further 30
  legs, confirmed to still end at entry 47 with 0 mentions of Weight-repairs or Kawahara.

Nothing numeric in the underlying results was touched — `INDEX.md`'s own header forbids it and
this leg produced no measurement of its own. The 2 remaining `GAP`s in the new rows (an
`*_evidence.py` for Weight-repairs v1, a BLOG + evidence + figure for Route-KA v1) are left
`GAP` deliberately: choosing what a figure shows is claim-bearing and outside this leg's remit.

## Comparison to the sibling passes

Leg 102 (JR2) found `JOURNAL.md` owed 20 pointer entries and 0 missing per-leg files. Leg 108
finds `INDEX.md` owed 3 rows, 1 convention block covering 28 legs, and 3 new gap-list items — a
lighter backlog per leg than the journal's, because 28 of 30 legs correctly produced nothing for
this file to index.
