# Leg 238 — ROUTE-PUB4: fold leg 176 into PUB2, cite Xu §8 in PUB1 §3

**Gate: YES on both conjuncts.** Prose-only leg, purely additive. Branch `leg/238-pub4-v1`.
Date 2026-08-06. **No new numerics, no new claim, no runner, no JSON, no figure** — thin quartet
per the leg-214 / leg-180 precedent.

**The gate, pre-committed:** *"Does PUB2 now include leg 176's construction outcome (both its YES
headline and its flagged NO magnitude, neither softened nor strengthened) as its fourth data
point, and does PUB1 §3's scope line now cite Xu §8 as the nearest published relative, per leg
183's own exact recommendation?"* — **YES on both.**

---

## Order of work

1. `plan_of_record.py` run first. Every ban it prints is a **measurement** ban (gCLM measurement,
   Route-D sharpening, DSS, β on the 2D object, the scaling gauge, GA compute on an unvalidated
   fitness, …). This leg runs no measurement and computes no number, so none is engaged. The
   explanation-bans (e.g. *"repeating 'closure is a property of the SPACE' as an explanation"*)
   were checked against the added prose: §3.5 attributes leg 176's failure to **the block-diagonal
   shape of `A`**, which is leg 176's own attribution and the opposite of the banned framing.
2. `CONTINUATION_PROMPT.md` read.
3. Leg 176's report (`experiments/journal/leg_176.md`, 244 lines) and leg 183's journal
   (`experiments/journal/leg_183.md`, 243 lines) read **in full**, not grepped, before any edit.
4. Leg 192's state checked across **all refs**, not assumed — see "Premise correction" below.
5. `writeup/novelty/leg_238.md` written before the prose edits, recording the provenance of every
   transcribed magnitude.

## Addition (i) — leg 176 as PUB2's fourth data point

**The slot PUB2 reserved.** PUB2's drafted §0 read: *"A fourth point was commissioned and has not
landed. A construction leg on the origin-`H²` formulation (leg 176) was authorised and is in
flight."* Leg 176 landed at `bb0f184` (verified: the hash resolves, headline *"GATE ANSWERS YES on
both conjuncts, with one magnitude that says NO reported in the same breath"*).

### What changed in `TECHNICAL_P2_PUB2_V1.md`

| site | before | after |
|---|---|---|
| §0 axis table | *"the axis now has **three** points"*, 3 rows | *"**four** points"*, new row §3.5 carrying `σ_min = 0.0908` **and** `Z₁` best **140.72** in the same cell |
| §0 fourth-point paragraph | *"was commissioned and **has not landed** … no runner, no data, no gate answer"* | *"was commissioned, and it **has since landed** (`bb0f184`)"* + two stated limits (§§5–6 left as drafted; leg 192 has returned no verdict) |
| §3.4 closing paragraph | *"**it has not landed.** At drafting it had committed only its novelty pass"* | headline made explicitly retrospective, *"kept exactly as written"*, pointer to §3.5. **Its body is byte-unchanged** — the pre-registration quotes stay pre-registration quotes |
| **§3.5 (NEW)** | — | the fourth data point in full |
| §7 ceiling | *"had not landed at drafting and nothing is attributed to it"* | same, plus: folded in at §3.5 **only**, float64, no `Y₀`/`Z₂`, carried as **one leg's own measurements** |

### The two halves, and the test that neither was softened or strengthened

Both are quoted at leg 176's own strength, with its own hedges kept:

- **YES half.** `σ_min = 0.0908`, `‖R‖_X = 11.0127`, 0.139 % over a 16-fold truncation range,
  full `N = 8…1024` ladder including the *rising* `N = 1024` row and leg 176's attribution of it
  to the float floor. Leg 176's hedge **"float64 evidence of a positive limit, not a proof of
  one"** is carried verbatim — dropping it would have been the strengthening. Xu closed form at
  4.767e−15 / worst 5.135e−15. Tail `‖T⁻¹‖_X = 4.026`. Both controls (unbordered → float floor;
  loose `L²` → `N^{−1.49}`, no gap).
- **NO half.** The full 5×3 `Z₁` table, **best cell 140.72 where `< 1` is needed**, growth `~K²`
  — stated in the §0 table row itself, not buried in §3.5, so the headline row cannot be quoted
  without it. Leg 176's own reading of *why* it differs from `ℓ¹_w` (the block-diagonal **shape of
  `A`** fails, not the operator and not the space) is carried, **and fenced**: §3.5 states that no
  claim is made that some other, non-block-diagonal `A` closes it, because that was not tested.
  That fence is this leg's sentence and is the guard against strengthening.

**The one thing added that leg 176 did not say about PUB2.** Leg 176 measures leg 163's `σ_min`
witness as **optimistic by 7.9×** (*"That is a witness, not a bound. The measured value is
0.0908"*). PUB2 quotes that witness as `≥ 0.71465` at **three** call-sites (§3.2, §4.5, §5(3)).
Those sites are **outside this leg's declared territory** (leg 176's data point only), so they are
**left as banked and the conflict is flagged inside §3.5** rather than silently edited — a reader
is told to take 0.0908 as measured and `≥ 0.71465` as superseded. §3.5 also records that the
correction's *direction* changes no conclusion in §3–§5, since those sections use only that
`σ_min` is bounded away from zero and truncation-independent, which both values satisfy.
**Escalated to the user as a residue, not resolved unilaterally.**

### `BLOG_P2_PUB2_V1.md`

Same data point in the blog register, inserted before the non-normality catch: the yes (`5e−15`,
`0.0908`, 0.139 % over sixteen-fold, both controls), the no (**140.72** against a required **1**,
growing like the square of the split), the different-reason framing with the same explicit fence
(*"nobody has tested whether a different shape closes it"*), the two honesties (one leg's float64,
independent check not reported; the `0.71465` witness revised 7.9×), and the ceiling restated in
leg 176's own words (*"infrastructure, not a theorem"*). The closing caveat paragraph's stale
*"had not reported when this was written"* was updated in place.

## Addition (ii) — Xu §8 in PUB1 §3's scope line

Leg 183's recommendation applied **verbatim**, as a block quote, appended after §3.1's existing
scope line (which is byte-unchanged):

> *Xu §8 rules out weighted resolvent enclosure of the raw grid truncation by norm-invariance of
> eigenvalues; §3 rules out bounded approximate inverses in `ℓ¹_w` by a norm-dependent
> lower-bound failure; the two are complementary and neither implies the other.*

Leg 183's framing travels with it: Xu §8 is the **nearest published relative** — *"same operator,
different realization, different certificate quantity, both concluding an off-the-shelf enclosure
does not enter"* — *"a referee who knows the paper will ask"* — and **"This is an addition, not a
correction to a claim."** Leg 183's own two contrasts are included so "complementary" is supported
rather than asserted: raw grid without an origin condition at `a > 0` vs the bordered `a = 0`
matrix whose gauge row **is** Xu's `v′(0)` functional (leg 181); and Xu's eigenvalue quantity
*"exactly invariant. It is a theorem, not a measurement"* where `Z₁` is not. Leg 183 also
recorded that it read §8 at full text and resolved that it does **not** pre-empt §3 — that
resolution is stated, so the citation cannot be misread as a concession.

## Premise correction: leg 192 has not verified anything

The dispatch states leg 176 *"has since landed … and been independently verified (leg 192)."*
**Checked across all refs and false at this writing.** Leg 192's only commit anywhere is
`483b0d7` — its pre-registered novelty / prior-art pass, one file, `writeup/novelty/leg_192.md`,
116 lines — sitting on a live worktree branch and **not an ancestor of `origin/main`**. There is
no leg 192 journal, no verification runner, no re-derivation, no verdict.

Consequence, applied rather than merely noted: **leg 176's numbers are written into PUB2 as leg
176's own single-leg float64 measurements**, and PUB2 says so in §0, §3.5 and §7. The dispatch's
premise would have licensed a stronger sentence; it is not available and was not used.

## Territory

`writeup/4_p2_lottery/TECHNICAL_P2_PUB2_V1.md`, `writeup/4_p2_lottery/BLOG_P2_PUB2_V1.md`
(leg 176's data point only), `writeup/4_p2_lottery/TECHNICAL_P2_PUB1_V1.md` (§3 scope line only),
`writeup/novelty/leg_238.md`, this file. **Five paths, exactly the declared set.** Legs 176 / 183
/ 186 / 192 material was read read-only and edited nowhere. No shared ledger touched:
`DIRECTION.md`, `experiments/JOURNAL.md`, `LITERATURE_CHECK.md`, `plan_of_record.py`,
`CONTINUATION_PROMPT.md`, `capabilities.py` unmodified. No solver, no test, no runner, no JSON,
no figure, no `writeup/INDEX.md` entry (no new document was created).

## Ceiling

**Nothing was measured and nothing is claimed.** Two documents were made current with two landed
legs. No link of the `L1 → L4` chain moved; none has moved. Clay odds unchanged at ~0.05 %. No
ban lifted, none engaged.

**PUB1 and PUB2 remain DRAFTS for the user's review. This leg's landing does not approve either
document**, and folding a fourth data point into PUB2 is emphatically not a step toward approval —
approval is parked for the user by standing instruction. Both documents still carry their own
"this leg's landing does not approve it" headers, unmodified.

## Residues for the user

1. **§3.2 / §4.5 / §5(3) of PUB2 still quote `σ_min ≥ 0.71465`**, which leg 176 measures as
   optimistic by **7.9×** against a true **0.0908**. Flagged in §3.5, not edited — those sites are
   outside this leg's territory. A one-line editorial pass would resolve it.
2. **PUB2 §§5–6 are the three-result synthesis** and were left as drafted; §3.5 states which of
   their sentences leg 176 sharpens (§3.3's *"escalated rather than built"* now describes leg 163
   specifically) and which stand unchanged.
3. **Leg 192's verification is still outstanding.** If it lands and re-derives leg 176's chain,
   PUB2's three "one leg's own measurements" hedges become removable.
