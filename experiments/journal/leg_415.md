# Leg 415 — `P2-DRAFT`: draft `P2_ALPHA_PIN_PINCER`. Wave 9, PAPER unit.

> **PRE-REGISTRATION — §0 through §4 were written and committed BEFORE a line of `DRAFT.md`
> existed. Nothing from this header to the end of §4 was edited afterwards.**

**Unit:** `P2-DRAFT` (`writeup/waves/WAVE9_PLAN.md` UNIT 3). Leg 415. Branch: none — siblings share
this tree, so commits are explicit-path on `main` and are **not pushed**.

## §0. WHAT THIS UNIT IS AND IS NOT

This is a **PAPER** unit. It runs **no experiment**, produces **no new number**, and touches **no
experiment code or artefact**. Its whole job is to state, for a referee, what the record already
holds — and to record, separately, everything that will not state cleanly.

**THE BINDING RULE, from `writeup/papers/README.md` and the dispatch, verbatim:**

> **A PAPER IS A VIEW OF THE RECORD, NEVER A SOURCE.**

Consequences I am bound by and will not soften:

- Every number in `DRAFT.md` cites the **banked JSON field** it came from. Not a journal, not a
  previous draft, not memory. A number that cannot be traced to a field does not go in.
- `UNVERIFIED` stays `UNVERIFIED`. `UNDER-RESOURCED` is **never** written as a null result.
- A control that **did not fire as planted** is disclosed **in the paper**, not only here.
- **No unit may cite a draft.** `DRAFT.md` is load-bearing for nothing.
- **PROHIBITED:** softening a caveat because it reads badly in a paper. **The caveats ARE the
  result here.**

## §1. THE HARD CEILING ON THE CONTRIBUTION — user directive, verbatim, PROHIBITED to exceed

> "Writing `P2`'s contribution as larger than **'the identification of `T₃` as the sole survivor and
> its `ṁ`-proportionality, in float64, on a synthetic profile.'**"

That sentence is the ceiling. It is not enlarged in the abstract, the introduction or the
conclusion, and I have written it into `DRAFT.md` §1 as a boxed statement so a reader meets it
before any result.

**Everything else is ceiling too, and is fixed here before any prose exists:**

1. **TIER 2 throughout.** float64. No certified bound, no interval arithmetic, no proof.
2. **No `L1 → L4` link moves.** Clay stays **~0.05%**. Drafting a paper moves nothing.
3. The numerics are on **leg 381's SYNTHETIC exactly-DSS realization**, not on route 4's own
   object (`CORRECTIONS.md` §34). Route 4 has **no banked continuous profile**
   (`p2_route_l5_finite_energy_v1.json` `realization_lesson_91.route_4_has_no_banked_profile = true`).
4. **The paper claims no theorem.** Where a theorem is used it is *cited*, and the object is shown
   by *measurement* to meet its *hypotheses*. That is a different and weaker act.

## §2. WHAT §51/§52/§53 DO TO THE NUMBERS — confronted, not routed around

`CORRECTIONS.md` §52 (`L-JVER`, leg 409) established that
`J(c) = ∫₀^{T_s}‖W(·,s)‖_{L^{3/2}} ds` is a **logarithmically divergent** integral — at `r → ∞`
and at `r → 0` — and that `L-JVER`'s independent operator reproduces `L6`'s `J` **at `L6`'s own
nodes and weights** to `1e-14`, *including at the point where the gate fails by 43%*. Neither
program is miscoded. **The integral does not exist.**

§53 established that the divergence **reaches the minimiser**, and that **the sign is POSITIVE**.

**THEREFORE, PRE-COMMITTED, BEFORE I WRITE ANY NUMBER:**

- **Any `J`/`ρ` value printed in `DRAFT.md` is a value AT A STATED TRUNCATION** and is printed with
  `r_min` / `r_max` / `nq_r` beside it. **A `J` printed bare is a false statement in this paper.**
- `c_mod = 869.288` is under an **open §53 flag**. `L5-cmod` (leg 413, this wave) is the unit that
  discharges it. It had **not landed** when this draft was written; the draft says so, and says what
  each outcome does to the sentence it sits in.
- The distinction I must hold everywhere: **the gate answers survive; the numbers do not.**

## §3. WHAT THE SOURCES MAY AND MAY NOT CARRY — pre-committed

`PB2` (leg 410) discharged §3k rule 2. **W4 clause (b) is carried by Tsai 1998 (*ARMA* 143, 29–51)
Theorem 2.** NRŠ (*Acta Math.* **176** (1996) 283–294) **does NOT apply** — its hypothesis is
`U ∈ L³` exactly, and `∫|U|³` is log-divergent at `α = 1`.

> **If my prose ever needs NRŠ to carry the step, I have made an error — I stop and report it.**

Depths are taken from `writeup/SOURCES.md` and are **not upgraded by this unit** except where I
actually opened the file, which is recorded in §5 below with the bytes.

## §4. THE SECOND OUTPUT, WHICH IS NOT THE PAPER

`writeup/papers/P2_ALPHA_PIN_PINCER/FINDINGS.md`: every contradiction, unsupported number, or claim
that dissolves when written for a referee. **NEVER smoothed over in prose.** A finding severe enough
to reopen a wall is flagged for **immediate escalation**, not end-of-wave.

`FINDINGS.md` applies nothing. `CORRECTIONS.md`, `WALLS.md`, `STATE.md`, `OPTIONS.md`,
`writeup/waves/**` and every experiment artefact are **not touched by this unit**.
