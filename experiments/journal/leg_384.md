# Leg 384 — ROUTE-COBV: the obligations document's remaining reviewer-read clauses

Slot D of the four-slot parallel run. **Verification class, NOT floor-eligible.**
Branch `leg/384-cobv-v1` off `origin/main` @ `15c365a`.
Figure number allocated at dispatch: **fig105** (no other).

## PRE-COMMITTED GATE — written before anything was run; immutable

> **Gate.** Does every checked clause either verify against its primary source / landed record,
> or get reported with the exact discrepancy verbatim?
>
> - **yes** -> The obligations document graduates from DRAFT-UNVERIFIED clause by clause, on
>   integration's edit.
> - **no** -> A source is unreachable: bank the refusal as a refusal; an unverifiable clause is
>   recorded as exactly that.

**CEILING: TIER 2 in both branches.** `CLAY_OBLIGATIONS.md` §6's two no-method obligations
(§4's certified far-field decay + admissible cutoff; §5's persistence under localisation) stay
**OPEN in every branch**. Nothing here is described as movement toward Clay unless a link of the
`L1 → L4` chain actually moved, and none does. **Clay stays ~0.05%.**

**This leg is a verifier. It reports gaps and never repairs them.** `CLAY_OBLIGATIONS.md` is
integration-owned and is read-only here; any proposed correction is written out as exact
proposed text in the report and routed to integration.

## Scope, exactly three items (DM cycle-11e resolution, DIRECTION.md §4 of that cycle)

**(i) Mechanical verbatim-match.** The landed `CLAY_OBLIGATIONS.md` (integration commit
`a962e4a`, merged in `d65419c`) against `writeup/data/p2_route_cloc_v1.json`'s `check_D`
verdicts, **programmatically**. Eleven named sub-items:

1. `with f ≡ 0` excised from the "target, as specified" paragraph;
2. the 4 CONFIRMED / 1 CORRECTED / 1 REFUTED ledger block present, with its counts;
3. the (b) → (C) labelling fix;
4. the not-a-shortcut clause;
5. §4's `VERIFIED` header;
6. its three amendments (DSS-costs-nothing-extra; the truncated-law repair; the measured 1.5×
   deficit);
7. the priced cutoff bill;
8. the §4-stays-open-until-DTOL rule;
9. §7 marked `STILL UNCHECKED`;
10. §8 ask #1 recorded `SATISFIED`;
11. statement (D) named-but-not-authorised.

Mismatches are reported **verbatim, in both directions**: a claim in the document unsupported by
the JSON, and a JSON verdict not reflected in the document. **The "has the edit landed yet?"
branch is DEAD** — the edit is landed at `a962e4a`; that tense is resolved and is not carried as
an open question.

**(ii) §2's four screen rows**, cross-checked against their named landed records,
**machine-read, not transcribed**: NRS 1996 / Tsai (T1/T2) (legs 253, 341); Chae–Wolf /
Pineau–Vicol (leg 330, `p2_route_pvlx_v1.json`); Chae–Tsai (leg 326,
`p2_route_ctrx_v1.json`); Morrey / Jiu–Wang–Wei arXiv:2006.15776 (legs 368/370,
`p2_route_mryx_v1.json`, `p2_route_b7m_v1.json`, `solver/dssp_screen.py`). Reading a number off
a document and retyping it is the failure mode this clause exists to catch, so every compared
value is parsed out of both sides by the runner.

**(iii) §7's prize-rules clauses** — refereed publication in a journal of worldwide repute; the
two-year waiting period; general acceptance in the mathematics community — against the **Clay
Institute's own published rules**. **If a source is unreachable, the refusal is banked verbatim
with its HTTP status code**, and the clause is recorded UNVERIFIED. An unverifiable clause is
never recorded as a pass and never quietly dropped.

**No outreach of any kind.**

## Pre-committed instrument control (the fabricated-zero lesson)

The headline is a mismatch count. A zero mismatch count must be shown to be a **measurement**,
not an **absence**. Pre-committed before the runner was written:

- every comparison records the rows it actually ran on, by name, and its own row count;
- a **planted-mismatch control** re-runs every check against deliberately corrupted copies of
  the document text and of the landed records;
- the control passes only if the corrupted run reports a **strictly positive** mismatch count on
  the same named rows the clean run reported zero on, and the JSON banks both.

## Territory (a diff outside it fails the merge gate)

`experiments/p2_route_cobv_v1.py`, `writeup/data/p2_route_cobv_v1.json`,
`writeup/novelty/leg_384.md`, `experiments/journal/leg_384.md`,
`writeup/4_p2_lottery/BLOG_P2_ROUTECOBV_V1.md`, `writeup/4_p2_lottery/TECHNICAL_P2_ROUTECOBV_V1.md`,
`experiments/p2_route_cobv_v1_evidence.py`, `writeup/build_figures.py` (registration line only).

Explicitly **not** touched: `CLAY_OBLIGATIONS.md`, `DIRECTION.md`, `plan_of_record.py`,
`experiments/JOURNAL.md`, `LITERATURE_CHECK.md`, `PHASE2_P2_NOTES.md`, `CONTINUATION_PROMPT.md`,
`reports/`, `solver/dssp_decay_enclosure.py` (leg 386), new `solver/dssp_decay_samples.py` files
(leg 385), `experiments/programme_r4/` (PROG-R4), `experiments/p2_route_cloc_v1_evidence.py` and
leg 381's TECHNICAL doc (a live DOCS unit). `writeup/data/p2_route_cloc_v1.json` is **read,
never written**.

## Quartet checklist (ORCHESTRATION.md §6 — checked against §6 itself, not against the merge gate)

The merge gate is known to pass when **both** of BLOG/TECHNICAL are absent, and it never checks
for a figure; leg 381 landed with no figure and leg 233 with no BLOG/TECHNICAL/evidence/figure.
This leg checks its own quartet:

- [x] (1) runner `experiments/p2_route_cobv_v1.py`
- [x] (2) curated `writeup/data/p2_route_cobv_v1.json` — every number quoted in prose is in it
- [x] (3) `BLOG_P2_ROUTECOBV_V1.md` **and** `TECHNICAL_P2_ROUTECOBV_V1.md`, plus
      `experiments/p2_route_cobv_v1_evidence.py` rebuilding the claims from the curated JSON
      **without re-running anything** (no document parse, no ledger execution, no re-fetch)
- [x] (4) **fig105** registered in `writeup/build_figures.py` `P2_EVIDENCE`, emitting
      `writeup/figures/fig105_route_cobv_v1.png`

## Log

- Novelty pass + this pre-registration committed **before** the runner exists (`2cd5e5e`).
- Runner + curated JSON (`62a727e`). **23 checks: 17 MATCH / 6 MISMATCH / 0 UNVERIFIED.**
  Gate answers **YES** — no clause was unverifiable, so the `no` branch never engaged and no
  refusal needed banking.
- Clay rules reachable: **HTTP 200** on both the rules page (79 319 B, sha256 `0a6f68d5…`) and
  the 2018-revision PDF (43 263 B, sha256 `9b500374…`), cached to the gitignored `Papers/`.
  Item (iii) is measured, not refused.
- **Instrument control: 23 planted controls run, 23 fired, 0 failed.** Both directions per row
  (corrupt-plant flips MATCH→MISMATCH; repair-plant flips MISMATCH→MATCH). Three plants failed
  to fire on first attempt and each exposed a checker defect, not a document fact:
  - I10 — the banked C2 rows step **2** decades, not 1, so the naive first difference was double
    the per-decade increment and the "cube of" branch could never trigger. Fixing the control
    turned a false MATCH into a genuine finding.
  - I5 — raw `str.replace` could not cross a markdown blockquote line wrap; replaced by a regex
    tolerant of `\n> `.
  - III2 — raw `str.replace` failed against the PDF's U+00A0 word separators; replaced by a
    whitespace-tolerant regex.
- Six MISMATCHes, all with fired controls: **I8** (tolerance `2.6e-5` is the single best row;
  banked `max_abs_error` is `0.0256893924479335`, factor **988.05**), **I10** (`326.875` per
  decade is the increment of the **cube**, not the norm, which runs `8.679 → 14.841`),
  **I15** (reverse direction — the `CONFIRMED AND STRENGTHENED` decay verdict is not reflected
  in the landed document), **III1** (refereed *journal* vs the rules' *publication*, plus the
  omitted 6(a)(ii) route), **III3** (dropped "global" and "sole discretion of CMI"), **III4**
  (§7 lists three conditions; the rules' Section 4 imposes four — 4(d) is missing).
- Recorded on MATCHing checks, not as mismatches: **II1** — §2 row 1 cites "legs 253, 341", but
  **no `writeup/data/*.json` declares leg 253**; the operative encoding is
  `solver/dssp_screen.py` (legs 357/359/362). **I11 / I13** — the DTOL numbers and §8 ask #1's
  ruling are **not in `p2_route_cloc_v1.json`**, i.e. outside this leg's comparison source, and
  are banked as unchecked-here rather than verified.
- Quartet + fig105. Exact proposed correction text for all six findings plus the II1 citation
  note is written out in `TECHNICAL_P2_ROUTECOBV_V1.md` §6 and routed to integration.
  **`CLAY_OBLIGATIONS.md` was not edited by this leg.**
- **No link of `L1 → L4` moved. §6's two no-method obligations stay OPEN. Clay stays ~0.05%.**
