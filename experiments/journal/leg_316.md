# Leg 316 journal — Route-DFRE

**Branch:** `leg/316-dfre-v1`. **Date:** 2026-08-11. **Difficulty:** heavy (as pre-committed).

## Order of work

1. `CONTINUATION_PROMPT.md` read in full, including the novelty-pass calibration
   blockquote. Cell status: DF-CGL / Grade-A-nonfluid is the precedent leg 174 named
   already-vacated (Breden–Chu, Dähne–Figueras, BCG, ALS all censused; nobody in the
   cell), so per the calibration the novelty pass weighted toward OLD prior art
   (this repository's own leg 48, and the paper's own bibliography) over a fresh
   recency sweep — leg 48 closed the recency question for this exact paper in 2026-08.
2. `.venv/bin/python plan_of_record.py` read in full; stage-V ban text captured verbatim
   in `writeup/novelty/leg_316.md` §1 and re-checked after construction in
   `TECHNICAL_P2_ROUTEDFRE_V1.md` §7.
3. `ORCHESTRATION.md` §6/§7/§7b/§8 read.
4. Leg 256's report/novelty/JSON and leg 61 read in full as the shape template.
5. `capabilities.py` grepped (`hermite`, `laguerre`, `arb_dump`, `Ginzburg`, `CGL`, `Q_zero`,
   `Q_infinity`) — only leg 48's float64 equation re-derivation exists; nothing decodes
   Arb's proof-witness dump format. New module justified (a script, not a `solver/*.py`
   module — the decode logic didn't need to live in `solver/`, so it's self-contained in
   the runner, inside declared territory).
6. **Novelty pass committed first** (`7e47f65`), stating the ban check explicitly per the
   brief's instruction, before any construction.
7. Construction: `experiments/p2_route_dfre_v1.py` (runner) and
   `experiments/p2_route_dfre_v1_evidence.py` (evidence + figure), committed together
   (`2315dfa`) once the gate had run and both were checked against each other.
8. `TECHNICAL_P2_ROUTEDFRE_V1.md` and `BLOG_P2_ROUTEDFRE_V1.md` written, all prose numbers
   checked against `writeup/data/p2_route_dfre_v1.json` via the evidence script's own
   assertions (16/16 pass).

## Mid-leg coordinator corrections, both applied

1. **Figure number**: briefed as fig70; corrected to **fig71** (leg 311 claimed fig70
   first). Applied throughout (filename `fig71_route_dfre_v1.png`, all prose references).
   Per the brief, `writeup/INDEX.md` is NOT edited by this leg — the orchestrator
   registers it, along with the `writeup/build_figures.py` `P2_EVIDENCE` list entry (see
   integration note below).
2. **Performance-first rule** (new standing rule, effective mid-leg): before running
   anything estimated to exceed 10 minutes, assess and cut it down first. Applied and
   recorded in `writeup/novelty/leg_316.md` §7 (pre-run estimate) and here (measured):
   - **Estimate before running**: ~49,465 CSV rows, pure hex/`Fraction` parsing, O(1)
     comparisons per row, no iterative solves — estimated low tens of seconds, well under
     the 10-minute threshold. No rewrite needed before first launch.
   - **Measured**: first run (cold, including the one-time `git clone` of `CGL.jl`) took
     64.5s wall (dominated by the network clone, not the decode). Second run, repo
     already cached: **26.8–31.0s** end to end, all of it the actual decode+check work.
     No further optimisation was warranted at that scale.

## What ran, and what it found

`experiments/p2_route_dfre_v1.py`: clones (once, cached under `Papers/_cgl_jl_cache/`,
gitignored, never committed) `github.com/Joel-Dahne/CGL.jl` at the exact commit
`be034923c0b63e3103a9b2cb030a02699d05625e` the paper's own bibliography names, decodes
`proof/data/branch_d=1_j=1/{top,turn,bottom,connection_points}.csv.gz` (Arb's
`arb_dump_str` format, read off Arb's own C source `arb/{arf,mag,arb}/dump_str.c`, exact
dyadic decode into `fractions.Fraction`), and checks two conditions the paper's own
Section 6 states are what makes their chained local-existence boxes constitute one
continuous curve: (A) each row's existence box is strictly inside its own uniqueness box,
and (B) each row's existence box is inside the *previous* row's uniqueness box.

**Result: 49,465 rows, zero failures on either condition, zero tiling gaps.** Decoder
cross-checked against the paper's own printed Section 6 starting values
(`μ₁ = 1.23203752321003`, `κ₁ = 0.8531088807225934`) — decoded midpoints differ by
6.41e-07 / 5.26e-06, both comfortably inside the decoded existence ball's own radius.

**Gate (pre-committed wording): "Does DF-CGL's published verification package reproduce
the paper's own corollary from its published constants, in this environment?" — YES.**

Under the leg brief's yes-branch: banked at full strength, §0b's framing recorded verbatim
in `TECHNICAL_P2_ROUTEDFRE_V1.md` §6. The lane's second entry becomes dispatchable, DM's
choice at landing.

Leg-256 attribution rule applied in `TECHNICAL_P2_ROUTEDFRE_V1.md` §8: since the answer is
YES, there is no "theirs" vs "ours" classification to make — no drift, no challenge, no
escalation triggered. Recorded explicitly that this differs from legs 61/256, which each
found a real issue; a clean result is reported as such, not inflated or suppressed.

## Ban check, re-verified after construction (per the brief: "if in the course of the
work that distinction stops being clean, stop and escalate")

It did not stop being clean. Nothing built here measures `HL_S2_nonsymmetric`, touches
`ℓ¹_w`/collocation/origin-H², or poses a fluid transport model. The only operator touched
is a pure integer/`Fraction` decode-and-compare over a third party's already-published,
already-refereed data. Full statement in `TECHNICAL_P2_ROUTEDFRE_V1.md` §7.

## Integration notes (things I could not do — outside declared territory)

1. **`writeup/INDEX.md`**: needs a `fig71` row (Route-DFRE, leg 316). Not edited here per
   the brief's explicit instruction (shared rebase-collision point; orchestrator's job).
2. **`writeup/build_figures.py`**: the `P2_EVIDENCE` list (module-level, shared across
   legs) should get one more line:
   `"../experiments/p2_route_dfre_v1_evidence.py",  # fig71 -- Route-DFRE v1 (leg 316)`
   — not edited here; this file is a second shared rebase-collision point structurally
   identical to `INDEX.md` (a single ordered list every landing leg would otherwise
   append to), so it was treated the same way even though the brief only names
   `INDEX.md` explicitly. If the orchestrator judges this file is in fact fair game for
   legs to touch directly, that's a correction for next time, not a contradiction here.
3. **`experiments/JOURNAL.md` / `LITERATURE_CHECK.md` / `PHASE2_P2_NOTES.md`**:
   integration-owned; would want a pointer to this leg (in `LITERATURE_CHECK.md`'s ledger,
   analogous to the `2410.05480` row already there for leg 48 — this leg's row should note
   the package was independently checked at the raw-data level, YES, no defect found,
   distinct from leg 48's equation-level float64 re-derivation).

## Standing disciplines exercised

75 (arithmetic model stated before numbers), 84 (window pre-committed, not moved), 86 (the
decoder is checked against an independent reference before being trusted), 91 (realization
named precisely: Case I j=1, three segments, exact arithmetic, one pinned commit — not
"the package" unqualified). New performance rule (2026-08-11, mid-leg): estimate-then-run,
recorded above.
