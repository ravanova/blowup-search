# Leg 229 — Route-PNRV: independent post-repair verification of leg 226's `profile_newton.py` repair

**Date** 2026-08-11 · **Role** LEG · **Branch** `leg/229-pnrv-v1` · **Difficulty** heavy

**Gate (pre-committed, `DIRECTION.md` "### 229").** Does an independent re-run of leg 226's
repaired `profile_newton.py`, applied fresh to Route-D v11's own inputs, reproduce leg 226's
own reported before/after `a_max_machine`/`GA_boundary` values (whether unchanged or
changed), at its own resolution, including the three-grid `a=1.50` disagreement?
**yes** → independently confirmed either way; do *not* apply the headline correction under
this leg's own authority, escalate it. **no** → report the exact discrepancy as the
highest-priority finding in the entire backlog.

## Answer: **PENDING — filled on completion of the measurement run.**

---

## 0. Staleness re-check of the committed novelty pass (required before construction)

The novelty pass (`writeup/novelty/leg_229.md`, commit `0954a86`, rebased to `f3a638d`) was
written when `main` was at `5c86a18`. Seventeen commits landed since. Re-checked against all
of them, item by item, rather than trusted:

* `git diff --name-only 5c86a18 origin/main` touches 24 files. **Neither
  `solver/profile_newton.py` nor `experiments/p2_route_d_v11_anchor.py` is among them**, and
  neither is `writeup/data/p2_route_d_v11_anchor.json`. The two objects this leg measures and
  the banked artifact it compares against are byte-unchanged.
* Leg 284 (Route-NU12) landed a `nu(1/2)` grid-convergence negative and re-pinned `a*` as a
  turning point at `0.3857 ± 0.004`. Different quantity, different solver file
  (`experiments/p2_route_nu12_v1_converge.py`); does not touch the `profile_newton` verdict.
* Leg 296 (Route-ASR) reconciled `a*` across legs 125/210/283/284 — append-only pointer
  blocks, re-measured nothing.
* Leg 295 (Route-PUB3P) closed PUB3's stale `a_max_machine` exposure row with a dated
  footnote citing legs 236/226/294. This is the closest hit and it **strengthens** the case
  for this leg rather than duplicating it: leg 295 propagated the corrected value `0.55` into
  prose *on the authority of legs 236/226*, without re-executing anything. If leg 226's
  re-derivation were wrong, leg 295 would have propagated the error further. Leg 295 also
  recorded that it deliberately does **not** touch the still-stale anchor JSON on `main`.
* Leg 297 (Route-D11ANCHOR) is parked, not landed — the anchor-JSON integration decision is
  prepared but held for the user. Nothing of it is on `main`.
* The remaining commits are orchestration/infrastructure (`ORCHESTRATION.md` §7b/§9g
  liveness rules, `reports/`, the headroom tooling).

**Verdict: the novelty pass is NOT stale.** Its central claim — that no leg has imported and
executed the repaired `profile_newton.py` fresh off leg 226's branch and independently
recomputed the four headlines plus the `a=1.50` case — still holds after every landed commit.
The plan it pre-committed is the plan executed here, with one addition recorded in §5.

Bans re-read via `.venv/bin/python plan_of_record.py`: none is contacted. This leg runs no
GA, proposes no new function space, re-measures no `beta`, re-opens no DSS entrance, and
builds no solver — it re-runs an existing one. `capabilities.py` grepped before building:
`solver/profile_newton.py` is registered at line 431 with `test_profile_newton.py` as its
test, so this leg's file is named `test_profile_newton_postrepair.py`, the repository's
existing convention for a post-repair suite alongside an existing one.

---

## 1. What was actually re-derived, and how independence was enforced

Leg 226's repair is **not on `main`**. It lives on `leg/226-pnr-v1-resume` at `d93b51f`,
which this leg reads with `git show` and **never checks out and never edits**;
`solver/profile_newton.py` in this worktree is untouched, as is every Route-D v11 artifact.

Independence was enforced at four separate points, because "re-ran it and got the same
answer" is worth nothing if the second run inherits the first run's inputs:

1. **The module** is fetched from leg 226's branch and imported under a private name. The
   pre-repair reference is fetched the same way at leg 226's own pin `6a17ce6`, and the
   runner **asserts it is byte-identical to the module on `main`** before using it — so
   "pre-repair" means what the repository actually ships, not a private variant. (Asserted,
   and true.)
2. **v11's test definitions** are extracted from `experiments/p2_route_d_v11_anchor.py` **by
   parsing its AST**, not transcribed from leg 226's summary: the 17-point V2 `a`-ladder, the
   V3 `np.round(np.arange(0.44, 0.76, 0.02), 3)` ladder, `N = 801`, V4's
   `ns = (401, 801, 1601)` and `a ∈ (0, 0.2, 0.5, 0.8, 1.0)`, and the `GA_boundary` literal.
   The five threshold *expressions* (`r["relres"] < 1e-8`, `max(cs) - min(cs) < 1e-3`,
   `n_ok >= 2`, and the two lines that fix V4's spread convention) are asserted present
   verbatim in v11's source, so a change of shape raises instead of silently falling back on
   a remembered value.
3. **Leg 226's reported numbers** are read from its banked JSON via `git show` at run time.
   Not one of them is typed into this leg's code.
4. **Leg 226's runner is not imported.** The measurement is re-implemented from v11's
   definitions. Exactly one thing is taken from that runner, and it is a *protocol* input
   rather than a number: the `a`-ladder its `a = 1.50` continuation walks, which is leg 226's
   own choice and must match for the comparison to mean anything. It is re-derived here as
   `np.arange(0.0, 1.51, 0.15)` and **asserted equal** to leg 226's `LADDER_A` literal
   (asserted, and true).

---

## 2. Result, clause (b): the three `a = 1.50` grids — reproduced to relative error **0.0**

The clause the gate names explicitly. Continuation on leg 226's ladder at `n = 101/201/301`,
run against both modules in this leg's own process:

| `n` | pre-repair `c` (mine) | leg 226's | rel. err | post-repair | `D3` | rel. err |
|---|---|---|---|---|---|---|
| 101 | 0.20426539933526314 | 0.20426539933526314 | **0.0** | REJECTED | 1.120415554e+06 | **0.0** |
| 201 | 0.23717365354680944 | 0.23717365354680944 | **0.0** | REJECTED | 6.255599072e+05 | **0.0** |
| 301 | 0.9728186873524793  | 0.9728186873524793  | **0.0** | REJECTED | 4.856745946e+03 | **0.0** |

Pre-repair relative spread across the three grids: **1.6302967970104654**, against leg 226's
**1.6302967970104654** — identical to all 17 significant figures, relative error `0.0`.
Grids accepted post-repair: **0 of 3**, matching leg 226's 0 of 3, each rejected with the
decay-class reason string. Three grids that pre-repair all reported `converged` at speeds
disagreeing by 163% are all rejected, and the leg does **not** dress that up as convergence
to a common value.

## 3. The licence for both legs' measurement shortcut, checked at four points not one

v11's V3 and V4 call only cold `solve()`, and both legs read pre-repair verdicts off
post-repair runs. That is valid only if the repair left the Newton iteration untouched. Leg
226 verified this at **one** point (`a = 0.5, n = 801`). This leg verified it at **four**,
spanning all three grids — `(a, n) = (0.2, 401), (0.5, 801), (0.72, 801), (0.0, 1601)` —
with `c_delta = 0.0`, `relres_delta = 0.0` and identical iteration counts at every one.
Exact equality, not a tolerance: agreement to `1e-12` would be a different and much weaker
statement.

## 4. Results, clauses (a) and (c): the four headline numbers

**PENDING — filled from the completed run.**

## 5. A property of the shared substrate, found by this leg's own control

Not part of the gate, recorded because it was found by a control that was supposed to be
routine and was not.

Building the D2 (gauge) control required a profile D2 accepts. The obvious candidate — the
exact `a = 0` anchor `-1/(1+X²)` — **fails D2 by 2.033109e-03**, three decades above
`GAUGE_TOL = 1e-6`. The mechanism, measured rather than argued:

* gauge 2 pins `Omega = -1/2` at `i1 = argmin|X - 1|`, and that node is at
  `X = 0.9959420145786071`, not at `X = 1`;
* the rho-grids are nested and **no refinement moves it**: `X[i1]` is bit-identical at
  `n = 101, 201, 401, 801, 1601` while `drho` falls by a factor of 16;
* so the anchor's gauge-2 miss is **the same 2.033109e-03 at every resolution**, and the
  discrete `a = 0` traveling wave is a slightly narrower profile than the continuum anchor,
  travelling at `X[i1]/2 = 0.49797100728930355` rather than at `1/2`.

That predicted speed is what v11's own V4 rows report at `a = 0` (`0.4979712` at `n = 401`,
`0.4979710` at `n = 801`); the per-grid relative errors against `X[i1]/2` are banked in the
JSON's `m6_width_gauge_substrate`.

Five identical numbers across a refinement ladder are normally the tell for a control that
cannot come out differently (**lesson 90**), which is why this was chased rather than
reported. Here the grid-independence *is* the finding, and it is asserted executably in
`test_profile_newton_postrepair.py` so it cannot decay at the rate of memory (**lesson 68**).

**This is a property of `solver/gclm_family`'s grid and of v11's gauge choice. It is NOT a
defect of leg 226's repair, it does not move this leg's gate, and this leg changes nothing on
account of it.** D2 is correctly scoped as a test of what `solve` *returns* — Newton drives
the gauge rows, and the returned solution's gauge residual is `0.000e+00`.

Recorded alongside it, since it is the same kind of scope statement: **D3 is one-sided.** It
rejects far fields fatter than the anchor's and never thinner ones — by design, since leg
226's own reasoning (via Route-D v12 T3) is that the genuine branch decays *faster* than the
anchor. The magnitudes it lets through are banked rather than left implicit: an accepted row
at `a = 0.2, n = 201` carries `D3 = 3.59e-06`, nearly six decades *below* the anchor.

## 6. Negative controls that can fail

Per the standing discipline, a positive result needs negative controls that can fail. The
banked suite (`test_profile_newton_postrepair.py`, 9/9 passing in seconds) carries them:

* **D3's control has distinct rungs.** Leg 226's own journal records that its first
  falsification probe was *vacuous* — it added a constant tail and then re-imposed the
  amplitude gauge, which subtracted exactly the constant it had added, so every rung returned
  `D3 = 1.000` and `all_must_fail` was silently `False`. This leg's version multiplies the
  outer half of the grid, leaves the gauge nodes alone, and **asserts the rungs are distinct
  before asserting they straddle the threshold**: `[1, 10, 1e3, 1e5]`, 2 accept / 2 reject.
* **The repair is not a blanket rejector.** A verdict that rejected everything would
  reproduce leg 226's headline for the wrong reason. The repaired verdict still accepts the
  genuine branch at `a = 0.0` and `a = 0.2`.
* **`GA_boundary` cannot move, and that is asserted structurally rather than observed.**
  Reporting "unchanged by the repair" without saying why would be exactly lesson 90's error.
  It is a literal `[0.5, 0.55]` in v11's V3 return dict (confirmed on v11's AST), and the
  string `GA_boundary` appears in **neither** module — pre-repair or repaired. No repair to
  `profile_newton.py` can move it.
* **v11's own spread convention is pinned as distinct from leg 226's.** v11's V4 computes
  `c_spread` over **all** grids and the count over the converged ones; leg 226 took both over
  the accepted rows. These are different statistics, so this leg reports **both** — see §4.
  Reporting one of them silently would be reporting a convention as a measurement.

## 7. Documentation quartet — and one deviation, stated

Runner `experiments/p2_route_pnrv_v1_postrepair.py`; curated JSON
`writeup/data/p2_route_pnrv_v1_postrepair.json` (every number quoted above and in §4 is in
it); banked executable suite `test_profile_newton_postrepair.py`.

**Deviation, declared rather than taken silently:** this leg's file territory
(`DIRECTION.md` "### 229", the verification-leg pattern shared with legs 228/230) grants
**five paths and no `writeup/` prose or figure path**. BLOG/TECHNICAL prose and a registered
figure therefore cannot be written without a diff outside territory, which fails the merge
gate. The prose is carried in this journal (§2–§6 are the technical account; §8 is the blog-
voice one) and the figure is the text table in §2/§4, which is the artifact a `fig` would
render. Territory was **not** widened unilaterally; folding this into the writeup tree with a
registered figure number is an integration action, and is flagged for the orchestrator.

## 8. In plain terms

**PENDING — filled on completion.**

## 9. Clay

Unchanged, **~0.05%**. Nothing here touches a link of the L1→L4 chain: this leg re-derives a
verdict inside a float Newton solver on the gCLM two-scale profile. No certificate, no
interval arithmetic, nothing rigorous.
