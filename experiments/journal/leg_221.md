# Leg 221 — Route-BVRR: repairing `boussinesq_rescaled.py`'s two fabrication mechanisms, with this leg's own zero-contamination re-confirmation

**Date: 2026-08-07. Kind: module repair + contamination re-confirmation — standard. No figure,
no new physics, and none claimed.** Runs under the "no measurement, no figure" convention
`writeup/INDEX.md` records for the audit / repair family; the quartet is runner + curated JSON
+ novelty pass + this journal.

**This leg was resumed twice.** The first session (external interruption) landed the repair
commit `6a0abb8` and the novelty pass `bd15606` and ran clause (b) only partially. The second
(2026-08-07) re-verified the repair and found one overclaim in it (§4). This third session
(2026-08-11) ran the clause-(b) sweep to completion and, in doing so, measured something none
of the earlier sessions had reached — see §2a, which is why this leg is **parked, not landed**.
Nothing inherited was taken on trust at any stage.

## Gate, verbatim (`DIRECTION.md` leg 221, pre-committed, both branches)

> Does repairing both named mechanisms cause every adversarial case in leg 205's own battery to
> now reject/raise correctly, AND does a fresh, explicit re-run of every banked result that
> calls `boussinesq_rescaled.py` (not just an assumption of dormancy) confirm zero
> contamination, bit-identical pre/post repair?

## Answer: **YES on both clauses as the gate words them — and the leg is ESCALATED anyway.**

Clause (a): 18 silent fabrications → 0. Clause (b): every `odd_field_x_slope` call in every
banked run agrees bitwise pre/post repair, and every artifact leaf that moved has been
**attributed by a control in which the repair is absent from the process**. But that control
is what turned the sweep into something bigger than a repair verification: **one banked
artifact no longer reproduces at all, with or without this repair**, and two of its banked
predicate checks flip `True → False` (§2a). That is a finding of a different order, it is not
this leg's to rule on, and folding it into a quiet repair landing is exactly what the gate's
no-branch forbids. **Branch pushed; `main` untouched.**

---

## 1. Clause (a) — leg 205's own 81-case battery, measured live on both sides

The battery is **leg 205's own file**, read out of `origin/leg/205-bvr-v1` and executed twice
in one process: once with `solver.boussinesq_rescaled` bound to the pre-repair module loaded
from `1a3e63c`, once with the repaired module. The "before" column is therefore **measured, not
quoted**, and the run asserts it reproduces leg 205's committed verdict totals or aborts.

| | OK | SILENT_WRONG | RAISED | NONFINITE | NO_REFERENT | RETURNED |
|---|---|---|---|---|---|---|
| leg 205, committed | 32 | 18 | 11 | 9 | 2 | 9 |
| pre-repair, re-measured here | 32 | **18** | 11 | 9 | 2 | 9 |
| post-repair | 42 | **0** | 19 | 9 | 2 | 9 |

**18 → 0 SILENT_WRONG.** Of the 18: **13 now return the correct value** (the repair *recovers*,
it does not merely refuse) and **5 now raise**. The battery's own gate answer flips YES → NO,
which is the battery reporting that it can no longer find a silent fabrication.

**Lesson-90 control, asserted before any "identical" count is believed.** The two module
objects must be distinguishable and must disagree by a *measured* amount, or the differential
is worthless. On leg 205's headline defect-B row (`lam=400`, truth `2.0`): pre `+0.269302`
(rel. err **8.6535e-1**), post `+2.000776` (rel. err **3.8798e-4**) — inside the module's own
`5e-4` acceptance tolerance. On defect A's empty window: pre returns exactly `0.0` against a
truth of `2.0`; post raises. The run aborts if any of these nine control checks passes
trivially.

## 2. Clause (b) — every banked result, re-run; the clause the prior session did not reach

The prior session ran this clause with `--skip-slow`, covering **2 of 6** banked artifacts, and
still reported `zero_contamination: true`. That is the same shape of error leg 205 was faulted
for — an *assumption* of dormancy standing in for a measurement — one level up. Two changes
were made before re-running:

* **`zero_contamination` now requires `len(runs) == len(BANKED)`.** A partial sweep can no
  longer report zero contamination however clean the artifacts it did run. `--skip-slow` is a
  development flag only.
* **Provenance leaves are classified, not dropped.** See §3.

The census is by `grep` over the whole repository, not by assumption: **13 files reach the
module; 6 banked artifacts, 2 unbanked (print-only) callers, 3 test suites.**

Method, per artifact: the generating script is run end to end with `odd_field_x_slope` replaced
by a shim that calls the pre- and post-repair implementations on **identical arguments**,
compares bitwise, and **returns the pre-repair value** — so the trajectory followed is exactly
the banked one. If every call agrees bitwise, the repaired module reproduces the whole run by
determinism. This is a proof over the actual call sequence, not a sample. The regenerated
artifact is then compared leaf-by-leaf against the committed one as an independent second
check, and the committed artifact is restored afterwards.

CLAUSE_B_TABLE_PLACEHOLDER

## 2a. The finding that parks this leg: a banked artifact that no longer reproduces

`writeup/data/spike1_stepC_gate.json` re-ran with **all 16008 `odd_field_x_slope` calls
bit-identical** pre/post repair — and **525 of its 1028 comparable leaves moved anyway.**

A leaf comparison alone cannot say whose movement that is. The sweep's regenerated artifact is
produced along the *pre-repair* trajectory (the shim returns the pre-repair value), so a
difference against the committed file is consistent with two completely different worlds: the
repair moved it, or the banked artifact does not reproduce in today's environment. Reporting
either without separating them would be the same error one level up that this leg exists to
close. So the runner now runs three **plain** regenerations, with no shim in the process:

| regeneration | module | question it answers |
|---|---|---|
| A | loaded from `1a3e63c`, the repair **absent** | — |
| A2 | same again | is the script deterministic today? |
| B | the repaired module | `diff(A,B)` = the gate's actual "bit-identical pre/post repair" |

`diff(committed, A)` is then **baseline drift** — movement the repair cannot have caused,
because the repair is not in the process.

**The control's verdict, measured — `BASELINE_DRIFT_NOT_THIS_LEG`:**

| measurement | result | what it settles |
|---|---|---|
| `diff(A, B)` — pre- vs post-repair | **0 leaves moved** | the gate's clause (b), directly: the repaired module reproduces the artifact exactly |
| `diff(A, A2)` — determinism today | **0 leaves moved** | the script IS deterministic now, so the movement is not run-to-run noise |
| `diff(committed, A)` — baseline drift | **525 leaves moved** | the committed artifact is the odd one out |

The determinism row is what makes the other two mean anything: without it, `diff(A,B) = 0`
could be luck and `diff(committed,A) = 525` could be jitter. Both alternatives are now measured
false. The 525 was also reproduced independently three times — an isolated hand-run, and two
runner passes whose differential legs took 403 s and 1086 s of wall clock under different
machine loads and returned **16008/16008 bit-identical calls and the same 525 leaves each
time**. What moved:

| leaf | committed | regenerated, repair absent | rel. |
|---|---|---|---|
| `predicate_checks.1_alpha_within_5pct` | **True** | **False** | — |
| `predicate_checks.4_resolution_stable_alpha` | **True** | **False** | — |
| `runs[0].alpha` | −0.3350763 | −0.3793564 | 1.321e-1 |
| `runs[0].alpha_far` | −0.3225619 | −0.4561381 | 4.141e-1 |
| `runs[0].c_omega` | −1.0265649 | −1.1622246 | 1.321e-1 |
| `runs[0].c_l` | 3.0636748831963683 | 3.0636748831963687 | 1.450e-16 |
| `runs[0].cut_omega[0]` | −1.5479e-3 | +1.4671e-4 | 1.095e+0 |

Moved-leaf relative differences run from **1.45e-16 to 4.47e+0**, median **2.04e-1** — so this
is not a round-off story with two round-off leaves in it; it is a genuinely different answer
with a few leaves that happen to agree to machine precision. `spike1_stepC_gate.json` was
banked **2026-07-25**; `requirements.txt` pins numpy only as `numpy>=2.0` and the environment
now carries **numpy 2.5.1**, which is the leading candidate and is *named as a candidate, not
asserted as the cause* — establishing it is a separate leg's work, not this one's.

**Why this parks the leg rather than landing with it.** The gate's no-branch says a banked
result that is reachable and does move is "a priority finding of a different order — escalate
immediately, do not fold it quietly into the repair's own landing." The strict reading is that
clause (b) asks about *pre/post repair*, and pre/post repair is clean. But the remedy for what
was found — a banked artifact whose committed predicate verdicts are wrong in today's
environment — is to **rewrite a banked result in `writeup/`**, which is the fourth of
`ORCHESTRATION.md` §8's four escalations verbatim, and is never merged without the user. This
leg neither rewrites it nor lands on top of it: the committed artifact is restored byte-for-byte
after every regeneration (`git status` clean on `writeup/data/` throughout), the branch is
pushed, and `main` is untouched.

## 2b. Flake-diagnosis before belief, applied twice

`ORCHESTRATION.md` §9g: a failure observed while many worktrees are gating at once is a flake
until it survives an isolated re-run. Both of this leg's alarming observations were put through
it, and they answered differently in the way that matters.

* **The inherited two-scale counterexample (43.2% error, 86x tolerance).** Re-run alone, under
  **load average 0.61**, in this leg's own worktree: **five independent evaluations returned
  the identical float**, `+1.135121` against a truth of 2.0 (rel. err **4.3244e-1**), and the
  wider-separation case returned `+1.006197` (**4.9690e-1**). Not a flake — deterministic, and
  it reproduces exactly the inherited magnitude. **But it is not clause-(b) contamination
  either**, and the earlier session's framing of it as residue survives scrutiny: it is a
  synthetic field, not a banked one. Measured on the actual banked fields, Step-C's
  `profile_ansatz` omega and eta each carry **one** interior maximum of the projected `d1`
  above 1% of peak, at `r_peak` 2.243 and 1.231, so the window cap is **non-binding** on both
  and the two-scale mechanism never engages on anything banked. The counterexample stays what
  §4 called it: banked residue with its magnitude, handed on.
* **The 525-leaf artifact movement.** Also not a flake, and here the isolated re-run *raised*
  the finding rather than dissolving it, which is the point of running the diagnosis instead of
  guessing which way it will go.

## 2c. An availability defect in this leg's own apparatus, found the hard way

The first complete sweep was killed by the harness **40 minutes in**, after
`spike1_stepC_gate`'s 403-second run had already succeeded, and **every completed artifact was
lost** — clause (b) wrote its JSON only at the end. That is a real defect in the verification
apparatus, not bad luck: a check that costs hours and cannot be resumed will eventually be run
partially and reported as if whole, which is precisely the failure mode §2 fixed one level up.
Clause (b) now **checkpoints each artifact to the JSON as it lands** and takes `--keys` to
re-run a subset, merging by key; `covers_every_banked_artifact` still requires all six, so
resumability cannot weaken the gate. Recorded because the next leg with an hours-long sweep
will otherwise pay for this twice.

## 3. What the full sweep found that the partial one could not: a date-stamped leaf

`p2_route_brs_v1_status_audit.json` carries `"generated": "2026-08-06"`. The prior session ran
on 2026-08-06, so that leaf matched by coincidence. Re-running on 2026-08-07 moves it — and the
comparator, which excluded wall-clock keys but not date keys, scored it as **contamination** on
an artifact whose 62 `odd_field_x_slope` calls were all bit-identical and whose 450 real leaves
were all identical.

**A same-day-only differential is not a differential.** Fixed by classifying `generated`,
`date`, `timestamp` and their kin into a `provenance_moved` channel that is **reported with its
before/after values and not counted** toward contamination — not silently dropped. Recorded
here because it is exactly the kind of false positive a later leg would otherwise re-derive,
and because a comparator that only works on the day the baseline was written is a defect in the
verification apparatus, not in the module.

## 4. Verifying the inherited repair — and one overclaim found and corrected

The repair was re-derived against leg 205's escalation rather than accepted. Both mechanisms
are addressed, and only those; the diff touches `odd_field_x_slope` and nothing else.

An **independent probe on shapes leg 205's battery does not contain** confirmed the repair
recovers across five decades of concentration (`lam` = 4 … 1e4, all at rel. err ≈ **3.9e-4**,
inside tolerance), is amplitude- and sign-independent, handles the Step-C rational envelope
(**3.5e-4**), propagates NaN visibly, and refuses empty windows and precondition-violating
constant fields.

**But one row failed, and it is a real residual gap.** A **two-scale** field,
`r cos(b) [exp(-r²) + exp(-400 r²)]` — smooth, odd-in-x, fully in contract — is read as
**1.135121 against a truth of 2.0: 43.2% error, 86x the module's own tolerance, silently.**
Mechanism: the window cap reads the field's scale off the radius at which `d1` **peaks**, and
for a two-scale field the peak resolves to the **outer** scale (deliberately — resolving inward
would shrink the window on fields with no small scale, a fabrication of the opposite sign), so
the cap goes non-binding and the inner scale is never resolved. The residual backstop is then
the only guard left, and it does not fire.

**Where the backstop actually sits, measured through the public API** (by bisecting
`max_rel_residual` to locate the residual — no re-implementation of the fit):

| field | returned | rel. err | rel. residual |
|---|---|---|---|
| module's own validated field, `lam=1` | +2.000940 | 4.698e-4 | 1.198e-5 |
| Step-C `profile_ansatz` omega (every banked relaxation runs on this) | +0.975919 | — | **4.301e-3** |
| Step-C `profile_ansatz` eta | +1.494255 | — | 1.311e-3 |
| leg 205's headline defect-B field, `lam=400` | +2.000776 | 3.880e-4 | 4.632e-5 |
| **two-scale, `exp(-r²)+exp(-400 r²)`** | **+1.135121** | **4.324e-1** | **8.125e-2** |
| **two-scale, `exp(-r²)+exp(-1e4 r²)`** | **+1.006197** | **4.969e-1** | **1.910e-2** |

Worst legitimate residual **4.301e-3**; tightest uncaught bad case **1.910e-2**; **separation
4.44x**. The landed threshold is **0.50** — roughly 26x above the tightest bad case, so it
cannot fire on either. A threshold anywhere in `(4.3e-3, 1.9e-2)` would catch both.

**This was not tuned in.** Changing the threshold is a behaviour change this leg's gate did not
scope — clause (b)'s bit-identical sweep was run at 0.5, and a new threshold would invalidate
it and risk raising on legitimate banked calls, which would be a regression against the gate's
own zero-clean-movement driver. So the gap is **banked with its magnitude and handed to the
postrepair-verification leg**, which is the flag the gate's yes-branch already calls for.

What *was* corrected: the inherited docstring claimed the residual was "a fit-quality backstop
for shapes the peak heuristic does not describe." That is an overclaim — measured false by the
two rows above. The docstring now states the heuristic is exact for the single-scale class and
**not claimed beyond it**, names the two-scale counterexample with its magnitude, and calls the
backstop partial. Prose only; no behaviour change, so clause (b) remains valid as run.

## 5. Hazard for whoever lands leg 205's branch

Leg 205 banked `test_boussinesq_rescaled_adversarial.py` as a **characterization** suite that
passes against the *unpatched* module and pins both defects with their magnitudes. Its
docstring names four tests whose expectations must flip **in the same commit as any fix**. That
file lives only on `origin/leg/205-bvr-v1`; it is **not on `main`**, and it is **outside leg
221's declared territory**, so this leg cannot flip it. **If leg 205's branch is landed after
this repair, those four characterization tests will fail** — correctly, because the behaviour
they characterize is now repaired. Flagged, not fixed: the four expectations belong to whoever
merges 205.

## 6. What this leg does NOT claim

No new mathematics, no bound, no movement on any link of the `L1→L4` chain, no statement about
the Chen–Hou constants. Zero banked numbers move, and that is asserted here as a **measurement
over every banked artifact**, not as an expectation from the shape of the envelopes — which is
the entire difference between this leg and leg 205's honest "flagging, not claiming."

## Files

* `solver/boussinesq_rescaled.py` — the two guards, in `odd_field_x_slope` only.
* `experiments/p2_route_bvrr_v1_repair.py` — the runner: both clauses, the lesson-90 control,
  the residual-margin probe.
* `writeup/data/p2_route_bvrr_v1_repair.json` — every case, every call, every leaf.
* `writeup/novelty/leg_221.md` — novelty pass, committed before construction (`bd15606`).

## Process notes

* `plan_of_record.py` run first; no ban in force touches this route.
* `capabilities.py` grepped before anything was written; untouched.
* Five shared ledgers untouched, as is `writeup/INDEX.md` — integration-owned.
