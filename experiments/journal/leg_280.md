# Leg 280 — ROUTE-PUB2X: the user-gated eighth correction pass to PUB2 — four sites corrected

**Gate: YES on all five clauses.** (a) all four sites corrected per leg 277's and leg 281's
verified findings, each with a dated note; (b) re-confirmed no other TECHNICAL site carries any
of the four defects — `TECHNICAL_P2_PUB2_V1.md` §3.2's `0.0420` mention is a backreference to
§0's convention note by the document's own design and needed no separate edit; (c)
`writeup/CORRECTIONS.md` §13 (leg 277's finding) and §14 (leg 281's two findings) appended, plus
two new register rows; (d) diffed against `origin/main` with `difflib.SequenceMatcher`: exactly
4 replace hunks, 7 of 722 old lines touched, everything else byte-identical — verified, not
asserted; (e) zero arguments changed — leg 176's gate, leg 249's verification and the
flatten-vs-decay contrast §§3–5 argue from are all untouched.

Documentation-correctness leg, no compute. Branch `leg/280-pub2x-v1`, merge base `5e30bf3`
(`origin/main` at dispatch). Date 2026-08-11.

Novelty log (committed `70ef0ea`, before any edit):
[`writeup/novelty/leg_280.md`](../../writeup/novelty/leg_280.md).

---

## 1. The four sites, before/after

| site | before | after |
|---|---|---|
| §0 convention note (L56) | `0.0420` named as "Xu's own `y`-space normalization" with no qualifier | `0.0420` is Xu's *equivalent* full-line reading (`κ=2π`); Definition 4.1's own *displayed* half-line definition carries `π`, giving `0.057643` (`‖R‖_X=17.348`), `1.37×` larger — leg 277 |
| §7 Ceiling (L699→712) | same unqualified `0.0420` restated | same correction restated, pointing to §0 |
| §4.5 (L442–446→451–458) | "any claim that `0.0908` and `0.71465` are convention-independent" [does not survive] | corrected to `0.0908` only — `0.71465` **is** convention-free to `1.87e−16` (leg 281 E5) |
| §4.5 (L420→426) | "optimistic by 7.9×", no caveat | flagged convention-relative, range `5.265…656.95` (`124.8×`) — leg 281 E6 |

## 2. How the four sites were located

Leg 281's own §8 (drafted, undispatched, pointer at this file's precursor) already named all
four with line locators from its own draft state; the live file's line numbers had shifted
slightly since (post-leg-276 edits), located this pass by `grep` against the current text and
cross-checked against leg 281's quoted surrounding prose, not against its stale line numbers
alone.

## 3. Clause (b), the sweep

`grep -n "0.0420\|optimistic\|convention-independent\|convention-relative"` over
`TECHNICAL_P2_PUB2_V1.md` post-edit: every remaining hit either carries the correction directly
or is a backreference to a corrected paragraph (§0, §7, or "§0's convention note"). One sibling
site found **outside declared territory**: `writeup/4_p2_lottery/BLOG_P2_PUB2_V1.md` prints the
same `0.0420` digit at its own convention-note equivalent (L102) — not edited (the dispatch
names "the four named TECHNICAL sites" only), flagged in `CORRECTIONS.md` §13 for whoever holds
that path.

## 4. Clause (d), the trace

No dedicated 122-row trace runner exists in the repository (leg 276's "122 rows" was a manual
re-check, not a saved script). Verified the equivalent guarantee directly: `difflib.SequenceMatcher`
between the pre-edit and post-edit `TECHNICAL_P2_PUB2_V1.md` reports exactly 4 `replace` opcodes
at the four sites above and 0 `insert`/`delete` elsewhere; 7 of the file's 722 original lines
were touched, all inside those 4 hunks. `writeup/CORRECTIONS.md`'s diff is **112 insertions, 0
deletions** (`git diff --stat`) — pure append, one register row insertion (precedented by every
prior register entry, e.g. leg 295/296) plus two new `## §N` sections at the end.

## 5. Territory

The four named TECHNICAL sites in `writeup/4_p2_lottery/TECHNICAL_P2_PUB2_V1.md`,
`writeup/CORRECTIONS.md` (append only), `writeup/novelty/leg_280.md`, this file — exactly what
DIRECTION.md §280 declares. Leg 277's and leg 281's reports/JSON read, never edited.
`writeup/4_p2_lottery/BLOG_P2_PUB2_V1.md` **not edited** — flagged in CORRECTIONS.md §13 instead
(see §3 above). No shared ledger touched: `experiments/JOURNAL.md`, `LITERATURE_CHECK.md`,
`plan_of_record.py`, `CONTINUATION_PROMPT.md`, `PHASE2_P2_NOTES.md` unmodified. `capabilities.py`
unmodified — no `solver/` module added. No GA compute, nothing measured, both corrections reuse
leg 277's and leg 281's already-banked numbers rather than re-deriving them.

## 6. Integration note for the orchestrator

`writeup/4_p2_lottery/BLOG_P2_PUB2_V1.md:102,108` carries the same `0.0420` digit and a
`0.71465` cross-reference this leg could not touch (out of territory). `CORRECTIONS.md` §13
names the site and the fix a future leg should make; no urgency — it does not touch a banked
gate property, same as the TECHNICAL sites did not.

## 7. Ceiling

Correcting four printed digits/directions does not lift any ceiling attached to them. `a = 0`
only, float64, nothing interval-enclosed beyond what legs 249/277/281 already banked. Leg 176's
gate answer, leg 249's verification, leg 182's `NO` are all untouched. **No link of the
`L1 → L4` chain moved. Clay odds unchanged at ~0.05 %. No ban lifted. No route promoted. No GA
compute.**

**PUB2 is re-closed from the record's side** (leg 278's phrasing) with these four corrections
folded in. Any ninth correction pass likewise needs the user's own sign-off, per leg 278 §4's
process rule.
