# Leg 276 — ROUTE-PUB2R3: all five item families from leg 270's trace, closed in one pass

**Gate: YES — the re-run trace shows 6 of 6 previously-failing sites green, 116 of 116
previously-exact printed values byte-unchanged, and 0 arguments changed.** Rework leg on a draft.
Branch `leg/276-pub2r3-v1`, created fresh from `origin/main` at `0ac83c2`. Date 2026-08-07.
**No GA compute. Nothing measured. 0 numbers invented** — every figure written into PUB2 was read
back out of a banked JSON or journal in this pass.

Full report: [`writeup/novelty/leg_276.md`](../../writeup/novelty/leg_276.md).
Register entry: [`writeup/CORRECTIONS.md`](../../writeup/CORRECTIONS.md) §6.

---

## 1. The answer, as magnitudes

| | |
|---|---|
| trace rows re-checked (superset of the 94 quantitative sites) | **122** |
| previously-MISMATCH sites now carrying what their source supports | **6 / 6** |
| previously-exact printed values still byte-identical | **116 / 116** |
| superseded strings confirmed absent | **8 / 8** |
| leg-163 claims re-read **from `main`** and reproducing | **23 / 23** |
| **arguments / conclusions / gate answers changed** | **0** |
| escalations | **0** |
| `convention` occurrences in PUB2: before → after | **0 → 21** |

## 2. The five families

**1 — the `σ_min` convention disclosure (`E1`).** Mechanism chosen **from the trace**: leg 270's
own repair sentence is *"one disclosing sentence at first use"*, and its 15 recorded sites are
six clusters, not fifteen locations. So: one prominent blockquote at §0 below the four-point
table (where `0.0908` is first printed), with a pointer back to it at each cluster — §3.2, §3.5's
ladder, §3.5's correction paragraphs, §4.5, §5(3), §7, and the blog's first use. Fifteen per-site
caveats were rejected because they would require editing lines carrying byte-exact banked figures,
risking this leg's own second gate clause for no gain. **Content, and the invariance is stated in
the same breath at every placement:** the border-amplitude weight swept over `10⁻²…10²` moves
`σ_min` across `0.01086…0.13580`, a factor of **12.5**; Xu's own `y`-space normalization gives
**0.0420** rather than `0.0908`; the **gate property** — bounded away from zero uniformly in the
truncation — **is invariant**, since a positive weight cannot send a positive limit to zero, and
PUB2 argues only from the ladder's *behaviour*, which is likewise invariant.

**2 — the four over-tight bounds**, loosened to exactly what the data supports:
`≤ 2.62e−03 → ≤ 2.63e−03` (banked `2.6248e−03`, over-tight by 0.19 %); *"at most 0.0090" → 0.00905*
(banked `0.00904587`, 0.51 %); *"`p`-blind to 0.009" → 0.00904* (banked `0.00903658`, 0.41 %);
`≤ 2.22e−14 → ≤ 2.221e−14` (banked `2.22012e−14`, 0.005 %). Each new bound was re-checked to hold
against its source. N2 and N3 now **agree exactly** with the sites 32 lines away that already
printed them correctly, so each quantity is stated one way.

**3 — the `0.71465` provenance chain.** The printed framing is what was wrong, not the number.
PUB2 printed a two-step chain (round the ratio to `1.3993`, invert, round up by `7.0e−06`); it
does not reproduce in either step. Verified this pass: leg 163's banked
`implied_sigma_min_lower_witness = 0.7146549471256172` is exactly `1/1.39927667753796`, the
**full-precision** ratio, and rounds to `0.71465` with no round-up, while `1/1.3993 = 0.7146430`
rounds to `0.71464`. Both sites now print the one-step chain. **One thing leg 270 did not flag:**
that five-decimal rounding is a round-**down** of `4.947e−06`, so `σ_min ≤ 0.71465` is marginally
tighter than the witness licenses — the N1–N4 class at a site leg 270 verdicted `MATCH`. The digit
was **not** moved (it is one of the 116 byte-exact values); §3.2 now states the witness bound
itself, `σ_min ≤ 0.71465495`, and names `0.71465` as the display form. **N6** in the same
paragraph: `0.868155` was seed 0's `M = 8192` value presented as "over four data" → **`0.8681539`**,
the four-data max, which is also what §3.5's own `7.88 %` row is derived from.

**4 — post-merge re-trace.** `git ls-tree -r origin/main` confirms leg 163's JSON and journal are
now on `main`. All **23 / 23** claims re-read from `main` this pass at the same values leg 270
read from the parked branch — the census, both symmetry-mode residuals, the three ratios, the
witness, the quadrature ladder, `α^{−3/2}`, the gate split. `P1` is closed by the merge; PUB2
needed no edit for it. `P2` and `P3` (same post-merge class) closed too: leg 249's three artifacts
are on `main` byte-identical to branch `e9db984`, which PUB2's three citation sites now say rather
than citing only a branch that is not an ancestor of `main`; and §3.5's quotation of leg 176's
`C1…reading` field now cites the leg 274 annotation beside it, as A1 requires. The correct half
was already quoted throughout — only the pointer was missing, and **0 defects were regenerated**.

**5 — the exit re-run**, mechanical, over all 122 rows after every edit. Result in §1. The one
reported non-hit is a tokenizer artifact (`99.80–99.99 %` parses as containing `-99.99` because
the en-dash reads as a minus); the site is present and unchanged, `grep -c` = 1.

## 3. C1–C5, which were not among the five families

Four closed, all pure wording, no number moved: **C2** (§3.5 now names §5's three-result opening
and §6's missing leg-176 row, leaving §5 standing as the three-result synthesis rather than
silently upgrading it), **C3** (the superseded `N = 1024` cause is now marked superseded in the
main text, with leg 249's explanation named operative), **C4** (the blog now keeps the leg 192 /
leg 249 distinction its technical companion keeps), **C5** (`185 legs` → `275 legs`, ~85 stale).

**C1 is left standing, and reported rather than repaired.** *"Seven successive legs"* against
*"roughly seventy"* is a 10× spread on a headline number at four sites, and leg 270 called it the
most visible defect to an outside reader. It is not in this leg's five families, and **no banked
artifact in this leg's read-set determines which figure is right** — PUB1 says `seven` at both of
its sites, but the two sentences may not be the same claim (legs that *attempted the certificate*
vs legs the *obstruction consumed*), and choosing would be changing a claim about the project's
own history on no source. **This is the one item a submission-approving reader should be handed
explicitly.**

## 4. Territory

`writeup/4_p2_lottery/TECHNICAL_P2_PUB2_V1.md`, `writeup/4_p2_lottery/BLOG_P2_PUB2_V1.md`,
`writeup/CORRECTIONS.md` (append: **67 insertions, 0 deletions**), `writeup/novelty/leg_276.md`,
this file — the five paths DIRECTION.md §276 declares, and nothing else. Leg 270's trace JSON,
leg 249's artifacts and leg 163's sources were **read, never edited**. No shared ledger touched:
`experiments/JOURNAL.md`, `LITERATURE_CHECK.md`, `plan_of_record.py`, `CONTINUATION_PROMPT.md`,
`PHASE2_P2_NOTES.md` unmodified. `capabilities.py` unmodified — no `solver/` module added.

## 5. Ceiling

Reworking a document does not lift the ceiling attached to it. `a = 0` only. What PUB2 reports is
still invertibility of an operator Xu already inverts in closed form, float64 and exact rationals,
nothing interval-enclosed. Leg 127's theorem, leg 163's census, leg 176's gate and leg 182's `NO`
are untouched. **No link of the `L1 → L4` chain moved. Clay odds unchanged at ~0.05 %. No ban
lifted. No route promoted. No GA compute.**

**Submission stays the user's action.** This leg reports only that the record's side of PUB2 now
contains no known unrepaired defect, with C1 named above as the single item left standing.
