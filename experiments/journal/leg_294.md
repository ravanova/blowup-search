# Leg 294 — Route-V11X: the consolidated Route-D v11 impact-trace

**Date** 2026-08-07 · **Role** LEG · **Branch** `leg/294-v11x-v1` · **Claim-bearing**
(synthesis of three parked branches; zero new measurement) · **Difficulty** standard

> **Gate (pre-committed, verbatim, DIRECTION.md "### 294").** Does the consolidated report
> (a) re-base every leg 236 number against leg 252's regenerated anchor values, stating the
> ~6.55e+11x stale-vs-regenerated gap on the corrected margin field explicitly; (b) carry an
> explicit provenance tier (banked-stale / banked-regenerated / fresh-resolve) on every
> compared value, no exceptions; (c) state leg 226's independent confirmation of
> `a_max_machine` (two methods, one number) AND the opposite-direction behavior of
> `grid_converged_a_max` as two separate, correctly-signed findings; (d) record that D1 is
> falsified and D3 is the real separator, correcting the record wherever D1 was cited as
> mechanism; (e) treat any "moved" claim as requiring movement beyond leg 252's own measured
> noise floor before calling it real; (f) restate leg 247's exoneration in the same document;
> (g) synthesize only, zero new re-measurement?

## Answer: **YES on all seven clauses.** Banked as the final Route-D v11 impact-trace.

Consolidated report: `writeup/data/p2_route_v11x_v1_consolidated.json`.

## What this leg found, by clause

**(a) Re-based.** Leg 236's numbers are read against leg 252's regenerated anchor
(`p2_route_d_v11_anchor.json` as regenerated on `leg/252-vbrg-v1-wip2`, and
`p2_route_vbrg_v1_regen.json`'s own gate data) wherever a like-for-like regenerated field
exists. The 6.55e+11x gap on `v5_budget.margin` is leg 252's own already-computed number
(stale `1.0467862586e+10` vs regenerated, worst-case-selected `1.5991923774880644e-02`),
reproduced here by direct division as `654571815954.7423`, i.e. `~6.55e+11x` — verified, not
re-derived. Not every leg 236 number *can* be re-based: nobody has regenerated the anchor with
leg 226's row-exclusion classifier applied on top of the regeneration, so the 640.1x
`newton_weighted_defect_max` exclusion effect remains fresh-resolve-tier on the stale bank's
rows. This gap is named as a residue, not silently carried forward as if it were regenerated.

**(b) Provenance-tiered.** Every compared numeric value in the consolidated JSON carries a
`"tier"` key drawn from exactly three values (`banked-stale`, `banked-regenerated`,
`fresh-resolve`), with the producing leg and environment noted for every `fresh-resolve` entry.
No numeric value in the report is untagged.

**(c) Two separately-signed findings, not merged.** Part 1 of the report states
`a_max_machine`'s two-method confirmation (236's row-exclusion, 226's D2/D3 repair, both
landing on `1.0 -> 0.55`). Part 2 states `grid_converged_a_max`'s opposite behavior
(`raw=1.0` in leg 226's own environment against v11's own unmodified test, `repaired=0.5`
which matches the original bank) as its own item, with an explicit `do_not_conflate_with_part_1`
field spelling out why the two are not one "moved" headline: `a_max_machine`'s raw value is
*stable* across every environment measured and its move under repair is real; `grid_converged_a_max`'s
raw value is *unstable* across environments (0.5 bank, 0.5 leg-252-regen, 1.0 leg-226-raw) and
its "return" to 0.5 under repair is a stabilization against noise, not a comparable move.

**(d) D1 falsified, D3 real, record corrected.** Part 4 states leg 226's measurement plainly
(on-branch D1 max `9.364e-03`, off-branch D1 min `9.535e-12` — nine decades of overlap, no
usable threshold) against D3's clean separation (on-branch max `24.06`, off-branch min
`197.30`). It names leg 226's own dispatch as the place D1 was cited as the intended mechanism,
and records that leg 236's independently-derived exclusion classifier is the *same* underlying
diagnostic as D3 (far-field inflation / decay class), which is why the two methods in part 1
agree — they key off the same physical quantity by two different code paths, not off D1.

**(e) Noise-floor discipline applied, not just declared.** Part 6 states leg 252's own
measured baseline verbatim (359 leaves compared, 202 changed outside `v5_budget`/`meta`, 46 of
206 changed leaves move by >10%, with the exact rows named: `v4_grids` at `a=0.8` and `a=1.0`).
Part 1 and Part 2 each explicitly test their own "moved" claim against this baseline:
`a_max_machine`'s raw value is confirmed stable across every measured environment (so its move
is real, not noise); `grid_converged_a_max`'s raw instability is confirmed to land on exactly
the two rows leg 252 already named as its worst movers (so its instability is noise-floor-consistent,
not claimed as a fourth independent disagreement).

**(f) Leg 247's exoneration restated in this document.** Part 5 states the exact-float-equality
match (`62.53156368658738` both from leg 252's regeneration and from leg 247's own independent
fresh re-solve of `a=0.45`, relative difference `0.000e+00`) and explains the published 0.82%
gap between that and leg 247's own headline (`62.02373957411577`) as banked-row-vs-fresh-row
provenance — the same cross-environment effect part 6 already documents elsewhere in the
artifact, not a defect in leg 247's repair logic.

**(g) Zero re-measurement.** No solver call, no Newton solve, no classifier pass was run by
this leg. Every number in the consolidated JSON is read from one of the four input branches
(236, 226, 247, 252), cited by branch, file, and field. Where composing two inputs' repairs
would require a run nobody has done (the joint regeneration+exclusion case, both parts 1 and 3
above), the report says so explicitly as a residue rather than estimating or guessing a value.

## A correction surfaced during the pass, not assumed from the dispatch

The dispatch for this leg described leg 247 as "already landed on main." Checked directly and
found false: `git log origin/main --grep="Leg 247:"` returns 0 commits, and
`origin/main`'s own `writeup/data/p2_route_d_v11_anchor.json` still carries the pre-repair
stale margin (`1.0467862586e+10`), which would be impossible had leg 247 landed. Leg 247 is
escalated and parked on `leg/247-vbr-v1`, in exactly the same state as legs 236, 226, and 252.
This is recorded in the consolidated JSON's `meta.inputs.leg_247.note`, in the novelty pass
(`writeup/novelty/leg_294.md` §2), and here, so it cannot be missed by a reader who trusts the
dispatch's framing over the actual repository state. It changes nothing about the gate answer
— all four inputs were read directly from their own branches regardless of `main` status — but
it is a factual correction worth being explicit about, given this report's stated purpose.

## Downstream documents flagged, not touched

Per the gate's yes-branch instruction ("flag, don't build"):

1. `writeup/4_p2_lottery/TECHNICAL_P2_PUB3_V1.md:134` quotes `a_max_machine=1.0` in a consumer
   table; now known stale (repaired value 0.55). No prose conclusion currently rests on the
   literal 1.0. Needs a light pointer-correction leg.
2. `writeup/data/p2_route_d_v11_anchor.json` on `origin/main` is unchanged and remains wrong on
   `margin` by ~6.55e+11x. Integration among leg 252's own three named options (land the
   regeneration whole / keep stale / re-bank deliberately with environment pinned) is leg 252's
   call, still unmade by anyone. Not this leg's territory to decide.
3. `writeup/4_p2_lottery/TECHNICAL_P2_ROUTED_V11.md:69,82-89` already states `a* ~= 0.5-0.55`
   and already flags `a=0.45` as over budget — confirmed NOT stale by all four inputs, no
   correction needed. Listed for completeness only.

## Residues

1. No leg has regenerated the anchor JSON with both leg 226's `continuation`/D2/D3 repair and
   leg 247's margin-selection repair applied jointly, in one environment. This is the natural
   next construction leg; not attempted here.
2. The banked anchor JSON on `main` remains stale until an integration decision is made on leg
   252's escalation. This leg reads it read-only, per territory, and does not act on it.
3. Leg 247's not-landed status (see above) should be corrected wherever else in the run's live
   state (DIRECTION.md, ORCH_STATE.md, any earlier orchestrator summary) it may have been
   described as landed — outside this leg's territory to fix, flagged for the orchestrator.

## Territory

`writeup/data/p2_route_v11x_v1_consolidated.json` (new), `writeup/novelty/leg_294.md`,
`experiments/journal/leg_294.md`. Read-only on legs 236/226/247/252's branches and files — no
branch merged into this one, nothing on those branches edited.

## Reproduce

Every cited number is traceable directly:
```
git show origin/leg/236-rddep-v1-resume:experiments/journal/leg_236.md
git show origin/leg/226-pnr-v1-resume:experiments/journal/leg_226.md
git show origin/leg/247-vbr-v1:experiments/journal/leg_247.md
git show origin/leg/252-vbrg-v1-wip2:experiments/journal/leg_252.md
git show origin/leg/252-vbrg-v1-wip2:writeup/data/p2_route_d_v11_anchor.json
git show origin/main:writeup/data/p2_route_d_v11_anchor.json   # stale bank, still on main
```
