# leg 414 — `P4-DRAFT` (wave 9, unit 2). PAPER unit. Drafting `P4`, the methodology paper.

**Territory:** `writeup/papers/P4_METHODOLOGY/**`, this file, append-only additions to
`writeup/SOURCES.md`. Nothing else was written. `STATE.md`, `WALLS.md`, `OPTIONS.md`,
`reports/`, `writeup/CORRECTIONS.md`, `writeup/waves/**` and every sibling unit's files were
**not** touched. `DIRECTION.md` was **not read**.

**Standing constraints observed.** No external contact of any kind: published material was
*read* (fetched over HTTP from arXiv, PMC, NTRS, PLOS, NIST, IEEE), and **no author, group,
maintainer or list was contacted**. No paywall was circumvented; four sources came back 403 or
had no open copy and are recorded `UNREACHABLE` rather than faked. Tier 2 is not a proof, no
`L1→L4` link moved, and nothing in this leg is progress toward Clay (~0.05%).

## §0 What this leg produced

| file | what it is |
|---|---|
| `writeup/papers/P4_METHODOLOGY/DRAFT.md` | the draft |
| `writeup/papers/P4_METHODOLOGY/FINDINGS.md` | the **second** output: what drafting revealed about the RECORD. It is worth more than the draft and the report says so. |
| `writeup/papers/P4_METHODOLOGY/inject_quotes.py` | the mechanism that puts verbatim quotations into the draft **without a human or an agent retyping them** |
| `writeup/SOURCES.md` (append-only block) | the sources this leg reached, at the depth it reached them, and the ones it did not |

## §1 The one instrument decision worth recording

`PB1`'s finding F8 (leg 411) says the tool-output channel silently drops words from long
outputs, so a unit retyping a "verbatim" quote off screen can transcribe a sentence its source
does not contain. **This leg re-confirmed that live**: rendering a 224-character quotation from
`arXiv:physics/0312102` through the output channel returned it with roughly a third of its
function words missing, while `len()` on the same string in the same process returned 224.

Consequence, adopted before a line of the draft was written: **no quotation in `DRAFT.md` was
typed by this unit.** `inject_quotes.py` extracts each one from the fetched source by
(start marker, end marker) and writes it into the file; the draft is authored with `{{TOKEN}}`
placeholders and the script fails loudly on any token it cannot resolve. Repository quotations
(`ORCHESTRATION.md` §6 clause 3, `L6`'s banked `why` string) are injected the same way, from the
file and the JSON field rather than from memory.

## §2 Numbers this leg re-derived from primary before quoting them

Every one was recomputed here, not accepted from `CORRECTIONS.md`:

| quantity | source field | recomputed |
|---|---|---|
| `L6` branch-B ladder `J1→J4` | `p2_route_l6_profile_v1.json` `gate.B.per_rung_residual` | `−4.994561%` ✓ matches §51 |
| `L6-b` budget step ×25 | `p2_route_l6b_v1.json` `gate.smallest_residual_at_20000` | `−6.751678%` ✓ |
| ratio budget/ladder | both | `×1.3518` ✓ |
| `J3`'s required drop / margin | both | `−7.2153%`, margin `0.4636` pp ✓ |
| `L-JVER` per-decade increments at the minimiser | `p2_route_ljver_v1.json` `cutoff_sensitivity` | `+9.573e-3, +3.316e-4, +6.537e-5, +6.226e-5, +6.246e-5, +6.212e-5` ✓ matches §53, **every increment positive** ✓ |
| §53's `×130` | ladder step `−8.06375e-3` over `+6.2257e-5` | `129.52` ✓ |
| `L6` rung reaches `nq_r` = 36/48/48/60/72 | **generator**, `experiments/p2_route_l6_v1.py:188` (`nq_r = 3*Nr+12`) with `joint_ladder` `Nr` = 8/12/12/16/20 | ✓ derivable; **not banked as a per-rung field** |
| §48's byte counts | `git show a7ffa1e:WALLS.md` / `a7ffa1e^` | `34,919` and `32,526` against the §3j cap `32 KB` ⇒ **2,151 over** ✓ |
| `PB2` evidence suite | executed | `31 checks, 0 failed` ✓ |
| `test_headroom.py` | executed | `§3j HEADROOM: PASS`, five rows ✓ |

**One number in the paper's own centre of gravity could NOT be traced to a banked field:
`32 of 49`.** It appears in `CORRECTIONS.md` §45, in `P4/STATUS.md` twice, and in a docstring
of `experiments/p2_verify_wave7_v1_evidence.py` — and **nowhere in `writeup/data/`**. Routed to
`FINDINGS.md` F1, and the draft labels it `UNBANKED` where it uses it.

## §3 What the drafting found that the record does not say

Full detail in `FINDINGS.md`. The largest is F2: `L6` **did** carry a control aimed at exactly
the hypothesis §51 says nobody tested — `gate.B.stability_against_the_iteration_cap`, caps
50/100/200/400/800, banked `verdict_is_stable_in_the_cap = true`. `V-W6`/§38 already ruled that
sweep a post-hoc truncation of the same 800-iteration runs and therefore not a budget control at
all. **So the record's account of §51 ("nobody divided one by the other") is incomplete in the
direction that flatters the discipline**: a control *was* planted, it did not fire as planted, it
read `true`, and no evidence check touched it (`D-VW6-10`). The paper discloses this in the body,
per the binding rule on controls.

## §4 Ceiling, declared

This leg measured nothing about fluid dynamics. It is a **view of the record**; it is not a
source, and no unit may cite it. Where the record is `UNVERIFIED` the draft says `UNVERIFIED`;
where a result is `UNDER-RESOURCED` the draft does not write it as a null.

## §5 LANDED (10:5xZ)

**`writeup/papers/P4_METHODOLOGY/DRAFT.md`** — 13 sections + abstract, ~63.7 KB. Structure:
ceilings BEFORE the claim (§1.3, six of them, `n=1` first); the setting in five constructs only,
explicitly not a repository tour (§2); a mechanism-by-mechanism map to published practice with an
**explicit RENAME verdict on each** (§3); the tally **2 prospective against 9 retrospective** in
those words with all twelve entries and *how each was actually found* (§4); **§51 as the centre of
gravity, §5, including §5.3 — the control that did not fire as planted**; the divergent functional
and the sign that saved every `NO`, found by a construction unit and not by any instrument (§6);
**§7, what the discipline failed to catch and for how long**, as a table of durations (45 legs, 11
legs, 6 legs, days, 90 minutes); the measured blind spots and the `UNBANKED` status of `32 of 49`
(§8); the two prospective catches graded (§9); related work at the depth reached with the
`UNREACHABLE` list (§10); threats to validity including the channel defect (§11); thirteen
transferable rules, each tied to the entry that forced it (§12).

**All 19 quotations script-injected**, none typed, via `inject_quotes.py`. One NASA quotation spans
a page break in the scan; rather than retype across it, the injector was split into two markers and
the elision is marked and disclosed.

**`writeup/papers/P4_METHODOLOGY/FINDINGS.md`** — F1…F11. F1 (`32 of 49` is `UNBANKED`), F2 (the
planted control that did not fire; §51's account incomplete), F3 (**the counter-evidence was inside
the same JSON object as the verdict** — `rate_dlogresid_dlogndof_last3` shrinks monotonically by
31.7% across the cap sweep, four lines from `verdict_is_stable_in_the_cap: true`, unread for eleven
legs), F4 (`nq_r` per rung is not a banked field), F5 (§53's ×130 is per-decade vs per-rung —
disclosed, not resolved), F6 (no cross-leg instrument exists at all), F7 (we have pre-registration
and not blinding; three gates amended by parties who could see the data), F8 (N-version independence
has adverse published evidence), F9 (unbanked conclusions are outside every mechanism), F10 (the
channel drops words — re-confirmed with a measurement), F11 (five debts this leg incurred).
**Stated in the file: the findings outweigh the draft, and why.**

**`writeup/SOURCES.md` rows 52–61 + an `UNREACHABLE` table** — append-only, no existing row edited.
Four PDFs re-fetched from their URLs at 10:55–10:57Z and confirmed **byte-identical**. One
discrepancy disclosed rather than smoothed: the NIST SP 800-53r4 copy read does not hash-match what
the canonical URL and the DOI redirect now serve; the *quotation* was re-verified character-for-
character against the freshly fetched copy, the *provenance of the local copy* was not resolved, and
the row says exactly that.

**Compliance.** No external contact — every acquisition was an anonymous fetch of published
material. No paywall circumvented; paywalled items banked `UNREACHABLE`, never as zeros.
`DIRECTION.md` not read. `CORRECTIONS.md`, `STATE.md`, `WALLS.md`, `OPTIONS.md`, `writeup/waves/**`,
`reports/`, `Papers/MANIFEST.md` and every sibling unit's files untouched. `test_headroom.py` run
after the `SOURCES.md` append: **`§3j HEADROOM: PASS`**. No `L1→L4` link moved; Tier 2 throughout;
this leg is not progress toward Clay (~0.05%).
