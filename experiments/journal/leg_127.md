# Leg 127 — ROUTE-NGX: the general class `A₂₁ ≠ 0`, decided

**Gate: YES, (i).** Exploration leg, reserve slot, not critical path. Branch
`leg/127-ngx-v1`.

## Gate, in its pre-committed wording

> *Can the no-go be DECIDED on the full bounded class — either (i) a proof that `Z₁ ≥ 1` for
> every bounded `A` (`A₂₁` free) at some `s < 1`, with hypotheses containing the `a = 0` CLM
> linearization, or (ii) an explicit admissible `A` with `A₂₁ ≠ 0` and measured `Z₁ < 1`,
> grid-stable over ≥ 3 resolutions?*

**YES, (i).** The yes-(i) branch merges normally: it does not lift a ban, does not change the
plan beyond precedent, and claims no Clay movement. The yes-(ii) branch (escalation #4) did
**not** fire — no banked conclusion is reversed.

## Order of work

1. `plan_of_record.py` read; `CONTINUATION_PROMPT.md` read in full; `capabilities.py` grepped
   before anything was built.
2. **Novelty pass FIRST**, committed as `writeup/novelty/leg_127.md` at `9d196bb`, before a
   line of construction. Links, not counts.
3. Module additions (append-only), runner, JSON, figure, gates, prose.

## What the novelty pass changed

Two findings, both binding:

- **The leg's central inequality is folklore and is not claimable.** `Z₁ ≥ 1 − ‖A‖σ_min` is
  the contrapositive-with-remainder of the `Z₁ < 1 ⟹ invertible` hypothesis every
  radii-polynomial paper states. Recorded, and the claim is forbidden everywhere.
- **[arXiv:2607.19762](https://arxiv.org/abs/2607.19762) (Xu, July 2026) is on this exact
  object** and post-dates leg 58's pass. It proves the `a = 0` CLM linearisation on
  origin-`H²` has point spectrum exactly `{0,1}` and essential spectrum meeting
  `{Re λ ≥ −1/2}` in the single line `{Re λ = −1/2}` — hence **invertible after modulation,
  spectral gap 1/2**. Consequence carried into every artifact: this leg's result is about the
  **`ℓ¹_w` realization at `s < 1`**, never about the operator. No sentence says the operator
  "has no bounded approximate inverse".

## The pre-registered argument failed, and is kept (lesson 76)

`DIRECTION.md` pre-registered a **two-direction** argument. Carried out, it gives only
`Z₁ ≥ 1/(1+η)` with `η = ‖G⁻¹Bh‖/‖h‖`, and `η` settles at **13.7** rather than decaying — so
the bound is `Z₁ ≥ 0.068`, vacuous. Recorded in the runner docstring and §1 of the TECHNICAL,
not deleted.

## What actually closed it

A one-direction argument that never splits `A` into blocks, so `A₂₁` never appears:
`Z₁ ≥ 1 − ‖A‖_w σ_min(L)`. The leg's own content is that `σ_min(L) = 0` in `ℓ¹_w` at `s < 1`,
witnessed by an **explicit** sequence.

| quantity | measured |
|---|---|
| fitted `p` in `σ_min ∼ M^{−p}`, `s = 0` / `0.3` / `0.7` | **0.9925** / **0.6985** / **0.3202** (predicted `1−s`) |
| max deviation from `1−s` over `s < 1`, all `K` | 0.0219 |
| `K`-spread of `σ_min` (`s < 1`) vs at `s = 1.5` | 0.29% vs 2.14 |
| explicit vector / numerical optimum | 1.0000000000045×, cosine 0.9999999999999998 |
| rows carrying the residual of `L v` | **1** (the truncation edge), `z_K = 0.0` exactly |
| edge-row exponent (gate 35) | −0.6942 vs predicted −0.70 |
| zero-pad into a 4× larger tail | ≤ **1.3543×** degradation (not an artifact) |
| trade-off vs leg 54's battery | **196/196** hold, min slack **7.73e−10**, at `exact_inv` |
| `μ > 0` control | \|exponent\| ≤ 2.62e−03 (saturates) vs 0.6985 at `μ = 0` |
| counterexample floor, `Z₁ = 0.99`, `s = 0.3` | 1.555 → 10.778 over the ladder, ×1.62/doubling |

**The bound is attained** (max slack at `A = L⁻¹`: 1.24e−08), so nothing leaks.

## Two things worth flagging to the orchestrator

1. **A `μ = 0` coincidence was interrogated under lesson 90 and turned out to be a finding.**
   The bordered and unbordered objects give identical `σ_min` at `μ = 0` (5.7e−15 relative).
   The same two arms differ by 6.1e−02 at `μ = 0.1`, so the code path does distinguish them.
   Reading: the far-field-amplitude component `z[K+1]` is **not** zero (6.5–6.8% of `‖v‖₁`,
   growing with `M`) — the mechanism is instead that its coupling column `C[:,K+1]` is
   supported on a single row, the truncation edge, so leg 52's bordering repair — the whole
   point of the assembled object — has nowhere else to reach and does not move the obstruction
   at all. This independently re-answers leg 58's NG2c split-placement objection in the
   general class.
2. **Two of my own gates failed on first run and both were real catches**: one test case
   violated the bound (fixed, and a deliberate violating case now gates that `holds` is not a
   constant); the other reported a NaN slope because the kernel's last coefficient is exactly
   zero — the same parity fact the proof uses for `z_K = 0`. Both are commented in place.

## Scope-line consequences (pointer-block work for the orchestrator, NOT done here)

Leg 54's `"A₂₁ ≠ 0 is measured, not proved"` and leg 58's `A₂₁ = 0` restriction are both
**superseded for this operator in this space**. Per the gate's own branch text those upgrades
across banked prose are the orchestrator's pointer-block work, not silent edits by this leg. I
touched no shared ledger, no banked JSON, and no other leg's prose.

## Territory

`solver/spectral_certificate.py` (append-only, new ROUTE-NGX section at the end; nothing
existing reordered or modified), `test_spectral_certificate.py` (gates 32–37; 37/37 pass),
`experiments/p2_route_ngx_v1_general.py`, `experiments/p2_route_ngx_v1_general_evidence.py`,
`writeup/data/p2_route_ngx_v1_general.json`,
`writeup/figures/fig63_route_ngx_v1_general.png`,
`writeup/4_p2_lottery/BLOG_P2_ROUTENGX_V1.md`,
`writeup/4_p2_lottery/TECHNICAL_P2_ROUTENGX_V1.md`, `writeup/novelty/leg_127.md`, this file.

`writeup/build_figures.py` and `writeup/README.md` register figures but are **outside my
declared territory**, so fig63 is deliberately left unregistered there for the orchestrator to
fold in.

## Ceiling

The `a = 0` CLM linearisation. `Y₀` exactly zero for the degenerate reason. No dynamics run.
Nothing claimed about `HL_S2_nonsymmetric`. **No link of the `L1 → L4` chain moved.** No ban
lifted; Xu's route to a CAP is a different method and is not a lift condition for any of them.
