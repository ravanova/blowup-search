# Leg 321 — Route-BLCX: blog surfaces L102/L108 + the two gate-text corrections pointers

**Gate: YES.** L102/L108 of `writeup/4_p2_lottery/BLOG_P2_PUB2_V1.md` corrected against leg
280's landed values, and `writeup/CORRECTIONS.md` §16 appended with the two gate-text
pointers, with zero other numeric content changed (diff-checked below).

Records/pointer leg, no compute, no re-derivation. Branch `leg/321-blcx-v1`, merge base
`origin/main` at dispatch. Date 2026-08-11.

Novelty log (committed before any edit): [`writeup/novelty/leg_321.md`](../../writeup/novelty/leg_321.md).

---

## 1. The two blog sites, before/after

| site | before | after | source |
|---|---|---|---|
| L102 | `0.0420` (unqualified) | `0.057643` (`‖R‖_X=17.348`, Def. 4.1's displayed half-line reading), with `0.0420` (`‖R‖_X=23.792`) named as Xu's separate full-line-equivalent reading, `1.37×` smaller | leg 280 / leg 277 — `CORRECTIONS.md` §13 |
| L108 | "(The same goes for the `0.71465` further down.)" | "(By contrast, the `0.71465` further down is convention-*free*, measured to `1.87e−16`.)" | leg 280 / leg 281 finding E5 — `CORRECTIONS.md` §14 |

Neither value was re-derived here; both are quoted verbatim from `CORRECTIONS.md` §13/§14,
which leg 280 already banked.

## 2. The two gate-text pointers

`writeup/CORRECTIONS.md` §16 appended, pointing at the two DIRECTION.md sites quoting
`6.855x wider` and `6.855x width ratio` **[ANCHOR CORRECTED 2026-08-12, leg 338 — Route-LCB1:
originally cited by line number, `DIRECTION.md:13330` and `:13338`, both already stale at this
file's own merge base; replaced with the grep-stable text anchor, per `CORRECTIONS.md` §16.1's
own correction]** (both still read `6.855x`, confirmed by direct read, not edited) with the closed form
`(7+3√5)/2 = 6.8541019662496845... → 6.854` and citations to `experiments/journal/leg_300.md`
§3 (re-derivation) and `experiments/journal/leg_319.md` §2/§15.3 (why those two surfaces are
immutable). `DIRECTION.md` itself was not opened for editing at any point.

## 3. Section numbering check

Checked `writeup/CORRECTIONS.md` against `origin/main` before writing: `## §15` (leg 319,
"width ratio `6.855` corrected to `6.854`") is the last section. `§16` was free and is what
this leg used.

## 4. Diff-check (gate's own requirement)

`git diff --stat`: `BLOG_P2_PUB2_V1.md` — 9 insertions, 3 deletions, both hunks confined to
the two flagged sentences (L102, L108 region); `CORRECTIONS.md` — pure append, one new `##
§16` section, 0 deletions. Manual read of the full diff confirms no other digit on any
touched or surrounding line changed: `0.0908`, `12.5`, `0.01086 … 0.13580`, `140.72`, `4.03`,
`7.9×` all byte-identical before/after. `DIRECTION.md` diff: empty (not opened for writing).

## 5. Territory

`writeup/4_p2_lottery/BLOG_P2_PUB2_V1.md` (the two sites only), `writeup/CORRECTIONS.md`
(append only), `writeup/novelty/leg_321.md`, this file — exactly what DIRECTION.md's leg 321
spec declares. `DIRECTION.md`, `experiments/JOURNAL.md`, `writeup/INDEX.md`,
`CONTINUATION_PROMPT.md`, `PROGRESS.md`, `plan_of_record.py`, `LITERATURE_CHECK.md`,
`PHASE2_P2_NOTES.md` all unmodified.

## 6. Quartet / figure

No figure by design: this is prose-pointer record work on already-landed numbers, not a new
finding — leg 280's and leg 319's own quartets already exist and are cited, not duplicated.
No runner, no new curated JSON, no figure number claimed (fig78 not taken; not needed here).

## 7. Ceiling

Correcting two printed digits and adding two pointers does not lift any ceiling. `a = 0`
only where applicable, nothing interval-enclosed beyond what legs 277/280/281/300/319 already
banked. No argument in `TECHNICAL_P2_PUB2_V1.md` §§3–5, leg 176's gate, leg 300's gate, or leg
319's gate is touched. No link of the `L1 → L4` chain moved. **Clay odds unchanged at
~0.05%.** No ban lifted, no route promoted, no GA compute.
