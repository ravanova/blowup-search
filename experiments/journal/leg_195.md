# Leg 195 — Route-PQVER: leg 60's landed correction, independently re-derived

**Agent:** VERIFIER. **Branch:** `verify/195-pqver-v1`. **Date:** 2026-08-06.
**Verdict: V1 YES — the correction CHECKS OUT, all five numbers, to the precision the
corrected prose states. V2 NO — the correction is arithmetically right but was applied
incompletely; two uncorrected copies of one corrected literal survive, neither consumed as a
number by live code.**

This is the paired post-landing review that `e0eba8e` never got. Verifier discipline: gaps
are **reported**, never repaired. Nothing leg 60 touched has been modified by this leg.

---

## What leg 60 claimed

Leg 60 (Route-PQ, branch `leg/pq-v1`) built two reproduction ledgers over the numbers
`writeup/4_p2_lottery/TECHNICAL_P2_ROUTEPORT_V{1,2}.md` and `writeup/README.md` items 46/47
quote, each checked to half a unit in its own last quoted digit. It reported **111/114**,
answered its own gate **NO**, and parked under escalation #4 rather than editing banked
prose. The user then reviewed the three discrepancies and approved the correction, and the
orchestrator landed `e0eba8e`, whose message claims:

> *3 non-ban-bearing discrepancies corrected (28x->63x mislabeled baseline,
> -2.541222->-2.541024 transcription slip, 1.168%->1.169% rounding), both ban-bearing
> numbers confirmed exact before and after, both scripts now report CLEAN 114/114*

Note the structural reason this needed an outside pass: **the corrected prose and the
scripts that certify the prose landed in the same commit**, so "CLEAN 114/114" is, on its
own, self-consistent by construction. The check that carries weight is one that goes from
the curated JSON to the document without passing through leg 60's ledger code.

## Clause V1 — the correction is right. Five numbers, with magnitudes.

Re-derived in `experiments/p2_route_pqver_v1_verification.py`, which imports nothing of leg
60's, rebuilds the half-ulp rule on `decimal`, and re-fits every slope itself.

| # | corrected claim | this pass, from the JSON | half-ulps out | verdict |
|---|---|---|---|---|
| (i) | gap at `ρ=10` is **63×** that at `ρ=6` | `4.374014e+09 / 6.944230e+07` = **62.9877** | 0.03 of 0.5 | confirmed |
| (ii) | reach ladder `ρ=8` row is **−2.541024** | `C_extrapolation/reach[ρ=8, n=301]` = **−2.5410243874** | 0.78 | confirmed |
| (iii) | `n=1201` gap is **1.169%** | `ratio_err_vs_CHL` = **1.1685027035%** | 0.995 | confirmed |
| ban | `distance / r_max` = **1.55e+08** | `E_ceiling/distance_over_r_max` = **1.548471e+08** | 0.31 | confirmed |
| ban | wrong-sign trend **+0.4703** per unit `ρ` | **+0.4703336113**, re-fitted by `polyfit` from the five rungs | 0.67 | confirmed |

**24/27 ledger rows PASS. The three that fail are the three OLD literals, scored on
purpose** — they are the errors leg 60 removed, and they must fail or the correction was
pointless:

* **OLD `28`** against the `ρ=6` baseline the sentence actually names: off by **34.99
  absolute, relative 1.25, 69.98 half-ulps**. Leg 60's diagnosis is confirmed exactly — `28`
  is the `ρ=8 → 10` factor, which re-derives here as **28.1618**. The old sentence
  **understated its own negative finding by 2.2496×**.
* **OLD `−2.541222`** in the reach row: off by **1.976e−04 absolute, relative 7.78e−05,
  395.2 half-ulps**. Confirmed as a transcription from the adjacent table: it *is* the
  `n=201` row of the resolution ladder, which re-derives to **−2.5412218988**.
* **OLD `1.168`**: off by **5.027e−04 percentage points, relative 4.30e−04, 1.005
  half-ulps** — just over the line, exactly as leg 60 said.

Every magnitude leg 60 reported in its own journal (1.25 relative, 70 half-ulps; 1.98e−04,
7.8e−05, 395 half-ulps; 5.03e−06, 4.3e−04, 1.0 half-ulps) **re-derives here to the digits it
printed.** Leg 60 did not overstate or understate its own findings.

Also confirmed unaffected by (ii), as leg 60 asserted: the reach power-law slope **−0.436974**
(re-identified here as the log-log fit of `|ratio − chl_ratio|` vs `X_max` over all six
rungs, then re-fitted — agreeing with the stored field to 2e−16), the extrapolated limit
**−2.5119260428**, its CHL error **2.0946e−04**, and the three untouched neighbour rows
`ρ = 6/7/9` (0.95 / 0.95 / 0.54 half-ulps). The ball `[2.9656e−12, 1.1822e−09]` against a
truncation distance `0.1830661` re-derives, so **clause P6b and both standing bans keep
exactly the evidence they had.**

### One caveat on correction (iii), reported as a margin and not a boolean

`1.169%` is right, but **only just**. The stored gap is `1.1685027035%`; the round-half tie
point is `1.16850%`. It clears the tie by **2.703e−06 percentage points — 0.54% of one
half-ulp.** So `1.169` holds under round-half-up *and* round-half-even, but a **relative
perturbation of 4.6e−06** in the stored value would flip the corrected digit back to
`1.168`. This does not move the correction (the stored value is what it is, and the headline
`1.17%` is 0.30 half-ulps clear either way); it is recorded because a correction whose
justification is a rounding rule deserves its distance from that rule's boundary stated.

## Clause V2 — the correction is arithmetically right and incompletely propagated

A scan of the whole tracked tree for surviving copies of the three corrected literals (with
`(?<![\d.])` guards, without which `28×` also matches `1.428×` and `4.0128×` — caught by
re-reading this pass's own first output, lesson 85 turned on myself). **22 sites hold a
corrected literal legitimately**: the audit trail (leg 60's journal, README's own account of
the correction, `DIRECTION.md`, `experiments/JOURNAL.md`, the JR3/PGF audit JSONs quoting the
commit message), leg 60's ledger literals, and — importantly — the places where `−2.541222`
is the **correct** value, namely the `n=201` row of the resolution table in
`TECHNICAL_P2_ROUTEPORT_V1.md` §2 and in `PHASE2_P2_NOTES.md` P-3's `n=201..1201` sentence.

**Two sites are stale.** Both hold the *reach* ladder with the corrected-away literal in the
`ρ=8` slot — the identical error leg 60 fixed in the banked writeup, off by the identical
**1.976e−04 absolute / 7.78e−05 relative / 395 half-ulps**:

| site | text | consumed as a number? |
|---|---|---|
| `PHASE2_P2_NOTES.md:2845` (P-3) | ``ratio **-2.583087 / -2.557642 / -2.541222 / -2.530473**`` | **no** — prose in the working doc |
| `experiments/p2_route_hrb_v1_postrepair.py:560` | `#  rho_max = 6/7/8/9 -> -2.583087 / -2.557642 / -2.541222 / -2.530473.` | **no** — a comment; the code recomputes its own ladder and only tests monotonicity |

A third site, `experiments/journal/leg_168.md:119`, quotes the banked ladder including
`−2.541222` and so **inherited the error from the uncorrected P-3** — but it quotes it
expressly to say its own ladder is *not* comparable to it, and takes no number from it.

**Nothing numeric is contaminated: 0 of the stale copies are consumed as a number by live
code, and no downstream result changes.** `PHASE2_P2_NOTES.md` is the working doc, not the
banked record (its own header says so), and the banked record is correct. So this is a
**bookkeeping gap in a landed correction, not a wrong result** — the smallest kind of
rework, and deliberately not escalation-level: repairing it would delete nothing and rewrite
no banked finding.

## What the rework leg is, if one is dispatched

Two one-line edits, both outside this verifier's territory and neither taken here:

1. `PHASE2_P2_NOTES.md` P-3, the reach-ladder sentence: `-2.541222` → `-2.541024` in the
   `ρ=8` slot **only**. The `n=201..1201` sentence two lines above must be left alone — its
   `-2.541222` is right.
2. `experiments/p2_route_hrb_v1_postrepair.py:560`, the same substitution in the comment.

Optionally a pointer in `experiments/journal/leg_168.md` noting the inherited value. No
JSON, no figure, no ban and no gate is affected by any of it.

## Files

* `experiments/p2_route_pqver_v1_verification.py` (new) — the runner; prints both clauses'
  ledgers with magnitudes, exits non-zero only on an unexpected V1 failure.
* `writeup/data/p2_route_pqver_v1_verification.json` (new) — every number above, including
  each row's `abs_err`, `half_ulp`, `half_ulps_out` and `rel_err`, the (iii) rounding margin,
  and the full propagation audit with its `legit`/`stale` classification.
* `writeup/novelty/leg_195.md` — novelty status **NONE claimed**, prior-art table, and the
  two clauses as pre-committed.
* No figure registered (repo convention for VERIFY-role legs; `fig59`/`fig60` already carry
  this material and both rebuild from committed data today). `writeup/build_figures.py`
  untouched.

## One line

Leg 60's landed correction is **independently confirmed on all five numbers** — `62.9877×`,
`−2.5410243874`, `1.1685027%`, `1.548471e+08`, `+0.4703336113` — with every magnitude it
reported re-derived to the digits it printed; the only finding against it is that the
identical `−2.541222` reach-row error **survives uncorrected in 2 tracked files**, neither
consumed as a number, so no banked result moves.
