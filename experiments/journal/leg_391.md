# Leg 391 — T1: the ban-wording escalation packet (Lane T, CONDUCTOR wave 1)

**Escalation-drafting leg. Nothing built, nothing run, no measurement made, no network touched.
No solver module written or edited. No ban lifted, narrowed, scoped, re-read, or argued against.
`plan_of_record.py` byte-identical to `origin/main` (§7).**

**Territory, exactly as dispatched — three files, and no fourth limb invented:**
`writeup/escalations/ESCALATION_BAN_WORDING_2026-08-13.md` (the packet; directory created by this
leg) · `writeup/novelty/leg_391.md` · this journal. **No figure. No runner script. No JSON.**

Following the precedent legs **334** and **348** set for pure-reading scoping legs: leg 348's own
journal (`experiments/journal/leg_348.md:10-17`) names exactly three files, declares *"No runner
script"* and *"No figure (per territory)"* explicitly, and records that it *"did not add a fourth
(runner) file outside declared territory"* — itself following leg 334. This leg does the same, and
says so here rather than manufacturing a fourth quartet limb for a unit that computes nothing. There
is no prose-vs-JSON consistency check to run because there is no JSON; every quoted string in the
packet was instead re-extracted from its named artefact at the file:line cited, and the ban texts
were taken from the executable output of `plan_of_record.py`, never paraphrased from `STATE.md`
(which says in terms: *"do not paraphrase from here"*).

Worked in an isolated `git worktree` per standing practice, never in the shared checkout.

---

## 1. Gate answer, in the gate's own pre-committed and immutable wording

> **GATE.** Does the packet state, for **each** of the three pending ban-wording questions, **both**
> supportable readings with the evidence for each drawn from the ban's own text and the landed
> record — **without the packet itself ruling any of them**?
> **yes →** the packet escalates to the user under §8; Lane T stays blocked until the user rules.
> **no →** name which question could not be given two supportable readings and why; that question is
> then not a wording defect and is withdrawn from the escalation rather than carried.

**YES.**

All three carry two supportable readings, each read is evidenced from the ban's own text **and** from
a landed artefact, and **the packet rules none of them.** No question was withdrawn — the `no` branch
was live and I checked it against each of the three (§4 below records what would have triggered it).

**Consequence, per the gate:** the packet escalates to the user under §8. **Lane T stays blocked
until the user rules — `T3` and `T4` included.**

I did not edit the gate.

## 2. What the packet contains, in one line each

- **(a) Cadiot.** *A1* — the clause asks for *a pass that resolves **whether***, a pass ran, the
  condition is met on its face; and the same finding shape ("prior work does not cover a zero
  diagonal") is what the entry's own text records as **discharging** the predecessor BDL reason at
  leg 57. *A2* — the Cadiot reason is *statement*, not *coverage*, so exclusion-by-hypothesis at 8 of
  12 clauses is the strongest possible **confirmation** of it, and lifting would lift a ban because
  its justification was validated (§3h rule 1 inverted).
- **(b) Stage V's "needs L1 first".** *B1* — retired in substance, unapplied in text; the phrase
  occurs in exactly one place in the whole `BANNED` table and the ban review's retirement sentence
  can only refer to it. *B2* — live and unliftable; the entry was never edited, the two entries have
  different subjects and justifications, and the table marks supersession explicitly where it means
  it and did not mark this one.
- **(c) The apparatus question.** *C1* — the ban names an **apparatus** ("machinery",
  "radii-polynomial", three NK/CAP realizations, a **model**-generality clause and no
  apparatus-generality clause; Theorem NGX's exclusion stated as *"a single bounded `A` working
  uniformly in `M`, which is the only sense the radii-polynomial method has"*). *C2* — the ban names
  a **conclusion**/space ("ℓ¹-Fourier" is a space; the lift clause is space-indexed; Theorem NGX's
  summary is *"a property of the certificate's space"*; leg 341, the only leg to work the clause,
  read it as space-indexed).
- **(d)** The **outreach hold** now sits on Lane T's critical path (leg 390 §4: (D)'s data conditions
  (8) and (9) unreadable without it). Raised as a fourth item for the user, not routed around.

## 3. The one thing this leg measured rather than argued

The dispatch authorises answering item (b)'s **factual** sub-question — *which text is operative* —
because it is settled by artefacts. Prior statements of item (b) are all **prose**: `WALLS.md`
asserts the clause is live, `4ff544a`'s commit message asserts the ban was re-posed, leg 316 quotes
it as live, leg 341 uses the same name for a different entry. **No landed file had checked the
diff.** I did:

1. `git log -S "which needs L1 first" -- plan_of_record.py` returns **exactly one commit**,
   `561bb68` (2026-08-05, *"Route-V v0: the novelty gate closes the stage"*) — the commit that
   introduced the clause. The 2026-08-06 ban review `4ff544a` does **not** appear.
2. `4ff544a`'s hunk `@@ -739,7 +824,20 @@ BANNED = [` shows the stage-V-as-posed entry as
   **unchanged context**. The one deleted line was the V-rigorous entry:
   `("reading V-rigorous's L1 prerequisite as optional -- ...", "L1")`.
3. That deleted entry's lift field was the bare string `"L1"` — it **did not contain** the phrase
   *"needs L1 first"*. In the whole table, past and present, the phrase occurs in **exactly one
   place**: the stage-V-as-posed lift clause.
4. Two landed legs call **two different entries** "the stage-V ban" — leg 341 (`:3-4`, `:113-123`)
   the re-posed ℓ¹-Fourier entry, leg 316 (`writeup/novelty/leg_316.md:28-34`) the V-as-posed entry.
   The ambiguity is **measured**, not asserted.

**Factual answer, and it is the only thing in this leg I assert:** *the V-as-posed entry is in force
today, unedited since 2026-08-05, and still carries "which needs L1 first"; the 2026-08-06 review did
not amend it; the entry the review replaced was a different one and did not contain the phrase.*
**Whether the declared retirement nonetheless reaches that clause is a question about meaning, and I
do not answer it.**

A separate operative-text observation, recorded and **not** built into a reading: `plan_of_record.py`'s
`SEQUENCE` prints `[x] L1  Certify HL_S2_nonsymmetric FOR REAL`, i.e. **stage L1 is marked
complete**, while the ban review's justification calls L1 dead in three realizations. Both statements
are in the same file. Surfacing it is the packet's job; resolving it is not.

## 4. The `no` branch was live, and here is what I checked it against

The gate's `no` branch is real: a question with only **one** supportable reading is not a wording
defect and should be withdrawn rather than carried. I tested each against that:

- **(a)** would have been withdrawn if A1 had no textual support — if the clause read *"resolves
  **that** Cadiot's construction covers…"*. It does not; it reads *"resolves **whether**"*, and the
  entry's own BDL precedent shows the same finding shape discharging a reason. A1 survives.
- **(b)** would have been withdrawn if the diff had shown `4ff544a` editing the clause (then it is
  simply retired, B2 dies) **or** if the deleted entry had itself contained the phrase (then the
  retirement is fully accounted for elsewhere and B1 dies). Neither holds: the clause is untouched
  **and** the deleted entry lacks the phrase. **Both survive, and they survive for the same reason** —
  the review's declared act and its actual edit do not coincide.
- **(c)** would have been withdrawn if the ban's text carried an apparatus-generality clause (C1
  dies) or named no space at all (C2 dies). It carries a **model**-generality clause and it names
  "ℓ¹-Fourier". Both survive.

**No question was withdrawn.** Had one been, this journal would name it here instead.

## 5. What this leg does not do — the refusals, which are the substance

- **Rules none of (a), (b), (c).** No reading is endorsed, preferred, ranked, or recommended. §3h
  rule 1 makes a defective ban wording a user escalation; §3g notes an entity that both raises and
  rules an escalation has defeated the mechanism. Leg 348 §5 refused this exact question; the
  Conductor refused it; this leg refuses it.
- **Does NOT exercise the 2026-08-11 scoping precedent.** Per the pre-committed reading: under
  reading C1 the precedent is **available in shape**, and *availability is not exercise*. The packet
  records that it is available and quotes the precedent's own shape so the user can see what would be
  being invoked — and stops there. **No scope is claimed, no lane is opened.**
- **Does NOT soften reading C2.** If the ban names the conclusion, `T3`/`T4` are inside it whatever
  the apparatus and Lane T is blocked on a **LIFT, not a SCOPE** — a materially worse position, and
  the packet says so in those words with the three adverse artefacts attached (leg 341 DIES; leg
  374's *"No row publishes a result meeting all four of the bridge's asks simultaneously"*; leg 376's
  two continuous channels).
- **Does NOT resolve the fourth-space/basis mismatch.** The lift clause names a *fourth space/basis*;
  what Lane T holds is a *fourth apparatus*. Surfaced as part of the defect, left unresolved, and the
  entailed sub-question (is `T³`'s discrete Fourier basis a namable fourth space/basis?) is flagged
  with no position taken.
- **Does NOT propagate a paraphrase.** `STATE.md`/`WALLS.md` compress leg 374 to *"Hermite sole
  survivor"*; leg 374 does not make that claim, and the packet uses leg 374's own headline instead
  and flags the compression.
- **Touches no Conductor-owned file.** `STATE.md`, `WALLS.md`, `DIRECTION.md`, `plan_of_record.py`
  all byte-identical to `origin/main`.
- **Did NOT read `DIRECTION.md`** (§3e and the dispatch). Grep locates item-(b)-relevant text at
  lines **755, 2262, 2275, 9798, 9997**; they are named in the packet as **unread artefacts** so the
  user can consult them before ruling. This is the one place I could not obtain a landed record, and
  I report it by name rather than paraphrasing around it.
- **No external outreach** (standing user hold) — which is also item (d) of the packet.

## 6. Honest ceiling and limits (lesson 91)

- **Realization named.** The packet's evidence base is: the executable output of
  `plan_of_record.py`; `git log -S` / `git show` over this repository's history; and targeted reads
  of `experiments/journal/leg_{304,316,341,348,374,376,377,390}.md`,
  `writeup/novelty/leg_{316}.md` and `writeup/4_p2_lottery/TECHNICAL_P2_ROUTENGX_V1.md`. Located by
  phrase-grep, not by semantic search — a relevant passage that paraphrases rather than quotes would
  be missed.
- **The two readings of (c) cannot be separated by any measurement this repository has.** Theorem
  NGX's own contrast is **space versus operator**; apparatus is not a variable it moved, and its
  object is the assembled bordered `a = 0` CLM linearisation, not a torus periodic orbit. So neither
  C1 nor C2 can claim the measurement settles it. That is *why* it is a wording question — and it is
  also the honest limit of what a packet can supply: the user is ruling on drafting intent, with no
  number available to arbitrate.
- **The three questions are the three I was given.** I did not sweep the remaining 16 live bans for
  further wording defects, and this leg is not evidence that there are none.
- **Leg 348's own ceiling still binds anything downstream of (c).** Its seven links were read at
  abstract level only, and seven links is not exhaustive; if the user rules toward C1, a scoping leg
  for the Galerkin-plus-tail apparatus needs its own full external novelty pass.
- **Item (a) has no operational consequence either way.** Its subject is a claim about the
  literature; no lane in `WALLS.md` waits on it. It is bundled because it is pending, and the packet
  says so rather than implying the bundle is three equally load-bearing blockers.

## 7. `plan_of_record.py` verification, pasted verbatim as required

```
$ git diff origin/main -- plan_of_record.py
$
```

**Empty.** `plan_of_record.py` is byte-identical to `origin/main`. Same check, empty, for
`STATE.md`, `WALLS.md` and `DIRECTION.md`:

```
$ git diff --stat origin/main -- STATE.md WALLS.md DIRECTION.md plan_of_record.py
$
```

## 8. Resourcing (§3d)

**No compute.** A reading-and-drafting unit cannot return `UNDER-RESOURCED` on budget, and it does
not. One artefact could not be obtained: `DIRECTION.md`'s five item-(b)-relevant entries, barred by
§3e and by the dispatch. Reported by name in §5 and in the packet, not paraphrased around.

## 9. Wall 1 / Wall 2 / Clay — the ceiling clause, stated because ambition makes it easier to erode

**No link of the `L1 → L4` chain moved. Nothing in this unit moves one, and nothing in it could.**
Walls 1 and 2 stand. **Tier 2 ceiling. Clay odds ~0.05%, unchanged.** Nothing here is described as
movement toward Clay, and **preparing an escalation is choosing what to ask, never movement toward
Clay** — it is strictly less than classifying a route, which leg 348 already recorded as not movement.

## 10. Push record

Branch `leg/391-t1-escalation`. `bash scripts/merge_gate.sh origin/main` → **MERGE GATE: PASS**.
Branch pushed to `origin`; **not merged, not pushed to `main`** — the Conductor audits and
lands. This leg self-terminates on push and picks no successor.
