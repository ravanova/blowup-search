# Leg 386 — Route-DTOL: the δ mode, PRE-REGISTERED

## PART I — THE PRE-REGISTRATION (immutable; committed before the runner exists)

**This part is written and committed BEFORE any measurement of this leg is run.** Nothing in it
may be amended afterwards. Where the measurement disagrees with it, Part II records the
disagreement as the finding and Part I stays on the record wrong.

### 0. What this leg is, and what it is not

Leg 382 landed `solver/dssp_decay_enclosure.py` with a relative-tolerance argument
(`rel_tolerance`, i.e. `δ`) that its own journal §13 labels **POST-HOC and NOT
GATE-DECIDING**: it was added after the run, in response to the measured finding that at
`δ = 0` the certified set is EMPTY for *every* profile that is not an exact power law — down
to a `1e-12` perturbation — so the exact instrument answers EMPTY on every real input.

This leg **promotes that parameter honestly**: it pre-registers the mode's predicted behaviour
*here*, measures it **fresh**, and ships it only if the pre-registration holds.

**Leg 382's numbers are LEADS, NEVER INHERITED.** They are recorded here once, so that a
disagreement is visible as a disagreement and not as a silent overwrite:

| leg 382's post-hoc lead | value |
|---|---|
| certified width under tolerance | `≈ 0.8686·δ` |
| `δ*` (C2, rational far cutoff) | `3.352868` |
| `δ*` (C1, two-power mixture) | `0.315697` |
| `δ*` (C3, log correction) | `0.069739` |
| `δ*` (K2, exact power law) | `0` |
| `δ = 0` widths, K1/K2/K3/K4 | `7.438e-15`, `1.599e-14`, `1.998e-14`, `1.554e-14` |

**No number in the table above is used as an input anywhere in this leg.** Every quantity is
re-measured, and every prediction below is derived independently, in closed form, in §2.

**Ceiling, declared before measurement: TIER 2, in every branch of both clauses.**
`CLAY_OBLIGATIONS.md` §6 items 1 and 2 stay **OPEN** in every branch. No `L1 → L4` link is
claimed to move. **Clay stays ~0.05%.**

### 1. Frozen configuration

| constant | value | why frozen |
|---|---|---|
| primary window | `[R0, R1] = [10, 1000]` | leg 382's window; keeping it is what makes a disagreement with 382's leads a disagreement about the *instrument*, not about the window |
| secondary window | `[10, 100]` | used only for the width law's **window-dependence** prediction (§2.1), which is new to this leg |
| cell count, gate rows | `N = 1000` |  |
| cell count, δ sweeps and bisections | `N = 200` | bisection runs many solves; leg 382's own sweep count, kept so the δ\* comparison is like-for-like |
| mode | `"cells"` | the only mode whose statement covers the whole window; `nodes` is never quoted as certified |
| search bracket | `p ∈ [0, 12]` |  |
| planted knowns | `K1..K4`: `3·r^-1`, `1·r^-2`, `0.25·r^-2.5`, `7·r^-3` |  |
| planted mismatches | `C1` `r^-2 + 0.01 r^-1`; `C2` `r^-2/(1+(r/300)^4)`; `C3` `r^-2 log r` | leg 382's three controls, unchanged, so `δ*` is comparable |
| bisection iterations | `60` |  |

### 2. The predictions, derived in closed form — NOT imported

#### 2.1 The width law

Let `t = log r`, `h = log(1+δ)` the half-height of the multiplicative tube in log space. For an
**exact** power law `g(t) = c₀ − p₀t`, the affine functions lying within `h` of `g` on
`[t₀, t₁]` are exactly those with

  `|p − p₀| ≤ 2h/(t₁ − t₀)`,

(the extreme tilt pivots about the window midpoint and uses the full tube at both ends), so

> **PREDICTION W.** `width(δ) = 4·log(1+δ) / log(R1/R0)`, and for small δ
> `width ≈ (4/log(R1/R0))·δ`.
>
> On `[10, 1000]`: coefficient `4/log(100) = 0.8685889638065035`.
> On `[10, 100]`: coefficient `4/log(10) = 1.737177927613007` — **exactly 2× the primary
> window's**, and this window-dependence is a prediction leg 382 never made or tested.

Note this derivation *explains* leg 382's `0.8686` rather than borrowing it: `0.8686` is
`4/log(100)`, a property of the **window**, not of the instrument.

**Pass band for W:** `measured/predicted ∈ [0.95, 1.05]` at each `δ ∈ {1e-9, 1e-6, 1e-3, 1e-2,
1e-1}` on the primary window, and the secondary-window coefficient within `[0.95, 1.05]` of
`2×` the primary one.

#### 2.2 The critical tolerances `δ*`

Write `g(t) = c − p₀t + φ(t)`, `φ` the profile's **departure** from an exact power law. A tube
of half-height `h` admits some affine function iff `h ≥ E∞(φ)`, the Chebyshev best-affine
approximation error of `φ` on `[t₀, t₁]`. Hence

> **PREDICTION D.** `δ* = exp(E∞(φ)) − 1`, and the exponent at the transition is
> `p_c = p₀ − b*`, `b*` the best-affine slope of `φ`.

Computed (numerically, to `1e-13`, on the closed-form `φ`s — this is arithmetic on the
*profile definitions*, not a run of the instrument):

| case | `φ(t)` | `E∞` | **`δ*` predicted** | `p_c` predicted | 382's lead |
|---|---|---|---|---|---|
| C1 | `log(1 + 0.01 e^t)` | `0.276728` | **`0.318807`** | `1.500000` | `0.315697` |
| C2 | `−log(1 + e^{4(t − log 300)})` | `1.493887` | **`3.454378`** | `3.047509` | `3.352868` |
| C3 | `log t` | `0.074203` | **`0.077025`** | `1.761439` | `0.069739` |
| K2 | `0` | `0` | **`0`** | `2` | `0` |

**Pass band for D:** each measured `δ*` within **±20 %** of the prediction. The predictions sit
**above** every one of 382's leads (by `+1.0 %`, `+3.0 %`, `+10.4 %`), and that direction is
itself predicted: the *cell* enclosure is slightly wider than the exact profile tube, so the
tube reaches feasibility at a slightly **smaller** δ than the continuum theory gives.
**Recorded in advance: if the fresh measurement lands on 382's leads rather than on the
closed-form values, the closed form is the thing that is wrong, and Part II says so.**

#### 2.3 The exact local-slope ranges (what "contained truth" can mean for a non-power law)

A profile that is not a power law **has no true exponent**, so demanding that the certified
interval contain the nominal `p₀` would be demanding the instrument certify something false.
The truth a non-power profile *does* have is its **exact local log–log slope range**
`[s_min, s_max] = [min_t (−g'(t)), max_t (−g'(t))]`, in closed form:

| case | `s_min` | `s_max` | nominal `p₀` |
|---|---|---|---|
| C1 | `1.090909…` (`2 − 10/11`) | `1.909090…` (`2 − 0.1/1.1`) | `2` |
| C2 | `2.000005…` | `5.967747…` (`2 + 4x/(1+x)`, `x = (10/3)^4`) | `2` |
| C3 | `1.565702…` (`2 − 1/log 10`) | `1.855285…` (`2 − 1/log 1000`) | `2` |

> **PREDICTION S.** At `δ = δ*`, the certified interval is a near-point at `p_c` and is
> **contained in `[s_min, s_max]`**, and — stated in advance so it is not read later as a
> failure — it does **NOT** contain the nominal `p₀ = 2` for C1, C2 or C3. Nominal containment
> is recorded as a magnitude, not as a pass condition.

### 3. GATE CLAUSE 1 — *the δ mode is an instrument, not a fit with extra steps*

**Wording, fixed:** *At the freshly-measured `δ*`: do the three non-power planted cases certify
with contained truth and reported widths, does the exact power law still certify at `δ = 0`
with 382's widths reproduced, AND does a sub-`δ*` control remain EMPTY?*

**YES iff all four sub-conditions hold:**

* **(1a) Width law.** PREDICTION W holds inside its pass band, on both windows.
* **(1b) The three non-power cases certify at their own freshly-measured `δ*`.** At
  `δ = δ*_hi` (the upper bisection bracket — the first tolerance at which EMPTY fails), each of
  C1, C2, C3 returns verdict `INTERVAL` (**not** `EMPTY`, **not** `INCAPACITY`), with a finite
  reported `width`, and `[p_lo, p_hi] ⊆ [s_min, s_max]` of §2.3.
* **(1c) The exact power law still certifies at `δ = 0`, with 382's widths reproduced.**
  `p₀ ∈ {1, 2, 2.5, 3}` each returns `INTERVAL` at `δ = 0` containing its truth, with
  `width ≤ 1e-13`, and each width **within a factor 10** of 382's corresponding lead
  (that factor-10 band is the pre-registered meaning of "reproduced": these are
  rounding-floor quantities and demanding bit-equality across a code change would be theatre).
* **(1d) A sub-`δ*` control remains EMPTY.** At `δ = 0.9·δ*_lo` **and** at `δ = 0.5·δ*_lo`,
  each of C1, C2, C3 returns `EMPTY`. *This is the clause that stops the mode being a fit: if
  a profile that should be excluded stops being excluded below its threshold, δ has become a
  knob.*

* **YES →** the instrument gains a usable mode whose looseness is a declared magnitude, never
  silent; certified-vs-fitted remains a distinction with teeth. **CEILING TIER 2;
  `CLAY_OBLIGATIONS.md` §6 items 1 and 2 OPEN.**
* **NO →** report the measured width law verbatim and which sub-condition failed. **A δ mode
  that cannot be pre-registered is not an instrument and is not shipped** — the argument is
  reverted to leg 382's landed state and the leg reports NO.

### 4. GATE CLAUSE 2 — *does the admissible-cutoff analysis tolerate δ > 0 AT ALL?* (co-equal, not a footnote)

**The mechanism, engaged with rather than restated.** `CLAY_OBLIGATIONS.md` §4's admissible
cutoff radius is a function of the **certified** exponent, and every bound in leg 381's priced
bill depends on the enclosure's **lower** endpoint `α_lo`, not its centre. Leg 381 composed
this as `α_lo = α_centre − 0.434·δ`, giving `δ < (α_centre − 1)/0.434` (fixed-ball energy and
critical `L³`) and `δ < (α_centre − 3/2)/0.434` (global `L²`). Meanwhile δ must **exceed** the
profile's own departure `δ*`, or the verdict is EMPTY and there is no `α_lo` at all.

Two observations this leg makes *before* running, because they determine what to measure:

1. **`0.434` is `2/log(R1/R0)`** (§2.1) — a property of the **window**, not a universal
   constant. Leg 381's threshold is therefore window-dependent, and a longer window relaxes it.
2. **Leg 381's `α_lo = α_centre − 0.434·δ` may be conservative.** If the half-width vanishes at
   `δ = δ*` rather than at `δ = 0` (call it **H-shift**: `half-width ≈ a·(δ − δ*)`), then
   `α_lo → α_centre` as `δ ↓ δ*`, and the admissibility condition collapses from
   `α_centre > 1 + 0.434·δ*` to the far weaker `α_centre > 1`. **Between those two readings
   lies the entire difference between a usable instrument and a relabelled one**, so this leg
   discriminates them by measurement:
   * **H-381:** `half-width(δ) = 0.434·δ`.
   * **H-shift:** `half-width(δ) = a·(δ − δ*)`, i.e. the intercept is at `δ*`.
   Discriminant, fixed now: measure `half-width(1.1·δ*)` for C1/C2/C3 and compare with
   `0.434·1.1·δ*`. H-shift is favoured if the measured half-width is below **half** of the
   H-381 value; H-381 is favoured if it is within `±10 %` of it. Any other outcome is reported
   as "neither law fits" with the numbers.

**The primary clause-2 measurement is DIRECT, not algebraic.** For each mismatch shape
`S ∈ {two-power, rational cutoff, log-corrected}` and each planted nominal exponent
`p₀ ∈ {1.0, 1.25, 1.5, 2.0, 2.5, 3.0}`, locate by bisection the δ set

  `D₁(S, p₀) = { δ ≥ 0 : verdict = INTERVAL and p_lo > 1 }` — fixed-ball energy / critical `L³`
  `D_{L²}(S, p₀) = { δ ≥ 0 : verdict = INTERVAL and p_lo > 3/2 }` — global `L²`

and report `[δ_min, δ_max]` and its width, or **EMPTY**. `δ_min` is the profile's own departure
threshold; `δ_max` is where the enclosure's lower endpoint drops through the physical threshold.
This measures the composed condition itself, with no law interposed.

**The α value in play is pre-registered as `α_centre = 1`**, because that is the value
`CLAY_OBLIGATIONS.md` §4's third amendment names for the banked object: *"`L²` needs
`α > 3/2`; the banked Type-I object gives `α = 1` — deficit 0.5, ratio 1.5×"*. The measured
`α_centre = (p_lo + p_hi)/2` of each row is recorded alongside, so the clause is decided at the
α actually realised and not at a nominal one.

**Decision rule, fixed:**

* **ADMISSIBLE →** `D₁` is non-empty at the α in play. Name the measured window
  `[δ_min, δ_max]`, the `α_centre` it is computed at, and state its width as a magnitude.
* **EMPTY →** `D₁` is empty at the α values in play. **Say so plainly**: it would mean the
  certified-exponent route cannot feed §4's cutoff analysis at any tolerance. No post-hoc
  widening, no rescue reported as gate-deciding.

**α-sensitivity is reported in BOTH branches**, as §4's consumers need to know whether a window
that exists at one α vanishes at another: the full `(S, p₀)` table above, plus the crossover
`α_centre` at which `D₁` first becomes non-empty for each shape.

**Prediction (recorded, may be wrong).** `D₁` is **EMPTY at `α_centre = 1`** for the two shapes
whose local slopes run *below* `p₀` (two-power, log-corrected), since `p_lo ≤ s_min < 1` there
and no tolerance can lift a lower endpoint. For the rational-cutoff shape, whose local slopes
run *above* `p₀` (the cutoff steepens the decay), `D₁` may be **non-empty even at `p₀ = 1`** —
and if so, that is a shape-dependence finding, not a rescue, and it will be reported with the
warning that a profile only decays that fast *because the window contains the cutoff*.

**If the two clauses answer in opposite directions, both verdicts are reported side by side and
are NOT netted into one word.**

### 5. What is built (territory, declared in advance)

* `solver/dssp_decay_enclosure.py` — δ made an explicit, always-recorded interface field
  (leg 382's early `INCAPACITY` return path drops `rel_tolerance` silently; leg 389 will
  consume these rows and must never receive a row without its δ), the docstring promoted from
  post-hoc to pre-registered with the closed-form width law, and one new function
  `cutoff_admissible_delta_window` implementing clause 2's direct measurement so that the
  composition is a capability rather than a one-off script. **The default stays `δ = 0`**: the
  exact statement remains the default, and leg 382's `EMPTY at every ε down to 1e-12`
  regression test must still pass, unweakened.
* `test_dssp_decay_enclosure.py` — **additive only**.
* `experiments/p2_route_dtol_v1.py`, `writeup/data/p2_route_dtol_v1.json`.
* `writeup/novelty/leg_386.md` (landed first, `36d01c7`), this journal.
* `writeup/4_p2_lottery/{BLOG,TECHNICAL}_P2_ROUTEDTOL_V1.md`.
* `writeup/figures/fig99_route_dtol_v1_delta_window.png`, registered in
  `writeup/build_figures.py` (**fig99 is this leg's allocation and no other number is used**).

### 6. Anti-tautology checks, fixed in advance

1. No planted known returns the full search bracket at any δ.
2. No exact power law is ever certified EMPTY at any δ ≥ 0.
3. Every mismatch is EMPTY at δ = 0 (the δ mode has not made the instrument fail-shy).
4. `δ` appears in **every** returned row, including all `INCAPACITY` paths.
5. The δ sweep is monotone: widths non-decreasing in δ, EMPTY downward-closed.

---

## PART II — WHAT WAS MEASURED

Runner `experiments/p2_route_dtol_v1.py`, JSON `writeup/data/p2_route_dtol_v1.json`,
figure `writeup/figures/fig99_route_dtol_v1_delta_window.png`, runtime **60.7 s**.
Tests `test_dssp_decay_enclosure.py` **16/16** — leg 382's 12 unweakened and still passing,
including `test_zero_tolerance_rejects_any_perturbation`.

## 1. THE TWO VERDICTS, SIDE BY SIDE AND NOT NETTED

> **CLAUSE 1 — *At the freshly-measured `δ*`: do the three non-power planted cases certify
> with contained truth and reported widths, does the exact power law still certify at `δ = 0`
> with 382's widths reproduced, AND does a sub-`δ*` control remain EMPTY?* → YES.**
> All four sub-conditions hold: 1a ✔, 1b ✔, 1c ✔, 1d ✔.

> **CLAUSE 2 — *Does the admissible-cutoff analysis tolerate `δ > 0` AT ALL?* → EMPTY, at the
> α value in play.** At the realised `α_centre = 1` that `CLAY_OBLIGATIONS.md` §4 names for the
> banked Type-I object, the composed window is **empty**, and it is empty for the same reason at
> every `α_centre ≤ 1`: **the tolerance buys exactly ZERO headroom on the exponent threshold.**
> **0 of 30** measured rows are admissible with a realised `α_centre ≤ 1`.

**They point in opposite directions and are left that way.** The δ mode is a real instrument
(clause 1) that the specific composition §4 needs **cannot use on the banked object** (clause 2).

## 2. Clause 1a — the width law, and the disagreement with leg 382's lead

`width(δ) = 4·log(1+δ)/log(R1/R0)`, PRE-REGISTERED. Measured on both windows,
`δ = 1e-9 … 1e-1`:

| quantity | measured | predicted | ratio |
|---|---|---|---|
| measured / closed form, all 10 rows | — | — | **1.005051 … 1.005074** |
| small-δ coefficient, `[10, 1000]` | **0.87283702** | `4/log 100 = 0.86858896` | `1.004896` |
| small-δ coefficient, `[10, 100]` | **1.74566404** | `4/log 10 = 1.73717793` | `1.004887` |
| window ratio (short / long) | **1.9999989** | exactly `2` | `1.00000` |

**PASS (band `[0.95, 1.05]`).** The uniform `+0.51 %` is the cell discretisation's own
contribution and is constant across nine decades of δ, which is why the law is usable as a
law rather than as a fit.

**Disagreement with leg 382's lead, stated as a disagreement.** 382's lead was `0.8686`;
fresh measurement gives **`0.87284`**, i.e. **`+0.49 %`, measured above the lead**. The
substantive correction is not the fourth digit — it is that **`0.8686` is `4/log 100`, a
property of the WINDOW, and it doubles to `1.7457` on `[10, 100]`.** Any threshold quoted
"at `0.434` per unit δ" is quoted at a window, and leg 381's composition inherits that.

## 3. Clause 1b — certification at the freshly-measured `δ*`

| id | measured `δ*` | verdict at `δ*` | `[p_lo, p_hi]` | width | exact local-slope range | inside? | `p_c` predicted |
|---|---|---|---|---|---|---|---|
| C1 | `0.31569700` | **INTERVAL** | `[1.49794908, 1.49794908]` | `2.220e-16` | `[1.090913, 1.909087]` | **yes** | `1.500000` |
| C2 | `3.35286818` | **INTERVAL** | `[3.03284150, 3.03284150]` | `1.332e-15` | `[2.000005, 5.967854]` | **yes** | `3.047509` |
| C3 | `0.06973929` | **INTERVAL** | `[1.76241212, 1.76241212]` | `2.220e-16` | `[1.565714, 1.855234]` | **yes** | `1.761439` |

**PASS.** Every interval is inside the profile's own exact local-slope range — the instrument
certifies no exponent the profile never exhibits. **As PRE-REGISTERED in §2.3, none contains
the nominal `p₀ = 2`, and that is recorded rather than repaired**: a profile that is not a
power law has no true exponent, and the transition exponents land on the closed-form
predictions to `0.0021`, `0.0147`, `0.0010`.

## 4. Clause 1c — `δ = 0` still certifies, widths reproduced

| id | `p₀` | width at `δ = 0` | leg 382 lead | ratio |
|---|---|---|---|---|
| K1 | 1 | `7.438494e-15` | `7.438e-15` | `1.00007` |
| K2 | 2 | `1.598721e-14` | `1.599e-14` | `0.99983` |
| K3 | 2.5 | `1.998401e-14` | `1.998e-14` | `1.00020` |
| K4 | 3 | `1.554312e-14` | `1.554e-14` | `1.00020` |

**PASS** — truth contained in all four, all `≤ 1e-13`, all within a factor `1.0002` of the
lead where a factor 10 was pre-registered as sufficient. **The δ mode changed nothing at
`δ = 0`**, which is the point of keeping `δ = 0` the default.

## 5. Clause 1d — the sub-`δ*` control, the clause that stops δ being a knob

| id | `0.9·δ*` | verdict | `0.5·δ*` | verdict |
|---|---|---|---|---|
| C1 | `0.284127` | **EMPTY** | `0.157848` | **EMPTY** |
| C2 | `3.017581` | **EMPTY** | `1.676434` | **EMPTY** |
| C3 | `0.062765` | **EMPTY** | `0.034870` | **EMPTY** |

**PASS, 6/6.** Below its threshold every mismatch is still *proved* impossible. The mode did
not make the instrument fail-shy.

## 6. The freshly-measured `δ*`, against BOTH the closed form and 382's leads

| id | measured `δ*` | closed form `exp(E∞)−1` | measured/predicted | 382's lead | measured/lead − 1 |
|---|---|---|---|---|---|
| C1 | `0.31569700` | `0.318807` | `0.99024` | `0.315697` | `−5.4e-09` |
| C2 | `3.35286818` | `3.454378` | `0.97061` | `3.352868` | `+5.5e-08` |
| C3 | `0.06973929` | `0.077025` | `0.90541` | `0.069739` | `+4.2e-06` |

**Two findings, in opposite directions, and both are reported.**

1. **With leg 382: full agreement.** The fresh measurement reproduces all three leads to
   `5e-9`, `5e-8`, `4e-6` relative. There is **no disagreement with 382 on `δ*`.**
2. **With this leg's own closed form: a systematic `−1 % … −9.5 %`, in the predicted
   direction.** §2.2 predicted in advance that the measurement would land *below* the
   continuum value because the cell enclosure is slightly wider than the exact tube; it does,
   in all three cases, and the deficit grows as `δ*` shrinks (`−0.98 %`, `−2.94 %`, `−9.46 %`)
   — consistent with a fixed discretisation width becoming a larger share of a smaller
   threshold. Inside the pre-registered `±20 %` band.

## 7. Clause 2, the mechanism — leg 381's composition is CONSERVATIVE, and its α demand is refuted

The pre-registered discriminant, at `δ = 1.1·δ*`:

| id | δ | measured half-width | H-381 `0.434·δ` | measured/H-381 | H-shift `0.434(δ−δ*)` | measured/H-shift |
|---|---|---|---|---|---|---|
| C1 | `0.347267` | `0.0203348` | `0.150816` | **`0.1348`** | `0.013711` | `1.483` |
| C2 | `3.688155` | `0.0743487` | `1.601745` | **`0.0464`** | `0.145613` | `0.511` |
| C3 | `0.076713` | `0.0056791` | `0.033316` | **`0.1705`** | `0.003029` | `1.875` |

**Verdict by the pre-registered rule: H-shift** (all three below half of the H-381 value; the
rule's H-381 branch would have needed `±10 %`). Stated precisely, because H-shift is not
exact either: **the half-width vanishes at `δ = δ*`, not at `δ = 0`** — that part is
confirmed — while the slope coefficient near the transition is **not** `0.434` (measured
ratios `0.51`–`1.88` of the H-shift value). What is refuted is the *intercept*, and the
intercept is what leg 381's α demand rests on.

**Consequence for `CLAY_OBLIGATIONS.md` §4, measured.** Leg 381's `δ < (α_centre − 1)/0.434`
implies that a profile with `δ* = 3.35` would need `α_centre > 1 + 0.434·3.35 = 2.456` before
any tolerance is admissible. **That implication is REFUTED.** Because the half-width vanishes
at `δ*`, the requirement is just `α_centre > 1`. Measured `δ_max` against 381's law, threshold 1:

| shape | `α_centre` | measured `δ_max` | 381's `(α_c−1)/0.434` | measured / 381 |
|---|---|---|---|---|
| two-power | `1.497949` | `2.126079` | `1.146570` | **`1.854`** |
| two-power | `1.997949` | `8.772372` | `2.297862` | **`3.818`** |
| rational cutoff | `2.032842` | `9.658038` | `2.378205` | **`4.061`** |
| rational cutoff | `3.032842` | `10` (capped) | `4.680790` | `2.136` |
| log-corrected | `1.262412` | `0.804843` | `0.604226` | **`1.332`** |
| log-corrected | `1.762412` | `4.642082` | `1.755518` | **`2.644`** |

**Leg 381's law understates the admissible `δ_max` by `1.33×` to `4.06×`** on every row where
a window exists. **It is conservative, never optimistic — it never claims a window that is not
there** — so nothing built on it is unsafe; it is simply tighter than the measurement requires.

## 8. Clause 2, the answer — and its α-sensitivity

**The composed window is non-empty if and only if the realised certified centre exceeds the
threshold. 30 rows, 0 exceptions.** Six crossover probes place the crossover threshold at the
realised centre itself (`admissible` just below the centre, `EMPTY` just above, at a relative
offset of `1e-6`), so **δ buys zero headroom on the threshold**: widening δ moves the LEFT
edge of what can be certified and never moves the centre.

| shape | `α_centre` | window at threshold `1` | width | window at threshold `3/2` | width |
|---|---|---|---|---|---|
| two-power `p₀=1` | `0.497949` | **EMPTY** | — | **EMPTY** | — |
| two-power `p₀=1.5` | `0.997949` | **EMPTY** | — | **EMPTY** | — |
| two-power `p₀=2` | `1.497949` | `[0.315697, 2.126079]` | `1.810` | **EMPTY** | — |
| two-power `p₀=2.5` | `1.997949` | `[0.315697, 8.772372]` | `8.457` | `[0.315697, 2.126079]` | `1.810` |
| rational cutoff `p₀=1` | `2.032842` | `[3.352868, 9.658038]` | `6.305` | `[3.352868, 5.570913]` | `2.218` |
| log-corrected `p₀=1` | `0.762412` | **EMPTY** | — | **EMPTY** | — |
| log-corrected `p₀=1.5` | `1.262412` | `[0.069739, 0.804843]` | `0.735` | **EMPTY** | — |
| log-corrected `p₀=2` | `1.762412` | `[0.069739, 4.642082]` | `4.572` | `[0.069739, 0.804843]` | `0.735` |

(Full 30-row table in the JSON; `δ_max = 10` rows are capped at the search ceiling and marked.)

**α-sensitivity, stated as §4's consumers need it.** The dependence is **a step, not a slope**:
the window is empty for every `α_centre ≤ threshold` and non-empty for every
`α_centre > threshold`, uniformly across all three mismatch shapes, and once open it is **wide**
(`0.735` to `≥ 9.93` in δ). So the answer does **not** vary shape-to-shape at fixed realised α —
it varies only across the threshold. **At `α_centre = 1` it is EMPTY; at `α_centre = 1.0001` it
is non-empty.** That is the whole α-sensitivity, and it means the question §4 must ask is not
"how accurate is the profile" but "**does the certified centre clear 1 (or 3/2) at all**".

**The nominal-vs-realised trap, disclosed.** One row — the rational cutoff at nominal `p₀ = 1` —
is admissible, and a nominal reading of the α ladder would have returned **ADMISSIBLE** for
clause 2 on the strength of it. It is not admissible at `α = 1`: it **realises
`α_centre = 2.0328`**, because the measurement window contains its cutoff and the profile there
decays far faster than `r^-1`. PART I §4 fixed the decision at the realised α precisely so this
could not be counted, and the JSON records both readings (`nominal_p0_reading` returns 1
admissible shape; `realised_alpha_reading_OPERATIVE` returns 0). **The operative answer is
EMPTY.**

## 9. Anti-tautology checks — 6/6

1. No planted known returns the full search bracket at any δ ✔
2. No exact power law is ever certified EMPTY, at any of 4 exponents × 4 tolerances ✔
3. Every mismatch is EMPTY at `δ = 0` ✔
4. δ recorded in every row ✔ — **and 4b, on the INCAPACITY paths specifically** ✔ (this was a
   real gap in leg 382's module: the zero-crossing early return dropped `rel_tolerance`)
5. Widths non-decreasing in δ ✔

## 10. Where PART I was WRONG — recorded, not amended

* **§2.2's `δ*` closed form is `1–9.5 %` high.** The direction was predicted, the size was not
  quantified in advance. The continuum formula `exp(E∞(φ)) − 1` is an upper bound on the
  measured `δ*`, not an equality, and PART I did not say so.
* **§4's prediction that the rational cutoff "may be non-empty even at `p₀ = 1`" was right, and
  the reason PART I gave was right** — but PART I did not anticipate that this would make the
  *nominal* and *realised* readings of clause 2 answer in **opposite words**. That collision is
  the most decision-relevant thing in the leg and PART I only half-foresaw it.
* **PART I's `p₀` ladder listed `1.25`, which cannot be planted.** See §11.

## 11. Deviations from the brief and the pre-registration, disclosed

1. **`p₀ = 1.25` dropped from the clause-2 ladder** (pre-registered as `{1.0, 1.25, 1.5, 2.0,
   2.5, 3.0}`, run as `{1.0, 1.5, 2.0, 2.5, 3.0}`). The planted generators go through
   `ipow_half_integer`, which accepts only non-negative **half-integer** exponents by leg 382's
   deliberate design (the substrate has `ilog` but no `iexp`). This is a limit of the PLANTED
   PROFILE, not of the enclosure, which searches the continuum `p ∈ [0, 12]`. Nothing in either
   gate clause turns on the dropped point; the step at `α_centre = threshold` is located to
   `1e-6` by the crossover probes, not by the ladder.
2. **Two files added beyond the literal territory list**, both under this leg's own route name
   and colliding with no other slot: `experiments/p2_route_dtol_v1_evidence.py` (the
   JSON-only figure rebuild, which is the established convention for a registered figure — a
   figure registered in `build_figures.py` must point at an evidence script) and the
   BLOG/TECHNICAL pair, which the brief names but the territory list phrases loosely.
3. **`capabilities.py` NOT edited.** ORCHESTRATION §6 permits an agent to append to its own
   object's entry, but `capabilities.py` is not in this leg's territory list and the brief says
   a diff outside it fails the merge gate. The proposed replacement text for the
   `solver/dssp_decay_enclosure.py` entry is written out in TECHNICAL §7 and **routed to
   integration** instead.
4. **`CLAY_OBLIGATIONS.md` NOT edited** (forbidden). §4's δ paragraph needs a correction — its
   `δ*`-driven α demand is refuted by §7 above — and the exact proposed replacement text is in
   TECHNICAL §7, routed to integration.
5. **The external novelty endpoints refused** (`arXiv` and Semantic Scholar, both HTTP 429,
   banked verbatim in `writeup/novelty/leg_386.md` §3). No external prior-art result was
   obtained. No methodological novelty is claimed, so nothing rests on it, but the hole is
   real and is left open for a successor with network access — the same hole leg 382 left.

## 12. Carried forward, unchanged by either verdict

* **`CLAY_OBLIGATIONS.md` §6 items 1 and 2 stay OPEN.** Item 1's cutoff half is still entirely
  unattempted: this leg measured what tolerance the cutoff analysis could *afford*, not the
  cutoff analysis itself.
* **No profile of route 4's object exists in this repository.** Every input here is a planted
  analytic known. The clause-2 answer is a statement about *what any profile would have to
  satisfy*, not about a profile that has been produced.
* **CEILING TIER 2. No `L1 → L4` link moved. Clay stays ~0.05%.**

## 13. Left to a successor

* **Leg 389 (CT2C) consumes this mode.** The interface is: argument name `rel_tolerance`
  (unchanged from leg 382, deliberately — renaming a landed, capability-indexed argument would
  break the one consumer), **default `0.0`** (exact statement stays the default), and the
  recorded per-row fields `rel_tolerance`, `tolerance_mode` (`"exact"`/`"relative"`) and
  `predicted_width_exact_power_law`, present on **every** return path including all three
  incapacity paths. `cutoff_admissible_delta_window` is the composed-condition entry point.
* **The samples→cells conversion is still named and not provided** (leg 382's open item,
  untouched here): a caller with pointwise samples still needs a certified modulus of
  continuity or a stated monotonicity hypothesis.
* **The near-transition half-width law is not pinned.** H-shift's intercept is confirmed at
  `δ*`; its slope coefficient is not `0.434` and is measured only to within a factor
  `0.51`–`1.88`. A leg that needs `δ_max` sharply, rather than conservatively, would have to
  derive it.
* **The `α_centre > 1` requirement is now the load-bearing question for §4**, and it is not a
  question about tolerance at all. Nothing in this leg produces an `α_centre` for the real
  object.

---

## PART III — THE HYPOTHESIS FIELD (mid-leg amendment, after PART II was measured)

A coordinator amendment landed after the measurement and before the commit. It **adds to the
spec and changes neither gate clause**. Recorded here rather than folded into PART I, because
**a pre-registration is not amended to match anything** — PART I stands exactly as written.

### What was adopted

**Standing rule (DM cycle 11g, binding on every consumer of the enclosure chain): EVERY OUTPUT
ROW CARRIES THE HYPOTHESIS IN FORCE. A CERTIFICATE-WITHOUT-HYPOTHESIS IS NO CERTIFICATE.**

The finding behind it: leg 385's control **X3** planted a **secret** monotonicity violation and
was **accepted, as it must be** (a caller-declared hypothesis cannot be verified from samples).
Its certificate came out with width **`7.438494264988549e-15`** — **bit-indistinguishable from
the true certificate of the genuine planted known K1**. Same width, false about its profile.
The downstream audit caught X3 at `+5.1420e-2` relative in 1000/1000 cells, **but the
certificate itself was indistinguishable.** Only the recorded hypothesis separates them.

### What was implemented

`hypothesis` / `hypothesis_detail` / `conditional_on` on **every return path of every function**
in `solver/dssp_decay_enclosure.py`, bound in the same breath as `δ` and **before any early
return** — same discipline, one level up.

| path | recorded | status of the hypothesis |
|---|---|---|
| `certified_decay_interval` mode `cells` | `EXACT-INTERVAL-EVALUATION` | **discharged** by whole-cell outward-rounded evaluation |
| `certified_decay_interval` mode `nodes` | `NODES-ONLY` | the weaker mode declares its weakness **in the row** |
| `certified_decay_from_cell_enclosures`, declared | `MONOTONE` / `MODULUS` / `MONOTONE+MODULUS` | **declared** upstream, passed through unmodified |
| the same, undeclared | `UNDECLARED` | **never absent**; `conditional_on` says the row is not a certificate about any profile |
| all three INCAPACITY paths | recorded | including the zero-crossing return that had already been caught dropping `rel_tolerance` |
| `cutoff_admissible_delta_window` | inherits `EXACT-INTERVAL-EVALUATION` | a composed window is exactly as conditional as what it composes |

Strings are **byte-identical to leg 385's adapter** and duplicated rather than imported, to keep
the dependency one-way (adapter → enclosure); `test_hypothesis_strings_match_the_adapter` pins
the duplication. Tests **16 → 18**, leg 382's 12 still unweakened. Anti-tautology checks
**5 → 9**, the new ones checking that the field **separates what the width cannot**: a cells row
and a nodes row of the *same* profile must carry *different* hypotheses, and an undeclared
caller must come back `UNDECLARED` rather than inheriting one it never declared.

### What it did NOT change

**The runner was re-run in full and every measured value is bit-for-bit identical.** `δ*` to all
17 digits (`0.31569699828492215` / `3.352868184122399` / `0.06973929323837502`), ratios
`1.005051–1.005074`, coefficients `0.87283702` / `1.74567308`, window ratio `1.9999989`, widths
`2.220446049250313e-16` / `1.3322676295501878e-15` / `2.220446049250313e-16`, PASS_1a–1d all
true, 30 clause-2 rows with 0 exceptions. **CLAUSE 1 → YES. CLAUSE 2 → EMPTY.** Unmoved.

### Cited, not rediscovered (leg 385, banked)

* **P11 REFUTED and recorded as refuted, not amended:** a shifted grid does **not** detect a
  node-aligned wiggle — a uniform shift multiplies every sample by one constant. Detection is a
  **commensurability** effect: `N = 1100`, `1500` refuse; `N = 500`, `997`, `1001`, `1010`,
  `2000` do not. **A violating profile can hide from any *fixed* grid.**
* A globally-stated **`absolute` modulus is unusable across three decades** (341/1000 cells
  non-positive from `r = 207.97`) and must be stated in log–log.

Neither bears on how `δ*` was measured here — every input in this leg is an interval-evaluated
closed form, never a sample, which is precisely the case that records `EXACT-INTERVAL-EVALUATION`
— but both bear on any caller arriving with samples, and they are why the `UNDECLARED` sentence
is worded as strongly as it is.

### Deviation created by the amendment, disclosed

**`capabilities.py` was edited** (its `holds` and `validated` text for this leg's own module),
in the **same commit** as the module change, under the amendment's instruction and the standing
rule that a solver module is indexed in the same commit — a mid-rebase `HEAD` published one
without its capability row this cycle and reddened `test_every_solver_module_is_indexed` for
every agent. This departs from the dispatch brief's literal territory list. `CLAY_OBLIGATIONS.md`
was **not** edited; its proposed §4 replacement text stays routed to integration in
`TECHNICAL_P2_ROUTEDTOL_V1.md` §7.1.
