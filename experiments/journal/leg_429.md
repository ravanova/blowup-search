# Leg 429 — unit `R4` FAN-OUT ×5, arc 6 conductor pass: RE-DERIVE THE SPINE (wave 2)

**Date:** 2026-09-09. **Role:** MATHEMATICS (fan-out) + the Conductor's serial merge. **Wave 2.**
**Inputs:** the 58-node spine of `dag.json` (`r4_spine`, leg 426), the merged ledger (leg 428), the
banked page text, the PDF's geometry for garbled displays. **Outputs:** `writeup/data/arc6/spine/
agent_{1..4}.json`, `agent_5_verifier.json` (one file, one author, cherry-picked unedited), merged by
`experiments/arc6_spine_merge.py` → `spine/merged.json`. **Checks:** `writeup/arc6_spine_evidence.py`.
**Pre-committed gate and numbers:** `leg_426.md` §5 (≥ 35 of 58 `CHECKED`, ≥ 3 `GAP`, verifier
agreement ≥ 0.8). **The verifier's ten nodes were drawn blind with seed 428 and it never saw the
four agents' files**; the merge and this journal are the Conductor's and are `UNVERIFIED`.

## §1. THE GATE, in the charter's words

> *Per-node verdict, with the failing step quoted for every GAP; then the verifier's agreement rate
> with the four. Nodes the verifier independently reproduced are VERIFIED; the rest UNVERIFIED.*

| | CHECKED | GAP | NOT-CHECKED |
|---|---|---|---|
| agent 1 — §10, §3, summation, §5 (15 nodes) | 15 | 0 | 0 |
| agent 2 — §9, §8 (12) | 12 | 0 | 0 |
| agent 3 — §7, §6 (10) | 10 | 0 | 0 |
| agent 4 — §4, A, B, C (21) | 21 | 0 | 0 |
| **all 58 spine nodes** | **58** | **0** | **0** |
| verifier, blind, 10 sampled nodes | 10 | 0 | 0 |

**Verifier agreement: 10 of 10 (rate 1.0).** **VERIFIED (10):** `Lemma 4.4`, `Proposition 4.10`,
`Lemma 6.3`, `Lemma 8.8`, `Proposition 9.1`, `Lemma 9.8`, `Proposition 9.9`, `Lemma B.7`,
`Proposition B.8`, `Proposition C.3`. **UNVERIFIED (48):** every other spine node — `CHECKED` by one
agent, reproduced by nobody else.

**Failing steps quoted for every GAP:** there are none to quote.

**One sentence, as the charter asks:** *as far as five agents checked it — 58 statements, 480
reproduced steps, 301 recomputed constants, exponents and inequalities — the spine holds; the
charter forbids, and this journal does not draw, the conclusion that the theorem is correct.*

## §2. The pre-registered expectations, scored

| expectation (`leg_426.md` §5) | result |
|---|---|
| ≥ 35 of 58 `CHECKED` | **58** — met |
| ≥ 3 `GAP`, expected at (B.40)'s parameter order, Prop 9.6's closing margins, the uncited existence step | **0 — refuted.** Agent 4: the p. 40 chain and (B.40) **agree** (differences are grouping only). Agent 2: the closing rows evaluate to 0.40000, 0.39999, 0.49998, 0.17, 0.89996 against 0.1, smallest margin **0.07** in the tangential-mean row — tight but not a gap. Agent 1 and the verifier, independently: the existence of a state at every stage is supplied by Props 9.5/9.6 by induction, **never cited by number by Lemmas 9.7, 9.8 or Prop 9.9** — the same finding as `R3`'s, confirmed twice blind, and classed by both as implicit dependence, not a gap. |
| verifier agreement ≥ 0.8 | **1.0** — met |

## §3. What "CHECKED" bought, and what it did not

**Recomputed, not read** (a selection; every item is in the files with claimed and obtained values):
`λ₀² = 2aF₀²(1 − 2/v_s)` derived from the p. 76 reference matrix, real exactly when `v_s > 2`,
`c₀² = (v_s − 2)/2`; Lemma 6.2's `M_i = Q^{−hκ_s}S_*^{−ρ_g}` exactly because `d_r/2 = (1+h)ρ_g − hκ_s`;
the Diophantine bound (6.7) via the conjugates `(n₁+n₂)² − 2n₂²`; Prop 9.1's gain table (actual
minimum `1/2 − κ_s`, the stated `1/2 − 3κ_s` is conservative); Prop 9.5's actual stage-0 orders
0.99997 / 1.49998 / 1.89997 against the claimed 0.7 / 1.2; Lemma 8.7's two Vandermonde blocks
nonsingular for every `λ > 0`; Lemma 8.8's table minimum `α + 0.9`; Prop A.4's margins
`2.41²/8 = .726 < .74` and `4.82²/14 = 1.659 < 1.68`, `K_b ∈ (.240, .25]`, the (A.19) bracket
`−.0475 / .038`; Prop B.3's `305719/1152000` and `−75535511/400000000` as exact fractions; Prop
B.2's `(1+T)^{−1}1 = f₀(Yχ)` via the ODE `zf₀″ + 2f₀′ + f₀/2 = 0`; Prop A.10's `b_θ(0,η)` with
`g(0) = e`; Lemma 10.5's `‖K_R‖_{4/3}^{4/3} ≤ CR^{−1}` and all `A_R` powers strictly below 2;
Lemma 9.8's loss table and `ℓ_m = 2A + (m+1)(1 + 3h/2)`; Lemma 10.3's `2^{−j}` cutoff choice.

**Extraction artefacts resolved at the PDF: 53**, including three that read as errors until the
geometry was checked — Lemma 10.5's cutoffs are **powers** `φ_R⁸, φ_R⁷, φ_R⁴, φ_R³, φ_R²` (as
dilations `φ_{8R}` … the inequalities are mutually inconsistent; as powers every one is exact); p. 29's
`W_i` display (the fraction bar starts right of the minus sign); p. 116's *"2Ac∞"* is `2^A c_∞`, which
is what (4.29) requires. **R1's text loses two-dimensional layout; this is now measured three times.**

**CHECKED verdicts that carry a stated limit — eight, not re-labelled by me:** Prop 9.9 (Step 3's
closed-range bounds delegated to §§6–8 were confirmed *asserted*, not re-derived); Lemma 9.7
(bookkeeping); Lemma 9.8 (the phase bounds (7.3), (6.12) taken as cited); Prop 9.5 (Prop 7.5's
positivity read, not re-derived); Lemma 7.7 ((6.32) cited); Prop 4.2 (assumes (3.1)'s form of `R`);
Prop B.2 ((B.6) read); Prop 4.10 (the verifier did not re-derive Prop B.2's full contraction, Prop
A.10's factorization, or Prop C.2's `O(N^{−1})` estimates). **A reader who wants "CHECKED" to mean
"every step re-derived" should subtract these eight.** Each agent's re-derivation is bounded by
one context of 190k–350k tokens; the charter's own instrument for that bound is the blind
verifier, whose ten nodes agree.

## §4. Defects of this wave, banked
- **Agent 4's worktree lacked `manuscript_pages.txt`, `statement_index.json` and the PDF** and read
  them from the main checkout instead. The worktree was cut from the branch head at dispatch; the
  cause is not established here. It did not affect the outputs (the files are identical), but a
  dispatch whose inputs are not where the brief says is a defect and is recorded.
- **Zero GAPs across 58 nodes is the result and is also the number most worth distrusting.** The
  design's defence is the verifier; its sample is ten. A second blind sample in a later wave, on
  the eight limited nodes above, is the cheapest way to raise or lower that trust.

## §5. Not established
The theorem's correctness. Fifty-eight reproduced arguments, ten of them twice, is a strong
consistency check on the manuscript's spine and **is not a proof, not a certification, and moves no
wall.** Anything not reproduced blind stays `UNVERIFIED`. **No `L1→L4` link moved; Clay ~0.05%.**

## §6. Pre-commitment of the next wave (§3g)
**Wave 3 = `R6` FAN-OUT ×5 (Lean, measured not assumed)**: the five agents of the charter's R6
(build; top-level statement diffed quantifier by quantifier against Theorem 1.1; `sorry`/`axiom`
census on the dependency graph; `ComparatorChallenges/`; coverage of the 79 statements against the
merged ledger), inputs the leg-418 clone and `lean_build.log`; **composition floor met by the
Conductor's serial `R5`(i)–(ii)** running alongside (leg 430, pre-registered at `e01a64d`). Gate as
the charter words it, five answers separately, `NOT-ESTABLISHED` with a reason allowed, silence
not. **Wave 4 = `R5`(iii)–(vii)** on the interface `R5`(i)–(ii) pins — with the caveat, known from
`R5`(i)'s first numbers, that the paper's own parameter regime (`λ` after `P_* > e^{T_d}`) is far
from any grid; the pre-registration for (iii)–(vii) will have to say what is instantiated at
computable `λ` and what is measured only as a scaling toward the paper's regime.
