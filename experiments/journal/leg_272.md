# Leg 272 — ROUTE-WESCV: independent verification of leg 263's sweep and 8-site edit

**Agent:** LEG-A. **Branch:** `leg/272-wescv-v1`. **Date:** 2026-08-07.
**Verification leg**, read-only on everything leg 263 touched.

**GATE ANSWER: NO on clause (a); YES on (b), (c) and (d).**

Taking the no-branch verbatim: *Name the missed/mis-pinned site(s) precisely; a rework leg
applies per the standing mandate.* **The sites are named below, all 30 of them, each with its
triple and its consequence.** No stage is claimed. No ban is lifted. No route is promoted. No
link of the `L1 → L4` chain moves. Clay `~0.05%`.

**Every magnitude below is a count over committed blobs or a re-run of an instrument. Not one
number is re-derived, no `solver/` module is imported, and no shared ledger is opened for
writing — `plan_of_record.py` is diffed, never touched.**

---

## The headline, stated fairly before the defect

**Leg 263's correction is substantively sound and its arithmetic is exactly reproducible.** Its
8 edits are correct, its refusal to `sed` was right, the ban it was protecting is intact, and
its two published census numbers reproduce to the occurrence. What it does not have is the
**universal** its clause (a) asserts. Its census is incomplete by **30 triple-asserting
occurrences across 17 files**, from **two named mechanical causes**, and — this is the part
that decides the severity — **0 of the 30 produced a wrong edit, and exactly 1 lies inside its
declared territory.**

A census that under-counts a set it then declines to edit costs nothing directly. What it costs
is the warrant: leg 263's class P is published as 18 and is actually 28, and the ban's own
protection was argued from that list.

## Clause-by-clause

| clause | answer | magnitude |
|---|---|---|
| **(a)** zero missed occurrences | **NO** | **30 missed**, 17 files; 78 of 108 triple-asserting sites covered |
| **(b)** 8 sites correctly pinned + worded | **YES** | 8/8 journal-triple, **0 mis-pins**, 8/8 originals still standing, **0** class-P files modified |
| **(c)** `plan_of_record.py` byte-identical | **YES** | diff **0 bytes**, blob sha `846701e…` identical either side; **0 deletions leg-wide** |
| **(d)** `CORRECTIONS.md` entries 1–4 accurate | **YES** | **16/16** cited magnitudes re-located in their source legs |

---

## Clause (c) — the strongest result, and it is stronger than the clause asked

`git diff 1fbbd0f^ 7724e67 -- plan_of_record.py` is **0 bytes**. The blob sha is
`846701efdea4bb1ffdc9b124d99894d0928664c9` on both sides — not merely equal content, the same
object. Leg 263 touched **7 files** total across both commits, **0** of which is any of the five
shared ledgers.

The stronger fact, which leg 263's own gate did not claim: the leg is **`+608 / −0`**.
**Zero deletions across the entire leg.** Every correction is an *inserted* block, so no
pre-existing sentence anywhere in the repository was rewritten, reworded or removed. The
mark-don't-hide convention was not merely followed, it is mechanically verifiable from the
numstat alone.

## Clause (b) — the 8 edited sites, re-pinned from scratch

Each pinned here **without** using leg 263's reasoning, then compared. **8/8 agree. 0 mis-pins.
0 sites belong to the plan-triple.** All 18 class-P files are byte-identical across leg 263.

| site | this leg's pinning instrument | verdict |
|---|---|---|
| `leg_111.md:14` | self-pinning — this *is* leg 111, the weighted-energy leg, banking its own realization | journal ✓ |
| `leg_111.md:120` | same file, same argument | journal ✓ |
| `leg_141.md:16` | same-file local enumeration at `:194` | journal ✓ |
| `leg_141.md:19` | same-file local enumeration, corroborated by dating | journal ✓ |
| `leg_141.md:194` | **local enumeration** — *"the coefficient-basis, collocation and weighted-energy realizations alike"* | journal ✓ |
| `leg_141.md:201` | same-file local enumeration | journal ✓ |
| `leg_165.md:29` | **local enumeration**, re-located verbatim in the baseline blob — *"two genuinely independent realizations beyond leg 127's — the collocation basis (56) and the weighted-energy form (111/141)"* | journal ✓ |
| `leg_183.md:232` | no local enumeration; two independent instruments, both re-run — see below | journal ✓ |

**Leg 183 is the only non-trivial pin, and both of its instruments re-run clean.**
*Dating:* `git log -S"collocation basis leg 56" -- plan_of_record.py` returns exactly one
commit, `4ff544a`, and `git merge-base --is-ancestor` confirms `4ff544a` is a descendant of all
three of `7bd6c08` (leg 141), `84a5c7e` (leg 165) and `e8eb831` (leg 183) — so none of them can
be naming a set that did not yet exist. *Internal consistency:* leg 183's own body reads
*"`σ_min = 0` in `ℓ¹_w` at `s < 1` and a spectral gap of `1/2` after modulation on origin-`H²`"*
— it treats origin-`H²` as the realization where the operator is **alive**, so origin-`H²`
cannot be its dead third member. Both checks are independent of leg 263 and of the ruling.

**The wording is also correct, not merely the pins.** All five correction blocks were read in
full. Each states what over-read what, carries the ceiling verbatim, and closes by pointing at
`plan_of_record.py`'s different triple. Two judgements worth recording because they could
easily have gone the other way and did not:

- `leg_111.md:120` is **left standing** and that is right. The sentence — *"the third
  realization is provably measured on the same object the first two died on"* — is **true**;
  the matrix identity to `0.0` holds. Only the label is re-scoped, which the `:14` block does.
  Rewriting it would have corrupted a correct statement.
- The `leg_141.md:194` block corrects the **enumeration** and explicitly preserves the
  **ceiling**. Also right: the ceiling (`a = 0` CLM, not `HL_S2_nonsymmetric`) was never the
  thing that was wrong, and a correction leg that widened its own scope to the ceiling would
  have been the error the ruling was written to prevent.

## Clause (d) — `CORRECTIONS.md`'s four original entries

**16/16** cited magnitudes re-located in their source legs. Verified against sources, not
against leg 263's restatement of them:

- **Entry 1** — leg 111's gate reads literally `NO — 0 of 7`; its banked phrase *"third dead
  realization"* is present (twice); `1.677722e+07` and `−0.4999241` are leg 111's own; leg 178
  carries `0.499999667` (7×), `5.890e−07`, `7.409e−08` and `1.998e-15`; leg 141's reproduction
  column is `0.000e+00`. **Accurate.**
- **Entry 2** — `CORRECTIONS.md` says *"15 documents."* Leg 180's own gate answer reads
  *"Thirty-one passages across fifteen `writeup/` files, and eight `PHASE2_P2_NOTES.md` scope
  lines"* (4 primary + 11 derivative = 15). **Accurate**, with one precision note: it is a
  narrow restatement — it omits `PHASE2_P2_NOTES.md`, a 16th file carrying 8 further scope
  lines, and the passage count (31) is double the document count. Narrow, not wrong.
- **Entry 3** — leg 185's gate answers literally `SOLVER ARTIFACT`, *"named precisely enough to
  repair"* is leg 185's own wording, and its body reads *"partly, and `a`-dependently"* on
  reachability. **Accurate.**
- **Entry 4** — leg 178 exists at `ba1a22c`, landed by the user's ruling; the entry correctly
  marks itself as the *same closure as #1 reached by a different instrument* (measurement
  rather than literature). **Accurate.**

**One further claim of leg 263's, checked because it is load-bearing for a decision not to
edit.** Its Territory paragraph asserts `capabilities.py`'s leg-111 entry *"already carries the
`p = 1` scoping correctly."* **VERIFIED TRUE.** That entry's `validated` field reads *"the
window has ZERO width ON THIS UNCONSTRAINED ODD-SINE TRIAL SPACE (p=1 vanishing order)"* and
*"what is capped is reading them as a structural obstruction rather than a property of the p=1
trial space."* The unscoped phrase sits in the sibling `object` field. The assertion stands.

---

## Clause (a) — the defect, with both causes named

### First: leg 263's arithmetic reproduces exactly

Its two patterns were re-run over the same 1 000-odd `*.md` and `*.py` blobs at its own merge
base `7578fe3`:

| | this leg measured | leg 263 published |
|---|---|---|
| P1 occurrences / files | **76 / 39** | **76 / 39** ✓ exact |
| P2 *further* occurrences | **58** | **58** ✓ exact |
| P2 further files | **32** | 34 — **overstated by 2** |
| total swept | **134** | **134** ✓ exact |

The occurrence counts are exact. The only arithmetic discrepancy in the whole leg is a
**2-file overstatement** in the second sweep's file count, which bears on no pin.

### Second: reproducing them determines *how* they were run, and that is the defect

P1 reproduces at 76/39 **only case-sensitively**. P2's "58 further" reproduces **only
case-insensitively**. So leg 263 ran `grep` on P1 **without** `-i` and on P2 **with** it — an
asymmetry stated nowhere in its journal, and almost certainly unnoticed by its author.

**Cause 1 — CASE.** Every `THIRD realization` in an upper-case docstring, section heading or
ledger field was invisible to P1. Running P1 with `-i` and nothing else surfaces **5**
occurrences immediately: `DIRECTION.md:3402`, `DIRECTION.md:6141`, `capabilities.py:294`,
`experiments/p2_route_xul_v1_lit.py:16`, `solver/energy_coercivity.py:2`.

**Cause 2 — FORM AND WRAP.** P1's alternations are `three realizations` (space, plural) and
`third realization` (space, singular). **Neither can match the hyphenated singular compound**
`three-realization` / `third-realization` — which is, precisely, the form
`plan_of_record.py`'s own lift condition uses. And a line-oriented grep still cannot see the
phrase where the 100-column hard wrap splits it across `three dead / realizations`, which
accounts for **9** of the 30. Leg 263 diagnosed the wrap hazard correctly and recorded it as
standing; it did not follow the diagnosis with a wrap-immune instrument. This leg's whole-file
normalized scan is that instrument.

### The 30 missed occurrences, graded

**Plan-triple — 10.** `plan_of_record.py:868` · `DIRECTION.md:9523, :9602, :10075, :10651` ·
`experiments/JOURNAL.md:3639` · `experiments/journal/leg_256.md:36` ·
`experiments/journal/leg_262.md:315` · `writeup/novelty/leg_255.md:26` ·
`writeup/novelty/leg_262.md:152`.

**0 edits were made to any of them and 0 could have been** — `plan_of_record.py` has a 0-byte
diff and every other site is outside leg 263's territory. The set the live stage-`V` ban rests
on is intact. What is defective is the census: **class P is published as 18 and is actually 28.**

**Journal-triple — 17.** `capabilities.py:294` · `solver/energy_coercivity.py:2` ·
`DIRECTION.md:1856, :3401, :3402, :3427, :3786, :4340, :4929, :5448, :6141, :10096` ·
`experiments/journal/leg_141.md:203` · `experiments/journal/leg_178.md:128` ·
`experiments/p2_route_wes_v1_space.py:377` ·
`writeup/4_p2_lottery/TECHNICAL_P2_WES_V1.md:29` · `writeup/novelty/leg_144.md:65`.

**16 of 17 are outside declared territory** and would have joined the J-recorded list of 45,
edited nowhere, even had the grep seen them. Four of the 17 are one and the same quoted string
— leg 178's own gate no-branch, *"a genuine third-realization revival"* — which is **about**
the revival and is correct as written. Eight are `DIRECTION.md` historical leg specifications,
the class leg 263 explicitly and correctly declines to edit. One
(`writeup/novelty/leg_144.md:65`) is a **quotation** of `capabilities.py`, which leg 263's own
rule forbids correcting.

**Single member or a different sense — 3.** `writeup/novelty/leg_113.md:167` names **one**
member (the `ℓ¹` tail estimate), common to both triples, so no pin is required — leg 263's own
class C. `experiments/p2_route_xul_v1_lit.py:16` is **Xu's own "Map of realizations"**, the
third distinct sense leg 263 classified as class N at *other* line numbers in the same file, so
the sense was censused even though this occurrence was not. `experiments/journal/leg_154.md:134`
(*"all three integer bands, C3's realistic near-miss"*) is a **false positive of this leg's own
pattern**, reported rather than silently dropped.

### The single in-territory miss

**`experiments/journal/leg_141.md:203`** — the only one of the 30 inside leg 263's declared
territory:

> *"**three dead realizations**, of which two (`ℓ¹` no-go, discrete-ball trap — leg 65) are
> confirmed novel and the third (weighted-energy zero-width window — leg 111) is pre-empted and
> must be de-rated to a statement about a trial-space choice."*

It is a **local enumeration** naming weighted-energy as the third member, it is wrapped across
lines 203–204 (hence invisible to a line-oriented grep), and it sits **below** leg 263's
correction block, which lands at `:215` post-edit. It is uncovered, and it is the one site a
rework leg should carry a pointer to.

**Its severity is low, and measurably so:** leg 141's own sentence continues *"…and must be
de-rated to a statement about a trial-space choice."* Leg 141 had already de-rated the third
member in the very sentence that counts it. **The count is stale; the reading beside it is
not.** A reader of that sentence does not come away believing something the repository now
knows is false — which is `CORRECTIONS.md`'s own admission test, failed.

## The escalation trigger, flagged and bounded in the same breath

This leg's mandate says a missed site is a normal landing *unless the miss touches
`plan_of_record.py`'s own ban text*. **It does**, once: `plan_of_record.py:868` is inside the
re-posed stage-`V` ban's **lift condition** — *"…with its own scoping leg establishing it is
not subject to the same **three-realization death**."* So it is flagged.

**And the direction of the miss is safe, which the DM should weigh with it.** The occurrence
was left byte-identical. It pins unambiguously to the plan-triple by *local enumeration* — the
ban's three members are spelled out seven lines above it at `:861–862`. A census that had caught
it would have classified it **P** and edited **nothing**. The ban's text, its members and its
lift condition are all exactly as leg 263 found them. What the miss costs is that leg 263 argued
the ban's protection from a class-P list that was 10 occurrences short of complete — a warrant
defect, not a text defect.

## What a rework leg would actually do

Small, and bookkeeping rather than finding. **One in-territory pointer** at
`experiments/journal/leg_141.md:203`. **A census correction** recording class P as 28 and the
second sweep's file count as 32. Optionally, folding the 16 out-of-territory journal-triple
sites into the J-recorded list of 45 that leg 263 already handed forward for exactly this
purpose. **Nothing about the ban changes, because nothing about the ban was ever wrong.**

**And one standing instrument rule, which is this leg's real product:** a phrase audit in this
repository must be run **case-insensitively**, must include the **hyphenated compound** form,
and must be run on a **whitespace-normalized whole-file stream** rather than line by line. Leg
263 recorded two instrument failures; this leg found two more of the same kind and one of its
own (see the novelty pass — the first-draft lexicon `realis?a?tions?` cannot match
`realizations` at all, and returned 0 hits while looking clean). Five instrument failures
across two legs on one phrase family is the finding worth carrying forward.

## Ceiling

**New to the world: nothing. New to this repository: one methodological rule.** This leg
measures counts over blobs; it derives no constant and re-runs no mathematics. Leg 178's
revival, leg 165's classification and leg 111's measurements are all exactly as they were.
**"Not dead" is still not "open."** The object under all of it is the `a = 0` CLM linearisation
whose `Y₀` is exactly zero for the banned degenerate reason; nothing here is a statement about
`HL_S2_nonsymmetric`. No ban lifts, no route is promoted, no link of the `L1 → L4` chain moves.
Clause S7 binds unchanged. Clay `~0.05%`.

## Territory

`experiments/p2_route_wescv_v1_verify.py`, `writeup/data/p2_route_wescv_v1_verify.json`,
`writeup/novelty/leg_272.md`, this file. **Four files, all inside the declared territory.**
`plan_of_record.py`: **diffed, never opened for writing.** No shared ledger touched. Nothing
leg 263 wrote is edited by this leg — the single in-territory miss is **reported**, not
repaired, because repairing it is out of this leg's territory.

## Reproduce

```
.venv/bin/python experiments/p2_route_wescv_v1_verify.py
git diff 1fbbd0f^ 7724e67 -- plan_of_record.py      # empty            -> clause (c)
git diff --numstat 1fbbd0f^ 7724e67                 # +608 / -0        -> 0 deletions
git rev-parse 1fbbd0f^:plan_of_record.py 7724e67:plan_of_record.py   # same blob sha
```
