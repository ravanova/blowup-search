# Leg 268 — ROUTE-PUB2R: correcting PUB2's `‖T⁻¹‖_X` figure and the two proved-wrong banked values

**Gate: YES on all four clauses. BANKED.** Submission-blocking rework leg, no computation, no
solver module, no GA compute. Branch `leg/268-pub2r-v1`. Date 2026-08-07.
**Leg 176's banked JSON is byte-for-byte unmodified; leg 176's gate-answer text is untouched.**

**Flag to the orchestrator, as the gate's yes-branch requires: PUB2 needs a fresh §7b review
pass before any actual submission.** Seven sentences in the technical note and two in the blog
changed; the §7b self-audit that signed off the previous wording did not see any of them.

---

## 1. The four gate clauses, answered

| clause | answer | evidence |
|---|---|---|
| (a) every site quoting `4.026` / truncation-independence states only what leg 249's certified data supports, trend stated honestly | **YES** | 5 sites in `TECHNICAL_P2_PUB2_V1.md`, 2 in `BLOG_P2_PUB2_V1.md`; post-edit grep for `truncation-independent` leaves 2 hits, both about `ℓ¹_w`'s §2 side (leg 127's finding), 0 about the origin-`H²` numbers |
| (b) the two proved-wrong banked values corrected via an explicit, cited correction artifact; no silent hand-edit | **YES** | `writeup/data/p2_route_h2c_v1_construction_correction_leg268.json`, 3 correction records `K1`/`K2`/`K3`, each citing branch `leg/249-h2cv2-v2` @ `e9db984` and its certified data file. `git diff` shows **0 bytes** changed in `p2_route_h2c_v1_construction.json` |
| (c) leg 259's two absorbed nits fixed | **YES** | §3.5's "exactly `1/1.3993`" now discloses the `7.0e−06` round-up (§3.2 already did); §3.1's conjunct-table cell no longer presents a proved floor as verified |
| (d) no other sentence's meaning changed | **YES** | 9 edited sentences + 1 new footnote + 1 new file; every other line byte-identical |

**The escalation clause did NOT fire, and the reason is checkable rather than asserted.**
The no-branch says: escalate if PUB2's *argument* depends on truncation-independence at
`4.026`. It does not. `4.026` occurs at exactly **one** site in the whole document (old L303),
and what the argument uses there is the **converges-vs-diverges contrast** with `ℓ¹_w` (legs
51/53). Leg 249 states the same conclusion independently: *"convergence to 4.03 makes that
contrast exactly as strong as convergence to 4.026 would."* No downstream section — §4.5's
scale table, §5's four-part statement, §3.3's ceiling — reads the tail number for anything but
finiteness. So the correction is a precision-and-wording rework, which is what banks.

## 2. The magnitudes carried, all of them leg 249's

Nothing in this leg was measured here. Transcribed with attribution:

| quantity | leg 176 banked | leg 249 certified | discrepancy |
|---|---|---|---|
| `σ_min` at `N = 512` | `0.09080465147034879` | `∈ (0.090804094, 0.090804194)`, exact enclosure | **proved wrong** from the 7th significant figure; `5.08e−7` absolute |
| `‖T⁻¹‖_X` at `N = 512` | `4.02614534796022` | `∈ [4.026239930769523, 4.026241551830974]` | **outside the bracket**, `6.0e−5` relative |
| `‖T⁻¹‖_X`, the descriptor | "truncation-independent" | rises `3.994032 → 4.028864` over `N = 64…1024`, **+0.865 %**, still rising; decrements `1.126e−3 → 5.745e−4 → 3.027e−4 → 1.617e−4`, ratios `0.510/0.527/0.534`; limit ≈ **4.0318** | **false as stated**; `4.026` understates the limit by **0.14 %** |

The `σ_min` verdict contains no rounding: `AᵀG_cA − λG_d` at `λ = (0.09080465147034879)²` is
**not positive definite** in exact rational arithmetic, so `σ_min` is strictly below the banked
value. That is why the correction is stated as *proved*, not as a second opinion.

## 3. What was edited, site by site

`writeup/4_p2_lottery/TECHNICAL_P2_PUB2_V1.md`:

1. **§1 summary table, origin-`H²` row** — `(σ_min = 0.0908, truncation-independent)` → the
   measured flatness (`0.139 %` over a 16-fold range) with "evidence of a positive limit, not a
   proved floor". *(Absorbed 259 nit's sibling site; the phrase was the same overclaim.)*
2. **§3.1 conjunct table, "no §2-class obstruction" check cell** — was `σ_min` *bounded away
   from zero and truncation-independent*, presented as verified. §3.5 already withdrew exactly
   that; the cell now says what §3.5 says and points at it. **(259 nit (b).)**
3. **§3.5 ladder table, `N = 512` cell** — `0.0908047` → `0.090804`, the six figures the
   computation supports, plus a new footnote carrying the certified enclosure, the whitening
   mechanism, the fact that a Cholesky whitening stays monotone through `N = 2048` (so leg
   176's reliable window is **wider** than it claimed), and a link to the correction artifact.
4. **§3.5 tail sentence (the one leg 249 named as "the one that must change")** — the whole
   ladder, the relative rise, the geometric decrements, "float64 evidence of a finite limit
   near 4.032, not a proof of one, and **not** a truncation-independent value", an explicit
   note that earlier drafts printed `4.026, truncation-independent`, and the surviving
   converges-vs-diverges contrast stated as the thing the number is there for.
5. **§3.5, new parenthetical** — the certified bracket at `N = 512`, the banked value's
   position outside it, and the observation that leg 176's own JSON `reading` field already
   said `4.03`.
6. **§3.5 provenance paragraph** — "`0.71465` is **exactly** `1/1.3993`" → "`1/1.3993` rounded
   up in the fifth decimal (`1/1.3993 = 0.7146430`, a round-up of `7.0e−06`)", cross-referencing
   §3.2 which already disclosed it. **(259 nit (a).)**
7. **§3.5 flattening figure** — `0.0908878 → 0.0908047`, `0.0915 %` → `0.090804`, `0.0920 %`,
   recomputed against the supported value.
8. **§5 item 3** — "truncation-independent to 1.44e−04" conflated the quadrature-**window**
   spread with truncation independence. Now: a ladder that flattens (`0.139 %` over 16-fold),
   window spread `1.44e−04`, **no proved floor**.

`writeup/4_p2_lottery/BLOG_P2_PUB2_V1.md` (leg 249 named L190; L104's sibling sentence had the
same defect and would have contradicted the fix):

9. **"the independent re-derivation we commissioned has not reported"** → it has, in exact
   rational arithmetic; it confirms the construction and the verdict and corrects digits, with
   the `4.03`-not-`4.026` point stated in the blog's register.
10. **closing caveats, "with the independent check still outstanding"** → the check has
    reported, confirms both halves, corrects digits below the ones quoted.

## 4. What was deliberately NOT done

* **`writeup/data/p2_route_h2c_v1_construction.json` not touched.** DIRECTION.md forbids it and
  the standing discipline forbids it. A regeneration would need a leg owning leg 176's
  territory; the correction artifact says so explicitly and tells that future leg to cite leg
  249's brackets.
* **`experiments/journal/leg_176.md` not touched** — outside territory, and leg 249 confirms its
  gate answer stands. It still contains the `4.026`/"truncation-independent" wording and the
  `1.29e−14` vs `1.4296e−14` prose slip leg 249's W7 found (a **9.8 %** discrepancy touching
  neither gate number). **Both are live, unfixed, and out of this leg's reach.**
* **`capabilities.py` not touched.** Grepped first, as required: one hit, leg 176's own row. Its
  `validated` field records `1.6e-13`, `2.6e-15`, `1.3e-16` and **neither proved-wrong value**,
  so the ledger is not stale and no append-only row is warranted. Also outside territory.
* **Leg 249's own artifacts not merged or cherry-picked.** Its branch is read-only here; its
  data file lives on `leg/249-h2cv2-v2` and the correction artifact cites it by commit rather
  than copying it into `main`. Whether 249's branch should itself land is the DM's call, not
  this leg's.
* **No claim upgraded.** Leg 249's two findings that make leg 176's case *stronger* are recorded
  in the artifact and one is footnoted, but PUB2's argument is not strengthened on the back of
  them (see `writeup/novelty/leg_268.md` §4).

## 5. Out-of-territory sites a §7b pass should look at

Grep for `4\.026|0\.09080465` across `writeup/` and `experiments/journal/` after the edits
returns, besides this leg's two files: `experiments/journal/leg_176.md` (the origin of the
wording), `writeup/novelty/leg_238.md`, `experiments/journal/leg_238.md`,
`writeup/novelty/verify_250.md`, and three data files whose `4.026` is unrelated
(`blowup_curve.json`, `p2_route_ngx_v1_general.json`, `p2_route_m_v1_targets.json`). **Only the
first four are real quoting sites, and all four are outside this leg's declared territory.**

## 6. Ceiling

Correcting what a document prints changes nothing about what was proved. `a = 0` only; float64
and exact rationals, nothing interval-enclosed in the interval-arithmetic sense; what is
certified is invertibility of an operator Xu already inverts in closed form;
`HL_S2_nonsymmetric` inherits none of it (leg 163 census item **O3**, fatal for transfer).
**No link of the `L1 → L4` chain moved. Clay odds unchanged at ~0.05%. No ban lifted. No GA
compute.**
