# Leg 432 — unit `R5`(ii), arc 6 conductor pass: THE RESIDUAL STRESS THE OUTER PROFILE LEAVES

**Date:** 2026-09-10. **Role:** NUMERICS, SERIAL, the Conductor's (the composition-floor unit beside
wave 3). **Pre-registration:** `leg_432_prereg.md` (`96657de`) and its **amendment before any number**
`leg_432_prereg_amend.md` (`fcac005`), both before the runner existed. **Runner:**
`experiments/arc6_residual_v1.py` (`--full`, 121 s). **Artefact:** `writeup/data/arc6_residual_v1.json`.
**Checks:** `test_arc6_residual_v1.py` (the six controls on a coarse grid), `writeup/arc6_residual_evidence.py`
(every number below re-derived from the artefact; draws `fig114`). **Inputs:** the `λ = 0.1` tail of leg
430 (`log₁₀ X_tail = 206.8`, `c_∞ = 1.25·10⁻³²`); `h ∈ {10⁻⁷, 10⁻³}`; `c_o = 0.1`. **Everything here is
`UNVERIFIED`** — one author, no blind reproduction — and **this is Tier 2, not a proof.**

## §1. THE GATE, as pre-registered and amended: H0–H7 each `YES`/`NO` with its number

| gate | what | `h = 10⁻⁷` | `h = 10⁻³` | answer |
|---|---|---|---|---|
| **H0** two routes to `T_0` — (4.11) on Lemma A.8's backward moments vs the paper's (A.54)/(A.46)/(A.53) at explicit `q` | rel. diff. of the inviscid bracket `𝒜`, the sub-dominant bracket `ℬ`, and `T_z` | `1.2·10⁻⁹`, `3.2·10⁻¹⁰`, `1.5·10⁻⁷` | `1.2·10⁻⁹`, `3.2·10⁻¹⁰`, `1.5·10⁻⁷` | **`YES`** |
| **H1** sign | `min 𝒜`, `min ℬ` on `y ∈ [½, 2.95]`; the three (A.54) terms `≥ 0` | `2.9·10⁻⁸`, `3.8·10⁻⁸`; all `≥ 0` | `2.9·10⁻⁴`, `3.8·10⁻⁴` | **`YES`** |
| **H2** (A.48) | (i) boundary term ÷ `b_θ(0,η) = 16ρ_oE_pow(X_b)g(0)/√(2X_b)` at `δ = .4, .2, .1, .05`; (ii) local `δ`-power of `e^{4/δ²}T_{0,θ}` at `δ = .05`; (iii) boundary fraction of the total; crossover `δ_×` | 2.66, 1.55, 1.23, **1.11** → 1 from above; **0.051** (`δ⁰`); `10^{−203}`; **`δ_× = 1.1·10⁻⁶⁹`** | same to 3 digits | **`YES`** as amended; the original "`T̂_θδ³ → b_θ(0,η)`" **NOT TESTABLE** |
| **H3** (A.49)–(A.50), (A.55) | local `δ`-power of `|T_z/T_θ|` at `δ = .05`; `C = sup |T_z/T_θ|/E_pow` | **3.02** (`δ³`); `C = 0.49` | 3.02; 0.49 | **`YES`** as amended; the original `[5.5, 6.5]` **NOT TESTABLE** |
| **H4** (A.56) `2 + h < a ≤ 2 + 2h` | `a − 2` on the tail | `[1.20h, 2.00h]` | `[1.20h, 2.00h]` | **`YES`** |
| **H5** admissible cone | `P_c − v_s = T_{0,θ}/F > 0`; `log₁₀ sup (a−2)(T_z/T_θ)²` | `> 0`; `−278` | `> 0`; `−275` | **`YES`** |
| **H6** scaling in `q` | `q^{A+½}T_θ`, `q^{A+½}T_z` at `q = 1, 10, 10³` | `≤ 1.2·10⁻¹⁵` | `≤ 1.6·10⁻¹⁵` | **`YES`** |
| **H7** support, (A.51) | `𝒜 = ℬ = 0` and the `T_z` bracket `= 0` for `y ≥ 3`; `δ³∂_δ log T_{0,θ}` at `δ = .05` | exact zeros; **8.0001** | exact; 8.0001 | **`YES`** |
| H8 (reported) the inner edge `X_a` | — | **could not determine** from the tail alone: `𝒜(y) > 0` at every `y < 3`; `X_a` is fixed by the axis construction (Prop B.2, Cor B.10), which this unit does not build | | — |

**The one sentence:** *Proposition A.10's residual stress on the terminal tail — its sign, its `e^{−4/δ²}`
outer weight, its vanishing beyond `X_b`, its shear bracket, its `q`-invariance and its direction bound
(A.55) — is reproduced from the paper's own formulas by two independent routes that agree to `10⁻⁹`;
its quoted `δ`-powers (`δ⁻³`, `δ³`, `δ⁶`) are the limit on a collar `δ ≲ 10⁻⁶⁹` that no grid reaches, and
at every resolvable `δ` the paper's own Lemma A.9 predicts, and the runner measures, `δ⁰`, `δ³`, `δ³`.*
The paper's *conclusions* (positivity, direction `→ (1, 0)`, smooth extension, strict cone slack) hold
either way. **`YES`, with the two limit statements marked `NOT TESTABLE` and the arithmetic of why.**

## §2. The six planted controls K1–K6 (`h = 10⁻³`), each against its unmodified twin — all fired
| control | must fail | did it | number |
|---|---|---|---|
| **K1** polynomial cutoff, no flat factor | H7 slope, H2 | **fired** | `δ³∂_δ log T = 0.011`, not 8; boundary ÷ `b_θ(0)` far from 1 |
| **K2** `D = A` in the axial scaling | H6 | **fired** | `T_z` changes by `1.4·10⁻²` between `q = 1` and `10³` (`≈ 2h log q`); `T_θ` does not |
| **K3** residual angular moment `ε = 10⁻³` | H7 | **fired** | `𝒜(y ≥ 3) = 10⁻³` |
| **K4** reversed cutoff | H1 | **fired** | `min 𝒜 = −3.0·10⁻³` |
| **K5** `h → −h` | H4 | **fired** | `a − 2 = −1.2·10⁻³ > 2h = −2·10⁻³` |
| **K6** no heat factor (new, from the amendment) | H7 | **fired** | `ℬ(y ≥ 3) = −2.002 = −(2+2h)`: the power law's own viscous residual |

K1's `H0 = NO` is the control's kink at `y = 1` (`f_o′` jumps from 0 to `2ρ_o`) under Simpson's rule,
not a route disagreement; it is not a pre-registered expectation of K1 and is recorded, not scored.

## §3. What the amendment changed and why it is not a widening (`CORRECTIONS.md` §70)
Writing route A by hand before the runner existed showed the heat factor must be kept to first order
(with it `X Q_s → (2+2h)L` beyond `X_b` **exactly**, measured here as `ℬ = 0` to the last digit; without
it `−(2+2h)`, control K6) and that the inviscid term is `X ≈ 10^{207}` times the boundary term — so
(A.48)–(A.50)'s powers are the `δ → 0` limit. The gates were amended to test the paper's Lemma A.9
where a grid can reach it and to record the limit as `NOT TESTABLE`; no tolerance moved. Four runner
defects were fixed after the quick run, none a tolerance (§70). **Leg 430 corrected:** with the (A.5)
step's derivative 8, `c_o = 0.1` gives `f_o′/f_o = 0.40h`, not the `< h/4` its pre-registration
claimed; the paper's `h/4` needs `c_o ≤ 1/16`; (A.56) needs only `2f_o′/f_o < h` and holds.

## §4. What this leg does NOT establish
The annulus's inner edge, anything about the axis profile, the correction cycle, or the theorem. It
does not touch leg 430's `G3`/`G7` finding (the tail is closure-independent under Lemma A.8's imposed
moment conditions). **This is Tier 2, not a proof. No wall moved; no `L1→L4` link moved; Clay ~0.05%.**

## §5. Pre-commitment of the next wave (§3g)
**Wave 4 = `R5`(iii)–(vii) FAN-OUT ×5** on the interface pinned by legs 430 and 432: the `λ = 0.1` outer
profile (constants, exponents, pressure datum reproduce; closure and cone do not at computable `λ`)
and its tail stress. Four workers, one adversarial verifier drawn blind who never sees the four;
every brief carries pre-registered gates, planted controls both ways, and the sentence *this is Tier 2,
not a proof*; every gate answer must say what is instantiated at computable `λ` and what is measured
only as a scaling toward the paper's regime. Then `R7` lands in `writeup/6_reproduction/`.
