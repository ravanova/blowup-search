# LEG 407 — UNIT `V-W6`: THE VERIFICATION OF WAVE 6

**Unit:** `V-W6` · **Lane:** verification · **Wave:** 7 · **Date:** 2026-08-19
**Covers:** WAVE 6 ONLY — `L6` (`e62c449`), `V5` (`dacc01c`), `V-W5` (`95cf861`), and the
Conductor's own landing audit in `writeup/waves/WAVE6_CLOSE.md`.
**Artefact:** `writeup/data/p2_verify_wave6_v1.json` (`self_hash 7116301a5abd3d82`)
**Evidence:** `experiments/p2_verify_wave6_v1_evidence.py` — 65 checks, 0 failures, exit 0.

---

## §0. PRE-REGISTRATION

### §0.1 Why this unit exists at all

§3g: *"The one thing the Conductor may not do: verify its own waves. It planned them, so it is
not independent of them."* The Conductor planned wave 6 and says so in `WAVE6_CLOSE.md`'s own
opening — *"I planned this wave, so I did not verify it"* — and labelled `L6` `UNVERIFIED`. This
unit is that verifier, dispatched in wave 7 so it could not be briefed by the construction it
checks.

**I am not here to confirm.** A verifier that finds nothing has told us nothing unless it says
precisely what it checked and how hard. So this leg records, for every judgement, the thing I
actually executed — and, under lesson 68, an explicit priced list of what I did **not**.

### §0.2 The standard

Each unit is judged against the gate **committed before it ran** — `writeup/waves/WAVE6_PLAN.md`
at commit `e202653`, which predates every worker branch — quoted verbatim in the artefact
alongside the delivered answer. Never against what a unit chose to report afterwards.

Verified from git that this standard is available: all three worker branches base at `5c49486`
and their first commits are pre-registrations carrying no result numbers (`e4598a5` for `L6`,
`a7e61e8` for `V-W5`, `eb756b1` for `V5`). No gate wording moved after dispatch.

### §0.3 The bound, and why it is enforced in code

Three sibling construction units (`R-bank`, `R-prof`, `L6-b`) were committing into this same
working tree while this leg ran. Their changes are wave 7 and are not mine to verify. My bound is
commit **`2a5ea0d`**.

I did not merely *promise* to respect the bound — the evidence script **reads every repository
file through `git show 2a5ea0d:<path>`**, including `SOURCES.md`, `requirements.txt`,
`leg_401.md`, `WAVE6_CLOSE.md`, `p2_route_l6_v1.py`, `p2_route_l6_v1_evidence.py` and all three
banked artefacts. It therefore returns the same verdicts no matter what has landed since. This
was not decorative: my first run against the working tree produced a spurious `FAIL` on `D14`
because unit `L6-b` had added a `SOURCES.md` row **after** my bound. Reading at the bound removed
the artefact of concurrency without hiding the finding.

For the record I also checked that the wave-6 files themselves have not moved: the three
artefacts, `L6`'s solver, `L6`'s evidence script, `leg_401.md`, `requirements.txt` and
`WAVE6_CLOSE.md` are **byte-identical** at `2a5ea0d` and at the time of writing. Only
`writeup/SOURCES.md` has changed.

### §0.4 Territory

`writeup/data/p2_verify_wave6_v1.json`, `experiments/p2_verify_wave6_v1_evidence.py`,
`experiments/journal/leg_407.md`. Nothing else. **Eleven defects are open below and I repaired
none of them** — reporting, not fixing, is the gate. Every mutation test was run against a
symlink mirror under the session scratchpad; the repository was never modified to test it.

---

## §1. WHAT I ACTUALLY EXECUTED

Three unit evidence scripts, re-run by me from the merged tree rather than read from any report:

| script | result | exit |
|---|---|---|
| `experiments/p2_route_l6_v1_evidence.py` | **30 checks, 0 failures** | 0 |
| `experiments/p2_route_v5_v1_evidence.py` | **75 checks, 0 disagreements** | 0 |
| `experiments/p2_verify_wave5_v1_evidence.py` | **120 checks, 0 failures** (121 `[PASS]` lines) | 0 |

Three `self_hash`es recomputed under the literal stated rule, all matching:
`6a033004deef39d3` (`L6`), `ae2b95efcbe5209b` (`V5`), `da8a0d7cb9fb4896` (`V-W5`).

Territory derived from `git diff --name-status 5c49486 <tip>`, **not** from any unit's own
claim: `L6` 7 files all `A`; `V5` 5 files all `A`; `V-W5` 3 files all `A`. Zero `M`, zero `D`
anywhere in wave 6.

Five mutation tests against the units (§4), three against my own script (§6).

Headline numbers recomputed from raw where the artefact carries it — most importantly, `L6`'s two
refinement rates re-derived by an **independent pure-python OLS** rather than the `numpy.linalg.
lstsq` the unit used (agreement to 1e-10 relative), and `L6`'s §5.1 decision rule re-applied from
scratch to reproduce the `NO` on both branches.

---

## §2. THE VERDICTS

| unit | verdict |
|---|---|
| `V5` | **VERIFIED** |
| `V-W5` | **VERIFIED** |
| `L6` | **VERIFIED-WITH-QUALIFICATION** |

### §2.1 `V5` — VERIFIED

Both clauses answered **separately**, which is the thing the gate was most emphatic about
(*"MUST NOT be folded into the clause-1 verdict"*). Clause 2 carries `answered_standing_alone =
true`, locates W2's predicate in the record at `p2_route_t4_v1.json :: controls.C_3D` with
`invented = false`, and I re-derived the 57.25% swirl fraction from the two banked component
maxima. Clause 1's four broken constants each carry printed value, recomputed value **and** the
verbatim failing clause, as demanded; `H30`'s diagnosis — `η₂ = 0.005` substituted where the
certified `M_2^U ≤ 0.0061` belongs — is a mechanism, not a discrepancy report. The wave-4 repair
landed as `_v2` deltas with no `_v1` edited, confirmed from git status letters.

**One scoping note, which is not a defect.** Clause 1's *"the certificate DOES close as claimed"*
is conditional on 22 Class-A interval-arithmetic outputs taken as given. The artefact enumerates
all 22, banks the condition under `what_this_audit_did_NOT_establish`, and the Conductor carried
it into `STATE.md` and named it in `WALLS.md`. This is a disclosed scope, correctly disclosed at
every level, and it is the single most important thing to keep attached to `V5`'s `YES` — it is
attached.

### §2.2 `V-W5` — VERIFIED

All five items answered, including **both halves of item 3** (*was C6's tolerance ever moved*
**and** *does anything the `NO` rests on depend on the constant C6 perturbs*) — the second half is
its own field, not folded in, and it is the half most easily dropped. `defects_repaired = 0` and
`banked_artefacts_edited = 0`, verified independently from git rather than taken on its word: 3
files, all `A`. For a `REPAIR NOTHING` gate, that git check *is* the verification.

Two cosmetic notes recorded so a future verifier does not re-find them: it banks 120 checks while
printing 121 `[PASS]` lines (the 121st is its own self-hash check, emitted after the summary and
outside the counter), and item 3's `git log --all -p --follow` subprocess raises an uncaught
`CalledProcessError` outside a checkout instead of reporting a `FAIL`.

### §2.3 `L6` — VERIFIED-WITH-QUALIFICATION

**The gate is answered, exactly, in the gate's own terms, with the gate's own numbers.** Every
clause of it: divergence-free field (identically, by construction); named realization (backward
λ-DSS, λ = 1.7, a = 0.5, `T_s = 1.0612565021243408`); named trial space and basis (Chandrasekhar
poloidal–toroidal; `Y_lm` × Chebyshev on `u = r/(r+2)` with `u^l` and `u^l(1−u)` prefactors ×
real Fourier in `s`); **measured** residual, not bounded; the norm read **verbatim** from `L5`'s
banked JSON rather than paraphrased; `ρ = 1.613811231995397` at `n_dof = 6720`;
`decreases_under_refinement = NO`; the rate `−0.0222` over the last three rungs and `−0.2126` over
all five. The §5.1 decision rule was fixed at `e4598a5` **before any number existed** and was not
softened afterwards — plan reading (b) is met, and I checked it by re-applying the rule from
scratch. Plan reading (d) fired: `UNDER_RESOURCED = true` with a named ~10³ core-h price. Ban C1
is clean — no `Y₀/Z₀/Z₁/Z₂` anywhere, and the apparatus is named at §0.3 before the run.

The artefact is fully re-derivable. `C18` re-synthesises the field from the 6720 banked
coefficients to `1.613811231995e+00` exactly; `gate.per_rung_residual` matches
`ladder_results[*].residual_load_bearing` rung by rung on both branches; `self_hash` verifies.
**No arithmetic defect was found anywhere in wave 6.**

The qualifications are all about what the unit's *prose* — and, as it turns out, the Conductor's
*audit* — then did with those numbers.

---

## §3. THE QUALIFICATIONS ON `L6`

### §3.1 `D-VW6-1` — §8.2's bolded sentence is not established by its own control

> **"The `NO` is a fact about the construction, not about the stopping point."** — `leg_401.md`
> §8.2, bolded.

The only control offered is the cap sweep 50/100/200/400/800, with
`verdict_is_stable_in_the_cap = true`. **I re-derived that entire block from the recorded
trajectories** — all five caps × five rungs × two branches — and it reproduces exactly, because
it *is* `residual_at_cap()` taking `min` over recorded trajectory points with `k ≤ K`. It is a
**post-hoc truncation of the same 800-iteration runs**.

And the truncation cannot be read as a smaller-budget experiment, because every rung's
continuation start was warm-started from the previous rung's **full-800-iteration** minimiser.
Concretely: `J1` at "cap 50" reads **3.2798**, which is a truncation of a run seeded from `J0`'s
800-iteration answer **3.6467** — whereas `J0`'s own cap-50 value is **14.0239**. The full budget
is inside every column of the control that is supposed to rule the budget out.

What the data *does* support is the unbolded sentence at §10.1: *"this construction, at this
resolution, under this budget, does not converge to one."* That one is sound.

### §3.2 `D-VW6-2` — the banked minimiser is not a stationary point, and it is getting worse

At the reported `ρ = 1.6138` the banked continuation start has `max_abs_grad = 252.21` and
`scale_invariant_grad = 153.216`, against the unit's **own pre-registered `gtol = 1e-12`** —
about fourteen orders of magnitude. Every one of the 133 starts exited `status = 1` (*maximum
iterations reached*); **not one start anywhere in the artefact converged by gradient or by
`ftol`.**

Worse, distance from stationarity **grows monotonically with `n_dof` on both branches**:

| `n_dof` | B `scale_invariant_grad` | A `scale_invariant_grad` |
|---|---|---|
| 1800 | 23.05 | 1.16 |
| 2400 | 24.06 | 1.75 |
| 6720 (A ladder rungs) | 60.11 → 153.22 | 3.11 → 16.12 |

The numbers **are** banked, honestly, per start. What is missing is any sentence anywhere — unit,
close doc, or `STATE.md` — saying that the ladder is moving *away* from stationarity as it
refines. That matters because a sequence of iterates whose gradients grow with `n_dof` is exactly
the signature of a budget that has stopped tracking the problem — which is the alternative §8.2
rules out.

### §3.3 `D-VW6-3` and `D-VW6-4` — pre-registered predictions banked, never adjudicated

§3.5 pre-registers three mechanism predictions and closes with: *"Each prediction is recorded so
that a **failure** of the prediction is as reportable as a success."* The string `prediction`
does not occur anywhere in `leg_401.md` after §7, nor in `WAVE6_CLOSE.md`. They were banked and
then dropped.

The banked diagnostics adjudicate two of them, and neither adjudication is favourable:

- **Prediction 2** (branch A's interior normalisation kills the α=1 far-field tail) is
  **REFUTED**: branch A's `far_field_alpha1_amplitude_mean = 0.3479`, not ≈ 0, and its fitted
  decay exponent is **0.9954** — α ≈ 1 exactly as Chae–Wolf pins.
- **Prediction 3** (the `L^{3/2}` norm grows under radial-quadrature refinement, log-divergence)
  is **NOT CONFIRMED**: the ×4/×1 ratio is 1.0000117.

Separately, §4 promised *"the far-field exponent of the minimiser"* be reported. It is banked and
reported in no prose anywhere: the **headline branch B** minimiser's fitted far-field decay
exponent over 5 < r < 200 is **0.2250** — not 1, not 0.5, and four times shallower than branch A's
0.9954. The string `0.225` occurs in `leg_401.md`, in `WAVE6_CLOSE.md` and in `STATE.md` exactly
zero times.

### §3.4 `D-VW6-7` — an undeclared dependency, and the check that cannot run

`experiments/p2_route_l6_v1.py` does `from scipy.optimize import minimize` and
`from scipy.special import lpmv`. `requirements.txt` says, verbatim:

> `# scipy is intentionally NOT required (no adaptive ODE integrators are used).`

`scipy` appears nowhere in `requirements.txt`. **Measured, not inferred:** with scipy blocked in
a scratch sandbox, `experiments/p2_route_l6_v1_evidence.py` dies with an uncaught `ImportError` —
exit 1, traceback, **zero checks reported** — because its full path does `import p2_route_l6_v1
as L6` inside `main()`. The `--fast` path survives.

So on a clean checkout following the repository's own stated dependencies, `L6`'s evidence does
not execute at all. This is lesson 68 in its purest form: a check nobody can execute decays into
a claim. It is also the **identical defect class** to `V5`'s `mpmath` omission, which was repaired
one commit earlier at `ac34fd4` — the failure is not that the class was unknown, it is that the
same sweep was not run for `L6`.

### §3.5 `D-VW6-10` — the coverage gap

Neither `stability_against_the_iteration_cap` nor `cost_and_shortfall` is touched by **any** of
`L6`'s 30 evidence checks (`by_cap`, `stability_against`, `cost_and_shortfall`, `hit_maxiter` and
`preregistered` all return zero hits in the script's source). The two blocks carrying the most
rhetorical weight in the unit's conclusion are the two with no executable check behind them.

---

## §4. ITEM (e) — ARE THE SELFTESTS REAL TESTS?

Every mutation below was performed on a **copy under the session scratchpad**. The repository was
never modified to test it.

| unit | mutation | result | verdict |
|---|---|---|---|
| `L6` | flipped `gate.B.decreases_under_refinement` `NO`→`YES`, recomputing `self_hash` so the hash check could not mask it | `FAIL C10[B] banked=YES recomputed=NO`, exit 1 | **REAL** |
| `L6` | perturbed **one** of the 6720 banked coefficients by 0.1% | `C18` and `C20` both FAIL, exit 1 | **REAL** — `C18` genuinely re-synthesises rather than reading a stored scalar |
| `L6` | replaced **all** branch-B coefficients with Gaussian noise | `C18` FAIL, `C20` FAIL, **`C19` still PASSED at 3.16e-13** | **`C19` IS A TAUTOLOGY** |
| `V5` | set the recomputed `x_1^U ≤ 6.2e-6` closure value to 9.9e-6 | `FAILED — 1 of 75 checks disagreed`, exit 1 | **REAL** |
| `V-W5` | set `L5`'s `gate.rho_exponent` to −0.5 | two item-1 checks FAIL bit-for-bit, exit 1 | **REAL** |

**`C19` (divergence-freeness) cannot fail for any input.** In the Chandrasekhar representation
`div V = 0` identically for *any* coefficient vector, so `C19` measures the implementation of
`curl`, not the solution. `leg_401.md` §8.5 says exactly this, so the check is **honest** — but
it is not evidence about the profile and should not be counted among the thirty that constrain it.
Recorded as `D-VW6-11`.

One further note on `L6`'s six named selftests. `T_D` (exact-basis `W[V]` against a
finite-difference `curl R[V]`) is `1.22e-4` — the loosest by seven orders, and the **only** one
that cross-checks the operator against an independent discretisation. That is the right shape for
a finite-difference comparison and the tolerances were pre-registered at §0.2 before any number
existed. It is not a defect. It is recorded because `1.2e-4` is the accuracy floor under
everything above it.

---

## §5. THE RULING ON THE CONDUCTOR'S `L6` LANDING AUDIT

I was asked to verify the Conductor's assertion independently and then rule on the consequence
drawn from it, and to say plainly if the audit is itself overclaimed or understated. **You are
checking the integrator as well as the units.**

### §5.1 Claim by claim

**"at every rung above the coarsest the reported `ρ` is attained by ONE start — the continuation"
— UPHELD, EXACTLY.** I confirmed it independently on **both** branches: at each of the four rungs
above the coarsest, the set of starts attaining the reported minimum to 1e-12 relative is the
single-element set `{continuation}`. (The coarsest rung has no continuation start at all — 5
starts; every other rung has 6.)

**And the unit never said it.** `leg_401.md` mentions `continuation` only in the §5 protocol
statement and the §8.4 spread table; the *finding* — that the minimum at every refined rung is the
continuation's alone — appears nowhere in the unit's own writing. **This is the audit's genuine
contribution**, and it is enough on its own.

**"five independent random seeds land 10–24× higher" — OVERSTATED IN RANGE.** True branch-B
factors, rung by rung: `n_dof=576` **3.93–4.02×**; `1800` 8.04–11.35×; `2400` 10.08–11.29×;
`6720` 18.32–23.67×. The 10–24× band is right only for the **top two** rungs. On branch A the
factor **never reaches 10× at any rung**: 1.01–1.04×, 2.55–2.62×, 2.58–2.62×, 5.36–5.95× — at the
first refined rung on branch A the seeds are within **4%** of the continuation, i.e. essentially
tied. The direction is right; the top of the range is presented as the range, and one of the two
branches does not support it at all.

**"and get monotonically WORSE as `n_dof` grows" — OVERSTATED.** True of the per-rung seed
*minimum* (6.680 → 13.470 → 16.355 → 29.567) and of 3 of the 5 seeds individually. **False** for
`seed402` (6.802, 18.265, **16.355**, 32.093) and `seed403` (6.751, 19.020, **18.314**, 29.567),
both of which fall at the third rung.

**"`J1 ≈ 6.8 → J4 ≈ 32–38`"** — the `J4` seed range is **29.567–38.197**. A minor slip, and
conservative in direction.

**"all 133 starts hit the 800-iteration cap" — IMPRECISE, not wrong.** 133 of 133 hit *their* cap
(every start has `hit_maxiter = true` and `status = 1`), which is exactly right. But only **58**
were capped at 800 — the joint ladder. The other **75** were capped at 250, the artefact's own
`maxiter_on_the_secondary_axis_ladders`. The gate-bearing ladder is the 58, and those were at 800.

**"against a pre-registered cap of 20,000" — CORRECT.** `leg_401.md` §5 pre-registers
`maxiter = 20000` from 6 starts per rung at `e4598a5`, and the artefact banks
`preregistered_maxiter = 20000`. A 25× shortfall.

**"a ceiling the unit itself did not state" — HALF RIGHT.** The unit *did* bank
`UNDER_RESOURCED = true`, the 20000-vs-800 shortfall, the 133/133 cap counts and the ~10³ core-h
price, and fired plan reading (d) explicitly; §10.1 already reads *"**this** construction, at
**this** resolution, under **this** budget"*. What the unit did **not** state, anywhere, is the
one-start finding.

### §5.2 Where the audit is **UNDERSTATED** — and this is the part that matters

**(i) It concedes ground it did not have to concede.** The audit says: *"The cap sweep (50→800)
supports that against the iteration budget."* **It does not.** As §3.1 above establishes by direct
re-derivation, the sweep is a truncation of the *same* full-budget runs, and each rung's warm
start carries the full budget down from the rung below. `verdict_is_stable_in_the_cap = true`
controls nothing about the budget — and no evidence check touches it. The Conductor was **right**
to refuse §8.2's sentence, and had a **stronger** reason available than the one it used.

**(ii) It never mentions that the banked minimiser is not a stationary point at all.** `153.2`
against a pre-registered `gtol` of `1e-12`, growing monotonically with `n_dof` on both branches.
A ladder whose iterates get *further* from stationarity as they refine is a stronger statement of
the audit's own thesis — *"biased toward measuring no change"* — than the seed spread is, and it
was sitting in the same artefact, per start, unremarked.

### §5.3 Where the audit is **WRONG**, in the conservative direction

§3i q2 reads: an NK/interval enclosure needs a residual small enough for a contraction to close,
*"and this one is **1.6 against a unit-normalised field**"*.

**1.6 is branch B's number, and branch B is not the unit-normalised branch.** Branch B pins the
far-field angular amplitude to 1; its Gaussian-weighted interior `L²` is **0.00219** — 0.22% of
branch A's. The unit-normalised branch is **A**, whose `weighted_L2_total` is 1.0000000000 and
whose residual is **7.58**. The unit discloses this asymmetry at its own §10 reading (c-3); the
audit did not carry it.

**Which way it cuts:** *against* the audit's own case. Measured against a genuinely
unit-normalised field the residual is 7.58, not 1.6, so `L7` is blocked **harder** than the audit
says, not less. The re-rank stands undisturbed and is if anything better justified.

### §5.4 THE RULING

**The consequence the Conductor drew — that `L6`'s `decreases_under_refinement = "NO"` cannot yet
be read as a statement about the ansatz — is SOUND, and on my evidence it is BETTER SUPPORTED
than the Conductor's own argument for it.** Three independent facts force it:

1. the minimum at every refined rung is one warm-started point, so the ladder measures the
   **continuation path**, not the trial space;
2. the cap sweep offered as the control against the budget hypothesis is a truncation of
   full-budget runs and controls **nothing**;
3. every iterate is ~14 orders from its own convergence criterion, and **getting worse** with
   `n_dof`.

Any one of these blocks the inference to the ansatz. Together they make it unavailable.

**The re-rank to `L6-b` is CORRECT** and is the right purchase — it is exactly the experiment that
separates (2) and (3) from the ansatz hypothesis. **One addition I would make to its gate that
the Conductor's wording does not require:** it must report the terminal `max_abs_grad` and
`scale_invariant_grad`, not only the terminal `ρ`. A `ρ` that does not move while the gradient
**does** fall means something entirely different from a `ρ` that does not move while the gradient
sits at 153.

**On the price.** *"~10¹ core-h from `L6`'s own timing (1,240 s per start at 800 iterations), two
orders below the ladder."* I re-derived it: 1238.7 s / 800 = **1.548 s per iteration**, × 20000 ×
3 starts = **25.8 core-h**. Inside "~10¹" but at its top, and ~**1.6** orders below a 10³ core-h
ladder, not two. The purchase is still cheap and the conclusion is unaffected.

**One line.** *The Conductor's audit is **correct in its finding**, **overstated in three of its
numbers**, **understated in two places that would have helped it**, and **wrong once in the
conservative direction**. The consequence it drew stands. The integrator did not confirm its own
unit — it contradicted it, which is the behaviour the rule exists to produce — but it did so with
prose running ahead of its arithmetic, which is the same defect it charged the unit with.*

---

## §6. ITEM (f), AND A NOTE ON FAIRNESS

`L6`'s branch-B selection leans on the exactly-self-similar exclusion of Nečas–Růžička–Šverák 1996
/ Tsai 1998, which `SOURCES.md` records at **SECOND HAND**. The Conductor's §3i q4 names this debt
but attributes it to `L7-src`; it is also live under `L6`'s branch choice. It is only *partially*
load-bearing — it motivates which of two pre-registered branches carries the headline, and both
branches are banked, so no *number* depends on it (`D-VW6-5`).

At the bound, `writeup/SOURCES.md` carries **no row and no DEPTH** for Byrd–Lu–Nocedal–Zhu 1995 —
the apparatus `L6` names to discharge ban C1 — nor for the Chandrasekhar poloidal–toroidal
representation, its trial space. `SOURCES.md`'s own footer says a row with no DEPTH is a defect.

**This is not `L6`'s fault and I will not charge it as such.** §3k and `SOURCES.md` were created on
the Conductor line at `1f0d6b0`/`9b9571a`, **after** `L6`'s branch point `5c49486`. A unit cannot
comply with a rule that did not exist when it was dispatched, and `L6` satisfied §3k rule 3 *in
substance* anyway by naming the published method with a full citation at §0.3 before running.
The gap belongs to the **integration boundary**: `e62c449` landed `L6` into a tree that already
had the register, and §3i q4 reviewed source ceilings in that same commit without noticing `L6`'s
two undepthed load-bearing citations (`D-VW6-6`). *Outside my bound, informational only:* `L6-b`
has since added the Byrd row at depth `ABSTRACT` with a written not-load-bearing argument; the
Chandrasekhar row is still absent.

### §6.1 Wave-level process checks

- **§3g step 1 — gates pre-committed before dispatch:** VERIFIED from git (§0.2).
- **§3g — no self-verification:** VERIFIED; `L6` was labelled `UNVERIFIED` and this unit is the
  verifier, one wave later.
- **§3i — seven questions per unit in the integration commit:** substantially met, one form
  deviation. `V5`'s are in `ac34fd4`, `V-W5`'s in `7c79397`. `L6`'s are **not** in its landing
  commit `e62c449` — whose body is empty — but in the wave-close commit `2a5ea0d` one commit
  later. Content complete, timing one commit late. Recorded, not charged.
- **§3g wave composition floor:** met — `L6` is a Lane L unit attacking W4/W5 directly. Two of
  three units are audit-kind, which the Conductor flagged itself under §3i q7 and acted on.
- **No link moved:** VERIFIED across all three artefacts. Clay ~0.05%.

### §6.2 My own evidence script, mutation-tested

A verifier's script must be able to fail, or it is decoration. Three mutations against a symlink
mirror under the scratchpad:

| mutation | result |
|---|---|
| corrupt `V-W6`'s own `self_hash` | `FAIL Z01`, exit 1 |
| perturb the headline `ρ` constant by 1e-6 relative | `FAIL B02`, exit 1 |
| invert the one-start assertion `C01` | `FAIL C01`, exit 1 |

(A 3.7e-13 relative perturbation of `ρ` passes, which is the declared `rtol = 1e-12` of the
comparison and is intended.)

---

## §7. WHAT I COULD NOT CHECK — WITH PRICES

Lesson 68: **a check nobody can execute decays into a claim.** An honest *"not checked, would cost
~X"* is a real deliverable; silence is not. I have not padded this list with checks I did run.

1. **Whether `L6`'s residual is actually the minimum of `J` over the trial space** — i.e. whether
   the 800-iteration cap, not the ansatz, produced 1.6138. Needs compute I do not have, and it is
   precisely `L6-b`'s job. **Price: ~26 core-h** at the top rung (3 starts × 20000 iterations ×
   1.548 s/iteration, re-derived from `L6`'s own banked timing). Decisive in both directions.

2. **Whether `J(c)` as coded is actually the `L^{3/2}` norm of the curl of route 4's profile
   residual** — i.e. whether the objective is the right functional. This is **SCIENCE, not
   arithmetic**, and outside a verifier's remit as this programme defines it. The strongest
   internal evidence is selftest `T_D` at `1.22e-4`, which is a real cross-check of the operator
   — but it compares two of the unit's *own* implementations, not an external one. **Price: an
   independent re-implementation of the vorticity residual in a different basis (e.g. a
   uniform-grid finite-difference evaluation of `W[V]` on the banked field), ~1 unit-week of agent
   time and ~1–5 core-h.** *This is the single highest-value unrun check on route 4's construction
   line*: every route-4 residual number in the record is downstream of this one function.

3. **Whether the 22 Class-A interval inputs `V5` took as given are correct.**
   UNRECOMPUTABLE-BY-DESIGN without the authors' interval code, which we do not hold; contacting
   the author group is PROHIBITED. **Price: ~2–4 unit-weeks plus an interval library we do not
   currently depend on — or ~0 if the code is ever published.** If it went the other way, `V5`'s
   CLAUSE_1 `YES` would become unsupported; the record already carries the condition explicitly.

4. **Whether branch B's far-field decay exponent of 0.225 is a genuine failure to sit in the
   Chae–Wolf α ≥ 1 class, or an artefact of fitting over 5 < r < 200 on a truncated algebraic
   map.** **Price: ~0.5 core-h from the banked coefficients** — genuinely cheap. I did not spend
   it because producing a new physical measurement is outside a verifier's gate. I report the
   number and its absence from the prose; I do not adjudicate what it means.

5. **Whether the four constants `V5` found broken are broken in the published paper or only in
   the arXiv version `V5` pinned.** **Price: ~15 minutes plus one network fetch per version.**
   Low severity — `V5`'s conclusion is about the pinned version and says so.

6. **Whether `L6`'s evidence script passes on a truly clean `pip install -r requirements.txt`
   checkout.** I *simulated* it by blocking scipy and confirmed the uncaught `ImportError`, but I
   did not build a fresh virtualenv. **Price: ~5 minutes, ~200 MB.** A real venv could only
   confirm the finding.

7. **The three sibling units in flight during this run** (`R-bank`, `R-prof`, `L6-b`). **OUT OF
   SCOPE BY CONSTRUCTION** — they are wave 7, and their verifier is `V-W7`.

---

## §8. CEILING, AND WHAT THIS UNIT DID NOT DO

**This unit verifies ARITHMETIC, PROVENANCE and GATE-COMPLIANCE. It does not verify the SCIENCE.**
Reproducing `L6`'s `ρ` does not make `ρ` small. Reproducing `V5`'s recomputation does not make
arXiv:2509.25116's certificate true. Reproducing `V-W5` does not make `L5`'s `NO` correct.

I did not repair any of the eleven defects found. I did not edit any banked artefact. I did not
read `DIRECTION.md`. I did not contact any author group. I ran no new physical measurement on
route 4. I did not verify the wave-7 units in flight.

**Tier 2. No link of the `L1 → L4` chain moved in wave 6, and none moved in this verification.
Clay ~0.05%, unchanged.** Scale is not evidence, and a long verification is not a thorough one —
what makes this one thorough, if anything does, is §4 and §6.2: every claim of soundness above is
backed by a mutation that made the corresponding check fail.

---

## §9. DEFECTS OPEN — REPORTED, NOT REPAIRED

| id | unit | severity | one line |
|---|---|---|---|
| `D-VW6-1` | `L6` | MATERIAL | §8.2's bolded *"a fact about the construction"* is not established by its only control; the cap sweep is a truncation of full-budget runs |
| `D-VW6-2` | `L6` | MATERIAL | banked minimiser is ~14 orders from its own pre-registered `gtol` (153.2 vs 1e-12) and the gap grows monotonically with `n_dof` on both branches |
| `D-VW6-3` | `L6` | MINOR | §3.5's three pre-registered predictions never adjudicated, against an explicit commitment; two are unfavourable on the banked data |
| `D-VW6-4` | `L6` | MINOR | headline branch's fitted far-field decay exponent is 0.2250; §4 promised to report it and no prose carries it |
| `D-VW6-5` | `L6` | MINOR | branch-B selection leans on NRŠ 1996 / Tsai 1998, held SECOND HAND |
| `D-VW6-6` | integration boundary, **not** `L6` | MINOR | at the bound, `SOURCES.md` has no row and no DEPTH for Byrd–Lu–Nocedal–Zhu 1995 or Chandrasekhar |
| `D-VW6-7` | `L6` | MATERIAL | undeclared scipy dependency; full evidence script dies with an uncaught `ImportError` on a clean checkout — zero checks reported |
| `D-VW6-8` | `V-W5` | COSMETIC | banks 120 checks, prints 121 `[PASS]` lines |
| `D-VW6-9` | `V-W5` | COSMETIC | item 3's git subprocess raises an uncaught `CalledProcessError` outside a checkout instead of reporting a `FAIL` |
| `D-VW6-10` | `L6` | COVERAGE | neither `stability_against_the_iteration_cap` nor `cost_and_shortfall` is touched by any of the 30 evidence checks |
| `D-VW6-11` | `L6` | COVERAGE | `C19` is a structural tautology — it passes on Gaussian noise (disclosed at §8.5) |

**`defects_repaired = 0`. `banked_artefacts_edited = 0`.**
