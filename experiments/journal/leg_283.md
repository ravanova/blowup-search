# Leg 283 — Route-M2SR: downgrade leg 174's catalog at `a = 1/2` to sign-only

**Date:** 2026-08-07. **Branch:** `leg/283-m2sr-v1`. **Novelty pass:** `writeup/novelty/leg_283.md`,
committed at `ae94b54` **before any edit to any historical file**. **Register entry:**
`writeup/CORRECTIONS.md` row **9**, §10. **Source read (never edited):** leg 210 (Route-M2SV),
parked branch `leg/210-m2sv-v1` @ `6e06880`, `experiments/journal/leg_210.md`, read in full.

**This leg produced no number of its own.** Every magnitude quoted here is leg 210's, carried.
No compute, no solver module imported, no profile solved, no artifact regenerated. Float
everywhere upstream, on a discretised and truncated domain, with no certificate. Clay stays
**~0.05%**.

---

## The gate, answered clause by clause

> **(a)** Catalog + every site quoting the `a = 1/2` magnitude or the `a*` value (found by a
> standing-rule-robust sweep — case-insensitive, wrap-immune, hyphenation-tolerant)
> downgraded/marked with pointers; **(b)** the `a = 0.30` confirmation and the
> gauge-independence strengthening recorded in the SAME pass; **(c)** leg 185's journal gets an
> append-only pointer, original text preserved; **(d)** no other meaning changed.

**YES on all four. Banked.** No site's downgrade changed an argument, so the escalation branch
did not fire — see "Why (d) held" below, which is the clause that could have gone the other way.

| clause | verdict | magnitude |
|---|---|---|
| (a) catalog + swept sites marked | **YES** | **8 files edited, 533 lines added, 0 deleted**; 21 patterns over 1,239 files; the catalog itself returned **0 hits** and is recorded as a null check rather than left unrecorded |
| (b) upgrade travels with downgrade | **YES** | all **9** pointer blocks carry both halves; the `a = 0.30` confirmation (`1.198e-06` relative) and gauge-independence appear in every one |
| (c) leg 185 append-only | **YES** | **5** pointer blocks, **130 lines added, 0 deleted** — verified by `git diff --numstat` |
| (d) no other meaning changed | **YES** | **0** numeric values altered anywhere, **0** assertions/thresholds changed, **0** run artifacts edited, **0** ledgers opened |

---

## What leg 210 measured, and what this leg did with it

Leg 210's verdict is a **SPLIT**, and the split is the whole point. A carrier that transmitted
the refutation and dropped the confirmation would misrepresent it worse than silence would, so
the upgrade is written into the same block as the downgrade at every single site.

**The upgrade half — and it is the larger half.**

| clause | verdict | magnitude |
|---|---|---|
| `ν = +0.01799364` at `a = 0.30` | **CONFIRMED independently** | Richardson **0.0179936185** at observed order **4.12** vs the banked **0.01799364** — **1.198e-06** relative, ~**5.9 significant digits**, from a differently discretized scheme |
| the gauge the reparametrization rests on | **VERIFIED AS A NUMBER** | predicted offset **8.634e-07**, observed **8.634e-07**, ratio **1.0000** |
| the covariance used as a *derivation*, gauges wrong by `κ ∈ {0.5, 0.8, 1.25, 2.0}` | **RECOVERS** | worst **3.184e-05** over a 4× gauge range |
| the sign law | **CONFIRMED AND STRENGTHENED TO GAUGE-INDEPENDENCE** | `Ω(X) → Ω(X/μ)` sends `ν → μ²ν` with `μ² > 0`, so **no gauge choice can flip the sign** |

The independence was pre-committed before any number was computed and is not a re-run: 6th-order
differences against leg 185's 4th, `∂ₓₓ` by chain rule against `D∘D`, degree-5 Lagrange panel
quadrature against 4-point Adams–Moulton, the system restricted to the odd subspace and solved
by **LU** rather than least squares — with all four operators validated against closed forms
(`1.283e-08`, `2.236e-09`, `5.319e-10`, `3.468e-10`) *first*.

**The downgrade half.** At Chen's `a = 1/2` leg 210's scheme gives **`−0.00082927`** from both
starts at relative residual `4.4e-15`, a **10.33×** gap against the banked `−0.00817525`. The
decisive instrument is the grid ladder **leg 185 never ran at `a = 1/2`** — it refined only
`a = 0.30` — and it exonerates neither scheme: leg 185's own spans **62.2%** non-monotonically
(`−0.00655 → −0.00818 → −0.00443`), leg 210's spans **119.2%** with amplitude collapsing to
min/max **0.483** toward the trivial null. The `a = 0.30` **control**, run identically at the
same three grids, spans **1.94e-03** (leg 185) and **2.11e-04** (leg 210) with amplitude flat at
**0.999** — 320× to 5600× better **in the very same code**, which is what separates a real
discrepancy from a broken re-implementation. `a = 0.45` is the worst point measured (leg 210's
amplitude collapses by **160×**). Conclusion: **sign only, no magnitude**, on the whole `ν < 0`
side.

**The `a*` half.** Leg 185's cross-check is **UNPINNED on evidence internal to its own journal**:
its `a`-sweep row at `a = 0.3865` reports **`+0.00000035`** from one start and **`−0.00425080`**
from the other — **its two starts straddle zero at that exact `a`**. Leg 210's independent
sign-change bracket between **converged** points is **[0.36, 0.37]**, and `a = 0.3865` is a
point its scheme fails to converge at (rel. residual **6.0e-03**).

---

## The sweep, and its pattern robustness — stated explicitly, as the standing rule requires

* **case-insensitive** — `re.IGNORECASE` on every pattern;
* **wrap-immune** — each file read as one stream with markdown leaders (`>`, `#`, `|`, list
  bullets, ordered-list markers) stripped and **all** whitespace collapsed to single spaces
  *before* matching, so a phrase or number broken at the 100-column hard wrap still matches;
* **hyphenation-tolerant** — every numeric pattern written as a **bare digit run with an optional
  `[-\s_.]{0,3}` separator between every character**, requiring **no** sign, **no** decimal point
  and **no** leading zero, so `−0.00817525`, `-.00817525`, `0.00817525` and any hyphen- or
  wrap-broken rendering all match one pattern; every intra-phrase space written `[-\s_]+`.

**21 patterns over 1,239 files** (`.md`, `.py`, `.json`, `.jsonl`, `.txt`, `.sh`; `.git`,
`.venv`, `Papers`, `__pycache__`, `.claude` excluded). Anchoring on **bare digit runs** rather
than prose is deliberate: it is the one pattern class that cannot be defeated by rewording, and
every refuted quantity here is a number.

**False positives are reported, not tuned away.** The separator class plus the dropped decimal
point makes the numeric patterns deliberately over-broad inside numeric JSON: `rounded_00818`
returned **34 hits in 14 files**, `rounded_00895` **56 in 23**, `astar_4dig_3865` **83 in 28** —
nearly all unrelated floats (`895316` matches inside `0.4986225895316804`). `phrase_zero_crossing`
returned **52 hits in 29 files**, almost all the fractional-gCLM / Route-F crossings, a different
object. `richardson_0179936185` returned **0**, correctly — leg 210's Richardson limit had never
reached `main` before this leg.

**Blind spots, because a sweep's blind spots are part of its result.** Six text extensions only
(no PDFs under `Papers/`, no binary or notebook formats). Anchoring is on numbers, so a site
paraphrasing *"the negative values at Chen's `a`"* with no digits would not surface. And leg 210's
branch is **parked and unmerged**, so its artifact and runner are not on `main` — after this leg,
`writeup/CORRECTIONS.md` §10 is the only place on `main` carrying leg 210's magnitudes.

---

## What was edited, and what was deliberately not

**8 files, +533 / −0.**

| file | what |
|---|---|
| `experiments/journal/leg_185.md` | **5** append-only pointer blocks (+130/−0): at the D5 table, the `a*` cross-check paragraph, the honest-limits paragraph, the D5 summary, and the leg-174-facing blockquote |
| `experiments/journal/leg_174.md` | dated append-only note (+45/−0) recording the **null check** — see below |
| `writeup/CORRECTIONS.md` | register row **9** + §10 (+157/−0) |
| `writeup/novelty/leg_207.md` | attribution pointer (+25/−0) |
| `experiments/journal/leg_207.md` | pointer beside the verbatim gate quote, quote itself untouched (+14/−0) |
| `writeup/4_p2_lottery/TECHNICAL_P2_PUB3_V1.md` | pointer on the `a*` parenthetical (+14/−0) |
| `experiments/p2_route_dpa_v1_adversarial.py` | **comment only** (+17/−0), at the `A_STAR` declaration |
| `test_dissipative_profile_adversarial.py` | **comment only** (+9/−0), at the `A_STAR` declaration |

**The dispatch's named target returned nothing, and that is recorded rather than glossed.** Leg
210 addressed its downstream finding to *"leg 174's (VBS) 'missing rung' catalog"*.
`experiments/journal/leg_174.md` and `writeup/data/p2_route_vbs_v1_scoping.json` returned **0
hits on all 21 patterns** — the catalog carries **no `ν` magnitude and no `a*` value at all**.
Its `CHEN-GCLM-DISS` row is model-level (model, dissipation `ν Λ^γ`, J. Chen *Nonlinearity* **33**
(2020) 2502, leg 125's retired-candidate flag). **There was nothing there to downgrade.** An
unrecorded check is indistinguishable from an unperformed one, so the null result is written into
`leg_174.md` itself with its instrument, rather than reported only here.

**What the sweep found that leg 210 did not: a provenance defect.** **Five** landed sites credit
`a* = 0.3864963972206034` to **leg 185** as its own measurement. It is **leg 125's / Route-M2P's**
— the `Δ(a)` sweep at `ν = 0` with `c_l` as the output, relative residual **1.933e-15**
(`TECHNICAL_P2_ROUTEM2P_V1.md` §3.5). Leg 185's own journal (line 175) gets this right; the five
downstream sites do not. All five now carry attribution pointers. **None is load-bearing** — in
leg 207's battery `A_STAR` is a bracketing *input*, never a claim, and in PUB3 it sits inside an
escalation-**counting** argument that does not depend on it.

**Left alone, each for a stated reason:**

* **Run artifacts** — `writeup/data/p2_route_m2sd_v1_diagnostic.json`, the conclusion strings in
  `experiments/p2_route_m2sd_v1_diagnostic.py`, `writeup/data/p2_route_dpa_v1_adversarial.json`.
  Leg 210's replay tier reproduced leg 185's banked ladder **exactly, to all 8 digits on all 4
  grids**: there is no bookkeeping error to fix, and editing them would destroy the record of
  what leg 185 concluded — which is the thing the correction is *about*.
* **Leg 125 / Route-M2P's own `a*`** — `leg_125.md:116`, `BLOG_P2_ROUTEM2P_V1.md:89,115`,
  `TECHNICAL_P2_ROUTEM2P_V1.md:241,369`, `p2_route_m2p_v1_promotion.json`. A different
  computation, which leg 210 neither measured nor refuted.
* **Leg 180's flags** — `leg_180.md:106`, `novelty/leg_180.md:112` already say M2P's `a*` is a
  *different* `a*`. Correct as written.
* **Integration- and DM-owned ledgers** — `experiments/JOURNAL.md` (`:3298`, `:4008`, `:4011`,
  `:4016`, `:4018`), `DIRECTION.md` (`:6711`, `:6714`, `:6793`, `:6794`, `:6825`, `:12238`,
  `:12244`, `:12258`), `CONTINUATION_PROMPT.md`. ORCHESTRATION.md §5a. **`DIRECTION.md:6793` and
  `JOURNAL.md:4011` still quote `−0.00818` / `−0.00895` as magnitudes** — flagged here and in
  `CORRECTIONS.md` §10.5 for the orchestrator/DM, never edited by this leg.

---

## Why (d) held — the clause that could have gone the other way

The gate's `no` branch is an escalation, and the honest question is whether any downgrade
**changed an argument** rather than qualifying a number an argument does not rest on. It did not,
and the reason is structural: **every load-bearing downstream statement is a SIGN statement, and
the sign is the part leg 210 confirmed and strengthened.**

* Leg 185's catalog input to leg 174 — *"Δ = 0 and Chen's `a = 1/2` demand **opposite signs of
  `ν`**"* — **stands, and is now gauge-independent.** Its recommended catalog wording never
  quoted a magnitude.
* Leg 187's inviscid construction context — Object B diffusive below the crossing,
  anti-diffusive at Chen's `a` — **survives fully**, per leg 210's own downstream note.
* Leg 207's battery uses `A_STAR` as a **bracketing input**; its four findings and its `YES`
  are untouched.
* PUB3's escalation **count** does not depend on `a*`.

The one clause genuinely re-worded is *"below `a* ≈ 0.3865` the object exists numerically"* →
existence **below the crossing**, with the crossing located only to a bracket by leg 185's own
instrument. That narrows a precision claim; it reverses no conclusion.

**The hazard I was most at risk of, and did not commit** (novelty pass §2(a)): marking every
`a*` site UNPINNED. Leg 210 refuted **leg 185's corroboration** of leg 125's value, not the
value. A leg that swept `3864963` and struck it everywhere would have deleted a measurement leg
210 never touched, on the strength of a sentence about a different quantity. **0 numeric values
of `a*` were changed anywhere.**

---

## Falsification conditions, fixed in the novelty pass before editing, checked after

| # | condition | result |
|---|---|---|
| 1 | 0 deleted lines in every pre-existing file | **PASS** — `git diff --numstat`: `+533 / −0` across 8 files |
| 2 | 0 files under `writeup/data/`, 0 runner output strings | **PASS** |
| 3 | every pointer block carries **both** halves | **PASS** — 9/9 |
| 4 | 0 occurrences of `3864963` have their value changed | **PASS** — every diff hunk is an addition |
| 5 | every sweep hit classified, left-alone classes named and counted | **PASS** — `CORRECTIONS.md` §10.5, 7 classes |
| 6 | the five integration-owned ledgers + `DIRECTION.md` appear 0 times in the diff | **PASS** |

---

## Forward note

**Leg 284 is running in parallel**, taking leg 210's ladder to actual grid-convergence at
`a = 1/2`. If it lands a converged number, this downgrade is **superseded by citation, not by
deletion** — the sign-only status and these pointers stay standing as the record of what was true
when it was true, per `CORRECTIONS.md`'s "marked, never hidden" convention. **The honest current
state is what is banked here**, which was the job regardless of what 284 returns.

Two follow-ups this leg is not authorized to do, recorded for the DM rather than acted on:
**(1)** `DIRECTION.md:6793` and `JOURNAL.md:4011` still carry `−0.00818` / `−0.00895` as
magnitudes and are ledger-owned. **(2)** Leg 210's branch is still parked and unmerged, so its
artifact (`p2_route_m2sv_v1_postconstruction.json`) and runner are not on `main`; only its
prose findings are, via this leg.

## Honest ceiling, pre-committed and unchanged

Record maintenance. **0 arguments changed, 0 gate answers changed, 0 numeric values altered, 0
run artifacts edited, 0 solver modules touched, 0 test assertions changed, `plan_of_record.py`
not opened.** This leg cannot lift a ban, promote a route, or move a link of `L1 → L4`.
"Not reproduced" is not "refuted as false"; "sign-only" is a **narrowing** of a banked claim, not
a new result; and the thing that got *stronger* — gauge-independence of the sign — is a statement
about a float measurement of a discretised, truncated profile equation, which is not an existence
proof and not a certificate. Stage P0 is unaffected. Clay stays **~0.05%**.

---

## [APPENDED 2026-08-07 BY LEG 296 (Route-ASR) — APPEND-ONLY, nothing above altered]

**This leg's `a*` UNPINNED note above was correct given what was known on 2026-08-07** — leg
210 was, at that point, the only independent check on leg 185's `a* = 0.3865` cross-check, and
its bracket `[0.36, 0.37]` genuinely excludes leg 125's value while leg 185's own two starts
straddled zero. Leg 284 (Route-NU12) then landed a third, independent method — pseudo-arclength
continuation from a converged root, unrelated to leg 185's or leg 210's fixed-`a` Newton
restarts — and re-pinned `a*` as a genuine turning point of Object B's branch at
**0.3857 ± 0.004**, grid-converged over four meshes, agreeing with leg 125's original
`0.3864963972206034` to ~2e-3.

**Leg 296 (dispatched to resolve this) determined: `a*` IS pinned near `0.386`.** Two
independent methods (leg 125's algebraic `Δ(a) = 0` crossing; leg 284's grid-converged fold)
agree to ~2e-3, and leg 210's exclusion bracket is explained mechanistically rather than
outvoted — leg 210 used the same family of fixed-`a` Newton-restart method that leg 284 showed
(its own Findings 2 and 4) folds and develops a singular Jacobian in exactly this neighborhood,
and leg 210's own report already shows the predicted symptom (non-convergence at `a = 0.3865`,
relative residual 6.0e-03; amplitude collapse toward the trivial null beyond it) independently
of anything leg 284 later found. **This does not restore leg 185's specific claim** — its own
two starts straddling zero at `a = 0.3865` stays exactly as refuted here. What is resolved is
the separate question of whether `a*` is a real, locatable feature of Object B at all.

Full reconciliation: `experiments/journal/leg_174.md` (append at end), `writeup/CORRECTIONS.md`
§11, `writeup/novelty/leg_296.md`, `experiments/journal/leg_296.md`. Sources: legs 125, 210,
283 (this file), 284, all cited in full.

**Ceiling.** 0 numbers re-measured, 0 arguments of this leg's own gate changed, 0 lines deleted.
Does not lift a ban, promote a route, or move a link of `L1 → L4`. Clay stays **~0.05%**.
