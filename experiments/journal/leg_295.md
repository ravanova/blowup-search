# Leg 295 — Route-PUB3P: fix the stale `a_max_machine` citation in PUB3's consumer table

**Date** 2026-08-07 · **Role** LEG · **Branch** `leg/295-pub3p-v1` · **Difficulty** light

> **Gate (pre-committed, DIRECTION.md "### 295").** Does the site state 0.55 with a dated note
> citing legs 236/226/294, with no other sentence's meaning changed, and a check (case-
> insensitive, wrap-immune, whole-file-normalized sweep) that no sibling document repeats the
> same stale value?
>   YES -> bank the fix, CORRECTIONS.md pointer.
>   NO (argument-touching resistance) -> escalate, same clause as the PUB2 family.

## Answer: **YES.** Banked.

## What this leg found, before touching anything (see `writeup/novelty/leg_295.md`)

Leg 294's report cited the site as `writeup/4_p2_lottery/TECHNICAL_P2_PUB3_V1.md:134`, quoting
`a_max_machine=1.0`. Checking directly: **line 134 is unrelated prose** (`` `c(a = 1.50)`
returns 0.20427 / 0.23717 / 0.97282 ``), and a whole-file, case-insensitive, wrap-immune sweep
found the literal string `a_max_machine=1.0` **nowhere in the target file** — it exists only in
leg 294's own journal/novelty notes, i.e. leg 294 paraphrased loosely. The real site is **line
148**, the Route-D v11 row of the "Exposure" consumer table, which names `a_max_machine` as
`DIRECT AND MATERIAL` exposure but prints no numeric value at all — so there was no literal
digit to swap.

## What was done

Appended one dated blockquote note directly after the exposure table (`TECHNICAL_P2_PUB3_V1.md`,
after the existing "second consumer" sentence), stating the resolved value:

> **Resolved 2026-08-07 (leg 295).** The `a_max_machine` exposure named in the table row above
> is closed: two independent methods confirm the corrected value **0.55** ... reconciled and
> provenance-tiered by leg 294's consolidated report ... This note adds the resolution; the
> table row's own text, above, is unchanged.

Zero existing sentences reworded; this is a pure addition, so no "argument-touching resistance"
applies and the PUB2-family escalation clause is not triggered.

## The mandatory sweep

Case-insensitive, wrap-immune (whitespace-normalized whole-file), pattern
`a_max_machine[^a-zA-Z0-9]{0,20}=?\s*1\.0\b` over every `writeup/**/*.md` and
`experiments/**/*.md`. Outside leg 294/295's own notes, the only hits are four lines in
`experiments/JOURNAL.md` (append-only history), all of which already state the correction inline
(e.g. `a_max_machine 1.0->0.55`). **No sibling prose document asserts the stale value as
current.** `writeup/data/p2_route_d_v11_anchor.json` on `main` still carries the raw stale JSON
field — that is leg 297/252's reserved integration decision, read-only here, not a prose
"sibling document repeat."

## CORRECTIONS.md

Register row **#11** and new **§12** appended (`writeup/CORRECTIONS.md`), recording both the
resolution and the leg-294 site-citation imprecision this leg found and corrected in the record
(not in leg 294's own file, which stays untouched per territory).

## Territory

`writeup/4_p2_lottery/TECHNICAL_P2_PUB3_V1.md` (one additive footnote after L148's table),
`writeup/CORRECTIONS.md` (append: register row #11, new §12), `writeup/novelty/leg_295.md`,
`experiments/journal/leg_295.md`. Read-only: `experiments/journal/leg_294.md`,
`writeup/novelty/leg_294.md`, `writeup/data/p2_route_v11x_v1_consolidated.json`, legs 236/226's
reports.

## The ceiling

**0 numbers re-measured. 0 existing sentences reworded. 0 lines deleted anywhere.** Does not
touch the banked anchor JSON on `main` (leg 297/252's territory). Clay stays **~0.05%**.
