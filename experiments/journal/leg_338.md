# Leg 338 — Route-LCB1: light corrections batch (314 cite / 321 anchors / 312 timing prose / 336's out-of-territory flags)

**Branch** `leg/338-lcb1-v1`. Correction/record leg, floor-ineligible (per `DIRECTION.md`'s
`[FLOOR-ELIGIBLE: no]` marker).

## 1. Dispatch, verbatim gate (`DIRECTION.md`, §338 + the cycle-10 amendment)

**Gate.** Are all four sites corrected to their source values with inline markers and ONE
batched CORRECTIONS entry naming each site and its evidence, with zero other content changed
(diff-checked)?
* `yes` -> Bank; done.
* `no` -> A site resists without touching an argument or gate text: report the resister
  verbatim, land the other sites, never widen.

**Answer: YES.** All four sites landed; none resisted.

Item (iv) — the two sites leg 336 flagged out-of-territory (`experiments/JOURNAL.md:5018`,
`writeup/INDEX.md:124`) — was added by the DM at cycle 10, *before dispatch*: `DIRECTION.md`
carries the note `[AMENDED 2026-08-12, cycle 10, before dispatch: item (iv) added …]`
immediately following the §338 spec. Confirmed verbatim by direct grep of `DIRECTION.md` before
any edit was made (`grep -n "### 338 — ROUTE-LCB1" DIRECTION.md` -> line 15995; the amendment
paragraph immediately follows at lines 16023-16029). This is part of the pre-committed spec,
not a widening performed by this leg.

## 2. The four sites, before/after

### (i) `writeup/data/p2_route_fus_v1.json`, `sources.USC.cite`

| before | after |
|---|---|
| `"Wang, Lai, Leger, Buckmaster -- unstable self-similar singularities via PINNs"` | `"Wang, Lai, Gomez-Serrano, Buckmaster -- unstable self-similar singularities via PINNs"` |

Verified directly against arXiv:2509.14185's own author list (fetched, not assumed — see
`writeup/novelty/leg_338.md` §2): 22 authors, including Javier Gómez-Serrano, none named
"Leger". Field-level fix; no comment channel exists in JSON, so the fix is the correction
itself, and it is documented with a before/after row in the batched `CORRECTIONS.md` entry
(§20) rather than an inline marker inside the JSON. The sibling field `sources.USC2.cite`
(line 49, arXiv:2511.22819) carries the identical misspelling and is **outside this leg's
territory** ("the one cite field" only, per the spec) — flagged in §20's table, not corrected.

### (ii) `writeup/CORRECTIONS.md` §16.1 anchor, plus its citation in `experiments/journal/leg_321.md`

Both files pointed at `DIRECTION.md:13330` / `:13338` by line number. Directly re-grepped
before editing: `grep -n "6.855x wider\|6.855x width ratio" DIRECTION.md` -> `:13614` and
`:13622` — i.e. the line numbers had already drifted by 284 lines from what §16.1 recorded,
confirming the verifier's finding that they were "wrong at merge base already." Both sites are
now anchored to the quoted text itself (`6.855x wider` / `6.855x width ratio`), which is
grep-stable under further `DIRECTION.md` edits. `DIRECTION.md` itself was not opened for
editing — the two immutable gate-text sites there stay exactly as leg 300 wrote them, per the
standing rule (`CORRECTIONS.md` §16.1, leg 319's §2/§15.3 reasoning, reaffirmed rather than
re-argued here).

### (iii) `writeup/4_p2_lottery/TECHNICAL_P2_APIA_V1.md` §5 (leg 312's landed timing prose)

| quantity | before | after | source (`writeup/data/p2_route_apia_v1.json`) |
|---|---|---|---|
| leg-176 `N=1024` cost | `117s` | `100.553s` | `N1024_seconds = 100.55331301689148` |
| leg-176 `N=512` cross-check | `48s` | `35.515s` | `N512_seconds = 35.51472449302673` |
| combined wall time | `346s` (`25.9s + 309.0s`) | `345.478s` (`36.480s + 308.998s`) | `total_seconds` (leg 178) `= 36.47977542877197`; `total_seconds` (leg 176) `= 308.99795627593994` |

Two independent defects in the original prose, both non-claim-bearing (this is performance
bookkeeping, not part of the leg 312 gate, which was about the two re-measured float64
quantities): the `N1024`/`N512` figures did not match the JSON's own fields, and the "346s"
combined figure did not even reproduce its own stated addends (`25.9 + 309.0 = 334.9 ≠ 346`).
The JSON is treated as authoritative and cited directly rather than re-derived — every number
above was already present in `writeup/data/p2_route_apia_v1.json` before this leg started.

### (iv) `experiments/JOURNAL.md:5018` and `writeup/INDEX.md:124`

Both repeated, verbatim, "largest elasticity (−13.708% of window per 1%)" for `C1_c_lap` —
already measured false by leg 336 against `writeup/data/p2_route_dwm_v1.json`'s own
`M2_pct_of_window_per_1pct` field (`CORRECTIONS.md` §18.1: `C9_a1` at `−31.058%` is `2.27×`
larger in magnitude). Leg 336 flagged, but explicitly declined to edit, these two sites at
§18.4 because they sit outside its own declared territory (integration ledgers). This leg
applies leg 336's already-measured correction with inline `[CORRECTED …]` markers, quoting the
original sentence rather than deleting it (the `JOURNAL.md`/`INDEX.md` convention set at
`CORRECTIONS.md` §17.4). No new measurement of the ledger is made here — every number in the
markers is quoted from `CORRECTIONS.md` §18.1.

## 3. The batched CORRECTIONS entry

`writeup/CORRECTIONS.md` §20 appended (pure append; §1-§19 untouched except for the §16.1
in-place anchor swap named in item (ii), which is itself one of the four named sites). §20
names all four sites in one table with before/after/source, states the amendment note for item
(iv), and closes with a ceiling paragraph: 0 numbers re-derived, 4 sites corrected, 0 gate
answers changed anywhere, 0 bans touched, `plan_of_record.py`/`DIRECTION.md` untouched.

## 4. Diff-check (gate's own requirement)

`git diff --stat` against the merge base, before adding the new `leg_338.md` journal/novelty
files:

```
experiments/JOURNAL.md                       |  8 +++-
experiments/journal/leg_321.md               |  7 +++-
writeup/4_p2_lottery/TECHNICAL_P2_APIA_V1.md | 19 ++++++---
writeup/CORRECTIONS.md                       | 60 +++++++++++++++++++++++-----
writeup/INDEX.md                             |  2 +-
writeup/data/p2_route_fus_v1.json            |  2 +-
6 files changed, 76 insertions(+), 22 deletions(-)
```

Read every hunk (`git diff` in full, not just `--stat`): every changed line sits inside one of
the four named sites or the §20 append; no argument, gate text, gate answer, banked number
(other than the four corrected ones, each cited to its own authoritative source), or figure
reference was touched. `writeup/data/p2_route_apia_v1.json` and `writeup/data/p2_route_dwm_v1.json`
are read-only in this leg — `git diff --stat` for both shows no change.

## 5. Territory and discipline followed

Exactly the six files named in `DIRECTION.md`'s territory clause (as extended by the
cycle-10 amendment): `writeup/data/p2_route_fus_v1.json`, `writeup/CORRECTIONS.md`,
`experiments/journal/leg_321.md`, `writeup/4_p2_lottery/TECHNICAL_P2_APIA_V1.md`,
`experiments/JOURNAL.md`, `writeup/INDEX.md`, plus this leg's own quartet
(`writeup/novelty/leg_338.md`, `experiments/journal/leg_338.md`). No solver module read,
written, or imported. No banned family touched. `plan_of_record.py` not opened for editing.
`DIRECTION.md` read only (to confirm the spec and the amendment, and to grep the current line
numbers for the anchor fix), never edited.

## 6. What was not done

No solver code run, no new numerical computation. Every corrected value is a transcription
already present in an already-banked source (a paper's own author list, `DIRECTION.md`'s own
live text, `writeup/data/p2_route_apia_v1.json`, `writeup/CORRECTIONS.md` §18.1). This leg's
only original work is locating the four sites' before-text precisely (by direct grep/read, not
from the DIRECTION.md spec's paraphrase) and writing the corrections and the batched entry.

## 7. Runtime

No solver runtime; a read/grep/edit correction leg. Wall-clock for the leg's research portion:
one arXiv author-list fetch plus grep/read/diff work, no compute jobs.

## 8. Walls, restated

* Wall 1/Wall 2 stand. Correcting a citation spelling, a stale line-number pointer, a timing
  arithmetic slip, and an already-adjudicated superlative does not discharge any obligation.
  No link of the `L1 -> L4` chain moves.
* Clay odds ~0.05%, unchanged.
