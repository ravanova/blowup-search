# Leg 366 — Route-LCB5 (Light Corrections Batch 5)

**Date:** 2026-08-12. **Role:** LEG, light-corrections batch, floor-eligible.
**Territory:** `experiments/journal/leg_341.md` (marker only, append),
`experiments/p2_route_pnrv_v1_postrepair.py` (the one diagnostic `zip`, nothing else in that
file), `solver/dssp_screen.py` (citation text only, two named locations) plus a re-run of
`test_dssp_screen.py`, `writeup/CORRECTIONS.md` (append one batched entry),
`writeup/novelty/leg_366.md`, this journal.

## Dispatch, restated

Three small, independently source-verified items, each with an inline marker/comment citing its
source, plus one batched `writeup/CORRECTIONS.md` entry.

## Item (i) — marker at leg 341's S2 site

Read `experiments/journal/leg_341.md`'s S2 section ("the NRS/Tsai composition DISSOLVES") and leg
359's adjudication (`experiments/journal/leg_359.md`, `writeup/data/p2_route_l3bd_v1.json`) in
full. Leg 359, reading Tsai 1998's Theorem 2 at primary text, found the local-energy-estimates
route needs no `L^q` integrability at all and reaches exactly this repo's target's decay class
(Tsai's own headline motivating example, eq. 1.5, `-1` exponent, vs. the target's measured
`-0.9971`) — so leg 341's decay-insufficiency ground is incomplete; the sole surviving ground is
the ansatz clause (SS-vs-DSS), which leg 341 already stated independently in its own "DSS
position, stated independently" subsection.

Added a blockquote marker immediately after leg 341's `**Verdict: DISSOLVES.**` paragraph, citing
leg 359 verbatim-sourced and pointing at the ansatz subsection as the sole surviving ground. Leg
341's original text is left fully intact — nothing removed, nothing edited out. This is a
grounds-vs-width correction: the width/conclusion (`NOT EXCLUDED`) is unchanged.

## Item (ii) — leg 229's diagnostic zip row-ordering bug

Read `experiments/journal/leg_229.md` §5a in full: leg 229 diagnosed, but (out of its own
territory) did not fix, that `m4_v4_grid_converged_a_max.comparison_to_leg226.per_row` in
`experiments/p2_route_pnrv_v1_postrepair.py` was built by `zip(v4_rows, t4["rows"])` — pairing
this leg's rows (`n`-outer/`a`-inner order) against leg 226's banked table (`a`-outer/`n`-inner
order) by position. Leg 229 verified this was cosmetic (all 15 `(n,a)`-keyed rows bit-identical)
and did not touch the actual gate (`v4_verdicts()`, built from a value-keyed lookup, `r["a"] ==
a`, never exposed to `per_row`).

Fixed the zip to key `t4["rows"]` on `(n, a)` explicitly, with an inline comment citing leg 229's
journal as the source of the diagnosis. Confirmed the fix demonstrates the claimed cosmetic status
live rather than trusting leg 229's prose: reconstructed both the OLD and NEW `per_row` blocks
from the already-banked `v4_rows` (in `writeup/data/p2_route_pnrv_v1_postrepair.json`) and leg
226's own already-banked table (fetched fresh via `git show
leg/226-pnr-v1-resume:writeup/data/p2_route_pnr_v1_repair.json`).

```
old (buggy positional zip) spurious mismatches (c_rel_err>1e-9): 12 / 15
new (fixed, keyed on (n,a)) spurious mismatches (c_rel_err>1e-9): 0 / 15
gate (v11_own_test) identical regardless of per_row fix: True
```

`data["gate"]["answer"]` (`"YES"`) is unaffected either way — it is assembled from
`m4_v4_grid_converged_a_max["v11_own_test"]`/`["repaired_verdict"]`, computed directly from
`v4_rows` via `v4_verdicts()`, never from `per_row`. Did not touch the banked JSON itself — this
leg's territory is the script only, and re-running the full ~40-minute solve is out of scope for a
light-corrections batch; the demonstration above uses the already-banked data, which the fix does
not change (the fix only changes how a diagnostic display is assembled from already-computed
rows).

## Item (iii) — `solver/dssp_screen.py`'s T1 citation re-attribution

Read `experiments/journal/leg_364.md` and `writeup/data/p2_route_nrsv_v1.json` in full before
touching the code. Leg 364 found: `_ledger_nrs_tsai_three_way()`'s `EXCLUDED-BY-T1` branch
`deciding_clause` (originally lines 534–539) and the leg-362 header-comment quote (originally
lines 364–367) cite Tsai 1998's Theorem 1 (`q ∈ (3,∞]`, open at 3, excludes `q=3`) to justify the
`q=3`-exact test `l3_norm_ladder()` actually implements — but the `q=3` result is NRS 1996's own,
attested secondhand (NRS 1996 itself remains paywalled after three independent refusal-to-obtain
attempts: legs 253, 359, 364) at two independent obtainable sources: Tsai 1998, p.30 (his own
restatement of NRS's result, immediately before stating his disjoint Theorem 1) and the Bradshaw &
Tsai survey, arXiv:1802.00038, p.3.

Re-attributed both cited locations to NRS 1996, quoting both secondhand-attestation sources in
full and retaining Tsai's Theorem 1 text for contrast (so a reader can see exactly what the ledger
used to cite and why it was the wrong theorem). Did not touch `l3_norm_ladder()`'s operational
logic, any verdict/reason string, or the branch structure — citation text only, in the two named
locations, exactly as dispatched.

**Re-ran the full test battery after the edit:**

```
$ .venv/bin/python test_dssp_screen.py
[... 19 checks ...]
ALL DSSP-SCREEN TESTS PASSED
```

**Confirmed all banked JSONs unmoved:**

```
$ git status --short
 M experiments/p2_route_pnrv_v1_postrepair.py
 M solver/dssp_screen.py
$ git diff --stat -- writeup/data/p2_route_dsspb7_v1.json writeup/data/p2_route_b7x_v1.json
(empty)
```

No file under `writeup/data/` appears in `git status` at all — every banked JSON `dssp_screen.py`
reads (`p2_route_ctrx_v1.json`, `p2_route_pvlx_v1.json`, `p2_route_dsspb7_v1.json`,
`p2_route_b7x_v1.json`, and any other the test battery checks against) is byte-unchanged.

## Gate

All three items applied with inline markers/comments citing their sources. One batched
`writeup/CORRECTIONS.md` §28 entry covers all three. All banked JSONs confirmed read-only
(`git status`/`git diff --stat` empty on every one). `test_dssp_screen.py`'s verdict battery
re-run and reproduces unmoved (19/19 pass). **GATE: YES** — none of the three items resisted.

## What this is not

No new number derived beyond the two live demonstrations in the item (ii)/(iii) sections above
(both re-derivations from already-banked data). No figure. No ban touched. `plan_of_record.py` and
`DIRECTION.md` untouched. No link of the `L1 → L4` chain moved. Clay odds unchanged, ~0.05%.
