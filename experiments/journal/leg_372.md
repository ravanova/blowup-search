# Leg 372 — Route-IDXB: close INDEX gap-list items 9, 10, and 13 — the batched evidence follow-up, cycle 5 reserved (322/327's exact shape)

**Branch** `leg/372-idxb-v1` (worktree `.leg372-work`), from `origin/main`. **LEG role**, light
difficulty. `plan_of_record.py` and `DIRECTION.md` untouched. No solve run, no re-measurement.
No banked JSON file (`writeup/data/*.json`) edited anywhere — verified with `git diff --stat`
below.

## The debt

`writeup/build_figures.py`'s own comment, added by leg 327, named items 9, 10 and 13 of
`writeup/INDEX.md`'s quartet-gap list as "the same class of debt" as leg 302's fig69 problem
and reserved them "for a later batched follow-up leg." This leg is that follow-up.

## Step 1 — read the gap list itself (not reconstructed from context)

`writeup/INDEX.md`'s "Quartet gaps found (plain list)" section, items 9, 10 and 13, read in
full before any other step:

- **Item 9** (Weight-repairs v1): originally flagged as missing `*_evidence.py`, but the
  item's own text already carries a correction — "**✖ WITHDRAWN by leg 138** — this clause
  was wrong when written, not stale," citing `writeup/4_p2_lottery/p2_weight_repairs_v1_evidence.py`
  landing in commit `9d9b7ea`, pre-dating leg 108's own pass.
- **Item 10** (Route-KA v1, leg 61): TECHNICAL exists, no BLOG, no `*_evidence.py`, no figure.
  "Not fixed here: BLOG prose and figure selection are claim-bearing" (a DOCS-remit leg wrote
  the item, correctly declining to make claim-bearing choices).
- **Item 13** (Route-M2P v1, leg 125 + Route-NKR v1, leg 128): both ship BLOG/TECHNICAL with no
  `*_evidence.py`. M2P has `fig61` already assigned (5 of 6 pieces). NKR has no figure by
  design (its own header: "a repair leg with no curve to plot") — 4 of 5 pieces, only
  `*_evidence.py` owed.

## Step 2 — read legs 322 and 327's actual banked output

Read `experiments/journal/leg_322.md`, `experiments/journal/leg_327.md`,
`writeup/novelty/leg_322.md`, `writeup/novelty/leg_327.md`, and the two evidence scripts they
produced (`writeup/build_figures.py`'s `fig_route_ivax_v1()` stanza, and
`experiments/p2_route_p2t1_v1_evidence.py`). The shape: a standalone script, importing no
solver module, reading only the route's own curated JSON by key, asserting several self-checks
before drawing (leg 327's G1–G5 pattern), producing a figure byte-identical across reruns
(verified by `sha256sum`), and registered additively in `writeup/build_figures.py`'s
`P2_EVIDENCE` list.

## Step 3 — the leg-310 lesson: check for intervening closers first

Confirmed on disk before writing anything:

```
find . -iname "*route_ka*"    -> runner + data JSON only, no evidence.py, no BLOG, no figure
find . -iname "*m2p*"         -> runner, data JSON, BLOG, TECHNICAL, fig61 — no evidence.py
find . -iname "*nkr*"         -> runner, data JSON, BLOG, TECHNICAL — no evidence.py, no figure
```

and `git log --all --oneline --grep="Route-KA\|Kawahara\|M2P\|NKR" -i`, which turns up leg 61
(construction), leg 105 (an unrelated post-repair regression check that happens to run the
Kawahara certificate as a known-answer battery, not a quartet-completion leg), leg 125, and
leg 128 — no intervening leg produced any of the three items' missing artifacts. For item 9,
the intervening closer (leg 138) is confirmed as already recorded in `writeup/INDEX.md`'s own
item-9 text and `writeup/4_p2_lottery/p2_weight_repairs_v1_evidence.py` is confirmed present
and runnable — cited, not duplicated.

## What was built

**`experiments/p2_route_ka_v1_kawahara_evidence.py`** (new). Reads only
`writeup/data/p2_route_ka_v1_kawahara.json`. 8/8 self-checks pass (Reading A/B, both
ablations, resolution sweep, poisoning linearity). No figure produced: this route's own
`TECHNICAL_P2_ROUTEKA_V1.md` header states "No figure ... established convention" — item 10's
`F` gap is therefore corrected to "not a gap, by design" rather than closed with an
unauthorized figure-number self-allocation (`writeup/INDEX.md`'s own standing practice: "legs
must not pick their own numbers; integration allocates," and this leg's dispatch carried no
figure reservation).

**`experiments/p2_route_m2p_v1_promotion_evidence.py`** (new). Reads only
`writeup/data/p2_route_m2p_v1_promotion.json`, imports no solver module, rebuilds the already-
assigned `fig61_route_m2p_v1_promotion.png`. Panel (a) is the one deliberate content change
from the original `build_figure()` (which re-solves Newton live): here it plots only Chen's
closed form from banked constants, with the banked Newton-reconstruction numbers as an
annotation instead of a re-solved curve. Ran twice, confirmed byte-identical
(`sha256sum` = `0c44b3554dce1b7da98fc73c70435f3f67cc8c6359cbe842ba5082df5577e6dc` both times).
Registered in `writeup/build_figures.py`'s `P2_EVIDENCE` list.

**`experiments/p2_route_nkr_v1_repair_evidence.py`** (new). Reads only
`writeup/data/p2_route_nkr_v1_repair.json`, imports no solver module. 6/6 self-checks pass
(gate a/b/c and the overall answer). No figure: this route's own `TECHNICAL_P2_ROUTENKR_V1.md`
header states the same "no curve to plot, established convention" as Route-KA v1's. Not
registered in `P2_EVIDENCE`, same reasoning.

`writeup/INDEX.md`: the three named rows (Weight-repairs v1, Route-KA v1, Route-M2P v1,
Route-NKR v1 — four rows for three items, since item 13 covers two routes) updated, plus their
gap-list annotations (items 9, 10, 13) extended with this leg's findings. `writeup/CORRECTIONS.md`
§29, one batched entry covering all three items' disposition.

## Verification run

```
$ python3 experiments/p2_route_ka_v1_kawahara_evidence.py
  [PASS] x8
8/8 checks pass -- ... no re-run.

$ python3 experiments/p2_route_m2p_v1_promotion_evidence.py   (run twice)
wrote .../fig61_route_m2p_v1_promotion.png
$ sha256sum writeup/figures/fig61_route_m2p_v1_promotion.png   (both runs)
0c44b3554dce1b7da98fc73c70435f3f67cc8c6359cbe842ba5082df5577e6dc  (identical both times)

$ python3 experiments/p2_route_nkr_v1_repair_evidence.py
  [PASS] x6
6/6 checks pass -- ... no solver import, no re-run.
```

`git diff --stat -- writeup/data/` is empty — no banked JSON touched anywhere.

## Territory

Touched: 3 new evidence scripts under `experiments/` (KA, M2P, NKR), 1 regenerated figure
(`writeup/figures/fig61_route_m2p_v1_promotion.png`), one additive line + one comment edit in
`writeup/build_figures.py`, `writeup/INDEX.md` (the named rows + gap-list items 9/10/13),
`writeup/CORRECTIONS.md` (one new §29), `writeup/novelty/leg_372.md` (new), this file (new).
`plan_of_record.py` and `DIRECTION.md` not touched. No solver module read or touched, no
BANNED item lifted, weakened, or reinterpreted, nothing escalated, nothing lifted. Clay odds
unchanged ~0.05%.
