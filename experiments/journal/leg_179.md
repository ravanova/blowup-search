# Leg 179 — ROUTE-PUB1: the combined publication-scoping methodological note

**Branch** `leg/179-pub1-v1`. **Slot** exploration/writeup, not critical path.
**Gate answer: YES.** Banked as the current publication-scoping draft and **flagged to the
user as ready for their own review — this leg's landing does not approve it.**

> **Gate (`DIRECTION.md` ### 179, verbatim).** Does the combined note state all four bundled
> results (plus 161's and 162's findings) accurately, with leg 58's superseded status and leg
> 162's honest ambiguity both stated explicitly, and does it correct leg 157's stale framing
> note?
>
> **yes →** Bank the combined note as the current publication-scoping draft; flag it to the
> user as ready for their own review, not as something this leg's landing itself approves.

## Sequence

1. `.venv/bin/python plan_of_record.py` run first; every live ban read. None binds a
   literature/synthesis leg that computes nothing: no gCLM measurement, no Route-D bound
   sharpened or re-opened, no DSS re-ask, no GA compute, no new `ℓ¹`-Fourier or collocation
   machinery, no solver module built (so the `capabilities.py` grep-before-building ban has
   no object here — `capabilities.py` was read, not edited).
2. `CONTINUATION_PROMPT.md` read in full; `DIRECTION.md` §179 read in full.
3. **Novelty pass FIRST**, committed before a line of the note existed
   (`writeup/novelty/leg_179.md`, `48cc50b`, verdict `PROCEED_AS_SYNTHESIS_ONLY`, links not
   counts).
4. The note: `TECHNICAL_P2_PUB1_V1.md`, then `BLOG_P2_PUB1_V1.md`, then the one-sentence
   housekeeping correction, then this file.

## The novelty pass is the gate, and that is the point of a bundling leg

A synthesis leg cannot answer its gate from a literature sweep — it has nothing of its own to
search for. What it can be wrong about is **provenance**, so the pass was run as a provenance
audit: every claim the note would make was read back from *the JSON or journal of the leg that
produced it*, never from prose about it. Sixty-plus numbers across legs 58, 65, 126, 127, 157,
161, 162 and (folded in beyond the original spec) 163, 165, 172, 175.

**Every one reproduces.** No bundled claim required softening; none was found misstated in its
own source; the gate's no-branch (report exactly which claim fails to reproduce, and escalate
rather than silently soften) did not fire. The full row-by-row table is in the novelty log.

A representative slice, so the claim is checkable rather than asserted:

| bundled item | number checked | source read |
|---|---|---|
| exponent-sum law | minimum **0.98**; ablation **0.98 → 0.00**, 9/9 values of `s` | `TECHNICAL_P2_ROUTED_SPACES.md` §3–§4 |
| discrete-ball trap | **2994.16 / 12233.31 / 49698.93** vs flat control **1.0275/1.0273/1.0272**, growth **`J^2.026`** | `writeup/data/p2_route_d_v6_bounds.json` |
| leg 58 (superseded) | kernel exponent **−2.0024**, crossing **1.0**, `μ = 2` control `Z₁` **0.174027** | `writeup/data/p2_route_ng_v1_nogo.json` |
| leg 127 (current) | **0.9925 / 0.6985 / 0.3202**, deviation **0.0219**, 196/196 rows, min slack **7.7343e−10** | `writeup/data/p2_route_ngx_v1_general.json` |
| leg 126 audit | **1686/1686**, `144/1032/510`, decades **1.019466 / 0.067202 / 0.171055 / 0.781209**, sum **1.0000000000000002** | `writeup/data/p2_route_bx_v1_stageb.json` |
| leg 161 | residual **7.92e−16** vs **6.05e−02** off-parameter; `LSS_A_C` offset **4.89e−08** | `experiments/journal/leg_161.md` |
| leg 162 (parked) | **0.2737 / 0.0874** at `a = 0.8`; **0.7370 / 0.2287** at `a = 0.5`; 2-of-4 gauges; 27/120 | `git show 0471428:experiments/journal/leg_162.md` |

## The three things the gate specifically asked for, and where each is

1. **Leg 58's superseded status, explicit.** `TECHNICAL` §3 opens with a boxed warning before
   any statement of the result: two versions exist, leg 127's supersedes leg 58's, and an
   external presentation citing the `A21 = 0` version is citing a weaker theorem than this
   repository holds. Leg 58's own scope line is then quoted verbatim as the sentence leg 127
   retired. The superseded form is **kept**, not deleted, because its hypothesis-necessity
   control (`μ` dial, `0.174027` at `μ = 2`) is still the sharpest one available and because
   its hypothesis (H2) is exactly what leg 162's corner turns on. `BLOG` §3 carries the same
   warning in plain language.
2. **Leg 162's ambiguity, explicit and unresolved.** `TECHNICAL` §5.2 states both readings in
   leg 162's own words — *"leg 126's completeness claim is reversed"* and *"leg 126's
   completeness claim was always scoped to an object where this corner is empty"* — as **both
   defensible readings of the same measurements**, says the choice is a human decision that has
   not been made, and says that anything published from §4 must carry §5.2 with it. The
   2-of-4-gauge caveat, the 27/120 count, and the `Z₂`/`a ≤ 1/2` ceiling all travel with the
   numbers rather than following them. It is also labelled at the top of the section as an
   **open escalation whose branch was deliberately not merged**.
3. **Leg 157's stale framing, corrected.** `writeup/novelty/leg_157.md` line 23 described leg
   127 as *"Route-NGX, live"* and its subject as *"the open `A21 != 0` question."* Leg 127
   landed and closed it. The row now reads **landed**, records that the question was open when
   that pass was run and is closed since, points at leg 127's JSON, and is explicitly labelled
   as a housekeeping correction applied by this leg. **One row, one sentence.** Leg 157's
   finding — every preconditioning induces `A21 = 0`; BPRT Thm 2.6 fails compact resolvent
   (`20 → 32` at `r = 10`, `min|λ|` *decreasing* `0.5392 → 0.3146`) against a control holding
   exactly `floor(r)`; a diagonal conjugation cannot move a diagonal — is untouched.

## Four things folded in beyond the drafted scope, and one deliberately not

The dispatch instructed the bundle be as current as the banked record allows without inventing
claims. `TECHNICAL` §6 carries four addenda, each stated as its own leg banked it:

* **165 — the weighted-energy row is de-rated twice.** Leg 141 had already left an instruction
  addressed to this note (*"three dead realizations, of which two … are confirmed novel and the
  third … is pre-empted and must be de-rated"*). Leg 165 sharpens *pre-empted* to
  *realization-dependent*: window width **0.0 → 2.0** and norm ratio **1.6777e+07 → 1.0000000000000002**
  on moving the trial space's vanishing order `p = 1 → 2`. **Consequence for this note: the
  weighted-energy result is not included as a publishable negative at all.** Leg 165's own
  tiering also matters — legs 52 and 53 come back realization-invariant and **subsumed by leg
  127**, so the note does not present them as independent companions to §3.
* **172 — Wall 2 is the field's wall, and its usual wording is wrong.** Included because it is
  what licenses §8's statement that the 1D/2D scope is not a self-imposed limit. The correction
  (time-dependent singularity, not dimension; van den Berg–Williams certified genuinely 3D
  *stationary* states) is carried, not smoothed.
* **175 — technique-specific, not model-specific.** Included because without it §§1–4 could be
  misread as evidence that a different model class would have worked.
* **163 — origin-`H²` scoped, transfer ambiguous.** Included principally for the *prohibition*
  it establishes: Xu bounds his own result in his own abstract, so "invertible there, therefore
  a certificate is possible" is forbidden and does not appear.

**Not folded in: leg 176.** `git log --all --oneline | grep "Leg 176:"` returned nothing at
draft time. The origin-`H²` construction is named as in flight and **nothing is attributed to
it** — stated in the note rather than left silent, so a later reader can tell the omission was
deliberate.

## Restraints

* **Leg 161's Restraint 1 honoured.** Leg 161 deliberately did *not* apply its
  source-independence correction to the ALS ledger row, leaving it *"for the bundle writeup to
  apply where it belongs."* The note **states** the correction (three citations, one ancestor;
  the independent second discoverer is J. Chen arXiv:1908.09385) in §5.1. It does **not** edit
  `solver/literature_gates.py` — out of this leg's territory. The ledger row still reads as
  three sources; that remains a pointer-block item for the orchestrator.
* **No shared ledger touched.** `experiments/JOURNAL.md`, `LITERATURE_CHECK.md`,
  `plan_of_record.py`, `CONTINUATION_PROMPT.md`, `PHASE2_P2_NOTES.md` are all untouched; this
  file and `writeup/novelty/leg_179.md` stand in for the first two.
* **No figure, no runner, no JSON.** This leg measures nothing — every number is quoted from
  another leg's curated JSON, and re-emitting them under a new filename would create a second
  copy that can drift from the first. The "no measurement, no figure" convention (legs 141,
  154, 165) applies. `writeup/INDEX.md` registration is **outside declared territory** and is
  left for the orchestrator to fold in.
* **The seven inherited prohibitions** (novelty log §2) are carried into the note's own §8
  rather than merely noted here: the folklore inequality is not claimed, the realization
  dichotomy is Xu's, "no certificate exists" is unwritable, the Xu-implies-certificate sentence
  is forbidden, Chen–Hou and Kosov–Temlyakov are cited as the nearest published relatives, and
  leg 52's search-index flag stands.

## Ceiling

The object is the `a = 0` CLM linearisation throughout, except §5.2's gCLM profile on
`0 < a < 1`. `Y₀` is exactly zero for the degenerate reason banked since leg 51, so every
magnitude bounds `HL_S2_nonsymmetric`'s difficulty **from below**. Nothing was measured by this
leg; the note's own §9 says so, including that the two results called theorems have exact proofs
but floating-point verifying measurements. **No link of the `L1 → L4` chain moved.** None has
in 178 legs. Clay unchanged at ~0.05%.

**For the user.** The gate's yes-branch is a bank-and-flag, not an approval. What is wanted is
a ruling on §5.2's ambiguity (which of the two readings of leg 162 vs leg 126 stands), and a
decision on whether §§1–4 go out as one note at the three different grades §8 assigns them.

## Territory

`writeup/4_p2_lottery/TECHNICAL_P2_PUB1_V1.md` (new),
`writeup/4_p2_lottery/BLOG_P2_PUB1_V1.md` (new), `writeup/novelty/leg_179.md` (new), this file
(new), and **one row** of `writeup/novelty/leg_157.md` (the declared housekeeping correction).
