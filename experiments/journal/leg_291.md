# Leg 291 — Route-HLR2 v1: has any later work certified HL_S2_nonsymmetric since stage B
closed? Journal.

**Branch** `leg/291-hlr2-v1`. **Floor-eligible (literature).** **Gate answered NO.**

## Order of work

1. `plan_of_record.py` read first (`.venv/bin/python plan_of_record.py`, run from
   `/home/andy/projects/Unsolved/.venv/bin/python` since the worktree has no `.venv` of its
   own). Every live ban carried; none binds a pure literature leg with no solver import.
2. `ORCHESTRATOR_PROMPT.md`, `ORCHESTRATION.md`, `DIRECTION.md` §291 read for the operating
   contract, gate and territory.
3. Read (never edited) `experiments/journal/leg_54.md`, `leg_55.md`, `leg_57.md`, `leg_126.md`
   for exactly what `HL_S2_nonsymmetric` is and how it was dated/closed.
4. **Novelty pass FIRST, committed before the runner existed** —
   `writeup/novelty/leg_291.md`, commit `ce10c84`. It planned the channel list and, while
   doing so, found the thing that makes this leg non-redundant with the repository's existing
   EXT-family literature-watch series (§1 below).
5. Ran the channels for real (arXiv API, Semantic Scholar, OpenAlex, two web searches, one
   direct author-page fetch), then built the runner and curated JSON from the actual results.

## §1. Why this leg is not a duplicate of the EXT family (74/77/82/90/93/123)

This repository already runs dated literature watches naming `HL_S2_nonsymmetric` directly
(EXT legs 74/77/82/90, rank 1) and, most recently, a widened net (leg 123, Route-EXT6,
"any inviscid self-similar profile by any method"). The novelty pass found the dispositive
fact that keeps leg 291 from just re-running leg 123: **leg 123's window closed at
2026-08-06T11:01:32Z (commit `de31eb7`), roughly eleven hours *before* the pivot ruling
landed** (leg 0, commit `4ff544a`, 2026-08-06T22:37:34+01:00 = 21:37:34Z). No leg of any
kind — EXT-family or otherwise — has re-asked "has anyone certified this" since the pivot,
which is exactly leg 291's brief: not whether the world has moved in the last few hours in
general (EXT123 already covers up to its own close), but whether it has moved **since this
repository's own programme pivoted away from the object**, which is a strictly later and
previously-unchecked boundary.

## §2. Two windows checked, both NO

Following the gate's own two named boundaries:

* **Wide window** — since legs 55/57's dating of the object (2026-08-05) through the pass
  time (2026-08-07T05:30Z), ~44 hours.
* **Narrow window** — since the pivot specifically (2026-08-06T21:37:34Z) through the pass
  time, ~8 hours. This is the framing the leg's own thesis foregrounds.

**Both: NO.** Fourteen channels run (12 structured endpoints + 2 web searches), zero return
a candidate inside either window. The decisive channel (C7, the raw `cat:math.AP` firehose,
newest-first) shows arXiv's newest announced math.AP paper as of the pass time predates the
*pivot* by ~4h39m — i.e. literally zero math.AP papers of any kind have been announced since
the pivot, so the narrow-window negative is a full-firehose enumeration, not a keyword-luck
argument. Reused, not re-derived: the channel *methodology* (arXiv ID/author/keyword
enumeration, Semantic Scholar citations, OpenAlex, the firehose-bounding trick) is the
EXT-family's own, proven across six prior legs; only the window and the specific object-name
framing are new to this leg.

## §3. Channels run, links not counts (leg 52's search-index flag respected)

* **C1a/C1b** — version-history re-check, `arXiv:2604.01868` (still v1, unchanged) and
  `arXiv:2603.25104` (still v2, unchanged).
* **C2a/C2b/C2c** — author enumeration (De Huang, Bojin Chen, Xiangyuan Li), newest-first:
  nothing newer than the source paper itself for any of the three.
* **C2d** — `https://sites.google.com/view/de-huang/research`, fetched directly (`WebFetch`).
  Lists four Hou-Luo-family papers (the two 2025/2022 analytic exclusions,
  `arXiv:2603.25104`, `arXiv:2604.01868`) and no successor. The author's own page is the
  strongest single "silence" signal collected.
* **C3** — Semantic Scholar `citations` endpoint on `arXiv:2604.01868`: **0 citations.**
* **C4a** — OpenAlex `cites:` filter on the paper's own work ID (`W7148668037`, resolved via
  its DOI): **0 citing works**, consistent with C3.
* **C4b** — OpenAlex full-text search for the literal string `2604.01868`: **1 hit**,
  `arXiv:2605.15130` (Jiajie Chen, *"Asymptotically Self-Similar Blowup for 3D Incompressible
  Euler with `C^{1,1/3-}` Velocity II"*). Read at abstract + reference level: it cites
  Chen-Huang-Li's 1D construction as an input to a **different object** (a 3D axisymmetric
  Euler profile *lifted from* a 1D one), by a **fixed-point analytic** method — not
  computer-assisted, not interval arithmetic, and not a statement about
  `HL_S2_nonsymmetric` itself. It also predates both windows: submitted 2026-05-14, more
  than eleven weeks before stage B closed. Recorded in the JSON as a near-miss, not a YES.
* **C5** — arXiv keyword enumeration `"non-symmetric" AND "self-similar"`: newest entry
  (`arXiv:2605.27974`, 2026-05-27) is off-topic (Dirichlet forms) and predates both windows.
* **C6** — arXiv keyword enumeration `"interval arithmetic" AND "self-similar"`: newest hit
  is `arXiv:2604.09949`, the already-audited `CLAIMED_UNUSABLE` 3D-NS claim
  (`PHASE2_P2_NOTES.md` M-5) — same object as before, no update, and irrelevant to
  `HL_S2_nonsymmetric` regardless.
* **C7** — the full `cat:math.AP` firehose, newest 15, newest-first (decisive channel,
  §2 above).
* **W1/W2** — two open web searches (`WebSearch`), links recorded in the JSON; both return
  only already-known papers (the source itself, the two analytic exclusions, the
  Huang-Qin-Wang-Wei CMP publication, the author's own page).

## §4. Bans respected

No gCLM measurement (nothing computed — pure literature). No Route-D sharpening; no bound
touched. No DSS re-ask. `solver/*.py` and `capabilities.py` not opened for writing anywhere
in this leg — territory is literature-only, taken literally (matching the EXT-family's own
convention, and leg 123's explicit precedent for this class of leg). The five shared
ledgers (`PHASE2_P2_NOTES.md`, `LITERATURE_CHECK.md`, `DIRECTION.md`, `plan_of_record.py`,
`CONTINUATION_PROMPT.md`) untouched.

## §5. Consequence, reported and not applied

**No link of the current Clay programme's L1→L4 chain moves** — that question is closed and
superseded by the pivot, exactly as the brief states. What this leg banks is retrospective
material: `HL_S2_nonsymmetric` remains uncertified, exactly as legs 54/55/57/126 last
measured it (numerical only, no proof of any kind), through the widest reading of "since
stage B closed" this repository has ever checked, and specifically through the narrower
window — since the pivot — that no prior leg had checked at all. A future retrospective of
this repository's original ten-plus-leg L1/M/PORT/V/C-PILOT/L1/T/TC/MM/NG/B programme can
now say the target it spent that effort on was, and as of this leg's pass time remains, open
in the literature — not merely "we stopped looking."

## Artifacts

| what | path |
|---|---|
| novelty pass (committed first) | `writeup/novelty/leg_291.md` |
| runner | `experiments/p2_route_hlr2_v1_precedent.py` |
| curated JSON | `writeup/data/p2_route_hlr2_v1_precedent.json` |
| journal | `experiments/journal/leg_291.md` (this file) |

No BLOG/TECHNICAL/figure — this leg's declared territory (`DIRECTION.md` §291) is the four
files above, matching the EXT-family's own convention for pure dated-watch literature legs
(leg 123's territory is the identical four-file shape).

**Clay stays ~0.05% behind Walls 1 and 2. No link of the chain moved.**
