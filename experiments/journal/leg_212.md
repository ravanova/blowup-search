# Leg 212 — ROUTE-USC2V: leg 196's STILL_SHORT, re-verified from the primary source

**Date 2026-08-06. Branch `verify/212-usc2v-v1`. VERIFY role. Literature-only; no solver
module read-write; no ledger edited; no figure registered.**

**Gate, verbatim:** *"Does an independent read of `arXiv:2511.22819` at full-text depth confirm:
(a) no certificate is stated or locatable, (b) 0 of leg 175's 4 open items are closed, and
(c) the same three obstructions leg 196 named are actually present in the paper's own text?"*

**Answer: YES, on all three clauses. 29/29 comparison rows agree, 0 disagreements.** Leg 196's
finding stands doubly-checked. **No escalation.** Three locator slips are recorded below; none
of them moves the gate, and each is reported with the reason it does not.

---

## Artifacts

| file | what |
|---|---|
| `writeup/novelty/leg_212.md` | light novelty pass, committed **before** construction (`9077910`) |
| `experiments/p2_route_usc2v_v1_verification.py` | the comparator; verdict **computed**, five self-tests prove it can answer NO on each branch |
| `writeup/data/p2_route_usc2v_v1_verification.json` | every claim, its verification status, and the three locator notes |
| `experiments/journal/leg_212.md` | this file |

Read-only, never edited: `experiments/journal/leg_196.md`, `writeup/data/p2_route_usc2_v1_lit.json`,
`solver/viscous_novelty.py`. No shared ledger touched.

## 1. What "independent" means here, precisely

This leg did **not** re-read leg 196's summary and rubber-stamp it. It pulled the PDF itself
from `https://arxiv.org/pdf/2511.22819` and extracted it itself:

| | |
|---|---|
| PDF | md5 `8367d9b73bbc615a65da1f99bb3e520e`, 3 934 832 bytes, 27 pp |
| extraction | `pdftotext -layout`, md5 `5e20c57bf6983934fd1f40292618545c`, 1593 lines |

**The extraction came out byte-identical to leg 196's.** That is not a shortcut taken — it is a
result obtained, and it is the strongest reproducibility check available on a literature leg:
two independent fetch-and-extract passes produced literally the same characters, so leg 196's
line locators can be checked here with **no pdftotext-version ambiguity**. Everything below was
then read off that text by this leg, by hand and by its own term census. The runner imports
nothing from `experiments/p2_route_usc2_v1_lit.py`.

## 2. Clause (a) — no certificate. CONFIRMED, on two independent discriminators

**Apparatus census, this leg's own count over all 1593 lines:**

| term | count |
|---|---|
| `interval arithmetic` | **0** |
| `enclosure` / `enclos` | **0** |
| `validated numerics` | **0** |
| `certif*` | **0** |
| `eigen*` | **0** |
| `verification` / `verified` | **0** / **0** |
| `operator` | **0** |

**Claim census.** All **4** `computer-assisted` occurrences, located and classified
independently — and the four line numbers came out **exactly** as leg 196 recorded them:

| line | section | kind |
|---|---|---|
| 41 | Abstract | PREREQUISITE |
| 49 | §1 Introduction | DEFINITION |
| 907 | §3 Gross–Pitaevskii | ASPIRATION |
| 1438 | §5 Conclusion | PREREQUISITE |

**`ACHIEVED`: 0.** The paper's own last word, verbatim at l.1437–1438: *"Beyond numerical
discovery, high accuracy is a **prerequisite** for rigorous mathematical verification via
computer-assisted proofs."* And the abstract, l.40–42: *"providing an important **ingredient**
for bridging the gap between numerical discovery and computer-assisted proofs."* Both quotes
verbatim present, both exactly as leg 196 transcribed them.

## 3. Clause (b) — 0 of leg 175's 4 open items closed. CONFIRMED

* **(a) Boussinesq / 3D Euler with boundary — OPEN.** §2 l.128–129 names the two models:
  *"two canonical models: the 1D Córdoba-Córdoba-Fontelos equation and the 2D Incompressible
  Porous Media equation."* `Boussinesq` occurs **2×** in 27 pp — abstract l.24 (recap of `[1]`)
  and ref `[2]`'s title l.1460 (Chen–Hou) — **never in the body**, exactly as leg 196 said.
  `3D Euler` also occurs **2×**: l.135, where CCF is motivated as *"a critical analog for
  understanding singularity formation in more complex systems, such as the 3D Euler
  equations"*, and the same ref `[2]`. **Neither model is solved.** These are the models
  nearest this repository's Wall-2 question and they got nothing, 251 days on.
* **(b) the ladder — OPEN, and re-aimed.** Re-derived independently: §2.3.3 l.676–679 gives
  Rmax 1e-3 → 1e-7 over 4 modes and l.688–689 gives the floor 1e-14 → 1e-10 over 4 modes, i.e.
  **1.0 decade/mode in residual**, matching leg 175's number. The λ statement runs l.690 → 769.
* **(c) the unstable-spectrum enclosure — OPEN, untouched.** **Zero `eigen`.** `spectrum`
  occurs once (l.36, *"a wide spectrum of unstable self-similar singularities"* — colloquial);
  `spectral` twice, both about **neural-network spectral bias** (l.1186, ref l.1553); `operator`
  zero times. There is no linearized-operator spectrum in this paper at all. **Leg 196's
  reading — the one obligation residual reduction cannot discharge is the one the paper is
  silent on — is confirmed at the source.**
* **(d) the CAP announced Sep 2025 — OPEN, 323 days on.** `preparation` occurs **0** times in
  the follow-up; the abs page shows **v1 only**, no journal ref; none of the **22** citers is
  such a manuscript (§5).

## 4. Clause (c) — the three author-named obstructions. ALL PRESENT AS CHARACTERIZED

**N1 — the binding quantity moved to λ's error bar. CONFIRMED, table transcribed exactly.**
Fig. 7(f), re-read by this leg at l.714–738. All five rows, both columns, match leg 196
digit-for-digit:

| mode | line | λₛ | accuracy |
|---|---|---|---|
| stable | 714 | 1.0285722760323 | ±1e-13 |
| 1st unstable | 720 | 0.472129736156 | ±2e-12 |
| 2nd unstable | 726 | 0.3149617817 | ±3e-10 |
| 3rd unstable | 732 | 0.24156641 | ±1e-8 |
| **4th unstable** | **738** | **0.1987237** | **±1e-7** |

Re-derived from that table by this leg: the residual-to-λ gap is **6.0 decades** (1e-13 vs
1e-7), and the least-squares degradation over the five rows is **1.570 decades/mode** —
**reproducing leg 196's 1.570 exactly**, against the **2.0 decades/mode** the authors state at
l.690+769 (*"decreases the achievable λ accuracy for a fixed computational budget by
approximately two orders of magnitude per model"*), a ratio of **1.274×**. Two sources agreeing
in order of magnitude, as leg 196 said, and not the same quantity, as leg 196 also said.

**N2 — the discovery frontier stalled. CONFIRMED verbatim.** §2.1.3 l.275–281: *"a persistent
non-smooth signal at the origin (inset of Fig.3b) remains for all tested λ ∈ [0.455, 0.4713].
This indicates that if a third unstable solution exists, it is located at a λ value
corresponding to an even higher-gradient profile."* And l.279–280: *"The challenge of exploring
the range λ < 0.455 and resolving such a feature in the 1D CCF model appears to be even greater
than finding highly unstable solutions in the 2D IPM equation."* Leg 196's supporting claim
that *"the full suite of previously discovered CCF solutions"* is a suite of **three** is also
verbatim in the paper, at l.157: *"Previous work successfully identified three self-similar
solutions (stable [12], 1st unstable [3, 12], and 2nd unstable [1])."*

**N3 — hardware- and arithmetic-bound. CONFIRMED.** The floor is double-float round-off by the
paper's own definition of success, l.50–51. Every remedy offered for λ accuracy is budget:
larger networks and a third stage (l.770–772), and l.774–775 verbatim — *"that can be
implemented on a single A100 GPU (80GB). To further improve the λ accuracy, one could use
larger networks or more training stages in parallel on multiple GPUs."* Nothing anywhere on
extended precision or interval arithmetic (see the zero apparatus census, §2).

**N4** was recorded by leg 196 as third-party and is outside this clause, but its
characterization was checked anyway and is exact: `arXiv:2606.25151`'s abstract states
*"poisoned models reach losses at or below the clean baseline yet differ by large margins"*
over Burgers / NS cavity / convection–diffusion, and claims no error in either singularity
paper.

## 5. The search side, re-run

`abs:"unstable singularities"` newest-first: **14 records, exactly 2 relevant** — the same pair,
reproducing leg 196's count. Abs page: **`[v1] Fri, 28 Nov 2025`, and nothing else** — no v2, no
journal ref. Semantic Scholar: **22 citers**, matching exactly; the four whose titles could
plausibly have been a certificate were pulled at abstract depth and are not one
(`2602.17570` proves lower bounds γ ≥ 2/5, γ ≥ 1/2 on *hypothetical* profiles; `2607.19700` is
a vanishing-dissipation construction; `2510.20757` is explicitly *"in principle amenable to
numerical testing"*; `2606.25151` is N4).

## 6. Three locator slips, reported because a verifier reports everything

None of these is a missed certificate, a miscounted item, or a mistranscribed obstruction — in
every case the **quoted string is verbatim present and exact**, and only the line pointer is
off. Because both passes share a byte-identical extraction, these are unambiguous:

1. `new_obstructions[N1].locator` says `sec.2.3.3 l.696`. **l.696 is a blank line.** The
   sentence it points at runs **l.690 → l.769** — a figure block splits it in the `-layout`
   extraction. The quote itself is exact.
2. `leg_175_open_items_recheck[b].evidence` says `l.684-696`. Same sentence; the range should
   read **l.684–690 (+ l.769)**.
3. `quotes[2].line = 130`. The quoted sentence occupies **l.128–129**; l.130 is the next line.

**Why this does not move the gate.** The gate asks whether the obstructions *are present in the
paper's own text as characterized*. They are — quote, table, and claim all verified at the
source. A pointer that lands a few lines off a correctly-quoted sentence is a citation-hygiene
defect, not a substantive one, and the leg-196 locators that actually carry weight (all four
CAP mentions, the five Fig. 7(f) rows, §2.1.3 l.275–281, §2.3.3 l.769–775) are **exact**.
Recorded so a later leg re-walking these citations is not misled.

## 7. Ceiling

**No link of the L1→L4 chain moved. Clay stays ~0.05%.** This leg produces no new mathematical
content and is not entitled to any; its whole output is that leg 196's STILL_SHORT is now
double-checked at the primary source.

For the DM, one line: **leg 172's Wall-2 question is unmoved, and the reason leg 196 gave for it
is now independently confirmed rather than single-sourced** — the follow-up bought precision on
a 1D and a 2D model, left the unstable-spectrum enclosure entirely untouched (zero `eigen` in
27 pages), and never returned to Boussinesq or 3D Euler with boundary. The gap that remains is
an enclosure obligation nobody has started, and that now rests on two independent reads.
