# Leg 437 — unit `K1`, arc 7: THE KERNEL CHECK — LANDED GREEN, three environments, `UNVERIFIED`

**Date:** 2026-09-10. **Role:** SERIAL, Conductor-run; this landing written by the laptop Conductor (the
session the user designated at 13:00Z), integrating three runs of one runner on one pinned commit.
**Prereg:** `leg_437_prereg.md` (pushed before any run) and its amendment `leg_437_prereg_amend.md` (§73).
**Runner:** `scripts/arc7_k1_kernel_check.sh`. **Pin:** `8937a8f4cbc7abaab5e9e97d1cc7f5d2319d9538`.
**Banked artefacts, never edited:** `writeup/data/arc7/k1/phaseA/phaseA.json`, `…/phaseB/phaseB.json`
(remote fresh container), `…/phaseB_local/phaseB.json` (the operator's laptop). Numbers below are cited by
JSON field; re-derive from the files, not from this page.

## §1. The gate, answered in `STATE.md`'s wording — (a)–(d), separately

| | (a) does `lake build` complete | (b) `#print axioms` verbatim, `_R3` / `_periodic` | (c) `sorryAx` reachable | (d) wall time and machine |
|---|---|---|---|---|
| **A** (resume, remote container 1) | YES — `build_rc` 0, `build_error_lines` 0, `Build completed successfully (11251 jobs)` | `[propext, Classical.choice, Quot.sound]` / same | **NO** | `build_s` 2047, PARTIAL by design (Euler remainder only); 4 × Xeon 2.80 GHz, 15 GB |
| **B** (fresh clone, remote container 2) | YES — `build_rc` 0, 0 error lines, 11251 jobs | same / same | **NO** | `total_s` **5605** clone→axioms (`clone_s` 2, toolchain 31, `cache_get_s` 121, `build_s` 5443, `axioms_s` 8); 4 × Xeon 2.10 GHz, 15 GB |
| **B-local** (fresh clone, the operator's laptop) | YES — `build_rc` 0, 0 error lines, 11251 jobs | same / same | **NO** | `total_s` **4272** (`clone_s` 2, `cache_get_s` 117, `build_s` 4148, `axioms_s` 5); 12 × i7-10750H 2.60 GHz, 31 GB |

All three: `tree_head` = the pin, `mathlib_rev` `85e3a25e006c35636f0e53b0e9296caca2685bc0`, `comparator_rev`
`19e111e2141cf333c7daff0f64c5f24acc91dd2e`, toolchain `leanprover/lean4:v4.34.0-rc2`, `olean_count` 2486.
**Reading, pre-committed (prereg §2): GREEN in every environment → STOP AND REPORT.** No extension, no
generalisation, no claim about Theorem 1.1.

## §2. The three limits the phase-B artefact itself banks — none softened
1. **mathlib's oleans were REPLAYED from the official cache, not rebuilt from source.** `cache_get_files`:
   8747 `.ltar` downloaded from `lakecache.blob.core.windows.net`; 0 `Built Mathlib.` lines in either build
   log. The kernel checked the project's 2486 modules against mathlib *as shipped in that cache*.
2. **The statements are Fefferman (C)/(D), NOT the paper's Theorem 1.1.** The two exported theorems are
   the DeepMind Formal-Conjectures statements (leg 431 gate (b): strictly weaker; five existence clauses of
   Theorem 1.1 absent, the blow-up rate absent). A green kernel here says nothing about Theorem 1.1.
3. **A second MACHINE is not a second AGENT.** All three runs were directed by the same Conductor role from
   the same brief. Under §3f rule 1 `K1` stays **`UNVERIFIED`** until `K2` slot 5 — a blind agent, told
   nothing of how these were produced — reproduces the kernel result from the banked artefacts.

## §3. Deviations recorded, not repaired
- The remote fresh-container session (`session_01VEipCs6B23bjiQ8aku2Pvn`) merged `origin/main` into its
  branch (816929a) and the container Conductor session (`session_01LX5qCimBmdRGUMvwBDT3DZ`) landed K1 on
  `main` at 1829d68 at 12:55Z, **while the user had designated the laptop session as Conductor**; the
  amendment said the branch would be integrated by the Conductor after reading the log. Nothing is
  retracted — the log was read here and the gate passes — but two Conductors were live on one arc for
  roughly an hour. The laptop landing (this file) is written BESIDE 1829d68's STATE row, not over it.
- The laptop's built tree from B-local was **deleted with its session scratchpad** before K2 could use it
  (ORCH_STATE had pointed K2 slots 3/5 at phase A's tree, in a container this session is not in). Rather
  than pretend a slot ran on a tree, the runner was relaunched here at 12:53Z (`B` recipe, same pin); its
  artefacts are banked under `writeup/data/arc7/k2/tree_rebuild/` as a support run, not a fourth gate answer.
- The remote B run's `toolchain_install_s` is a derived field, not a runner field (`phaseB.json` says so).

## §4. What this leg does NOT establish
Theorem 1.1; that the Lean proof follows the paper's argument; that mathlib's cached oleans match their
sources; anything about priority; verification (see §2 item 3). **No wall moved. No `L1→L4` link moved.
Tier 2 is never a proof. Clay ~0.05%.**

## §5. Addendum, 2026-09-10 — §4's olean clause split by the follow-on (landed 1b9f9ea)
§4 above stands verbatim; this is written beside it, not over it. One of its clauses — "that mathlib's
cached oleans match their sources" — was two claims in one. The follow-on
(`writeup/data/arc7/k1/phaseB_integrity/`, pre-registered `ad0f429` before it ran) establishes the
**INTEGRITY** half on the container tree: `lake exe comparator` rc 0 on both challenges, statements
identical to the challenge modules, axioms confined to the standard three, and the solution environment
replayed through the Lean kernel AND nanoda 0.4.17 — an independent Rust kernel — both accepting; nothing
recompiled. The **SEMANTIC-MATCH** half — that the oleans mean what mathlib's sources say — is untouched
and still **NOT established**; it needs a recompile of all 8371 mathlib modules. Provenance was re-checked
to labelling, not compilation. `landrun` was a declared shim. Same Conductor lineage, so it verifies
nothing: `K2` slot 3 is re-run blind, not retired. **`K1` remains `UNVERIFIED`.** Record and both rulings:
`CORRECTIONS.md` §76.

## §6. Addendum, 2026-09-11 — §4's olean clause is now CLOSED by the from-source rebuild (phase C)
§4 and §5 above stand verbatim; this is written beside them, not over them. §5 left the
**SEMANTIC-MATCH** half open — "that the oleans mean what mathlib's sources say" — needing a recompile
of all 8371 mathlib modules. **The user scheduled it. It ran. It passed.**

Pre-registered in `leg_437_phaseC_prereg.md`, readings fixed and pushed before anything built, with
`leg_437_phaseC_prereg_addendum.md` re-measuring the disk precondition after a first attempt was banked
`INCOMPLETE` (3.24 GiB free against the required ~20 GB) and stopped rather than started.

Fresh clone at the same pin `8937a8f4`, outside the repository, then `lake build` alone —
**`lake exe cache get` was never invoked, not once**. The string `cache` does not occur anywhere in the
732 KB build log. The prereg's void condition (if `Built Mathlib.` is 0 a cache was used and the run is
void) passes decisively: **8370 `Built Mathlib.` lines, 0 `Replayed Mathlib.`**

**Reading: `MATCH`** — `lake build` rc 0, 0 error lines, `Build completed successfully (11259 jobs)`, and
both theorems `[propext, Classical.choice, Quot.sound]`, compared as **bytes**: phase C, phase B laptop
and phase B container all hash to `0883b714ddd5c08564bf58dbb22edf44`, `diff` empty. `sorryAx` NO. Per the
pre-registration: **the cached oleans were not load-bearing.** Same laptop as phase B, so the cost is
like for like: `total_s` 11578 against 4272 = 2.71×, and a 28.5 GB tree.

Prereg §5(c): the comparator was re-run against this from-source tree under a **real `landrun` built from
source** (v0.1.17) — the declared shim was NOT substituted. Lean's default kernel **accepts** both
challenges. The nanoda leg did not run: `nanoda_bin` is absent and uninstallable on that machine, so the
comparator's "nanoda kernel rejected" is its phrasing for an `exec` failure, not a mathematical verdict.

**Banked unreconciled, not glossed:** lake reports 11259 jobs against phase B's 11251 and the prereg's
expected 11251, and the named target differences do not sum to the +8; and the prereg's ~20 GB disk
requirement was an underestimate (real tree 28.5 GB; ~10.7 GB was freed elsewhere mid-run by something
never identified). Neither bears on the reading.

Same Conductor lineage on the same laptop, so it verifies nothing: **`K1` remains `VERIFIED` only through
`K2` slot 5**, and the trust path now ends at the Lean compiler binary rather than at mathlib's oleans.
Artefacts `writeup/data/arc7/k1/phaseC/`; record `CORRECTIONS.md` §81.
