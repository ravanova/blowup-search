# Leg 328 — ROUTE-ORC5: execute over-read closure #5

**Branch** `leg/328-orc5-v1`. **Date** 2026-08-12. **Correction/record leg, NOT floor-eligible.**
**Difficulty** light. **No figure** (spec says so explicitly). **No runner, no JSON**: this
leg re-derives no number — every figure it uses is quoted from an already-banked source
(leg 174's occupancy matrix, leg 316's reproduction of DF-CGL, the DM's own cycle-6 Item 3
arithmetic).

**Preconditions:** none outstanding per the DM's cycle-10h ruling that dispatched this leg
into slot D.

## 0. Read before acting

`ORCHESTRATION.md` (§0 dispatch discipline, §1 law, §0a novelty-pass-before-mathematics),
`CONTINUATION_PROMPT.md` (for calibration; NOT edited — §6 reserves it to integration, already
corrected at `ce74d6b`, cited not re-touched), and `DIRECTION.md`'s leg-328 spec confirmed by
grep at `DIRECTION.md:15098` ("### 328 — ROUTE-ORC5..."), verbatim against the dispatch text
supplied. `plan_of_record.py`'s ban list confirmed via `.venv/bin/python plan_of_record.py`;
none of the live bans bear on this leg's territory, and `plan_of_record.py` itself is never
touched (md5 confirmed byte-identical before and after, §3 below).

## 1. State check before acting (the spec is several cycles old)

The dispatched spec (drafted DM cycle 6, amended cycle 6b) told this leg to verify current
state rather than trust the old line numbers or old territory list. Checked:

- **`CLAY_ROADMAP.md:343`** — still carries the over-wide sentence, unfixed. **This leg's one
  edit site.**
- **`CONTINUATION_PROMPT.md:68-80`** — already fixed, at `ce74d6b` (orchestrator). Cycle 6b
  removed this file from this leg's territory for exactly that reason. Honoured: read only,
  not edited.
- **`plan_of_record.py:47`** — already fixed, same commit `ce74d6b`. Never this leg's
  territory (the ban on touching this file is absolute per dispatch instructions); confirmed
  present and quoted, not edited.
- **`DIRECTION.md`'s two DM sites** (directive sentence + leg 303's thesis) — already fixed,
  DM cycle 6, inline `[CORRECTED …]` markers. Not this leg's territory (`DIRECTION.md` is the
  DM's file).
- **`writeup/CORRECTIONS.md`'s over-read-closure-#5 entry** — does NOT exist yet
  (`grep -c "closure #5"` at HEAD returns hits only inside leg 339's §17, which itself records
  that closure #5 has no entry — see `writeup/novelty/leg_328.md` §1). **This leg's second
  site: write it.**
- **The `6.5457e+11` label (Item 3)** — ruled by the DM as dispatched gate text, immutable.
  Not edited; carried into the CORRECTIONS entry as a pointer, per the DM's own ruling.

Conclusion: of the six sites named in the spec, four are already fixed elsewhere (two DM, two
orchestrator-on-two-files-in-one-commit) and one is immutable gate text carried as a pointer.
Exactly one site (`CLAY_ROADMAP.md:343`) needed this leg's edit, plus the CORRECTIONS.md entry
itself, which nobody had written.

## 2. What was done

1. `CLAY_ROADMAP.md:343` corrected in place with an inline `[CORRECTED 2026-08-12, over-read
   closure #5, leg 328: …]` marker, following the same convention as the DM's and
   orchestrator's fixes at the other sites — original sentence not silently deleted, corrected
   wording follows it in the same paragraph.
2. `writeup/CORRECTIONS.md` §21 appended: the closure #5 register entry, enumerating all six
   sites, their fixers, and current status, plus the Item 3 `6.5457e+11` pointer and the
   `CONTINUATION_PROMPT.md`-not-this-leg's-territory note (§21.5).
3. `writeup/novelty/leg_328.md` committed (this leg's §0a pass).
4. This journal.

## 3. Territory-clean confirmation

`plan_of_record.py` — md5 `a4ece173c3eb890043332127a7d4d900` before this leg's work and after;
byte-identical, confirmed both at worktree checkout (`origin/main` at `d797e44`) and again
immediately before push. `CONTINUATION_PROMPT.md` — read, not written; diffed against the
worktree's origin/main copy, zero changes. `DIRECTION.md` — not opened for writing at all.
Diff of this leg's commit(s) touches exactly: `CLAY_ROADMAP.md` (one paragraph, one inline
marker), `writeup/CORRECTIONS.md` (append-only, one new §21 section), `writeup/novelty/leg_328.md`
(new file), `experiments/journal/leg_328.md` (new file, this one).

## 4. Gate

**Gate.** Are both sites corrected to the measured wording and the CORRECTIONS entry complete,
with zero other content changed (diff-checked)?

- `CLAY_ROADMAP.md:343` — corrected. ✓
- `writeup/CORRECTIONS.md` closure #5 entry — written, complete, all six sites enumerated
  (§21.2), Item 3 pointer included (§21.3), the `CONTINUATION_PROMPT.md` ownership note
  included (§21.5). ✓
- Zero other content changed — confirmed by diff review before commit; territory list above
  matches the actual diff exactly. ✓

**GATE ANSWER: YES.** Bank; closure #5 fully executed across all six sites (two DM-fixed
pre-existing, two orchestrator-fixed pre-existing in one commit, one this-leg-fixed, one
carried as an immutable-gate-text pointer), with the register entry now on record for the
first time.

## 5. The ceiling

**0 numbers re-derived.** **1 site edited** (`CLAY_ROADMAP.md:343`). **4 sites verified
already-fixed, 0 re-edited.** **1 site is immutable gate text, pointer only.** `plan_of_record.py`
byte-identical. No ban touched, no ban lifted. No gate answer anywhere else changes. No link
of the `L1 → L4` chain moves. Clay odds stay **~0.05%**.
