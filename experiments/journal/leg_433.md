# Leg 433 — WAVE 4: `R5`(iii)–(vii) FAN-OUT ×5 — four workers, one adversary, and the rule that decides between them

**Date:** 2026-09-10. **Role:** NUMERICS (fan-out) + the Conductor's serial merge. **Wave 4.**
**Pre-registration:** `leg_433_prereg.md` (`c6aaf72`), pushed before dispatch. **Inputs:** the pinned
interface of legs 430 and 432. **Outputs:** `writeup/data/arc6/wave4/agent_{3_pulses,4_iteration,5_headline,
6_support,7_adversary}.json` and their runners (two files per agent, cherry-picked unedited), merged by
`experiments/arc6_wave4_merge.py` → `wave4/merged.json`. **Checks:** `writeup/arc6_wave4_evidence.py`.
**The adversary (slot 5) saw only the gate definitions, never the four workers' files; the workers never
saw the adversary's.** The merge and this journal are the Conductor's and are `UNVERIFIED`.
**Every unit's file carries the sentence "This is Tier 2, not a proof."**

## §1. THE PRE-COMMITTED RULE, APPLIED (`leg_433_prereg.md` §3)

> *A signal the adversary faked is `NOT EVIDENCE` whatever the worker's gate said.*

| gate | worker | adversary | status |
|---|---|---|---|
| **P1** pulses reproduce `T` | NO | FAKEABLE | **NOT EVIDENCE** |
| **P2** residual after absorption | NO | FAKEABLE | **NOT EVIDENCE** |
| **P3** the `q^{2h}` hierarchy | NO | FAKEABLE | **NOT EVIDENCE** |
| **I1** one-stage gain vs `1/10` | NOT-INSTANTIATED | FAKEABLE | **NOT EVIDENCE** |
| **I2** the exponent ledger | YES | FAKEABLE | **NOT EVIDENCE** |
| **I3** a second stage | NOT-INSTANTIATED | FAKEABLE | **NOT EVIDENCE** |
| **V1** the headline exponents | NO | FAKEABLE | **NOT EVIDENCE** |
| **V2** the leading order's force | YES | NOT FAKEABLE | **EVIDENCE** |
| **V3** `T*` and refinement | NO | FAKEABLE | **NOT EVIDENCE** |
| **S1** residual `= 0` beyond `X_b` | YES | FAKEABLE | **NOT EVIDENCE** |
| **S2** §10's support clauses | YES | FAKEABLE | **NOT EVIDENCE** |
| **S3** the (A.51) bounds | YES | FAKEABLE | **NOT EVIDENCE** |

**Counts:** workers `YES` 5, `NO` 5, `NOT-INSTANTIATED` 2; **faked 11 of 12; evidence 1 of 12.**

**The one sentence:** *of wave 4's twelve pre-registered signals, the adversary produced eleven from
deliberately wrong or under-resolved runs, so eleven are `NOT EVIDENCE` as defined whatever the workers
found; the one that survives — the leading order's own force grows like `q^{−3/2−h}` and the paper's
corrections must close every power of it — is a measured gap, not a reproduction; the wave's
substantive results are the four findings below, none of which was a gate.*

## §2. The four workers, in their own numbers

**(iii) pulses — agent 3.** **P1, P2, P3 `NO` at the pinned interface, with the exact part of Proposition 7.5 confirmed.** The homogeneous
pulse of Lemma 7.4, built from the tail's frame, has `x/P ∈ [0.55, 0.90]` and `y/x = c_0√(1+s²)` to `2·10⁻⁴` at
`Λ = 2000` (deviation `∝ Λ^{−1.00}`, as (7.21) says); its covariance columns have the (7.28) form with
`|e_σ| = 4.1·10⁻⁴ ∝ Λ^{−1.00}`; `H` is invertible with `y_± > 0` at all 25 slow points; and **the prescribed-amplitude
identity (7.26) reproduces the θ-entry of `T_0` to `1.4·10⁻¹⁶`** at every `N`, with the phase-shift control at
0.50 and the amplitude-halved control at 0.75 (both fired; the twin `1.4·10⁻¹⁶`). What fails: the paper's
`u_osc` is the curl of a potential (Lemma 7.7), and **at the pinned tail `F_0 = E/R ≈ 10⁻²⁴⁰`** in chart units, so
the pulse's own rates sit `10⁻²⁴⁰` below the chart's slow rates: the curl field's covariance defect is
`8.1·10²²¹` at `N = 32` (P1), its radial-divergence residual `4.8·10²²¹` (P2). In the paper-regime scaling
`F_0 := 1, R := 1` the defect is `1.4·10⁻¹⁸` (`h = 10⁻⁷`) with `N`-exponent **−6.00** (pre-derived in `claimed`; the
paper's bound is `−1`, satisfied one-sidedly, outside the pre-registered `±0.3` two-sided window — both
recorded). The `z`-entry (`T_z/T_θ ≈ 10⁻¹³⁷`) is reproduced only through the exact `σ`-split algebra, not from
float64 products. **P3:** at the pinned point every one of Proposition 9.1's seven (9.2) groups is *larger*
than the principal operator (9.1), from `1.6·10¹⁰` to `6.5·10²⁶⁹`, at all three `q` and both `h`; the measured
`q`-exponents equal `h × gain` to float precision (vacuous against `±0.05`). **The regime:** the paper's own
admissibility `S*²(ε + ε² + 1/k) ≤ 1` (p. 77) needs `ℓ ≥ 2.5·10⁹`, i.e. **`log₁₀ q ≤ −7.5·10⁸`** at `h = 10⁻⁷`; the pinned
profile additionally needs `log₁₀ q ≤ −2.4·10⁹` for the waves to fall below the base velocity (they are
`10³³–10¹¹⁹` times `E` at `ε = 1`) and for the carrier wavelength (`10¹⁸ × R`) to fit inside the annulus. A prefactor
bug in the first run (the ½ of (7.27)) was fixed with no tolerance touched and is recorded in the file.

**(iv) iteration — agent 4.** **I2 `YES`:** Definition 9.4's recursion (`σ_j = 1/5 + j/10`, `B_j = ½ + σ_j`,
`C*_j = 1 + σ_j`) reproduces Prop 9.5's `B_0 = 0.7`, `C*_0 = 1.2` and Prop 9.6's `+1/10`; all 29 inequalities
of pp. 106–111 hold at `κ_s = 10⁻⁵`; Lemma 9.8's `g_j = hj/10 > 0` makes Lemma 5.4's tail geometric, so
Prop 9.9's summation converges. **The smallest requirement margin is 0.020** (row (9.9),
`B − κ_s = 0.69999 > 0.68`); leg 429's 0.07 was the minimum over the five *closing* rows only; three rows
hold with equality as written. **I1, I3 `NOT-INSTANTIATED`, and the reason is a finding about the
pre-registration:** the paper's `+1/10` is an exponent of `ε = Q^h`, i.e. **`h/10 = 10⁻⁸` as a `q`-exponent**,
so the pre-committed `±0.02` window is `2·10⁶` times wider than the claim — a literal `YES` would be
vacuous and a `NO` would test a claim the paper does not make; both arithmetics are recorded, neither
taken. What was instantiated: Corollary 8.5's stress inverse reproduces `T_0` to `5·10⁻¹⁰` (θ) / `1.3·10⁻⁸`
(z); Lemma 8.7's five-equation map gives a tail residual ratio `2.2·10⁻⁶` (discretisation, verified 2nd/
4th order); the before-exponent is `−1.5000001 = −(A+1)`; the measured `q`-gain is `2·10⁻¹⁴`. Arithmetic
consequences at `h = 10⁻⁷`: the per-stage factor `q^{h/10}` is a relative `5–9·10⁻⁸`; flatness of order
`N = 1` needs `≈ 2·10⁸` stages; (5.37) switches the first stage on only below `q ≈ 10^{−6·10⁷}` unless the
paper's unquantified constants are `< ½`. Controls: skip-the-solve ratio exactly 1.0 (fired); negative
source 1.99999992 (fired); the twin passes; neither control can fire on the `q`-exponent (stated).

**(v) headline norms — agent 5.** **V2 `YES`:** `‖f⁽⁰⁾‖_∞ ∼ q^{−1.5000001}` (target `−3/2 − h`), from `3.7·10¹³`
at `τ = 0.1` to `1.2·10²¹` at `τ = 10⁻⁶`, the sup at the inner edge in the `z`-component — the unbalanced axial
pressure gradient the paper's Appendix B axis profile (not instantiated) is built to balance; **the
leading order's force is not bounded at `t = 1`**; the corrections must close `3/2 + h` powers of `q` for
boundedness and every power for flatness. **V1 `NO`** (the exponent sub-gates pass — `‖u_θ‖_∞ ∼ τ^{−0.500000100}`
against `−A = −0.5000001`, the paper's-core `L²` `∼ τ^{0.249999940}` against the bookkeeping `¼ − h/2` — the
refinement sub-gate is `NOT-INSTANTIATED`: the `dy = 2, 1·10⁻³` builds did not finish in the budget).
**V3 `NO`** for the same reason; `T* = 1` is prescribed and the agent says so. Drifts: quadrature `10⁻⁸`,
incompressibility `1.1·10⁻⁵`, an independent physical-space finite-difference route agrees to
`3·10⁻⁵…5·10⁻⁴`. **A finding decided before the run and confirmed:** the change of variables gives
`dV = 2πτ^{1+D}L d^{−2−D} dX dη`, whose `η`-weight `d^{−3/2+3h}` is **not integrable at `η = ±1`** — the
un-cut-off leading field's `‖u⁽⁰⁾(t)‖_{L²(ℝ³)}` is infinite at every `t < 1` (truncated-`η` divergence
exponent `−0.52` vs predicted `−0.50`); on the paper's domain `{q < q*}` it is bounded and tends to a
constant. **The bounded-energy headline is carried by the cutoff, not by the leading field's decay.**
Controls: `A → 0.85` flips the `L²` slope's sign (fired); `U → 0` moves the sup (fired); the twin passes.

**(vi) compact support — agent 6.** **S1 `YES`:** the leading residual profiles `−(∂_y+1)T_{0,θ}`,
`−(∂_y+½)T_{0,z}` and every scaled ingredient are exactly `0.0` at all 501 grid points `y ≥ 3`, **only with
the heat factor**; `r_b(t) = √(2(1−t)X_b)` is `10^{103.2}, 10^{102.2}, 10^{101.2}` at `1 − t = 10⁻², 10⁻⁴, 10⁻⁶` —
exact `√(1−t)` bookkeeping, and at this profile's `X_b ≈ 10^{208}` it reaches 1 only at `1 − t ≈ 10^{−208}`.
**S2 `YES` on the checkable clauses, flatness not claimed:** eight clauses extracted with equations; the
paper's `H` of (A.32) satisfies its ODE (A.37) to `5.8·10⁻¹⁰` and `H′(0) = −h(1+h)` to `10⁻¹⁴`; the leading
residual's own `q`-exponent is `−1.5000001 = −(A+1)`, a growing power; **the gap to (3.4)'s `q^N` is `N + 1.5 + h`
for every `N`**, carried by the pulses and the cycle. Recorded discrepancy with the brief: the paper does
**not** extend `f` by zero at `t = 1`; it uses the series (10.11); zero extension is spatial and for `t` near
0 / `t ≥ 2`. **S3 `YES`:** `N_min` non-increasing over `δ = 0.4…0.05` (θ: 0, 3, 6; z: 0, 0, 3), local powers 2.92,
5.92, stable to `1.6·10⁻⁶` across three `dy`; (A.51)'s lower bound `δ⁻³` recorded `NOT TESTABLE`. Controls: no
heat factor → `ℬ̂ = −2.002` beyond `X_b` (fired); polynomial cutoff → `N ≈ 1729` (fired); the twin passes.

## §3. The adversary, row by row (agent 7)
P1: the joint two-entry norm hides `T_z` entirely (`T_z/T_θ = 10⁻¹³⁶` here); a `π/2`-phase-wrong field
averaged at `M = 2N` with a `−π/(4N)` offset passes with error `5.8·10⁻¹⁶` **while the pre-registered `π/2`
control does not fire**; the "convergence exponent in `N`" is the averaging window (`−1.00, −0.16, −0.38, −346`
or undefined on the same exact field). P2: inherits P1's exponent for any multiplicative error. P3: the
tolerance `±0.05` is `2.5·10⁵` times the signal `2h`; honest aside — the `2h` scaling is real (`2.0096·10⁻⁷`)
but with prefactor `2.2·10²⁰⁷`: on the pinned annulus axial viscosity *exceeds* radial by `2X`. I1: the
zero-correction control is within `±0.02` of a `10⁻⁸` claim; a built-in ansatz measures `0.1000`; a `q`-tied
grid measures `0.105` from a solve with gain `2·10⁻¹⁶`. I2: the ledger passes for every `κ_s ≤ 1/30`, any
`B_0 ≥ 0.7`, any row-4 constant `> 0.1` — a transcription check. I3: the ansatz repeats; the grid fake did
not (`0.105 → 0.047`, recorded). V1: a wrong Gaussian profile gives the exponents to `5·10⁻¹⁵` at all three
resolutions; the `ℝ³` norm of the similarity field is infinite and the finite value is a box choice.
**V2: NOT FAKEABLE** — a report gate with no `YES`; profile-blind (`−A−1` for every prescribed field) but
honest. V3: automatic for any prescribed field. S1: truncating to the `√X` term, or cutting off the stress
instead of the profile, gives exactly 0 (the no-heat profile is honestly nonzero: `1.4·10⁻²⁴⁰`). S2: the
support clauses are tautologies of the cutoffs; **the flatness clause is the only content and is not
fakeable** (force `∝ q^{−1.5}`). S3: a cutoff `e^{−4/δ²+0.2/δ}` violating (A.51) passes the four pre-registered
`δ`; caught only at `δ = 0.02` or by the local slope at `0.05`.

**X2, the adversary's verdict:** *none of (iii)–(vi)'s `YES` gates, as defined, is evidence the construction
works; each is consistent with the paper's algebra being transcribed correctly. What would be evidence
is a two-route agreement on a quantity the adversary cannot choose — the sign and shape of `T_0`, the
exact `ℬ` zero with the heat factor against `−(2+2h)` without, the flat weight's `δ`-slopes, the measured
`2h` viscous scaling.* (Leg 432 is of that kind; wave 4's gates were not.)

## §4. What the Conductor records about its own pre-registration
Three of the twelve gates were mis-scaled by the Conductor before dispatch and the workers and the
adversary found it independently: **I1's `±0.02`** against a `10⁻⁸` claim (agent 4 and agent 7), **P3's `±0.05`**
against a `2·10⁻⁷` signal (agent 7), and **V1/V3's "three resolutions"** on a prescribed field (agent 5:
budget; agent 7: automatic). One brief clause was wrong: the paper does not extend `f` by zero at `t = 1`
(agent 6). None was widened after a number; each is a `CORRECTIONS.md` §71 item. The rule of §3 was applied
as written: **eleven `NOT EVIDENCE`, one `EVIDENCE`.**

## §5. What this wave does NOT establish
That the pulses cancel the residual, that the cycle converges, that the energy is bounded or that `f` is
flat at `t = 1` — the four gates that would say so were either not instantiable at any `h` a float can
resolve (`h/10 = 10⁻⁸`), or fakeable as defined, and the file says which. **What it establishes:** four
measured gaps between the leading order and the theorem — the force's `q^{−3/2−h}`, the `η`-weight's
non-integrability, the `N + 1.5 + h` flatness gap, the `10⁻⁸` per-stage gain — each of which the paper
assigns to a part of the construction (the axis profile, the cutoff, the pulses, the cycle) that no unit
here instantiated. **This is Tier 2, not a proof. No wall moved; no `L1→L4` link moved; Clay ~0.05%.**

## §6. Next (pre-committed): `R7` lands in `writeup/6_reproduction/` (leg 434).
