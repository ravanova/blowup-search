# Leg 102 — Route-JR2: the second freshness audit of `experiments/JOURNAL.md`

**Branch** `leg/jr2-v1`. **Exploration leg (light), mechanical/documentation.**
**Gate: NO — the journal was stale again, by 20 legs.** Lands normally on `main`.

Same audit as leg 72's (Route-JR), different time window. Leg 72 closed the gap as of its
own dispatch; this leg closes the gap that reopened after it.

---

## Order of work (the discipline, as executed)

1. `plan_of_record.py` run first. `NG` is NEXT. Every ban read; **none binds this leg** —
   it imports no solver module, computes nothing, derives no number, and re-opens no
   closed stage. It counts files and copies already-landed headlines.
2. **Novelty pass BEFORE any edit**, committed on its own (`writeup/novelty/leg_102.md`,
   commit `c29dcd8`). For an audit leg the novelty pass *is* the coverage inventory: which
   legs have landed since leg 72's pass, and how many of them the journal already covers.
3. Only then was `experiments/JOURNAL.md` appended to.
4. Finish protocol: territory check, rebase on `origin/main`, `scripts/merge_gate.sh`,
   push.

## The gate, verbatim

> Does `experiments/JOURNAL.md`'s narrative and `experiments/journal/`'s per-leg file exist
> for every leg that has landed (gate answered) since leg 72's own pass?

**Answer: NO** — for the narrative half. **YES** — for the per-leg-file half, inside the
window.

## Method

Leg 72's own audit commit on `main` is `117a176`; that is the cut line. The window is
`117a176..origin/main`, filtered to `Leg N:` subjects with `N != 0`, deduplicated and
sorted numerically, then cross-checked against `ls experiments/journal/`,
`ls writeup/novelty/`, and a per-leg `grep -c "Leg N " experiments/JOURNAL.md`.

Nothing was re-derived. Every headline appended to the journal is that leg's own landed
headline, copied verbatim in substance from its journal file / PR body.

## The counts (magnitudes, not booleans)

| quantity | count |
|---|---|
| legs that answered a gate and landed on `main` since leg 72's pass | **20** |
| of those already carrying a narrative pointer in `experiments/JOURNAL.md` | **0** |
| of those missing one — the gap this leg closed | **20** |
| of those missing `experiments/journal/leg_N.md` | **0** |
| of those missing `writeup/novelty/leg_N.md` | **0** |
| legs with branch progress but **not** landed on `main` (out of scope) | **6** |
| standing carry-over from leg 72 (leg 60's two files, still absent from `main`) | **1 leg / 2 files** |
| bench-repairs landed in this window, outside the leg system | **5** |
| lines appended to `experiments/JOURNAL.md` | **81** (0 deletions) |

The twenty legs: **59, 61, 73, 74, 77, 78, 79, 80, 81, 82, 84, 86, 87, 88, 89, 90, 91, 93,
94, 97**.

Legs 59 and 61 are in this window rather than leg 72's because leg 72 explicitly deferred
them ("branch progress but no answered gate") and their gates answered afterwards.

Leg 67 appears in the window only as a post-landing `VERIFY` commit; its gate was already
narrated by leg 72, so it is carried as a one-line addendum, not a new entry.

## The six legs deliberately excluded

Legs **92, 96, 98, 99, 100, 101** exist on their own branches but have **0** commits on
`origin/main` at this pass, so they fall outside the gate's literal scope ("every leg that
has landed"). Note for the DM: `DIRECTION.md` records "legs 92/97/98/99 landings", but on
`main` only **97** of those four is actually present — 92, 98 and 99 are still unlanded.
That is a live 3-leg discrepancy between `DIRECTION.md`'s record and `main`, reported, not
acted on (DIRECTION.md is outside this leg's territory).

This also corrects the leg's own thesis, which estimated "roughly 30 legs (73 through 99)":
the true landed, gate-answered, unnarrated count is **20**, an overstatement of 10.

## What this leg did NOT do

- Did not edit or reorder a single pre-existing line of `experiments/JOURNAL.md` (verified:
  `git diff` shows 81 insertions, 0 deletions).
- Did not create `experiments/journal/leg_60.md` or `writeup/novelty/leg_60.md` on leg 60's
  behalf — reported as still-missing, per the gate's no-branch.
- Did not create or edit any other leg's `journal/leg_N.md` (read-only throughout).
- Did not touch `DIRECTION.md`, `capabilities.py`, `reports/STATUS.md`, `writeup/INDEX.md`
  or any other integration-owned ledger. `experiments/JOURNAL.md` is normally one of those
  five; this leg's append-only edit to it is the explicit, pre-committed DM exception for
  this audit task, the same exception granted to leg 72.
- Re-derived no number. Every figure in the appended block is a pointer.

## Territory

`experiments/JOURNAL.md` (append-only), `writeup/novelty/leg_102.md`,
`experiments/journal/leg_102.md`. Nothing else.

## Standing recommendation

The journal went stale twice in the same way, at roughly a 20-30-leg cadence. The remedy
that would end the recurrence is not a third audit leg but a per-leg quartet requirement
that includes a one-line `JOURNAL.md` pointer at merge time. That is a contract change and
therefore the DM's call, not this leg's.
