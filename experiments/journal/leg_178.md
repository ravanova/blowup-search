# Leg 178 — ROUTE-WES: does a DIFFERENT SPACE shift the weighted-energy zero-width window?

**Agent:** LEG (exploration, not critical path). **Branch:** `leg/178-wes-v1`, landed as
`leg/178-wes-landing-v1`. **Date:** 2026-08-06; **unparked and landed 2026-08-07 by the
user's ruling.** **Claim-bearing. GATE ANSWER: YES.**
**Territory:** `solver/energy_coercivity.py` (append-only, leg 111's functions untouched),
`experiments/p2_route_wes_v1_space.py`, `writeup/data/p2_route_wes_v1_space.json`,
`writeup/novelty/leg_178.md`, this file, plus the quartet's
`writeup/4_p2_lottery/BLOG_P2_WES_V1.md`, `TECHNICAL_P2_WES_V1.md`,
`experiments/p2_route_wes_v1_space_evidence.py` and its `build_figures.py` registration
(fig66), added at landing. Nothing else.

---

## THE RULING — 2026-08-07, THE USER'S, NOT THIS LEG'S

This leg finished with its two readings disagreeing and **refused to break the tie itself**.
That refusal was correct and is left standing below, unedited. The tie was broken by the user
on **2026-08-07**. Verbatim:

> **RULING 1 — the gate's LITERAL wording governs. The answer is YES.** Clause 3 of the leg's
> own five-clause predicate is **scoped, not overruled**: it is only ever evaluated at grading
> depths where the runner's own contamination diagnostic is below 1.
>
> The reasoning, so it is on the record and not re-litigated: clause 3 did not detect
> instability in the mathematics. It detected that **the instrument stops working at
> `n_grade = 96`**, where the innermost panel sits at `θ ~ 3e−31` and contamination reads
> `3.1e+03` — the roundoff floor exceeding the signal by three thousand times. Over the three
> depths where contamination is below 1 the spread is `2.2839e−07`. This is not relaxing a
> pre-registration after seeing the answer; it is declining to read a number the leg's own
> diagnostic declares meaningless, which is lesson 86 and which leg 178 itself cites. The leg
> was right to refuse to make this call unilaterally, and right to take the YES branch's action
> (escalate, do not build, do not land) as the one safe under both readings.

**The scoping, as magnitudes, every one from `writeup/data/p2_route_wes_v1_space.json`
(`quadrature_stability["T2_egm|E_egm"]`), nothing re-run and nothing re-derived:**

| `n_grade` | 12 | 24 | 48 | 96 |
|---|---|---|---|---|
| contamination | `2.518e−21` | `1.031e−17` | `1.730e−10` | **`3.066e+03`** |
| read under the ruling? | yes | yes | yes | **no — diagnostic ≥ 1** |
| `T2_egm \| E_egm` gap | `+0.4999998187` | `+0.4999997045` | `+0.4999997527` | `−230.7108028` |

Relative spread over the three read depths: **`2.2839e−07`**, against clause 3's own tolerance
`1e−3` — it passes by a factor of **`4.379e+03`**. Over all four depths it is `4.0349e+00`, the
number the unscoped predicate saw. The banked `"answer": "NO"` field in the JSON is the
**predicate as it computed, unscoped**, and is deliberately left as it stands: the ruling scopes
how clause 3 is *read*, it does not rewrite what the runner measured.

**Gate answer under the ruling: YES**, on `T2_egm | E_egm` at `γ = 4`, gap `+0.499999667`,
last two relative refinement steps `5.890e−07` and `7.409e−08`, admissibility ratio `1.000000`,
exponent margin `+3.0`, under Xu's ceiling `0.5`.

### WHAT THE RULING DOES NOT LICENSE — the user's own bounding language, 2026-08-07

> The goal is now a full Clay solve… Read the leg's own scope line before ranking anything: EGM
> buy the origin conditions with two free modulation parameters, so a gap on a constrained trial
> space is not a certificate; the `+0.499999667` is EGM's published `−1/2` reproduced numerically
> and leg 178 says so in as many words (*"The −1/2 is theirs. Reproducing it numerically is not a
> new theorem"*); and the object is the `a = 0` CLM linearisation, whose `Y₀` is exactly zero for
> the banned degenerate reason. **Do NOT open a weighted-energy lane. "Not dead" is not "open."**

So, explicitly: **no weighted-energy lane opens.** This is a **method fact about a constrained
trial space**, not a certificate and not a stage. No ban lifts, no link of the `L1 → L4` chain
moves, Clay stays at `~0.05%`, and clause S7 (§*What this does not mean*) binds unchanged. The
YES branch's original instruction — *do not build further under this leg's own authority* —
survives the ruling intact; what the ruling changes is only that the finding is **landed and
readable** rather than parked.

---

## THE HEADLINE, AS THE LEG ITSELF WROTE IT (pre-ruling, left standing)

**The two readings of this leg's own gate disagree, and the disagreement is the result.**

- **The gate's literal wording answers YES.** *"Is the measured coercivity gap positive and
  grid-stable across two refinements, outside the `γ>3`-needs/`γ<3`-exists coincidence?"* On
  `T2_egm` with EGM's own weight: **`+0.499999667`**, relative change across the last two
  refinements **`5.890e-07`** and **`7.409e-08`**, at `γ = 4` — outside the coincidence,
  because the constrained space's membership threshold is `7`, not `3`.
- **This leg's OWN five-clause predicate, pre-committed in `writeup/novelty/leg_178.md` §7
  before any computation, answers NO — 0 of 44 rows.** Every decisive row fails on exactly
  one clause: **clause 3, quadrature-depth stability**, and only at the deepest pre-committed
  grading depth `n_grade = 96`.

**The reason clause 3 fails is measured, not argued.** At `n_grade = 96` the innermost
quadrature panel sits at `θ ~ 3e-31`, where the order-`θ³` cancellation that DEFINES the
constrained space is below float64's ability to represent it. The runner's own
`contamination` diagnostic — the weighted energy of a bound on the pointwise cancellation
error, divided by the weighted energy of the basis function — reads:

| `n_grade` | 12 | 24 | 48 | **96** |
|---|---|---|---|---|
| `T2_egm \| E_egm` gap | `0.500000` | `0.500000` | `0.500000` | **`−230.710803`** |
| contamination | `2.5e−21` | `1.0e−17` | `1.7e−10` | **`3.1e+03`** |

At depth 96 the roundoff floor exceeds the signal by **3.1e+03×**. Over the three depths where
contamination is below 1, the spread is **`2.2839e−07`**; over all four it is **`4.0349e+00`**.
That is lesson 86 exactly — *a quantity dominated by its own evaluation error is a statement
about the code* — and it is my own clause, applied to my own instrument, doing its job.

**I am not permitted to resolve this by relaxing my own pre-committed clause after seeing the
answer, and I have not.** The JSON records `"answer": "NO"` as the predicate computed it. But
writing the NO branch's prose — *"the coincidence persists under a second, independently-chosen
construction"* — into the repository would be **false**, and this leg's own measurements are
what falsify it:

| | leg 111 (`p = 1`) | this leg (`p = 3`, `T2_egm`) |
|---|---|---|
| admissible window vs `γ > 3` | `(3, 3)` — **width 0.0** | `(3, 7)` — **width 4.0** |
| membership ratio per refinement at `γ = 4` | **`1.677722e+07`** (divergent) | **`1.000000`** (convergent) |
| exponent margin at `γ = 4` | **`−1.0`** | **`+3.0`** |
| largest admissible gap | **`−0.4999241`** | **`+0.499999667`** |

**So the coincidence does NOT persist.** The gate's NO branch has no true statement to make
here. That is why this leg is **parked for the user under escalation #3**, pushed as a branch
and never to `main`, rather than landed under either branch's prose.

---

## THE GATE, VERBATIM, AND BOTH BRANCHES AS PRE-COMMITTED

> "For at least one pre-named alternative weighted-energy construction (different from leg
> 111's), is the measured coercivity gap positive and grid-stable across two refinements,
> outside the gamma>3-needs/gamma<3-exists coincidence?"
>
> yes -> The zero-width window is a construction artifact, not an operator fact — a genuine
>        third-realization revival. ESCALATE to the user; do not build further under this
>        leg's own authority.
> no  -> The coincidence persists under a second, independently-chosen construction,
>        strengthening (not just repeating) leg 111's finding toward an operator-level fact.
>        Bank it.

**Answer as recorded by the pre-committed predicate, unscoped: NO (0/44).**
**Answer on the gate's own literal wording: YES.**
**Action taken at run time: the YES branch's action — ESCALATE, do not build further, do not
land.** The YES branch is the one whose *action* is safe under both readings: it forbids
building and hands the decision to the user. The NO branch's action ("bank it") would require
asserting something this leg measured to be untrue.

**RESOLVED 2026-08-07 by the user's ruling (§ THE RULING, above): the gate answers YES.**
Clause 3 is scoped to grading depths whose contamination diagnostic is below `1`; over those
three depths the spread is `2.2839e−07`, inside clause 3's own `1e−3` tolerance by `4.379e+03`.
The decision was **the user's, not this leg's** — this leg neither took it nor is credited with
it, and its refusal to take it stands above as written.

---

## WHAT WAS NOT CLAIMABLE, AND IS NOT CLAIMED

The construction is **not this repository's**, and the novelty pass (committed at `0ed6bde`,
**before** the module was appended to and before any number existed) said so in advance:

- The constrained trial space is **Elgindi–Ghoul–Masmoudi**, [arXiv:1906.05811](https://arxiv.org/abs/1906.05811),
  **Prop. 2.1** — `f` odd, `f′(0) = Hf(0) = 0`, `∫|f|²φ < ∞` with `φ = (1+y²)²/y⁴`, conclusion
  `∫ f M_a f φ ≤ (−1/2 − C|a|) ∫ f² φ`. At `a = 0`, by EGM's own §1 (*"when `a = 0` we get CLM
  model"*), that is **this object**.
- The frame is **Xu**, [arXiv:2607.19762](https://arxiv.org/abs/2607.19762) §3.1 Prop. 2, the
  realization dichotomy, re-fetched at run time this leg.
- The `−1/2` is **theirs**. Reproducing it numerically is not a new theorem.

This leg claims **only the measurement**, and prices nothing: EGM buy the origin conditions
with two free modulation parameters, so a gap on a constrained trial space is **not** a
certificate.

---

## THE CONSTRUCTION (pre-named in the novelty log, §6, before any computation)

Four trial classes, subspaces of `span{sin kθ}`, cut out by functionals exact in this basis
(`(sin kθ)′(0) = k`, `H(sin kθ)(0) = −1 + (−1)ᵏ`):

| class | constraints | measured `p` | declared `p` | admissible `γ` |
|---|---|---|---|---|
| `T0_unconstrained` | none | **`1.000000`** | 1 | `< 3` — leg 111's own |
| `T1_dprime` | `Σ k c_k = 0` | **`3.000000`** | 3 | `< 7` |
| `T2_egm` | that **and** `Σ_{k odd} c_k = 0` | **`3.000000`** | 3 | `< 7` |
| `T3_hilbert_only` | `Σ_{k odd} c_k = 0` | **`0.999999`** | 1 | `< 3` — falsification control |

Vanishing order is **measured** from each class's probe by log-log slope, not asserted. Odd
trigonometric polynomials vanish to **odd** order only, so leg 165's exponent-counted `p = 2`
is unattainable; `T1`/`T2` land at `p = 3` and the window is `(3, 7)`, **width 4.0**, not
leg 165's 2.0. This was registered before the run so it could not be presented afterwards as a
strengthening.

Eleven weights: family `A` at `γ = 0,2,3,4,5,6,7`; family `B` at `γ = 0,2,4`; and `E_egm`,
EGM's `(1+X²)²/X⁴` transported through `X = tan(θ/2)`, `dX = ((1+X²)/2) dθ`, written from its
own closed form.

---

## AN EXACT FACT THAT FELL OUT, AND IT IS ABOUT LEG 111's ENUMERATION

**P0 held.** `φ^E / φ^B4` is **constant `32`**: relative spread **`1.998e-15`**,
`|ratio − 32| ≤ 3.553e-14` over the whole quadrature. **EGM's own published weight,
transported into this module's variable, IS leg 111's family `B` at `γ = 4`** — the one member
leg 111's seven-weight enumeration excluded (`B` stopped at `γ = 2`, `A` at `γ = 4`).

**And it is the weight that makes the damping factor constant.** `D_φ ≡ −1/2` identically in
`θ` for both, max deviation **`2.220e-16`** (`B4_egm`) and **`4.441e-16`** (`E_egm`), against
**`1.000e+00`** for `A4_chen_hou`. That is the arithmetic reason EGM's constant is `−1/2`, and
it is why the measured gap is exactly `1/2` and not merely near it: on `T2_egm` the local half
of the form contributes **`+0.499999667`** and the nonlocal Hilbert half contributes
**`−0.000000000`**, with the integration-by-parts identity holding to **`1.45e-14`**.

---

## THE INSTRUMENT FINDING — AND IT CHANGED THE ANSWER BY 21 %

The first assembled run of this leg was **wrong**, and the check that caught it was one I had
built for a different purpose: a whole-space admissibility ratio that the single-vector probe
cannot fake.

**Assemble-then-project is invalid here.** At `γ > 3` the *unconstrained* Gram's own entries
are divergent (`∫ sin jθ sin kθ θ^-γ ~ jk ∫ θ^{2-γ}`), growing by `1.677722e+07` per grading
refinement. Restricting *after* assembly computes the constrained form as a cancellation
between divergent numbers, and an SVD null-space basis satisfies its constraint only to
**`1.73e-14`** — a leak that divergence then amplifies. Measured consequences at `γ = 4`:

| | assemble-then-project | exactly-constrained basis |
|---|---|---|
| constraint residual, `T2` | `1.73e-14` | **`0.0e+00`** |
| whole-space Gram ratio, `T2` | **`7.811662e+02`** (looks divergent) | **`1.000000`** (converged) |
| gap, `T2 \| B4`, `n = 32` | `+0.396742` | **`+0.500000`** |
| gap, `T1 \| B4`, `n = 32` | `−5.767290` | `−5.778339` |

**The artifact is `1.033e-01` on a quantity whose value is `0.5` — it understated the gap by
21 %.** The repair is an integer basis whose constraint residual is *exactly* zero in float64
(`v_j = (j+1)e_j − j e_{j+1}` for `T1`; `w_m = u_{m+1}v_m − u_m v_{m+1}` for `T2`), contracted
against the basis functions **pointwise** before any weight is applied, so the cancellation
happens at scale `ε·K·θ` rather than at the scale of a divergent integral.

**Control that this did not quietly become a different computation:** on `T0_unconstrained` the
new machinery is **bit-identical** to leg 111's own function — `0.00e+00` at `n = 16` and
`n = 32`, on `A0` and `A2` — and reproduces leg 111's banked raw values `−2.101463` and
`−4.080051` at `n = 16`. The exact-basis path reduces to the old path exactly where it should.

---

## THE MEASUREMENTS

### `T2_egm`, the EGM class — the gap ladder (`n = 32, 64, 128, 256`)

| weight | `γ` | ladder | verdict |
|---|---|---|---|
| `A2` | 2 | `−0.481895 −0.495329 −0.498814 −0.499701` | negative: below the damping threshold |
| `A3` | 3 | `+0.013421 +0.004064 +0.001210 +0.000355` | **collapsing** (last rel step `0.707`) — the same shape leg 111's `A3` had |
| `A4_chen_hou` | 4 | `+0.502412 +0.500603 +0.500151 +0.500038` | positive, grid-stable; **fails ceiling by `3.765e-05`** |
| `A5` | 5 | `+0.869083 +0.868438 +0.904082 +0.894630` | **above Xu's ceiling `0.5`** — instrument, contamination `4.0e-07` |
| `A6` | 6 | `−11.2 … −78356.4` | contaminated, `1.2e+05` |
| `A7` | 7 | `−1994.8 … −94793.5` | contaminated, `3.2e+15` |
| `B4_egm` | 4 | `+0.500000 +0.500000 +0.500000 +0.499993` | contamination `4.13e-17` |
| **`E_egm`** | 4 | **`+0.500000 +0.500000 +0.500000 +0.499999667`** | contamination **`4.13e-17`** |

`A4` approaching `0.5` **from above** is not a defect: a Galerkin Rayleigh quotient over nested
subspaces is an upper bound on the limit, so it must. My pre-committed ceiling clause
(`gap ≤ 0.5 + 1e−9`) is strict enough to fail it by `3.765e-05`; `B4`/`E` pass it exactly
because their `D_φ` is constant. **`A5`'s `+0.894630` exceeding the published ceiling is the
known-answer window (lesson 84) firing as designed** — and the contamination diagnostic flags
it ten orders of magnitude above the clean rows, so the two instruments agree on which rows
are trustworthy.

### The other three classes

- **`T1_dprime`** — every gap `≤ −1` (`E_egm`: `−5.778 −8.160 −11.500 −16.205`). Required:
  its point-mode intersection is **dimension 1**, so the published eigenvalue `1` lives in the
  trial space and forces `gap ≤ −1`. An internal consistency check, not a defect.
- **`T2_egm`** — intersection **dimension 0**: the two origin constraints annihilate
  `span{sin θ, sin 2θ}` exactly. **The origin constraints ARE the modulation**, which is why
  the measured `1/2` is comparable with Xu's *modulated* `1/2`. This is this leg's own
  arithmetic; Xu's §3.2 attributes mode removal to centering, and today's fetch confirmed he
  does **not** state this.
- **`T3_hilbert_only`**, the falsification control — **inadmissible at every `γ > 3`**
  (ratio `1.677722e+07`, margin `−1.0`), and `0` rows pass at `γ > 3`. **C2 fires as
  designed.** A class that removes a direction without changing the vanishing order does not
  open the window, so the instrument is measuring the *space*, not dimension reduction.

### Controls

- **C1** — leg 111's four banked `n = 256` gaps reproduce through the untouched path to
  `1.652e-07`, `1.101e-07`, `1.101e-07`, `5.506e-08` (tolerance `1e-5`). **PASS.**
- **C2** — falsification control fires, 0 passing `T3` rows at `γ > 3`. **PASS.**
- **C3** — `μ > 0` flips the sign on the *appended* path: `T0` `−5.51616 → +0.49674`,
  `T2` `−2.85389 → +2.87951`, monotone. **PASS.**
- **C4** — no `μ > 0` row answers the gate; every gate-answering row is at `μ = 0` exactly.
- **P0, P5** — held (above). **P2, P4** — held. **P6** — held: the gap does not grow without
  bound toward `γ = 7`; it degrades into contamination instead, which is a stronger statement
  than the saturation I predicted and is reported as such.

### A lesson-90 flag on my own output

`T0_unconstrained` returns **identical** ladders at `γ = 5, 6, 7`
(`−0.044755 −0.022898 −0.011583 −0.005825`). Three different weights cannot legitimately give
one number: that computation has **no content** — the whitening has dropped essentially
everything — and those rows are already excluded by the admissibility clause (margin `−2`,
`−3`, `−4`). Recorded rather than quietly passed over, because four identical numbers are
exactly what leg 53 mistook for a finding.

---

## WHAT THIS DOES NOT MEAN

- **No stage is claimed. No ban is lifted.** The gate's YES branch forbids building further
  under this leg's authority, and nothing here licenses re-opening `B`'s space axis: that
  clause is about the `ℓ¹_w` **coefficient** space of the radii-polynomial machinery, which
  shares **zero quantities** with this weighted-`L²` energy form.
- **A gap on a constrained trial space is not a certificate.** EGM buy `f′(0) = Hf(0) = 0`
  with two free modulation parameters. This leg prices nothing and proposes nothing.
- **Clause S7 binds.** The object is the `a = 0` CLM linearisation — one mode, analytic, the
  friendliest object in the repository. A gap measured here bounds `HL_S2_nonsymmetric`'s
  difficulty **from below, never above.**
- **No link of the `L1 → L4` chain moved.** Clay remains `~0.05%`.
- Plain float64 throughout; nothing interval-enclosed.
- **NO WEIGHTED-ENERGY LANE OPENS** (user's bounding ruling, 2026-08-07). The YES answer is a
  **method fact about a constrained trial space**, not a certificate, not a stage, not a
  ranked next step. *"Not dead" is not "open."* The `+0.499999667` is EGM's published `−1/2`
  reproduced on this repository's own instrument; the `−1/2` is theirs.

---

## FOR THE USER — THE DECISION THIS LEG DOES NOT TAKE

Three things need a ruling, and this leg deliberately takes none of them:

1. **Which reading of the gate governs** — its literal wording (YES) or this leg's own
   stricter five-clause predicate (NO, on one clause measured to be instrument-limited at one
   grading depth, contamination `3.1e+03`).
   **→ RULED 2026-08-07: the literal wording governs; the answer is YES, clause 3 scoped to
   contamination `< 1`. Closed.**
2. **Whether leg 111's headline needs re-scoping.** Leg 111's finding is not wrong, but its
   scope line — already present in `capabilities.py` as *"ON THIS UNCONSTRAINED ODD-SINE TRIAL
   SPACE (p=1 vanishing order)"* — is now load-bearing in a way it was not when written, and
   the `B4` member its family excluded is the published one.
3. **Whether the pre-committed grading-depth ladder `{12, 24, 48, 96}` should stand** for
   future legs on this module, given that depth 96 is measurably past float64's reach for any
   constrained space. Changing it is a change to a pre-registration and is not mine to make
   after seeing an answer.
   **→ NOT changed by the 2026-08-07 ruling.** The ruling scopes how a depth is *read* — skip
   any depth whose contamination is `≥ 1`, and at `n_grade = 96` it is `3.066e+03` — and leaves
   the ladder `{12, 24, 48, 96}` itself standing, so depth 96 keeps reporting its diagnostic
   rather than being deleted.

**Items 2 and 3 remain OPEN after the ruling; only item 1 was decided.** In particular
`capabilities.py`'s leg-111 scope line (*"ON THIS UNCONSTRAINED ODD-SINE TRIAL SPACE (p = 1
vanishing order)"*) is left exactly as leg 111 wrote it — it is accurate as written, this leg's
`T2_egm` is a *different* (constrained, `p = 3`) trial space and does not contradict it, and
re-scoping it is integration's call rather than this landing's.
