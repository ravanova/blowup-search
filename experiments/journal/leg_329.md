# Leg 329 — Route-EGMF: the `T2_egm` five-clause check at arbitrary precision

Branch `leg/329-egmf-v1`. LEG role, difficulty `standard`, **claim-bearing**.
Novelty pass: `writeup/novelty/leg_329.md`, landed on `main` **before** any mathematics
(contract §0a). Everything below was written as it happened, not retroactively.

**Gate (DM's wording, verbatim).** *Do `B4_egm` and `E_egm` pass all five clauses at
arbitrary precision?*

- `yes` → leg 178's `NO` is measured to flip in substance: REPORT AND ESCALATE; the
  historical gate text stays byte-identical, the flip lives in this leg's own record only.
- `no` → bank the corrected magnitudes and which clause still fails; escalation #3 stays
  parked, the artifact explanation stands **refined, not overturned**.

---

## 1. Situation on arrival (2026-08-11)

The worktree was clean at `origin/main` tip `bd438de`. This leg has been dispatched once
before: it was killed mid-flight by a monthly spend limit after writing its novelty pass and
nothing else, and the orchestrator preserved that file in `e81a6ea` ("No mathematics reached;
`T2_egm` five-clause question UNANSWERED"). So the §0a pre-registration in
`writeup/novelty/leg_329.md` — §7's five-clause table, §7a's precision protocol, §7b's ten
controls C1–C10, §7c's what-a-`no`-looks-like — is **already on `main`, committed before any
number existed**, which is exactly the property it needed to have. This leg executes it and
changes none of it.

### 1a. The banked object, read out of leg 178's own JSON (read-only)

`writeup/data/p2_route_wes_v1_space.json`, `verdicts`:

| row | `gap_n256` | clauses 1/2/3/4/5 | `quad_rel_spread` | `PASS` |
|---|---|---|---|---|
| `T2_egm\|B4_egm` | `+0.4999930266006214` | T T **F** T T | `4.034902354370309` | False |
| `T2_egm\|E_egm` | `+0.4999996674832294` | T T **F** T T | `4.034902353014096` | False |

Both fail **only** `clause_quad_stable`, exactly as the DM's thesis says. The quadrature
sweep at `n = 128` that produces those spreads:

| row | `n_grade=12` | `24` | `48` | `96` |
|---|---|---|---|---|
| `B4_egm` | `+0.4999999620` | `+0.4999998496` | `+0.4999994064` | **`−230.71080359`** |
| `E_egm`  | `+0.4999998187` | `+0.4999997045` | `+0.4999997527` | **`−230.71080279`** |
| contamination | `2.5e-21` | `1.0e-17` | `1.7e-10` | **`3.07e+03`** |

The whole failure is one cell per row: the `n_grade = 96` entry, whose *measured* roundoff
floor `contamination = 3066.4` is three thousand times the size of the quantity being
computed. Leg 312 (`d89bedd`) already re-measured exactly one of those cells —
`T2_egm|E_egm`, `n=128`, `n_grade=96` — and got `+0.4999999875`. This leg's job is the
remaining seven cells plus every other clause, for both rows, under one protocol.

### 1b. What I must NOT let happen

Both rows sit at `0.49999…`, and the whole repository, the DM's brief and leg 312's own
finding all point at a flip. §7d of the novelty pass names that as the failure mode being
guarded against. The specific traps:

- reading `+0.4999930` as `+0.5` (it is not; the ceiling clause is one-sided, so *below* is
  a pass — the trap runs the other way, toward *widening* `CEILING_SLACK` if MP pushes a
  number above `0.5 + 1e-9`, and that will not happen);
- patching only where a flip is expected. The MP treatment is applied to **every** cell of
  both rows, and to the three adversarial rows C6/C7 where MP must **leave the answer
  alone**;
- letting a `no` from C4/C5 be argued away. C4 is a rigorous-in-spirit bypass of the float64
  eigensolve and it is pre-registered to force `no` on failure.

---

## 2. Reading the machinery before writing any of my own

`solver/energy_coercivity.py` is read-only to this leg. The chain that produces a gap:

1. `graded_quadrature(n_unif, n_grade, order)` — composite Gauss–Legendre, `n_unif` uniform
   panels with the first and last geometrically refined `n_grade` levels at ratio `1/2`
   toward `0` and `π`.
2. `wes_constrained_basis(n, "T2_egm")` — an **exact integer** `(n, n−2)` basis. Its columns
   are `w_m = u_{m+1} v_m − u_m v_{m+1}` with `v_j = (j+1)e_j − j e_{j+1}`, so each column
   has at most **3** nonzeros. That sparsity is what makes an MP patch affordable.
3. `wes_form_matrices_constrained` — contracts `F = Vᵀ E` **pointwise** (before the weight),
   then `G = (F·pw) Fᵀ`, `B = (F·pw) LFᵀ`.
4. `wes_coercivity_gap_exact` — whitens with `G`'s eigendecomposition at `rcond = 1e-12` and
   returns `gap = −λ_max(W ᵀ Sym(B) W)`.

The float64 loss is at step 2/3's *pointwise* contraction: at `θ ~ 1e-31` (which
`n_grade = 96` reaches) `F_m(θ) = Σ V[k,m] sin kθ` is an `O(θ³)` cancellation between
`O(θ)` terms, so float64 returns dust. **Neither the weight `φ` nor the quadrature weights
`qw` are where the loss is** (§7a), so they are not patched — patching only where the
cancellation is, is leg 312's own protocol and it is inherited unchanged.

The patch is therefore: recompute `F`, `LF`, `HF` at the unsafe nodes in `Decimal`, leave
everything else identical, re-assemble. That is leg 312's `experiments/p2_route_apia_v1.py`
`leg178_remeasure()` exactly; this leg re-uses its `_mp_sin_cos` / `_mp_recurrence_all_k` /
`_spot_check_against_rigorous_mp` **by import**, not by copy (§4 of the novelty pass), and
generalises the surrounding driver from one cell to twenty-eight.

---

## 3. What the runner does, and what it deliberately does not do

`experiments/p2_route_egmf_v1.py`. Three layers:

1. **`mp_patch(class, n, n_grade, n_unif, tau, prec)`** — recomputes `F`, `LF`, `HF` in
   `Decimal` at every node with `min(θ, π−θ) < τ`, from one `sin/cos` base pair per node via
   the angle-addition recurrence (leg 312's `_mp_sin_cos`, imported). Cached, because the
   patch depends on `(class, n, n_grade)` **and not on the weight** — so `B4_egm`, `E_egm`
   and `A4_chen_hou` share one patch each and the C7 ceiling control is free.
2. **`assemble_gap(patch, family, gamma, which, rcond)`** — mirrors
   `wes_form_matrices_constrained` + `wes_coercivity_gap_exact` line for line, on either the
   float64 or the patched contraction. The mirror is not trusted: control C1 checks it
   against leg 178's banked numbers.
3. **`five_clauses(...)`** — leg 178's predicate, its five constants unchanged.

**Not patched, on purpose, and each for a stated reason:**

- **`φ` and the quadrature weights** — cancellation-free by inspection (§7a); patching where
  the cancellation is not is how a patch starts manufacturing answers.
- **the mesh.** The object being re-measured is *leg 178's quadrature rule, evaluated
  exactly*. Moving the nodes would measure a different rule and C1 would then have nothing
  to reproduce.

### 3a. Diagnostic D1 — a mesh failure I did not go looking for

Counting nodes before patching them turned up something not in leg 178's record and not in
leg 312's: at `n_grade ≥ 48`, **exactly 12 nodes have `min(θ, π−θ) == 0.0` in float64.**
The geometric refinement at the **right** endpoint descends below the float64 spacing near
`π` (`4.44e-16`), so the innermost right-hand panel collapses onto `π` itself. The left
endpoint has no such problem — `θ` near `0` is representable down to `7.1e-34` at
`n_grade = 96`, and the mesh does reach that. So leg 178's grading is **asymmetric in
practice while symmetric in intent**, and the asymmetry appears at `n_grade = 48`, one rung
*below* the depth everyone has been discussing.

This is a **mesh** representation failure, categorically different from the **pointwise
contraction** failure this leg patches, and it is not repaired here. Control D1b sizes it:
the gap recomputed with those 12 nodes deleted outright, against the gap with them kept.
Deleting them is a different quadrature rule, so D1b is a ruler, never the answer.

### 3b. Ten controls, four of which can come out against a flip

C1 reproduction · C2 τ-independence · C3 precision-independence · **C4 MP Rayleigh** ·
**C5 rcond ladder** · **C6 falsification rows** · **C7 `A4_chen_hou` ceiling control** ·
C8 exact-basis residual · C9 series containment · C10 cancellation-depth budget.
All ten are §7b of the novelty pass, verbatim, tolerances included.

C4 is the one with real teeth. For the float64 maximiser `x` it evaluates
`R(x) = Σᵢ pwᵢ f(θᵢ) lf(θᵢ) / Σᵢ pwᵢ f(θᵢ)²` **node by node in `Decimal`**, forming neither
`G` nor `B` and calling no eigensolver. Since `x` is a genuine trial vector,
`R(x) ≤ λ_max`, so `−R(x) ≥ gap`: a **one-sided upper bound on the gap**, which is the side
of the one-sided ceiling clause that can refute it.
