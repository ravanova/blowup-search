# Leg 434 — unit `R7`, arc 6 conductor pass: LAND

**Date:** 2026-09-10. **Role:** the Conductor's serial landing. **Outputs:** `writeup/6_reproduction/`
(`TECHNICAL_REPRODUCTION.md`, `BLOG_REPRODUCTION.md`, `reproduction_evidence.py` — which runs every unit's own
evidence script and re-derives every number in the note from the banked artefacts), `writeup/INDEX.md` arc-5/6
rows, the root `README.md` second-pass block beside the struck first-pass banner, `STATE.md` (the arc-6 block
retired verbatim under §3j → `WALLS_HISTORY.md` §STATE-ARC6-CONDUCTOR, replaced by the verdict), `reports/ORCH_STATE.md`.
Figures `fig113` (leg 430) and `fig114` (leg 432) are registered in `build_figures.py`.

## The verdict paragraph (the charter's one paragraph), as landed in all three documents
*Read twice — once solo and once by five blind shards — all 79 numbered statements index, cite and close as the
manuscript says, with one citation-discipline finding (Propositions 9.5 and 9.6 are never cited downstream).
Re-derived by four agents and checked blind by a fifth, the 58-statement spine holds step by step — 480 steps,
301 recomputed constants, no `GAP`, ten nodes `VERIFIED`. Instantiated from the paper's own Appendix A schedule,
Lemma 4.8's outer profile reproduces its bracket constants, exponents, pressure datum and pulse-end moment
cancellation, and its terminal-tail stress reproduces Proposition A.10 by two independent routes to `10⁻⁹`; its
closure and its cone do not reproduce at any `λ` a grid can reach, because the paper's own asymptotics place
them at `λ ≲ 3·10⁻⁴` with `√λ P_* ≪ 1`, and its outer-edge powers are the limit on a collar `δ ≲ 10⁻⁶⁹`. Fanned
out on that interface, the pulses' exact identity holds to `10⁻¹⁶` and the correction ledger's inequalities hold
with a smallest margin of 0.020, while the paper's own admissibility puts the pulses at `log₁₀ q ≲ −7.5·10⁸`
and the per-stage gain at `10⁻⁸` in `q` — and an adversary who saw only the gate definitions faked eleven of
wave 4's twelve signals, so by the rule fixed before dispatch they are not evidence. Measured rather than
assumed, the Lean project is large, `sorry`-free outside its challenge placeholders, source-covers all 79
statements, exports Fefferman's (C) and (D) — strictly weaker than Theorem 1.1 — and no kernel check of it
was reached. Nothing this pass measured contradicts the manuscript; nothing this pass measured is a proof of
it; every place where the reproduction stops is a place the paper's own asymptotics put beyond a grid; and no
wall of ours moved.*

## Escalations (the charter: ESCALATE, DO NOT RULE)
None raised by this pass: no `GAP` in `R4` sinks anything; no ban in `plan_of_record.py` is lifted; no `W4`
movement arose from `R5`. The five open escalations in `ORCH_STATE.md` are unchanged and unruled.

## What is deliberately left open
The Conductor's background Lean build (cache complete, project modules compiling) had not reached the
theorem's module at landing; a watcher will run `#print axioms` on the two main theorems if it does, and the
result — machine-checked, Conductor-run, `UNVERIFIED` by a second agent — is a dated addendum to `leg_431.md`
that changes `R6`(a) only.

**Tier 2 throughout. No wall moved. No `L1→L4` link moved. Clay ~0.05%. No successor scheduled.**

**Recorded, not fixed:** `writeup/build_figures.py` aborts on a legacy P2 runner (`p2_route_cadx_v1_scope.py`,
leg 304) because the gitignored `Papers/2505.03091.pdf` is absent in this environment; it refuses, by design,
to overwrite a quote-verified artefact. It predates this arc and is unrelated to it; `fig113` and `fig114` are
drawn by their own evidence scripts, which `reproduction_evidence.py` runs and which pass.
