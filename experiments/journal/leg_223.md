# Leg 223 — Route-PUB3: the audit-family synthesis, at the count that is actually true

**Date** 2026-08-06 · **Role** LEG · **Branch** `leg/223-pub3-v1` · **Claim-bearing** (a gate
answer and numbers in prose; no new measurement) · **Difficulty** standard

> **Gate (pre-committed, both branches, verbatim).** *"Does the combined note state every
> actual audit-family escalation this cycle accurately, with each one's contamination status
> stated exactly as landed (no softening any uncertain or materially-exposed status into a
> false 'zero' before its repair leg confirms it, and no undercounting how many escalations
> actually occurred)?"*
>
> **YES →** Bank the combined note as a THIRD publication-scoping/methods draft (distinct from
> 179's `ℓ¹_w`-death bundle and 186's space-axis bundle — this one is about the audit-family's
> own hit-rate discipline, not about the certificate mathematics). Flag to the user at the next
> natural check-in. Normal landing straight to `main`.
>
> **NO →** Report exactly which bundled claim doesn't reproduce from its own banked source;
> escalate rather than silently soften it, same discipline as 179/186. Push the branch only,
> never `main`.

## Answer: YES — with three recorded disagreements printed rather than resolved, and the count corrected from seven to thirteen.

---

## The scope correction, which was the first task and not a formality

The dispatch stated its own spec was stale and instructed this leg to establish the real count
before writing. It was stale by six.

**Method.** `experiments/JOURNAL.md`'s tail read in full (the orchestrator-audited pointer per
landed/escalated leg), cross-checked against `DIRECTION.md`'s DM bookkeeping updates in commit
order, then against `git log origin/main` and the branch tip of every `leg/*` branch named in
either. Inclusion rule stated in the note so the count is auditable: an adversarial/degenerate
input audit of a `solver/` module, a consistency audit of a shared ledger, or a repair leg
closing one of those, **that ended parked on its own branch with `main` untouched**.

**Result: thirteen** — 188, 198, 199, 200, 201, 202, 203, 204, 205, 208, 209, 213, 215.

Against the dispatch's own candidate list: it named twelve of the thirteen and asked each to be
verified. **All twelve verified.** The thirteenth, **213 (LGC2)**, was on no list handed to this
leg and was found in the journal tail — escalated, cosmetic, `0` banked numbers.

**One escalation excluded, named so the exclusion is auditable:** leg **211 (XU11)** escalated
this cycle but is a literature cross-validation, not an input audit. Under a looser rule the
count is fourteen.

**Two audit-family legs are in the denominator, not the escalation list:** **206 (GSA)** and
**207 (DPA)** found real defects and landed directly. The dispatch asked whether they belong in
the synthesis — **yes, and they are load-bearing there**: without them §3's rate is 10/10 rather
than 12/12, and the honest denominator is the set of modules audited, not the set that escalated.

## The status correction the dispatch did not anticipate

The dispatch asked which repair legs had landed, since that decides "confirmed zero" versus
"pending". **Checked, not assumed: not one has landed.** `git log origin/main` carries no
`Leg 216/217/218/219/220/221/222/225/226` commit. Legs 218 and 221 have committed a novelty
pass only; 217/219/220/222/225 sit at their base commit with no leg commit at all; 226 (the
highest-ranked item in the whole backlog) has not been dispatched.

**Consequence, applied throughout the note:** every "zero" is the *finding leg's own*
measurement, independently re-confirmed by nobody, and the note never uses the phrase
"confirmed zero". This is a real weakening relative to the dispatch's own framing.

## The three disagreements, printed not resolved

The novelty pass pre-committed the rule (print both, name the disagreement, resolve neither).
Three fired:

1. **Leg 198's `Z₁` ratios vs leg 218's in-flight re-measurement.** 198 reports `1.198e9×`; 218
   measures `2.287e8×` (5.24× apart) and `1.505e12` vs `2.651e12` (1.76×). 218's own reason:
   `Z₁ = ‖I − A·DF‖` at `A = inv(DF)` is a near-total cancellation with both values at the
   round-off floor, so the ratio is environment-dependent and the verdict is not — lesson 86.
   198's `‖A‖` and `Z₂` columns reproduce exactly to 4–6 s.f. **Neither number adopted.**
   Contamination status (zero) unaffected either way.
2. **The DM's blast-radius reason for ranking 218 first is measured false.** DIRECTION.md says
   `bordered_hl.py` "sits directly upstream of every bordered-certificate battery (54, 58,
   127)". Leg 218 measured that **none of the three imports it** — 0 of 7 runners/evidence
   files — they route through `spectral_certificate.py`. The ranking may stand; its stated
   reason does not.
3. **Route-D v12's exposure appears in no shared ledger.** Leg 202's own journal grades it
   PLAUSIBLY EXPOSED (its upper rows reach `a ≤ 0.55`; M1's first departure is `a = 0.45`) and
   records it as not re-run. Every pointer and DM summary names v11 only.

**Why these do not answer the gate NO.** The gate asks whether each escalation's *contamination
status* is stated exactly as landed. None of the three moves a contamination status: (1) is a
magnitude dispute inside an item whose status is zero on both readings, (2) is a priority
rationale, (3) is an *addition* to 202's exposure, which is already the note's most severe
grade. Softening any of them would have failed the gate; printing them satisfies it. Had a
"zero" been contested, the answer would have been NO.

## The finding this leg did not expect to make

The gate's "no undercounting" clause turned out to have a structural cause, and it is in the
bookkeeping rather than the code.

`DIRECTION.md` states **26 times** that escalations are recorded in `PROGRESS.md`'s NEEDS YOU.
**`PROGRESS.md` is not in the repository** — `.gitignore:33` excludes it by design ("rewritten
every cycle; the committed snapshot is `reports/STATUS.md`"), and `git log --all -- PROGRESS.md`
is **empty**: no commit in the repository's history has ever touched it.

The committed snapshot, `reports/STATUS.md`, was last refreshed at `4b6f4d8` — summary
**"6 landings, 3 escalations"** — and its NEEDS YOU section lists exactly three (188, 199, 198).
`main` is now `b8eae4f` and the count is thirteen.

**The only committed escalation register is stale by ten, including leg 202**, the one item that
materially exposes a banked headline. Every individual escalation *is* recorded correctly, each
in at least two places; what does not exist is one committed place recording them together.
Reconstructing the list requires reading a narrative log and a journal in commit order and
taking the union — which is exactly what this leg did, and why the number moved by six.

Stated in the note as a bookkeeping observation, not as a criticism of any leg. It is not
escalated by this leg (no banked number moves, and the fix is a `reports/STATUS.md` refresh
that belongs to the orchestrator, not to a writeup leg's territory), but it is flagged for the
user at the next check-in alongside the note itself.

## What the note claims, and what it refuses to claim

**Refused, on the novelty pass's pre-commitment.** The headline "adversarial input finds latent
defects that ordinary use never triggers" is **established prior art** — silent-bug incidence
studies ([arXiv:2112.13314](https://arxiv.org/pdf/2112.13314)), Franco et al.'s 269-bug
NumPy/SciPy/LAPACK dataset, the metamorphic-testing/oracle-problem line
([STVR 2016](https://onlinelibrary.wiley.com/doi/10.1002/stvr.1594)), and the CAP-reliability
literature which already states in print that any computer-assisted proof carries "the software
had no bugs" as an implicit hypothesis ([arXiv:2502.07850](https://arxiv.org/pdf/2502.07850)).
The note says this in §0, before any result.

**Claimed.** The ledger: twelve legs ran a silent-wrong gate; **twelve answered YES**; graded by
a pre-committed *does-a-banked-number-move* question, the distribution is 1 materially exposed /
1 claim-adjacent / 1 uncertain / 9 zero / 1 pending-ruling. The grading axis is what none of the
four prior literatures uses, and it is a bookkeeping contribution, claimed as nothing more.

**The one substantive observation.** Reachability, not magnitude, predicts contamination. The
cycle's largest magnitude (198, `Z₂` to `1.189e17×`) contaminates nothing; the item that reaches
a banked headline (202) produces *machine-zero residuals*. Every latent defect needed an input no
caller produces; 202's needed nothing — default path, warm start, a parameter genuinely swept.

## Quartet shape

**No runner, no JSON, no figure**, following the leg 179/186 bundle precedent and the
214/180/108 runner-free prose-correction precedent (both cited in the novelty pass, which was
committed before construction). This leg measures nothing new; every number is transcribed from
a named banked source, and the provenance table is §8 of the technical note.

## Territory, as declared and as diffed

`writeup/4_p2_lottery/TECHNICAL_P2_PUB3_V1.md` (new),
`writeup/4_p2_lottery/BLOG_P2_PUB3_V1.md` (new), `writeup/novelty/leg_223.md`,
`experiments/journal/leg_223.md`. Four files, all new, all inside the declared set. **No solver
module, no shared ledger, no other leg's artifact touched** — every escalated leg's banked
JSON/report was read via `git show` against its own branch, never checked out or edited.

## For the user, at the next check-in

1. **The note is a DRAFT and this leg's landing does not approve it** (179/186 precedent).
2. **Leg 202 / Route-D v11 is the live question**, and it is not answered by anything here.
   Leg 226 (PNR) would answer it, is ranked first in the entire backlog, and has not been
   dispatched.
3. **Leg 205's contamination status is uncertain, not zero**, and stays that way until leg 221
   re-runs the banked results — 221 is live but has committed only a novelty pass.
4. **`reports/STATUS.md` is stale by ten escalations.** Not this leg's territory to fix.

---

## Dated pointer — 2026-08-07 (leg 279, Route-Z1X). Append-only: nothing above is altered.

**The `Z_1` disagreement recorded above is CLOSED AS EXPLAINED — not adjudicated.**

Leg 198 reported the `Z_1` understatement on the `w_r = ±1e−6` configuration as **1.198e9×**;
leg 218 re-measured **2.2867e8×** (5.24× apart); leg 223 banked both and adopted neither. Leg
228 (Route-BHRV, `experiments/journal/leg_228.md` §4) closed it. It did **not** re-run the
same arithmetic a third time as a tie-breaker. It treated leg 218's lesson-86 explanation as
the falsifiable variance claim it is, with the falsification condition fixed **before** the
numbers were looked at, and perturbed only in ways that provably cannot change the
mathematics — algebraically identical re-associations of the same matrix products, and BLAS
thread counts.

| quantity | spread under math-neutral perturbations |
|---|---|
| **the disputed `Z_1` ratio itself** | **11.77×** (`2.6422e8` … `3.1108e9`) vs a disagreement of only **5.239×** |
| the ratio's denominator (flipped `Z_1`, a cancellation) | 6.407× |
| the ratio's numerator (honest `Z_1`) | 1.165× |
| `Z_1` at an admissible weight | 1.022× |
| **control: the `‖A‖` ratio, the column both legs agree on** | **1.0000000000072×** (7.2e−12 relative) |

**Thread count alone** spans the entire disagreement and then some: 1 thread gives `2.6422e8`,
4 threads `3.1108e9` — same code, same machine, same inputs. The `einsum` re-association alone
moves it from `1.8015e9` to `3.2766e8`. The control is what rules out "the perturbations simply
broke the computation": it did not move.

**Forward practice, per leg 228's recommendation to integration: adopt NEITHER `1.198e9×` nor
`2.2867e8×` as *the* ratio — there is no such number to adopt.** The quantity is round-off-floor
noise in its denominator. **Quote the reproducible columns instead:**

* **`‖A‖` understated = 6.6712e+09×** (`w_r = ±1e−6`) and **6.0663e+10×** (`w_om = ±1e−9`) —
  legs 198 and 218 agree to 4 s.f.;
* **`Z_2` = +1.188608e+17 / −1.779456e+07** (`w_r = ±1e−6`) — the two legs agree to 6 s.f.

**Both legs' verdicts are unaffected**, and no banked value moves: a sign flip corrupts these
constants catastrophically, which was never the part in dispute. Recorded in
`writeup/CORRECTIONS.md` (leg 279 entry), with leg 228's locators and the separate
caller-enumeration note.

---

> **POINTER — 2026-08-07, leg 282, on disagreement 2 (`:72–78`), carrying leg 228 §3.**
> **Disagreement 2's conclusion is unaffected and is not re-opened here.** It rests on leg 218's
> caller enumeration, and leg 228 measured that enumeration **materially incomplete**: **13**
> direct importers of `solver/bordered_hl.py` today, **10** contemporaneous with leg 218
> (membership by `git cat-file -e d9a20fb:<path>`, not a name list), **6 named, 5 missed** —
> `test_bordered_hl_adversarial.py`, `p2_route_bhn_v1_adversarial.py`,
> `p2_route_hlb_v1_contraction_lit.py`, `p2_route_nb_v1_targetnorm.py` and
> `p2_route_tn_v1_consistency.py`. The gap's mechanism: leg 218's sixth name,
> `p2_route_port_v2_reach.py`, is a *transitive* caller (it imports `p2_route_port_v1_bordered`,
> not `solver.bordered_hl`), so the set was never purely "by import".
>
> **Why disagreement 2 still stands, checked rather than assumed.** Its claim is that legs
> 54/58/127 do **not** import `bordered_hl.py` — 0 of 7 runners/evidence files. **0 of the five
> missed importers belongs to leg 54, 58 or 127**, so the re-enumeration adds nothing to the
> side of the ledger that would weaken it; if anything the live caller set is larger than either
> leg said and still excludes all three batteries. Leg 218's repair is likewise unimpeached: leg
> 228 re-earned the bit differential over the full ten-importer space at **45 configurations,
> 77,040 float leaves, 0 bit mismatches**.
>
> **One precision note, since this leg is about keeping the record honest.** Leg 279 §5 forwarded
> this site as one that "reproduces the six-name list." It does not reproduce it *literally* —
> this journal names no importer — it reproduces the **claim derived from** that enumeration.
> The pointer is carried anyway, because a reader auditing disagreement 2 will go to leg 218's
> list, and that list is the thing that is wrong. Corrected in full at
> `writeup/4_p2_lottery/TECHNICAL_P2_PUB3_V1.md` §5(b), indexed at `writeup/CORRECTIONS.md`
> §8–§9.
