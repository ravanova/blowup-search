# Leg 213 — ROUTE-LGC2: post-append consistency audit of the two shared ledgers

**Gate (pre-committed, quoted verbatim).**

> Do the 190 and 197 rows match `literature_gates.py`'s and `viscous_novelty.py`'s own
> existing row format exactly, with no duplicate/conflicting entry and no transcription drift
> from legs 190's/197's own banked reports?
>   yes -> Bank the audit as confirming ledger integrity post-append; record the pass in
>          `capabilities.py` (append-only). Normal landing straight to `main`.
>   no  -> Name the exact inconsistency (format drift, duplicate, or transcription error)
>          precisely; escalate rather than silently reconcile it, since both ledgers are
>          shared infrastructure every literature-pass leg reads. Push your branch only.

## Gate answer: **NO** — and it splits unlike leg 145's, so the split is the finding

**The gate's three named clauses are all CLEAN.** Format: 2/2. Duplicate/conflict: 0 found.
Transcription drift from the source legs' own banked reports: 0 found, at **8/8 verbatim
fields byte-identical** and **9/9 numeric claims traceable** for leg 190, **20/20 claims
traceable** for leg 197. If the gate's wording were the whole question the answer would be
YES.

**It is NO because of a defect in a fourth class the gate's wording does not name**, which
the spec's own note about leg 214 pointed at: **post-hoc staleness**. One field of leg 190's
row was true when appended and has since been **falsified by leg 214**, at **5 of the 5
sites it names**, without anyone touching the row. `capabilities.py` is therefore **not**
appended to, and this branch is **not** pushed to `main`.

## THE FINDING — `sign_correction_leg_190` is stale at 5/5 of its own named sites

**The field.** `solver/literature_gates.py`,
`EGM_PRIMARY_READ["spectral_gap"]["sign_correction_leg_190"]`. It is the ledger's only record
of a three-leg propagated transcription error and its repair. Its second sentence reads, in
the **present indicative**:

> "`experiments/journal/leg_141.md:30,117, writeup/novelty/leg_141.md:233 and
> experiments/journal/leg_165.md:120,128 all render the bracket as (-1/2 - C|a|)`, i.e. a gap
> **IMPROVING** with `|a|`"

**What those five lines say today**, read out of the live tree by `A6`:

| site | renders | ledger's claim holds? |
|---|---|---|
| `experiments/journal/leg_141.md:30` | `-(1/2 - C\|a\|)` | **no** |
| `experiments/journal/leg_141.md:117` | `-(1/2 - C\|a\|)` | **no** |
| `writeup/novelty/leg_141.md:233` | `-(1/2 - C\|a\|)` | **no** |
| `experiments/journal/leg_165.md:120` | `-(1/2 - C\|a\|)` | **no** |
| `experiments/journal/leg_165.md:128` | `-(1/2 - C\|a\|)` | **no** |

**0 of 5.** All five carry the **corrected** form. The falsifying commit is leg 214
(`2c901c4`, Route-EGMB): 5 lines, 5 insertions, 5 deletions, across those exact three files.
Leg 214's own journal states the ground for leaving the ledger alone — *"`solver/
literature_gates.py` deliberately untouched — leg 190's own row there is already correct."*
**That ground is right about the mathematics and too coarse about the row.** The row's
*mathematical* content — the bracket `-(1/2 - C|a|)`, the gap degrading with `|a|`, the
`-0.5` at `a = 0` — is correct, and this audit re-confirms it byte-for-byte against leg 190's
JSON. What is now false is the row's *bibliographic statement about this repository's own
current files*.

**Why this is worth a NO rather than a footnote.** The field's whole purpose is to stop a
reader from trusting five specific lines. Read today it does the exact inverse: it sends five
readers to five files and tells each of them the file is wrong when it is right. That is the
same failure mode leg 190 created the field to record, pointed the other way. And it is in
**shared infrastructure** — every literature-pass leg reads `literature_gates.py` — so it
propagates by being believed, and it can only get staler as those line numbers drift.

**Magnitude of what it does NOT move, stated so the escalation is not inflated:**
**0 banked numbers move.** `novelty_verdict()` returns **`YES`** on `arXiv:2410.05480` alone,
unchanged. **0 of 12 `CLAIM_LEDGER` verdicts** move. EGM's `-1/2` at `a = 0` and BCG's Grade
B both stand exactly as legs 190 and 174 banked them. This is a wrong sentence about file
state, not a wrong number and not a wrong citation.

**Not reconciled here, deliberately.** `solver/literature_gates.py` is read-only to this leg
under *both* branches. The repair is one tense change — re-tense to *"…rendered the bracket
as `(-1/2 - C|a|)` until leg 214 (`2c901c4`) corrected all five"* — leaving every other word
intact. It is recorded in the JSON as `suggested_repair_not_applied` and belongs to whoever
owns the ledger, not to its auditor.

## What is clean, stated as magnitudes

**A1 — format, `literature_gates.py`.** Leg 190's novelty §6 names its template explicitly:
`LSS_PRIMARY_READ` (leg 161). Checked against it. The **7-slot citation header is identical
in sequence** — `arxiv, tag, authors, title, venue, read, provenance_before_leg_N` — with the
leg number normalised so `_leg_161` and `_leg_190` compare as one slot. Both store the arXiv
id **bare** (`1906.05811`), which is `literature_gates.py`'s convention and deliberately
*not* `viscous_novelty.py`'s `arXiv:`-prefixed one; the two ledgers are internally consistent
and different, and control **C2** confirms the checker notices if one convention leaks into
the other. Both blocks carry exactly **1 payload sub-dict per banked quantity** (LSS: 2, for
`a_c` and `alpha_half`; EGM: 1, for `spectral_gap`) followed by prose fields.

One sub-dict field name differs and is **reported, not gated**: LSS uses `locators` (plural —
several pointers per number), EGM uses `locator` (singular — one displayed inequality). The
singular/plural tracks the cardinality of what is being pointed at, so it is the field doing
its job, not drift.

**A2 — format, `viscous_novelty.py`.** All **7 of 7 rows** carry all seven schema keys, all 7
ids are `arXiv:`-prefixed, all 7 verdicts are in the closed vocabulary, all 7 `what` fields
clear the 60-character floor, and leg 197's row is **last**, as an append must be. Leg 197's
row is the **only one of 7 carrying extra keys** (`grade`, `banked_by`,
`why_exclusion_not_pre_empts`). That is legal by the module's own test — `test_viscous_
novelty.py:50` is `assert fields <= set(p)`, a **subset** check — and it is documented in the
source at the point of use (*"the seven fields above are the schema the gate reads"*), which
is the same standalone-provenance-field precedent `LSS_PRIMARY_READ`/`EGM_PRIMARY_READ` set
in the other ledger. **Not drift.** Recorded as an asymmetry so a later leg reading six lean
rows and one fat one knows it is intentional.

**A3 — duplicate / conflict.** `7/7 unique` arXiv ids in `PRECEDENTS`; **0 duplicates**, **0
same-paper verdict conflicts**. `1906.05811` occurs at exactly **2 sites** in
`literature_gates.py` — the block's own `arxiv` field and the scope comment explaining why it
is kept out of any `CLAIM_LEDGER` `source` string — and **0 `CLAIM_LEDGER` rows** mention it,
so the scope note's promise is kept and leg 145's `cited_ids` phantom-citation check stays
sound. `EGM_PRIMARY_READ` and `LSS_PRIMARY_READ` are different papers. Leg 189's non-append
leaves no phantom row: **0** found.

**A4 — transcription, leg 190.** The strongest instrument available here, and it is **byte
equality**, not similarity: leg 190's JSON carries `citation` and `verbatim` dicts that are
supposed to *be* the objects the ledger holds. **6/6 header fields identical. 8/8 `verbatim`
fields identical.** `scope` and `the_relayed_p2_claim` identical. The `re_derived_at_leg_190`
prose quotes 9 numbers and **9/9 are traceable** to the JSON's own measured values: E1's
`4.44e-16` (measured `4.4409e-16`), spread `8.88e-16`, 4000 nodes, decoys missing by `0.500`
and `0.343`; E2's `1 of 10` γ-scan with spread `0.0500` at γ = 3.9; E3's `err·S = 0.639`
against `2/π = 0.6366` over four grid extents (measured `0.6389…0.6421`), negated target off
by `2.0`. The bracket, the slope direction, and the LaTeX source line are all present verbatim.

**A5 — transcription, leg 197.** Weaker instrument by construction and the JSON says so: leg
197 *wrote* a characterization from leg 174's fields rather than copying them, so the test is
substring containment of leg 174's own wording. **20/20 claims traceable** to
`writeup/data/p2_route_vbs_v1_scoping.json` key `BCG-NS` — including the §7 domination
sentence verbatim, `10000` Taylor coefficient pairs, `14 hours on a single CPU`, Lemmas
`A.27/A.28`, Appendix B's title, system `(1.5)` as the ODE reduction of the **inviscid** Euler
system `(1.3)`, the Lamé conditions `mu_1 > 0, 2 mu_1 + mu_2 > 0`, Grade B, and the journal
ref/DOI. This independently re-derives leg 197's own check `C2` (which reported 11/11) at
roughly double the claim count, and agrees with it.

## Negative controls (banked lesson 90) — **12/12 fire, 0 ornaments**

Every checker is re-run against a deliberately corrupted copy: a renamed header slot; the
other ledger's `arXiv:` prefix leaking in; a row losing its `dial` key; an unprefixed id; the
appended row duplicated verbatim; a duplicate with a **conflicting** verdict; two words
changed inside a `*_verbatim` field; the bracket **reverted to the erroneous form**; an
inflated node count; the §7 quote paraphrased; Grade B upgraded to A against leg 174; and —
for the staleness checker — **the same predicate run against the pre-leg-214 blobs**
(`git show 2c901c4^`), which reports the ledger's claim **held 5/5 before** and holds **0/5
after**. Without that last one the staleness finding could have been an artifact of the
predicate rather than a fact about the commit.

**Two controls caught defects in this leg's own instrument, and the checker was fixed rather
than the control retired.** (i) C9 did not fire at first: `"4000" in prose` is satisfied by
`"40000"`, so the node-count check was blind to exactly the inflation it existed to catch —
now word-bounded. (ii) C12 did not fire at first because the predicate was keyed to the
literal string `(-1/2 - C|a|)`, which is blind at sites 2 and 5: leg 214's diff has **two
shapes** — 3 relocated brackets in the verbatim blockquotes and **2 fresh pairs inserted** on
the inline constants, which carried no parentheses at all before. The predicate now keys on
the **absence of the corrected form**, which is shape-independent. A false FAIL was also
produced and removed in A4, where `err·S` was compared against a single row instead of the
four grid extents the prose actually quotes. A false positive from an auditor is the same
defect class it audits, and all three are recorded in the runner at the point of the fix.

## Why the existing automated gates could not have caught this

- `test_literature_gates.py::test_8_ledger_does_not_rot` gates `CLAIM_LEDGER`'s **shape**.
  `EGM_PRIMARY_READ` is a top-level dict **outside** `CLAIM_LEDGER`; no clause reaches it.
- `test_literature_gates_selfconsistency.py` pins `len(CLAIM_LEDGER) == 12` and asserts leg
  145's three drifts stay absent. Silent on anything appended beside `CLAIM_LEDGER`.
- `test_viscous_novelty.py:50` is a **subset** check, so extra keys, a duplicated `id`, and
  prose drifted from the banking leg all pass.

None of them is wrong; all three check shape. Staleness is not a property of a string, and no
structural test can hold it.

## Discipline, bans, territory

`plan_of_record.py` run first; every live ban read; **none tripped** — no gCLM object is
constructed or measured, no GA runs, no solver is built, no certificate is floated, stage V's
*question* is untouched (only its ledger's consistency is examined). `capabilities.py` is
**not** appended to: that is the YES branch, and the gate answered NO. `solver/
literature_gates.py` and `solver/viscous_novelty.py` are **edited 0 times**, as declared, and
their own suites are unaffected because nothing in them changed. No figure is registered —
leg 145's ledger-audit precedent (runner + JSON + journal) owes none, and there is nothing
here a plot would show that the 0/5 table does not. Files written: this journal,
`writeup/novelty/leg_213.md`, `experiments/p2_route_lgc2_v1_ledgeraudit.py`,
`writeup/data/p2_route_lgc2_v1_ledgeraudit.json`.

## The honest residual

**This leg re-read no paper.** A4 and A5 certify that the ledger says what legs 190 and 174
said; they cannot certify that legs 190 and 174 read their PDFs correctly. Leg 214 did
independently re-fetch `OSWModel_Final.tex` and confirm leg 190's bracket, so the EGM side has
a second primary-source witness; **the BCG side does not** — leg 197's own novelty pass says
plainly that the interior locators are leg 174's full-text reading, transcribed and not
re-read, and this leg does not change that. A perfectly faithful transcription of a misread
paper passes A4 and A5 at 8/8 and 20/20.

**And the fourth failure mode is now known to be live, which generalises past this one field.**
Any ledger row asserting something about *this repository's own files* — a path, a line
number, a "still absent from", a "not yet done" — is falsifiable by any later leg, silently,
without touching the row, and no append-only discipline protects it. This audit found one
instance and checked only the rows it was pointed at. **It did not sweep the two ledgers for
others**, and the same defect class is available to every self-referential sentence in both.
