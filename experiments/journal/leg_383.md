# Leg 383 — Route-ST2G: the screen learns Tsai's Theorem 2 and an ansatz check

Construction class, floor-eligible, slot C. Refill for leg 382 (`104f5b3`), whose landing
made the precondition "no live leg editing `solver/dssp_screen.py`" TRUE.

**Gate answer: YES** — in the gate's own wording, the upgraded screen classifies (i)
Tsai's own headline example as REACHED by Theorem 2, and (ii) this programme's DSS object
as NOT REACHED with the deciding clause recorded as the exact-SS ansatz hypothesis, with
planted controls able to fail in both directions.

## Part I — what the novelty pass changed about the leg

The dispatch thesis was leg 359's: leg 357's B7 screen tests only L³ convergence, missing
Theorem 2's local-energy route and any SS/DSS ansatz check. The novelty pass
(`writeup/novelty/leg_383.md`, committed at `1ca4ce5` before any construction) found that
thesis **partially discharged already**: leg 362 (`ec48622`) landed
`decays_to_zero_at_infinity()`, `classify_ss_ansatz()` and `_ledger_nrs_tsai_three_way()`,
and leg 370 (`9a3dd41`) added `ledger_morrey()` — all after leg 359's flag was written.

Had I built to the thesis as dispatched, I would have written leg 362's functions a second
time. The honest reading is that the *functions* exist and the *report path* does not use
them: both extensions were landed as strictly opt-in arguments defaulting to `None`,
precisely so no existing caller's key set moved, and nobody went back to rewire
`screen_candidate()`. The gate's yes-branch — "every future candidate report carries the
two new columns" — is a statement about `screen_candidate()`, and it was FALSE at
`104f5b3`.

So the leg is: make the columns unconditional in the report path, and build the
adversarial battery the existing tests do not have. Legs 362's and 370's controls are all
fire-direction (a planted case that should be excluded, and is). That is the shape leg 340
was burned by.

**Measured baseline, before touching anything** (this is the whole justification for the
leg, so it was measured first): planted `U(y) = (y/|y|)/|y|` through `screen_candidate()`
returns fitted exponent `-1.0000000000000002`, L³ ladder `converged=False` with
`rel_change_last_step = 0.1305` vs `rel_tol = 1e-4`, and verdict **NOT EXCLUDED** — while
Tsai's Theorem 2 excludes that field outright, using machinery in the same module.

## Part II — pre-registration (fixed at `1ca4ce5`, not renegotiated)

Six controls, three required to FIRE and three required to stay SILENT, with their
deciding clauses named in advance. Full text in `writeup/novelty/leg_383.md` §4. A control
that did not fire was to be a STOP and a verbatim report, never a widening — the leg-361
lesson, treated as a hard stop.

## Part III — results

All eight checks in `test_dssp_screen_t2.py` passed on the first run; nothing was
adjusted after the numbers arrived. Magnitudes:

| control | direction | measured | verdict before → after |
|---|---|---|---|
| C1 Tsai eq (1.5) | FIRE | exponent `-1.0000000000000002`, \|diff\| vs source's exact −1 = **2.22e-16**; L³ divergent (0.1305 vs 1e-4) | NOT EXCLUDED → **EXCLUDED-BY-T2** |
| C2 repo DSS object | FIRE | λ = `2.691234472349262`, S₀ = 1.98; exponent `-0.997134373688964` | NOT EXCLUDED → **NOT-REACHED-BY-ANSATZ**, clause = eq (1.2)₁ |
| C3 exponent −2 | FIRE | exponent `-2.0000000000000004`, L³ = 3195.93 converged (rel change 0.0) | EXCLUDED (357 binary) → **EXCLUDED-BY-T1** |
| C4 non-zero limit | SILENT | \|U\| 0.954545 → 0.999500, exponent `+0.008377` | **NOT EXCLUDED** |
| C5 ansatz not a catch-all | SILENT | ansatz EXACT-SS, satisfies=True, λ=None | not deflected (verdict EXCLUDED-BY-T2, by decay) |
| C6 growing field | SILENT | exponent `+1.0000000000000002` | **NOT EXCLUDED**, no clause claimed |

The repo's own measured exponent (`-0.997134373688964`) against Tsai's exact −1 differs by
**2.87e-03**, reproducing leg 359's banked `-0.9971`. That is the "source's decay exponent
matching this repo's measured one" the gate names, and it is why the old screen's NOT
EXCLUDED was a genuine under-warning rather than a hypothetical one.

All four verdicts are reachable through the report path; ≥2 controls fire and ≥2 stay
silent. Regression: `machine_read_ledger(l3, lam)`'s two-argument form still returns leg
357's exact shape, and `screen_candidate()`'s ledger key set is unmoved.

## Part IV — decisions a successor should know about

1. **Leg 382's certified enclosure was read and deliberately not wired in.** Reasoning in
   `TECHNICAL_P2_ROUTEST2G_V1.md` §7. The fitted column stays in place, per dispatch.
   **Open clause:** a second, certified Theorem-2 column driven by
   `solver/dssp_decay_enclosure.py`, recorded alongside the fitted one and never replacing
   it. That is a clean, well-scoped successor leg, and it would strictly increase what the
   T2 column is worth — the current column's exponent is a least-squares fit on a single
   generic off-axis ray, with all the limitations leg 382's own BLOG spells out.
2. **Morrey stays opt-in.** Leg 370's ball-average sweep is a much more expensive pass and
   the gate did not ask for it in the report path. A successor wanting it on every report
   should cost it first; note that leg 357's landed
   `test_screen_candidate_end_to_end_is_reportable()` asserts the ledger key set is exactly
   `{NRS_Tsai, Chae_Tsai, Pineau_Vicol, reportable}`, so adding a `Morrey` key to
   `screen_candidate()` requires editing that test, which was outside this leg's territory.
3. **`capabilities.py`'s `test` field takes a single path.** Listing two files
   (`"test_dssp_screen.py, test_dssp_screen_t2.py"`) fails `test_capabilities.py`'s
   existence check. The new battery is named in the row's `validated` prose instead.
   Banked as an environment fact.
4. **The class of bug is worth remembering.** No individual leg did anything wrong. 362 and
   370 each landed correct, tested, registered capabilities, and each chose opt-in defaults
   for a good reason. The gap lived entirely between "the capability exists" and "the
   report path calls it", and it survived two subsequent legs editing the same file. A
   capability row that says a check exists is not evidence that any report runs it.

## Part V — ceiling

**TIER 2.** Surviving this screen means only "not already excluded by a published theorem
reachable on this repository's record". Not evidence for existence, not a proof, not a
Clay claim. `CLAY_OBLIGATIONS.md` §6's two no-method obligations stay **OPEN** regardless
of this gate's answer. No ban touched, no `plan_of_record.py` or `DIRECTION.md` edit, no
`L1 → L4` link moved. Clay odds unchanged, **~0.05%**.
