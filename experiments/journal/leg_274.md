# Leg 274 — Route-J176R2 — the §3.5 wrong-subject label + the root-cause pointer for leg 176's `reading` field

**Gate: YES on all three clauses (a), (b), (c).** Light correction leg, nothing computed.

Branch `leg/274-j176r2-v1`. `plan_of_record.py` runs clean; stage **P0** is NEXT.

---

## 1. What was wrong (residues (1) and (2) of `verify_271`)

### Residue (1) — a 16× subject-attachment slip in TECHNICAL §3.5

The sentence printed the `‖T⁻¹‖_X` ladder — **rising** — and then attributed *decrements* to it:

| quantity | successive differences | ratios |
|---|---|---|
| `‖T⁻¹‖_X` = `3.994032 → 4.012071 → 4.021340 → 4.026241 → 4.028864` (**rises**) | `1.804e−2 → 9.269e−3 → 4.901e−3 → 2.623e−3` | `0.514 / 0.529 / 0.535` |
| `σ_min = 1/‖T⁻¹‖_X` = `0.25037356 → 0.24924783 → 0.24867333 → 0.24837063 → 0.24820892` (**falls**) | **`1.126e−3 → 5.745e−4 → 3.027e−4 → 1.617e−4`** ← the printed set | **`0.510 / 0.527 / 0.534`** ← the printed ratios |

Mismatch, rung by rung: **`16.024× / 16.134× / 16.191× / 16.221×`**. The reciprocal ladder
reproduces leg 268's banked `tail_sigma_min` list digit for digit, which is how the subject was
identified rather than guessed. Provenance of the slip: `experiments/journal/leg_249.md`
L233–234 → PUB2 via leg 268 (`3f6d5d0`); leg 271 did not list it among its nine untouched sites.

### Residue (2) — the regeneration hazard upstream of it

`writeup/data/p2_route_h2c_v1_construction.json`, key `C1_bordered_sigma_min_X.reading`, **opens**
with *"BOUNDED AWAY FROM ZERO and truncation-independent"* and hedges only in its **last**
sentence. Both halves of the opening fail at the quoted precision:

- "truncation-independent" — the ladder's own `relative_spread` is `0.0013899483438452888`
  (`0.139 %` over a 16-fold range). Flat, measurably; independent, no.
- "bounded away from zero" — float64 **evidence** of a positive limit. The field's own closing
  sentence concedes exactly this, and no proved floor exists in leg 176 or in PUB2.

Leg 268 corrected the **tail block's** twin descriptor (`K3`, on `C2_tail_block_sigma_min.reading`)
and never covered `C1`. That uncovered field is what legs **250, 268, 271** each had to chase out
of PUB2's prose separately, and what `verify_271` §6b named as the source.

---

## 2. What I did — three mechanisms, no fourth

Per the gate's explicit instruction not to improvise a fourth mechanism, and following the
verifier's own suggestion:

| clause | mechanism | artifact |
|---|---|---|
| (a) | one-sentence relabel in §3.5, both difference sets now standing under their own names | `writeup/4_p2_lottery/TECHNICAL_P2_PUB2_V1.md` |
| (b) | register entry **#5** + a full §5 section indexing both companions and stating the binding quotation rule | `writeup/CORRECTIONS.md` (**append only**) |
| (b) | companion annotation `A1` (the `C1` `reading` descriptor) and `A2` (the §3.5 label), modelled byte-for-byte on leg 268's companion schema | `writeup/data/p2_route_h2c_v1_construction_annotation_leg274.json` (**new file**) |

**Why a second companion rather than extending leg 268's.** Leg 268's file is itself a banked
record of leg 249's certified data, issued by a different leg on a different date; appending to it
would blur which leg established what. Two companions, both in force, neither superseding the
other, is the same "marked, never hidden" discipline one level up.

**Why an annotation rather than a JSON edit.** The banked file is a measurement record. Silently
mutating a `reading` field destroys the audit trail that is the only reason legs 249/268/271 and
`verify_271` could find any of this. Leg 263's convention is explicit: *corrections are marked,
never hidden.*

### The §3.5 sentence, before → after

Before: *"…`‖T⁻¹‖_X` **rises monotonically** across the ladder (…) with decrements shrinking
geometrically (`1.126e−3 → 5.745e−4 → 3.027e−4 → 1.617e−4`, ratios `0.510 / 0.527 / 0.534`) —
float64 evidence of a finite limit near 4.032…"*

After: the rise carries **its own increments** (`1.804e−2 → 9.269e−3 → 4.901e−3 → 2.623e−3`,
ratios `0.514 / 0.529 / 0.535`), and the original four numbers are retained and named as the
**reciprocal `σ_min = 1/‖T⁻¹‖_X`'s** decrements, with the `≈16×` relation stated in the clause.
Both sets are now recomputable from the ladder printed in the same sentence. Nothing else in the
sentence — the ladder, the `0.865 %`, the `4.032`, the "not a proof of one", the "not a
truncation-independent value" — moved.

---

## 3. Gate

**(a) YES.** The decrements are attached to `σ_min`, and `‖T⁻¹‖_X` carries increments. Recompute
either from the printed ladder and you land on the printed figures; the 16× mismatch is gone and
is additionally stated explicitly, so a reader who *expects* the old numbers finds out why.

**(b) YES.** `CORRECTIONS.md` #5 + §5 index the hazard; the annotation JSON carries the
corrected framing verbatim and the binding instruction (*any quotation of that key must carry the
corrected framing, or the banked text with this annotation beside it*). Verified byte-untouched:
`git diff` reports **0 bytes** changed in `p2_route_h2c_v1_construction.json` and **0 bytes** in
`..._correction_leg268.json`; both are absent from this leg's commit entirely.

**(c) YES.** The TECHNICAL diff is a single hunk in §3.5. `CORRECTIONS.md` gains one table row and
one appended section — no existing line rewritten, so leg 263's own §4 invariants are untouched.
Everything else is a new file.

**Consequence per the leg spec: leg 270's serialization condition is met — flag it dispatchable.**

---

## 4. Ceiling

A descriptor and a prose subject label. Nothing measured, nothing verified anew. Leg 176's gate
answer is **unchanged**. The `ℓ¹_w` converges-vs-diverges contrast is **unaffected** — both
number sets are the same geometric decay. `Z₁` still fails at `140.72`. `a = 0` only. **No ban
lifts. No route is promoted. No link of the `L1 → L4` chain moves. Clay `~0.05%`. No GA compute.**
