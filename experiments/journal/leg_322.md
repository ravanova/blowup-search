# Leg 322 — Route-F70: produce fig70 from leg 311's banked JSON, or correct the claim

**Branch** `leg/322-egmf-v1` (worktree), from `origin/main`. **LEG role**, light difficulty.
`plan_of_record.py` and `DIRECTION.md` untouched. No solve run, no re-measurement. Leg 311's
banked JSON (`writeup/data/p2_route_ivax_v1.json`) read only, never edited.

## The debt

Leg 311's quartet claimed `fig70` as **provisional** (its own journal, `experiments/journal/leg_311.md`)
because `fig69` collided with leg 301 on rebase; the claim never resolved to a shipped file, and
`writeup/INDEX.md:107` recorded the gap as `**GAP: number claimed, no file**` / `` `fig70`
**reserved** ``, closed by integration rather than papered over.

## Gate

> Does leg 311's banked JSON yield fig70 through `build_figures.py` without any re-measurement?

**GATE: YES.** `writeup/data/p2_route_ivax_v1.json`'s `census` block carries a `rows` array
(one row per model: `FRAC-GCLM`, `CRIT-DISS-GCLM`, `FRAC-BOUSSINESQ`, each with
`killed_by_iv_a`, `n_operators`, `n_unnamed_by_remark_40`, `fluid_adjacent`,
`unnamed_operator_classes`) plus top-level scalars (`n_killed_by_iv_a`, `n_total`,
`leg_261_reference_fluid_kill_count` = "18 of 18"). That is plottable data already banked, no
re-measurement needed.

## What was built

Additive stanza `fig_route_ivax_v1()` in `writeup/build_figures.py`, matching the file's existing
per-figure-function pattern (loads via the shared `load()` helper, writes into `FIGS`, registered
in the `__main__` build order). Two panels, both read straight off the JSON:

- **Left**: kill-rate bar comparison, the fluid pool leg 261 measured (18/18, hardcoded from the
  JSON's own `leg_261_reference_fluid_kill_count` string, cross-checked against the number "18")
  against the non-fluid pool this leg measured (`n_killed_by_iv_a` of `n_total`, read live = 3/3).
- **Right**: per-model horizontal bar of `n_unnamed_by_remark_40`, colored by `fluid_adjacent` —
  the mechanism (nonlocality) the gate's own verdict names (`verdict_code =
  SCREEN_IV_A_BITES_OFF_AXIS_MECHANISM_NONLOCALITY_NOT_INCOMPRESSIBILITY`).

Ran `fig_route_ivax_v1()` directly (see below) and confirmed
`writeup/figures/fig70_route_ivax_v1.png` is written and matches the JSON's numbers (18/18, 3/3,
n_unnamed=2 for all three rows).

```
.venv/bin/python -c "
import sys; sys.path.insert(0, 'writeup')
import build_figures as bf
bf.fig_route_ivax_v1()
"
```

`writeup/INDEX.md`'s `fig70` row for leg 311 closed: the `**GAP...**` cell replaced with `Y`, the
`` `fig70` **reserved** `` cell replaced with `` `fig70` ``, and the notes cell rewritten to
describe what was built and where.

## Territory

Touched only: `writeup/build_figures.py` (additive stanza), `writeup/figures/fig70_route_ivax_v1.png`
(new), `writeup/INDEX.md` (the one fig70/leg-311 row), `writeup/novelty/leg_322.md` (new), this
file. `writeup/data/p2_route_ivax_v1.json` read, never edited. `plan_of_record.py` and
`DIRECTION.md` not touched.

No solver module read or touched, no BANNED item lifted, weakened, or reinterpreted, nothing
escalated, nothing lifted.
