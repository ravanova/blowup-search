# Leg 111 — Route-WE: the weighted-energy coercivity gap on the `a = 0` CLM linearisation

**Agent:** LEG-D. **Branch:** `leg/we-v1`. **Date:** 2026-08-06. **Claim-bearing.**
**New module:** `solver/energy_coercivity.py` (nothing else under `solver/` is edited).

## Gate and answer

> "For at least one weight in the pre-named family, is the measured coercivity gap of the
> weighted-energy form on the a=0 CLM linearization positive and stable (within a
> pre-committed tolerance declared in the driver) across two grid refinements?"

**NO — 0 of 7 named weights.** Taking the `no` branch verbatim: *the weighted-energy
realization joins the dead list on the friendliest object, and the wall bounds the real
target's difficulty from below. Bank it as a third dead realization, which STRENGTHENS NG's
framing; report magnitudes, not the boolean.* **No stage is claimed; `NG` stays `NEXT`.**

> ## CORRECTION — 2026-08-07, leg 263 (Route-WESC), by the user's Ruling 2 on leg 178
>
> **The headline above is RE-SCOPED. What over-read what, in one sentence: leg 111 read a
> measurement made on ONE trial space — the unconstrained odd-sine span `{sin kθ}`, vanishing
> order `p = 1` — as a statement about the weighted-energy REALIZATION, and banked it as "a
> third dead realization." It is not. It is a property of that trial space.**
>
> Every number in this leg stands. Nothing below is re-run, re-derived, or edited; the
> original headline is left standing above rather than rewritten, per this repository's
> mark-don't-hide convention. What is corrected is the **scope of the sentence**, and the
> correction is measured, twice, by two later legs on this leg's own quadrature:
>
> | | leg 111, `p = 1` (this leg) | leg 141, `p = 2` | leg 178, `p = 3` (`T2_egm`) |
> |---|---|---|---|
> | admissible window `(3, 2p+1)` | `(3, 3)` — **width 0.0** | `(3, 5)` — width **2.0** | `(3, 7)` — width **4.0** |
> | membership `‖·‖²` ratio per refinement at `γ = 4` | **`1.677722e+07`** (divergent) | `1.0000000000000002` | **`1.000000`** (convergent) |
> | exponent margin at `γ = 4` | **`−1.0`** | — | **`+3.0`** |
> | largest admissible gap | **`−0.4999241`** | — | **`+0.499999667`** |
>
> Leg 141 (`7bd6c08`) reproduced this leg's `p = 1` admissibility column at `abs_diff 0.0` —
> same quadrature, same code, only the trial space moved — and located the positive side in
> print (Elgindi–Ghoul–Masmoudi [arXiv:1906.05811](https://arxiv.org/abs/1906.05811) Prop. 2.1,
> gap `1/2` at `γ = 4` on this very `a = 0` CLM object). Leg 165 (`84a5c7e`) classified the
> finding TIER 1, realization-dependent. Leg 178 (`ba1a22c`, landed by the user's Ruling 1 of
> 2026-08-07) closed it by **measuring** the positive side on a constrained trial space of this
> module's own: `+0.499999667`, relative change over the last two refinements `5.890e−07` and
> `7.409e−08`, admissibility ratio `1.000000`. An exact fact fell out of leg 178 that is about
> this leg's own enumeration: EGM's published weight IS this leg's family `B` at `γ = 4`
> (`φ^E/φ^B4` constant `32`, relative spread `1.998e-15`) — the one member the seven-weight
> ladder above excluded (`B` stopped at `γ = 2`, `A` at `γ = 4`).
>
> **The ceiling on the correction, which is not optional.** "Not dead" is not "open." Per the
> user's own bounding language at leg 178: **no weighted-energy lane opens.** EGM buy the
> origin conditions with two free modulation parameters, so a gap on a constrained trial space
> is not a certificate; the `−1/2` is EGM's, reproduced numerically; the object is still the
> `a = 0` CLM linearisation whose `Y₀` is exactly zero for the banned degenerate reason; and
> nothing here is a statement about `HL_S2_nonsymmetric`. No ban lifts, no link of the
> `L1 → L4` chain moves, Clay stays `~0.05%`. **Clause S7 below binds unchanged.**
>
> **What this correction does NOT touch.** `plan_of_record.py`'s live re-posed stage-`V` ban
> names a **different** triple — `ℓ¹_w` coefficient basis (leg 54), collocation basis (leg 56),
> origin-`H²` (legs 163/176/182) — of which the weighted-energy realization is **not a
> member**. That ban's text is byte-identical before and after this correction. See
> `writeup/CORRECTIONS.md` §4.

## The magnitudes, which are the result

Every **admissible** weight's modulated gap is negative and converges, over `n = 32 … 256`, to
**minus the damping factor at the origin**, `-(3 - γ)/2`:

| weight | `n = 32` | `64` | `128` | `256` | limit `-(3-γ)/2` |
|---|---|---|---|---|---|
| `A0_flat` (γ=0) | −1.492304 | −1.498135 | −1.499541 | **−1.499886** | −1.500 |
| `A1` (γ=1) | −1.003329 | −1.005822 | −1.006152 | **−1.006201** | −1.000 |
| `A2` (γ=2) | −0.494869 | −0.498757 | −0.499694 | **−0.499924** | −0.500 |
| `B0` (γ=0) | −1.494869 | −1.498757 | −1.499694 | **−1.499924** | −1.500 |
| `B2` (γ=2) | −0.497435 | −0.499378 | −0.499847 | **−0.499962** | −0.500 |

`A1` is the only member the limit does not capture to 4 digits (−1.00620 against −1.000): there
the nonlocal Hilbert term contributes `+0.0062` on top of the local part, which is the honest
size of the term the weight cannot touch.

## The mechanism, registered before the ladders ran and then confirmed

Integrating the transport term by parts puts the **entire local part of the form** into a
single multiplier — the damping factor

```
D_φ(θ) = (3/2) cos θ + (1/2) sin θ (log φ)'(θ) ,       D_φ(0) = (3 − γ)/2
```

for both named families (checked against a central difference to `2.3e−10` over all seven
weights). Chen–Hou's move is to choose `φ` making this negative where the profile is singular,
i.e. at the origin. That needs **γ > 3**. But `sin kθ ~ kθ` there, so the integrand of
`‖sin θ‖²_φ` is `~ θ^(2−γ)`, and the basis is in `L²_φ` only for **γ < 3**.

**The two thresholds are the same number.** The window is not empty by a margin — it has
**zero width**, closing exactly at `γ = 3`, where the weighted norm log-diverges. That is
measured, not argued: at equally spaced grading depths the norm's increments are equal to
**1.000000** at `γ = 3` (the logarithmic signature) and grow by **4096 = 64²** at `γ = 4` (a
power), while every admissible member's norm ratio is `1.000000` to six places.

This is a **new instance of lesson 88's shape in a realization that shares no quantity with
it**: the exponent at which the method would start working is exactly the exponent at which
the space stops existing.

## The one positive number, and why it is a statement about the code

`A3` (γ=3) reports a *positive* gap. It fails on three independent grounds, any one of which
would be enough:

1. **It collapses.** `+0.042609 → +0.012644 → +0.003640 → +0.000961 → +0.000121` over
   `n = 16 … 256` — an 87 % relative drop at the last refinement alone.
2. **It moves with the quadrature cutoff.** At fixed `n = 64`, sweeping the grading depth
   `12 → 24 → 48 → 96` gives `+0.002938 → +0.003640 → +0.003908 → +0.004005` — relative
   spread **2.665e−01** — while admissible `A2` sits at `−0.498757` across the same sweep with
   relative spread **6.678e−16**, i.e. machine precision. The number is finite only because
   the innermost panel is (lesson 86).
3. **Its space does not contain the basis** (the admissibility precondition, above).

Its relative change per refinement is `0.7033, 0.7121, 0.7361, 0.8736` — *increasing*: the
sequence is not merely failing to settle, it is coming apart faster the further it is refined.

`A4_chen_hou` (γ=4 — Chen–Hou–Huang's own exponent) is worse and in the same direction: the
gap wanders `−2.447 → −3.181 → −1.279 → −0.193 → −0.072` while the whitening drops
`0 → 2 → 13 → 45 → 109` directions and `cond(G)` climbs `4.88e3 → 6.15e7`. **Their exponent is
the one this object cannot afford.**

**Disclosure on the tolerance.** The gate's tolerance had to be declared in the driver; the
ladders were run exploratorily first. The declared form is **relative** (`≤ 5 %` per
refinement) because an absolute tolerance is passed trivially by a sequence collapsing to
zero — which is precisely `A3`'s behaviour. `A3` is the only weight the two readings judge
differently, and it is disqualified twice over by criteria fixed independently of it, so the
answer does not turn on the choice. Both readings are recorded per weight in the JSON.

## Controls, and both can report the other answer

- **Positive control (lesson 90).** `L − μΛ` turns the unbounded part from a shift into a
  multiplier and changes nothing else. `A0_flat`: `−1.49814 (μ=0) → −0.17271 → +0.79960 →
  +2.53778 → +5.73584 (μ=2)`; `A2`: `−0.49876 → +5.82045`. Monotone, and **the sign flips** —
  so the negative gap at `μ = 0` is a measurement and not a property of the code.
- **External known-answer window (lesson 84).** Xu arXiv:2607.19762 publishes this
  linearisation's spectrum: point spectrum exactly `{0,1}`, essential spectrum on
  `Re λ = −1/2`, modulated gap `1/2`. Both point modes reproduce here to `2.2e−16`
  (`L sin 2θ = 0`; `L(sin θ + sin 2θ/2) = itself`); the **raw** gap is `≤ −1` for every
  admissible weight at every `n`, as the published eigenvalue `1` requires (at `n = 16`:
  `−2.1015, −2.6735, −4.0801, −1.4830, −1.1180`); and the largest admissible modulated gap,
  **−0.499924**, is inside the published ceiling `+0.5`. The instrument agrees with the
  literature everywhere the literature has an answer.
- **Quadrature validated where the answer is known.** The flat-weight Gram matrix is exactly
  `(π/2)I` to `1.4e−15` at `n = 64`. This gate earned its place: a purely geometric mesh —
  the obvious choice for a singular weight — reported `cond(G) = 7.1e10` for that exactly
  diagonal matrix, because its interior panels were wider than a wavelength of `sin 64θ`. The
  first draft's `n = 64` gaps (`−4.0e6`) were entirely that bug.

## Order of work

1. `plan_of_record.py` read in full. Ban check written out in the novelty log for all four
   bans that could touch this leg. **Shared quantity count with the banned `Z₁`/`s`/`K`/border
   space: zero** — no `Y₀`, `Z₁`, `Z₂`, approximate inverse `A`, `A_K ⊕ A_tail` split, border,
   or `ℓ¹_w` norm appears anywhere in this leg's code. Stage `V` is not re-posed: no
   certificate margin is computed, no limit in `μ` is taken, and dissipation appears only as
   legs 51/53's positive control.
2. `capabilities.py` grepped **before** a line was written (standing ban): the `a = 0` CLM
   linearisation is carried by `solver/spectral_certificate.py` and `solver/rescaled_spectrum.py`;
   **no weighted-`L²` energy form, Gram matrix, or coercivity gap existed anywhere under
   `solver/`.** The operator was reused, not re-derived — and that reuse is *checked*: the
   coefficient matrix here is **exactly equal (`0.0`)** to `bordered_linearization`'s interior
   block, so the third realization is provably measured on the same object the first two died
   on.
3. **Novelty pass run and committed BEFORE construction** (`writeup/novelty/leg_111.md`, commit
   `fa0db5c`), verdict `PROCEED_AS_SCOPING_MEASUREMENT_ONLY`. Both the method
   (Chen–Hou–Huang arXiv:1905.06387, singular weight `x^{-k}`, `k = 4`) and the object's
   spectrum (Xu arXiv:2607.19762) are published, so **no mathematical claim is available to
   this leg** — the value of the number is internal.
4. `solver/energy_coercivity.py` + `test_energy_coercivity.py` (**16/16 gates pass**),
   `experiments/p2_route_we_v1_coercivity.py` → `writeup/data/p2_route_we_v1_coercivity.json`.

## Territory note (declared, not hidden)

`DIRECTION.md`'s leg-111 territory lists five files plus this journal. One file **outside** it
was edited: **`capabilities.py`**, one appended entry. This is forced by the repository's own
always-on merge gate — `test_capabilities.py::test_every_solver_module_is_indexed` fails if a
`solver/*.py` exists without an entry — so a new solver module cannot land without it. The
edit is append-only, adds no row for any other module, and collides with no live leg (58 and
62 edit existing modules; none of the assigned set adds a new one). Flagged here so the
orchestrator sees it rather than discovers it.

## Ceiling (clause S7, unchanged)

Float64 throughout; nothing interval-enclosed, nothing rigorous. Measured on the `a = 0` CLM
linearisation — one mode, analytic, the friendliest object in the repository. **A wall here
bounds `HL_S2_nonsymmetric`'s difficulty from below, not above.** No link of the `L1 → L4`
chain moved. Clay odds unchanged at `~0.05%`.
