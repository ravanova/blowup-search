# Leg 292 — Route-CAPA: `capabilities.py` freshness audit, second generation

**Branch.** `leg/292-capa-v2` (a RESUME — the novelty pass was committed in an earlier
session, at merge base `4db7d1c`, and this session re-based it forward).
**Rebased merge base.** `80c0cc4` (`origin/main`).
**Runner.** `experiments/p2_route_capa_v2_audit.py`
**Data.** `writeup/data/p2_route_capa_v2_audit.json`
**Figure.** `writeup/figures/fig80_route_capa_v2_audit.png`
**Prose.** `writeup/4_p2_lottery/BLOG_P2_ROUTECAPA_V2.md`,
`writeup/4_p2_lottery/TECHNICAL_P2_ROUTECAPA_V2.md`
**Predecessor.** Leg 71 (Route-CAP), the first generation, at `e203b52`/`10fed85`/`e742f6d`.

---

## 0. Staleness re-check of the committed novelty pass — done FIRST, as instructed

The brief required the inherited novelty pass to be re-checked against everything that
landed on `main` since it was written, and the row/test counts in particular to be
**re-measured at current HEAD rather than inherited**. Both were done before any
construction.

**(a) Subject-matter staleness — measured, not assumed.** The novelty pass was written
at merge base `4db7d1c`; this leg rebased to `80c0cc4`. The decisive query:

```
git log --oneline 4db7d1c..80c0cc4 -- capabilities.py solver/     ->  0 commits
```

**Zero commits in the intervening range touch either `capabilities.py` or any
`solver/*.py`.** The intervening history is leg 291's Route-HLR2 landing (a literature
leg), an orchestrator integration commit, and the Headroom context-compression tooling
(`docs/`, `.mcp.json.example`, a SessionStart script). So the novelty pass's *subject*
could not have drifted: the file under audit and the module set it indexes are
byte-identical to what the pass described.

**(b) Counts re-measured at HEAD anyway, not inherited.** The pass claims 48 rows / 46
distinct tests. Re-measured directly from `capabilities.CAPABILITIES` at `80c0cc4`:

| quantity | novelty pass claimed | re-measured at HEAD | verdict |
|---|---|---|---|
| rows in `CAPABILITIES` | 48 | **48** | agrees |
| distinct `module` values | (not stated) | **48** (0 duplicates) | — |
| distinct cited test files | 46 (47 after the S5 repair below) | **46** | agrees |
| `solver/*.py` on disk (ex-`__init__.py`) | (not stated) | **48** | — |

**(c) Its one falsifiable prediction, scored.** The pass predicted, in writing and
before measuring, that the module-completeness axis would be *clean*, because the
append-only convention (`ORCHESTRATION.md` §5a — a landing leg appends its own row on
the commit that adds its module) is self-maintaining. That prediction is **confirmed**:
0 missing, 0 ghost (§1 below). It also predicted the real audit surface would be
red-at-HEAD tests and stale `validated` prose. That prediction is **half right and half
wrong**, and the way it is wrong is this leg's actual contribution (§3, §4).

**Conclusion of the staleness check: the novelty pass is NOT stale and is carried
forward unmodified.** Had the range contained a single `solver/` or `capabilities.py`
commit, it would have been re-run rather than trusted.

---

## 1. S1 — module completeness, both directions

| quantity | count |
|---|---|
| rows in the index | 48 |
| distinct modules named | 48 |
| duplicate module rows | **0** |
| `solver/*.py` on disk (ex-`__init__.py`) | 48 |
| **S1 missing** (module on disk, no row) | **0** |
| **S1 ghost** (row naming a module not on disk) | **0** |

The set equality is exact in both directions. Leg 71 audited 42 rows; the index has
grown by 6 rows across ~220 legs with **zero** completeness defects introduced. The
brief's named example, `solver/bc_weighted_sobolev.py` (leg 256), is present and carries
a full `validated` field with magnitudes (Breden-Chu Theorem 42 reproduced end to end,
ubar to 4.36e-10 in H^2(mu), Y to 6.0e-05 relative).

This is worth stating as a positive result rather than a shrug: the append-only
convention is doing the job it was designed for, and the axis that a freshness audit
exists to catch is the axis that has not drifted at all.

**Note on leg 302** (per the brief): it is building `solver/p2_apparatus_term1.py` and
was told to leave its registration as an integration note rather than edit this file. It
is not on disk at `80c0cc4` and its absence from the index is therefore correct, not an
S1 miss.

## 2. S2 — cited-test existence

| quantity | count |
|---|---|
| rows with an empty `test` field | **0** |
| rows citing a test file absent from disk | **0** |
| distinct cited test files | 46 (47 after the S5 repair below) |

Clean, as leg 71 also found (its S1=0, S2=0). This is the one axis the existing drift
detector `test_capabilities.py` genuinely enforces, so a defect here would have been a
detector bug rather than index drift.

## 3. S3 — greenness at HEAD (the check the drift detector does NOT do)

`test_capabilities.py` enforces exactly three things: the `test` field is non-empty, the
cited path exists on disk, and `validated` is longer than 20 characters. It never runs
the cited test. Leg 71 named this gap, and that it is unchanged is **measured, not
re-asserted**: `git log e203b52..80c0cc4 -- test_capabilities.py` returns **0 commits**,
so the detector is byte-identical to the one leg 71 read, same five test functions, while
the index it guards grew by 6 rows over ~220 legs.

Every distinct cited test file was executed with `<python> test_x.py` (this repo's
convention: every `test_*.py` is a self-running script, no pytest), 2 workers, BLAS
pinned to one thread per process. The set is **46 before the S5 repair and 47 after** —
repointing the `solver/finite_support.py` row introduced one test file the index had
never cited, so the repair enlarged the very sweep that justified it; both were run.

**Result: 47 of 47 cited tests green at `80c0cc4`. Zero red, zero timed out.**

| | count | cumulative wall |
|---|---|---|
| distinct cited tests executed | **47** | **5396.5 s** |
| green | **47** | |
| red at HEAD | **0** | |
| timed out (5400 s cap) | **0** | |

Slowest five: `test_advection_scope.py` **2512.2 s**, `test_marginal_flow.py` **556.0 s**,
`test_viscous_novelty.py` **310.3 s**, `test_spectral_certificate.py` **308.2 s**,
`test_fractional_boussinesq.py` **222.0 s**.

**Leg 71's two reds are both green now**, which is the comparison that matters, since a
first-generation audit's red entries are exactly what a second generation exists to
re-measure: `test_fractional_boussinesq.py` **222.0 s green** and `test_profile_newton.py`
**160.5 s green**. Neither was repaired by this leg; both were fixed by their owning legs
in the intervening ~220 legs and nothing recorded that they had been. So the index's
*pass-status* dimension has not decayed — it has improved, and silently.

`test_advection_scope.py` deserves its own line. Leg 71 measured it at **PASS 2363.7 s**
clean and **3045.6 s** under load, and hit the 3600 s cap when it ran with 6 workers. Here
it took **2512.2 s** — squarely inside leg 71's band, so it is genuinely slow rather than
hung or degraded. It is the single dominant cost of this axis: 2512.2 of 5396.5 s, i.e.
**47% of the entire sweep is one test**.

**Provenance of each verdict, stated because they are not all of equal strength:**

| source | n | what it means |
|---|---|---|
| `log-ingest` | 45 | real subprocess exit statuses, printed by this runner, recovered from its own progress log after the driver process was killed at 45/46 before serialising. The measurements are real; only the write-out was lost. |
| `direct` | 1 | `test_finite_support_adversarial.py`, exit status captured in-process (33.4 s) |
| `detached-run` | 1 | `test_advection_scope.py`. Run under `nohup` so the 42-minute cost could overlap other work; **its exit status was not captured.** Its green verdict rests on the test's own terminal banner `ALL ADVECTION-SCOPE TESTS PASSED` being the last non-empty line of the log with no traceback anywhere in it, plus all six of its `[ok]` gate lines. That is weaker evidence than a captured `rc`, it is labelled `source: "detached-run"` with `returncode: null` in the JSON, and it is stated here rather than smoothed over. Re-running it purely to convert a banner into an exit code costs another 2512 s and changes no conclusion. |

### 3b. Cost of S3, reported explicitly (standing performance rule, 2026-08-11)

The rule: before running anything expected to exceed 10 minutes, estimate it, improve it,
and record estimate / change / achieved runtime — but never by weakening the gate.

- **Estimate before running:** 46 test files, this repo's dedicated tests historically
  30–300 s each, so 25–60 min of process time; the pinned-BLAS, 2-worker choice was made
  up front from leg 71's measured oversubscription blow-up (6 workers turned a 112 s test
  into >3600 s, 32x), *not* discovered here.
- **Achieved:** 46 tests, **2884.3 s cumulative process wall**, longest single test
  `test_marginal_flow.py` at **556.0 s**; 46 of 46 individually under 10 minutes. Plus
  `test_advection_scope.py`, the 47th, which alone exceeds the others (below).
- **What was changed to make it cheaper:** the sweep is memoised in the curated JSON and
  re-entrant — `--only` merges single verdicts and `--static-only` re-runs the eight
  cheap static axes in seconds without re-executing anything. A killed sweep is therefore
  recovered, not repeated; that is what made the mid-run interruption cost one test rather
  than 46. `--ingest-sweep-log` recovers verdicts already printed, stamped
  `source: "log-ingest"` so recovered records are never confusable with fresh ones.
- **Why the cost is nevertheless unavoidable:** the gate's pre-committed wording asks
  whether every module has an accurate capability line including **pass status**. Pass
  status is not statically derivable — the only way to know a cited test passes at HEAD is
  to run it. A cheaper variant (sampling, or trusting the last CI verdict) cannot answer
  the gate as written, and the gate text is immutable. So the run stands, with the number:
  **2884.3 s + the advection outlier**, spent once, for a standing runner that any future
  leg re-runs at that same price rather than rediscovering the axes.

### 3a. The one red I caused myself, and how flake-diagnosis-before-belief resolved it

After repointing the `solver/finite_support.py` row, `test_finite_support_adversarial.py`
returned **rc = 1 in 30.1 s** — the same file that had returned rc = 0 when run by hand
minutes earlier, and the sole evidence the repair rests on. Per the brief and
`ORCHESTRATION.md` §9g it was re-run **alone, under low load, before being recorded as
red**. It failed identically, so it is not a flake, and the stored `stderr_tail` names the
cause without ambiguity:

```
AssertionError: ['capabilities.py:62: ...', 'capabilities.py:661: ...', 'capabilities.py:665: ...']
```

Leg 124's gate **S10** proves the SUPERSEDED module has zero importers by walking every
`*.py` in the tree and flagging any line containing both the module's stem and the
substring `import`. The explanatory comment **this leg had just written** — prose about
the fact that a test imports that module — matched on three lines. To a substring scan a
documentation comment and an import statement are the same object.

Reworded so no line outside leg 124's own two files carries both tokens; the test is
**green again at 33.4 s**, and the verified-green evidence under the repair is restored.

**State this plainly, because it is a finding in its own right: leg 124's S10 gate can be
turned red by prose anywhere in the repository.** Any `*.py` in the tree — a comment, a
docstring, a string literal in an unrelated experiment — that puts `finite_support` and
`import` on one line fails that assertion. That is a property of **the gate**, not of the
code it guards: `solver/finite_support.py` can be byte-identical and correct while the
gate is red. The gate is deliberately conservative (it is a soundness gate whose whole
job is to catch a real importer appearing), and the cost of that conservatism is that
`capabilities.py` cannot describe, in words, the relationship it is documenting.

Two consequences worth banking:

1. A **CAUTION** now sits beside the row in `capabilities.py`, because the next editor of
   that comment will otherwise rediscover this by a red test with no visible link to the
   edit. Diagnosis cost here: roughly one re-run plus one JSON read; without the note it
   is a cold debug of somebody else's soundness gate.
2. This is the audit's own premise firing on the auditor. The axes are executable, so an
   error introduced **by the audit itself** was caught by the audit's own sweep in the
   same run rather than shipped as a green-looking claim. A written-down check would have
   caught nothing.

## 4. S4 — known-answer-gate presence in the `validated` field

Each row's `validated` prose was classified into three exhaustive classes:

| class | count of 48 | meaning |
|---|---|---|
| **magnitude** | 33 | states a gate AND a number (`4.4e-16`, `2.1%`, `42x`, …) |
| **declared-absent** | 3 | says "no known-answer gate" plainly — the file's own header explicitly blesses this as an honest value |
| **prose-without-magnitude** | 12 | claims validation, names no number |

The 12 are `op_lower`, `decay_collocation`, `collocation_newton`, `reduced_certificate`,
`nk_fourier`, `nk_seminorm`, `turning_point`, `profile_newton`, `advection_scope`,
`target_selection`, `ga_search`, `finite_support`.

**This is a shape finding, not 12 defects.** Read individually, most of the 12 are
honest: `reduced_certificate` says "explicitly NOT a proof -- float64 throughout";
`nk_fourier` and `nk_seminorm` each say "no independent published known answer" in so
many words; `finite_support` says "nothing -- SUPERSEDED". What the count actually
measures is that **the index's own stated contract — `validated` is "the strongest
KNOWN-ANSWER gate it passes, with the magnitude, or 'no known-answer gate' said
plainly"** — is met by 36 of 48 rows in the letter (33 magnitude + 3 declared-absent)
and by the remaining 12 only in spirit. The honest headline is a **75% magnitude rate**,
not "12 stale rows", and this leg does not rewrite those 12: inventing magnitudes for
another leg's module from an audit chair is exactly the fabrication the `validated`
field exists to prevent.

## 5. S5 — RELEVANCE: does the cited test actually load the module citing it?

**Correction to this leg's OWN first framing, recorded rather than quietly fixed.** My
first draft of this section claimed leg 71 "named this gap in words and never
systematised it". That is **false**, and reading leg 71's banked JSON rather than only
its commit message is what falsified it: `writeup/data/p2_route_cap_v1_audit.json`
carries per-row `test_imports_module` and `test_mentions_module` booleans. Leg 71
systematised relevance perfectly well. **My S5 is a re-implementation, not a new axis.**

That makes the result stronger, not weaker, because the two independent implementations
agree exactly:

| | leg 71 (42 rows) | leg 292 (48 rows) |
|---|---|---|
| rows whose cited test never loads the module | **1** | **1** |
| which row | `solver/finite_support.py` | `solver/finite_support.py` |
| cited test | `test_first_integral.py` | `test_first_integral.py` |
| leg 71's flag for it | `S4_no_coverage` | — |

A scanner written ~220 legs later, without reference to leg 71's code, lands on the
identical single row. Hand-verified: `grep -n finite_support test_first_integral.py`
returns nothing at all.

### 5.1 The actual finding: a correct flag that stayed open for ~220 legs, and why

**Leg 71 was right and could not fix it.** At its base `e203b52` (2026-08-05) *no test in
the repository imported `solver/finite_support.py`* — hence the honest flag
`S4_no_coverage` rather than a repair. There was nothing to repoint the row at.

Then `test_finite_support_adversarial.py` was created by **leg 124** (`1424da6`,
Route-FSA) — 53 legs later, 22.6 kB, opening with `from solver.finite_support import ...`.
**Leg 124 did not update the index row**, and nothing in the repository would have
noticed: the drift detector cannot see relevance, and leg 71's relevance check lived
inside leg 71's own runner rather than in the merge gate or the detector.

So the sequence is precise and datable:

1. leg 71 measures the gap correctly, flags it, cannot repair it (no such test exists);
2. leg 124 builds exactly the missing test — and the row still points elsewhere;
3. ~220 legs pass with the flag open, because **the check was run once, not standing**.

**The result of this leg is not the repair. It is that leg 124's fix sat on disk,
unnoticed, for roughly 220 legs.** The one-line correction to a `test` field is trivial
and would be worth nothing on its own; what the audit actually measured is the latency of
this repository's audit loop — the interval between a defect being correctly recorded and
anyone acting on it, when the fix was available for three quarters of that interval. That
is a fact about how the project re-reads its own findings, not a fact about
`capabilities.py`, and it does not get smaller because the repair succeeded.

This is banked lesson 68 — *a check that is not executable decays at the rate of memory*
— in a sharper form than usual: this check **was** executable. It simply was not
*re-run*. An audit that ships a runner and no standing hook has a half-life of one leg.

### 5.2 Repair

`test_finite_support_adversarial.py` was run alone at HEAD under the pinned environment:
**rc = 0, green**. Its gate **S10** checks the exact property the row's `validated` field
claims — `zero importers, and the SUPERSEDED / DO NOT USE banner is intact` — so the
corrected pointer is not merely *a* test that loads the module, it is the test that
re-checks the sentence the row makes its claim in.

**`solver/finite_support.py`'s `test` field corrected to
`test_finite_support_adversarial.py`**, with an inline comment recording the evidence,
following leg 71's `ga_search.py` precedent. `validated` left frozen — this leg corrects
factual pointers, it does not re-word other legs' claims. **Leg 71's one unrepairable
flag is now closed**, which is the concrete deliverable of this generation.

## 5a. S7 — merge-gate coverage (leg 71's other axis, re-measured)

`scripts/merge_gate.sh` maps a changed `solver/<name>.py` to `test_<name>.py` at the repo
root and runs it **only if that file exists**. A module whose test is named anything else
is ungated at merge time, however good the test the index cites.

| | leg 71 | leg 292 | delta |
|---|---|---|---|
| rows | 42 | 48 | +6 |
| modules ungated by the merge gate | **7** | **8** | **+1** |
| rate | 17% | 17% | flat |

The one new entry is `solver/certificate_guards.py` (registered by leg 128, after leg 71
measured). Its index row cites `test_nk_bounds_adversarial.py` — a real, relevant, green
test — but `test_certificate_guards.py` does not exist, so editing the shared hypothesis
guard that *all three* certificate-assembly modules now call triggers no test under the
merge gate's own name mapping. The other seven are leg 71's list unchanged:
`hilbert_holder`, `hilbert_pointwise`, `ga_search`, `boussinesq`, `gclm`,
`spectral_utils`, `finite_support`.

**Reported, not repaired.** Fixing it means adding or renaming test files across seven
other legs' modules — outside this leg's territory (`capabilities.py` only), and a
merge-gate design question rather than an index-freshness one. The count is the
deliverable, and the flat rate is the honest reading: the convention is not degrading,
but it is not improving either.

## 6. Negative controls that could have fired and did not

Per the standing discipline, controls that cannot fail are not controls.

1. **The `(N checks)` claims.** Three `validated` fields make a literally falsifiable
   arithmetic claim about a file on disk: `test_boussinesq_dedicated.py (17 checks)`,
   `test_gclm_dedicated.py (14 checks)`, `test_spectral_utils_dedicated.py (10 checks)`.
   The runner counts `^def test_` in each and compares. **3 of 3 agree exactly.** Had a
   leg added a check without updating the row, this would have caught it; it did not
   fire, so the claim "these three counts are current" is measured, not assumed.
2. **The S5 scanner itself.** It returns a *positive* on 47 of 48 rows, including the
   non-obvious `solver/certificate_guards.py`, whose row cites
   `test_nk_bounds_adversarial.py` — a file named after a *different* module. Hand-checked:
   that file does `import solver.certificate_guards as cg`. So the scanner is not simply
   pattern-matching filename against module name; it distinguishes a genuinely relevant
   cross-named citation from an irrelevant same-family one.
3. **The drift detector after the edit.** `test_capabilities.py` re-run post-correction:
   all five tests pass, 48 distinct object keys, 1 superseded module correctly labelled.

## 7. Gate answer

> **Gate.** Does a fresh audit find every `solver/*.py` module represented by an accurate
> capability line (test presence, pass status, known-answer gate presence), with zero
> modules missing and zero stale entries?
>   yes -> Bank as current. no -> Fix the specific stale/missing entries found; report
>   the count.

**Answer: NO — one stale entry, now fixed. Count reported below. Zero modules missing.**

Taking the gate's three named sub-conditions one at a time, in its own terms:

| gate sub-condition | measured | verdict |
|---|---|---|
| zero modules missing | 48 rows / 48 distinct modules / 48 `solver/*.py` on disk; 0 missing, 0 ghost, 0 duplicate | **PASS** |
| **test presence** | 0 empty `test` fields, 0 cited files absent from disk | **PASS** |
| **pass status** | 47 of 47 distinct cited tests executed green at `80c0cc4`; 0 red, 0 timed out, 5396.5 s cumulative | **PASS** |
| **known-answer-gate presence** | 33 rows name a magnitude, 3 say "no known-answer gate" plainly, 12 claim validation in prose with no number — 36/48 = **75%** meet the file's own stated contract | **PARTIAL — see below** |
| zero stale entries | **1 stale entry of 48** (0 by the drift detector's definition, 1 by the accuracy the gate asks about) | **FAIL, fixed** |

**The count the gate asks for: 1 stale entry, and it is repaired in this commit.**
`solver/finite_support.py` cited `test_first_integral.py`, a file containing zero
occurrences of the module's name, which therefore cannot fail when the module breaks. The
`test` field is repointed to `test_finite_support_adversarial.py` — a test that loads the
module and whose gate S10 re-checks the exact property the row's `validated` field claims.
`validated` is left frozen: this leg corrects pointers, it does not reword other legs'
results. Nothing else in the index was edited.

**On the 12 magnitude-free rows, so the "no" is not overstated.** They are a *contract*
shortfall, not stale entries: each names a real, existing, green test, and none makes a
false claim — they simply describe validation in words instead of digits ("explicitly NOT
a proof — float64 throughout"; "no independent published known answer"). They are named
and counted in §4 and deliberately **not** rewritten, because supplying a magnitude for
another leg's module from an audit chair is precisely the fabrication that field exists to
prevent. They are therefore reported as an open, owned-elsewhere count, not repaired, and
not counted as the gate's "stale entries".

**So the disposition is: fix-and-bank.** One specific stale entry found, fixed, and the
count reported, per the gate's `no` branch; every other sub-condition passes at HEAD, so
the rest of the index banks as current.

**What the leg actually establishes, which is larger than the repair.** The one-line
correction is worth little on its own. The finding is that **leg 71 measured this same
defect ~220 legs ago, correctly, and could not fix it because no test then loaded the
module — and leg 124 wrote exactly that test 53 legs later, after which the fix sat on
disk, unused, for the remaining ~167 legs.** That is a measurement of this repository's
audit-loop latency, not of `capabilities.py`. The mechanism is stated plainly: leg 71's
relevance check lived inside leg 71's runner, so nothing re-asked it. A check that is run
once is a check with a half-life of one leg — banked lesson 68 in a sharper form, because
this check *was* executable and still decayed, simply by never being re-run.

**Secondary finding, banked in its own right:** leg 124's S10 gate can be turned red by
prose anywhere in the repository — any `*.py` line carrying both the module stem and
`import`, comment or docstring included — while `solver/finite_support.py` stays
byte-identical and correct. That is a property of the gate, not of the code it guards.
This leg's own explanatory comment tripped it (rc=1, 30.1 s), which is how the property
was found; a CAUTION note now sits beside the row so the next editor inherits it.

## 8. Clay odds

**Unmoved, ~0.05%.** This leg is repository hygiene. It measured no property of any
mathematical object, moved no link of the L1→L4 chain, and banked no new constant. Its
value is entirely instrumental: the standing ban "building a solver without grepping
`capabilities.py` for the object first" (lifted by: never) is only as good as the file's
accuracy, and this leg measures that accuracy at 48/48 on completeness and 47/48 on
relevance rather than assuming it.

---

## 9. Revalidation at landing time (main moved ~30 commits under this leg)

The sweep was measured at `80c0cc4`. By the time this leg landed, `origin/main` had
advanced to `c3aedff`, which would normally invalidate a greenness measurement. It does not
here, and that is measured rather than hoped:

```
git diff --stat 80c0cc4 origin/main -- solver/           ->  (empty)
git diff --stat 80c0cc4 origin/main -- capabilities.py   ->  (empty)
```

**Neither the audited file nor a single `solver/*.py` changed in the interval.** Leg 302
is the one leg that touched this surface, and it *dropped* `solver/p2_apparatus_term1.py`
before landing (`55166b8`), resolving the same territory collision the brief warned about
— so the module set is byte-identical and the S1/S2/S5 results carry over exactly. The one
new test file in the range, `test_collocation_newton_postrepair.py`, is cited by no row,
so it does not enter the S3 set.

All eight axes were nevertheless re-run at the landing base (`--static-only`, seconds) and
returned identical counts: 48/48/0/0 on S1, 0 on S2, 33/3/12 on S4, 0 on S5 post-repair,
19/0 on S6, 8 on S7, 44/3/0 on S8. Only S3 is inherited from `80c0cc4`, justified by the
empty diff above rather than by assumption.
