# Leg 214 — Route-EGMB — the five-site bracket-sign repair

**Role:** LEG, mechanical repair. **Branch:** `leg/214-egmb-v1`.
**Shape:** pure prose correction. No runner, no JSON, no figure — precedent: legs 180, 179,
186, 108 (see `writeup/novelty/leg_214.md` §4). No new numerics, no new claim.

## Pre-committed gate

> "Do all 5 flagged sites (in leg 141's and leg 165's own journal/prose files) now state the
> bracket form correctly (degrades with `|a|`), matching the arXiv LaTeX source, with every
> other claim in those files byte-for-byte unchanged?"

## Gate answer: **YES**

All five sites corrected; the complete diff is **5 changed lines, 5 insertions, 5 deletions**,
and at character level it is **exactly** 5 inserted `(`, 2 inserted `)`, 3 removed `(` — and
nothing else, in any of the three files.

## The source of truth, re-verified by this leg

Elgindi–Ghoul–Masmoudi, arXiv:1906.05811, Prop. 2.1. E-print fetched independently by this
leg (`https://arxiv.org/e-print/1906.05811` → `OSWModel_Final.tex`); line 356, first
proposition of `\section{Coercivity}`:

```
\int_\RR f\MM f \phi(y)dy&\leq-\Big(\frac{1}{2}-C\,|a|\Big)\int_\RR f(y)^2 \phi(y)dy.
```

Minus **outside** the bracket ⇒ the gap **degrades** with `|a|`. Corroborated at `.tex` lines
531, 535 (which cites the proposition by label), 548, and 608 (§6's `C^α` analogue,
`(1/2 − Cα)`). Leg 190's transcription confirmed, not assumed.

## Site-by-site, before and after

### Site 1 — `experiments/journal/leg_141.md:30`

Inside a blockquote presented as the proposition **verbatim**.

- **before:** `` > `∫_R f M_a f φ(y) dy ≤ (−1/2 − C|a|) ∫_R f(y)² φ(y) dy`." ``
- **after:**  `` > `∫_R f M_a f φ(y) dy ≤ −(1/2 − C|a|) ∫_R f(y)² φ(y) dy`." ``
- **touched:** the `(` moved from before the `−` to after it. Nothing else on the line.

### Site 2 — `experiments/journal/leg_141.md:117`

A table row in the three-source constant comparison.

- **before:** `` | EGM Prop. 2.1's constant `−1/2 − C\|a\|` at `a = 0` | **−0.500000** | ``
- **after:**  `` | EGM Prop. 2.1's constant `−(1/2 − C\|a\|)` at `a = 0` | **−0.500000** | ``
- **touched:** one `(` and one `)` inserted. The value cell `**−0.500000**` is unchanged, and
  is still correct — it is the `a = 0` evaluation, identical under both readings. The row's
  claim, and the table's `max absolute discrepancy 0.000e+00` two lines below, both stand.

### Site 3 — `writeup/novelty/leg_141.md:233`

Same blockquote, in leg 141's novelty file; explicitly headed *"Proposition 2.1, verbatim:"*.

- **before:** `` > `∫_R f M_a f φ(y) dy ≤ (−1/2 − C|a|) ∫_R f(y)² φ(y) dy`." ``
- **after:**  `` > `∫_R f M_a f φ(y) dy ≤ −(1/2 − C|a|) ∫_R f(y)² φ(y) dy`." ``
- **touched:** the `(` relocated. The following line, *"At `a = 0` the constant is exactly
  `−1/2`"*, is left **unchanged and remains true** — that is precisely the value both
  readings share.

### Site 4 — `experiments/journal/leg_165.md:120`

Same blockquote, inherited by leg 165 from leg 141.

- **before:** `` > `∫_R f M_a f φ(y) dy ≤ (−1/2 − C|a|) ∫_R f(y)² φ(y) dy`" ``
- **after:**  `` > `∫_R f M_a f φ(y) dy ≤ −(1/2 − C|a|) ∫_R f(y)² φ(y) dy`" ``
- **touched:** the `(` relocated. (Note this copy closes without the trailing `.` inside the
  quote — that difference from sites 1 and 3 is leg 165's own and was **not** normalised.)

### Site 5 — `experiments/journal/leg_165.md:128`

Inline, in the three-ways sentence.

- **before:** ``…gives **−0.500000**; EGM's constant `−1/2 − C|a|` at `a = 0```
- **after:**  ``…gives **−0.500000**; EGM's constant `−(1/2 − C|a|)` at `a = 0``
- **touched:** one `(` and one `)` inserted. The three quoted values (`−0.500000`,
  `−0.500000`, `0.500000`) and the `Max absolute discrepancy 0.000e+00` on the next line are
  untouched and still correct.

## The critical check — nothing else moved

`git diff --stat` against the merge base:

```
 experiments/journal/leg_141.md | 4 ++--
 experiments/journal/leg_165.md | 4 ++--
 writeup/novelty/leg_141.md     | 2 +-
 3 files changed, 5 insertions(+), 5 deletions(-)
```

`git diff --word-diff=porcelain --word-diff-regex=.`, tallied over the whole diff:

| change | count | where it comes from |
|---|---|---|
| `+(` | 5 | 3 relocations (sites 1, 3, 4) + 2 fresh opens (sites 2, 5) |
| `+)` | 2 | sites 2 and 5 only |
| `-(` | 3 | the 3 relocations |
| anything else | **0** | — |

The arithmetic closes exactly: 3 relocated brackets and 2 inserted pairs, which is the whole
of the intended edit and nothing besides. **No word, number, hypothesis, citation, table
value or line of surrounding argument in any of the three files changed.**

## What moves and what does not

| quantity | before | after | moved? |
|---|---|---|---|
| the constant at `a = 0` | `−0.500000` | `−0.500000` | **no** — the readings agree on axis |
| the three-source discrepancy in legs 141/165 | `0.000e+00` | `0.000e+00` | **no** |
| sign of the `d/d\|a\|` slope of the gap | improving | **degrading** | **yes** — this is the whole repair |
| gap error off axis (`C = 1`), `\|a\| = 0.01 / 0.1 / 0.2` | `0.02 / 0.20 / 0.40` too favourable | `0` | **yes** |
| banked numbers anywhere in the repository | — | — | **0 moved** |
| `solver/literature_gates.py` | already correct (leg 190) | untouched | **not this leg's territory** |

The magnitude of the repair is therefore `2C|a|` in a quantity nothing in this repository
currently evaluates off `a = 0` — a fidelity defect with zero numerical blast radius today,
and a live trap for any future leg that reads these quotes and moves off axis, which is
exactly why it is worth fixing rather than annotating.

## Territory

Declared: the 5 sites (3 files) + `writeup/novelty/leg_214.md` + this file. `git diff
--name-only` against the merge base returns exactly those 5 paths and no others.
`solver/literature_gates.py` deliberately untouched.

## Residual

Leg 190's sixth flagged site — its own `writeup/novelty/leg_190.md` §2 — was already
self-corrected in that file's §8 and is **not** re-edited here; §2 stands as leg 190's
deliberate record of committing the error while warning against it.
