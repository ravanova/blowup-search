# Leg 355 — Route-LCB3: light corrections batch 3 (accumulator items 3-4)

**Branch** `leg/355-lcb3-v1`. Correction/record leg, floor-ineligible, difficulty light, no
preconditions.

## 1. Dispatch, verbatim gate

**Gate.**
* (i) applied and the registry regeneration re-run reproduces the banked artifact (alphas to
  float64 precision, per leg 335's baseline) — verified by actually re-running the regeneration
  after the fix and diffing against the banked JSON.
* (ii) applied and demonstrated per (a) missing-PDF-no-write and (b) normal-path-no-regression.
* ONE batched `writeup/CORRECTIONS.md` entry covering both.
* If BOTH succeed -> bank; accumulator items (3)-(4) close.
* If EITHER resists -> STOP for that item, report verbatim, land the other, do not widen scope.

**Answer: YES, both items.** Both premises were re-verified by this leg (not trusted from
dispatch), both fixes applied, both demonstrated per the gate's own requirement.

## 2. Item (i) — `spike1_stepC_gate` argv fix, applied and re-verified

### 2.1 The fix

`experiments/p2_route_bvrr_v1_repair.py`'s `BANKED` registry entry for `spike1_stepC_gate`:

```diff
- script="experiments/spike1_stepC_gate.py", argv=["--logged"], slow=True,
+ script="experiments/spike1_stepC_gate.py", argv=["--logged", "--steps", "2500"],
+ slow=True,
```

### 2.2 Verification 1 — the real harness (`rerun_one`, differential apparatus)

Ran the fixed registry entry through `experiments/p2_route_bvrr_v1_repair.py`'s own
`rerun_one(spec, pre_path)` (the per-call differential: pre-repair vs. post-repair
`odd_field_x_slope`, on the identical trajectory, backs up and restores the artifact itself).
Invoked directly (not via `main()`, to avoid clause (a)'s 81-case battery and the other three
slow `BANKED` entries, which are out of this leg's territory and not needed to answer this
gate):

```python
import experiments.p2_route_bvrr_v1_repair as bvrr
spec = [s for s in bvrr.BANKED if s["key"] == "spike1_stepC_gate"][0]
pre, n_lines = bvrr.load_pre_repair()
out = bvrr.rerun_one(spec, pre.__file__)
```

Result: `returncode=0`. `artifact_comparison`'s `leaves` list (spot-checked, full JSON in the
run's stdout) shows every compared field — `.runs[0].alpha`, `.runs[0].c_l`, `.runs[0].c_omega`,
`.runs[0].residual`, `.runs[0].anisotropy_p90`, `.runs[0].cut_omega[*]` — at `rel_diff` between
`0.0` and `~1e-13`, i.e. float64 rounding, not divergence. The banked artifact's sha256
(`dfe4433cdd54891e3e3bc45b915440b6cc7c573b715f1e1d9e7d1fce150718f4`, recorded before this run)
was confirmed **identical after** — `rerun_one` backs up and restores the file itself, and this
leg additionally re-checked the sha256 post-run.

Runtime note: this call runs the differential apparatus (both pre- and post-repair
implementations, at every call, on all four resolution rungs at `steps=2500`) and took
approximately 29 minutes wall-clock in this environment — substantially slower than a plain
regeneration because of the doubled call path, not because of any correctness issue.

### 2.3 Verification 2 — a direct, lighter re-run

To get a clean, fast, leaf-level comparison against leg 335's own quoted table (and as a second,
independent check not routed through the differential shim), ran the fixed argv directly:

```
cp writeup/data/spike1_stepC_gate.json /tmp/spike1_stepC_gate.json.banked_ref
PYTHONPATH=. .venv/bin/python experiments/spike1_stepC_gate.py --logged --steps 2500
```

Output (`PREDICATE: PARTIAL/FAIL`, matching the banked verdict exactly):

```
LOGGED gate: resolution study (renorm=True), targets c_l=3.0065 c_om=-1.0294 alpha=-0.3424
  n_r=300 r_max=1e+05: c_l=3.0637 c_om=-1.0266 alpha=-0.3351 a_far=-0.3226 aniso=0.026 res=1.67e-02
  n_r=450 r_max=1e+05: c_l=3.0642 c_om=-1.0233 alpha=-0.3340 a_far=-0.3114 aniso=0.027 res=7.71e-02
  n_r=600 r_max=1e+05: c_l=3.0622 c_om=-1.0312 alpha=-0.3368 a_far=-0.2992 aniso=0.027 res=2.67e-01
  n_r=450 r_max=1e+06: c_l=3.0617 c_om=-1.0293 alpha=-0.3362 a_far=-0.3146 aniso=0.026 res=1.16e-01

PREDICATE: PARTIAL/FAIL
    PASS  1_alpha_within_5pct
    FAIL  2_alpha_far_within_10pct
    PASS  3_anisotropy_below_0p23
    PASS  4_resolution_stable_alpha
```

Diffed against `/tmp/spike1_stepC_gate.json.banked_ref` (the pre-run copy of the banked file)
programmatically:

| run | config | banked `alpha` | this leg's re-run `alpha` | `rel_diff` |
|---|---|---|---|---|
| 0 | `n_r=300, r_max=1e5` | `-0.3350763095343765` | `-0.33507630953437806` | `4.638681369730669e-15` |
| 1 | `n_r=450, r_max=1e5` | `-0.33396053473198634` | `-0.33396053473198667` | `9.973241528519034e-16` |
| 2 | `n_r=600, r_max=1e5` | `-0.3367555909794855` | `-0.3367555909794855` | `0.0` (exact) |
| 3 | `n_r=450, r_max=1e6` | `-0.33619163643975625` | `-0.33619163643975647` | `6.604703414887614e-16` |

`predicate_checks` and `predicate_pass` reproduce exactly (both `False` on
`2_alpha_far_within_10pct` only). **These are the identical rel_diffs leg 335 reported in its
own §4 table** — an independent re-derivation, not a copy, landing on the same digit.

`writeup/data/spike1_stepC_gate.json` was copied back from `/tmp/spike1_stepC_gate.json.banked_ref`
immediately after the diff. Confirmed: `sha256sum` matches `dfe4433c…` both before and after;
`git status --porcelain` / `git diff --stat` show no change to the file at any point in the
final commit.

### 2.4 Disposition

**CORRECTED.** `experiments/p2_route_bvrr_v1_repair.py`'s registry entry now reproduces
`writeup/data/spike1_stepC_gate.json` to float64 precision on regeneration, verified two
independent ways.

## 3. Item (ii) — CADX missing-PDF write guard

### 3.1 The hazard, reproduced live before fixing

At leg start, `Papers/2505.03091.pdf` is absent (gitignored, not fetched by default). Ran the
unmodified script:

```
sha256sum writeup/data/p2_route_cadx_v1.json
# 75fab364a3b79ecc58129563d12e734810fb10cb54409760cb5d7025e9758e1d
PYTHONPATH=. .venv/bin/python experiments/p2_route_cadx_v1_scope.py
# [CADX] gate: yes ...  quote check: NOT_AVAILABLE ...
# EXIT 0, and the banked JSON is overwritten
```

Confirmed this is the exact mechanism legs 322 and 327 hit: the script computes a full,
gate-answering payload even with the quote-check degraded, and writes it over the banked,
`VERIFIED`-quote-checked artifact without complaint.

### 3.2 The fix

Added a guard at the top of `main()`, before any computation, in
`experiments/p2_route_cadx_v1_scope.py`:

```python
def main():
    t0 = time.time()
    if not PDF.is_file():
        sys.exit(
            "[CADX] ABORTING, NOT WRITING %s: %s is absent.  Papers/ is gitignored on "
            "purpose; re-fetch with `bash Papers/fetch.sh 2505.03091` and re-run to "
            "regenerate the artifact for real.  Refusing to overwrite the banked, "
            "quote-verified JSON with a diminished (NOT_AVAILABLE quote-check) rerun."
            % (OUT.relative_to(ROOT), PDF)
        )
    res = { ... }
```

### 3.3 Demonstration (a) — PDF absent, no write

```
sha256sum writeup/data/p2_route_cadx_v1.json   # before: 75fab364...758e1d
PYTHONPATH=. .venv/bin/python experiments/p2_route_cadx_v1_scope.py
# [CADX] ABORTING, NOT WRITING writeup/data/p2_route_cadx_v1.json: .../Papers/2505.03091.pdf
#   is absent.  ...
# exit code: 1
sha256sum writeup/data/p2_route_cadx_v1.json   # after:  75fab364...758e1d  (unchanged)
git diff --stat writeup/data/p2_route_cadx_v1.json   # empty
```

Hash unchanged, exit code nonzero, `git diff` empty. The hazard is closed on this path.

### 3.4 Demonstration (b) — PDF present, no regression

Fetched the paper for a real run: `bash Papers/fetch.sh 2505.03091` -> egress `HTTP 200`,
`1004K`, sha256 `0f1bc6181ce0d4375df3a012846d851c366cfc768b73fbfa3e2f7b6631f8081d` — matches the
script's own `PDF_SHA256` constant exactly. Copied the banked JSON and figure aside, then ran
the guarded script:

```
PYTHONPATH=. .venv/bin/python experiments/p2_route_cadx_v1_scope.py
# [CADX] gate: yes (i) -- the hypotheses exclude it
# [CADX] quote check: VERIFIED (11/11 probes, sha256 match True)
# [CADX] wrote writeup/data/p2_route_cadx_v1.json and writeup/figures/fig67_route_cadx_v1_zero_diagonal.png
# exit code: 0
```

Programmatic diff of the new output against the pre-change reference, `elapsed_s` excluded:

```python
a = strip(json.load(open("/tmp/cadx_before_normal_run.json")))
b = strip(json.load(open("writeup/data/p2_route_cadx_v1.json")))
a == b   # True
```

`elapsed_s`: before `6.138346195220947`, after `1.1012506484985352` — both real run times, not
a claim-bearing field, excluded from the equality check on that basis alone.
`writeup/figures/fig67_route_cadx_v1_zero_diagonal.png`: `cmp` reports **byte-identical**.

Both files were then restored from the pre-run reference copies. Confirmed:
`writeup/data/p2_route_cadx_v1.json` sha256 `75fab364…758e1d` (unchanged),
`writeup/figures/fig67_route_cadx_v1_zero_diagonal.png` sha256
`a7ee71c9fe39640eefe658b50a0f77f0bd683bd66590a5c3c4ae57466f07e6fb` (unchanged), `git diff`
empty on both.

### 3.5 Disposition

**CORRECTED.** The guard fails loudly and writes nothing when the PDF is absent; the normal
(PDF-present) path is unmodified in its output, byte-for-byte except the wall-clock field.

## 4. The batched CORRECTIONS entry

`writeup/CORRECTIONS.md` §24 appended (pure append; §1-§23 untouched). Records both fixes, both
verification methods for (i), both demonstrations for (ii), and the ceiling (0 numbers
re-derived beyond independent reproduction, 2 sites edited, 1 batched entry, 0 gate answers
changed, 0 bans touched).

## 5. Diff-check (gate's own requirement)

```
$ git diff --stat
 experiments/p2_route_bvrr_v1_repair.py |  3 ++-
 experiments/p2_route_cadx_v1_scope.py  | 14 ++++++++++++++
 writeup/CORRECTIONS.md                 | ~100 +++++++++++++++...
```

`writeup/data/spike1_stepC_gate.json` and `writeup/data/p2_route_cadx_v1.json`: zero diff, sha256
unchanged from merge base, confirmed both before adding this leg's own journal/novelty files and
again immediately before commit. Every changed line in the two `.py` files sits inside the one
named site each (the `argv` list, the `main()` guard); no argument, gate text, gate answer, or
banked number touched beyond what §24 records.

## 6. Territory and discipline followed

Only the files named in the leg spec's territory clause were touched:
`experiments/p2_route_bvrr_v1_repair.py` (one `argv` list), `experiments/p2_route_cadx_v1_scope.py`
(one guard in `main()`), `writeup/CORRECTIONS.md` (one append), plus this leg's own pair
(`writeup/novelty/leg_355.md`, `experiments/journal/leg_355.md`). `plan_of_record.py`,
`DIRECTION.md`, `CLAY_ROADMAP.md` not opened for editing. No BANNED item in `plan_of_record.py`
lifted, weakened, or reinterpreted. Both banked JSONs (`spike1_stepC_gate.json`,
`p2_route_cadx_v1.json`) and the CADX figure were opened, written to during verification runs,
and restored to byte-identical state before commit — never left modified.

## 7. Runtime

Item (i) verification: ~29 minutes (differential harness, `rerun_one`) + ~9 minutes (direct
re-run at `--steps 2500` across four resolution rungs) — genuine solver compute, not stalled;
confirmed by monitored, increasing CPU time throughout. Item (ii): two short runs (PDF-absent
abort, PDF-present full run) plus one `Papers/fetch.sh` egress call. No solver code modified.

## 8. Walls, restated

* Wall 1/Wall 2 stand. Fixing a regeneration-harness argv bug and guarding a script against a
  known accidental-overwrite hazard does not discharge any obligation. No link of the
  `L1 -> L4` chain moves.
* Clay odds ~0.05%, unchanged.
