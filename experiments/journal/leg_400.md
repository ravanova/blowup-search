# Leg 400 — `L5`: W4 clause (b), the natively finite-energy ansatz

**Unit `L5`, Lane L, leg 400. Branch `leg/400-l5-finite-energy`. CONSTRUCTION + measurement.**
Dispatched FIRST in wave 5. No ban lifted, narrowed, re-read or argued against. `STATE.md`,
`WALLS.md`, `OPTIONS.md`, `ORCHESTRATION.md`, `reports/`, `writeup/INDEX.md`,
`writeup/build_figures.py` and every other unit's banked JSON are **outside this unit's territory**
and are not touched; anything owed to them is written in §8 as a flag to the Conductor and applied
nowhere.

**Territory:** `experiments/journal/leg_400.md`, `experiments/p2_route_l5_*.py`,
`writeup/data/p2_route_l5_*.json`.

---

## §0. PRE-REGISTRATION — written and committed BEFORE any result exists

Everything in §§0–3 is committed to this branch **before** the ansatz was coded, before any residual
was computed and before any gate answer existed. The classification rules in §3 are fixed here so
they cannot be tuned to whatever the numbers turned out to be.

### §0.1 THE GATE — final wording, committed to `main` at `1e49a00` BEFORE this unit existed. NOT RE-SCOPED.

> Construct the natively finite-energy ansatz explicitly — a **modulated, localised** ansatz in
> which the spatial cutoff and the scaling modulation are part of the ansatz rather than applied
> afterwards — derive its profile system including **every extra term the modulation and the cutoff
> generate**, and then MEASURE, on route 4's **banked** discrete profile: **is there a cutoff radius
> `ρ` and a named norm in which the total localisation-plus-modulation error is SMALLER than the
> closure threshold it must beat — YES or NO, with the number?** This is the **analogue for clause
> (b) of the bill leg 381 banked for clause (a)**, and the answer must be a number, not a verdict.

### §0.2 THE PRE-COMMITTED READINGS — restated VERBATIM from `writeup/waves/WAVE5_PLAN.md` @ `1e49a00`

> **(a)** A **YES** is a **LEAD, NOT A BROKEN WALL.** It would be a statement about a *discrete,
> banked* profile in a *named* norm — **not a certified bound and not a blow-up** — and it must be
> reported with what certification would cost (§3d) before anything else is said about it.
> **(b)** A **NO with a number** is a **real, landable result** and **must not be softened**: it
> prices clause (b) the way leg 381 priced clause (a), and together with `L2′` it would say the
> localisation problem is priced on **both** of its live clauses for the first time.
> **(c)** **THE MOST LIKELY OUTCOME IS THAT THE ANSATZ IS EXACTLY (D)SS IN DISGUISE.** If the
> constructed ansatz reduces to an exactly-(D)SS object under any change of variables, **SAY SO,
> STOP, AND CALL IT A `NO`** — the pin then applies to it and clause (b) is shut too. **Do not paper
> over this**; it is the single most valuable thing this unit can discover, and finding it early is
> a success, not a failure.
> **(d)** **Every number comes from the ARTEFACT, never from prose** — route 4's banked profile and
> `writeup/data/p2_route_cloc_v1.json` (`self_hash 58c57b62c0cbc80d`). Re-derive; do not quote
> `STATE.md`.
> **(e)** **C1 BINDS AND IS NOT WAIVABLE BY THE CONDUCTOR.** If this unit reaches for a
> `Y₀/Z₀/Z₁/Z₂` radii-polynomial contraction **in any space** it is inside the ban unless it, **in
> its own pre-registration**, (1) names its apparatus with a citation and (2) shows that apparatus
> does not construct a single bounded approximate inverse uniform in `M`. **C1 may not be cited as
> evidence that any apparatus closes for any object class.**
> **(f)** **Lesson 91:** a negative names its **realization, trial space and basis**. A number
> without them is not a measurement.
> **(g)** §3d: **`UNDER-RESOURCED` with a cost, never a bare `no`.**
> **(h)** **A ban is superseded by a MEASUREMENT, never by a decision.** If a ban's *wording*
> obstructs, that is a **user escalation** — record it, do not rule it, and do not stop.
> **(i)** **No learned or evolved fitness (leg 349). No proposals for more seed supply. READ, do not
> CONTACT** — reading published material is authorised, contacting any author group is not.

### §0.3 C1 PRE-REGISTRATION (reading (e)), written before any apparatus was reached for

This unit **constructs no certificate and no contraction**. It computes the residual of an explicit
ansatz and measures its size. Where a *threshold* is needed to answer "smaller than what?", the
threshold used is the **summability requirement of the Duhamel/mild formulation** in the critical
space — Fujita–Kato (Arch. Rational Mech. Anal. 16 (1964) 269–315) and Kato, *Strong L^p solutions
of the Navier–Stokes equation in R^m*, Math. Z. **187** (1984) 471–480: for the perturbation
`ε = u − u_ansatz` with `ε(0) = 0`, `ε = −∫ e^{(t−τ)Δ} P F dτ + (bilinear)`, any Banach fixed point
in a critical space requires **at minimum** `‖F‖_{L¹_t L³_x} < ∞` (velocity form) / `‖curl F‖_{L¹_t
L^{3/2}_x} < ∞` (vorticity form, pressure-free).

**(1) The apparatus is named with a citation:** Fujita–Kato / Kato mild-solution Duhamel bound, as
above. **(2) It does not construct a bounded approximate inverse uniform in `M`:** the operator
inverted is the **exact** heat semigroup `e^{tΔ}` on `ℝ³`, not a numerically-constructed approximate
inverse of a truncated operator; **there is no truncation dimension `M` in the argument at all**, and
no `Y₀/Z₀/Z₁/Z₂` polynomial is formed in any space. **C1 is therefore not engaged, and C1 is not
cited here as evidence that anything closes for any object class.**

Moreover the gate answer below is **made independent of the value of any closure constant**: it is
stated for **every** positive threshold `ε_close > 0` (see §3.3). No numerical value of a bilinear
constant is used, claimed, or needed.

### §0.4 WHAT THIS UNIT MAY NOT DO

- It may not amend `STATE.md`, `WALLS.md`, `OPTIONS.md`, `DIRECTION.md`, `ORCHESTRATION.md`,
  `plan_of_record.py`, `reports/*`, `CLAY_ROADMAP.md`, `CLAY_OBLIGATIONS.md`, `writeup/INDEX.md`,
  `writeup/build_figures.py`, or any other unit's banked JSON. Corrections owed to any of them go to
  §8 verbatim and are applied nowhere.
- It may not quote `STATE.md` as the source of a number (reading (d)).
- It may not rule a ban, and it may not run a learned or evolved fitness function (leg 349).
- It may not contact any author, group, maintainer or list. This unit sends nothing; it reads only
  what is already in the repository plus published mathematics already banked by legs 253/260/381/397.
- **Scale is not evidence. Tier 2 is never a proof.** No output of this unit is movement toward Clay
  unless a link of the `L1→L4` chain actually moved, and §7 says plainly whether one did.

---

## §1. THE OBJECT, AND THE ONE PLACE THE GATE'S WORDING MEETS THE RECORD

**The object (lesson 91, named in full).** A **backward discretely-self-similar** blow-up profile `U`
for **3D incompressible Navier–Stokes** on **ℝ³**, non-axisymmetric, at ratio `λ = 1.7`, realised as
a periodic orbit of period `2 log λ = 1.0612565021243408` in the similarity variable `s`. Its far
field is `|U(y)| ≲ |y|^{−α}` with **`α = 1` exactly** — pinned from above and below by leg 397
(`L2′`, `1493e5e`): `α ≥ 1` by Chae–Wolf `arXiv:1610.09464` Thm 1.1, `α ≤ 1` by Chae–Wolf Rmk 1.2 +
Escauriaza–Seregin–Šverák, and any `α > 1` puts `U ∈ L³(ℝ³)` and yields **full regularity**.

**THE ONE PLACE THE GATE'S WORDING MEETS THE RECORD, DECLARED BEFORE ANY MEASUREMENT.** The gate says
"on route 4's **banked** discrete profile". **Route 4 has no banked profile.** This is banked, not
inferred: `experiments/journal/leg_382.md` line 174 — *"that any profile of route 4's object exists
(**none does**)"* — and `experiments/journal/leg_397.md` §1 — *"no profile of route 4's object exists
in this repository"*. The nearest banked discrete objects are

1. the **synthetic exactly-DSS, exactly-divergence-free poloidal field** of leg 381
   (`experiments/p2_route_cloc_v1.py`, banked in `writeup/data/p2_route_cloc_v1.json`,
   `self_hash 58c57b62c0cbc80d`), whose DSS symmetry is checked to `1.14e−15` and whose far field is
   `|U| ~ r^{−α}` by construction; and
2. the `PROG-R4` **2-D Kolmogorov-flow RPO** surrogates (`p2_prog_r4_*_v1.json`), which are not
   3-D NS DSS profiles at all.

**This unit therefore measures on (1), and says so everywhere.** That is not a workaround, and it is
declared in advance rather than discovered: **the quantity this unit measures is a COMMUTATOR** —
the error that the localisation and the modulation introduce *over and above whatever residual the
profile itself has* — and a commutator is well defined for **any** field, solution or not. It depends
on the profile only through the far-field data `(α, the angular structure, the modulation amplitude)`,
and `α = 1` and `λ = 1.7` are banked. The parts of the answer that are **structural** (the ρ-exponent,
the SS/DSS dichotomy) are therefore properties of the *class*; the parts that are **realization
constants** are properties of field (1) and are reported as such, with an `UNDER-RESOURCED` cost for
the profile-specific constant (§3.4). **Lesson 91 is discharged by naming this in §0, not in §6.**

**The bill leg 381 banked for clause (a)**, re-derived mechanically from
`writeup/data/p2_route_cloc_v1.json` by this unit's own evidence script (never from prose):
`L2_threshold_alpha = 1.5`, `L3_threshold_alpha = 1.0`, `banked_type_I_alpha = 1.0`,
`deficit_to_L2_in_exponent = 0.5`, `required_over_available_exponent_ratio = 1.5`,
`check_C2….increment_per_decade ≈ 326.875` per decade of window.

---

## §2. THE CONSTRUCTION — the ansatz, written down before it is measured

### §2.1 Similarity variables with the modulation carried

`λ(t) > 0` is the modulation scale, `y = x/λ(t)`, `ds/dt = λ^{−2}`, `u(x,t) = λ^{−1} V(y,s)`, and
`a(s) := −λ λ_t` is the **modulation parameter** (`a ≡ 1/2` ⟺ `λ = √(T*−t)`, the (D)SS normalisation).
Then, identically,

    R[V] := V_s + a(s)(V + y·∇V) − ΔV + V·∇V + ∇P = 0     ⟺     u solves NS,     div V = 0.

### §2.2 The ansatz `A(ρ₀, κ, α, λ)` — cutoff and modulation INSIDE the ansatz

The profile is poloidal, `U(y,s) = m₁(s) 𝒫[ψ_α](y;c) + m₂(s) 𝒫[ψ_α](y;d)` with
`𝒫[f](y;e) = curl curl (f(|y|) e) = (f″ − f′/r)(ŷ·e)ŷ − (f″ + f′/r) e`, `ψ_α(r) = (1+r²)^{(2−α)/2}`,
`c ⟂ d` (non-axisymmetric), and `m₁, m₂` `2 log λ`-periodic in `s` — leg 381's banked realization.

**The ansatz cuts off the POTENTIAL, not the field**, so that it is **exactly divergence-free by
construction and needs no Bogovskiĭ corrector at all**:

    V(y,s) := m₁(s) 𝒫[g(·,s)](y;c) + m₂(s) 𝒫[g(·,s)](y;d),
    g(r,s) := χ(r/ρ(s)) ψ_α(r),        ρ(s) = ρ₀ e^{κ s},

with `χ ≡ 1` on `[0,1]`, `χ ≡ 0` on `[2,∞)`, `χ` a degree-9 (`C⁴`) smoothstep on `[1,2]`. The
**cutoff radius `ρ(s)` and its rate `κ` are part of the ansatz**, as is the modulation `a(s)`. In
physical space the cutoff radius is `R(t) = λ(s) ρ(s) = R₀ e^{(κ−a)s}`.

**The branches, fixed here:**
- `κ = 0` — cutoff frozen in **similarity** variables. Then `V(y, s+2logλ) = V(y,s)`: the ansatz is
  **exactly DSS**. Reading (c)'s branch.
- `κ = a` — cutoff frozen in **physical** space, `R(t) ≡ R₀`. The natively finite-energy branch.
- `0 < κ < a` — intermediate.
- `κ > a` — `R(t) → ∞`; energy unbounded in `t` (checked in §3.5), so **Clay condition (7) forbids
  it**. Recorded, not pursued.

### §2.3 The profile system of the ansatz — EVERY extra term the modulation and the cutoff generate

With `w := V − χU` (the potential-cutoff commutator, `w ≠ 0` only on the annulus `ρ ≤ r ≤ 2ρ`), the
**total localisation-plus-modulation error** is the commutator

    R_loc := R[V] − χ R[U]
           = T1 + T2 + T3 + T4 + T5 + T6 + T7,

    T1  (cutoff drift)          =  the `∂_sχ` part of `V_s − χU_s`,  `∝ κ`
    T2  (modulation transport)  =  a[(V + y·∇V) − χ(U + y·∇U)],      `∝ a`
    T3  (MODULATION COMMUTATOR) =  the `ṁ` part of `V_s − χU_s`,     `∝ ṁ`, ZERO iff the profile is
                                                                     exactly self-similar
    T4  (viscous commutator)    =  −[ΔV − χΔU]
    T5  (nonlinear commutator)  =  V·∇V − χ(U·∇U)
    T6  (pressure)              =  ∇P_V − χ∇P_U   — removed exactly by taking `curl`
    T7  (divergence corrector)  =  ≡ 0 by construction here — the potential cutoff is div-free; the
                                   field-cutoff variant needs a Bogovskiĭ corrector, which leg 381
                                   measured GROWING at `ρ^{+0.5004}` in `L²`

**Predicted magnitudes on the annulus (`|U| ~ ρ^{−α}`, `|∇χ| ~ ρ^{−1}`), written before measuring:**
`T1, T2 ~ (κ − a)·ρ^{−α}` (so they **cancel identically at `κ = a`**), `T3 ~ |ṁ|·ρ^{−α}`,
`T4 ~ ρ^{−α−2}`, `T5 ~ ρ^{−2α−1}`. In the scale-invariant norms of §3.1 that is
`‖T1+T2‖ ~ |κ−a| ρ^{1−α}`, `‖T3‖ ~ |ṁ| ρ^{1−α}`, `‖T4‖ ~ ρ^{−α−1}`, `‖T5‖ ~ ρ^{−2α}`.
**The modulation term dominates the cutoff terms by a factor `ρ²`.**

---

## §3. CRITERIA — fixed here, applied mechanically

### §3.1 The named norms (lesson 91: realization, trial space, basis)

- **Velocity form.** `‖F‖_{L¹_t L³_x}`, the scale-invariant forcing norm dual to the critical space
  `L^∞_t L³_x` under Duhamel (§0.3). In similarity variables it is **exactly**
  `∫ ‖R(·,s)‖_{L³_y} ds`, because `‖F(t)‖_{L³_x} = λ^{−2}‖R(·,s)‖_{L³_y}` and `dt = λ² ds`.
- **Vorticity form (LOAD-BEARING, because it is PRESSURE-FREE).**
  `‖curl F‖_{L¹_t L^{3/2}_x} = ∫ ‖curl_y R(·,s)‖_{L^{3/2}_y} ds`. `L^{3/2}` is the critical Lebesgue
  space for 3-D vorticity (`ω ∈ L^{3/2} ⟺ u ∈ L³`), and taking `curl` annihilates `T6` identically,
  so **no pressure term, no Leray projector, and no operator-norm constant enters the load-bearing
  number**.
- **Realization:** leg 381's synthetic poloidal exactly-DSS field, `α ∈ {1.0, 1.25, 1.6}`, `λ = 1.7`,
  `m₁ = 1 + 0.35 cos ωs`, `m₂ = 0.6 + 0.25 sin(ωs + 0.7)`, `ω = π/log λ`, `c = e_z`, `d = e_x`.
  **Trial space / basis:** product Gauss–Legendre quadrature on geometric radial shells × Gauss in
  `cos θ` × uniform in `φ`; the cutoff basis is the degree-9 `C⁴` smoothstep on
  `[ρ, 2ρ]` (control `C6` repeats with leg 381's quintic `C²` smoothstep). Derivatives of `g` are
  analytic; `curl R_loc` is 4th-order central finite differences with a refinement control.

### §3.2 What is measured

For each `(α, κ, mode ∈ {DSS, SS}, ρ₀)` and for `s` on a uniform grid over **one whole DSS period**:
`‖R_loc(·,s)‖_{L³_y}`, `‖curl R_loc(·,s)‖_{L^{3/2}_y}`, per-term `T1..T5` contributions, and the
period-averaged values. Then the **ρ-exponent** of each, by least squares over
`ρ₀ ∈ {10, 30, 100, 300, 1000}`, against the predictions of §2.3.

### §3.3 THE CLOSURE THRESHOLD, and what counts as YES / NO — fixed before any number exists

A backward-DSS blow-up requires the construction to run over **`s ∈ [s₀, ∞)`, i.e. infinitely many
DSS periods**. The **closure threshold** is therefore, for any positive `ε_close`:

    Σ(S) := ∫_{s₀}^{s₀+S} ‖curl_y R_loc(·,s)‖_{L^{3/2}_y} ds   must satisfy   Σ(∞) < ε_close.

- **`YES`** — there exist `ρ₀` and one of the named norms with `Σ(∞) < ε_close` for some fixed
  `ε_close > 0`, i.e. the per-period accrual `c_mod` **decays with `ρ₀`** and is **summable in `s`**.
  A `YES` is reported under reading (a) as a **LEAD, NOT A BROKEN WALL**, with the certification cost.
- **`NO`** — `Σ(∞) = ∞` for every `ρ₀`, i.e. the per-period accrual is bounded below uniformly in
  `ρ₀`. Then the number reported is `c_mod` **per unit `s`** and per DSS period, the measured
  ρ-exponent with its residual, and `N(ε) = ε/(c_mod · 2 log λ)` — the number of DSS periods any
  threshold `ε` can afford. **This form of `NO` is threshold-free: it holds for every `ε_close > 0`
  and uses no closure constant.**
- **Reading (c) fires** if the ansatz that achieves a small error is **exactly (D)SS** — checked
  mechanically by testing `V(y, s + 2 log λ) = V(y, s)` to `1e−12` relative, exactly as leg 381
  checks its own field. If the `κ = 0` branch is the only cheap one, the answer is **`NO`** and this
  unit says so and stops.
- **Falsifier that can flip `NO` → `YES` (planted, must be reported either way).** The ansatz is
  *modulated*: the parameters `(a, κ)` are free functions of `s`, and a modulated construction is
  entitled to spend them absorbing the residual. Control `C7` minimises
  `‖R_loc − δa·(V + y·∇V) − δκ·∂_κ V‖` over `(δa, δκ)` **on all of ℝ³, core included**, and reports
  the residual fraction and its ρ-exponent after optimal absorption. **If optimal absorption drives
  the ρ-exponent strictly negative, the gate answer is `YES`.**

### §3.4 `UNDER-RESOURCED` rule (§3d)

The **exponent** measurements are properties of the ansatz class and of `α`. The **constants** are
properties of leg 381's synthetic realization, because **route 4 has no banked profile** (§1). Any
part of the answer that needs the true profile's modulation amplitude is banked
`UNDER-RESOURCED` **with a cost**, never as a bare `no`, and never as a zero.

### §3.5 CONTROLS — planted before the run, each able to fire in BOTH directions

- **`C1` reproduction (instrument validity).** Re-implement leg 381's `cutoff_magnitudes` from
  scratch and reproduce its **banked** ρ-exponents at `α = 1.0` and `α = 1.25`
  (`nonlinear_residual_L2 → −1.5`, `viscous_residual_L2 → −1.5`, `divergence_defect_L2 → −0.5`,
  `bogovskii_corrector_L2_scale → +0.5`, `pressure_perturbation_at_origin → −2.0`,
  `tail_L3_norm → 0.0`) to `< 1e−2` absolute. **If this fails, every number in this leg is void.**
- **`C2` negative control (must return machine zero).** With `χ ≡ 1` (no cutoff) `R_loc ≡ 0` to
  `< 1e−12` relative. A non-zero means the commutator bookkeeping is wrong and the leg is void.
- **`C3` SS control (the decisive one, fires both ways).** With `m₁, m₂` **constant** (exactly
  self-similar) the modulation term `T3` must vanish and the total ρ-exponent at `α = 1`
  must drop from `≈ 0` to `≈ −2`. **If SS also gives exponent `≈ 0`, the attribution of the
  obstruction to the modulation is WRONG and must be reported as such.**
- **`C4` κ control.** `κ = a` must cancel `T1 + T2` to quadrature precision; `κ = 0` must not.
  The `κ = 0` branch must additionally pass the exact-DSS symmetry test of §3.3 (reading (c)).
- **`C5` α control.** Measured ρ-exponent must track `1 − α` across `α ∈ {1.0, 1.25, 1.6}`.
  This is the clause-(b) analogue of leg 381's `α` bill and is where the two clauses are compared.
- **`C6` basis control.** Quintic `C²` vs degree-9 `C⁴` smoothstep: exponents must agree to `1e−2`.
- **`C7` modulation-absorption falsifier.** §3.3. Reported whether or not it fires.
- **`C8` energy control (Clay condition (7)).** `E(t) = λ ∫|V|² dy` must be **bounded uniformly in
  `t`** for `κ = a` at `α = 1` (this is what "natively finite energy" means) and **unbounded** for
  `κ > a`. Both directions required.
- **`C9` quadrature/FD refinement.** Norms stable under refinement of the angular and radial orders
  and halving of the FD step; the achieved relative stability is banked, not asserted.

### §3.6 Checkpointing (process rule, paid for three times already)

A host-process exit on 2026-08-14 took five in-flight workers and wave 3 lost three of four the same
way; the pre-registrations survived because they were committed. This leg commits: **(0)** this
pre-registration, before any code; **(1)** the ansatz script + the `C1`/`C2` control run;
**(2)** the full sweep artefact; **(3)** the gate sections. Resumable granularity: the sweep writes
`writeup/data/p2_route_l5_finite_energy_v1.json` in one pass from cached per-`(α,κ,mode,ρ)` rows, and
a successor resumes by re-running one script.

---

## §4. WHAT WAS BUILT — the ansatz, and the derivation of its profile system

### §4.1 The ansatz, in one line

    V(y,s) = m₁(s)·P[g(·,s)](y; c) + m₂(s)·P[g(·,s)](y; d),   g(r,s) = χ(r/ρ(s))·ψ_α(r),
    ρ(s) = ρ₀ e^{κs},   P[f](y;e) := curl curl (f(|y|) e),   ψ_α(r) = (1+r²)^{(2−α)/2}.

Three things are **inside** the ansatz, not applied to it afterwards:

1. **the spatial cutoff**, through `χ(r/ρ(s))`;
2. **the scaling modulation**, through `a(s) := −λλ_t` and through the cutoff's own rate `κ`;
3. **the profile modulation**, through `m₁(s), m₂(s)`, which carry the `2 log λ`-periodic part that
   makes the object *discretely* rather than exactly self-similar.

**The one design decision that does real work: the cutoff acts on the POTENTIAL, not on the field.**
Because `P[f] = curl curl(f e)` is a double curl, `div V ≡ 0` *identically* — for every `χ`, every
`ρ`, every `s`. There is **no divergence defect and therefore no Bogovskii corrector.** This
deletes, by construction, the single worst object leg 381 banked for clause (a): the corrector whose
`L²` size it measured **GROWING** at `ρ^{+0.5004160}`
(`p2_route_cloc_v1.json`, `check_C_cutoff_magnitudes.by_alpha["alpha=1.0"].measured_rho_exponents.
bogovskii_corrector_L2_scale`, re-derived independently here as control `C1`). Term `T7` below is
identically zero, and that is not an assumption — control `C2` measures it.

### §4.2 The three κ-branches, and why only one of them is this unit's object

`κ` is the rate at which the cutoff radius moves in **similarity** space. In **physical** space the
cutoff sits at `R(t) = λ(s)ρ(s) = R₀ e^{(κ−a)s}`, so the energy `E(t) = λ ∫|V|² dy` grows like
`e^{(κ−a)s}`.

| branch | physical cutoff | measured `β` in `E ~ e^{βs}` | predicted `κ−a` | `E_last/E_first` | status |
|---|---|---|---|---|---|
| `κ = 0` | shrinks like `e^{−as}` | `-0.500000` | `-0.50` | `0.04143` | **exactly (D)SS** — reading (c) |
| `κ = a` | **frozen**, `R ≡ R₀` | `-0.000015` | `+0.00` | `0.9998` | **THIS UNIT'S OBJECT** |
| `κ = 3a/2` | grows | `+0.249992` | `+0.25` | `4.912` | forbidden by Clay condition (7) |

Control `C8` samples `E` at `s = j·2 log λ`, where the modulation is at an identical phase at every
sample, so `β` isolates `κ` alone; `β = κ − a` is recovered to `1.46e-05` across all three branches.

**"Natively finite energy" is therefore not a slogan here — it is `β = 0`, i.e. Clay condition (7)
verbatim, and it FORCES `κ = a`.** That is the whole content of "the cutoff is part of the ansatz".

### §4.3 The profile system of the ansatz — EVERY term the modulation and the cutoff generate

Write `U := m₁P[ψ_α](·;c) + m₂P[ψ_α](·;d)` for the un-cut profile and
`R[W] := W_s + a(W + y·∇W) − ΔW + W·∇W + ∇P_W`. The gate's quantity is the
**localisation-plus-modulation error**, i.e. the commutator

    R_loc := R[V] − χ·R[U] = T1 + T2 + T3 + T4 + T5 + T6 + T7.

| term | name | closed form | measured `ρ`-exponent (α=1, κ=a, tail-3) | value at `ρ₀=1000` |
|---|---|---|---|---|
| `T1` | cutoff drift | `Σ_k m_k P[∂_s g] = −κ Σ_k m_k P[r χ_r ψ]` | `-0.0000` | `179.702` |
| `T2` | modulation transport | `a[(V + y·∇V) − χ(U + y·∇U)]` | `-0.0000` | `179.702` |
| `T3` | **modulation commutator** | `Σ_k ṁ_k (P[χψ] − χP[ψ])` | `-0.0000` | `46.7697` |
| `T4` | viscous commutator | `−(ΔV − χΔU)` | `-2.0000` | `0.00153232` |
| `T5` | nonlinear commutator | `V·∇V − χ(U·∇U)` | `-2.0000` | `0.000405896` |
| `T6` | pressure | `∇P_V − χ∇P_U` | **annihilated by curl**; residue `∇χ×∇P_U ~ ρ^{−2α}` | — |
| `T7` | divergence corrector | `≡ 0` by construction (§4.1) | — | `0` |

`T1` comes from `∂_s g = ∂_s[χ(r/ρ(s))]ψ = −κ r χ_r ψ` — the chain rule *through* `ρ(s)`, which is
exactly the term a post-hoc cutoff never generates, because a post-hoc cutoff has no `s`. `T3` comes
from the failure of `P[·]` to commute with multiplication by `χ` — again a term that exists only
because the modulation is inside the ansatz.

**The pressure is handled by taking the curl, not by estimating it.** `curl ∇P ≡ 0`, so the
load-bearing norm `‖curl F‖_{L¹_t L^{3/2}_x}` is *exactly* pressure-free. The one surviving piece,
`∇χ × ∇P_U`, is `O(ρ^{−2α})` — subleading to the `ρ^{1−α}` terms by `ρ^{−α−1}` — and leg 381's
banked `pressure_perturbation_at_origin` exponent `-1.9996966` at `α = 1` (re-derived here as `-1.999697`)
corroborates the `ρ^{−2α}` rate independently. Measured, `T4` and `T5` come in at exponent `-2.0000`
and `-2.0000`, i.e. `ρ^{−2}` at `α = 1`, exactly as §2.3 predicted before the run.

---

## §5. WHAT WAS MEASURED

Realization / trial space / basis (lesson 91) are banked in
`writeup/data/p2_route_l5_finite_energy_v1.json` under `realization_lesson_91`: leg 381's synthetic
exactly-DSS poloidal field; product Gauss–Legendre (`n_theta=16`, `n_phi=32`, `6` radial panels ×
`n_gl=14`) on the annulus `ρ ≤ |y| ≤ 2ρ` where `R_loc` is exactly supported; degree-9 `C⁴` smoothstep
cutoff basis; `6` similarity times per DSS period; `ρ₀ ∈ {10, 30, 100, 300, 1000}`;
`α ∈ {1.0, 1.25, 1.6}`; `κ ∈ {0, a/2, a}`; `mode ∈ {DSS, SS}`. `18` configurations, `1529` s.

### §5.1 The decomposition at `α = 1`, `DSS` — where the error actually lives

`L³` norms at `ρ₀ = 1000`, period-averaged:

| `κ` | `T1` | `T2` | `‖T1+T2‖` | `T3` | `T4` | `T5` | **total** | `|T1+T2|/|T1|` | **total/`T3`** |
|---|---|---|---|---|---|---|---|---|---|
| `0` | `0` | `179.702` | `179.7` | `46.7697` | `0.002178` | `0.0005523` | `186.029` | n/a (`T1 ≡ 0`) | `3.977543` |
| `a/2` | `89.8511` | `179.702` | `89.85` | `46.7697` | `0.001805` | `0.0004677` | `101.808` | `1.000e+00` | `2.176799` |
| **`a`** | `179.702` | `179.702` | `4.633e-06` | `46.7697` | `0.001532` | `0.0004059` | `46.7696` | `2.578e-08` | `0.999998` |

**Read the `κ = a` row.** `T1` and `T2` — each of size `179.702` — cancel to `4.633e-06`, a relative
cancellation of `2.578e-08`. `T4` and `T5` are `3.28e-05` and `8.68e-06` of the total. What is left is
`T3` **and nothing else**: `‖R_loc‖ / ‖T3‖ = 0.999998`.

This cancellation is not luck. `κ = a` means the cutoff is *time-independent in physical space*, so
the drift of the cutoff and the transport by the scaling are the same motion with opposite signs.
It is also **not available anywhere else**: at `κ = a/2` the ratio is `1.0000` and at `κ = 0`, `T1`
vanishes identically and the uncancelled `T2 = 179.702` dominates. The branch Clay condition (7) forces
is exactly the branch where the two cutoff terms annihilate — and it is still not enough.

### §5.2 The sweep — does a bigger cutoff radius buy anything?

Measured `ρ`-exponents (tail-3 fit, `ρ₀ ≥ 100`; the 5-point fit including the pre-asymptotic
`ρ₀ = 10` row is banked alongside):

| `α` | `κ` | mode | `curl_L32` exponent | value at `ρ₀=1000` | `L3` exponent | value at `ρ₀=1000` |
|---|---|---|---|---|---|---|
| 1 | `0` | DSS | `-0.0001` | `4706.84` | `-0.0000` | `186.029` |
| 1 | `0` | SS | `-0.0001` | `4582.06` | `-0.0000` | `178.737` |
| 1 | `a/2` | DSS | `-0.0001` | `2492.11` | `-0.0000` | `101.808` |
| 1 | `a/2` | SS | `-0.0001` | `2291.03` | `-0.0000` | `89.3683` |
| 1 | `a` | DSS | `+0.0001` | `869.288` | `+0.0001` | `46.7696` |
| 1 | `a` | SS | `-2.0000` | `0.049549` | `-2.0000` | `0.0014911` |
| 1.25 | `0` | DSS | `-0.2501` | `737.218` | `-0.2500` | `29.4129` |
| 1.25 | `0` | SS | `-0.2501` | `717.417` | `-0.2500` | `28.2546` |
| 1.25 | `a/2` | DSS | `-0.2500` | `380.736` | `-0.2500` | `15.7024` |
| 1.25 | `a/2` | SS | `-0.2501` | `348.78` | `-0.2500` | `13.7617` |
| 1.25 | `a` | DSS | `-0.2498` | `129.807` | `-0.2498` | `6.98359` |
| 1.25 | `a` | SS | `-0.2299` | `10.9063` | `-0.2364` | `0.586856` |
| 1.6 | `0` | DSS | `-0.6001` | `55.1183` | `-0.6000` | `2.22662` |
| 1.6 | `0` | SS | `-0.6001` | `53.609` | `-0.6000` | `2.13794` |
| 1.6 | `a/2` | DSS | `-0.6000` | `27.5283` | `-0.6000` | `1.15026` |
| 1.6 | `a/2` | SS | `-0.6001` | `25.0857` | `-0.6000` | `1.00506` |
| 1.6 | `a` | DSS | `-0.5996` | `9.20987` | `-0.5997` | `0.495934` |
| 1.6 | `a` | SS | `-0.5915` | `1.84339` | `-0.5942` | `0.0992701` |

**At the pinned `α = 1` every `DSS` exponent is zero to `1e-04`.** The largest cutoff radius actually
evaluated was `|y| = 1261.72`, two decades of `ρ₀` beyond the onset of the asymptotic regime.
**`ρ` buys nothing.**

### §5.3 The controls — all nine, reported whether or not they fired

| control | what it tests | result | fired as planted |
|---|---|---|---|
| `C1` | independent re-implementation reproduces leg 381's **banked** ρ-exponents | max &#124;exponent − banked&#124; = `1.996e-12` over 14 exponents (`α = 1.0, 1.25`) | **YES** |
| `C2` | `χ ≡ 1` ⟹ `R_loc ≡ 0` | `max|R_loc| = 0.0` **exactly**, against field scale `2.6022` | **YES** |
| `C3` | SS control: constant `m` must kill the obstruction | `α=1, κ=a`, SS: exponent `-2.000005`, value `0.049549` at `ρ₀=1000` vs DSS `+0.0001` / `869.288` — **a factor `17544` and a completely different power** | **YES, decisively** |
| `C3′` | error must be LINEAR in the modulation amplitude | `curl_L32/amp` constant to relative spread `1.424e-04` over `amp ∈ {0.25, 0.5, 1, 2}`; at `amp = 0` (exactly SS) only `0.550545` remains — the `T4+T5` residue | **YES** |
| `C4` | `κ = a` cancels `T1+T2`; `κ ≠ a` does not | `2.578e-08` at `κ=a`; `1.0000` at `κ=a/2` | **YES** |
| `C4b` | reading (c): is the ansatz exactly (D)SS? | `κ=0`: DSS defect `7.46e-16` ⟹ **IS exactly DSS**. `κ=a/2`: `1.7036`. **`κ=a`: `1.0857` ⟹ is NOT** | **YES — and only for the branch Clay (7) forbids** |
| `C5` | exponent must track `1 − α` | `α=1` → `+0.0001` (pred `+0.00`); `α=1.25` → `-0.2498` (pred `-0.25`); `α=1.6` → `-0.5996` (pred `-0.60`) | **YES** |
| `C6` | exponent must not depend on the cutoff basis | **pre-registered 5-point form did NOT fire**: `curl` disagreement `2.03e-02` > tolerance `1e-02`. Tail-3 fit (the fit used everywhere else): `5.41e-05`. The CONSTANT does depend on the basis, ratio `C⁴/C²quintic = 1.476038`, as pre-registered. | **NO on the 5-point form — see §5.4** |
| `C7` | **falsifier**: can `(δa, δκ)` absorption flip `NO → YES`? | exponent `+0.0392` → `+0.0290` after optimal `L²` absorption on all of ℝ³; residual fraction `1.00091` at `ρ₀=1000` — absorption made it very slightly **WORSE** | **NO — the gate is not flipped** |
| `C8` | Clay condition (7): natively finite energy | `β = κ − a` to `1.46e-05` across `κ ∈ {0, a, 3a/2}`; bounded at `κ=a`, unbounded at `κ>a`, decaying at `κ=0` | **YES, both directions** |
| `C9` | quadrature / FD refinement | worst relative change `7.430e-05` (angular+radial refinement `7.43e-05`, FD step halved `8.36e-12`) | **YES** |

### §5.4 The one control that did NOT fire as planted — reported, not papered over

`C6` was pre-registered as "quintic `C²` vs degree-9 `C⁴` smoothstep: exponents must agree to
`1e−2`". On the 5-point fit they agree to `7.66e-04` in `L³` but only to `2.03e-02` in the `curl` norm —
**above the tolerance I fixed before the run.** The artefact records `fired_as_planted: false` and
the evidence script asserts that it still says so; the tolerance was **not** moved.

What the number actually is: the disagreement is entirely the `ρ₀ = 10` row, which is
pre-asymptotic and where the two bases differ most (`curl` `1020.64` vs `615.942`, against
`853.185` vs `578.023` at `ρ₀ = 1000`). On the tail-3 fit — the fit used for every exponent quoted
anywhere else in this leg — the two bases agree to `5.41e-05`, and **both give exponent zero**
(`-6.18e-05` and `-7.72e-06`). The **constants** differ by a factor `1.476038`, which `C6` explicitly permitted.

**This does not rescue anything and is not being used to.** Both bases give a `ρ`-independent,
strictly positive `c_mod`; the gate answer is the same under either, and the only thing `C6`'s
partial failure costs is the right to quote the 5-point exponent, which this leg does not do.

---

## §6. THE GATE — answered in the pre-committed wording of §3.3

> **IS THERE A CUTOFF RADIUS `ρ` AND A NAMED NORM IN WHICH THE TOTAL LOCALISATION-PLUS-MODULATION
> ERROR IS SMALLER THAN THE CLOSURE THRESHOLD IT MUST BEAT — YES OR NO, WITH THE NUMBER?**

### **`NO`.**

There is **no** cutoff radius `ρ` and **no** named norm in which the total
localisation-plus-modulation error of the natively finite-energy ansatz beats the closure threshold.

**THE NUMBER.** In the load-bearing, pressure-free norm `‖curl F‖_{L¹_t L^{3/2}_x}` the error
accrued **per unit similarity time** saturates at

    c_mod = 869.287822          (velocity norm ‖F‖_{L¹_t L³_x} :  46.769635)

with measured `ρ`-exponent **`+1.09e-04`** (velocity norm **`+8.35e-05`**) over `ρ₀ ∈ [10, 1000]`, the
largest cutoff radius actually evaluated being `|y| = 1261.72`. **Enlarging the cutoff radius buys
nothing.** Per DSS period the accrual is `c_mod · 2 log λ = 922.537353`, so a threshold `ε` can afford

    N(ε) = ε / (c_mod · 2 log λ)

| `ε` | `1` | `0.1` | `0.01` | `1e-06` |
|---|---|---|---|---|
| `N(ε)` periods | `0.00108397` | `0.000108397` | `1.08397e-05` | `1.08397e-09` |

A backward-DSS blow-up needs **infinitely many** periods. `Σ(∞) = ∞`. Even `ε = 1` buys `0.00108397` of a
single period.

**This `NO` is threshold-free.** It compares `c_mod` to no closure constant: a positive,
`ρ`-independent `c_mod` already makes `Σ(S) = c_mod·S` divergent, so the answer is `NO` for **every**
`ε_close > 0`. No closure constant is computed, no approximate inverse is constructed, and **the C1
ban stays disengaged** — the Fujita–Kato/Kato apparatus pre-registered in §0.3 turned out not to be
needed at all.

### §6.1 WHAT THE OBSTRUCTION IS — named, not gestured at

**`T3`, the modulation commutator.**

    T3 = Σ_k ṁ_k ( P[χψ_α] − χ P[ψ_α] ) ,   ‖T3‖_{scale-invariant} ~ |ṁ| · ρ^{1−α}.

`T3` is proportional to `ṁ`. It is **exactly zero if and only if the profile is exactly
self-similar** — `C3′` measures `c_mod` linear in the modulation amplitude to a relative spread of
`1.424e-04`, with only `0.550545` (the `T4+T5` residue) left at zero amplitude. So:

- **an exactly-SS profile pays nothing** — and is excluded by Nečas–Růžička–Šverák (ARMA **136**
  (1996) 55–98) and Tsai (ARMA **143** (1998) 29–51);
- **a genuinely DSS profile pays `|ṁ|·ρ^{1−α}` per unit `s`, forever** — and `α` is **pinned to
  exactly 1** (leg 397: Chae–Wolf [1610.09464] Thm 1.1 from below; Chae–Wolf Rmk 1.2 +
  Escauriaza–Seregin–Šverák from above), which makes that exponent exactly `0`.

`C5` confirms the exponent is `1 − α` and not an artefact: `+0.0001` at `α=1`, `-0.2498` at `α=1.25`,
`-0.5996` at `α=1.6`, against `0`, `−0.25`, `−0.60`.

**The natively finite-energy ansatz does not fail because it was built badly. It fails at the
endpoint — and the endpoint is exactly where the object is pinned to live.**

### §6.2 THE CLAUSE-(b) BILL — the analogue of leg 381's clause-(a) bill

| | clause (a) — leg 381, `p2_route_cloc_v1.json` | clause (b) — this leg |
|---|---|---|
| what must be beaten | tail in `L²`: needs `α > 1.5` | error **summable in `s`**: needs exponent `< 0`, i.e. `α > 1` **strictly** |
| what is available | `α = 1` (banked type-I) | `α = 1` ⟹ exponent `+1.09e-04` |
| **deficit in the exponent** | **`0.5`** | **`0`** (measured `1.09e-04`, i.e. zero to quadrature precision) |
| required/available ratio | `1.5` | — (an endpoint, not a ratio) |
| character of the failure | a **gap**: `0.5` of exponent missing | an **endpoint**: nothing missing, and still nothing left |

Clause (a) is a gap one could imagine buying. Clause (b) is not: the deficit is **zero** and the
construction still does not close, because summability needs the inequality **strict** and the
pinned `α = 1` delivers equality.

**A `YES` on clause (b) requires either `α > 1` — which the `α`-pin forbids — or `ṁ ≡ 0` — which
NRŠ/Tsai forbid.** Both doors were already shut in the record; what this leg measures is that the
natively finite-energy ansatz walks into exactly those two and no third one.

### §6.3 Which pre-committed reading fired

- **(a)** did **not** fire: there is no `YES`, so there is no lead to report and no certification
  cost to quote. (§7.3 records what it would have cost, so the asymmetry is visible.)
- **(c)** fired — **but only for the `κ = 0` branch**, which the collapse test shows **IS** exactly
  DSS (defect `7.46e-16`) and which is therefore called a `NO` on sight and stopped. **The `κ = a`
  branch — the natively finite-energy one, the one this unit was asked about — is NOT exactly (D)SS
  (defect `1.0857`).** The ansatz did **not** collapse into (D)SS in disguise. It is
  DSS-*equivariant*, and the reason it still fails is a different and more informative one.
- **(d)** governed every number: all of them re-derived from
  `writeup/data/p2_route_cloc_v1.json` (`self_hash 58c57b62c0cbc80d`) and from this leg's own banked rows.
  No number in this journal was taken from prose, and `STATE.md` was never opened as a source.

---

## §7. WHAT THIS IS NOT, AND WHAT MORE WOULD COST

### §7.1 The ceiling, stated before anyone else has to find it

- **Tier 2.** Float64 composite Gauss–Legendre quadrature and 4th-order central differences. No
  interval arithmetic, no certified bound, no proof. **Tier 2 is never a proof.**
- **The realization is synthetic.** Route 4 has **no banked discrete profile** — `leg_382.md:174`
  ("that any profile of route 4's object exists (**none does**)") and `leg_397.md §1`. The gate's
  words "route 4's BANKED discrete profile" were reconciled **in §1, before any result existed**, by
  measuring a **commutator**, which is well defined for any field, solution or not. Therefore:
  - the **ρ-EXPONENTS** are properties of the ansatz class and of `α` — **settled here**;
  - the **CONSTANT `c_mod = 869.287822`** is a property of leg 381's realization — **NOT route 4's
    number**.
- **NO LINK OF THE L1→L4 CHAIN MOVED.** Clay remains at ~0.05%. Scale is not evidence. This unit
  priced a clause; it did not break a wall, and nothing here is movement toward Clay.

### §7.2 `UNDER-RESOURCED`, with a cost (§3d) — and why the answer survives it

**Missing:** the true modulation amplitude `|ṁ|` of route 4's object.

**Why the `NO` survives:** control `C3′` measures `c_mod` **linear** in the modulation amplitude to a
relative spread of `1.424e-04`. Any `|ṁ| > 0` therefore gives a positive, `ρ`-independent `c_mod` and the
same divergent `Σ`. Only `|ṁ| ≡ 0` — exactly self-similar — gives `c_mod = 0`, and that object is
excluded by NRŠ/Tsai. **The `NO` is robust to the missing constant; only the SIZE of the number is
not.**

**Cost to remove the ceiling** (banked in the artefact under `under_resourced_with_a_cost`):

1. **A banked discrete profile for route 4.** None exists. That is the wave-4/5 construction
   programme, order `10²` agent-hours, and it is not purchasable inside leg 400.
2. **Interval arithmetic on the same commutator**, to turn the float exponent into a certified
   bound: a Route-D-style interval core over the annulus `ρ ≤ |y| ≤ 2ρ`, order `10¹` agent-hours
   **on top of** (1). Note this would certify a **`NO`**, which nothing downstream needs; it is
   listed for completeness, **not proposed**.

Neither is requested. Neither is a proposal for more seed supply.

### §7.3 What a `YES` would have cost, had one appeared (reading (a))

Reading (a) did not fire, because there was no `YES`. Had `C7` driven the exponent negative, the
report would have been a **LEAD, NOT A BROKEN WALL** — a statement about a discrete banked profile in
a named norm, not a certified bound and not a blow-up — and the certification cost would have been
item (2) above on top of a genuine profile from item (1). Recorded so the asymmetry is visible: this
`NO` costs nothing to believe; a `YES` would have cost `10²` agent-hours to make mean anything.

---

## §8. FLAGS TO THE CONDUCTOR — written here, applied NOWHERE

Per §0.4 nothing below is acted on in `STATE.md`, `WALLS.md`, `OPTIONS.md`, `ORCHESTRATION.md`,
`reports/`, `writeup/INDEX.md`, `writeup/build_figures.py` or any other unit's artefact. These are
observations for whoever integrates. **This unit rules on nothing.**

1. **W4 clause (b) is now priced, and it is an ENDPOINT failure, not a gap.** Clause (a) (leg 381)
   is short `0.5` of exponent; clause (b) is short **`0`** (measured `1.09e-04`) and still fails, because summability
   needs a strict inequality that the pinned `α = 1` turns into an equality. A conductor reading only
   "both clauses fail" would mis-price the two. Only the conductor may rule on what that means for
   W4's status.

2. **The natively finite-energy ansatz is NOT (D)SS in disguise.** Reading (c) fired **only** for
   the `κ = 0` branch (defect `7.46e-16`), which Clay condition (7) already forbids because its energy
   decays like `e^{−as}` (measured `β = -0.500000`). The `κ = a` branch has DSS defect `1.0857`: it is
   DSS-*equivariant*, not DSS. The intuition "any finite-energy truncation of a DSS profile is just a
   DSS profile" is **measurably false**, and the reason the ansatz still fails is a different one —
   which is worth more to the record than a collapse would have been.

3. **The potential-cutoff trick deletes leg 381's worst object for free, and may be reusable.**
   Cutting off the *potential* of a poloidal field rather than the field gives `div V ≡ 0`
   identically, so the Bogovskii corrector leg 381 measured **growing** at `ρ^{+0.5004160}` in `L²`
   simply does not exist (`T7 ≡ 0`, and `C2` measures `max|R_loc| = 0.0` exactly with `χ ≡ 1`). If any
   other unit carries a Bogovskii correction for a poloidal or otherwise potential-representable
   field, it may be removable the same way. A flag, not an instruction; applied nowhere outside this
   leg.

4. **`C7` is the one control that could have flipped this leg, and it is worth knowing WHY it did
   not.** The `(δa, δκ)` absorption is measured on **all of ℝ³, core included**. The two generators
   `V + y·∇V` and `∂_κV` are nearly parallel in the annulus and are supported in the core, where
   absorbing them *injects* error: optimal absorption left `1.00091` of the original `L³` residual, i.e.
   made it very slightly **worse**, and moved the exponent only from `+0.0392` to `+0.0290`. Any future
   unit tempted to "spend the modulation" on a localisation error should read that row first.

5. **`C6` did not fire as planted, and the tolerance was not moved.** §5.4. The artefact records
   `fired_as_planted: false`; the evidence script asserts it still does. Nothing in the gate depends
   on it.

6. **No ban was ruled on, and none obstructed.** `C1` never engaged — the `NO` is threshold-free and
   constructs no bounded approximate inverse in any space, uniform in `M` or otherwise. The
   Fujita–Kato/Kato apparatus pre-registered in §0.3 was in the end **not needed at all**, because no
   closure constant enters a divergent sum. There is no wording to escalate.

---

## §9. WHAT IS BANKED

| path | what |
|---|---|
| `experiments/journal/leg_400.md` | this journal; §§0–3 committed **before** any result existed |
| `experiments/p2_route_l5_v1.py` | the ansatz, the seven terms, the poloidal machinery, the quadrature |
| `experiments/p2_route_l5_v1_driver.py` | the controls, the sweep, the gate assembler (`--gate-only`) |
| `experiments/p2_route_l5_v1_evidence.py` | re-derives every number quoted here and **exits non-zero** on any disagreement |
| `writeup/data/p2_route_l5_finite_energy_v1.json` | the artefact; `self_hash 0c5e0f827f526df6`, `1528.6` s of compute |

Everything a later verifier must re-derive is **in the JSON**: the ansatz, every derived term, the
measured numbers, the norm, the realization/trial-space/basis, all nine controls with both
directions, the honest ceiling and the `UNDER-RESOURCED` cost.

