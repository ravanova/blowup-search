# Leg 345 — Route-PUB0C: the §0c census-spine section, assembled from banked records

**Branch:** worked in an isolated detached worktree off `origin/main` (per standing practice),
pushed directly to `main`. **Claims no stage** in `plan_of_record.py` (untouched, confirmed
byte-identical before and after — §5 below). **Not floor-eligible**: this is record/publication
assembly work, not new mathematics. **No link of `L1 → L4` moves.** Clay odds stay **~0.05%**.

## Precondition, verified before doing anything else

Leg 328 (Route-ORC5) landed at `fa1ab94` on `origin/main`, closing the closure-#5 register debt
at `writeup/CORRECTIONS.md` §21. Verified directly, not assumed:

```
$ git merge-base --is-ancestor fa1ab94 origin/main && echo ancestor
ancestor
$ grep -n "^## §21" writeup/CORRECTIONS.md
1445:## §21 — over-read closure #5, the register entry (six sites, four already fixed elsewhere)
```

Closure #6 (`writeup/CORRECTIONS.md` §17) was already landed by leg 339, predating this leg.
Both are citable. Confirmed by reading both sections in full before drafting anything.

## Order of work

1. Read `ORCHESTRATION.md` §1 (inherited law), `plan_of_record.py` (`.venv/bin/python
   plan_of_record.py`), and this leg's own dispatch spec as reproduced verbatim by the
   orchestrator (no live ban touches record/publication assembly of already-banked content —
   no solve, no certificate, no solver module, no dynamics run).
2. Novelty pass FIRST (`writeup/novelty/leg_345.md`, committed before the spine document):
   confirmed no `writeup/PUB_0C_CENSUS_SPINE.md` or equivalent exists anywhere in the repo, and
   no other leg's territory has ever claimed this filename.
3. Read every named banked source at source, not from memory or from this dispatch's own
   paraphrase: `experiments/journal/leg_174.md` (full), `experiments/journal/leg_309.md`
   (full), `experiments/journal/leg_323.md` (full), `experiments/journal/leg_317.md` (full),
   `experiments/journal/leg_237.md` (full), `experiments/JOURNAL.md:3304-3315` (leg 202's
   digest entry — no standalone `leg_202.md` file exists, confirmed by leg 317's own note that
   it read the same digest location), `experiments/journal/leg_316.md` (the DF-CGL
   reproduction cited by closure #5), `writeup/CORRECTIONS.md` §17 and §21 in full,
   `plan_of_record.py:47-55` and `CLAY_ROADMAP.md:335-355` (read only, to confirm the corrected
   wording closure #5 landed is actually present).
4. Cross-checked every leg-303 "four clause" reference against leg 303's own journal
   (`experiments/journal/leg_303.md`) and data file (`writeup/data/p2_route_gaf_v1_sweep.json`)
   to name the four clauses precisely (certificate / finite-time blow-up / dissipative object
   itself / fluid) rather than paraphrase them.
5. Drafted `writeup/PUB_0C_CENSUS_SPINE.md`, with every claim carrying its source locator, and
   a clause-by-clause constraint checklist (§8 of the document) verifying each named constraint
   in the thesis against its banked source.
6. Confirmed no banked source file was edited: `git status` and `git diff --stat` against the
   merge base show only the three declared territory files touched.

## What was found

**Every claim in the thesis survives at measured width.** No claim required softening, and no
claim named in the dispatch's thesis broke against its banked source when diffed. Specifically:

* The Grade-A/fluid cell's emptiness (leg 174's target-absence ground) and leg 309's defense
  against the one claimant (`arXiv:2604.09949`, refuted on an NRS/Tsai exclusion-theorem
  ground, theorem-level not arithmetic) both hold exactly as their own journals state them.
* Leg 323's variant-robust census numbers — **58 distinct ids, 51 novel (absent from
  `PRECEDENTS` and leg 303's result sets), 0/51 clearing all four named clauses, one 3-of-4
  mechanical near-miss (`arXiv:1504.02775`, failing clause (a): no certificate)** — are all
  quoted directly from `experiments/journal/leg_323.md` Part 6 and reproduce exactly.
* Both closures land as citable register entries: closure #5 (`writeup/CORRECTIONS.md` §21) narrowed
  "no certified viscous blow-up exists in any model, in any dimension" (FALSE) to "the
  Grade-A/fluid cell is empty" (the width that survives), with all six of its named sites
  individually verified fixed or pointer-carried; closure #6 (`writeup/CORRECTIONS.md` §17)
  corrected the mechanism/grounds behind a "closed three ways" claim (one misattributed, one
  overstated) while leaving the width — the cell is still empty — untouched. **This section
  never re-invokes a "closed N ways" count**, in keeping with what both closures corrected.
* Leg 317's prior-art map is cited, not restated: no `CAP_SILENT_FAILURES.md`-shaped claim is
  drafted anywhere in this section, matching leg 317's own NO-branch discipline.
* The leg-202/237 mechanism is presented as **known methodology applied**, per leg 317's own
  Family B verdict (folklore, older/broader than the assignment's framing anticipated — the
  named justification for Beyn–Thümmler freezing, pseudo-arclength phase conditions, and
  dynamic-rescaling normalization conditions) — not re-derived or presented as this
  repository's original contribution.

**The gate's yes-branch fires.** Every claim is stated at its measured width with the
constraint checklist (§8 of the spine document, 12 named constraints) verified clause-by-clause
against a banked source; no count stands in for a named realization anywhere in the assembled
section. **Banked as the PUB track's spine section.**

## What this leg did not do

No computation was run, no experiment reproduced, no new numerical claim introduced. No banked
source file (any leg's journal, `writeup/CORRECTIONS.md`, `plan_of_record.py`,
`CLAY_ROADMAP.md`) was edited — every one was read-only. `plan_of_record.py` byte-identical
before and after this leg's work (confirmed by hash, §5). No figure claimed — a spine section of
citations has no curve or table a figure would carry that the prose and its locators do not.

## Territory

`writeup/PUB_0C_CENSUS_SPINE.md` (new), `writeup/novelty/leg_345.md` (new),
`experiments/journal/leg_345.md` (this file). `git diff --stat` against the merge base returns
exactly these three files.
