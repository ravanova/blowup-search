# Leg 315 — Route-TMS: validated integration and Taylor models, scoped

**Branch** `leg/315-tms-v1` off `origin/main` (`69e5bc4`). Difficulty: standard.
**Scoping leg. Nothing built.** No solver module written or edited. No ban lifted.
`requirements.txt` NOT touched.

**Quartet:** runner `experiments/p2_route_tms_v1_scoping.py` · data
`writeup/data/p2_route_tms_v1.json` · prose
`writeup/4_p2_lottery/TECHNICAL_P2_ROUTETMS_V1.md` +
`writeup/4_p2_lottery/BLOG_P2_ROUTETMS_V1.md` · figure
`writeup/figures/fig79_route_tms_v1_reach.png` · novelty `writeup/novelty/leg_315.md`
(committed FIRST, `3362c9f`).

---

## 1. Gate answer, in the pre-committed and immutable wording

> **Gate.** Does the scoping name at least one concrete object currently unreachable by the
> radii-polynomial/NK apparatus that Taylor-model arithmetic reaches, with the reaching
> mechanism stated and its build cost classed?

**`yes`** → *Bank the scoping; the DM drafts the build leg; any requirements/policy change
routes via integration note, never in-leg.*

I did not edit the gate and do not think it is wrong. It is answerable as written and it was
answered as written.

**Answer in full:** YES. Two concrete objects are named — `O1`, `O2` — each unreachable by the
radii-polynomial/NK apparatus **as this repository realizes it** (realizations named, per
lesson 91), each with a named reaching mechanism and an evidence-backed cost class. A third,
`O3`, is named as reached by **neither**, so the YES is not over-read.

## 2. The named object and its named reaching mechanism (lesson 91)

**`O1` — the sonic-crossing `r`-tube.** A rigorous enclosure of BCG's autonomous `(W, Z)`
self-similar **Euler** ODE flow **through the sonic point**, carried as a Taylor model
polynomial in the similarity exponent `r` over an `r`-interval at γ = 7/5, whose output is the
profile-vs-`F_dis` domination margin as a certified function of `r` extending across and beyond
the dominance-window upper endpoint `r = 1.1909830`.

**Reaching mechanism: SONIC-POINT-DESINGULARIZED TAYLOR-MODEL STEPPING IN THE SIMILARITY
PARAMETER.** (i) The object is a finite-dimensional autonomous ODE — the verifier's own words,
"the autonomous `(W,Z)` ODE system that Thms 1.1/1.2 solve" — so the enclosure is of the **flow
map** and needs **no function space**. (ii) The sonic degeneracy is crossed by quasi-homogeneous
desingularization plus local analytic continuation — a technique **already in-repo** at
`writeup/data/p2_route_w2l_v1_lit.json:180`, verbatim *"quasi-homogeneous compactification plus
rigorous integration in interval arithmetic"*. (iii) Wrapping control by Lohner-QR / mean-value
form / shrink-wrapping (Berz–Makino) — the part that is Taylor-model arithmetic **proper** and
not merely interval arithmetic, and exactly what `solver/interval.py` cannot supply.

**Why NK does not reach it, per realization:** `ℓ¹_w` coefficient basis (leg 54, `1.167×` where
`>8×` needed); sup-norm collocation (leg 56, `1.85e7×`/`2.04e11×` over budget); origin-`H²` at
`a=0` with no transfer (legs 163/176). Structural reason: NK is a **zero-finder** needing an
approximate inverse in a fixed space, and the sonic point is a degeneracy a global basis handles
badly — plus the re-posed ban forecloses a fourth space. Lesson 87: the shape is a property of
the operator, and here NK's shape is a *zero* where a *flow map* is wanted.

**The claim I explicitly do NOT make.** The `r`-interval property is a **convenience, not the
load-bearing distinction** — radii-polynomial certificates are routinely run with interval
parameters for branch continuation, so "TM sweeps `r`, NK does not" would be false. The reach
rests only on (i) and (ii).

**`O2`** — rigorous enclosure of the degree-4503 Gauss–Laguerre nodes/weights and quadrature
that leg 256 hand-rolled in float64, closing leg 256's own recorded ceiling
(`capabilities.py:496-498`). Mechanism: arbitrary-precision **ball arithmetic** with rigorous
special-function and root enclosures — labelled as the **validated-integration half of the route
name, not the Taylor-model half**, rather than smuggled in.

## 3. Cost classes and what each estimate rests on

| object | class | rests on |
|---|---|---|
| `O1` | **C** — research, with a class-B core | **Not A**: no maintained pip-installable Python TM ODE integrator located; `python-flint 0.9.0` gives balls + `arb_series` but no TM IVP integrator; field's tools are C++ (CAPD `arXiv:2010.07097`, VNODE-LP, Flow\*), restricted-licence (COSY), or Julia; TERA `arXiv:2607.01189` is Python but control-oriented and brand-new. **B core**: a TM type over `solver/interval.py` + Lohner-QR is a leg-256-style hand-roll, and leg 256's was one leg for a *static* rule, so ≥1 leg and plausibly several. **Why C**: the bridge from validated ODE to validated PDE is Zgliczyński's self-consistent a-priori bounds (`math/0005247`), whose hypotheses are **dissipative**; BCG's rescaled system is **quasilinear hyperbolic** with dissipation as an `e^{−δ_dis s₀}`-decaying forcing. **That mismatch is a theorem nobody has proved, not a package nobody installed.** |
| `O2` | **A / B split** | **A**, pinned: `python-flint` **0.9.0**, PyPI, released **2026-07-03**, SPDX `MIT AND LGPL-3.0-or-later`, Python ≥3.10, **no scipy**. **B** for the layer: **not confirmed** that `acb_calc_integrate` is exposed in the Python bindings (API page reached documents v0.3.0, lists no `.integral()`) — recorded **under-evidenced**, not assumed. |
| `O3` | **C**, deliberately not costed | Costing an object neither apparatus reaches would be vibes. |

## 4. Controls (lesson 90) and instrument hygiene

Reach matrix carries **two controls that come out the other way**, asserted **in code**
(`_check_matrix_is_not_a_tautology`, which fails the run if no row is NK-only or no row is
"neither"): `C1` the repo's own bordered certificate (`capabilities.py:238-243, :333-337,
:359-360`), and `C2` **`arXiv:2305.08221`** (van den Berg–Breden–Sheombarsing, *Validated
integration of semilinear parabolic PDEs*) — which **refutes the naive form of this leg's own
thesis**, since NK demonstrably *does* validated time integration. Scored off an external
paper, so it could not come out my way by construction.

**One instrument found BROKEN and discarded:** the arXiv search UI returns **zero for ANY two
quoted phrases ANDed** (`"Taylor models"` alone = **84** results, `"validated integration"`
alone = **20**, every two-phrase conjunction = **0**). Six all-zero queries discarded, not
banked. `pypi.org/search` blocked by a Client Challenge page, discarded; project pages used.
Three controls passed: positive (`arXiv:2505.03091` matches in-repo record), negative
(fabricated arXiv path → HTTP 404), and spelling-variant (`"Navier--Stokes"` with the LaTeX
double hyphen returned this repo's own recorded negative control `arXiv:2604.09949`).
**Eleven links banked, not counts.**

## 5. Assess-before-running-long

Estimated runtime **< 2 s** before running (a table, a JSON dump, one figure). Achieved
**1.9 s**. No long run arose; nothing needed improving.

## 6. Wall 1 / Wall 2 / Clay

**Naming a reachable object is not reaching it.** Nothing here is movement toward Clay. No link
of the L1→L4 chain moved. Walls 1 and 2 stand. **Clay odds ~0.05%, unchanged.** `O1` is a
spherically-symmetric ODE profile object, i.e. it sits on the *already-known* side of Wall 2's
corrected line (3D-ness supplied by an ODE reduction, never by the certificate itself).

---

# INTEGRATION NOTES — actions for the orchestrator / DM

**(A) `requirements.txt` / policy note — ROUTED, NOT ACTIONED.** I did not touch
`requirements.txt`, per the spec. Two findings for whoever does:
1. The line *"scipy is intentionally NOT required (no adaptive ODE integrators are used)"* is
   confirmed a **policy, not a finding** — it is recorded nowhere as a measurement.
2. **On this leg's evidence the policy is ALSO not the binding constraint.** The tooling that
   would matter is `python-flint`/Arb, which **is not scipy**; and the Taylor-model IVP
   integrators are **not Python at all**, so no scipy decision would have admitted them.
   Repealing the line would not by itself unlock `O1`. If a build leg proceeds, the dependency
   question it actually raises is *adding `python-flint` (MIT + LGPL-3.0-or-later)*, which is a
   licence question, not the scipy question.

**(B) fig79 is TAKEN and needs registering.** I shipped
`writeup/figures/fig79_route_tms_v1_reach.png`. Figure numbers through fig78 were taken or
spoken for, so I took **fig79** as instructed. **It is not yet registered in
`writeup/INDEX.md`** (orchestrator-owned; I did not edit it). I shipped the figure I claim —
no claimed-but-absent number.

**(C) Territory extensions, declared plainly.** My spec's territory named the JSON, one
TECHNICAL file, the novelty log and the journal, and said "Nothing else." The §6 quartet
requires a **runner**, a **registered figure**, and **BLOG and TECHNICAL** prose, which that
list does not cover. I therefore added three files beyond territory, all **newly created with
unique names**, so no concurrent leg can conflict:
- `experiments/p2_route_tms_v1_scoping.py` (§6-required runner)
- `writeup/figures/fig79_route_tms_v1_reach.png` (§6-required figure, per (B))
- `writeup/4_p2_lottery/BLOG_P2_ROUTETMS_V1.md` (§6-required BLOG sibling; the merge gate
  requires a BLOG to have a TECHNICAL sibling, and it does)

I deliberately did **not** edit shared `writeup/build_figures.py` — with nine legs live, the
figure is built self-contained inside the runner instead. If you prefer fig79 folded into
`build_figures.py`, that is a one-function move and I have left the plotting code isolated in
`make_figure()` to make it a copy-paste.

**(D) The adjacent ban — a reading routed to the user, NOT decided by me.** The re-posed
radii-polynomial ban bars *"re-attempting the `ℓ¹`-Fourier/radii-polynomial machinery … measured
DEAD in three realizations, on ANY model"*, lifting only on a namable **FOURTH space/basis**
with its own scoping leg. My reading: it **does not bite** `O1`, because the ban's subject is
radii-polynomial machinery **in a function space** and a Taylor-model flow enclosure proposes
**no function space at all** — so it is not the "fourth space/basis" the lift clause
contemplates; the two subject matters are disjoint. **I do not lift, narrow, or argue against
the ban, and a build must not start on my reading alone.** Please have the DM/user confirm the
disjointness before any build leg is dispatched. Full 13-row term-by-term walk in
`writeup/novelty/leg_315.md` §0.

**(E) Sequencing and territory collisions the build leg must respect.**
- **Leg 312 (Route-APIA)** owns the arbitrary-precision interval build
  (`solver/interval_mp.py`). **`O2` is a consumer of APIA, not a new build leg** — please do
  not draft a separate leg for it.
- **Leg 320 (Route-MTSC)** is already sequenced to consume this scoping
  (`DIRECTION.md:14106-14110`).
- **Leg 267 (Route-FDL)** owns the precedent census for non-autonomous
  forcing-domination / trap-region enclosures. **I did not pre-empt or predict FDL's answer and
  do not claim the precedent is absent.** A build leg for `O3` should wait on FDL.
- Per the standing rule that **apparatus specs must grant `capabilities.py` territory
  explicitly or a solver module cannot land** (leg 302): if you draft the `O1` build leg, grant
  it `capabilities.py` in its territory line, or the Taylor-model module will not be able to
  register itself.

**(F) A caveat to carry, not to bury.** The most likely way this leg gets mis-carried is as
"Taylor models unlock the compressible route." They do not. They unlock **one ODE rung** of a
re-posed obligation whose **PDE step has no known validated form for a hyperbolic system**. The
headline is `O1` at cost class **C**, and the C is the news.
