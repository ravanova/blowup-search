# Leg 187 — Route-M2CI: Chen's inviscid a = 1/2 profile against every certificate hypothesis

**Gate: NO.** Failing clauses **H3 (isolation)**, **H5 (contraction)**, **H7 ($Z_2$ bounded)**
— while $Y_0$, the clause leg 125 measured, is **exactly zero**.

**Realization named:** *the dilation orbit through Object A.* The steady equation's scaling
symmetry puts a one-parameter continuum of exact solutions through Chen's profile, so its
tangent is an exact kernel of the linearisation, so $Z_0 + Z_1 \ge 1$ for **every** approximate
inverse. Isolation — the theorem's *conclusion* — is false, hence its hypotheses cannot all
hold, whatever $A$ or space is chosen.

## Session note

Resumed after an external laptop shutdown interrupted the prior agent mid-construction. The
salvaged state (commit `ce1e7ed`) was **usable and substantially complete**: the novelty pass
was properly committed *before* construction (`711b26a`), and `solver/chen_inviscid_certificate.py`
already contained the central algebraic idea and all seven clause functions. It had never been
executed. Nothing was restarted; the work was finishing, correcting and evidencing it.

A worktree note for future legs: `leg/187-m2ci-v1` was checked out in a *different* agent
worktree than the one this session was pinned to. `EnterWorktree` switched the session but the
Bash sandbox stayed pinned, wedging the shell (the `cd` back was itself refused, because it
evaluated from the disallowed cwd). Recovery was `EnterWorktree` back to the pinned worktree,
then `git reset --hard origin/leg/187-m2ci-v1`. The venv lives only in the main checkout, so
worktree runs need `/home/andy/projects/Unsolved/.venv/bin/python` by absolute path.

## What the prior agent had right

* The exact `Fraction` rational-function arithmetic (`RF`) and `exact_defect_numerator`.
* The orbit $\Psi_g = -\frac{16}{3}g^{3/2}X/(X^2+g)^2$ and its generator.
* All seven clauses, and the pre-commitment to answer the gate on the **battery**, not on $Y_0$.

## What this session changed

1. **Ran it.** The module had never executed. It ran clean against the real
   `solver/dissipative_profile.py` API on first attempt.
2. **Rewrote the H5 float column.** The docstring predicted the `pinv` shadow "must come out
   at 1". It comes out at **2.87e-09**. That is not a refutation of the exact argument — NumPy's
   default `rcond` ($\sim n\varepsilon \approx 10^{-13}$) sits *below*
   $\sigma_{\min}/\sigma_{\max} = 1.35\mathrm{e}{-08}$, so `pinv` keeps the near-null direction
   and numerically inverts a kernel the continuum forbids. Truncated at `rcond` = 1e-6 the
   shadow returns to 1 from below: 0.99998 (n = 401) → 0.9999990 (n = 801). Both regimes are
   now reported, because the gap between them is the measurement. **A reusable trap:** this
   fires whenever $\sigma_{\min}/\sigma_{\max} > n\varepsilon$, i.e. the normal case for a
   discretised singular operator.
3. **Added `divergence_exponents`.** H7 was single-resolution (lesson 86). Over n = 201…1201:
   $Z_2 \sim n^{2.36}$ (9.44e10 → 6.31e12, 66.9×, monotone), $\|A\| \sim n^{1.36}$,
   $\|B\| \sim n^{1.00}$. $Z_2$ is **not** an operator constant in this space — a second,
   independent failing clause. Its negative control, which could have come out flat:
   $\sigma_{\min}/\sigma_{\max} \sim n^{-6.68}$, collapsing 1.52e05×.
4. **Added `gate_verdict`** — the gate answered in the module, with its magnitudes and its
   scope disclaimer, not only in prose.
5. **Removed dead code** (`z_needed` / `"unused"` in the battery).
6. **Caught a real test bug of my own.** `chen_profile` returns the 3-tuple
   `(Omega, U_x, U)`; comparing my orbit against the tuple produced a spurious 3.394
   discrepancy. Fixed, and *strengthened*: the test now also verifies that the exact
   arithmetic's $H\Psi = (g-X^2)/D^2$ and $U = X/D$ equal leg 125's returned `U_x` and `U` to
   < 1e-13. Without that, the exact defect could have been solving a different equation
   correctly.

## Magnitudes

| clause | verdict | magnitude |
|---|---|---|
| H1 membership | holds for $s \le 3$ | ratio-vs-half-domain 1.000000 ($s\le3$); 1.4333 at $s=3.5$ |
| H2 defect | $Y_0 = 0$ **exactly** | zero polynomial at 5/5 $g$; controls all nonzero |
| H3 isolation | **FAILS** | exact competitors at $r$ = 1e-2, 1e-4, 1e-6; residuals at the centre's floor |
| H4 injectivity | **FAILS** | kernel defect 3.27e-06 vs control 0.689 at n=801; ratio 2.11e05 |
| H5 contraction | **FAILS, exactly** | $Z_0+Z_1 \ge 1$ for every $A$; $1-Z_0-Z_1 \le 0$ |
| H6a bordering | available | $\sigma_{\min}$ lift 4.77e04× at n=801 |
| H6b far-field symbol | degenerate at the centre | $\sigma(s)=s/3-1$, $\sigma(3)=0$; measured decay **2.999955** |
| H7 $Z_2$ | **FAILS** | $Z_2 \sim n^{2.36}$, 66.9× over a 6× ladder |

## Framing preserved (pre-committed in the novelty pass, before any number)

* **INVISCID**, ν = 0, Chen's own framing. Nothing here bears on the viscous "missing rung";
  it stands exactly where leg 125 left it.
* **Object A is a published closed form** (Chen eq (2.2); HQWW arXiv:2305.05895 for all
  $a \le 1$), so the YES branch was always hollow: with $Y_0 = 0$ any bordered
  $Z_0+Z_1 < 1$ closes a polynomial asserting a zero at an already-known point. Nil existence
  content. The NO is the more useful outcome.
* **The scaling zero mode is Xu's** (arXiv:2607.19762, $a=0$ sibling, point spectrum
  $\{0,1\}$), cited not claimed. **Symmetry reduction is standard**, cited not claimed. The
  contribution is the certificate-clause audit at $a = 1/2$ with magnitudes, plus the N4
  sub-question (symbol vs decay rate) which the novelty pass found unstated anywhere.

## Deliberately not supplied

A bordered $Z_0 + Z_1$. Bounding it rigorously needs an interval enclosure of the tail block,
which `solver/interval_certificate.py`'s banked ceiling says this repository does not have.
The JSON records it as `NOT MEASURED` with the reason rather than shipping a float dressed as
a bound (lesson 73).

## Verification

* `test_chen_inviscid_certificate.py` — **45/45**, every measured clause against a control that
  can fail (perturbed amplitude, $a=0$, $c_l=1/2$, localised bump, $X^{-2}$ decay profile,
  $\sigma$-ratio collapse).
* `experiments/p2_route_m2ci_v1_construction_evidence.py` — **30/30**, rebuilds every quoted
  number from the curated JSON without re-running the construction; builds fig65.
* `scripts/merge_gate.sh origin/main` — PASS.

## What outlives the gate

**Budget-under is not certificate-ready** — banked now in its strongest form, with the budget
pinned at exactly zero. Leg 125's $Y_0/\text{budget} \in [1.325\mathrm{e}{-09}, 5.800\mathrm{e}{-05}]$
was correct and load-free; it was never evidence about the clauses that decide the certificate.
