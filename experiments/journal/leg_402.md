# Leg 402 — Unit `V5` (Lane V): adversarial audit of `arXiv:2509.25116`, plus the wave-4 repair

**Branch** `leg/402-v5-audit-25116`. **Date** 2026-08-18. **Unit** `V5`, wave 6.
**Gate source** `writeup/waves/WAVE6_PLAN.md` §`V5`, committed at `e202653` BEFORE dispatch, NOT
re-wordable. **INSERT-ONLY journal.**

---

# PART 0 — PRE-REGISTRATION

**THIS SECTION WAS WRITTEN AND COMMITTED BEFORE ONE BYTE OF `arXiv:2509.25116` WAS FETCHED OR READ.**
It contains **NO VERDICT** on either clause. Reading (c) of the gate requires exactly this ordering.
The commit carrying this section carries no adjudication, no constant value from the paper, and no
answer to either clause.

## 0.1 The gate, copied verbatim from `writeup/waves/WAVE6_PLAN.md` §`V5`

> **GATE, final wording, TWO CLAUSES THAT MUST BE ANSWERED SEPARATELY.**
> **Clause 1 — the audit.** Enumerate the paper's load-bearing constants **before** adjudicating
> anything; recompute every recomputable one; check the localisation step (Remark 2, §1.3 together with
> §2) at full text. **Does the certificate close as claimed — YES or NO — and if any constant fails to
> recompute, name it, quote the failing clause verbatim, and give both numbers?**
> **Clause 2 — three-dimensionality, ANSWERED ON ITS OWN.** **Is the certified profile genuinely 3D
> against W2's own pre-committed test — YES or NO?** **W2's test is the arbiter and this answer MUST NOT
> be folded into the clause-1 verdict.** Wave 2 measured that two certified rows elsewhere were **2D
> lifts**; that is the failure mode this clause exists to catch.

## 0.2 W2's pre-committed test — LOCATED IN THE RECORD, not invented

I located it. It is banked in two places and I quote both **character-exact**.

**(i) The control that carries it**, `writeup/data/p2_route_t4_v1.json`, key `controls.C_3D`, field
`asserts` — verbatim:

> `Table 1 records Nx3 = 0 BOTH certified rows`

and field `two_sided` — verbatim:

> `if Nx3 > 0 either row, W2's crack survives at full text leg 348's classification confirmed on its most load-bearing clause`

**(ii) The operationalisation**, `experiments/journal/verify_wave2.md` §2.1 (*"Item (1) — `T4`'s 2D
lift"*), which fixes the derivation path **in advance** as a table of six checks, for `lab ∈ {p1, p2}`.
The six observables and their required values, as banked there (this list is a **paraphrase of the
table's structure**; the individual cell strings are quoted character-exact in §0.2.1 below):

1. `N_x3 = 0` in Table 1 — field `rows[lab].table1_row.Nx3`, computation `== 0`
2. `N_x3 = 0` in `Nrec` — field `rows[lab].solshape_Nrec[2]`, computation `== 0`
3. and in the control — fields `controls.C_3D.p1_Nx3`, `.p2_Nx3`, computation `== 0`
4. extent 1 in `x₃` — `rows[lab].dimensionality.x3_mode_extent`, axis 2 of `omega_shape`, `u_shape`,
   `p_shape`, all `== 1`
5. `max|u⁽³⁾|`, `max|ω⁽¹⁾|`, `max|ω⁽²⁾|` — all `== 0.0` under `float.__eq__`
6. `max|ω⁽³⁾|` nonzero (`1.6351` / `1.5274`); and `setup` field `== "2D"`

### 0.2.1 The test, stated as the predicate I will apply

**W2's 2D-LIFT PREDICATE (as banked).** A certified profile on `ℝ³` (or `T³`) is a **2D LIFT** when,
in the certificate's own Cartesian coordinates:

- (L1) it has **no dependence on one Cartesian coordinate** — zero modes in `x₃`, extent 1 in `x₃`; and
- (L2) the **corresponding velocity component vanishes identically**, `u⁽³⁾ ≡ 0`; and
- (L3) consequently **two of the three vorticity components vanish identically**, `ω⁽¹⁾ ≡ ω⁽²⁾ ≡ 0`,
  with only `ω⁽³⁾` nonzero.

It is **genuinely 3D** when the conjunction (L1) ∧ (L2) ∧ (L3) **fails**.

**PRE-COMMITTED, BEFORE READING — the four adjudication rules that will decide clause 2.** These are
fixed now precisely because I do not yet know which case the paper falls into.

- **R2-a.** If the paper ships decodable certified data (coefficient arrays / `.mat`-equivalent), I
  apply W2's test to **the decoded arrays**, exactly as W2 did, and the answer is the measurement.
- **R2-b.** If the paper ships no decodable arrays, I apply the **same predicate** to the paper's own
  **stated analytic ansatz** for the certified profile — the symmetry class it declares, the components
  it declares zero, the variables it declares the profile to depend on — and I say so explicitly, that
  the test was applied to the declared ansatz and not to decoded bytes.
- **R2-c — THE AXISYMMETRIC CASE, PRE-COMMITTED NOW SO IT CANNOT BE DECIDED AFTER THE FACT.**
  An **axisymmetric** profile reduces to two *independent variables* `(r, z)`, and it is tempting to
  call that a 2D object. **Under W2's test as banked it is NOT a 2D lift**, and I pre-commit to that
  reading now:
  - axisymmetric **with swirl** (`u_θ ≢ 0`): in Cartesian, `u⁽³⁾ = u_z ≢ 0`, the field depends on
    `x₃ = z`, and all three `ω` components are generically nonzero. (L1), (L2), (L3) all fail ⇒ **3D**.
  - axisymmetric **without swirl** (`u_θ ≡ 0`): in Cartesian, `u⁽³⁾ = u_z ≢ 0`, the field depends on
    `x₃ = z`, `ω = ω_θ e_θ` has `ω⁽¹⁾, ω⁽²⁾ ≢ 0` and `ω⁽³⁾ ≡ 0`. This is the **mirror image** of the
    `1902.00384` pattern, not the pattern itself: (L1) and (L2) both fail ⇒ **3D under W2's test**.
  - **In both sub-cases I will ALSO report, as a flagged OBSERVATION that is NOT folded into the
    YES/NO, that the object has two independent variables.** Reporting it is obligatory; letting it
    change the verdict is forbidden, because W2's test is the arbiter and W2's test keys on the
    Cartesian observables (L1)–(L3), not on the count of independent variables.
- **R2-d.** A profile that is a 2D lift **after a rigid rotation** counts as a 2D lift. I will check
  the rotated case, not only the coordinate-aligned one.

## 0.3 PRE-COMMITTED BRANCHES — CLAUSE 1

Fixed before reading. Whichever fires, fires.

- **B1-YES.** Every enumerated load-bearing constant either (i) **recomputes** from the paper's own
  stated definitions to within the precision the paper itself claims, or (ii) is
  **`UNRECOMPUTABLE-BY-DESIGN`** — an output of a computer-assisted enclosure the paper does not ship
  in runnable form — and is banked as `UNRECOMPUTABLE`, **never as a failure and never as a zero**;
  **and** the localisation step (Remark 2, §1.3 with §2) carries the certified object to the statement
  the theorem makes, with every link nameable. ⇒ **clause 1 = YES, with an explicit list of what was
  taken on trust.**
- **B1-NO.** At least one **recomputable** constant disagrees with its printed value beyond the
  paper's own stated precision, **or** the localisation step has a gap I can name and quote. ⇒
  **clause 1 = NO**, and I must name the constant, quote the failing clause **character-exact with
  location**, and give **both numbers**.
- **B1-PARTIAL.** Some constant fails to recompute but is **not** load-bearing for the closure (an
  arithmetic slip in a remark, a mis-stated rounding). ⇒ I report it with both numbers and state
  **separately** whether the certificate still closes; the YES/NO is decided on the load-bearing set
  only, and the non-load-bearing failure is banked in full.
- **`UNRECOMPUTABLE` IS NOT `NO`.** Pre-committed, and this is the trap I am naming in advance: an
  interval-arithmetic certificate is *supposed* to contain numbers I cannot re-derive by hand. Finding
  that I cannot re-run somebody's INTLAB job is **not** a finding that the certificate fails. The
  audit's value is concentrated exactly where a number is **transcribed rather than measured**, where
  a hypothesis is **assumed rather than verified**, and at the **seams between the computer-assisted
  part and the pen-and-paper part** — which is what the localisation step is.
- **The ban discipline (reading (g)).** A ban is superseded by a MEASUREMENT, never a decision. Where
  I cannot reach a primary source, the outcome is `UNREACHABLE`, banked as such, **never as a zero**.

## 0.4 PRE-COMMITTED BRANCHES — CLAUSE 2

- **B2-YES.** W2's predicate (L1)∧(L2)∧(L3) fails on the certified profile ⇒ **genuinely 3D = YES**.
- **B2-NO.** W2's predicate holds ⇒ **genuinely 3D = NO, the certified profile is a 2D LIFT.** Per
  reading (b) this is the **more consequential** answer and is stated plainly **even if clause 1
  returns YES**, and it is a **W2 finding, not a W3 one**.
- **B2-UNMEASURABLE.** If the paper ships neither decodable arrays nor a determinate declared ansatz,
  the answer is `UNMEASURABLE-AT-PRIMARY`, banked as such, **never as a YES by default**.
- **NOT FOLDED IN.** The clause-2 answer is written in its own artefact field and its own journal
  part. It does not appear as a term in the clause-1 verdict, and the clause-1 verdict does not appear
  as a term in it.

## 0.5 PRE-COMMITTED CONSTANT-ENUMERATION PROCEDURE

Executed and **committed in its own commit BEFORE any adjudication**, so that the enumeration cannot
be trimmed to fit a verdict.

1. **Fetch and pin.** Fetch the arXiv PDF and, if available, the LaTeX e-print. Record `sha256`,
   byte count, version stamp and fetch URL for each. Cross-check at least three pages of extracted
   text against the rendered source to rule out extraction artefacts (leg 309 step 3).
2. **Sweep, mechanically, before judging.** Enumerate as `H1 … Hn` every item in these five classes:
   - (E1) every **numerical value appearing in the statement** of a Theorem, Proposition, Lemma or
     Corollary that the main result depends on;
   - (E2) every **named constant** entering the **closure inequality** / fixed-point condition, and
     the closure inequality itself;
   - (E3) every **tolerance, interval width, rounding mode and precision claim**;
   - (E4) every **exponent and threshold** (decay rates, scaling exponents, Sobolev indices);
   - (E5) every **hypothesis** whose failure would break the closure — including the ones the paper
     states once and never revisits.
3. **Classify each**, before adjudicating: `symbol` / `printed_value` / `location (§, eq, page)` /
   `role_in_the_closure` / `RECOMPUTABLE | UNRECOMPUTABLE-BY-DESIGN | HYPOTHESIS`.
4. **Freeze and commit the enumeration.** No item added or removed after this commit; anything found
   later is appended as `Hn+…` with a note that it was late, never silently merged.
5. **Recompute** every `RECOMPUTABLE` item independently, from the paper's stated definitions only,
   in `mpmath` at precision well above the paper's, and where the item is an enclosure, with directed
   rounding. Record **printed value**, **my value**, and **absolute/relative difference**.
6. **Localisation at full text.** Read Remark 2, §1.3 and §2 in full. Write the implication chain out
   link by link — what is certified locally, what the localisation multiplies/cuts off, what is
   claimed globally — and mark each link `CHECKED` / `TAKEN-ON-TRUST` / `GAP`. A `GAP` must be
   quotable character-exact.
7. **Both numbers, always.** Any disagreement is banked with printed and recomputed values.

## 0.6 QUOTING DISCIPLINE (reading (d)) — pre-committed

`V-W4` found **four** fields in wave 4 presented as verbatim that were not. Therefore, for this unit:
every string I present as a quote is copied **mechanically from the extracted text**, carries its
location, and is checked by a script that re-anchors it in the source file and **exits non-zero** if
it does not appear character-exact. Anything I cannot anchor is **labelled a paraphrase** in the text
itself. De-hyphenation of `pdftotext` line breaks is the one normalisation permitted and it is
declared at the site.

## 0.7 WHAT THIS UNIT WILL NOT DO — pre-committed

- **Journal-ref (reading (e)).** I **record what I observe** about the presence or absence of a
  journal-ref and stop there. I do **not** grade `PUB_0C_CENSUS_SPINE.md` §1's *"any published work"*,
  I do **not** rule the open escalation
  `writeup/escalations/ESCALATION_PUB0C_PUBLISHED_2026-08-18.md`, and I do **not** stop for it.
- **Authors (reading (f)).** No contact of any kind. Reading published material is authorised; that
  is the whole of the permitted interaction.
- **Walls.** Reading (a) is pre-accepted: a `YES` on clause 1 **does not move W3 by itself**, and a
  `NO` **does not vacate the Grade-A × fluid cell**, which is occupied as a matter of grading. I will
  say what changes and what does not, and I will not touch `WALLS.md`, `STATE.md`, `OPTIONS.md`,
  `DIRECTION.md`, `ORCHESTRATION.md` or `reports/`.
- **Ceiling (reading (h)).** Tier 2 is never a proof. Nothing this unit produces is movement toward
  Clay unless a link of the `L1 → L4` chain actually moved, and an audit of somebody else's paper
  moves no such link.

## 0.8 THE SECOND DELIVERABLE, PRE-COMMITTED

The wave-4 repair, under ruling Q3: **correction RECORD, artefact UNTOUCHED**. **No `_v1` file will be
edited.** Two new files, `writeup/data/p2_route_v3_gradeA_v2.json` and
`writeup/data/p2_route_l2_decay_v2.json`, each carrying `supersedes`, a `correction_record` pointer to
`writeup/CORRECTIONS.md` §33, and **only** the fields §33 names (`D1`–`D4`, `D5`/`D6`, `N1`).

**A wording mismatch I record now, before it can look like a convenience.** My dispatch says `D1`–`D4`
are quote/attribution fields in `V3`'s rows *"about THIS PAPER"*, and `WAVE6_PLAN.md` §`V5` says they
are *"quote/attribution fields of `V3`'s rows on this very paper"*. **`CORRECTIONS.md` §33 says
otherwise**: `D1` is row `R7` (`2509.14185`), `D2` is row `R6` (`2208.09445`), `D3` is row `R5`
(`2305.05660v3`), `D4` is row `R3` (`2605.15149v1`) — **none of them is `2509.25116`**. §33 is the
authoritative correction record and the deliverable is scoped to *"ONLY the fields §33 names"*, so
**I repair the rows §33 names** and re-fetch those four sources to anchor the corrected quotes. The
plan's rationale sentence is defective; per reading (g) I **record it and do not rule it**.

## 0.9 Order of work, fixed

1. This pre-registration, committed alone. **← the commit this section lands in**
2. Fetch + pin the paper. Enumerate `H1 … Hn`. **Commit the enumeration with no adjudication.**
3. Recompute. Localisation at full text. Clause 1.
4. Clause 2, written on its own, against §0.2.1 and §0.4.
5. The wave-4 repair: the two `_v2` files.
6. `writeup/data/p2_route_v5_audit_v1.json`, `experiments/p2_route_v5_v1_evidence.py`, push.

**NO VERDICT APPEARS ABOVE THIS LINE, AND NONE APPEARS IN THE COMMIT THAT CARRIES IT.**

---

# PART 1 — THE SOURCE, PINNED; AND THE LOAD-BEARING ENUMERATION, FROZEN BEFORE ADJUDICATION

**This part is committed on its own. It contains the enumeration and NO recomputed value and NO
verdict.** Honest statement of what I had seen when I wrote it: I had read §1, §2, §3.5 and §7's
opening at full text, so I knew *which* numbers the paper prints. I had not written down a single
recomputed number, and none appears below. That is the discipline the pre-registration bought.

## 1.1 The source, pinned

| item | value |
|---|---|
| arXiv id | `2509.25116` |
| version | **v2**, stamp `arXiv:2509.25116v2 [math.AP] 19 Mar 2026` |
| PDF `sha256` | `2d369eade29bd0d5e8dfc6d7def7ed19b8282ab4450860978f3691176d9f8318` |
| PDF bytes / pages | `2148337` / **74** |
| PDF url | `https://arxiv.org/pdf/2509.25116` |
| LaTeX e-print url | `https://arxiv.org/e-print/2509.25116` |
| e-print archive bytes | `2171349` |
| `main.tex` lines / bytes | `4613` / `315538` |
| title | `Nonuniqueness of Leray-Hopf solutions to the unforced incompressible 3D Navier-Stokes Equation` |
| authors | Thomas Hou, Yixuan Wang, Changhe Yang |
| code repo (named in the abstract) | `https://github.com/HouGroup2026/3d-navier-stokes-nonuniqueness` |

**Cross-check against extraction artefacts (leg 309 step 3).** I hold **both** the rendered PDF and
the **LaTeX source**. Every quote below is taken from `main.tex`, i.e. from the authors' own bytes,
not from `pdftotext` output. This removes the entire class of defect `D3` recorded in
`CORRECTIONS.md` §33 (a `pdftotext` hyphenation carried into an artefact as verbatim).

**The `sha256` matches `V-W4`'s independent re-fetch** (`p2_verify_wave4_v1.json`, item 1, records
`2d369e...8318`). Two units, two fetches, same bytes.

## 1.2 The journal-ref observation — RECORDED, NOT RULED (reading (e))

**What I observe, and nothing more.** The arXiv abstract page for `2509.25116`, fetched today, has
**no `journal-ref` field** — there is no `tablecell jref` element in the served HTML. The only DOI
present is arXiv's own DataCite DOI `https://doi.org/10.48550/arXiv.2509.25116`, which arXiv mints
for every submission and which is **not** a journal DOI. The submission history shows
`[v1] Mon, 29 Sep 2025` and `[v2] Thu, 19 Mar 2026`. **That is the whole of my observation.**

I do **not** grade `PUB_0C_CENSUS_SPINE.md` §1's *"any published work"* against it, I do **not** rule
the open escalation `writeup/escalations/ESCALATION_PUB0C_PUBLISHED_2026-08-18.md`, and I did not
stop for it. `V-W4` observed the same thing; this is a second, independent observation of it.

**One further thing I observed and record without ruling.** §7 (`\label{sec7}`), the section that
carries *"the rigorous justification for the constants in Section \ref{Sec:num prev}"*, contains a
**commented-out sentence** in the source that the rendered paper does not show. Character-exact from
`main.tex` line 3519, with the LaTeX comment character as it appears:

`%A subsequent version will report the final constants (we expect only minor adjustments) and the full error budgets from the completed verification.`

The same line also carries, commented out, `Section \ref{sec7} describes the numerical procedure for
the ongoing computer-assisted verification.` **I record that these sentences are commented out and
therefore are NOT claims the paper makes.** They are not evidence about the paper's status and I draw
no inference from them. They are recorded because an adversarial audit that saw them and did not say
so would be concealing what it saw.

## 1.3 The structure the certificate actually has

Fixed here so the adjudication cannot re-draw it later.

- **Theorem 1** (`thm:main`, §1, line 85): infinitely many distinct **suitable Leray–Hopf** solutions
  on `ℝ³ × [0,1]` with the same compactly supported divergence-free initial datum.
- **Theorem 2** (`thm:stationary solu main`, §1.3, line 294): existence of a weak solution
  `(Ũ, ṽ, λ̃)` to the coupled profile-plus-eigenpair system `(eq:NS eq eig)`. **This is the
  computer-assisted statement.**
- **Assumption 1** (`asm:num`, §1.4): the numerical tuple `(Ū, P̄, v̄, q̄, λ̄)` is an approximate
  solution with `‖E^U‖_{L²} ≤ ε^U`, `‖E^v‖_{L²} ≤ ε^v`.
- **Proposition 1** (`prop:U`) and **Proposition 2** (`prop:u lambda`): Assumption 1 ⇒ exact profile,
  exact unstable eigenpair. Theorem 2 follows from these two.
- **§3.5** (`Sec:num prev`, *"Numerical results"*): the **certified input list** (20 displayed
  bounds) and the **two closure computations** that discharge Propositions 1 and 2. **This is where
  every recomputable number lives.**
- **§2** (`sec:settt`): Theorem 2 ⇒ Theorem 1. **This is the localisation step**, and `Remark 2`
  (§1.3, line 250) is the sentence that says localisation is needed at all.

## 1.4 THE LOAD-BEARING ENUMERATION, `H1`–`H41`

**Frozen. Nothing may be added or removed after this commit; anything found later is appended as
`H42+` and labelled late.**

### Class A — certified numerical inputs (`UNRECOMPUTABLE-BY-DESIGN`)

These are outputs of the authors' interval-arithmetic computation. Per the pre-registration, being
unable to re-run somebody's Julia/interval job is **not** a finding that a constant fails; these are
banked `UNRECOMPUTABLE-BY-DESIGN` and their role is recorded. All are from §3.5's displayed list.

| id | quantity | printed bound | role |
|---|---|---|---|
| `H1` | `‖E^U‖_{L²} ≤ ε^U` | `6.5 × 10⁻⁷` | residual of the profile; drives `M₄^U`, `x₀^U` |
| `H2` | `η₁` | `0.199` | coercivity split parameter; `¼ − η₁` is the coercivity budget |
| `H3` | `‖λ_max(Q)‖_{L^∞}` | `4.5` | enters `M₂^U`, `M₂^v`, `K₅` |
| `H4` | `‖E^v‖_{L²} ≤ ε^v` | `1.9 × 10⁻⁵` | residual of the eigenfunction |
| `H5` | `λ̄` | `≈ −0.11314203274385938 ≤ −0.113` | approximate eigenvalue; **the instability itself** |
| `H6` | `‖v̄‖_{L^∞}` | `2.52` | enters `λ^con`, `ε₅` |
| `H7` | `‖∇v̄‖_{L^∞}` | `3.4` | enters `ε₅` |
| `H8` | `‖P_v̄(½x·∇v̄ − Δv̄ − Ū·∇v̄ + v̄·∇^T Ū)‖_{L²}` | `6.38` | `M₀`, hence `M₃^v` |
| `H9` | the two orthogonal-complement Rayleigh sups | `0.1024` | enters `K₅^U`, `K₅^v` |
| `H10` | `K₆^U` sup | `0.46` | `M₁^U` |
| `H11` | `K₆^v` sup | `0.46` | `M₁^v` |
| `H12` | `ε^U₃, ε^U₄, ε^v₃, ε^v₄` | `5.3 × 10⁻⁵` | quasi-orthogonality defects |
| `H13` | `√tr(G^U_{Q²} G^U_r)` = `K₇^U` | `0.0012` | `M₂^U` |
| `H14` | `‖G^U_{Q²}‖_F` | `0.025` | `M₃^U`, `y₀^U`, `y₁^U` |
| `H15` | `√ΣΣ‖φ̄^U_l·∇φ̄^U_j‖²_{L²}` | `0.12` | `M₃^U` |
| `H16` | `√Σ‖∇φ̄^U_j‖²_{L^∞}` | `1.33` | `M₃^U`, `y₁^U` |
| `H17` | `√Σ‖φ̄^U_j‖²_{L²}` | `0.36` | `y₀^U` |
| `H18` | `√tr(G^v_{Q²} G^v_r)` = `K₇^v` | `0.004` | `M₂^v` |
| `H19` | `‖G^v_{Q²}‖_F` | `0.029` | `M₃^v`, `λ^lin` |
| `H20` | `√Σ‖φ̄^v_j‖²_{L²}` | `1.02` | `M₃^v`, `λ^lin` |
| `H21` | `η₂` | `0.005` | `L²` coefficient of the `E`-norm; enters `M₂^U`, `M₂^v` |

### Class B — recomputable derived constants (`RECOMPUTABLE`)

Every one of these is an arithmetic consequence of Class A **and the paper's own stated definitions**.
Each will be recomputed at 50-digit precision from Class A alone.

| id | quantity | printed value | defining formula, as printed |
|---|---|---|---|
| `H22` | `K₄` (sharp Sobolev) | `3^{3/4}/(2³π) ≈ 0.0907`, used as `≤ 0.091` | Appendix A, `lem:sobolev const` |
| `H23` | `K_s` (Talenti) | `2^{2/3}/(3^{1/2}π^{2/3})` | Appendix A proof |
| `H24` | `M₁^U` | `≤ 0.9301` | `K₅^U + K₆^U/(1−ε^U₄)²` |
| `H25` | `K₅^U` | `≤ 0.47` | `‖λ_max(Q)‖_∞ · (orthogonal-complement sup)` |
| `H26` | `M₂^U` | `≤ 0.0061` | `η₂M₁^U + ε^U₃‖λ_max(Q)‖_∞ + K₇^U` |
| `H27` | `M₃^U` | `≤ 0.22` | `√(‖G^U_{Q²}‖Σ‖∇φ̄^U_j‖²_∞) + ‖G^U_{Q²}‖√ΣΣ‖φ̄^U_i·∇φ̄^U_j‖²` |
| `H28` | the reduced form of `M₄^U` | `(0.051 − M₂^U)² − 2.8 × 10⁻⁶ × M₃^U` | `(¼ − η₁ − M₂^U)² − 4M₃^U ε^U` |
| `H29` | `M₄^U` | `≥ 0.002` | same, evaluated |
| `H30` | `x₀^U` | `≤ 1.44 × 10⁻⁵` | `2ε^U/(¼ − η₁ − M₂^U + √M₄^U)` |
| `H31` | `x₁^U` | `≤ 6.2 × 10⁻⁶` | `√((¼−η₁−M₂^U)/(1−M₁^U)) · x₀^U/2` |
| `H32` | `y₀^U` | `≤ 1.3 × 10⁻⁷` | `x₀^U ‖G^U_{Q²}‖_F √Σ‖φ̄^U_j‖²` |
| `H33` | `y₁^U` | `≤ 4.8 × 10⁻⁷` | `x₀^U ‖G^U_{Q²}‖_F √Σ‖∇φ̄^U_j‖²_∞` |
| `H34` | `|λ^con|` | `≤ 1.62 × 10⁻⁵` | `x₁^U‖v̄‖_∞ + y₁^U` |
| `H35` | `ε₅` | `≤ 6.6 × 10⁻⁵` | `(x₀^U + y₀^U)‖∇v̄‖_∞ + x₁^U‖v̄‖_∞ + y₁^U` |
| `H36` | `M₁^v` | `≤ 0.9301` | `K₅^v + K₆^v/(1−ε^v₄)²` |
| `H37` | `M₂^v` | `≤ 0.0089` | `η₂M₁^v + ε^v₃‖λ_max(Q)‖_∞ + K₇^v` |
| `H38` | `M₅` | `≥ 0.155` | `¼ − η₁ − λ̄ − 2K₄x₁^U − y₁^U − |λ^con| − M₂^v` |
| `H39` | `M₀`, `M₃^v`, `M₄^v` | `≤ 6.4`, `≤ 8.82`, `≥ 0.021` | `M₃^v = M₀(1+√(‖G^v_{Q²}‖Σ‖φ̄^v_j‖²))²` |
| `H40` | `x₀^v`, `x₁^v` | `≤ 5.67 × 10⁻⁴`, `≤ 4.3 × 10⁻⁴` | `2(ε^v+ε₅)/(M₅+√M₄^v)`, `√(M₅/(1−2K₄‖∇U‖−M₁^v))·x₀^v/2` |
| `H41` | `abs(λ^lin(v))`, `abs(λ)` | `≤ 0.0044`, `≤ 0.0045` | `M₀‖v‖_{L²}`; `abs(λ^lin) + abs(λ^con)` |

### Class C — hypotheses whose failure would break the closure (`HYPOTHESIS`)

| id | hypothesis | where |
|---|---|---|
| `HA` | the two closure inequalities `M₄^U > 0` and `M₄^v > 0` | §3.5, and `eq:cond ref U` / `asm:cond ref v` |
| `HB` | `Ū, v̄ ∈ C^∞_σ(ℝ³\{0}) ∩ H²_loc` with far-field `Ū = abs(ξ)⁻¹A(ξ/abs(ξ)) + O(abs(ξ)⁻³)` and all derivatives | Assumption 1 |
| `HC` | `‖v̄‖_{L²} = 1` and `⟨E^v, v̄⟩ = 0` | Assumption 1 |
| `HD` | the Schauder fixed-point argument is applied where the map is compact | §3.3 |
| `HE` | **the localisation**: Theorem 2 ⇒ Theorem 1 | Remark 2 (§1.3) together with §2 |
| `HF` | `Ũ` smooth with `U_near(ξ) = O(abs(ξ)^{−3/2})` decay | Props `prop smooth`, `prop decay` |
| `HG` | essential spectrum of `L_Ũ` in `Re λ ≤ −¼`; Riesz projections bounded and finite rank | §2.2, citing `[albritton2022non]` §4.1 |
| `HH` | the constructed `u` satisfies the **energy inequality** in the Leray–Hopf definition | §2.2 conclusion |

## 1.5 The clause-2 object, identified before it is judged

The certificate's profile is stated by the paper to be **axisymmetric** (§1.4, and §4.3
`\label{sec:sym}` *"Axisymmetry, parity and boundary conditions"*). My pre-registration §0.2.1 rule
**R2-c** was written before I knew this, and it fixes in advance how an axisymmetric object is
adjudicated against W2's test. **R2-a applies**: the authors ship decodable arrays, so W2's test is
applied to the decoded data, not to the declared ansatz. The files are
`data/UP.mat` (profile `Ū`) and `data/up_eig.mat` (eigenfunction `v̄`) in the repository named in
the paper's own abstract.

**No measurement, no verdict, appears in this commit.**

---

# PART 2 — CLAUSE 1: THE AUDIT

Adjudicated against the branches pre-committed in §0.3. Nothing in this part touches clause 2.

## 2.1 What was recomputable and what was not

Of the 41 frozen items: `H1`–`H21` are **Class A**, outputs of the authors' interval-arithmetic
computation, banked `UNRECOMPUTABLE-BY-DESIGN` — per §0.3's rule **"UNRECOMPUTABLE IS NOT NO"**,
being unable to re-run somebody's certified Julia job is not a finding that a constant fails. (The
authors' `README` requires *at least 800 GB RAM*; re-running was never on the table and its
impossibility is not evidence either way.) `H22`–`H41` are **Class B** and **all 24 were recomputed**
at 50 decimal digits from Class A alone, using the paper's own printed defining formulas.

Recomputation script: `experiments/p2_route_v5_v1_evidence.py`. It re-derives every row from the
banked artefact and exits non-zero on disagreement.

## 2.2 The recomputation table — BOTH NUMBERS FOR EVERY ROW

`rel` is the direction the paper claims. `holds` is whether the recomputed value satisfies the
printed claim.

| id | quantity | printed | recomputed (50 dps, shown to 12 s.f.) | rel | holds |
|---|---|---|---|---|---|
| `H22` | `K_4 = 3^{3/4}/(2^3 pi)` | `0.0907` | `0.0906987039818` | `~` | **YES** |
| `H25` | `K_5^U` | `0.47` | `0.4608` | `<=` | **YES** |
| `H24` | `M_1^U` | `0.9301` | `0.930048763877` | `<=` | **YES** |
| `H26` | `M_2^U` | `0.0061` | `0.006089` | `<=` | **YES** |
| `H27` | `M_3^U` | `0.22` | `0.213291464401` | `<=` | **YES** |
| `H28` | coefficient in the reduced `M_4^U` | `2.8e-6` | `2.6e-6` (`= 4 eps^U`) | `=` | **NO — F1** |
| `H29` | `M_4^U` | `0.002` | `0.002015438` | `>=` | **YES** |
| `H30` | `x_0^U` | `1.44e-5` | `1.45054706437e-5` | `<=` | **NO — F2** |
| `H31` | `x_1^U` | `6.2e-6` | `6.19510011291e-6` | `<=` | **YES** |
| `H32` | `y_0^U` | `1.3e-7` | `1.30549235793e-7` | `<=` | **NO — F2 propagated** |
| `H33` | `y_1^U` | `4.8e-7` | `4.82306898903e-7` | `<=` | **NO — F2 propagated** |
| `H34` | `abs(lambda^con)` | `1.62e-5` | `1.60939591834e-5` | `<=` | **YES** |
| `H35` | `eps_5` | `6.6e-5` | `6.58564267737e-5` | `<=` | **YES** |
| `H36` | `M_1^v` | `0.9301` | `0.930048763877` | `<=` | **YES** |
| `H37` | `M_2^v` | `0.0089` | `0.008889` | `<=` | **YES** |
| `H38` | `M_5` | `0.155` | `0.155082296226` | `>=` | **YES** |
| `H39a` | `M_0` | `6.4` | `6.38006585643` | `<=` | **YES** |
| `H39b` | `M_3^v` | `8.82` | `8.81645492394` | `<=` | **YES** |
| `H39c` | `eps^v + eps_5` | `8.5e-5` | `8.48564267737e-5` | `<=` | **YES** |
| `H39d` | `M_4^v` | `0.021` | `0.0210567838661` | `>=` | **YES** |
| `H40a` | `x_0^v` | `5.67e-4` | `5.65347963559e-4` | `<=` | **YES** |
| `H40b` | `x_1^v` | `4.3e-4` | `4.21048246141e-4` | `<=` | **YES** |
| `H41a` | `abs(lambda^lin(v))` | `0.0044` | `4.24671205354e-3` | `<=` | **YES** |
| `H41b` | `abs(lambda)` | `0.0045` | `4.26280601272e-3` | `<=` | **YES** |

**20 of 24 recompute. Four do not.** Rows `H31`–`H41b` are computed by propagating the *corrected*
`x_0^U`, not the printed one — that is the point of the table, and it is why `H31` and everything
below it still holds.

## 2.3 THE FOUR FAILURES, WITH THE FAILING CLAUSE QUOTED VERBATIM

Quoting discipline per §0.6 and dispatch reading (d). Every quote below is **character-exact from the
authors' own LaTeX source** `main.tex`, with a line number; LaTeX markup is reproduced as written,
because reproducing it is what makes the quote exact. No quote is taken from `pdftotext` output.

### F1 — `H28`: `2.8 x 10^-6` where the paper's own definition gives `2.6 x 10^-6`

**Failing clause, character-exact, `main.tex` line 2247** (in `\subsubsection{Verification of
Proposition \ref{prop:U}}`, the sentence preceding Step 1):

`and we take $\eta_1 = 0.199$, $M_4^U$ reduces to`

immediately followed by, character-exact, `main.tex` line 2248:

`\[ M_4^U \geqslant (0.051 - M_2^U)^2 - 2.8 \times 10^{- 6} \times M_3^U . \]`

The quantity being reduced is defined a few lines above, character-exact, `main.tex` line 2242:

`\[ M_4^U = (\frac{1}{4} - \eta_1 - M_2^U)^2 - 4 M_3^U \varepsilon^U > 0. \]`

**Both numbers.** Paper: `2.8 x 10^-6`. Recomputed: `4 eps^U = 4 x 6.5 x 10^-7 = 2.6 x 10^-6`.
(The other substitution on the same line is right: `1/4 - eta_1 = 0.25 - 0.199 = 0.051` exactly.)

**Direction: CONSERVATIVE.** `2.8e-6 > 2.6e-6`, so more is subtracted and the stated lower bound on
`M_4^U` is weaker than the truth. **This failure cannot break the closure.** It is also *not used* —
the very next step (Step 2) re-derives `M_4^U` from the unreduced formula with
`4 x 0.22 x 6.5 x 10^{-7}`, which is correct. So F1 is a **transcription slip in a displayed
intermediate that the proof then bypasses**. I record it because the gate says recompute every
recomputable constant, and this one does not recompute.

### F2 — `H30`: `0.005` substituted for a quantity the paper has just bounded by `0.0061`

**This is the one that matters.** Failing clause, character-exact, `main.tex` lines 2278–2280
(in `\paragraph{Step 3: Estimate of $x_0^U$ and $x_1^U$.}`):

`\[ x_0^U = \frac{2 \varepsilon^U}{\left( \frac{1}{4} - \eta_1 - M_2^U +`
`   \sqrt{M_4^U} \right)} \leqslant \frac{2 \times 6.5 \times 10^{- 7}}{0.25 -`
`   0.199 - 0.005 + \sqrt{0.002}} \leqslant 1.44 \times 10^{- 5}, \]`

The formula's third denominator term is `- M_2^U`. The substitution's third denominator term is
`- 0.005`. But `M_2^U` was bounded a few lines earlier, character-exact, `main.tex` lines
2270–2271, at the end of Step 1:

`\[ M_2^U \leqslant 0.005 \times 0.9301 + 5.3 \times 10^{-5} \times 4.5 + 0.0012`
`   \leqslant 0.0061. \]`

so the substituted value should be `0.0061`, not `0.005`. **`0.005` is `eta_2`** — the `L^2`
coefficient of the `E`-norm, quoted character-exact from `main.tex` line 2255:
`where $\eta_2 = 0.005$ is the $L^2$ coefficient of $E$-norm in \eqref{eq:eig M}.` The two constants
have been transposed. **PARAPHRASE, LABELLED AS SUCH:** the substitution appears to have taken the
first factor of the `M_2^U` product in place of the product's bound.

**Both numbers.** Paper: `x_0^U <= 1.44 x 10^-5`. Recomputed with `M_2^U <= 0.0061`:
`x_0^U <= 1.45054706437 x 10^-5`. (With the paper's own substituted `0.005` one does get
`1.432959125 x 10^-5 <= 1.44 x 10^-5`, so the printed arithmetic is internally consistent with the
wrong substitution — this is a substitution error, not an arithmetic error.)

**Direction: NON-CONSERVATIVE.** Using `0.005 < 0.0061` makes the denominator larger and `x_0^U`
smaller. The printed bound `1.44e-5` is **below** the value the paper's own inputs support. This is
the failure mode that matters, and it is the reason clause 1 could not be answered by inspection.

### F2-a and F2-b — `H32`, `H33`: `y_0^U` and `y_1^U`, both inheriting F2

Failing clauses, character-exact, `main.tex` lines 2287–2292:

`     y_0^U\assign\| U_2 \|_{L^2} & \leqslant & \| U_1 \|_{L^2} \| G_{Q^2}^U \|_F \sqrt{\sum_{j`
`    = 1}^m \| \overline{\phi}_j^U \|_{L^2}^2}  \leqslant 1.44 \times 10^{- 5} \times 0.025 \times 0.36 \leqslant 1.3 \times 10^{-7} ,\\`
`    y_1^U\assign\| \nabla U_2 \|_{L^\infty} & \leqslant & \| U_1 \|_{L^2} \| G_{Q^2}^U \|_F \sqrt{\sum_{j`
`    = 1}^m \| \nabla \overline{\phi}_j^U \|_{L^\infty}^2}  \leqslant 1.44 \times 10^{- 5} \times 0.025 \times 1.33 \leqslant 4.8 \times 10^{-7}  .`

**Both numbers.**
- `y_0^U` — paper: `1.3 x 10^-7`. Recomputed: `1.30549235793 x 10^-7`.
- `y_1^U` — paper: `4.8 x 10^-7`. Recomputed: `4.82306898903 x 10^-7`.

Both fail **only** because `1.44e-5` is carried in as the bound on `\| U_1 \|_{L^2}`. The
multiplications themselves are exact. These are propagated failures, not independent ones. I count
them separately because the gate says *name any constant that fails to recompute*, and these two
printed values are false as printed.

## 2.4 DOES THE CERTIFICATE STILL CLOSE? THE CORRECTED CHAIN, CARRIED ALL THE WAY THROUGH

The whole question is whether F2's non-conservative direction propagates into a broken inequality.
It does not. Carrying `x_0^U <= 1.45054706437e-5`, `y_0^U <= 1.30549235793e-7`,
`y_1^U <= 4.82306898903e-7` through **every** downstream step (rows `H31` onward in §2.2 are exactly
this computation):

| gate the paper needs | needed | corrected value | margin |
|---|---|---|---|
| `M_4^U > 0` | `> 0` | `0.002015438` | unaffected by F2 (`x_0^U` is downstream of `M_4^U`) |
| `x_1^U <= 6.2e-6` | `<= 6.2e-6` | `6.19510011291e-6` | holds, **0.08 %** to spare |
| `eps_5 <= 6.6e-5` | `<= 6.6e-5` | `6.58564267737e-5` | holds |
| `eps^v + eps_5 <= 8.5e-5` | `<= 8.5e-5` | `8.48564267737e-5` | holds |
| `M_5 >= 0.155` | `>= 0.155` | `0.155082296226` | holds |
| `M_4^v > 0` | `>= 0.021` | `0.0210567838661` | **holds — this is the closure condition** |
| `abs(lambda) <= 0.0045` | `<= 0.0045 << 0.113` | `0.00426280601272` | holds |

**The two closure conditions `M_4^U > 0` and `M_4^v > 0` both survive the correction**, with
`M_4^v = 0.02106` against a requirement of positivity. The certified eigenvalue perturbation
`abs(lambda) <= 0.00426` remains far below `abs(lambda-bar) ~ 0.113`, so the eigenvalue stays
unstable.

The tightest surviving margin in the whole chain is `x_1^U`: `6.19510e-6` against a printed
`6.2e-6`, i.e. **0.08 %**. It survives only because the paper was *simultaneously* conservative
there — the `x_1^U` substitution drops the `- M_2^U` from its own numerator (see O1 below), which
inflates the bound by about 6 %, far more than F2's 0.73 % deflation. Two errors in opposite
directions. That is luck, not margin, and I record it as such.

## 2.5 Two further substitution mismatches, both CONSERVATIVE — recorded, not counted as failures

- **O1.** `main.tex` lines 2282–2284: the formula is
  `x_1^U = \sqrt{\frac{\frac{1}{4} - \eta_1 - M_2^U}{1 - M_1^U}}` but the substitution is
  `\sqrt{\frac{\frac{1}{4} - 0.199}{1 - 0.9301}}`, character-exact — the `- M_2^U` is dropped from
  the numerator. Dropping a positive subtrahend from a numerator under a square root makes the
  bound **larger**, so this is conservative. Recomputed both ways: with `- M_2^U` the prefactor is
  `0.802283`, without it `0.854174`; the paper uses the larger.
- **O2.** `main.tex` line 2373: `\leqslant  6.4 \times (1 + 0.18) \times 5.67 \times 10^{- 4}`,
  where the bracketed quantity is `1 + 1.02 \sqrt{0.029} = 1.173700...`; `0.18` rounds `0.1737` up.
  Conservative.

Neither is a failure to recompute a *value*; both are substitutions that do not match their own
formula but err in the safe direction. **They are recorded because an audit that reported F1 and F2
and silently passed over O1 and O2 would be selecting its evidence.**

## 2.6 THE LOCALISATION STEP, CHECKED AT FULL TEXT (Remark 2 with §2) — `HE`

The gate names this step specifically. **Remark 2, character-exact, `main.tex` lines 250–256:**

`\begin{remark}`
`  Note that due to the slow decay of $u_{\tmop{in}} = O ( \frac{1}{| x |}`
`  )$, the solution is not in $L^2$ and thus not a`
`  Leray--Hopf solution. However, in {\cite{jia2015incompressible}}, the authors`
`  showed that such solutions can be localized to yield valid Leray--Hopf`
`  solutions. `
`\end{remark}`

This is a *pointer*, not an argument; the argument is §2.2 (`\label{Sec2:nonuniqueness}`). Read at
full text, link by link:

| link | content | status |
|---|---|---|
| `HE-1` | Bogovskii: `u_in = u_loc + w`, `u_loc` compactly supported and divergence-free, `w = 0` for `abs(x) < R`, `norm(w,L^p) <~ R^{-(p-3)/p}` for `p>3` | **CHECKED.** Recomputed the scaling independently: for `u_in = O(1/abs(x))`, `int_{abs(x)>R} abs(x)^{-p} dx = 4 pi R^{3-p}/(p-3)`, so `norm(w,L^p) ~ R^{(3-p)/p} = R^{-(p-3)/p}`. Exact match. Existence of the Bogovskii decomposition itself is cited to `jia2015incompressible`: **TAKEN-ON-TRUST** |
| `HE-2` | `u^cut = -e^{t Delta} w`; the estimate `norm(U^cut, W^{k,r}) <~ t^{1/2 - 3/(2p)} norm(w,L^p) <~ (e^{tau/2}/R)^{1-3/p}` | **CHECKED, exactly.** Composing the printed heat-smoothing exponent `-k/2 - (3/2)(1/p - 1/r)` with the printed similarity scaling `t^{k/2+1/2}` and `t^{-3/(2r)}` gives net `t^{k/2 + 1/2 - 3/(2r) - k/2 - 3/(2p) + 3/(2r)} = t^{1/2 - 3/(2p)}`. The `k` and `r` dependence cancels identically — which is what makes the printed `W^{k,r}` claim uniform in `k` and `r`, as stated. And `t = e^tau` with `(p-3)/p = 1 - 3/p` gives the printed right-hand side |
| `HE-3` | `L_Utilde - L_0` compact; essential spectrum in `Re(lambda) <= -1/4` | **TAKEN-ON-TRUST**, cited to `\cite{albritton2022non}` Section 4.1. **FLAGGED, not a gap:** ABC's profile is compactly supported whereas `Utilde` here decays only like `O(1/abs(xi))`. The paper handles exactly this objection for the *smoothing* estimate — character-exact, `main.tex`: `Compact support of $\widetilde{U}$ is not essential for the proof therein, as only its boundedness in $C^k$ norms is used in the standard estimates of energy dissipation along with a gain of regularity by the heat kernel, relating back to the equation in physical time.` — but does **not** repeat the remark for the compactness claim. The statement for a `1/abs(xi)`-decaying self-similar profile is available in `jia2015incompressible`, cited in the same paragraph. I record this as a **citation-precision observation**, not a mathematical gap |
| `HE-4` | Riesz projections `P_{lambda_j}`, finite rank, hence bounded; `X_j` finite-dimensional; `A_s` growth bound non-positive | **TAKEN-ON-TRUST**, cited (`engel2000one`, `albritton2022non` Lemma 4.2). Structurally correct; the finite-rank sentence is a v2 insertion in braces |
| `HE-5` | `alpha := 1 - 3/p = 1/4` at `p = 4` | **CHECKED.** `1 - 3/4 = 1/4`. Also `(p-3)/(2p) = 1/8 = alpha/2`, so the paper's two notations for the same exponent agree |
| `HE-6` | `delta = (1/2) min{(p-3)/(2p), min_j Re(lambda_j)} > 0`, and the integral lemma needs `delta_1 > delta` with `delta_1 = 1/8` | **CHECKED.** Requires `min_j Re(lambda_j) > 0`, supplied by Theorem 2 (see §2.7 on the sign convention). Numerically `min{0.125, ~0.113} = ~0.113`, so `delta ~ 0.0565 < 1/8 = delta_1`. The strict inequality the integral lemma needs holds, with room. **Observation:** `delta` is set by the eigenvalue here, not by `p` — the `min` does not select the term the notation suggests |
| `HE-7` | self-map: `C R^{alpha/2} sum_j norm(U_{j0}) + 2 C sum_j R^{-alpha/2}(1 + norm(U,X) + norm(U,X)^2) <= 1/4 + 1/2 < 1` | **CHECKED.** With `C eta <= 1/4` and `C sum_j R^{-alpha/2} <= 1/12`: second term `<= 2 x (1/12) x 3 = 1/2`; `1/4 + 1/2 = 3/4 < 1`. The printed `1/4 + 1/2 < 1` is right |
| `HE-8` | contraction: `2 C sum_j R^{-alpha/2}(1 + norm(U,X) + norm(V,X)) <= 1/2` | **CHECKED.** `2 x (1/12) x 3 = 1/2` exactly. Banach applies |
| `HE-9` | infinitely many distinct solutions, distinguished by `P_j U(.,0) = U_{j0}` | **CHECKED**, immediate from the definition of `D_{R,eta,U_{j0}}` and `dim X_j >= 1` from Theorem 2 |
| `HE-10` | `u in L^inf L^2 cap L^2 H^1`: `norm(u^cor,L^2) = t^{1/4} norm(U^cor,L^2)`, `norm(grad u,L^2) = t^{-1/4} norm(grad U,L^2)` | **CHECKED.** `t^{-1/2} . t^{3/4} = t^{1/4}` and `t^{-1} . t^{3/4} = t^{-1/4}`; and `int_0^1 t^{-1/2} dt = 2 < inf`, so `L^2 H^1` holds |
| `HE-11` | `L^s L^q` for `q >= 2`, `3/q + 2/s > 1` from `norm(u,L^q) <~ t^{3/(2q) - 1/(2(1-eps/3))} + 1` | **CHECKED, and the STRICTNESS is load-bearing and correctly used.** Time-integrability needs `s(1/(2(1-eps/3)) - 3/(2q)) < 1`. As `eps -> 0` this is `s(1/2 - 3/(2q)) < 1`, i.e. `3/q + 2/s > 1` — **strictly**. At the Prodi-Serrin equality `3/q + 2/s = 1` the exponent is exactly `-1` and the integral diverges logarithmically. The theorem claims only the strict side and its own sentence says so, character-exact, `main.tex` line 92: `They just miss the Prodi--Serrin condition that guarantees a unique solution, see \eqref{eq:Serrin}.` **The claim is exactly what the arithmetic supports and no more.** This is the single place where an over-claim by one endpoint would have made Theorem 1 contradict strong-weak uniqueness, and the paper does not make it |
| `HH` | the **energy inequality** — clause 2 of the paper's own Leray-Hopf definition | **TAKEN-ON-TRUST.** Discharged in one sentence, character-exact, `main.tex`: `Their smoothness implies that they are moreover suitable and \eqref{suitability} holds with equality, as in \cite{albritton2022non}.` Smoothness for `t>0` gives energy *equality* on `[t_0,1]`, and `limsup_{t->0} norm(utilde + u^cut - u_loc, L^2) -> 0` together with `norm(u^cor,L^2) -> 0` closes the gap to `t=0`. The chain is complete but is **asserted by analogy, not written out**. The smoothness bootstrap it rests on is itself a braced v2 sketch (`we can bootstrap from ... via the smoothing estimates`) |

**No GAP was found in the localisation step.** Two links are `TAKEN-ON-TRUST` by citation
(`HE-3`, `HE-4`) and one (`HH`) is discharged by a one-sentence appeal to `albritton2022non`. Every
link that carries an *arithmetic* claim was recomputed and every one of them checks.

## 2.7 One thing I checked because it looked wrong and is not

`lambda-bar ~ -0.11314203274385938 <= -0.113` is **negative**, which would appear to certify a
*stable* mode. It does not. The eigenvalue system `\eqref{eq:NS eq eig}` carries `\widetilde\lambda
< 0` in its own statement, and the convention is declared, character-exact, `main.tex` line 288:

`We remark that we flip a sign of the linearized operator in the second equation of \eqref{eq:NS eq eig} due to the convenience of elliptic estimates.`

So the eigenvalue of `L_Utilde` is `-lambda-tilde ~ +0.113 > 0`: **unstable**, as required by `HE-6`
and by Theorem 2's role. The sign convention is stated where it is used and is used consistently.
Recorded so that a later reader does not re-raise it.

## 2.8 CLAUSE 1 — THE VERDICT

> **"Does the certificate close as claimed — YES or NO?"**

# YES.

Against §0.3, this is branch **`B1-PARTIAL`**, whose pre-committed reading was: *constants fail but
the closure survives once corrected — report YES with the failures named.* I report it as **YES**
because the pre-registration fixed, before I read anything, that a `B1-PARTIAL` finding resolves to
the verdict the closure supports, with the defects named in full and not buried.

**Four printed constants do not recompute** — `H28` (`2.8e-6` for `2.6e-6`), `H30`
(`x_0^U`: `1.44e-5` printed, `1.45054706437e-5` recomputed), `H32` (`y_0^U`: `1.3e-7` printed,
`1.30549235793e-7` recomputed), `H33` (`y_1^U`: `4.8e-7` printed, `4.82306898903e-7` recomputed).
All four trace to **two** transcription defects, F1 and F2, of which only F2 is non-conservative.
**Corrected, every closure inequality still holds**, including both `M_4^U > 0` and `M_4^v > 0`, and
the eigenvalue remains unstable. The localisation step closes with no gap found.

**What this does NOT mean, per dispatch reading (a), stated because I am required to state it:**
a YES on clause 1 **does not move wall W3 by itself**. W3 stands on a prose test by user ruling and
this unit measured constants, not that test. Nothing here vacates or occupies any census cell either:
`2509.25116` occupies the Grade-A x fluid cell *as a matter of grading*, and a clause-1 YES leaves
that exactly where it was. Per reading (h): **no link of the L1->L4 chain moved.** This is a Tier-2
audit of somebody else's certificate and it is not a step toward Clay.

**What it DOES mean.** The Grade-A x fluid cell is now occupied by a paper that has been read at
full text and recomputed rather than only graded, and the occupancy no longer rests on the abstract.
The four defects are now on the record with both numbers, so a future unit does not re-derive them.
And **the certificate is thinner than it looks**: `x_1^U` survives with 0.08 % margin only because a
conservative slip (O1) happened to be larger than a non-conservative one (F2). That is a fact about
the *write-up*, not about the underlying interval computation, which I could not and did not re-run.

---

# PART 3 — CLAUSE 2: THREE-DIMENSIONALITY, ANSWERED ON ITS OWN

**This part is answered entirely on its own.** Nothing in PART 2 is used as an input here and
nothing here is folded back into PART 2. The gate says so and §0.4 pre-committed it: *"THIS ANSWER
MUST NOT BE FOLDED INTO THE CLAUSE-1 VERDICT."* Clause 1 could have returned NO and this part would
read identically.

## 3.1 The arbiter: W2's pre-committed test, located in the record

Per §0.2, W2's test **was located** and is not invented. It is
`writeup/data/p2_route_t4_v1.json::controls.C_3D`, with the operationalised six-check table at
`experiments/journal/verify_wave2.md` §2.1. The two character-exact quotes anchoring it are already
banked in §0.2 of this journal and are not re-quoted here. §0.2.1 restated it as the predicate:

> A certified profile is a **2D LIFT** iff **(L1)** it has no dependence on one Cartesian
> coordinate, **(L2)** `u^(3) = 0` identically, and **(L3)** `omega^(1) = omega^(2) = 0`
> identically with only `omega^(3)` non-vanishing. It is **genuinely 3D** iff the conjunction
> fails.

W2 applied exactly this to two certified rows of `arXiv:1902.00384` and measured them to be 2D
lifts. That is the failure mode this clause exists to catch.

## 3.2 Which rule applies — fixed before the object was known

§0.2.1 pre-committed **R2-a**: *if the authors ship decodable arrays, apply W2's test to the decoded
data, not to the declared ansatz.* They do. **R2-a applies; R2-b (declared ansatz) does not.**

§0.2.1 also pre-committed **R2-c**, and it is worth restating that it was written **before I knew
this paper was axisymmetric**: *an axisymmetric profile, with OR without swirl, is 3D under W2's
test, because W2's test keys on Cartesian observables and not on the count of independent variables;
the reduced-variable fact is reported as a flagged OBSERVATION and not folded into the verdict.*
That ordering is the whole reason this clause can be trusted — the axisymmetric case was adjudicated
in advance, not after inspection.

## 3.3 The object, and the proof that it is the certified one

| file | `sha256` (first 16) | source |
|---|---|---|
| `data/UP.mat` (the profile `U-bar`) | `c654a7f3ae86446e` | `github.com/HouGroup2026/3d-navier-stokes-nonuniqueness`, named in the paper's abstract |
| `data/up_eig.mat` (the eigenpair `v-bar`, `lambda-bar`) | `8b72114a0856615a` | same |

**These files are the certified object, and here is the check that proves it.** `up_eig.mat`
contains a scalar field `lambda` whose decoded value is

`0.1131420327438595`

against the paper's printed `\overline{\lambda}   \approx   - 0.11314203274385938`
(character-exact, `main.tex` line 2196; the sign is flipped by the convention declared at line 288
and analysed in §2.7). **Sixteen significant figures agree.** The shipped arrays are the arrays the
paper certifies. This check was not available a priori and it is what makes R2-a legitimate here.

## 3.4 THE MEASUREMENT

Decoded with `scipy.io.loadmat`. Components follow the authors' own naming, fixed by `src/My_Vector.jl`
line 103, character-exact:

`    # u1 and u2 are the radial and angular components, need to transform to spherical harmonic basis`

and by the divergence-free basis of §4.6, where the `Y` family is `S_l e_r`, the `X` family is
`S_l' e_theta` and the `Z` family is `S_l' e_phi`, with the `X` family carrying `M+1` modes against
the `Y` family's `M`. The decoded shapes match that exactly — `u2` is `(301,150)` where `u1` and
`u3` are `(300,150)` — so **`u1 = u_r`, `u2 = u_theta`, `u3 = u_phi`**, the azimuthal (swirl)
component. Independent structural confirmation: `from_mat_file` calls `set_conv_mat!` on `u1` and
`u2` only, and then `set_div_free!(up)`, which the source documents as updating `u2`. For an
axisymmetric field `div u` involves only `u_r` and `u_theta`; `u_phi` is unconstrained by
incompressibility. The code treats `u3` as exactly that free component.

### The profile `U-bar` (`UP.mat`)

| component | array shape | `max abs` | `l2` of coefficients | non-zero coefficients |
|---|---|---|---|---|
| `u1 = u_r` (radial) | `(300,150)` | `15.291462690010507` | `22.0333096357` | `45000 / 45000` |
| `u2 = u_theta` (polar) | `(301,150)` | `3.036715501940647` | `5.04228112465` | `45150 / 45150` |
| **`u3 = u_phi` (AZIMUTHAL / SWIRL)** | `(300,150)` | **`8.754198315291823`** | **`10.1889313589`** | **`45000 / 45000`** |
| `p` | `(300,150)` | `28.482734829516136` | `50.1300770951` | `45000 / 45000` |

### The eigenfunction `v-bar` (`up_eig.mat`)

| component | `max abs` | `l2` | non-zero |
|---|---|---|---|
| `u1 = u_r` | `0.1979578133558578` | `1.42824177851` | `45000 / 45000` |
| `u2 = u_theta` | `0.03216534366937078` | `0.115904372088` | `45150 / 45150` |
| `u3 = u_phi` | `0.002229944469064202` | `0.00903121395957` | `45000 / 45000` |
| `p` | `0.2397703209030848` | `1.56452645992` | `45000 / 45000` |

The `theta`-mode extent is **150**, not 1, in every component of both objects.

**The swirl of the profile is not a rounding artefact.** `max abs(u_phi) = 8.754`, which is
**57 % of `max abs(u_r)`** and **2.9x `max abs(u_theta)`**. It is a first-order feature of the
certified profile. (The eigenfunction's swirl is comparatively small, `l2` about 0.6 % of its
`u_r` — recorded, but the object clause 2 asks about is the *profile*, and in any case non-zero is
non-zero.)

## 3.5 W2'S PREDICATE, APPLIED CONJUNCT BY CONJUNCT

| conjunct | W2's operational check | measured | 2D-lift? |
|---|---|---|---|
| **(L1)** no dependence on one Cartesian coordinate; operationally `N_x3 = 0`, extent 1 | `theta`-mode extent | **150**, all `45000`/`45150` coefficients non-zero in every component | **FAILS** — the profile depends on `r` and `theta`, i.e. on `(rho, z)`, i.e. on all three Cartesian coordinates through `rho = sqrt(x^2+y^2)`. There is no Cartesian coordinate it is independent of |
| **(L2)** `u^(3) = 0` identically | `max abs` of the third velocity component | **`8.754198315291823`**, `45000` non-zero coefficients | **FAILS** — decisively. This is the conjunct W2's own two 2D-lift rows failed on |
| **(L3)** `omega^(1) = omega^(2) = 0`, only `omega^(3)` non-zero | see below | all three non-zero | **FAILS** |

**On (L3), separating measurement from derivation, per §0.6.** *Measured:* `u_phi` is not identically
zero, and neither is the poloidal pair `(u_r, u_theta)`. *Derived* — and I label this a **derivation
from exact identities, not a measurement:* for an axisymmetric field in cylindrical coordinates,
`omega_rho = -d_z u_phi`, `omega_phi = d_z u_rho - d_rho u_z`, `omega_z = (1/rho) d_rho (rho u_phi)`.
A non-zero swirl forces `omega_z` non-zero unless `rho u_phi` is `rho`-independent, i.e.
`u_phi = c/rho`, which is excluded by the axis condition the paper itself imposes — character-exact,
`main.tex` §4.3: `\widetilde{U}_{\theta} (r, 0) = \widetilde{U}_{\phi} (r, 0)` `= 0`. And it forces
`omega_rho` non-zero unless `u_phi` is `z`-independent, which together with far-field decay would
force `u_phi = 0`. The poloidal part gives `omega_phi` non-zero. So all three Cartesian vorticity
components are non-vanishing.

**I flag that (L3) is not load-bearing here.** (L1) and (L2) each fail independently and on direct
measurement alone, so the conjunction is already broken before (L3) is reached. (L3) is reported
for completeness, not because the verdict needs it.

**The conjunction (L1) AND (L2) AND (L3) fails on all three conjuncts.**

## 3.6 The flagged observation R2-c requires, NOT folded into the verdict

**OBSERVATION.** The certified profile is axisymmetric and therefore a function of **two**
independent variables `(r, theta)`, not three. The paper says so itself — character-exact,
`main.tex` line 2509: `We consider the axisymmetric case throughout the computations and denote the
cylindrical coordinates as $(\rho,z,\phi)$.` — and the computational saving is stated at line 2192,
character-exact: `we remark that our verifications are carried out effectively in a subspace of`
`$H^1_{\sigma}$, leveraging axissymmetry and parities, to reduce the number of` `basis functions in
our finite-rank approximation.` (The misspelling `axissymmetry` is the authors'.)

**Per R2-c, pre-committed before this was known, this observation does NOT enter the verdict.**
W2's test asks about Cartesian observables, not about the number of independent variables. An
axisymmetric-with-swirl flow is a genuinely three-dimensional velocity field: all three Cartesian
velocity components and all three Cartesian vorticity components are non-vanishing, and the flow
depends on all three Cartesian coordinates. A unit that had reduced this to "two variables,
therefore 2D" would be applying its own test, not W2's, and would be doing so after seeing the
answer. **I record the observation, and I do not weigh it.**

## 3.7 The contrast that makes this measurement worth having

The paper's own description of the work whose profile *is* a lift, character-exact, `main.tex`
lines 98–99:

`They instead leveraged the unstable vortex solutions constructed by Vishik \cite{vishik2018instability,vishik2018instability1,albritton2021instability}`
`that are steady states of the 2D Euler equations, and lifted them to 3D axisymmetric vortex rings. The case of the unforced Navier--Stokes equations is much more challenging since it is almost impossible to find an explicit self-similar Leray--Hopf solution with an unstable eigenvalue. `

That is the shape of the failure mode wave 2 caught in `arXiv:1902.00384`: a 2D object lifted. I
make **no claim about the swirl content of Albritton-Brue-Colombo's background** — I did not measure
it and it is outside my read list. What I record is that **this** paper's profile was measured, has
swirl at 57 % of its radial amplitude, and solves the unforced 3D equation rather than being lifted
from a 2D one.

## 3.8 CLAUSE 2 — THE VERDICT, STANDING ALONE

> **"Is the certified profile genuinely 3D against W2's own pre-committed test — YES or NO?"**

# YES.

Against §0.4, this is branch **`B2-YES`**. W2's predicate fails on **all three** conjuncts, two of
them on direct measurement of the authors' own certified arrays. The decisive single number is
`max abs(u_phi) = 8.754198315291823` in `data/UP.mat` (`sha256` `c654a7f3ae86446e...`), whose
provenance as the certified object is established by the sixteen-figure agreement of that file's
companion `lambda` with the paper's printed `lambda-bar`.

**Per dispatch reading (b), stated plainly.** Reading (b) pre-committed that a **NO** here would be
the more consequential answer of the two, and would be a W2 finding rather than a W3 one. **The
answer is not NO.** So the consequence reading (b) was written to force does not arise:
**there is no 2D lift in the Grade-A x fluid cell.** I state this as plainly as I would have stated
the opposite, and I note that the pre-registration would have compelled me to state a NO even
against the clause-1 YES.

**What follows and what does not.** Nothing about wall W3 follows from this; W3 stands on a prose
test by user ruling and this clause measured a velocity field. Nothing about the census cell's
occupancy changes; it was and remains occupied as a matter of grading. Per reading (h),
**no link of the L1->L4 chain moved.** What is now on the record is that the Grade-A x fluid cell's
occupant has been measured against W2's arbiter rather than assumed, and that the specific failure
mode wave 2 found elsewhere is **absent** here.

---

# PART 4 — THE SECOND DELIVERABLE, THE BANKED ARTEFACT, AND THE GATE

*(INSERT-ONLY. PARTS 0–3 above are untouched. PART 0 was committed at `eb756b1`
BEFORE the paper was opened and contains no verdict; PARTS 1–3 carry the
enumeration, the clause-1 measurement and the clause-2 measurement in that
order. Nothing above this line has been edited.)*

## §4.1 What PART 4 is, and what it is not

PART 4 discharges the **second deliverable** — the wave-4 repair — and banks
the audit. It contains **no new adjudication of either clause**. Both verdicts
were fixed in §2.8 and §3.8 respectively and are reproduced here only so the
gate can be read in one place.

## §4.2 The wave-4 repair — `_v2` files issued, NO `_v1` edited

Standing rule (user ruling Q3) is **CORRECTION RECORD, ARTEFACT UNTOUCHED**.
Two new files were issued:

| file | `self_hash` | supersedes | discharges |
|---|---|---|---|
| `writeup/data/p2_route_v3_gradeA_v2.json` | `6322309acc4d8bf8` | `p2_route_v3_gradeA_v1.json` | D1, D2, D3, D4 |
| `writeup/data/p2_route_l2_decay_v2.json` | `9a58a36c81734066` | `p2_route_l2_decay_v1.json` (`fd5c410859eb764b`) | D5, D6, N1 |

Both carry `correction_record: "writeup/CORRECTIONS.md SS33"`.

**Both are DELTA documents, not re-issues of the whole artefact.** The dispatch
said "ONLY the fields §33 names". A delta is the only shape that can honour that
literally: a full re-issue would have silently re-asserted every field §33 does
*not* name, under a new hash, which is how a correction record becomes a fork.

`git status --porcelain` was run at the moment of the repair commit and showed
**exactly two untracked files and nothing modified.** No `_v1` was edited. This
is recorded because the constraint was absolute and an attestation with no check
behind it is worth nothing.

### §4.2.1 Why every quote was re-anchored against LaTeX, not extracted text

D3's defect was that `linear` had been banked where the paper reads
`non- linear`. Fetching the authors' own e-print settles what happened: the
LaTeX source contains the single word `nonlinear` and **no hyphen anywhere**.
The `non- linear` was manufactured by `pdftotext` at a rendered line break.

That is a **defect class, not an incident.** So all four D1–D4 anchors were
re-taken from LaTeX e-prints fetched today, with archive hashes banked in the
`_v2`. This eliminates the class at the root rather than patching one instance
of it. The same discipline killed the "in INTLAB" defect in D4, which turned out
to be a **splice of two sentences** eleven lines apart.

### §4.2.2 The one thing verified rather than trusted

§33 says the R6 second clause describes prior work "[69]". I did not take the
bracket number on trust. I counted `\bibitem` occurrences in the authors' own
source up to and including `MeRaRoSz19c`: **it is number 69.** §33 was right.
Recording this because a correction record that repeats an unchecked number is
just relocating the risk.

### §4.2.3 Two defects found that §33 did NOT name — flagged, not merged

1. R6's `failing_clause_second_quoted` has a single space in `can be treated`
   where the authors' source has a **double space**: `can be  treated`.
2. R5's quoted field upper-cases `INVISCID`; the authors write `inviscid`.
   Editorial emphasis inside bytes presented as a quotation is the same defect
   class as a dropped word, pointing the other way.

Neither was silently folded into the D2/D3 corrections. Both sit in a
`beyond_SS33_flagged_not_smuggled` field with the character-exact form supplied,
so a future unit need not re-fetch. **Widening a repair's scope without saying so
is how correction records rot.** Both are immaterial to the verdicts.

### §4.2.4 The D1–D4 scope mismatch, recorded and not ruled

The dispatch describes D1–D4 as concerning "`V3`'s rows about THIS PAPER … you
will have the full text open". **§33's D1–D4 name rows R7, R6, R5, R3, and none
of them is `2509.25116`.** They are `2509.14185`, `2208.09445`, `2305.05660v3`
and `2605.15149v1`.

I repaired **the rows §33 names**, because §33 is the authoritative defect table
and the deliverable is scoped to "ONLY the fields §33 names". I record the
mismatch rather than resolve it. It is not mine to rule.

### §4.2.5 What the D5/D6 repair actually costs

Not the pin. The pin holds: α is still exactly 1.

What it costs is the record's **honesty about trust**. The "at most 1" direction
rests on unproved one-sentence remarks in secondaries plus one reachable
ESS-author restatement — *not* on a primary text read here. The `_v1` understated
that; the `_v2` states it.

Two structural points, both now in the artefact:

- The direction runs through the **LOCAL suitable-weak-solution form** (Seregin
  `arXiv:math/0510396` §1 — no finite-energy hypothesis), **not** the global
  Leray–Hopf form. The object's global energy is **measured infinite** (the
  cutoff integrals grow linearly in radius: 1153.09, 125445.0, 1.2566e7,
  1.25664e9). **The object is not Leray–Hopf.** Saying "by
  Escauriaza–Seregin–Šverák" without qualification invited a form of the theorem
  the object provably fails.
- `independently` is **removed** from `the_pin`. Pineau–Vicol state it for
  rotated *globally* self-similar solutions, not DSS, and their route terminates
  at ESŠ as well. **Two secondaries citing the same unreachable primary are one
  line of evidence, not two.** Independent AUTHORS, not an independent PROOF.
  The DSS extension step is the *verifier's*, and is attributed as such rather
  than absorbed.

ESŠ is banked **UNREACHABLE at primary — never as a zero.** Re-stated in the
`_v2` because an UNREACHABLE quietly becoming a zero is the exact failure the
record exists to prevent.

N1: `326.875` per decade is marked **AMPLITUDE-DEPENDENT** and must not travel.
It scales as `c³` under amplitude rescaling. What travels is the
**log-divergence**; the amplitude-free model value is `4π ln10 = 28.935`. The
bill is not re-opened: it rests on the divergence, which is amplitude-free.

## §4.3 The banked audit artefact

`writeup/data/p2_route_v5_audit_v1.json`, `self_hash` **`ae2b95efcbe5209b`**,
computed as `sha256(json.dumps(doc_without_self_hash, sort_keys=True)).hexdigest()[:16]`.

It carries the gate verbatim, the pre-registration pointer to `eb756b1`, both
branch sets as pre-committed, the full Class-A input list, the 24-row Class-B
table **with machine-evaluable formulas**, the four failures with both numbers
and character-exact failing clauses, the localisation findings, the clause-2
measurements, the journal-ref observation, and readings (a)–(h) discharged.

## §4.4 The evidence script and its negative test

`experiments/p2_route_v5_v1_evidence.py` re-derives **every** Class-B constant
from the artefact's Class-A inputs alone, at 50 dps, and **exits non-zero on any
disagreement**. It runs **75 checks** and currently passes all of them, exit 0.

A passing test proves nothing unless it can fail. Four tampering tests were run
against a copy, each restored afterwards:

| tamper | result |
|---|---|
| flip `H30`'s recomputed value to the paper's printed number | **exit 1** |
| set `H30.agrees = true` (hide a failure) | **exit 1** |
| zero the swirl while keeping `CLAUSE_2 = YES` | **exit 1** |
| mutate a byte without refreshing `self_hash` | **exit 1** |
| control, artefact restored | **exit 0** |

The artefact was verified byte-intact afterwards: `self_hash` still reproduces as
`ae2b95efcbe5209b`.

One correction to the script during the run, recorded rather than hidden: the
first version compared at `1e-12` relative against values the artefact banks to
**12 significant digits**, i.e. it tested *below the banked precision* and
reported seven "disagreements" that were pure tolerance artefacts — every
re-derived number printed identically to its banked string. The comparator now
tests at the precision actually banked. **This was a defect in my test, not in
the numbers**, and it is exactly the kind of thing that, left unexamined, would
have been reported as a finding about the paper.

## §4.5 THE GATE

### CLAUSE 1 — **YES**, branch **B1-PARTIAL**

The certificate **does close as claimed**. Four printed constants fail to
recompute; carrying the corrected values through the entire chain, **both
closure conditions still hold**.

| id | what | printed | recomputed | direction |
|---|---|---|---|---|
| `H28` | coefficient in the reduced `M_4^U`, which *is* `4ε^U` | `2.8e-6` | `2.6e-6` | CONSERVATIVE |
| `H30` | `x_0^U` | `1.44e-5` | `1.45054706437e-5` | **NON-CONSERVATIVE** |
| `H32` | `y_0^U` | `1.3e-7` | `1.30549235793e-7` | propagated from `H30` |
| `H33` | `y_1^U` | `4.8e-7` | `4.82306898903e-7` | propagated from `H30` |

`H30` is the substantive one. **`η₂ = 0.005` has been substituted where
`M_2^U ≤ 0.0061` belongs** — two different objects that look alike. The smaller
was used, enlarging the denominator and shrinking `x_0^U`. Failing clause,
character-exact, `main.tex` lines 2278–2280:

> `0.25 - 0.199 - 0.005 + \sqrt{0.002}`

Corrected chain: `x_1^U = 6.19510011291e-6 ≤ 6.2e-6`, `ε₅ = 6.58564267737e-5`,
`M_5 = 0.155082296226`, `M_4^v = 0.0210567838661 ≥ 0.021`,
`|λ| = 0.00426280601272 ≤ 0.0045`.

**The 0.08% survival on `x_1^U` is luck, not margin.** It is stated that way in
the artefact and it is stated that way here. Had `H30` gone the other way by a
factor the size of the two conservative slips `O1`/`O2`, this row would have
gone red.

Localisation (Remark 2, §1.3, §2): **twelve links checked at full text, no gap
found.** The strictness of `3/q + 2/s > 1` is load-bearing — at equality the
time integral diverges logarithmically — and the paper claims only the strict
side. The apparently-stable eigenvalue is resolved by the authors' declared sign
flip at `main.tex:288`.

**What this does NOT do:** a YES does not move **W3**, which stands on a *prose*
test by user ruling that this audit did not run. A NO would not have vacated the
cell either, which is occupied as a matter of **grading**.

### CLAUSE 2 — **YES**, branch **B2-YES** — ANSWERED ON ITS OWN

**Not folded into clause 1, and not conditioned on it.** This verdict rests on
measurements of the shipped data alone.

W2's test **was located in the record** — `p2_route_t4_v1.json::controls.C_3D`
plus `verify_wave2.md` §2.1. **Nothing was invented.**

The shipped data **is** the certified object: `up_eig.mat`'s
`lambda = 0.1131420327438595` matches the paper's printed
`-0.11314203274385938` to sixteen significant figures.

W2's predicate **fails on all three conjuncts**:

- **L1** fails — θ-mode extent 150, not 1; 45000 of 45150 coefficients nonzero.
- **L2** fails — `max|u_φ| = 8.754198315291823`, **57% of `max|u_r|`**. Not a
  residue; a principal component.
- **L3** fails — swirl with θ-dependence generates poloidal vorticity.

**The profile is genuinely 3D.** The failure mode this clause exists to catch —
Vishik's 2D-Euler steady states lifted to swirl-free axisymmetric vortex rings,
which is what wave 2 measured in `arXiv:1902.00384` — is precisely the
construction whose `u⁽³⁾ ≡ 0`. This profile is not that.

Rule **R2-c** (axisymmetric with or without swirl counts as 3D) was frozen in
PART 0 **before the paper was opened**, so the axisymmetry the paper advertises
could not be used to bend the verdict in either direction. It was flagged and
**not weighed**.

Per pre-committed reading (b), a NO here would have been the more consequential
answer and a **W2** finding. The answer is YES, so **that branch is not taken:
there is no 2D lift in the Grade-A × fluid cell on this paper's account, and W2
gains nothing here.**

## §4.6 Journal-ref — OBSERVED, NOT GRADED, NOT RULED

`arXiv:2509.25116v2` carries **no `journal-ref`** in its arXiv metadata.
`PUB_0C_CENSUS_SPINE.md` §1 speaks of "any published work". That mismatch is an
**open user escalation**
(`writeup/escalations/ESCALATION_PUB0C_PUBLISHED_2026-08-18.md`).

Per dispatch reading (e): recorded, **not graded, not ruled, not stopped for.**

Adjacent, recorded for the same reason: `main.tex` line 3519 carries a
commented-out §7 sentence, character-exact —

> `%A subsequent version will report the final constants (we expect only minor adjustments) and the full error budgets from the completed verification.`

**Commented-out text is not a claim.** It is not in the rendered paper and this
unit does not treat it as one. It is recorded because it was found in the
load-bearing source file, and suppressing it would be the same defect class as
quoting it as though it were published.

## §4.7 What this audit did NOT establish

- It did **not** verify the authors' interval arithmetic (Class A, `H1`–`H21`).
  Those are UNRECOMPUTABLE-BY-DESIGN here and are banked as a **limit of this
  audit, never as a pass**.
- It did **not** re-run the Julia certification.
- It did **not** test W3's prose criterion, so it does **not** move W3.
- **No link of the L1→L4 chain moved.** Tier 2 is never a proof. Clay stays
  ~0.05%.

## §4.8 Self-termination

The gate is answered on both clauses, the second deliverable is discharged, the
artefact is banked and hash-verified, the evidence script passes and has been
shown able to fail. **V5 terminates here.** V5 does not pick the next unit and
has not re-planned. No Conductor file was touched; no `_v1` was edited; territory
was new files plus this journal only.
