# Leg 278 — ROUTE-SVSL: the declared-final, artifact-scoped PUB2 pass

**Gate: YES on all four clauses — the `seventy` typo is fixed at the one site the 2/2 split
identifies, the artifact-wide claim-sharing sweep finds 0 remaining cross-document
contradictions with its 1 unanchored residue banked to `CORRECTIONS.md` §7, leg 276's 122-row
trace re-runs at 122/122 with 185/185 numeric tokens byte-identical, and 0 arguments changed.**

Rework leg on a draft. Branch `leg/278-svsl-v1`, created fresh from `origin/main`.
Date 2026-08-07. **No GA compute. Nothing measured. 0 numbers invented.**

Full report: [`writeup/novelty/leg_278.md`](../../writeup/novelty/leg_278.md).
Register entry: [`writeup/CORRECTIONS.md`](../../writeup/CORRECTIONS.md) §7.

---

## 1. The answer, as magnitudes

| | |
|---|---|
| files in the artifact scope swept | **117** `.md` under `writeup/4_p2_lottery/` |
| claim keys swept | **36** |
| files sharing ≥ 1 claim with PUB2 | **11** |
| shared claims checked pairwise | **21** |
| cross-document contradictions found | **1** |
| cross-document contradictions fixed | **1 / 1** |
| correct `seven` sites touched | **0 / 2** |
| residues banked rather than deferred | **1**, with 2 locators |
| trace rows green | **122 / 122** |
| numeric tokens byte-identical pre/post edit | **185 / 185** |
| banked JSON values re-read and reproducing | **4 / 4** |
| lines of PUB2 changed | **1** |
| arguments / conclusions / gate answers changed | **0** |
| escalations | **0** |

## 2. What this leg actually did differently

`verify_276` §10 diagnosed the process defect behind six correction passes: **each pass scoped
its read-set to the previous pass's defect list**, so the sources that resolve a finding can sit
one grep away, in the same directory, indefinitely. Leg 278 scoped **by artifact** — all 117
markdown files in `writeup/4_p2_lottery/`, keyed on all 36 of PUB2's printed figures and named
claims — and that is the only reason the 2/2 split could be *confirmed* rather than inherited.

## 3. The one fix

`BLOG_P2_PUB2_V1.md:14` — *"about **seventy** work-legs"* → *"**seven successive** work-legs"*,
matching `BLOG_P2_PUB1_V1.md:9` verbatim and `TECHNICAL_P2_PUB2_V1.md:35`. Anchored to legs
51–57 by `BLOG_P2_ROUTECP_V1.md:12` and `TECHNICAL_P2_ROUTENG_V1.md:22–24`.

`TECHNICAL_P2_PUB2_V1.md:627` and `BLOG_P2_PUB2_V1.md:52` were **deliberately not touched** —
they carry the broader obstruction-span claim. A sweep of `writeup/`, `experiments/journal/` and
`reports/` returns **0** banked anchors for `seventy` against **2** for `seven`/51–57; that
asymmetry is banked in `CORRECTIONS.md` §7 rather than repaired by substituting one unsourced
number for another.

## 4. Ceiling

Read 117 markdown files, re-read 4 banked JSONs, changed 1 word. Measured nothing. Verified no
mathematics. Leg 176's gate answer, leg 127's theorem, leg 163's census and leg 182's `NO` are
untouched. `a = 0` only. **No dynamics run. No ban lifts. No route promoted. No link of the
`L1 → L4` chain moved. Clay odds `~0.05%`. No GA compute.**

**PUB2 is CLOSED from the record's side.** Submission is the user's action. An eighth correction
pass requires the **user's own sign-off** — a process rule, not a future leg's discretion.
