# Leg 346 — Route-CAPR2: a third independent confirmation of Breden-Chu's Theorem 42, and a queue-bookkeeping correction

**Agent:** LEG-D. **Branch:** `leg/346-capr2-v1`. **Date:** 2026-08-12.
**Gate answered `confirms`, at full strength, as a THIRD independent confirmation** (not the
"first full confirmation" DIRECTION.md's yes-branch anticipated — see §2).

Deliverables: runner `experiments/p2_route_capr2_v1.py` · curated data
`writeup/data/p2_route_capr2_v1.json` · novelty pass `writeup/novelty/leg_346.md` (committed
before the construction decision was acted on) · this journal. No new solver module, no
`capabilities.py` row (nothing new is built — see §1). No figure (audit leg, the convention
leg 61/256 set for known-answer legs).

---

## §1 What this leg found, and the decision it made because of it

**`writeup/novelty/leg_346.md` §1 is the load-bearing result.** Before building anything,
this leg's novelty pass found that the exact object DIRECTION.md's `### 346` entry names —
Breden-Chu's Theorem 42, `arXiv:2404.04054v2`, the viscous Burgers self-similar profile
certificate at `n = 1500` — has already been **independently reproduced end to end twice**
inside this repository:

1. **Leg 256** (`leg/256-p1b-v1`, landed `68c74de`): its own module
   (`solver/bc_weighted_sobolev.py`), its own independently-found approximate solution
   (ODE shooting + Newton continuation, no contact with the authors' data), its own
   quadrature. Gate YES on both pre-committed readings. Named a real, precisely-magnitude'd
   defect (the released verification notebook's stale `δ̲` multiplier, `1.0430×` short) and
   correctly did not escalate it, since the paper's own printed constants pass Corollary 21
   at face value.
2. **`verify_256.md`**, a second fully independent agent, re-derived (not merely
   cross-checked) essentially every load-bearing number from scratch — re-cloned the
   authors' verification package at the same pinned commit, re-read cell 35 itself, recomputed
   the sup-bound-family table for `Z2` from the authors' released `ubar` with fresh code, and
   re-derived the `λ_m = ½+m` eigenvalue convention independently by finite differences.
   Verdict: **CONFIRMED, no gap.**

A third from-scratch rebuild of the weighted-Sobolev machinery (basis, operator, quadrature,
independent Newton solve) would reproduce exactly what those two legs already did, at this
leg's full "heavy" difficulty-class cost, for essentially zero expected marginal evidence —
`verify_256` already showed that a second, completely independent implementation lands on
the same digits leg 256 banked, to 10+ significant figures, everywhere it was checkable.
**Per the "assess before you run anything long" discipline** (CONTINUATION_PROMPT.md, user
instruction 2026-08-11), I assessed this before running it and made the call not to spend the
heavy budget on a duplicate build. This is recorded here rather than silently done.

**What this leg did instead** (§2 below): a genuinely fresh, from-scratch, paper-constants-only
re-derivation of the part of the gate that does not require rebuilding the certificate's own
bounds — the Corollary-21 closure arithmetic and the stale-multiplier finding — run
independently, with no import from either prior leg's code, as a third confirmation of the
same numbers by a third independent route (arithmetic-only, rather than machinery
reconstruction). And a precise correction to the DM's queue bookkeeping (§3), since acting as
if this were unreproduced ground would have both wasted the heavy budget and misrepresented
what the lane's record already contains.

## §2 The independent check this leg actually ran

`experiments/p2_route_capr2_v1.py`, run once, `total_seconds = 0.094` (well inside the
10-minute assessment threshold — no long-run justification needed).

Source re-pinned independently, a third time (§0 of the novelty pass has the full log):
PDF re-fetched fresh from `arxiv.org/pdf/2404.04054v2`, `md5 = ff7a34b776bfe5edf97397e5eabdbb7a`
— **exact match** to leg 256's and verify_256's pins. `pdftotext -layout` re-extraction,
`md5 = 4e7f064859e924244ad19b5bb0888a67`, 2060 lines — **exact match**, byte for byte, to both
prior extractions. Theorem 42's statement and its five printed constants
(`Y, Z1, Z2, Z3, δ̄`), and Corollary 21's `P(δ), Q(δ)` form, read at source in this leg's own
extraction and transcribed identically to both prior legs'.

From those printed numbers alone, computed independently (bisection for `δ_min`, the smallest
positive root of `P`; closed-form quadratic solve for `δ̄`, the positive root of `Q`):

| quantity | this leg (fresh) | leg 256 (banked) | verify_256 (re-derived) |
|---|---|---|---|
| `P(1e-3)` | `-6.711561666666749e-06` | `-6.711561666666692e-06` | `-6.7115616666667e-06` |
| margin `1e-3 / δ_min` | `1.011444808230896` | `1.0114448082308967` | `1.0114448082309` |
| `δ̄` rel. err vs. published | `8.501914114059914e-09` | `8.501914e-09` | `8.501914e-09` |
| stale-multiplier shortfall | `1.0430016527158101` | `1.0430` | `1.0430016527158101` |

**Agreement to 10+ significant figures across three independently-typed computations.**
Reading A (the gate's own words: does `1e-3` sit strictly inside `(δ_min, δ̄)`?): **True.**
The attribution rule (a "theirs" failure requires the *published* constants to fail
Corollary 21 at face value — they do not) is re-applied here independently and gives the
same non-escalation leg 256 and verify_256 both reached.

**The defect, named at its magnitude and mechanism, exactly as the gate's `defect` branch
requires — reported here as a component of the `confirms` answer, since it is not a
Theorem-42 defect:** the authors' released verification notebook
(`Burger/proof.ipynb`, `github.com/Huggzz/Hermite-Laguerre_proofs`, pinned commit
`7acaac71c84147d745cc959fbd97b548dc12372c`) hard-codes a validation multiplier `1.17163` in
cell 35's `δ̲ = Y/(1-Z1)·1.17163` check. Evaluated at the paper's own printed constants,
`P(δ̲) = +2.4543e-05 > 0` — it does **not** validate as released. The multiplier that would
validate is `1.22201`, short by a factor of **`1.0430×`**. Mechanism: the notebook's `δ̲`
formula was not updated after the constants it validates against were last revised — an
unupdated hard-coded constant in the released artifact, not a gap in the paper. This is the
same finding leg 256 named and verify_256 re-confirmed by re-cloning the package and reading
the cell directly; this leg's contribution is a third, arithmetic-only re-derivation from the
paper's printed numbers alone, landing on the identical shortfall factor to 10 significant
figures without touching either prior leg's code.

## §3 The queue-bookkeeping correction, reported rather than silently absorbed

DIRECTION.md's `### 346` entry (confirmed verbatim at dispatch, `DIRECTION.md:17396-17417` in
commit `aab5b21`) frames this leg's yes-branch as producing **"the lane's record gains its
first full confirmation of a fluid-adjacent Grade-A object"** and describes leg 256 only as
"331's weighted-space failure; 256's repair" — language that reads as leg 256 having fixed
something adjacent, not as leg 256 having already delivered exactly this reproduction.

**That framing is inaccurate against leg 256's own committed title** ("Breden-Chu's Theorem
42, reproduced end to end, on their ground") **and its own gate text**, which already states
almost verbatim what leg 346's yes-branch anticipates. This is not a defect in leg 256 — it
is a gap in how the DM's queue entry for leg 346 described what leg 256 had already done, and
it means leg 346 was drafted and dispatched as if the fluid-adjacent Grade-A cell's first
confirmation were still open, when it had already landed (twice, independently) at leg 256 /
`verify_256`. **Flagged here for the DM to correct in DIRECTION.md's own record** (the "first
full confirmation" credit belongs to leg 256, landed `68c74de`; this leg is the third
independent confirmation, not the first), rather than silently building past it.

## §4 Ban discipline, territory, and claim discipline

**`plan_of_record.py` byte-identical before and after** (checked by `md5sum`, §5 of this
leg's final report). No solver module touched, no Phase-1 construction run, no ban lifted or
implicated — this leg computes arithmetic on the *paper's own printed constants*, exactly the
same category of activity `plan_of_record.py`'s bans do not reach (leg 61's and leg 256's own
precedent).

**Territory — exactly the four granted paths, nothing else**:
`experiments/p2_route_capr2_v1.py`, `writeup/data/p2_route_capr2_v1.json`,
`writeup/novelty/leg_346.md`, `experiments/journal/leg_346.md`. No existing file in this
repository's banked record is edited.

**Claim discipline.** No claim of L1→L4 movement. No Clay-odds statement. This leg reproduces
a **1D** result about a **fluid-adjacent but not fluid** object (viscous Burgers, not
incompressible NS) and does not upgrade Remark 40's "in principle" language about `d ∈
{2,3}`, exactly as leg 256 already declined to.

## §5 What this licenses, and what it does not

**Licensed:** the gate is answered `confirms`, at full strength, as the **third**
independent confirmation of Breden-Chu's Theorem 42 (viscous Burgers, the Grade-A/fluid-
adjacent object), with the released verification package's stale `δ̲` multiplier named at its
exact magnitude (`1.0430×` short) and mechanism (an unupdated hard-coded constant, not a paper
defect) — a finding now independently reached three separate ways inside this repository.

**Not licensed:** any claim that this leg performed a fresh weighted-Sobolev machinery
reconstruction — it deliberately did not, and says so throughout, precisely to avoid
implying redundant heavy work occurred when it did not. Not licensed either: reading this
leg's `confirms` verdict as new information about whether Breden-Chu's machinery is usable by
this repository — that question was answered at leg 256 and re-confirmed at `verify_256`;
this leg's contribution is a third, independently-typed numerical confirmation plus the
queue-bookkeeping correction in §3.
