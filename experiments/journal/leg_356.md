# Leg 356 — ROUTE-ESPX — journal

Territory: `experiments/p2_route_e_v1_spectrum.py` (generator, repair only), the banked
`writeup/data/p2_route_e_v1_spectrum.json` (regenerated content since the repair path landed),
`writeup/CORRECTIONS.md` (append), `writeup/novelty/leg_356.md`, this file. `solver/
rescaled_spectrum.py` is deliberately NOT touched (out of territory) even though the actual sort
call lives there — the fix is a post-processing step in the generator instead.

## 1. Reading the census (287) first

`writeup/data/p2_route_epa_v1_census.json` `families[0]` (`p2_route_e_v1_spectrum`) has:
- `comparison` (banked-at-c45e8189 vs regenerated-at-c45e8189-in-this-environment):
  `max_rel_move = 1.8797`, worst mover `E5_sweep[0].kept.0.1[5][1]` (banked `-8.1349`,
  regenerated `+7.1563`), 62 leaves over 10%, `n_flag_flips = 0`.
- `determinism_control` (two independent same-commit, same-environment regenerations):
  `two_processes_agree_on_nonvolatile_leaves: false`, `n_leaves_differing_between_repeats: 1`,
  `max_rel_move_between_repeats: 0.0`, `worst_between_repeats: null`.

The second one is suspicious on its face: `max_rel_move` exactly `0.0` with `worst_between_
repeats: null` means whatever differed was NOT a numeric leaf move at all (the compare() function
only sets `worst`/`max_rel` from `numeric_moves`). The `comparison` object's own `string_moves_
sample` (the only place a non-numeric diff would show up) lists exactly one entry: `{"path":
"generated", "banked": "2026-08-02T00:17:23Z", "regenerated": "2026-08-11T17:27:03Z"}`. 287's own
`VOLATILE_TOKENS` list (`experiments/p2_route_epa_v1_census.py`) has `"generated_at"` but not
bare `"generated"` — the actual JSON key this generator writes is `data["generated"] = ...`
(a plain ISO timestamp), which does not contain the substring `"generated_at"`. So the
determinism-control "failure" is 287's own filter missing its own vocabulary by one token, not a
real content disagreement. Recorded in CORRECTIONS.md §25 as a finding about the census script,
not a fix to it (out of this leg's territory).

## 2. Finding the real mechanism

Read `solver/rescaled_spectrum.py`'s module docstring FIRST (it states its own theory): at a=0
the linearization's spectrum is known in closed form — two isolated structural eigenvalues (0,
-1) plus a CONTINUUM filling `-1 < Re lambda < 1`, discretized as many eigenvalues pinned near
`Re lambda = 0`.

> **[CORRECTED 2026-08-12 by leg 378 (ROUTE-LCB6), applying item (10) of the DM's accumulator,
> finalized by leg 371 citing the 367+369+371 trilogy — original wording quoted above, not
> deleted.]** This leg's own framing (here and echoed in `writeup/CORRECTIONS.md` §25(a) as
> "sits on the imaginary axis in exact arithmetic (`Re lambda = 0`)") stated the discretized
> essential/continuum spectrum's pinning to `Re(lambda) = 0` as a settled, theorem-level fact.
> That overstates what the trilogy actually established. **Corrected status:** leg 367 found the
> claim UNVERIFIED as a theorem for the full truncated matrix — the module's own closed-form
> proof covers only the analytic-at-`X=0` subspace (the two structural eigenvalues), and says
> nothing about the other `K-2` eigenvalues `np.linalg.eigvals` actually returns; six natural
> skew-symmetrization mechanisms were tried and none derived the pinning, though the claim was
> empirically corroborated with ~6.5 orders of margin over `canonical_order()`'s `1e-6`
> threshold. Leg 369 then tried the generic route — standard non-normal eigensolver
> backward-error theory (LAPACK's own Table 4.5/4.6 bounds) — and found it CANNOT license the
> observed tightness: the matrix's condition numbers (~1e15) sit 4-5 orders outside the licensed
> precondition, and the only unconditional bound available is ~19 orders of magnitude too loose
> to be informative — that route was tried and foreclosed, GAP-NAMED. Leg 371 (the thread's
> terminal leg) then found the pinning IS derivable, but only from LAPACK's specific real-Schur
> numerical structure (`dlanv2.f`'s shared 2x2-block half-trace plus the Ahues-Tisseur
> machine-epsilon-local deflation criterion in `dlahqr.f`/`dlaqr3.f`), NOT from any generic or
> abstract argument — independently reproduced via `scipy.linalg.schur` (LAPACK `dgees`, a
> second driver), matching leg 367's four measured points to within a factor of 0.93-1.68x.
> `canonical_order()`'s repair is unaffected either way — it only ever needed the empirical floor
> to sit below `1e-6`, which holds on both the empirical and now the mechanistic ground. See
> `writeup/CORRECTIONS.md` §25's annotation and the new batched entry for the full trilogy
> citation. THE E5-FLOOR THREAD (legs 367, 369, 371) IS CLOSED.

`match_filter()` sorts kept eigenvalues by `-np.real(kept)` — a key that is
degenerate (all ~0, to noise) across that whole continuum piece.

Direct repro, bypassing the ~41-minute full pipeline: called `spectrum(0.0, 96)`, `spectrum(0.0,
144)`, `match_filter(ev_c, ev_f, 0.1)` directly (`/tmp/.../scratchpad/repro.py`). Kept-set real
parts: `1e-11 .. 1e-17` in magnitude, sign essentially arbitrary. Compared the resulting 16-entry
`kept["0.1"]` array's set of `|Im|` values against the banked `E5_sweep[0].kept["0.1"]`: IDENTICAL
multiset (`{0, 0.271, 0.840, 7.156, 8.135, 14.828, 23.008, 45.174}`, each ±), just reshuffled
across array positions with the paired sign swapped between banked and regenerated — exactly what
a noise-driven sort on a near-constant key produces. This is the direct, measured mechanism: not
"two eigenvalues crossing" (there is no genuine crossing at a=0 — the module's own closed-form
theory rules that out) but the WHOLE continuum sharing one degenerate sort key.

Confirmed `kept_ref` (the `tol=1e-2` list, which is what `fig34` actually plots) is NOT exposed to
this: at that tolerance only the two structural eigenvalues survive (`{~0, -1}`), both real, no
tie, always the same order.

## 3. Enumerating consumers

`grep -rln "p2_route_e_v1_spectrum\|fig34_p2_route_e_v1_spectrum\|route_e_v1\b"` across the repo
(excluding `.git`). Full list and per-file "what it reads" table in CORRECTIONS.md §25(b). The
two claim-bearing numeric consumers outside this family's own figure
(`p2_route_f_v1_viscosity.py`, `p2_route_j_v1_literature.py`, `test_literature_gates.py`) all read
only `E2_branch` and `E7_end.a_c_linear_extrapolation` — sections that never call `match_filter`
(they use `continuation()`/Newton directly) and are therefore mechanically immune to the ordering
bug, independent of whether it gets fixed.

## 4. Repair

Added `canonical_order(kept, dist, round_dp=6)` to `experiments/p2_route_e_v1_spectrum.py`
(module-local helper, `solver/rescaled_spectrum.py` untouched): re-sort by `(round(Re, 6),
-Im)`. 1e-6 sits ~5-11 orders of magnitude above the observed noise floor and well below any
genuine isolated real part this module has ever produced (0, -1, or a would-be Hopf pair). Applied
at both call sites inside `e5_sweep()` (`keeps[t]` and `kept_ref`).

Verified determinism directly, three independent processes, same environment:
- standalone `e5_sweep([...])` call #1: 149.8s, `/tmp/.../scratchpad/e5_run1.log`
- standalone `e5_sweep([...])` call #2: 131.9s, `/tmp/.../scratchpad/e5_run2.log` — `diff` against
  run #1 shows only the timing line differs, the entire printed spectrum (order and all) is
  identical.
- the full 11-section pipeline (`main()`), 2070.2s wall clock,
  `writeup/data/p2_route_e_v1_spectrum.json` — its `E5_sweep[0].kept["0.1"]` array compared
  programmatically against the standalone run's: `IDENTICAL: True`, byte-for-byte.

## 5. Re-banking and consumer diff-check

Regenerated the full artifact (`.venv/bin/python -u experiments/p2_route_e_v1_spectrum.py`,
2070.2s) — this IS the new banked `writeup/data/p2_route_e_v1_spectrum.json` (repair path landed,
so this is a content change per the territory rules, not a marker-only edit). Rebuilt
`fig34_p2_route_e_v1_spectrum.png` from it (`writeup/4_p2_lottery/p2_route_e_v1_evidence.py`) —
noted explicitly since the brief said touch fig34 only if the repair requires it: the JSON it
reads changed, so it was rebuilt for consistency even though (confirmed by inspection) no pixel
depends on the repaired leaf family.

Ran `test_literature_gates.py` (a plain script, not pytest-discoverable in this venv — no
`pytest` installed; ran it directly as `python3 test_literature_gates.py`) end to end against the
repaired banked JSON: `ALL GATES PASS (4s)`, all nine tests including `test_7_branch_against_xu`
(reads `E7_end.a_c_linear_extrapolation` directly) and `test_9_artifact_matches_the_module`.

Old-vs-new leaf diff (banked-at-c45e8189 vs repaired-today): `E2_branch` alpha values moved by
<3e-13 relative (environment float noise, same order of magnitude as everything else in this
repo's noise-level leaves); `E7_end.a_c_linear_extrapolation` moved by 2.4e-15 relative. Both
orders of magnitude below every consumer's actual tolerance (`5e-3`, `0.02`, `1e-12` against a
DIFFERENT reference (XU), not against the old banked value).

## 6. What was deliberately NOT done

- `solver/rescaled_spectrum.py`'s `match_filter()` itself is untouched — the fix lives entirely
  in the generator script, per territory. A future leg with `solver/` in its territory could push
  the canonicalization down into `match_filter` itself so every caller (including `E6_control`'s
  internal use via `planted_eigenvalue_control`) inherits it; flagged, not done here.
- `E6_control`'s `plain`/`planted` lists still go through the solver's raw `match_filter` (tight
  `tol=1e-3`, no ordering incident observed in the census or in this leg's regeneration) — left
  as a residual, out-of-territory risk, not evidenced as broken.
- `experiments/p2_route_epa_v1_census.py`'s `VOLATILE_TOKENS` near-miss (`"generated_at"` vs
  `"generated"`) that produced the false determinism-control alarm is NOT fixed here — that file
  belongs to leg 287's territory, not this leg's; recorded as a finding only.
- `writeup/data/p2_route_epa_v1_census.json` is read-only per this leg's brief and was not
  touched (its own `read_only_guarantee` SHA-256 manifest was not re-checked here since this leg
  never wrote to it).

## Ceiling

TIER 2. Reproducibility/record-integrity repair only. No proof, no certificate, no Clay claim.
Clay stays ~0.05%.
