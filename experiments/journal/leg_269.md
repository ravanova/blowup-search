# Leg 269 — ROUTE-J176P — fix leg 176's journal prose slip

**Branch** `leg/269-j176p-v1`, cut from `origin/main` at `06e96d2`. Date 2026-08-07.
**Difficulty** light. **Kind** DOCS-only.

**Gate.** *Does the journal prose match the underlying banked value at stated precision after
the fix, with a one-line correction note citing leg 249, and no other sentence's meaning
changed?*

## Gate answer: **YES**

## 1. The site

`experiments/journal/leg_176.md` L92, gate-conjunct-2 bullet block:

> max relative residual of `(L₀⁺u − f)(y)`: **4.767e−15** (max absolute `1.29e−14`), against leg
> 163's established **2.8e−14** class.

`1.29e−14` is the only wrong number. Repo-wide grep for that literal (both unicode-minus and
ASCII spellings) returns exactly **one** occurrence in prose that asserts it as a measurement —
this L92. The other hits are leg 249's finding, leg 268's mention, DIRECTION.md's leg spec, and
leg 249's verification script, all of which *quote the slip as a slip*. It never propagated: the
PUB2 pair quotes `σ_min = 0.0908…` and `‖T⁻¹‖_X = 4.026`, not this residual.

## 2. Independent confirmation — the JSON was re-read, not leg 249 trusted

Source of truth `writeup/data/p2_route_h2c_v1_construction.json`, object `C0_xu_closed_form`.
Every numeric claim in the same bullet block was re-checked at once, so the correction is not
made on a single-number look:

| journal prose | JSON key | JSON value | verdict |
|---|---|---|---|
| `4.767e−15` rel. residual | `max_rel_residual` | `4.766912579638455e−15` | agrees |
| ladder `5.289 / 4.767 / 5.160 e−15` | `quadrature_ladder_kpan/12,24,48` | `5.289131…/4.766912…/5.160134… e−15` | agrees, 3/3 |
| worst over six data `5.135e−15` | `over_seeds` max | `5.134937812813957e−15` | agrees |
| leg 163's class `2.8e−14` | `leg163_established_class` | `2.8e−14` | agrees |
| **max absolute `1.29e−14`** | `max_abs_residual` | **`1.4295603459064552e−14`** | **disagrees** |

Magnitude of the disagreement: prose low by `1.396e−15` absolute, **9.76 %** relative
(`|1.29 − 1.42956| / 1.42956`). Leg 249's stated 9.8 % is confirmed to the digit it gave.
5 of 6 neighbouring claims agree, so there is no second slip hiding beside this one.

## 3. The edit

`1.29e−14` → `1.4296e−14`, carried at 5 significant figures — one more than the block's own
`4.767e−15` convention, chosen so the prose value is unambiguously the banked one rather than a
rounding that could drift again (`1.43e−14` at 3 s.f. would be equally correct and less
traceable). A one-line correction note follows the bullet, naming leg 269 as the fixer, leg 249
§W7 as the finder, the JSON key path, and the 9.8 % magnitude.

## 4. No other sentence's meaning changed

Read the full paragraph before and after. Checks:

* The corrected `1.4296e−14` is still **below** leg 163's `2.8e−14` class (ratio `0.51`, versus
  `0.46` for the wrong value), so the comparative clause *"against leg 163's established
  2.8e−14 class"* keeps its direction and its force.
* The section heading *"Xu's closed form: **YES, and a decade better than the target**"* is about
  the **relative** residual `4.767e−15` against `2.8e−14`, untouched.
* `gate/conjunct_2_value` in the JSON is `4.766912…e−15` — the relative residual. **The repaired
  number is not a gate number**, exactly as leg 249 said (*"It touches neither gate number"*).
* Nothing else in the file was edited: the diff inside `leg_176.md` is one changed line plus four
  added note lines.

## 5. Deliberately NOT touched (out of territory)

`experiments/p2_route_h2cv_v1_postconstruction.py` L1280 hardcodes `1.29e-14` as *"what the
journal says"* and records the mismatch as leg 249's finding. That is a **historical record of a
run**, and rewriting it would falsify leg 249's evidence rather than repair anything; it is also
outside this leg's declared territory. It remains as-is by intent, not by oversight. Same for
`experiments/journal/leg_268.md` L93 and `DIRECTION.md` — both quote the slip as a slip.

## 6. Territory

Three paths, exactly the declared set: `experiments/journal/leg_176.md` (the slip site + note
only), `writeup/novelty/leg_269.md`, `experiments/journal/leg_269.md`. No shared ledger touched:
`experiments/JOURNAL.md`, `LITERATURE_CHECK.md`, `plan_of_record.py`, `DIRECTION.md`,
`CONTINUATION_PROMPT.md` are all unmodified. No solver, no test, no JSON.
