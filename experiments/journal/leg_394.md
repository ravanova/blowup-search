# Leg 394 — `T6`: discharge leg 348's own ceiling, at FULL TEXT

**Unit `T6`, Lane T, WAVE 2. Literature only.** Nothing built, no solver module written or edited,
no ban lifted, narrowed, re-read or argued against. `plan_of_record.py` untouched. **No figure**
(none allocated; territory forbids one).

**Object:** `experiments/journal/leg_348.md` and `writeup/data/p2_route_pocp_v1.json`. **Leg 348 is
this unit's OBJECT, not its territory — it is not amended here.** Anything leg 348 got wrong is
reported; the Conductor decides what happens to the record.

**Authorisation, stated because it is narrow.** The user narrowed the outreach hold on 2026-08-13
(`writeup/escalations/RULING_BAN_WORDING_2026-08-13.md` §(d)): **reading any published document is
authorised; CONTACTING an author, group, maintainer or mailing list remains HELD.** No email, no
issue, no form, no list post was sent by this unit — not even to request a PDF. A full text
obtainable only by asking a human is banked `UNREACHABLE`.

---

## §0. PRE-REGISTRATION — written and committed BEFORE the first fetch

Everything in §0 was committed before any network call of this leg. The verdict rules in §0.3 are
fixed here so they cannot be adjusted after a full text is seen; that adjustment is exactly how a
lane confirms itself.

### §0.1 THE GATE — final wording, pre-committed by the Conductor, restated verbatim, not re-scoped

> Read at **full text** each of the seven papers leg 348 classified at abstract level. For **each**,
> does the full text **confirm**, **strengthen**, or **UNDERCUT** leg 348's recorded classification
> of it? **Answer per paper, in a table, with the deciding sentence quoted verbatim and located by
> section or page.** A paper whose full text could not be obtained is banked as **`UNREACHABLE`**
> with the reason, **never as a confirmation.**

### §0.2 THE SEVEN PAPERS, WITH LEG 348'S RECORDED CLASSIFICATION QUOTED VERBATIM

Quoted from `writeup/data/p2_route_pocp_v1.json` (`located_technology[]`), which is the banked
record; the journal prose is cross-checked against it, never the other way round.

| # | id | leg 348's recorded classification — VERBATIM from the JSON |
|---|---|---|
| P1 | `math/0005247` | method: *"self-consistent a-priori bounds justifying rigorous Galerkin projection + Conley index"*; target: *"FIXED POINTS (not periodic orbits, per this paper's own abstract) of the Kuramoto-Sivashinsky equation"*; domain: *"canonical KS setting; abstract does not state BC explicitly but the method's 'Galerkin projection' + 'neglected modes' tail estimate is the standard periodic-domain Fourier-truncation construction in this line of work"* |
| P2 | `arXiv:2305.08221` | method: *"computer-assisted rigorous time integration via variation-of-constants zero-finding in Fourier space + Newton-Kantorovich, Chebyshev interpolation, domain decomposition"*; domain: *"EXPLICIT: 'scalar semilinear parabolic PDEs with periodic boundary conditions' (abstract, verbatim)"* |
| P3 | `arXiv:1902.00384` | method: *"constructive/computer-assisted existence proof, Newton-Kantorovich on a Banach space of geometrically decaying Fourier coefficients"*; domain: *"EXPLICIT: 'forced, autonomous Navier-Stokes equations on the three-torus' (abstract, verbatim); Taylor-Green forcing"*; relevance: *"the single most directly on-topic paper: a periodic orbit of genuine 3D NS (viscous term inside the certified equation), computer-assisted, but on a COMPACT domain via the same NK-in-exponentially-decaying-coefficient-basis family already measured DEAD in this repository (4 realizations, stationary-profile problem)"* |
| P4 | `arXiv:2409.09234` | method: *"rigorous chaos statement via a discrete map approximation, unstable periodic orbits (UPOs) of NS"*; domain: *"EXPLICIT: 'a minimal periodic domain capable of sustaining coherent structures' (abstract, verbatim)"* |
| P5 | `arXiv:2105.04148` | method: *"computer-assisted proof of branches of stationary/periodic solutions and Hopf bifurcations"*; domain: *"EXPLICIT worked example: 'the Brussellator system with diffusion and Dirichlet boundary conditions' (abstract, verbatim) -- bounded, not periodic, not unbounded"* |
| P6 | `arXiv:2009.12762` | method: *"constructive, computer-assisted estimates; Hopf bifurcation producing a periodic cycle from a stationary branch"*; domain: *"EXPLICIT: 'Navier-Stokes equation for an incompressible viscous fluid on a square, satisfying Navier boundary conditions' (abstract, verbatim) -- 2D, bounded"* |
| P7 | `arXiv:2308.01528` | method: *"purely analytic fixed-point method, refining an earlier computer-assisted proof"*; domain: *"EXPLICIT: 'the 1D Hou-Luo model on the real line' (abstract, verbatim) -- unbounded, with 'rigorous estimates on the algebraic decay rates of the profiles in the far field'"*; relevance: *"the one located instance combining unbounded domain + algebraic far-field decay + a rigorous/computer-assisted existence result -- but STATIONARY profile, 1D, no Leray projector, no axisymmetry question"* |

**The load-bearing aggregate claim** those seven support, quoted verbatim from
`p2_route_pocp_v1.json` `obstruction_named.statement`, is what an undercut ultimately bites:

> *"Every located realization of the self-consistent-a-priori-bounds / Galerkin-plus-tail /
> NK-in-Fourier-space periodic-orbit-or-branch certification technology closes its tail-domination
> (neglected-mode) estimate against a COMPACT domain … No located instance of the technology has
> closed a tail-domination estimate against an algebraic (non-exponential) far field for a
> time-periodic target, in more than 1D, or with a Leray projector."*

And `WALLS.md` W2's crack, which is the single sentence with the most weight on it:

> *"Leg 348 located `arXiv:1902.00384` — a certified periodic orbit of 3D Navier–Stokes with the
> viscous term inside the certified equation, on the three-torus."*

### §0.3 VERDICT RULES — fixed here, applied mechanically, never adjusted after reading

For each paper exactly one verdict is banked.

* **`confirm`** — the full text was obtained, and it contains no statement contradicting or
  materially narrowing leg 348's recorded classification of that paper. **CONSISTENCY IS NOT
  STRENGTHENING** (pre-committed reading (b)): a full text that merely fails to contradict the
  abstract is `confirm`, and this is the default landing place for a reachable paper. A `confirm`
  still requires a deciding sentence quoted verbatim and located; absent one, the paper is
  `UNREACHABLE`, never `confirm`.
* **`strengthen`** — the full text was obtained, contains nothing contradicting leg 348, **and**
  supplies at least one of the following that the abstract did **not**:
  * **S1** — an explicit statement that the viscous / dissipative term is *inside* the certified
    equation (not merely present in the model being discussed);
  * **S2** — an explicit dimensionality of the certified object;
  * **S3** — an explicit statement of what the computer-assisted part covers;
  * **S4** — an explicit statement of the domain and boundary conditions the abstract did not carry.
  The specific S-code(s) fired are banked per paper. **S-codes are checked against the ABSTRACT
  TEXT ITSELF, fetched in the same run**, so "the abstract did not say it" is measured, not
  remembered.
* **`UNDERCUT`** — the full text was obtained and contains a statement that **contradicts,
  materially weakens, or narrows** leg 348's recorded classification, or the aggregate claim of
  §0.2 as it rests on that paper. Pre-registered, non-exhaustive triggers, each with the paper it
  would bite:
  * **U1** — the paper's domain is *generalized beyond compact/periodic in the body* (e.g. an
    unbounded-domain section, an `ℝ^n` theorem, a whole-space variant). Bites the domain census and
    therefore `obstruction_named` directly. Any paper.
  * **U2** — the certified equation does **not** contain the viscous/dissipative term (it is
    dropped, projected away, or the certified object is for a truncated/inviscid surrogate). Bites
    P3 and W2's crack hardest.
  * **U3** — the certified object is not what leg 348 recorded: e.g. P3's object is not a genuine
    periodic orbit, or is a lift/2D reduction carried up, or the "3D" is a symmetry-reduced ODE.
    Bites P3 and W2.
  * **U4** — the computer-assisted part does not cover what leg 348 asserted (e.g. the CAP covers
    only a sub-step, or the existence proof has a non-rigorous ingredient).
  * **U5** — the apparatus is misattributed: e.g. P1's method is not Galerkin-plus-tail as
    described, or P3's is a radii-polynomial/`Y₀ Z₀ Z₁ Z₂` contraction that leg 348's own
    apparatus-distinction language mis-sorts.
  * **U6** — the paper's own text asserts the technology *has* been applied to an unbounded domain
    / algebraic weight for a time-periodic target (in itself or in a cited work it characterises),
    which is the direct negation of `obstruction_named`.
* **`UNREACHABLE`** — the full text was not obtained, or was obtained in a form that could not be
  read. **`UNREACHABLE` IS NOT A ZERO AND IS NOT A CONFIRMATION** (pre-committed reading (c)). The
  reason, the URL or identifier tried, and the HTTP status / parse outcome are banked.
* A query or fetch that fails to reach the service is banked **`THROTTLED`** or **`FAILED`**,
  **never as a zero** (pre-committed reading (e); leg 387's fabricated zero).

### §0.4 CONTROLS — they can fire in BOTH directions, and a control that cannot fail is not a control

Ten controls, pre-registered here, all executed by `experiments/p2_route_t6_v1_evidence.py` and
gated **by exit code**, never by a printed line (lesson 68).

* **K1 — instrument-reached-the-service positive datum, every fetch.** Every full-text record banks
  HTTP status, byte count, SHA-256 of the retrieved bytes, and extracted-character count. The
  arXiv-API abstract fetches additionally bank the **served opensearch namespace verbatim**. **No
  parser written by this leg names a namespace version anywhere** — that is leg 387's exact bug
  (arXiv serves `1.1`, the harness listed `1.0`, and the leg reported a clean fabricated zero).
  *Can fail:* if any record lacks a positive datum, the evidence script exits non-zero.
* **K2 — extraction sanity, positive direction.** A full text banked as reachable must yield
  ≥ 20 000 extracted characters **and** ≥ 5 pages. Below either, it is `UNREACHABLE(parse)`, not a
  confirmation. *Can fail in both directions:* a truncated or image-only PDF is caught, and an
  over-eager pass on a stub is refused.
* **K3 — extraction sanity, anti-tautology (negative direction).** For each reached paper, a
  **string known to be absent** must return **0** hits and a **string known to be present** must
  return **≥ 1**. The absent-string probe is `zzqxjvbnmwkl` (nonsense) in every text; the
  present-string probe is the paper's own title's most distinctive token, pre-registered per paper
  in the runner. *Can fail:* a grep instrument that hits everything, or nothing, is caught.
* **K4 — UNDERCUT IS REACHABLE, DEMONSTRATED NOT ASSERTED.** For each paper, a concrete
  pre-registered **undercut probe string set** is searched in the full text; a hit is *not*
  automatically an undercut, but the probe firing is what routes a passage to human adjudication.
  Probes (fixed here, before reading): `unbounded`, `whole space`, `R^3`/`\mathbb{R}^3`, `real
  line`, `Gaussian weight`, `algebraic decay`, `polynomial weight`, `inviscid`, `Euler equation`,
  `two-dimensional reduction`, `axisymmetric`. *Can fail in both directions:* if **zero** probes fire
  across **all seven** papers, the probe set is banked as **VACUOUS** and the leg says so in its
  own report rather than claiming a clean confirm sweep — an undercut instrument that cannot fire is
  not an instrument. Conversely a firing probe forces an adjudicated quotation.
* **K5 — STRENGTHEN IS REACHABLE, and is measured against the abstract, not against memory.** Each
  S-code claim is checked by locating the strengthening statement in the full text **and** showing
  the corresponding claim absent from the same run's fetched abstract. *Can fail:* an S-code claimed
  without both halves is refused by the evidence script.
* **K6 — the deciding-sentence locator is real.** Every non-`UNREACHABLE` verdict banks a verbatim
  quote **and** a locator (section number or page number). The evidence script re-finds each quote
  **in the extracted text** by exact substring match after whitespace normalisation. *Can fail:* a
  paraphrase, a misremembered quote, or a quote from the abstract when the full text was claimed,
  all exit non-zero.
* **K7 — page locators are machine-checked.** For quotes located by page, the quote must be found in
  that page's own extracted text (`pdftotext -f N -l N`). *Can fail:* an off-by-one or an invented
  page number exits non-zero.
* **K8 — no leg-348 file is modified.** SHA-256 of `experiments/journal/leg_348.md`,
  `writeup/data/p2_route_pocp_v1.json` and `writeup/novelty/leg_348.md` are banked at the start and
  re-checked by the evidence script against `git show` of the merge base. *Can fail:* any edit to
  the object exits non-zero.
* **K9 — no contact was made.** The runner performs **HTTP GET only**, against published-document
  endpoints. The evidence script asserts the runner source contains no `POST`, no `smtp`, no
  `mailto:`, and no form submission. *Can fail:* trivially, if the runner is ever changed to contact.
* **K10 — the seven are the seven.** The set of paper ids adjudicated must equal, exactly, the set
  in `p2_route_pocp_v1.json.located_technology[].id`, read from that file at evidence time. *Can
  fail:* a substituted, dropped or added paper exits non-zero.

### §0.5 WHAT THIS UNIT WILL NOT DO

* It will not re-scope its gate, and it will not construct a sixth reading.
* It will not amend leg 348. It reports; the Conductor decides.
* It proposes no build, and it claims no movement on the `L1 → L4` chain. **Tier 2 is never called a
  proof.** Clay stays ~0.05 %.
* It contacts nobody. Reading only.

### §0.6 PRE-COMMITTED REPORTING ORDER

**An undercut is the valuable branch and is reported FIRST** — at the top of this journal (§1) and
at the top of the final reply. If an undercut lands on `arXiv:1902.00384`, that is said in the first
line, because unit `T4` (leg 393) is running against that paper in this same wave and its target may
be void.

---

## §1. RESULT — filled in after the run, undercut first

*(pre-registration commit: this section is deliberately empty until the run has happened.)*
