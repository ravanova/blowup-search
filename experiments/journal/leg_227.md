# Leg 227 — Route-EGMT — one-field tense repair in `EGM_PRIMARY_READ`

**Role:** LEG (mechanical fix). **Branch:** `leg/227-egmt-v1`. **Territory:**
`solver/literature_gates.py` — the single field
`EGM_PRIMARY_READ["spectral_gap"]["sign_correction_leg_190"]` — plus
`writeup/novelty/leg_227.md` and this file. No runner, no JSON, no figure, per the
leg-214 / leg-180 prose-correction precedent.

## Gate, pre-committed, both branches

> Does `EGM_PRIMARY_READ`'s `sign_correction_leg_190` field now correctly state the 5 sites as
> fixed (past tense, citing `2c901c4`), with every other field in the same ledger entry
> byte-for-byte unchanged?

## **ANSWER: YES.**

Both halves hold, and both were measured rather than asserted:

1. **The field states the 5 sites as fixed, past tense, citing `2c901c4`.** New text quoted in
   full below; it reads `all RENDERED … UNTIL LEG 214 (2c901c4, Route-EGMB) CORRECTED ALL FIVE`.
2. **Every other field is byte-for-byte unchanged.** `git diff` against the merge base is
   **1 file, 1 hunk, 5 deletions, 11 insertions**, entirely inside the one string literal. No
   other key of `EGM_PRIMARY_READ` — `arxiv`, `tag`, `authors`, `title`, `venue`, `read`,
   `provenance_before_leg_190`, `scope`, `the_relayed_p2_claim`, `re_derived_at_leg_190`, and
   the seven sibling keys of `spectral_gap` — appears in the diff at all.

## The defect, in one sentence

Leg 190 wrote the field to warn readers that five named prose lines carried a sign-slope error.
Leg 214 (`2c901c4`) fixed all five and deliberately did not touch this module. Leg 213
(`bafe9ac`) then measured the field's own claim at **0/5 holding** and escalated rather than
reconcile, because `literature_gates.py` was read-only to it under both its branches. The
field has since been telling every reader of the shared ledger that five **correct** files are
wrong — the exact failure mode it was created to prevent, pointed the other way.

## Before / after — the changed sentence, verbatim

**BEFORE** (as of merge base):

> `"THIS ROW CORRECTS A TRANSCRIPTION THAT PROPAGATED THROUGH THREE LEGS.  "`
> `"experiments/journal/leg_141.md:30,117, writeup/novelty/leg_141.md:233 and "`
> `"experiments/journal/leg_165.md:120,128 all render the bracket as (-1/2 - C|a|), "`
> `"i.e. a gap IMPROVING with |a|; leg 190's own novelty pass sec 2 repeated it and "`
> `"additionally accused the ar5iv HTML mirror of the error.  The paper's LaTeX "`

**AFTER**:

> `"THIS ROW RECORDS A TRANSCRIPTION THAT PROPAGATED THROUGH THREE LEGS, AND ITS "`
> `"REPAIR -- ALL FIVE SITES ARE NOW FIXED; DO NOT READ THIS ROW AS AN OUTSTANDING "`
> `"DEFECT.  experiments/journal/leg_141.md:30,117, writeup/novelty/leg_141.md:233 "`
> `"and experiments/journal/leg_165.md:120,128 all RENDERED the bracket as "`
> `"(-1/2 - C|a|), i.e. a gap IMPROVING with |a|, UNTIL LEG 214 (2c901c4, "`
> `"Route-EGMB) CORRECTED ALL FIVE -- each of them carries the corrected form "`
> `"-(1/2 - C|a|) today, so the five files are RIGHT and this sentence is history, "`
> `"not a warning about the live tree (staleness found and escalated by leg 213's "`
> `"post-append ledger audit, Route-LGC2; re-tensed here at leg 227, Route-EGMT).  "`
> `"Leg 190's own novelty pass sec 2 repeated the error and "`
> `"additionally accused the ar5iv HTML mirror of it.  The paper's LaTeX "`

Everything from `"source settles it -- Prop. 2.1 reads "` onward — the LaTeX line, the
`-(1/2 - C|a|)` verdict, the `M_a f` split argument, the sec 6 `C^alpha` analogue, the
"two readings AGREE EXACTLY at a = 0 (both -0.5)" clause, the `pdftotext` diagnosis — is
**untouched**. Those sentences were already correct and already past-tense-neutral.

## What was verified before the edit was written

The new sentence makes a falsifiable claim about the live tree. Measured directly in this
worktree (not copied from leg 213's audit, which ran on a different commit):

| site | renders today | claim holds |
|---|---|---|
| `experiments/journal/leg_141.md:30` | `−(1/2 − C\|a\|)` | yes |
| `experiments/journal/leg_141.md:117` | `−(1/2 − C\|a\|)` | yes |
| `writeup/novelty/leg_141.md:233` | `−(1/2 − C\|a\|)` | yes |
| `experiments/journal/leg_165.md:120` | `−(1/2 − C\|a\|)` | yes |
| `experiments/journal/leg_165.md:128` | `−(1/2 − C\|a\|)` | yes |

**5/5.** Leg 213 measured these same five at **0/5** against the *old* claim; this leg
measures them at **5/5** against the *new* one. The two audits are consistent and the field is
now true under both readings of "does the sentence describe the tree".

Additionally: the module still imports and the field still parses as one string
(`EGM_PRIMARY_READ["spectral_gap"]["sign_correction_leg_190"]` reads back intact, and
`list(EGM_PRIMARY_READ.keys())` returns the same 11 keys in the same order as before).

## What does not move

**0 banked numbers. 0 of 12 `CLAIM_LEDGER` verdicts. 0 bans lifted. 0 routes advanced. 0
figures owed. `novelty_verdict()` untouched.** EGM's `-0.5` at `a = 0`, `γ = 4` stands exactly
as legs 141, 165 and 190 banked it — both the erroneous and the correct bracket give `-0.5`
there, which is why no number was ever wrong. What moves is one tense, and with it whether a
reader of shared infrastructure is sent to five correct files and told they are defective.

## Why this was worth a leg at all

`literature_gates.py` is read by every literature-pass leg. A false sentence there propagates
by being believed, and its five line-number pointers can only get staler as those files drift.
Leg 213 was right to escalate rather than silently reconcile; this leg is the escalation being
answered by the field's owner, with the wording leg 213 itself prescribed.
