# Leg 270 — ROUTE-PUB2V2: the full pre-submission review pass of PUB2

**Gate: NO — 88 of 94 quantitative sites reproduce exactly, 6 mismatch, and 0 of the 6 change an
argument. Five internal inconsistencies and one epistemic disclosure gap remain. NOT an
escalation; fourteen findings, all prose, provenance or precision.** Review leg,
submission-gating for PUB2. Branch `leg/270-pub2v2-v1`, merge base `e9d5a25`. Date 2026-08-07.
**No GA compute. Read-only on both PUB2 files — `git diff` over `writeup/4_p2_lottery/` is
empty. Nothing repaired: this leg reports, future rework legs fix.**

Full report: [`writeup/novelty/leg_270.md`](../../writeup/novelty/leg_270.md).
Trace table: [`writeup/data/p2_route_pub2v2_v1_trace.json`](../../writeup/data/p2_route_pub2v2_v1_trace.json).

---

## 1. The answer, as magnitudes

| | |
|---|---|
| quantitative sites traced | **94** |
| matching their banked source exactly | **88** |
| mismatching | **6** |
| **mismatches that change an argument** | **0** |
| epistemic-qualifier sites checked / clean | 27 / 26 |
| epistemic disclosure gaps | **1** |
| internal-consistency sites checked / inconsistent | 14 / **5** |
| provenance findings | **3** |
| escalations | **0** |

## 2. Method

Both files read end to end (652 + 214 lines). Every printed number located, then read back out of
the JSON key or journal line it comes from **in this pass**, with the venv python — not
transcribed from prose about it. Leg 186's own provenance audit was used only to *locate*
sources; every value it lists was re-read independently from the source file rather than
inherited from its table. Five sources: `p2_route_ngx_v1_general.json` (127),
`p2_route_h2s_v1_scoping.json` @ `origin/leg/163-h2s-v1` (163), `p2_route_h2c_v1_construction.json`
+ its two correction layers (176/268/274), `p2_route_h2i_v1_scoping.json` (182), and leg 249's
journal + `p2_route_h2cv_v1_postconstruction.json`.

## 3. What reproduces

* **All 15 cells** of §3.5's `Z₁` battery; the full 8-rung `σ_min` ladder **with leg 268's K1
  digit correction applied** at `N = 512`; both of leg 249's certified brackets.
* **Leg 274's A2 fix is applied correctly**, checked by recomputing both sets from the ladder
  printed in the same sentence: `‖T⁻¹‖_X`'s own increments `1.804e−2 → 2.623e−3` (ratios
  0.514/0.529/0.535) carry `‖T⁻¹‖_X`'s label, the reciprocal's decrements carry `σ_min`'s, and
  the ≈16× relation is named.
* **The truncation-independence defect is closed.** All 5 surviving occurrences are negations,
  historical markers, or the correct `ℓ¹_w` statement where the claim genuinely is a theorem.
  **0 regenerate it.** Legs 250/268/271/274 hold under an independent read.
* **0 float64 measurements described with proof-language.** Seven sites attach "evidence of a
  positive limit, not a proof of one" to `σ_min`, consistently. The one "proves" on a computed
  value is leg 249's exact-rational definiteness verdict, which is correct.
* Leg 182's §4 reproduces at 34/37, including all 8 columns of the §4.3 window table and both
  7-cell exponent rows; leg 127's §2 at 23/24; leg 163's §3 at 22/23.

## 4. The fourteen findings, worst first

**E1 (highest-priority rework item) — `0.0908` is convention-relative and PUB2 never says so.**
15 sites print it. Leg 249 §10 (W5), carried into the leg 268 correction artifact: sweeping the
border-amplitude weight over `10⁻²…10²` moves `σ_min` across **0.01086…0.13580, a factor of
12.5**, and Xu's own `y`-space normalization gives **0.0420** instead of 0.0908. Leg 249's own
sentence: *"PUB2 quotes the digits without the convention."* `grep` for
`convention|normaliz|0.0420|2π` over both files: **0 hits**. **Not an escalation** — leg 249
certifies in the same paragraph that the gate property (bounded away from zero uniformly in the
truncation) is **invariant** under that weight, and PUB2 argues only from the contrast in ladder
*behaviour*, which is convention-invariant. One disclosing sentence repairs it.

**P1 — leg 163's sources are not on `main`.** `p2_route_h2s_v1_scoping.json` and
`experiments/journal/leg_163.md` exist only on `origin/leg/163-h2s-v1`. **23 traced claims**
depend on them; all 23 reproduce exactly from the branch and **none from `main`**.

**P2** — PUB2 cites leg 249 only as branch `e9db984`, which is not an ancestor of `main`, while
leg 249's three artifacts **are** on `main` and byte-identical to the branch. The leg 268
correction artifact still says leg 249 was "never merged to main". **P3** — the leg 274
annotation JSON is cited **0** times although §3.5 L329 quotes the field it covers (correct half
quoted, pointer missing).

**N1–N4 — four bounds printed tighter than the data allow**, by **0.19 % / 0.51 % / 0.41 % /
0.005 %**: `≤ 2.62e−03` vs `2.6248e−03` (TECH L139); "at most 0.0090" vs `0.0090459` (L425);
"`p`-blind to 0.009" vs `0.0090366` (L539); `≤ 2.22e−14` vs `2.22012e−14` (L198-199). N2 and N3
are *also* internal inconsistencies — the same two quantities are printed correctly as `0.00905`
and `0.00904` at L457-458.

**N5 — the provenance of `0.71465` is wrong at two sites.** §3.2 and §3.5 both derive it as
`1/1.3993` "rounded up in the fifth decimal" by `7.0e−06`. Leg 163's banked
`implied_sigma_min_lower_witness` is `0.7146549471256172` = `1/1.39927667753796`, which rounds to
`0.71465` with **no round-up**; and `1/1.3993 = 0.7146430` rounds to `0.71464`, not `0.71465`.
The printed intermediate is off from the banked witness by `1.19e−05`. The number and its `≤`
direction are right; the two-step chain did not happen.

**N6** — "0.868155 over four data" is the `M = 8192` value of seed 0; the four-data max is
`0.8681539`. Sixth decimal.

**C1 — seven legs or seventy legs.** TECH L35 says "Seven successive legs"; TECH L564, BLOG L14
and BLOG L52 say "seventy". PUB1 says seven at both of its sites. A **10×** spread on a headline
number, unreconciled, and the blog carries only the larger one.

**C2** — §0 L61-63 promises §3.5 says which of §§5–6's sentences leg 176 revises; §3.5 is silent
on §5's "three results" opening and on §6's three-row grade table, which has no leg-176 row.
**C3** — §3.5 L326-328 leaves leg 176's superseded cause for the `N = 1024` rise in the running
text with its refutation four lines above in a parenthetical. **C4** — BLOG L109 credits "the
independent re-derivation we commissioned", collapsing the leg 192 / leg 249 distinction the
technical companion keeps at both of its sites. **C5** — "185 legs" at three sites; claim still
true, count ~85 legs stale.

## 5. Why this is not an escalation

The escalation branch of my gate is *"escalate only if any mismatch is substantive rather than
prose."* Of the fourteen findings: four are rounding directions on bounds, one is a
sixth-decimal subject slip, two are provenance sentences describing a derivation that did not
happen, three are provenance/citation staleness, four are internal wording inconsistencies, and
one is a missing disclosure whose underlying property leg 249 explicitly certifies invariant.
**Zero change an argument, a conclusion, or any bundled leg's gate answer.** Leg 127's theorem,
leg 163's census, leg 176's gate and leg 182's NO are all confirmed as PUB2 states them.

**On the record's side, PUB2 is one rework leg away from submission-ready, not zero.** Five
candidate rework items in the report's §4, E1 and C1 first.

## 6. Territory

`writeup/novelty/leg_270.md`, `writeup/data/p2_route_pub2v2_v1_trace.json`, this file — the three
paths DIRECTION.md §270 declares, and nothing else. Both PUB2 files read and **not edited**, not
even for a typo. `origin/leg/163-h2s-v1` read read-only via `git show`, never checked out, never
merged. No shared ledger touched: `experiments/JOURNAL.md`, `LITERATURE_CHECK.md`,
`plan_of_record.py`, `CONTINUATION_PROMPT.md`, `PHASE2_P2_NOTES.md` unmodified.
`capabilities.py` unmodified — no `solver/` module added.

## 7. Ceiling

Reviewing a document does not lift the ceiling attached to it. `a = 0` only. What PUB2 reports is
invertibility of an operator Xu already inverts in closed form, float64 and exact rationals,
nothing interval-enclosed. **No link of the `L1 → L4` chain moved. Clay odds unchanged at
~0.05 %. No ban lifted. No route promoted. No GA compute.**
