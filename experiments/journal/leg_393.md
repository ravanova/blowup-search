# Leg 393 — `T4`: reproduce `arXiv:1902.00384` row for row. LANE T, CONSTRUCTION.

**Branch:** `leg/393-t4-repro`. **Wave 2, unit 1.** **Figure allocated at dispatch: `fig110`.**

---

# PART I — PRE-REGISTRATION

**Committed BEFORE the first computational attempt.** Everything in Part I was written and
committed before any script existed, before any datum was fetched from the paper's own code
package, and before any number in Part II was produced. The only prior act was **reading the
paper at full text**, which the outreach-hold narrowing of 2026-08-13 (ruling (d)) authorises and
which is a precondition of the C1 naming requirement — I cannot name an apparatus I have not read.

## 1. The gate, restated verbatim. I am not permitted to re-word it and I have not.

> Does the unit reproduce at least one published enclosure row of `arXiv:1902.00384` — the
> certified quantity, its interval, and the paper's own criterion for the row being certified — to
> within the paper's stated precision, **using an apparatus named and shown compliant with ruling
> C1 in the unit's own pre-registration**?
> **yes →** name the row, the reproduced interval, the paper's interval, and the agreement; state
> the apparatus and the C1 compliance argument; state what the reproduction does **and does not**
> establish about Lane T's target.
> **no →** return the **furthest point reached** — which row, which quantity, what failed, and a
> **cost** for reaching it (§3d). **A reproduction that runs out of budget is `UNDER-RESOURCED`,
> never `no`.**

## 2. THE C1 NAMING REQUIREMENT — both halves, discharged here and not elsewhere

Ruling C1 (`writeup/escalations/RULING_BAN_WORDING_2026-08-13.md`) binds this unit and is not
waivable. Two things are owed, in this pre-registration, and absent **both** the ℓ¹-Fourier /
radii-polynomial ban applies to me in full.

### 2.1 (1) I NAME MY APPARATUS

**My apparatus is: NO CERTIFICATION APPARATUS AT ALL.** It is a two-part audit:

* **(A) A documentary extraction.** Verbatim strings pulled out of the paper's own text
  (`Papers/1902.00384.pdf`, fetched from arXiv, `pdftotext -layout`), naming the closure the
  paper itself uses and the mode-support of the objects it actually certifies.
* **(B) Arithmetic on the authors' own published data.** The authors publish their numerical
  solution and proof scripts at reference [41] of the paper —
  `https://www.math.vu.nl/~janbouwe/code/navierstokes/navierstokes-code.zip`, containing
  `dataorbit1.mat` and `dataorbit2.mat`. On that data I evaluate **the paper's own Lemma 6.2**,
  which is a closed-form inequality in already-computed quantities:
  `‖p − p̄‖_{ℓ¹_η} ≤ (2‖ū‖_X + r)·r`, with `ū = M ω̄` and `M` the paper's Biot–Savart operator.
  Summing `|coefficient| · η^{|n|₁}` over a finite published array is the whole of the numerics.

**Citation for the precedent, not for the apparatus:** leg 316 (`experiments/journal/leg_316.md`)
reproduced Dähne–Figueras row-for-row **while the ban was in force**, by exact arithmetic on a
third party's already-published, already-refereed data. That is the move I am making, on a
different paper. Leg 316's own ban check is the standard I hold myself to: *"the only operator
touched is a pure integer/`Fraction` decode-and-compare over a third party's already-published
data."*

### 2.2 (2) I SHOW IT DOES NOT CONSTRUCT A SINGLE BOUNDED APPROXIMATE INVERSE UNIFORM IN `M`

**It constructs no inverse whatsoever, so the showing is by construction rather than by argument.**
Precisely, and each clause is falsifiable against the script that Part II ships:

* No operator `A` of any kind is built. No finite Galerkin block is inverted, numerically or
  otherwise. There is no `A^{(N†)}`, no diagonal tail operator, no `Â`.
* No `Y₀`, no `Z₀`, no `Z₁`, no `Z₂` is computed, in any space, under any name.
* No contraction, no fixed-point theorem, no Newton-like operator, no radii polynomial, no
  `r_min`/`r_max` interval is derived by me.
* The only norm evaluated is the `ℓ¹_η` norm **of a given finite vector of published numbers**.
  An `ℓ¹` norm of a vector is not an operator norm, is not an inverse, and is not uniform in
  anything — there is no `M`-indexed family in my computation at all, because there is no
  truncation family: I read one fixed published array.
* Theorem NGX's excluded quantity is *a single bounded `A` working uniformly in `M`*. My apparatus
  has no `A` to be bounded and no `M` to be uniform in. **The exclusion is vacuous on my object.**

**This is deliberately the strongest available form of the showing, and it is also the weakest
available apparatus.** I record the trade honestly here, before knowing the outcome: an apparatus
that constructs nothing can audit a certificate but cannot *be* one. If the gate's "the paper's own
criterion for the row being certified" half turns out to be inaccessible without constructing the
thing I have just sworn not to construct, then that is the gate failing on a scope fact, and §4's
branch (c) governs — not my ingenuity.

## 3. WHAT I ALREADY KNOW AT PRE-REGISTRATION TIME, AND WHY IT IS DECLARED HERE

**I read the paper before writing this, and honesty requires that the pre-registration say what the
reading found rather than pretend to a blindness I do not have.** Two facts are already in hand.
They are declared here **as hypotheses to be tested by the controls of §5**, not as results:

* **H1.** The paper's own certification apparatus is a **Newton–Kantorovich / radii-polynomial
  contraction** in a Banach space `X` of geometrically weighted (`ℓ¹_η`) Fourier coefficients,
  closed by `Y₀/Z₀/Z₁/Z₂` bounds (Theorem 2.15, eqns (2.17)–(2.21); symmetry-reduced form
  Theorem 4.23, eqns (4.28)–(4.32)), around **a single bounded approximate inverse**
  `A : X_{−2,−1} → X` (§2.3) built as a numerical inverse on a finite index block `E†` plus an
  exact diagonal on the complement.
* **H2.** The two solutions the paper actually certifies (Theorems 5.1 and 5.2, i.e. the rows) are
  **essentially 2D**: independent of `x₃`, third velocity component vanishing, `N_{x₃} = 0` in
  Table 1 for both.

**If H1 holds, pre-committed branch (c) fires and this unit STOPS.** That is the brief's own
instruction and it is why the paper was read first: branch (c) is a *scope* finding about the ban,
and a scope finding that arrives after the banned machinery has been run is worthless.

## 4. VERDICT RULES — pre-committed, and the branch is chosen by the controls, not by me

* **V1.** If control `C+` passes and control `C−` does not fire (see §5), H1 is established: the
  paper's apparatus **is** the banned machinery, complete with a single bounded approximate
  inverse. **Pre-committed reading (c) FIRES. The unit STOPS.** The gate is answered
  **`STOP (branch c)`** — explicitly *not* `no` and *not* `yes`, because the brief says
  *"Report it as a **stop**, not as a failure"*.
* **V2.** If `C−` fires — the paper's own stated method is a Zgliczyński-style self-consistent
  a-priori-bounds / Galerkin-plus-tail dynamical closure rather than an NK contraction — then
  branch (c) does **not** fire, C1's scope covers the reproduction, and I proceed to a full
  reproduction of Theorem 5.1's row at Table 1's `p₁` parameters (`η=1, N_{x₁}=N_{x₂}=17,
  N_{x₃}=0, N_t=11, N†=130, Ñ=265`), budgeted against the paper's own stated 10 GB / 6 CPU-days.
* **V3.** **Under V1 or V2 alike, the furthest-point-reached datum is banked**, because a stop is
  not a licence to return empty-handed. Two sub-claims, both outside the ban and both computable
  under §2's apparatus:
  * **F1 — the pressure enclosure row.** Theorem 1.1 publishes `r^p_sol = 5.6486·10⁻⁵` and
    `r^ω_sol = r^u_sol = 2.2491·10⁻⁶`. Lemma 6.2 gives `‖p − p̄‖ ≤ (2‖ū‖_X + r)·r`. So the
    published pair **implies** `‖ū‖_X = (r^p_sol / r^ω_sol − r^ω_sol)/2`, a number the paper never
    prints. I compute `‖ū‖_X` **independently** from `dataorbit2.mat` via `ū = M ω̄`, and compare.
  * **F2 — the dimensionality of the certified object.** Measure the mode support of the published
    `ω̄` in the `x₃` direction, on the authors' own data.
* **V4 — agreement thresholds for F1, fixed here, before the number exists.** Let
  `δ = |‖ū‖_computed − ‖ū‖_implied| / ‖ū‖_implied`.
  * `δ ≤ 1·10⁻³` → **REPRODUCED to the paper's stated precision** (the paper prints 5 significant
    figures; this is two orders looser than its print precision and is deliberately generous,
    because `ū = M ω̄` involves a Biot–Savart reconstruction I must implement from the paper's
    own formula and small convention differences are possible).
  * `1·10⁻³ < δ ≤ 1·10⁻¹` → **RIGHT SHAPE, WRONG INTERVAL.** Per pre-committed reading (d) this
    is reported as **`no`** for F1 with the discrepancy quantified. It is not talked up.
  * `δ > 1·10⁻¹` → **NOT A REPRODUCTION.** Reported as `no` with the discrepancy quantified.
  * If the data cannot be decoded unambiguously — file format unreadable, array conventions not
    determinable from the paper — F1 is **`UNDER-RESOURCED`** with the specific ambiguity named
    and a cost, **never `no`** (§3d).
* **V5.** If the code package cannot be reached over the network, that is banked as
  `UNREACHABLE`/`THROTTLED` with the HTTP evidence, **never as a zero** (leg 387's fabrication
  mode, and leg 392's instrument discipline).

## 5. PLANTED CONTROLS — each can fire in BOTH directions. A control that cannot fail is not a control.

| id | what it asserts | fires against me how |
|---|---|---|
| **`C+`** | The extracted text contains ALL of: an `approximate inverse` of `DF(W̄)⁻¹`; the four bounds `Y₀,Z₀,Z₁,Z₂` with defining inequalities; the contraction condition `Z₀+Z₁<1`; a weighted-`ℓ¹`(`η`) coefficient space. | If ANY is absent, H1 is **not** established, branch (c) does **not** fire, and I must go to V2 and reproduce. |
| **`C−`** | The paper does **not** describe its own method as self-consistent a-priori bounds / a Galerkin-plus-tail *dynamical* closure, and Zgliczyński appears only as a bibliography entry, never as the method used. | If the paper DOES state that method, `C−` fires, V2 governs, and the reproduction goes ahead under C1's scope. **This control is the one that decides whether the ban reaches me, and it is genuinely two-sided.** |
| **`C3D`** | Table 1 records `N_{x₃}=0` for **both** certified rows, and the prose asserts `x₃`-independence and a vanishing third velocity component. | If `N_{x₃} > 0` for either row, W2's crack **survives at full text** and leg 348's classification is confirmed on its most load-bearing clause. |
| **`Cdec+`** | Decode positive control (leg 316's discipline): the decoded `dataorbit1.mat` coefficient array's extent must match Table 1's `p₁` row `(17,17,0,11)` and `dataorbit2.mat`'s must match `p₂`'s `(21,21,0,16)`. | If the shapes do not match Table 1, **my decode is wrong and F1/F2 are void**. I must report the decode as failed, not push a number through it. |
| **`Cdec−`** | Decode negative control: the two files must **not** decode to the same shape. | If they do, the decoder is not discriminating between the two published rows and every number it produces is suspect. |
| **`Cnet`** | A positive datum that an external service was actually reached: HTTP status and byte count for arXiv and for the VU code host, both banked. | A zero return with no HTTP evidence is banked as `FAILED`, never as a measured absence. |

## 6. WHAT THIS UNIT WILL NOT CLAIM, WHATEVER IT FINDS

Pre-committed readings (a) and (b), restated in my own words so that Part II cannot quietly drift:

* **(a)** A successful reproduction would be a **CAPABILITY, NOT A RESULT ON THE CLAY CHAIN.** It
  would show this repository can run a piece of published technology. **No link of `L1 → L4`
  moves.** §3h rule 2: scale is not evidence; re-deriving somebody else's theorem is by
  construction not new mathematics. **Clay stays ~0.05%.** Tier 2 is never called a proof.
* **(b)** The paper's object is a **PERIODIC ORBIT, NOT A BLOW-UP.** **W3 — the empty Grade-A ×
  fluid *blow-up* cell — is untouched by anything in this leg**, and nothing here may be read as
  filling leg 174's cell. Not partially, not "in technology", not at all.

## 7. Standing checks performed before writing a line of code

* `capabilities.py` grepped before building (the standing ban: *"building a solver without
  grepping `capabilities.py` for the object first"*, lifted-by: never, leg 45). Searched
  `galerkin`, `tail`, `zgliczy`, `periodic orbit`, `navier`, `torus`, `interval`. **Nothing in
  this repository decodes a MATLAB `.mat` Fourier-coefficient array or evaluates a weighted-`ℓ¹`
  norm of published Navier–Stokes vorticity data.** `solver/interval.py` and
  `solver/interval_mp.py` exist and are *not* needed: F1 is a float-level agreement check against
  a printed 5-significant-figure constant, and dressing it in intervals would imply a rigour it
  does not have. Recorded so the choice is visible rather than assumed.
* `.venv/bin/python plan_of_record.py` executed, not read. `.venv/bin/python capabilities.py`
  executed. `DIRECTION.md` **not read** (§3e).
* Numbers re-derived from `writeup/data/*.json` and from the paper's own PDF, never from
  repository prose.

---

*Part II is written below, after the pre-registration above was committed (`9e36e3c`).*

---

# PART II — WHAT HAPPENED

Artefacts: `experiments/p2_route_t4_v1.py` (audit runner, network + PDF + `.mat`),
`writeup/data/p2_route_t4_v1.json` (curated), `experiments/p2_route_t4_v1_evidence.py`
(**68/68 checks, exit 0**, gated by exit code per lesson 68, reads the JSON only),
`writeup/figures/fig110_route_t4_v1.png`.

## 8. THE GATE, ANSWERED IN THE GATE'S OWN WORDING

> *Does the unit reproduce at least one published enclosure row of `arXiv:1902.00384` — the
> certified quantity, its interval, and the paper's own criterion for the row being certified — to
> within the paper's stated precision, using an apparatus named and shown compliant with ruling C1
> in the unit's own pre-registration?*

**`STOP` — pre-committed reading (c) fired. Not `yes`. Not `no`. Not `UNDER-RESOURCED`.**

Verdict rule **V1** fired exactly as written in §4: control `C+` passed and control `C−` did not
fire. Hypothesis H1 is established. The brief's branch (c) says: *"IF THE APPARATUS, WHEN ACTUALLY
IMPLEMENTED, REQUIRES A BOUNDED APPROXIMATE INVERSE UNIFORM IN `M`, STOP AND SAY SO … Report it as
a **stop**, not as a failure, and commit what you have."* I am reporting it as a stop.

The reason the gate is not answered `yes` is precise and it is not a technicality. The gate has two
conjuncts. The **first** conjunct — reproduce the quantity, the interval, and the paper's own
certification criterion to its stated precision — **is met, on both published rows** (§10). The
**second** conjunct — *using an apparatus named and shown compliant with ruling C1* — **cannot be
met by any apparatus that actually certifies this paper's rows**, because the paper's certifying
apparatus **is** the banned machinery. My audit apparatus is C1-clear (§2), but it certifies
nothing; it only reads and re-arithmetises what the authors certified. A `yes` would claim the
conjunction. It is not available.

## 9. FINDING 1 (adverse, and the reason for the stop): C1's SCOPE DOES NOT REACH THIS PAPER

Ruling C1 (2026-08-13) is a **scope** ruling: the ℓ¹-Fourier/radii-polynomial ban names an
*apparatus*, and a Zgliczyński-style Galerkin-plus-tail **dynamical** closure is a different
apparatus, outside it. Leg 348 classified `arXiv:1902.00384` as an instance of
"self-consistent-a-priori-bounds / Galerkin-plus-tail / NK-in-Fourier-space technology" — **at
abstract level only**, and said so. At full text that classification is **wrong**, and `WALLS.md`
and the escalation inherited the error from it.

Measured, not asserted (`C+`, all checks in the evidence script):

* **`Newton-Kantorovich`** occurs **4×**; **`approximate inverse`** occurs **7×**; `INTLAB` 2×,
  `interval arithmetic` 5×.
* Verbatim, from the paper's own text, **6/6 located**:
  * *"a Newton-Kantorovich theorem is applied to obtain the (computer-assisted) proofs of
    existence"*
  * *"a zero finding problem posed on a Banach space of geometrically decaying Fourier
    coefficients"*
  * *"We circumvent this difficulty by working with an approximate inverse `A`"*  (§2.3)
* The apparatus, as measured: **Theorem 2.15, eqns (2.17)–(2.21)**; symmetry-reduced **Theorem
  4.23, (4.28)–(4.32)**. Bounds `Y₀,Z₀,Z₁,Z₂`; contraction condition `Z₀+Z₁ < 1`; radii polynomial;
  weighted-`ℓ¹_η` Fourier space. `A : X_{−2,−1} → X` is the numerical inverse `A^(N†)` on the
  finite block `E† = {n : μ(n) ≤ N†}` **plus** the exact diagonal `λ_n = 1/(ν ñ² + i Ω̄ n₄)` off
  it — **one operator, acting on the whole infinite-dimensional space at once.**
* `C−` **did not fire**, and it was genuinely able to: `self-consistent` **0**, `a priori bounds`
  **0**, `isolating` **0**, `trapping region` **0**, `logarithmic norm` **0**, `dynamical closure`
  **0**. `Zgliczyński` appears **once**, as bibliography entry **[48]**, never as the method.
  `Galerkin` appears **once**, describing the size of the projection *inside* the NK apparatus.

So the object of pre-committed reading (c) is present in the strongest possible form. **This is the
more valuable finding**, exactly as the brief predicted: it is a correction to a scope ruling that
the programme was relying on, and it was obtained *before* any banned machinery was run — which was
the whole point of reading the paper first (§3).

**What Finding 1 does not do.** It does not overturn C1. C1's *general* proposition — that a
dynamical Galerkin-plus-tail closure is a different apparatus from the ban's — is untouched. What
falls is only the *instantiation*: `1902.00384` is not an example of it. Any lane document that
cites this paper as the C1-clear exemplar is citing the wrong paper. Naming the replacement is the
Conductor's call, not mine.

## 10. WHAT WAS ACTUALLY REPRODUCED (the furthest point reached, all of it outside the ban)

Everything below is arithmetic on constants the authors published, in my §2 apparatus: no operator
is built, no `Y₀/Z₀/Z₁/Z₂` is computed, no contraction is claimed. Leg 316's precedent exactly.

**The paper's own certification criterion (4.32), verified on both rows:**

| row | theorem | `Z₀+Z₁ < 1` | `2Y₀Z₂ < (1−(Z₀+Z₁))²` | certified? |
|---|---|---|---|---|
| `p₁` | Thm 5.1 | `0.973067` | `3.7377e-04 < 7.2539e-04` | **yes, by the paper's own test** |
| `p₂` | Thm 5.2 / Thm 1.1 | `0.972880` | `1.4831e-05 < 7.3549e-04` | **yes, by the paper's own test** |

**The enclosure rows, reproduced from the authors' published `Y₀,Z₀,Z₁,Z₂` through the radii
polynomial (2.22)/(4.33)–(4.34) and the package's own `significantceil`:**

| row | quantity | reproduced | published | rel. dev. |
|---|---|---|---|---|
| `p₁` | `r_min` | `2.6313456851e-05` | `2.6313456857e-05` | `2.435e-10` |
| `p₁` | `r_max` | `8.662341154e-05` | `8.662341154e-05` | `0.000e+00` |
| `p₁` | `r_sol^Ω` | `2.6314e-05` | `2.6314e-05` | **exact** |
| `p₂` | `r_min` | `2.249006029e-06` | `2.249006029e-06` | `8.335e-13` |
| `p₂` | `r_max` | `2.219327131e-04` | `2.219327131e-04` | `5.618e-15` |
| `p₂` | `r_sol^Ω` | `2.2491e-06` | `2.2491e-06` | **exact** |
| `p₂` | `r_sol^p` | `5.6484e-05` | `5.6486e-05` | `3.541e-05` |

`r_sol^p` sits inside V4's `δ ≤ 1e-3` **REPRODUCED** band, and the residual gap has the **right
sign**: my value is a *lower* bound on theirs, the difference being the authors' `raddevp` term,
which can only widen their radius. It is **not** in V4's right-shape-wrong-interval band, so
pre-committed reading (d) does **not** apply to it.

**F1 as pre-registered** (the number the paper never prints): Lemma 6.2 makes the printed pair
`(r_sol^p, r_sol^ω)` imply `‖ū‖_X = 12.557466307`. Computed **independently** from
`dataorbit2.mat` via `ū = M ω̄`: **`12.557400068`**. `δ = 5.275e-06` — **REPRODUCED** under V4's
pre-committed threshold, which was fixed before the number existed.

**Decode controls both fired** (leg 316's discipline): `Cdec+` — decoded extents `[35,35,1,23,3]`
and `[43,43,1,33,3]` match Table 1's `Nrec` `(17,17,0,11)` and `(21,21,0,16)` as `2N+1` on every
axis, and `N†, Ñ, η, ν` all match Table 1. `Cdec−` — the two files decode to **different** shapes.
`Cnet` fired: arXiv PDF HTTP 200, 874 126 B, `sha256 97e81647…`; VU code zip (the paper's own
reference [41]) HTTP 200, 4 851 078 B, `sha256 edf64bc0…`. **No source is banked as a zero.**

## 11. FINDING 2 (adverse, and it is about W2, not about me): THE CERTIFIED OBJECTS ARE 2D LIFTS

`C3D` fired. On **both** certified rows, on the authors' own arrays:

* `N_{x₃} = 0` in Table 1 **and** in the package's own `solshape.Nrec`; the `x₃` mode extent is
  **exactly 1** (the single mode `n₃ = 0`).
* `max|u⁽³⁾| = 0.0` **exactly**; `max|ω⁽¹⁾| = max|ω⁽²⁾| = 0.0` **exactly**; and
  `max|ω⁽³⁾| = 1.6351` (`p₁`) / `1.5274` (`p₂`) is **not** zero — so the zeros are **structure**,
  not an empty array. That negative-control pairing is why this is a measurement and not a
  file-reading accident.
* The authors' own output field `setup` reads, literally, `'2D'`.
* The paper says so itself, verbatim: *"the solutions we present in Theorem 1.1 below are
  two-dimensional (in space) time-periodic solutions"*, *"they are independent of `x₃` and the
  third component of the velocity vanishes"*, and — the load-bearing one — *"The only reason for
  this reduction is that the physical memory requirements for a three-dimensional solution are, for
  now, prohibitive in our current implementation."*

**W2** states: *"No certificate has ever supplied its own three-dimensionality."* Its own
pre-committed test requires naming the certified equation, showing the viscous term is inside it,
and **showing the certified object is not a lift of a lower-dimensional one**. The equation is 3D
Navier–Stokes and the viscous term is inside it — the first two clauses pass. The third **fails**:
the certified object **is** a lift. **W2 stands, and stands more firmly than before**: the strongest
CAP in the fluid literature confirms it rather than cracking it. The crack leg 348 recorded at
abstract level is not there at full text.

## 12. W6 — WHAT THE APPARATUS COSTS, AND WHY IT IS BOUNDED HERE AND NOT ON ROUTE 4

Measured on the authors' parameters (this is the one place the audit says something the paper does
not): the infimum over **all** nonzero modes of the symbol modulus `μ(n) = |ν ñ² + i Ω̄ n₄|` equals
**`ν` exactly** — `0.286` (`p₁`), `0.265` (`p₂`) — a **strictly positive floor independent of the
truncation**. On the tail `μ(n) > N†`, so the diagonal part of `A` has norm `≤ 1/N†`: measured
`sup|λ| = 7.692262e-03 ≤ 1/130 = 7.692308e-03` and `4.761523e-03 ≤ 1/210 = 4.761905e-03`.

Contrast **Theorem NGX**, which excludes a single bounded `A` uniform in `M` because
`σ_min(L_M) = c_s M^{−(1−s)} → 0`. The two situations differ *structurally*: this apparatus is
bounded **precisely because** the domain is a compact torus and the equation is viscous, so the
linear symbol has a positive spectral floor. Route 4's object has neither. **The paper is not a
counterexample to NGX and does not weaken W6.** It shows what NGX's hypothesis being false looks
like — which is a useful calibration and nothing more.

**Cost of the apparatus, the authors' own Table 1:** `p₂` = **95 CPU-days, 110 GB RAM** — for a
**2D** row. That is the price of the machinery this leg was asked to run, and it is a §3d datum:
had branch (c) not fired, V2's reproduction of the *smaller* row (`p₁`: 10 GB, 6 CPU-days) would
have been the only affordable one, and even it would not have been a three-dimensional result.

## 13. READINGS (a) AND (b), DISCHARGED — in my own words, on the record

* **(a) NO LINK OF `L1 → L4` MOVED.** Nothing in §10 is new mathematics. Every constant reproduced
  was published in 2019 by van den Berg, Breden, Lessard and van Veen; my contribution is arithmetic
  on their constants and a decode of their data files. This is a **capability datum and an audit
  datum**, not a result. §3h rule 2: scale is not evidence, and re-deriving somebody else's theorem
  is by construction not new mathematics. **Tier 2 is never called a proof. Clay is ~0.05% and
  stays there.** This leg does not move it by any amount, in any direction.
* **(b) THE OBJECT IS A PERIODIC ORBIT, NOT A BLOW-UP.** `arXiv:1902.00384` certifies *spontaneous
  periodic orbits* — bounded, recurrent, globally smooth solutions of forced Navier–Stokes on a
  torus. **W3 — the empty Grade-A × fluid *blow-up* cell (leg 174) — is untouched by every line of
  this leg.** Nothing here fills that cell: not partially, not "in technology", not at all. A
  certificate of a periodic orbit is evidence *against* singularity in its own regime, and even that
  reading is not claimed here.

## 14. HONEST CEILING, AND WHAT THIS UNIT DOES **NOT** ESTABLISH

* It does **not** establish that this repository can run the paper's apparatus. **I never ran it.**
  The 95-CPU-day / 110-GB figure was read from Table 1, not measured here. Any claim of reproduction
  *capability* would be unsupported; what is supported is that the paper's **published numbers are
  internally consistent and independently re-derivable from its own constants and data**.
* It does **not** validate the authors' `Y₀/Z₀/Z₁/Z₂` themselves. Those are inputs to my arithmetic,
  taken on the authors' authority. I verified the criterion **given** them, and the radii **given**
  them. Verifying the bounds would require building the banned apparatus.
* It does **not** claim `r_sol^p`'s `3.541e-05` gap is fully explained. The sign is right and
  `raddevp` is the identified cause, but I did not reconstruct that term.
* The `‖ū‖_X` agreement at `5.275e-06` is a **float-level** agreement against a 5-significant-figure
  printed constant. It is not an interval statement and is not dressed as one (§7).
* Finding 1 is a finding about **this paper**. Whether some *other* paper instantiates C1's
  Galerkin-plus-tail class is not decided here and was not looked for.

## 15. LANE / WALL / CLAY STATEMENT

* **Lane T target:** the reproduction the unit was built to attempt is **not delivered**, and cannot
  be delivered by any C1-clear apparatus, because the paper's certifier is the banned one. The lane
  needs a Conductor decision, not a worker's improvisation.
* **W2 (three-dimensionality):** **stands, strengthened.** §11.
* **W6 (function space / NGX):** **stands, unchallenged.** §12 explains why this paper is not a
  counterexample.
* **W3 (Grade-A × fluid blow-up):** **untouched.** §13(b).
* **Clay `L1 → L4`:** **no link moved. ~0.05%.**

## 16. STOPPING HERE

Branch (c) is an instruction to stop, and I am not permitted to construct a fifth reading that would
let me continue. I have committed what I have. No further unit is selected; the gate is answered and
this worker self-terminates.
