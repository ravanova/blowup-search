# Leg 199 — Route-CGA: the fabrication-acceptance battery, turned on the guard layer

**Branch** `leg/199-cga-v1`. **Slot** audit, not critical path. **Difficulty** standard.
**Gate answer: YES — 16 silent accepts across four mechanisms, all LATENT, 0 banked numbers
impeached. `solver/certificate_guards.py` NOT patched, per the gate. ESCALATED.**

> **Gate (pre-committed, both branches, verbatim).** Under adversarial and degenerate
> inputs, does `certificate_guards.py` ever silently ACCEPT (fail to reject) a case that
> should fail — the same fabrication-acceptance shape leg 116 found in `nk_bounds.py`?
>
> **YES →** Name the exact mechanism, magnitude, and every certificate battery whose banked
> verdict it could have silently affected. Push the branch only, never `main`, escalate — do
> not patch `certificate_guards.py` under this leg's own authority.
>
> **NO →** Bank the battery as the permanent regression suite; record the pass in
> `capabilities.py` (append-only). Normal landing straight to `main`.

## Sequence

1. **Novelty pass FIRST**, committed before any construction (`d0d30cf`,
   `writeup/novelty/leg_199.md`), verdict `PROCEED_AS_ADVERSARIAL_AUDIT`, six channels,
   five probe families and six controls pre-registered before a single number was computed.
2. `plan_of_record.py` read in full; no ban bites (none names this module, an adversarial
   audit, or any of the three consumers). `capabilities.py` grepped before building — its
   own entry supplied the gap this leg was dispatched into.
3. `solver/certificate_guards.py` read end to end (210 lines) before the first probe, and
   all eight of its call sites in `solver/` located and read.
4. Construction, then the quartet.

## Why the module was worth the leg, in its own ledger's words

`capabilities.py:303–334`:

> "**NO known-answer gate of its own** — it is exercised through the three modules' suites
> and through `test_repaired_all_three_modules_share_one_guard`."

Its entire history is **one commit** (`bfe6883`, leg 128), and the leg that wrote the module
wrote every number validating it, in that same commit — leg 169's authorship shape. The only
prior art that touches it at all, `test_nk_bounds_adversarial.py:528–554`, drives **five
plain float64 triples** (`-1.0`, `NaN`, `+inf`) through the three *consumers*: exactly the
three shapes the guard was written to catch. Nothing had ever handed it a value of a
different **type**, an **empty container**, or a range **endpoint**.

## The correction the novelty pass made before the battery rather than after

The dispatch calls this "the guard-layer module class", which invites reading any finding as
a three-pipeline finding. Measured, not assumed (`grep` over `solver/`, excluding the module
itself):

| exported guard | consumers |
|---|---|
| `hypothesis_violations` | **3** — `port_certification.py:346`, `interval_certificate.py:140`, `nk_bounds.py:526` |
| `invalid_input_reason` | 1 — `nk_bounds.py:533` |
| `radius_violation` | 1 — `nk_bounds.py:561` |
| `unit_range_violation` | 1 — `nk_bounds.py:392` (`gamma`), `:430` (`alpha`) |
| `positive_weight_violations` | 1 — `nk_bounds.py:225`, `:227` |

**One of the five is genuinely shared; the other four have a single consumer each.** So M1,
M2, M5 and M7 are `nk_bounds` findings that happen to live in a shared file, and only M3 and
M4 (both in `hypothesis_violations`) touch all three pipelines. The report keeps them apart.

## What was found

67 cases, **16 gate hits**, **43 controls, 0 failed**, 7 secondary items explicitly not
counted as hits. Full data: `writeup/data/p2_route_cga_v1_adversarial.json`.

### M1 — the headline. A non-finite Hölder exponent is admissible at `nk_bounds.py:430`

Four of the five exported guards test finiteness **explicitly**: `hypothesis_violations` and
`radius_violation` both call `math.isinf`, and `positive_weight_violations` counts infinite
entries. `unit_range_violation` is the only one that does not — it delegates non-finiteness
to two comparisons:

```
lo_bad = (f <= lo) if lo_open else (f < lo)
hi_bad = (f >= hi) if hi_open else (f > hi)
```

and `nk_bounds.py:430` asks it for a **closed lower endpoint at minus infinity**:

```
unit_range_violation("alpha", alpha, -float("inf"), 2.0, lo_open=False, hi_open=True)
```

so the lower test is `-inf < -inf`, which is `False`. **`alpha = -inf` is admissible.**

At the consumer, `farfield_modelling_error_bound(-inf, 0.5, 1.0)` returns
`bound = NaN`, `hilbert_term = NaN`, **40 of 40 window samples NaN**, and **does not raise**
— against an honest `2.537396530974982` at `alpha = 1.5`. The two sibling non-finite alphas
both refuse with a named `ValueError`: **1 of 3 non-finite alphas is accepted, and it is the
only one that manufactures a NaN in an upper-bound slot** — the exact object this module's
own `NAN_HINT_*` constants exist to warn about ("`NaN >= 1.0` is False, so an unguarded NaN
would slip past the contraction test").

**The finding is localised, and the localisation is a gate.** The *same function* refuses
both infinities at the `gamma` site, because there the lower endpoint is finite (`0.0`,
open) and `-inf <= 0.0` is True. So this is not "the guard is careless about infinity"; it
is the **interaction** of a missing `isinf` test with a caller-supplied infinite endpoint,
and `nk_bounds.py:430` is the only such caller in the repository. That bounds the repair to
one line and is asserted as
`test_gap_m1_localised_the_gamma_site_with_a_finite_endpoint_is_clean`.

Second half, recorded as secondary rather than as a hit: the guard's declared admissible
range `[-inf, 2.0)` also contains a whole sub-range the guarded function cannot evaluate —
`alpha = -1e300` is accepted and then dies of an unnamed `OverflowError` from inside the
quadrature.

### M2 — a type gate in one guard, absent in the others, in the same file

`hypothesis_violations` enforces `isinstance(v, numbers.Real)` and raises, because "a
constant that is not a number is a caller bug … and must not be laundered into a
`closes=False`". `radius_violation` and `unit_range_violation` call `float()` with no such
check. `radius_violation` accepts `'0.5'`, `Decimal('0.5')`, `Fraction(1,2)` and `True` as
admissible **radii** — **4 of 4** — while the sibling refuses 2 of those 4 by `TypeError`. A
string `gamma` passes the exponent guard, and the refusal that eventually happens is an
unnamed `TypeError` from the power operator three lines into `hilbert_farfield_bound`: leg
98's B20/B21 shape, in the module that was repaired for it.

### M3 — `bool` is `numbers.Real`, and is reported at its honest weight

`hilbert_farfield_bound(1.0, 1.5, gamma=True)` returns a claimed upper bound of
`1.2209630139367613` against `1.5390840894727127` at the live `gamma = 0.5` —
**1.2605493138651522× smaller**. `budget(Y_0=False, …)` returns `closes=True` with a `reason`
that narrates the boolean into a mathematical claim: *"Y_0 is exactly 0.0, which is the
honest zero-residual case — the centre itself is a zero"*.

**What this is not**, stated because inflating it would be the dishonest move: `True` *is*
arithmetically 1.0 and `gamma = 1.0` *is* a legitimate exponent, so each number is correct
**for the coerced value**. This is a missing signal, not an arithmetic lie, and it is pinned
at that weight. It is reported because refusing inputs no run could have produced is the
module's entire purpose, and a Boolean in a norm slot is one.

### M4 — a negative constant below the denormal floor is accepted as nonnegative

`float()` underflows a negative rational to `-0.0` **before** the `f < 0.0` test, so
`hypothesis_violations((("Y_0", Fraction(-1, 10**400)),))` returns `[]` and `budget` closes
on it. **Magnitude, stated so it cannot later be overstated:** accepted negatives span
`(-5e-324, 0)`, so the budget inflation is ≤ 1 ULP — the accept is real, the magnitude is
nil. Unreachable from float64 by construction (a negative float below the denormal floor
already *is* `-0.0`). The discrimination control lives in the same gate: `Fraction(-1, 3)` is
correctly refused, so this is a float-conversion boundary, not a `Fraction` blind spot.

### M5 — the empty container is a vacuous pass in two guards

`hypothesis_violations(())` → `[]` ("every constant is admissible", zero constants examined);
`positive_weight_violations("v_cod", [])` → `[]` (an empty weight vector "comes from a
norm"). The same empty-window shape leg 99 measured in `boussinesq_velocity.py`. At the
consumer the reach was measured rather than assumed: `nk_bounds.py:225`'s own numpy
pre-screen **also** passes the empty array (`np.all` of an empty array is `True`), so the
guard is never even consulted and the failure surfaces four lines later in numpy's
vocabulary.

### M6, M7 — recorded, and explicitly not counted as gate hits

**M6:** a Python `int` too large for float64 raises an uncaught `OverflowError` from the
`float(v)` on the line *before* the `isinf` test that exists precisely to name that case.
**Sound** — it is a refusal, not a false accept — and it is filed as secondary. It is noted
only because it is leg 98's B20/B21 shape and leg 131/147's shape (an exception a caller
with a broad `except` converts to a silent skip).

**M7:** `invalid_input_reason([])` assembles a full rejection sentence with an empty reason
clause — 174 characters of rejection prose naming zero violated hypotheses. Not reachable at
`nk_bounds.py:533`, which is guarded by `if violations:`. Hygiene.

## Blast radius — the part that decides whether this is a priority

The gate asks for "every certificate battery whose banked verdict it could have silently
affected", because a false accept here could mean a banked *"certificate closes"* is
unsound. **It could not have, and that is measured, not assumed.** Every in-repo call site
was re-driven at its own live parameters:

| mechanism | reach | verdict |
|---|---|---|
| M1 (`alpha = -inf`) | 8 call paths — `test_nk_bounds.py:192,207` (`ALPHA=1.5`), `p2_route_d_v6:170,200,224`, `v7:298`, `v8:306`, `v9:326` (all `alphas=(1.1…1.8)`), `p2_route_nka_v1_adversarial.py:408,425`, `p2_route_nkr_v1_repair.py:230`, `p2_route_nkb_v1_postrepair.py:150`. **Every in-repo alpha is in [1.1, 1.8]**; the accept needs exactly `-inf`, which no caller passes and no computation in the repo produces as an exponent. 8/8 live alphas return finite positive bounds. | **LATENT** |
| M2 / M3 | `r_min` at `nk_bounds.py:561` is always the module's own float64; `gamma`/`alpha` are floats at all 8 call paths | **LATENT** |
| M4 | unreachable from float64 by construction; all three pipelines are driven with float64 | **LATENT** |
| M5 | the caller's own numpy pre-screen passes the empty array too, and the downstream `min` reduction then raises | **LATENT** |

**Banked numbers impeached: 0.** No previously-banked "certificate closes" result is
unsound on account of anything found here. The guard restores a *guarantee*, not a *margin*
— which is exactly what leg 116 said of its own findings, and exactly what leg 128 said of
`two_point_dual`. The honest statement of this leg's value is therefore: the shared guard is
sound on every input the repository actually produces, and it has four ways to be silent on
inputs it does not.

## What was NOT done, and why

- **`solver/certificate_guards.py` is byte-identical to what this leg inherited**, under
  both branches of the gate. The YES branch forbids patching under this leg's authority.
- **`capabilities.py` is untouched.** The append-only entry is the **NO** branch's
  instruction; recording a pass would misstate the result.
- **No BLOG/TECHNICAL and no figure**, per the declared territory and the leg
  92/115/116/118/120/122/124/139/142/143/144/146 precedent for an audit-family leg: the
  audit legs ship novelty + journal + runner + JSON + battery, and only the *repair* legs
  (128/NKR) ship a write-up pair. Leg 170's novelty pass states the same precedent
  explicitly.
- **Leg 128's scope limit is inherited, not re-litigated.** A hypothesis-*satisfying* input
  that lies about a magnitude (leg 116's surviving 4; leg 147's `P06_Z2_shrunk_1e-6` at
  714285.7×) is structurally invisible to any hypothesis guard. Finding it again here would
  be a re-measurement, not a finding.

## For whoever lands the repair

The 7 GAP-PIN gates in `test_certificate_guards_adversarial.py` each **assert the defect**
and name their own inversion in the docstring, with the pre-repair magnitude kept (lesson
68). Two constraints bind any repair, and both are already executable:

1. **Leg 128's stop condition.** This module "may NEVER change a number that a
   hypothesis-satisfying input already produced". `test_control_legitimate_inputs_are_still_accepted`
   (13 values) and `test_gap_m1_…`'s pin on `2.537396530974982` enforce it.
2. **The repair is one line for M1.** `test_gap_m1_localised_…` is the evidence that the
   `gamma` site is already clean, so an `isinf` test added beside the existing `isnan` one in
   `unit_range_violation` closes M1 without touching the range logic.

## Files

- `experiments/p2_route_cga_v1_adversarial.py` — the runner, 67 cases
- `writeup/data/p2_route_cga_v1_adversarial.json` — every case, magnitude and blast-radius row
- `test_certificate_guards_adversarial.py` — 12 gates (7 GAP-PIN, 3 HOLDS, 2 CONTROL), 12/12 pass
- `writeup/novelty/leg_199.md` — the novelty pass, committed before construction
