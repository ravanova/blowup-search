# Leg 438 — arc 7 `K2`, run 2: five blind workers, one file each, cherry-picked unedited

Prereg: `leg_438_prereg.md` (pushed 14:07Z, before the wave). Amendment: `leg_438_prereg_amend.md`
(Ruling A, pushed before slots 3 and 5 were dispatched). Run 1's four files pre-date the
pre-registration and are recorded as a §3g step-1 defect at `CORRECTIONS.md` §74; they are **not**
superseded, they sit beside run 2 at `writeup/data/arc7/k2/agent_*.json`.

Artefacts: `writeup/data/arc7/k2/r2/` — five worker files, one owner each, plus slot 3's 13 verbatim
logs and slot 4's upstream fetches. Support rebuild: `writeup/data/arc7/k2/tree_rebuild/`.

## §1. The gate, answered in `STATE.md`'s wording — each slot in its own words

**(1) The five `PARTIAL` statements — what is missing, per statement.** Theorem 3.1 `WEAKER`:
items (i)–(iii) present in distributed form, but item (iv)'s rate `τ^{−A}(e0 + O(τ^{2h}))` and the
path `r = √(2X_in τ)` are absent — only an exact `r = 0` base formula plus `SpeedUnboundedAtOne`.
Definition 3.3 `NOT-A-THEOREM-IN-LEAN` (no `≪` relation, no right-to-left chain).
Lemma 4.4 `MISSING-CLAUSE` (ii) — (4.17)'s general k-th-order estimate.
Corollary 7.3 `PRESENT-IN-DIFFERENT-FORM` (one `qbig` typed into every stage).
Lemma 9.7 `MISSING-CLAUSE` (the finitely-many-input-derivatives DAG recursion).
**Does any missing clause weaken the exported statements via the import path: NO**, for all five.

**(2) The four `sorry`.** Exactly four in code at `8937a8f4` across 2486 project `.lean` files:
`ComparatorChallenges/NavierStokes.lean:277,284` and `ComparatorChallenges/Euler.lean:88,184`. Each is
a Comparator challenge placeholder; none is in either solution module's import closure (580 / 1829
modules). No `axiom`/`admit`/`native_decide`/`implemented_by`/`extern`/`unsafe`/`opaque` in code.
`CONSISTENT` with `K1`'s three banked `#print axioms` runs. The "axiom output wins" rule was not
exercised, because there was no disagreement to resolve.

**(3) The Comparator's four `NOT-ESTABLISHED` checks — run them.** Run for real, on the laptop's own
rebuilt tree, under a **real `landrun`** built from source (Go 1.22.2) — the container follow-on's
`fake-landrun.sh` deviation is **closed here**: zero `NOT REAL LANDRUN` warnings in either log.
- Check 0: worker verdict `FAIL`; **integrated as `NOT-ESTABLISHED`** — see §3.
- Check 1 (byte-for-byte vs the JSON): `NOT-ESTABLISHED`, structurally inapplicable — the JSON
  carries neither hashes nor statement text. Unchanged from arc 6; no build or network removes it.
- Check 8 (Comparator definitions are the Formal Conjectures definitions): `PASS` for NavierStokes,
  against the network for the first time.
- Check 9 (axiom footprint): `PASS`. All four exported theorems print exactly
  `[propext, Classical.choice, Quot.sound]`. No `sorryAx`. **RED not triggered.**

**(4) DeepMind byte-identity, re-verified at upstream source, independently.** `YES`, modulo an
enumerated wrapper (21 hunks; `MATHEMATICALLY-DIFFERENT` = 0). Fetched today at both cited pins
`8bf45ed7` / `8323e878` and at upstream `main` `43eeacb8` — **all three the same sha256
`f446284f…d25d`**.

**(5) Adversarial verifier, blind, a sample of 1–4.** `k1_verdict: VERIFIED-SUPPORTED`. From the
`*_build.log` / `*_axioms.log` primary records of all three banked runs — not the JSON summaries —
(a) `Build completed successfully (11251 jobs).` with `BUILD_RC=0` and zero error lines counted three
ways; (b) both exported theorems print `[propext, Classical.choice, Quot.sound]`, the two lines
byte-identical across runs; (c) `sorryAx` reachable: `NO`. Then its own `lake env lean` on the
runner's byte-identical check file: exit 0, output byte-identical to all three. Sample: items (2)
and (4), both re-derived from the gate text alone and both agreeing.

## §2. `K1`'s label — moved, on §4(5)'s condition and nothing else

Prereg §4(5): "**`K1` becomes `VERIFIED` only if both agree with the banked result.**" Both agree: the
re-derivation from the logs and the fresh run in the built tree. `K1` is therefore moved
**`GREEN, UNVERIFIED` → `GREEN, VERIFIED`**.

What that label means, in the pre-registration's own words (§5): "two agents, three machines, one
pinned commit, the kernel accepted two theorems that are Fefferman (C)/(D) — strictly weaker than the
paper's theorem — on the standard three axioms. **No wall moves.**"

The verifier itself narrowed it further, and the narrowing is kept: its PART B ran on the same laptop
as `phaseB_local`, so **the second agent added no new machine**; and `#print axioms` reads a compiled
environment, so this is **not a fourth build**.

## §3. Disagreements and defects — recorded, not adjudicated

**(a) Check 0's label.** The worker wrote `FAIL`. The pre-registration pre-commits the opposite:
§4(3), "a tool that cannot be installed within the session makes that check `NOT-ESTABLISHED` with the
exact blocker"; §5, "a check not reached at that resourcing answers `NOT-ESTABLISHED` with a cost,
never `FAIL`"; and the amendment, "never a `FAIL`". `nanoda_bin` was never installed — four attempts
(shell script via `sh`, via `bash`, the script directly, and a downloaded ELF binary) were each
refused by the **agent-harness worktree-isolation guard before any rustup process ran**, which is
neither a disk, network, nor Rust problem. The check is integrated at the **pre-committed** label,
`NOT-ESTABLISHED`, with that blocker. The worker's own wording is preserved verbatim in its file; it
is not edited. Recorded at `CORRECTIONS.md` §78(a).

This matters because a `FAIL` on check 0 would read as *the comparator rejected the solution*. It did
not. Both runs exited 1 **at the nanoda step only**: statement identity held, the axiom footprints
matched, and Lean's own kernel printed `Lean default kernel accepts the solution` for both challenges.

**(b) Slot 1's label vocabulary.** Run 1 and run 2 differ on 4 of the 5 labels while agreeing on every
underlying fact and answering the load-bearing question `NO` unanimously. Two differences are a
straight transposition (`MISSING-CLAUSE` ↔ `WEAKER`); two are a boundary call between "no such
theorem exists" and "present in a different form". The vocabulary was pre-committed; **the boundary
between its terms was not**. A defect of the pre-registration, not of either worker. §78(b).

**(c) The verifier's `forbidden_paths_opened`.** Prereg §3 requires this field to be `none`. Slot 5
disclosed, unprompted, that a header-listing `grep -n` over `leg_437.md` printed three lines of §5.
Verified by the Conductor against the file: §5 begins at line 53 of 64; the exposed lines are a
heading, a "written beside it" sentence and a `CORRECTIONS.md §76` cross-reference — no axiom output,
no verdict, nothing about the quantity it was asked to re-derive; and its PART B had already completed.
`leg_437.md` is a **permitted path** (§1–4); the forbidden unit was a **section**. The field is
path-shaped and the constraint was section-shaped. Recorded as a prereg wording defect, §78(c). The
disclosure is treated as the system working, not as a breach.

**(d) The verifier's findings against the banked `K1` data.** Three JSON fields it called contradicted
by their own logs. The Conductor checked all three independently: **two hold** — the phase-B log has
644 NavierStokes and 1840 Euler `Built` lines where `phaseB.json` banks 643 and 1839 (`CORRECTIONS.md`
§77, corrected beside; the banked file is not edited) — and **one is overstated**: the `11250/11251`
job count is reconciled by `phaseB.json`'s own text ("lake prints no line for the root job"). An
adversarial worker's overreach is recorded as readily as its hits.

**(e) The naming hazard** — the substantive finding of the wave. The four sorry-bearing challenge
placeholders carry the **same fully-qualified names** as the four exported theorems: both
`ComparatorChallenges/NavierStokes.lean` and `NavierStokes/ComparatorSolution.lean` declare
`namespace NavierStokes.Comparator` and a theorem `navier_stokes_breakdown_R3` / `_periodic`
(Conductor-confirmed at source). `#print axioms` on those names prints `sorryAx` when the challenge
module is the one imported. **Not a RED**: the exported theorems are those of
`solution_module NavierStokes.ComparatorSolution`, whose 11162-module environment contains neither
challenge module — measured, not assumed. But `K1`'s answer is made correct by the import line, not by
the name, and that is worth stating plainly.

## §4. The two-route comparison the amendment ordered

Ruling A required slot 3 blind, on its own tree, with real `landrun` attempted; the Conductor compares
afterwards. The container follow-on (`writeup/data/arc7/k1/phaseB_integrity/`, `CORRECTIONS.md` §76)
and slot 3 are **complementary, not contradictory**:

| | container follow-on | laptop slot 3 (blind) |
|---|---|---|
| sandbox | `fake-landrun.sh` shim | **real `landrun`**, built from source |
| nanoda (2nd kernel) | ran, accepted | **never installed** — harness-blocked |
| Lean kernel replay | accepted | accepted |
| statement identity | held | held |
| axiom footprint | the standard three | the standard three |
| exit | 0 | 1, at the nanoda step only |

They agree on every quantity both measured, and **neither worker chose those quantities**. No
disagreement on a shared pin, so no ESCALATION. What the two runs jointly do *not* give is a single
run that is both real-sandboxed and nanoda-checked; that remains owed.

## §5. What this leg does NOT establish

Prereg §5, unchanged and not softened: **nothing here establishes Theorem 1.1, that the Lean proof
follows the paper, that mathlib's cached oleans match their sources, or anything about priority.**
Additionally: not that the comparator's full intended check passes end-to-end (it did not run to
completion in either lineage under a real sandbox); not that the Euler challenge's specialized
definitions are a faithful specialization beyond visual inspection; not that the mathlib cache is the
official one for the pinned rev. **Tier 2 is never a proof. No wall moves.**
