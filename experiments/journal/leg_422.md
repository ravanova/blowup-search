# Leg 422 — unit `U6`, arc 6, SOLO MODE (§3f): LAND

**Date:** 2026-09-09. **Role:** DOCS unit. **Artefacts:** `writeup/6_adjudicated/` (the quartet),
`writeup/figures/fig112_*.png`, `STATE.md`, `WALLS.md`, `WALLS_HISTORY.md`, `README.md`,
`writeup/README.md`, `writeup/build_figures.py`, `reports/ORCH_STATE.md`, this journal.

---

## §1. THE DOCUMENTATION CONTRACT (§6), item by item

| §6 item | arc 6 |
|---|---|
| **① a runner in `experiments/` that produced the numbers** | `experiments/arc6_instantiate_v1.py` (`U4`), `experiments/arc6_w4_port_v1.py` (`U5`), and the module `solver/arc6_residual_ledger.py` (`U3`) with `test_arc6_residual_ledger.py` and `capabilities.py` row 58. **`U1` and `U2` have no runner and that is stated rather than skipped:** they fetch, hash, grep and diff; they compute nothing. |
| **② curated data, every prose number present** | five JSONs — `arc6_acquire_v1`, `arc6_lean_v1`, `arc6_skeleton_v1`, `arc6_instantiate_v1`, `arc6_w4_port_v1` |
| **③ `BLOG_*` + `TECHNICAL_*` and an `*_evidence.py` rebuilding from the JSON without re-running anything** | `writeup/6_adjudicated/BLOG_ADJUDICATED.md`, `TECHNICAL_ADJUDICATED.md`, `adjudicated_evidence.py`. **It runs all five per-unit evidence scripts and requires each to exit 0**, then looks up every prose number in the JSON it came from. **Nothing is re-run:** no residual is recomputed, no manuscript re-fetched, no Lean parsed — `CORRECTIONS.md` §54's lesson, that a script re-running the experiment it checks cannot disagree with it. |
| **④ a figure registered in `build_figures.py`** | **`fig112`**, registered, **self-checked** (five assertions on its own content), cited in both documents. `check_figure_coverage.py` lists it under REBUILT **and** SELF-CHECKED. |

**Arc 5 recorded item ④ as a deliberate gap — *"this arc has no measurement of its own to plot"*.
Arc 6 has measurements and does not have that gap.** The repository's **pre-existing** figure debt
(52 cited-with-a-`.png`-but-not-rebuilt, 14 cited-with-no-`.png`) is untouched by this arc and is
neither reduced nor added to.

## §2. RETIREMENT UNDER §3j — SIX BLOCKS, VERBATIM, BEFORE ANY ARC-6 ROW WAS ADDED

**§3j: "the mechanism is retirement, not compression."** Retired verbatim, each replaced by a
pointer:

| from | block | to |
|---|---|---|
| `STATE.md` | the `⚠ 2026-08-18` `W4` block | `WALLS_HISTORY.md` §STATE-W4-2026-08-18 |
| `STATE.md` | `WAVE 9` | §STATE-WAVE9 |
| `STATE.md` | `WAVE 7` (`E-FE`) | §STATE-WAVE7-EFE |
| `STATE.md` | `WAVE 6` (`V5`) | §STATE-WAVE6-V5 |
| `WALLS.md` | the `§51` under-claim paragraph | §W4-L6-LADDER |
| `WALLS.md` | the `§52`/`§53` divergent-functional paragraph | §W4-JDIV |

`STATE.md` went **23,743 → 20,575 → 23,548** bytes; `WALLS.md` **32,162 → 33,478 (OVER) → 32,157**.
**The over-cap intermediate is recorded rather than hidden**: `W4`'s new paragraph put the file
`710 B` over, `test_headroom.py` caught it, and a second block was retired instead of the paragraph
being trimmed. **Nothing was deleted to make room.**

## §3. WHAT LANDED IN THE LIVE SURFACE

- **`STATE.md`** — an arc-6 block with the six gate answers in their own words, the two new
  measurements (`α = −1.000004`, `δ = 1.974126`), and the discharge of **Open item 3** (Fefferman
  (8) and (9) read at primary).
- **`WALLS.md` `W4`** — `U5`'s measurement beside the wall: **clause (a) ported against and it does
  not break**, with both failures, the log, what is **not** claimed, and **`W4` STANDS**.
- **`reports/ORCH_STATE.md`** — a new LIVE block (5,345 / 8,192 B); the previous one demoted
  verbatim. It records the arc's **own defects** — `U4`'s wrong pre-registration formula, `U5`'s
  failed control `K1`, `U2`'s 4→5 `sorry` re-count, `U5`'s `UNDER-RESOURCED` stencil — and what arc
  6 **did not do**.
- **Escalation row 1** gains a **measured fact** (`U1`: the manuscript claims (D)) and **is still
  not ruled**. §3h rule 1 binds and an entity that both raises and rules an escalation has defeated
  the mechanism.
- **`README.md` / `writeup/README.md`** — arc 6 is the new entry point, with **what it does not
  establish stated in the same breath as what it does**.

## §4. WHAT THIS ARC DID NOT DO

- **Did not read manuscript §§4–9 or Appendices A–C.** 131 of 166 pages are unread and every
  statement about them in this arc is a statement about their *role*, taken from §3's outline.
- **Did not compile the Lean.** The blocker is measured and named: the mathlib olean cache host is
  **egress-denied**. The source build was started and **left running**; its final state is not known
  here. **A future session should re-check it, and if it contradicts `arc6_lean_v1.json`'s gate (a),
  record that as a `CORRECTIONS.md` entry rather than editing the gate.**
- **Did not rule any escalation and raised none.** The charter reserves an escalation for a `W4`
  `YES`; the answer was `NO`.
- **Did not move a wall, a link, or the odds.** `W4` stands. `L1 → L4` links moved: **0**. Clay
  **~0.05%**. **Every gate `UNVERIFIED`.**

## §5. THE ARC'S OWN DEFECTS, IN ONE PLACE

1. **`U4`'s pre-registration formula was wrong** — it omitted axial diffusion — and **`U4`'s own
   planted control caught it**. The conjunction is reported **UNMET** and **not re-scored**.
2. **`U5`'s control `K1` failed** its pre-committed tolerance (`0.060` vs `0.05`). Reported as
   failed; the diagnosis sits beside it, not instead of it.
3. **`U2`'s first `sorry` count was 4** and its own evidence script returned **5**. The larger
   number is banked with the split.
4. **`U5`'s `M6` stencil is `UNDER-RESOURCED`** (spread `0.857` vs `0.025`) and the conclusion is
   drawn from a different, stable measurement.
5. **`WALLS.md` went 710 B over its cap** during this leg and `test_headroom.py` caught it.

**Five defects, all of them found by this arc's own machinery rather than by a reader.** In solo
mode that machinery — pre-committed gates, planted controls firing both ways, executable evidence
scripts, and the headroom test — is the whole defence, and §3f says to weight it accordingly.

## §6. ARC 6 IS COMPLETE

Six units, six gates, six legs. **`U5`'s `NO` is the arc's result and it is banked with the same
care a `YES` would have been.**
