# Leg 439 — unit `K3`, arc 7: WAVE 4 REDUX under one rule — the rule bit back

**Mode:** `ORCHESTRATION.md` §3g CONDUCTOR, fan-out ×5, on the operator's laptop.
**Pre-registration:** `leg_439_prereg.md` (on `main` at `b091c21`, 08:30Z, before any K3 run) and
`leg_439_prereg_amend.md` (Q6 dropped, before any run). Wave row committed before dispatch.
**Tier 2, not a proof.**

## §1. The verdict

**Zero of the five live gates survive as evidence.**

The wave's one rule was: *evidence is a two-route agreement on a quantity the adversary cannot choose.*
Applied honestly, the rule disqualified the gates that were written to satisfy it. The blind adversary
(slot 5) made **all five** live gates say `YES` from runs it knew to be wrong, each with a recipe and
numbers. Independently, the Conductor found that one of them (`Q5`) never had two routes at all — its
"numerical" route B is a closed form in `h` and a boolean that reads no field.

This outcome is mostly a finding about the GATES, and about the pre-registration the Conductor wrote.
It is **not** a finding that the manuscript is wrong. Nothing here shows any of `Q1`–`Q5` is false.

## §2. The gate table (`writeup/data/arc7/k3/merged.json`)

| gate | slot | worker | adversary | integrated status |
|---|---|---|---|---|
| `Q1` realised stress: closed form vs direct quadrature | 1 | `YES` — θ-entry sup-err `1.71e-14`, z-entry `1.54e-14` at `M=256`, slopes `−10.54` / `−10.58`, each normalised by its OWN sup and gated separately | `FAKEABLE` | **NOT EVIDENCE** (adversary faked it) |
| `Q2` viscous hierarchy exponent `2h` | 1 | `UNDER-RESOURCED` — route B needs Prop 4.2's `T_b`/`Z_b` on the outer profile, not built; cost stated, not faked | `FAKEABLE` | **NOT ESTABLISHED** (not instantiated; NOT a `NO`) |
| `Q3` per-stage gain `h/10` | 2 | `NOT-INSTANTIATED` — measured twin gains `~1e-14`/`1e-15` at `h ∈ {1e-1,3e-2,1e-2}`; `gain_scales_with_h_as_route_A_predicts: false`; cost of instantiating stated | `FAKEABLE` | **DROPPED by the rule** |
| `Q4` leading-order force, physical vs profile space | 3 | 4 cells `NO`, 3 `YES` of 8: pointwise `1.963e-4` (`h=1e-7`) and `9.28e-3` (`h=1e-3`) against a `1e-6` gate = `NO`; stencil error FLAT across three halvings (`slope_mean 4.65e-4`) = `NO`; sup-exponent `−1.5000001` route A **and** route B at `h=1e-7` = `YES`/`YES` | `FAKEABLE` | **4 cells `NO`**; the 3 `YES` cells **NOT EVIDENCE** |
| `Q5` `ℬ`'s exact zero with/without the heat factor | 4 | `YES` at both `h`, both legs, `abs_error` **exactly `0.0`** in all four cells | `FAKEABLE` (its `F5`) | **NOT EVIDENCE** — the two routes were one closed form evaluated twice (§4) |
| `Q6` flat weight's linear coefficient | 4 | — | not attempted | **DROPPED before any run** (`leg_439_prereg_amend.md`, §75) |

Also dropped, by slot 3, under the rule and recorded: `V1`/`V3` (norm scalings and refinement stability
of a prescribed field are the same Jacobian identity evaluated twice on any second route).

## §3. `X1` — the blind adversary, row by row

All of `Q1`–`Q5` `FAKEABLE`, each with a recipe AND numbers. Recorded, **not adjudicated**.

- **F1a** `Q1`, wrong profile: relative error `3.5e-16`.
- **F1b** `Q1`, a harmonic-256 contaminant contributing `0.18` — **31% of a typical entry `0.577`** — is
  invisible at every Simpson node. *Conductor reproduced this exactly:* `sin(256θ)` evaluates to `1.7e-13`
  at every node for `M ∈ {16, 64, 256}`.
- **F2** `Q2`, assert `R(q) = 4.2·q^{2h}` and fit the slope: nothing solved, no profile read, `h` an input
  exponent. *Conductor reproduced:* recovers slope `2h` to relative `1.3e-15`.
- **F3** `Q3`, gain in the operator's weight — a real tridiagonal solve of the **wrong problem**; the gate
  never looks at which problem was solved.
- **F4** `Q4`, a Gaussian stand-in stress.
- **F5** `Q5`, the pinned builder with `rho` 170× and the wrong cutoff — `ℬ` beyond `X_b` is the
  construction's own constant `(2+2h)(s_h − 1)`, independent of `rho`, of the cutoff shape, and of the
  profile's normalisation.

**`X2`, the adversary's own summary:** every one of the five gates tests an algebraic RELATION that a
FAMILY of objects satisfies, and pins the OBJECT only through route A's prediction — which is exactly
what the adversary is free to choose.

## §4. `Q5`: two routes that were one closed form

`experiments/arc6_residual_v1.py:101-103` (pinned, unedited) computes

    sh = 1.0 if heat else 0.0
    calB_beyond = (2 + 2 * h) * (sh - 1.0)

— algebra in `h` and a boolean, reading no field, no profile, no quadrature. Route A is
`0.0 if heat else -(2.0 + 2.0*h)`: the same algebra. Hence `abs_error` is **exactly `0.0`** in all four
cells rather than the `~1e-16` an independent numerical route gives. **An error of exactly `0.0` rather
than `~1e-16` is the fingerprint of one closed form evaluated twice**, and is now a standing diagnostic.

Conductor's confirmation, run before the finding was recorded: bit-identical to `−(2+2h)` at
`h = 1e-7, 1e-3, 3e-3`; **invariant** under `c_o=0.5`, `cutoff='poly'`, `flip=True`, `eps_moment=1e-3`
(one value, `−2.002`, across all of them) — **a quantity that does not move when the profile is replaced
measures nothing about the profile**; and `psi_hat`, `fop_hat`, `lW` are all exactly `0` beyond `y = 3`,
so the worker's stated cross-check against `fields.B_hat_eta0` collapses onto the SAME term.

This is a defect of the PRE-REGISTRATION, **not of slot 4**, which executed §3 `Q5` faithfully, banked
its numbers and ran both planted controls. The blind adversary reached the same conclusion independently
(`F5`) — two independent routes to the same defect. `CORRECTIONS.md` §79(a).

## §5. Disagreements — recorded, not adjudicated

1. `Q1` — worker `YES` vs adversary `FAKEABLE` (slot 1).
2. `Q4` — worker `YES` on the exponent cells vs adversary `FAKEABLE` (slot 3).
3. `Q5` — worker `YES` vs adversary `FAKEABLE` (slot 4), plus the Conductor's independent finding.
4. **A control that did not fire as planted** (slot 3, `A_to_A+0.1`): route A moved `−0.19999999998`,
   route B `−0.09999999998`, against a planted `−0.1`. Slot 3 reported this as a defect **with a full
   diagnosis rather than hiding it**. Diagnosis: the `h`-test holds `D` pinned while substituting
   `A_vel = 1/2 + h`, which breaks the paper's own `A + D = 1` identity that route B's pressure term
   relies on. **A defect of the plant, not of the run** — and it is downstream of §79(b).

## §6. Defects in this leg's own conduct

**Three pre-registration defects, all the Conductor's** (`CORRECTIONS.md` §79):

(a) `Q5` had no second route (§4).
(b) The second scale `h = 10⁻³` **violates the construction's own validity**. At `λ = 0.1`, Lemma 4.8 /
    (A.6) requires `h < min{1/100, λ, e^{−T_d}}` with `e^{−T_d} = 3.0·10⁻⁶`. Confirmed directly:
    `arc6_profile_v1.build` builds at `h = 1e-7` and `1e-6` and raises
    `AssertionError: (A.6)/Lemma 4.8: h < min{1/100, lambda, e^{-T_d}}` at `h = 1e-3`. §0's scale check
    verified that the scale was RESOLVABLE; it never asked whether the scale was ADMISSIBLE. Slot 3
    found this unaided and declared it; slot 2 likewise confined its large `h` to the tail-stress
    operator. Both were honest; the prereg was wrong to ask.
(c) `Q2` cited Proposition 9.1, whose own (9.2) table slot 1 read and found to contain **no `q^{2h}`
    term** — every entry `≤ 1`, giving `q^{h·gain} ≤ q^h`. The quantity traces to p. 47 and p. 16.

**§71's lesson is restated in a stronger form:** a scale check must ask, per gate, (i) is the quantity
resolvable, (ii) is the scale admissible to the construction's own hypotheses, and (iii) does route B
read anything the profile determines.

**(d) Harness loss and a method change.** Slots 1, 3 and 5 were killed mid-run by an API session rate
limit. Slots 1 and 3 had pushed nothing and their worktrees were removed — that work is gone and was
re-run from the brief. Slot 5's artefact was fully written but never committed; it was rescued from its
worktree and committed **UNEDITED** by the Conductor on the worker's behalf (`a574d89`), provenance in
the commit message. Re-dispatched workers were told to commit the `claimed` block and the artefact as
soon as each is valid; slots 1 and 3 therefore carry independently timestamped pre-run commits
(`879f421` — `status: "CLAIMED WRITTEN, RUN NOT YET STARTED"`, no gates block — and `ce63cf9`).

**(e) A limitation on slot 2, recorded:** its `claimed`-before-run ordering rests on its own `--claimed`
mechanism inside a single commit. Asserted, **not** independently timestamped, unlike slots 1 and 3.

**(f) A limitation on slot 1, recorded:** `Q1`'s two routes share the same frozen fine ODE solution and
differ only in the reconstruction QUADRATURE. That is weaker than `Q4`'s genuinely different machinery,
and it is precisely the seam the adversary's `F1b` exploits. Not the `Q5` failure mode — its errors are
`~1.5e-14`, not exactly `0.0` — but not two independent routes to the object either.

## §7. What this leg does NOT establish

Nothing here says any of `Q1`–`Q5` is false, or that the manuscript's algebra fails. A `FAKEABLE`
verdict says only that the gate **as pre-registered** admits a run known to be wrong, so a `YES` from it
is not on its own evidence. `Q4`'s four `NO` cells are failures of *this* two-route comparison at *this*
resolution, not refutations. No wall moves. Tier 2 throughout, and every gate here stays `UNVERIFIED`
until a blind agent reproduces it.

## §8. Next

`K4` (leg 440), SERIAL: `writeup/7_confirmation/`. Leads with the verdict; every damning finding sits
under the verdict it qualifies; does not exceed `READING_THIS_REPO.md`'s canonical claims list; takes
**no** position on the priority dispute, and says so explicitly.
