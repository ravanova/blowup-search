# Leg 375 — Route-PBLG: write the missing BLOG for Route-PORT v2, close the quartet-gap list

**Branch** detached HEAD off `origin/main` (worktree `.leg375-work`), `e2ec84f`. LEG role
(spec calls it PBLG, record/prose work), difficulty "light", `[FLOOR-ELIGIBLE: no]`.
`plan_of_record.py` and `DIRECTION.md` untouched. No solve run, no re-measurement, no
banked JSON edited anywhere (`p2_route_port_v2_reach_evidence.py` was not re-run; its own
byte-for-byte output was already committed).

## The debt

Route-PORT v2's quartet (TECHNICAL, evidence script, figure, BLOG) was missing exactly its
BLOG. `writeup/INDEX.md`'s gap-list item 2 named this as the only remaining open clause on
the whole quartet-gap list.

## What was read, in full, before writing anything

- `writeup/4_p2_lottery/TECHNICAL_P2_ROUTEPORT_V2.md` (105 lines, whole file)
- `writeup/data/p2_route_port_v2_reach.json` (whole file, 5 rungs + verdict block)
- `writeup/4_p2_lottery/BLOG_P2_ROUTEPORT_V1.md` and `BLOG_P2_ROUTED_DRESS.md` for house
  voice/convention (negative-result BLOG posts, "still a toy model" framing, explicit
  "what this doesn't say" section, Clay-odds closing line)
- `writeup/INDEX.md` lines 86, 293–303 for the exact row and gap-list-item-2 text to edit

## Traceability table — every number in the new BLOG, and its source

| BLOG claim | Value | Source |
|---|---|---|
| Ratio slope (headline) | +0.4703 dec/unit ρ (stated as "+0.47") | TECHNICAL §2 code block `d log10(ratio)/dρ = +0.4703`; JSON `verdict.slope_log10_ratio_per_rho = 0.47033361126385204` |
| Distance slope | −0.0196 dec/unit ρ | TECHNICAL §2 code block; JSON `verdict.slope_log10_distance_per_rho = -0.019575251606707186` |
| r_max slope | −0.4899 dec/unit ρ | TECHNICAL §2 code block; JSON `verdict.slope_log10_r_max_per_rho = -0.4899088628705595` |
| Gate threshold | −0.05 | TECHNICAL §1 ("Slope ≤ −0.05 → brute force closes ... Slope > −0.05 → tail lemma forced"); JSON `verdict.slope_gate = -0.05` |
| Full rung table (ρ_max, X_max, distance, r_max, ratio) | 5 rows, ρ=6..10 | TECHNICAL §2 markdown table; JSON `rungs[]` array, fields `rho_max`, `X_max`, `distance`, `r_max`, `distance_over_r_max` |
| Distance not monotone, rises over last 3 rungs 0.184→0.205→0.331 | — | TECHNICAL §2 prose; JSON `rungs[2..4].distance` = 0.18360836226225985, 0.2048678787748369, 0.3305974502606743 |
| r_max falls ~factor of 70 across ladder | — | TECHNICAL §2 prose ("a factor of ~70 across the ladder"); JSON r_max ρ=6: 5.296e-9 vs ρ=10: 7.558e-11, ratio ≈70 |
| w_l = 0.01·X_max by construction | — | TECHNICAL §2 prose |
| Gap at ρ=10 vs ρ=6 is 63× worse, computed as 4.374e9/6.944e7=62.99 | — | TECHNICAL §2 prose (exact sentence, including the parenthetical); JSON `rungs[0].distance_over_r_max` = 69442303.3, `rungs[4].distance_over_r_max` = 4374013820.4 |
| Reproduction-audit correction (28.16× was the wrong ρ=8→10 baseline, understating by 2.25×) | — | TECHNICAL §2 prose, same sentence as above |
| Verdict: reach cannot close the gap at any size; tail lemma forced | — | TECHNICAL §3 prose |
| Analytic far-field enclosure description | — | TECHNICAL §3 prose (paraphrased, no new claim added) |
| Re-pricing of the L1 road (interval arithmetic = engineering, `solver/interval.py` exists unwired; tail lemma = mathematics, forced, unwritten) | — | TECHNICAL §3 prose |
| "What this does not say" (doesn't say object uncertifiable; doesn't invalidate leg 46/earlier certificate; says nothing about Clay) | — | TECHNICAL §4, all three bullets |
| Clay odds ~0.05% | — | TECHNICAL header line ("Clay unchanged at ~0.05%"); consistent with every other BLOG in this repo (e.g. `BLOG_P2_ROUTED_DRESS.md`) |
| Runtime 4.4s, 3/3 pre-committed clauses | — | TECHNICAL header line and §5; JSON `wall_s = 4.4`, `predicate_checks` all `true` |

No number in the new BLOG falls outside this table. No re-derivation, recomputation, or
independent re-measurement was performed anywhere.

## What was built

- `writeup/4_p2_lottery/BLOG_P2_ROUTEPORT_V2.md` (new) — the missing quartet piece.
- `writeup/INDEX.md`: the Route-PORT v2 row (line 86) updated from
  `**GAP: T only, no BLOG**` to `Y` with the `[T]·[B]` link pair and a "quartet completed
  by leg 375, gap-list item 2 now closed" note (mirroring the exact convention leg 60 used
  for Route-PORT v1's closure). Gap-list item 2's own paragraph (lines 293–303) appended
  with a `✅ CLOSED by leg 375` clause, stating the list now stands at 0 open items. No
  other row and no other gap-list item touched — confirmed by diff (see below).
- `writeup/novelty/leg_375.md` (new).
- This file (new).

## Verification

```
$ git status
  (only the four territory files touched)
$ git diff --stat -- writeup/data/
  (empty — no banked JSON touched)
$ git diff -- writeup/INDEX.md | grep '^-' 
  (only the two intended lines removed, the Route-PORT v2 row and gap-list item 2's
  closing sentence)
$ git diff -- plan_of_record.py DIRECTION.md
  (empty — untouched)
```

## Outcome

Gate closes clean: the BLOG exists, every numeric claim is traceable per the table above,
the adverse headline (+0.47 dec/unit ρ, gap worse not better) is stated at full strength as
the post's title and opening, and `writeup/INDEX.md`'s quartet-gap list's last open clause
(item 2) is now closed. No claim was left open — nothing in the intended BLOG content
exceeded what `TECHNICAL_P2_ROUTEPORT_V2.md` and its JSON already support. Clay odds
unchanged, ~0.05%. `plan_of_record.py` and `DIRECTION.md` never touched.
