# Leg 310 — ROUTE-EPAP — journal

## Assignment

Per `DIRECTION.md`'s leg-310 entry: leg 287's own yes-branch instruction says non-portable
families (from its environment-portability census) each get exactly one `writeup/CORRECTIONS.md`
pointer ("values are environment-local; compare via fresh re-solve, not banked bytes"), citing
leg 287's specific census row. Leg 310's job is to execute that instruction, purely additively,
without touching any other CORRECTIONS.md entry, `plan_of_record.py`, or `DIRECTION.md`.

Prerequisite reading, per this leg's own dispatch note, done in full before any edit:
- `experiments/journal/leg_287.md` (leg 287's own journal, including its top-of-file leg-356/361
  correction banner)
- `writeup/novelty/leg_287.md` (the four registered predictions P1-P4)
- `writeup/data/p2_route_epa_v1_census.json` (the actual census table)
- `experiments/journal/leg_298.md` (leg 298's CORRECTIONS.md freshness audit, so as not to
  duplicate any defect-fixing already done through §28)

## Step 1 — census census: how many non-portable families did leg 287 actually find?

Read `writeup/data/p2_route_epa_v1_census.json` directly (not paraphrased from prose):

```
classification_counts: {'NON-PORTABLE': 1, 'PORTABLE': 9}
families_by_classification:
  NON-PORTABLE: ['p2_route_e_v1_spectrum']
  PORTABLE: [the other 9]
classification_counts_at_head: {'IRREPRODUCIBLE-AT-HEAD': 1, 'NON-PORTABLE': 2, 'PORTABLE': 7}
```

The primary measurement (banked bytes vs regeneration AT THE ARTIFACT'S OWN BANKING COMMIT —
"the condition leg 252 measured under", per the JSON's own `summary.primary_measurement` field)
is the one leg 287's gate answer is staked on; it finds exactly **one** non-portable family:
`p2_route_e_v1_spectrum`. The HEAD-comparison numbers add `p2_route_cap_v1_audit` as
NON-PORTABLE-at-HEAD and `p2_route_nka_v1_adversarial` as IRREPRODUCIBLE-AT-HEAD, but leg 287's
own gate answer (§6 of its journal) and its own CORRECTIONS.md §24 text explicitly attribute
those two HEAD movements to source-code change since banking, not environment portability, and
explicitly withhold a pointer for them ("no CORRECTIONS pointer is warranted for it because it is
not a claim about banked VALUES being untrustworthy"). So the family count that leg 310's gate is
actually about is **1**, not 3.

The specific census row for `p2_route_e_v1_spectrum` (`writeup/data/p2_route_epa_v1_census.json`
→ `families[]` → `family == "p2_route_e_v1_spectrum"` → `comparison`):
`max_rel_move: 1.8797120248172916`, `n_leaves_over_10pct: 62`, `n_leaves_banked: 1370`,
`n_numeric_leaves_moved: 756` of `1235` compared, worst mover
`E5_sweep[0].kept.0.1[5][1]`: banked `-8.1349` → regenerated `+7.1563`.

## Step 2 — does a pointer already exist?

`grep -n "p2_route_e_v1_spectrum\|NON-PORTABLE\|environment-local" writeup/CORRECTIONS.md` turns
up `## §24 — environment-portability census (leg 287): leg 252's finding generalizes to ONE other
family, not to the other eight numerically-live ones`.

Traced its provenance with `git log -S"§24 — environment-portability" -- writeup/CORRECTIONS.md`:
it was added in leg 287's OWN landing commit, `b0cf565676e819885e817b78378f3058ab9f6958`
("Leg 287: LEG — GATE ANSWERS YES..."), in the same diff that closed leg 287 — leg 287 executed
its own yes-branch instruction itself, before leg 310 was ever dispatched. Confirmed by reading
the full diff (`git show b0cf565 -- writeup/CORRECTIONS.md`): §24's text is exactly the pointer
the instruction calls for —

> "banked leaf values are environment-local and (per its own determinism control) not fully
> process-stable even within one environment; compare its findings via a fresh re-solve in the
> environment you are actually working in, not by reading the banked bytes as ground truth"

— matching the instruction's suggested wording ("values are environment-local; compare via fresh
re-solve, not banked bytes") near-verbatim, and citing the specific census numbers
(`max_rel_move = 1.880`, `62` leaves `>10%`, the `E5` sweep sign/ordering-swap example) — i.e.
citing leg 287's own census row, exactly as required.

§24 was subsequently touched twice, both by OTHER legs, both corrections rather than new pointers
for a different family:
- **§25 (leg 356, ROUTE-ESPX):** repaired the underlying defect in
  `experiments/p2_route_e_v1_spectrum.py` (a degenerate `match_filter` sort key causing the
  eigenvalue-ordering swap) and added an inline correction marker inside §24 itself, without
  deleting or renumbering it.
- **§26 (leg 361, ROUTE-LCB4):** traced and fixed a `VOLATILE_TOKENS` instrument gap
  (`"generated"` bare substring missing) in leg 287's own census script, and corrected §24's
  "within-environment nondeterminism" clause in place (that specific sub-claim was a false alarm
  from an uncaught timestamp leaf; the family's core NON-PORTABLE classification and
  `max_rel_move` finding were NOT affected and are not what §25/§26 correct).

Neither §25 nor §26 is a second pointer for a second non-portable family — both are corrections
scoped to the one existing §24 entry, consistent with "no more, no fewer, no duplicates across
entries." Grepped the full file for `p2_route_e_v1_spectrum` (11 hits total, lines 1672-1851):
all either inside §24 itself or inside §25/§26's corrections of it; none is a second independent
pointer.

## Step 3 — conclusion

Leg 287's own count of non-portable families (primary measurement): **1**
(`p2_route_e_v1_spectrum`). Existing `writeup/CORRECTIONS.md` pointers for that family: **1**
(§24, added by leg 287 itself, refined in place by §25/§26). **1:1 — the gate is already
satisfied.** Appending a second entry here would violate this leg's own standing practice
("exactly one CORRECTIONS.md entry per non-portable family — no more, no fewer, no duplicates")
and would misrepresent §24 as missing when it is present and correctly cited. No edit was made
to `writeup/CORRECTIONS.md`.

This is not "argument-touching resistance" in the gate's no-branch sense (there is no ambiguity
about which families are non-portable or dispute about wording/citation) — it is simply that the
work this leg was dispatched to do was already done, by leg 287 itself, in leg 287's own landing
commit. Recorded here rather than silently closed with no output, per this leg's "record work"
floor-ineligible status.

## Gate answer

**YES.** Every family leg 287 classified non-portable (exactly one: `p2_route_e_v1_spectrum`)
already has exactly one CORRECTIONS.md pointer (§24), citing leg 287's census row, and no other
entry's meaning was changed by this leg (none was touched — `writeup/CORRECTIONS.md` is
byte-identical before and after this leg, verified below).

## Territory / diff discipline

- `writeup/CORRECTIONS.md`: **untouched** (verification found nothing to append; touching it
  further would create a duplicate pointer, which the standing practice forbids).
- `writeup/novelty/leg_310.md`: new file, this leg's novelty pass.
- `experiments/journal/leg_310.md`: new file, this leg's journal (this file).
- `plan_of_record.py`, `DIRECTION.md`: not read for editing purposes beyond locating leg 287's
  and leg 310's own dispatch text; not modified.

## Runtime

Read-only investigation: `git log`/`git show` on two commits, one JSON read via `python3 -c`,
`grep` over `writeup/CORRECTIONS.md`. No solves run. Sub-minute.

## Clay odds

Unchanged, **~0.05%**. No solver file touched, no gate answer of any OTHER leg moved, no ban
lifted, no `L1 → L4` link moved.
