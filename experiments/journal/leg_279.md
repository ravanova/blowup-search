# Leg 279 — Route-Z1X: close the 198/218 `Z_1` disagreement as EXPLAINED, across every site that quotes either number

**Role** LEG (record maintenance). **Gate: YES**, all four clauses. **0 escalations.**

Artifacts: `writeup/CORRECTIONS.md` §8 + register row 7 (appended),
`writeup/4_p2_lottery/TECHNICAL_P2_PUB3_V1.md` §5(a) (dated update block appended),
`experiments/journal/leg_218.md`, `experiments/journal/leg_223.md`,
`writeup/novelty/leg_218.md` (dated pointers appended), `writeup/novelty/leg_279.md`.
`experiments/journal/leg_228.md`, `experiments/p2_route_bhrv_v1_postrepair.py` and
`writeup/data/p2_route_bhrv_v1_postrepair.json` were **read and never edited**.

**BANKED NUMBERS MOVED: 0.** No solver module, no shared ledger, no compute.

---

## 0. The open item, and why closing it is not the same as deciding it

Leg 198 measured `solver/bordered_hl.py`'s silent acceptance of an inadmissible border weight
and reported the `Z_1` understatement on `w_r = ±1e−6` as **`1.198e9×`**. Leg 218's re-run
measured **`2.2867e8×`** — `5.24×` apart — while `‖A‖` (`6.6712e+09×`) and `Z_2`
(`+1.188608e+17 / −1.779456e+07`) agreed to 4–6 s.f. Leg 223 banked both, adopted neither, and
printed it as one of three disagreements. It stayed open for 30 legs.

Leg 228 (Route-BHRV §4) closed it — **as explained, not as adjudicated.** The temptation this
leg had to refuse is the obvious one: pick the number that "won." There is no such number. Leg
228's measurement is that the disputed quantity spans **`11.77×`** (`2.6422e8` … `3.1108e9`)
under perturbations that provably cannot change the mathematics — algebraically identical
re-associations, and BLAS thread counts — against a disagreement of only `5.239×`. Thread count
*alone* covers the whole gap: `2.6422e8` at 1 thread, `3.1108e9` at 4, same code, same machine,
same inputs. And the control is the sharp number: the `‖A‖` ratio, the column both legs
reproduce, moves by **`7.2e−12` relative** under those same perturbations. A ratio whose
denominator is a near-total cancellation at the round-off floor is a statement about the code,
not about the operator — lesson 86, finally measured rather than asserted.

Leg 198's `1.198e9×` lands **inside** the band; leg 218's `2.2867e8×` lands `1.16×` **outside**
its minimum. Neither fact is a verdict on either leg, and this journal declines to round the
second into agreement. Leg 228's own third measurement (`1.801e9×`) is a third point in the same
band, not a tie-breaker.

**What the record now says everywhere: adopt NEITHER number. Quote `‖A‖` and `Z_2`.**

## 1. The sweep — scoped by artifact, enumerated before any edit

The standing scoping rule (`verify_276` §10: PUB2 needed six passes because each scoped to the
prior pass's defect list). The dispatch named four legs; the read-set was **the whole
repository**, grepped for both numbers and their roundings — `1.198e9`, `1.198e+09`, `2.2867e8`,
`2.287e8`, `2.2867e+08`, `2.287e+08`, `1198000000`, `228670000` — across every file type, `.git`
excluded, with two broad passes discarded for false-positive noise (`745.2394128947751` matches
`5.239`) before the anchored pass was kept.

**13 occurrences, 9 files.** Full enumeration, with disposition fixed by the material/cosmetic
split pre-committed in `writeup/novelty/leg_279.md` §4:

| file | lines | disposition |
|---|---|---|
| `writeup/4_p2_lottery/TECHNICAL_P2_PUB3_V1.md` | `410–411` | **carried** — dated update block appended after §5(a) |
| `experiments/journal/leg_218.md` | `60`, `182`, `258` | **carried** — dated pointer appended |
| `experiments/journal/leg_223.md` | `69`, `70` | **carried** — dated pointer appended |
| `writeup/novelty/leg_218.md` | `163` | **carried** — dated pointer appended |
| `experiments/JOURNAL.md` | `3173`, `3465`, `3924` | **flagged, not edited** — integration-owned ledger |
| `DIRECTION.md` | `7137`, `7361`, `7536`, `7612`, `7947`, `8958` | **flagged, not edited** — DM-owned dated dispatch records |
| `experiments/journal/leg_228.md` | `158`, `188` | read-only; **already states the closure** |
| `experiments/p2_route_bhrv_v1_postrepair.py` | `573–574`, `599–600` | read-only; the numbers are the variance test's **inputs** |
| `writeup/data/p2_route_bhrv_v1_postrepair.json` | `2530–2531` | read-only; banked |

Two calls worth stating rather than burying.

**`DIRECTION.md`'s six sites were not edited, and that is a judgement, not an omission.** All six
quote `1.198e9x` as the *corruption potential* that justified a dispatch ranking at the time
(`"1.198e9x/1.189e17x corruption potential"`). They are dated records of decisions already taken.
Under the pre-committed split they are cosmetic — no reader takes a dispatch-priority note as an
adjudication of a numeric dispute — and the file is the DM's besides. Annotating history is this
leg's job; rewriting it is not.

**`experiments/JOURNAL.md:3924` is the standing open item itself**, and it is the one site this
leg most wanted to close and had no right to. ORCHESTRATION.md §5a: the five shared ledgers are
integration-owned, no leg touches them. It is flagged in `CORRECTIONS.md` §8 with its line
number so the orchestrator can close it in the next integration commit.

## 2. Gate clause (c) — and the journal that does not exist

The clause names legs 198/218/223. **`experiments/journal/leg_198.md` is not on `main`**: branch
`leg/198-bha-v1` is unmerged, which leg 228 §5 independently records for the same leg's
characterization gates. Two of three journals were reachable and both carry the pointer.

The third was **not** manufactured. Creating a leg-198 journal to satisfy a clause would put a
sentence leg 198 never wrote into the historical record, in a leg whose entire purpose is
keeping the record honest. Reported as a magnitude instead; leg 198's number reaches the record
only through the seven non-198 sites above, every material one of which now carries the closure.

## 3. Append-only, verified

Every historical site keeps its original wording standing, with the closure **beside** it — the
register's own convention (leg 263) and this leg's pre-commitment. `git diff` shows **0 deleted
lines** across `experiments/journal/leg_218.md`, `experiments/journal/leg_223.md`,
`writeup/novelty/leg_218.md` and `TECHNICAL_P2_PUB3_V1.md`; every change is an insertion.
`TECHNICAL_P2_PUB3_V1.md`'s block explicitly preserves §5's framing (*"printed rather than
resolved"*, correct as of leg 223) and records that item (a) was resolved **elsewhere** — by leg
228, not by that document.

## 4. The escalation that did not fire, and why it was a real test

Gate `no`-branch: *any site where the substitution would change an argument.* Operationalized
before editing (novelty §2): a substitution changes an argument iff the site's conclusion depends
on which number is right, or on the disagreement being open.

**0 of 13 sites failed it.** The one that could have — `TECHNICAL_P2_PUB3_V1.md` §5(a), live
publication prose — already ends *"Neither number is adopted here; the verdict … is not in
dispute, and the contamination status (zero) is not affected."* That is leg 228's conclusion
reached a leg early, from a weaker reason (leg 223's provenance rule rather than a measurement).
The closure supports all three of its claims and contradicts none. This is why the gate answer
is YES rather than parked.

## 5. The second finding, carried but explicitly not repaired

Leg 228 §3 also measured leg 218's caller enumeration **materially incomplete**: 13 direct
importers today, 10 contemporaneous with leg 218 (membership by `git cat-file -e d9a20fb:<path>`,
not a name list), 6 named, **5 missed**, of which `p2_route_tn_v1_consistency.py:219` calls
`b.weights(p=P_STAR, w_l=0.01*|X|max)` and therefore **touches the weight surface** — material by
a split fixed in advance. The repair is unimpeached: leg 228 re-earned clause B over the full
ten-importer space at 45 configurations, 77,040 float leaves, **0** bit mismatches.

That note is in `CORRECTIONS.md` §8 per gate clause (b), with the full table. Its **consequence
sites** — `TECHNICAL_P2_PUB3_V1.md` §5(b) and `leg_223.md`'s disagreement 2, which reproduce the
six-name list — quote no disputed number, so they are outside this leg's sweep, and editing them
would breach clause (d). **Recommended follow-up leg:** carry leg 228 §3's ten-importer table to
both. Flagged, named, not quietly absorbed.

## 6. Bans and ceiling

`plan_of_record.py` read in full; no ban bites (not gCLM, not DSS in either entrance, no GA
compute, no `ℓ¹`-Fourier/collocation machinery, no re-claim of leg 51, no misreading of leg 53).

**0 arguments changed. 0 gate answers changed. 0 banked values moved. 0 solver modules touched.
0 lines deleted.** Legs 198's and 218's verdicts stand exactly as banked — a sign flip corrupts
these constants catastrophically, which was never the disputed part — as do leg 218's repair,
leg 228's YES on both clauses, and the contamination status (zero). No ban lifts, no route is
promoted, no link of the `L1 → L4` chain moves. What changed is one thing: **the record no
longer carries an open disagreement it had the measurement to close.** Clay `~0.05%`.
