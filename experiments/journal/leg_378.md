# Leg 378 — ROUTE-LCB6 — journal

**Gate (verbatim intent).** The accumulator's early-fire amendment for the empty-queue case:
when the accumulator holds at least one finalized item and nothing else is dispatchable, the
batch fires early rather than waiting for arbitrary company. Item (10) — leg 371's finalized
marker, citing the 367/369/371 trilogy — is the sole item in queue this cycle. Apply it at leg
356's exact journal site, annotate `writeup/CORRECTIONS.md` §25 with a cross-reference (do not
rewrite §25's own content), and append ONE new batched CORRECTIONS entry. Text-only: nothing
re-derived, nothing re-measured.

**Territory (strict).** `experiments/journal/leg_356.md` (marker, edit in place),
`writeup/CORRECTIONS.md` (append + §25 annotation only), `writeup/novelty/leg_378.md`,
`experiments/journal/leg_378.md` (this file). Everything else, including every banked JSON,
`plan_of_record.py`, and `DIRECTION.md`, is strictly read-only.

## 1. Locating the exact original claim

Read `experiments/journal/leg_356.md` in full (121 lines). §2 states, quoting the module's own
docstring in `solver/rescaled_spectrum.py`: "at a=0 the linearization's spectrum is known in
closed form — two isolated structural eigenvalues (0, -1) plus a CONTINUUM filling
`-1 < Re lambda < 1`, discretized as many eigenvalues pinned near `Re lambda = 0`." This is the
site named by the dispatch (leg 356's journal record of the "theoretically pinned to
`Re lambda=0` exactly" ground). The same substantive claim also appears, near-verbatim, in
`writeup/CORRECTIONS.md` §25(a) — "sits on the imaginary axis in exact arithmetic (`Re
lambda = 0`)" — which is leg 356's own CORRECTIONS entry, but that site is out of this leg's
territory to rewrite; only an annotation is added there (see §3 below), per the brief.

Cross-checked against leg 367's own dispatch text (`experiments/journal/leg_367.md` line 3-8),
which independently names leg 356's ground as "theoretically pinned to `Re lambda = 0` exactly"
— confirming this is the correct claim and that leg 356's journal §2 (plus §25(a)) is the correct
site, not a different passage.

## 2. Reading the trilogy's exact findings before writing the marker

- Leg 367 (`experiments/journal/leg_367.md`): (iii) UNVERIFIED-ASSUMPTION, empirically
  corroborated. No theorem located whose hypotheses match the actual `K x K` truncated matrix;
  the module's own closed-form proof covers only the analytic-at-`X=0` subspace (the two
  structural eigenvalues), and is silent on the other `K-2` eigenvalues. Six natural
  skew-symmetrization mechanisms tried, none derived the pinning. Empirical margin: ~6.5 orders
  below `canonical_order()`'s `1e-6` threshold.
- Leg 369 (`experiments/journal/leg_369.md`): GAP-NAMED. Generic non-normal eigensolver
  backward-error theory (LAPACK's own Table 4.5/4.6 bounds) cannot license the observed
  tightness — condition numbers (~1e15) put the matrix 4-5 orders outside the licensed
  precondition, and the only unconditional (disk) bound is ~19 orders too loose to be
  informative. That route was tried and explicitly foreclosed, not merely left untried.
- Leg 371 (`experiments/journal/leg_371.md`, terminal): STRUCTURE-DERIVED. Realness, channeled
  through LAPACK's real-Schur-form construction (`dlanv2.f`'s shared 2x2-block half-trace,
  finalized by the Ahues-Tisseur machine-epsilon-local deflation criterion in
  `dlahqr.f`/`dlaqr3.f`) — not the generic eigenvector-condition-number channel leg 369 ruled
  inapplicable. Independently reproduced via `scipy.linalg.schur` (LAPACK `dgees`, a second
  driver), matching leg 367's four measured points to within a factor of 0.93-1.68x. Leg 371's §7
  itself supplies the finalized item-(10) marker text, cited directly in the marker applied here.

## 3. Applying the marker and the CORRECTIONS.md work

1. `experiments/journal/leg_356.md` §2: inline `[CORRECTED 2026-08-12 by leg 378 …]` blockquote
   marker inserted immediately after the original sentence, quoting it verbatim in place (not
   deleted), recording the corrected status per §2 above, matching this repo's standing
   inline-marker convention (`experiments/journal/leg_287.md`'s and `leg_339.md`'s markers used
   as the style model).
2. `writeup/CORRECTIONS.md` §25: a short blockquote annotation added after the section's existing
   "ceiling" paragraph and before the `---` separator — points to the trilogy and to the new §30
   entry, does not alter a single word of §25's own existing prose.
3. `writeup/CORRECTIONS.md`: new §30 appended at the end of the file, the one batched entry for
   this leg, recording the correction and citing the trilogy.

## 4. Verification

`git status --short` before writing showed a clean tree (detached HEAD at `origin/main`). After
writing, `git status --short` shows exactly three modified/new paths:
`experiments/journal/leg_356.md` (modified), `writeup/CORRECTIONS.md` (modified),
`writeup/novelty/leg_378.md` (new), `experiments/journal/leg_378.md` (new) — matching this leg's
territory exactly. `git diff --stat` confirms `plan_of_record.py`, `DIRECTION.md`, and every
`writeup/data/*.json` file are absent from the diff. No banked JSON was opened for writing at any
point in this leg — all reads (of leg 356/367/369/371's journal files, and of `CORRECTIONS.md`
itself) were read-only.

## Ceiling

TIER 2. Correction/record-only leg (light, FLOOR-ELIGIBLE: no). No proof, no certificate, no
Clay claim, no measurement, no re-derivation — the trilogy's already-landed findings are recorded
accurately, not extended. `canonical_order()`'s repair is unaffected either way. Clay stays
~0.05%.
