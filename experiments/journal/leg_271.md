# Leg 271 — ROUTE-PUB2R2 — completing leg 268's correction across the whole claim family

**Gate: YES on all four clauses.** 7 sites repaired across the two PUB2 documents, of which
**3** were the ones `verify_268.md` named and **4** were not. Zero computation run; zero new
magnitudes; leg 268's companion artifact and leg 176's banked JSON are **0 bytes** changed.

---

## 1. Why this leg was not just "fix A, B, C, D"

Leg 268's clause-4 check keyed on the literal string `4.026`. That string occurs at exactly one
site, so the check passed — while the *claim* it stood for was asserted at three further sites
that never mention the number. `verify_268.md` found those three by reading. The recurring defect
shape is **"correction applied to some sites, claim family left standing elsewhere"**, and an
enumerated fix of the three named sites would have been the same instrument failure one iteration
later. So this leg's primary product is a **documented search over the claim, not over a string**,
recorded below so the exhaustiveness is checkable rather than asserted.

The search found a **fourth** untouched independence-family site (`BLOG` L99, the blog mirror of
TECHNICAL §5(1)) and a **fifth** verification-status site (`TECHNICAL` §0 L64–67, the twin of §7's
L629–631) that `verify_268.md` did not name. Both are the same defect class as the sites it did
name, in the same paragraph-pairs — which is direct evidence that enumeration was the wrong
instrument here.

## 2. The search, reproducible

Run over `writeup/4_p2_lottery/TECHNICAL_P2_PUB2_V1.md` (636 lines) and
`writeup/4_p2_lottery/BLOG_P2_PUB2_V1.md` (209 lines), case-insensitive extended regex, every
hit read **in context** rather than counted. Script:
`grep -Ecni "$p" TECHNICAL_P2_PUB2_V1.md BLOG_P2_PUB2_V1.md` over the 12 patterns below.

Hit counts on the **post-edit** text (`TECHNICAL | BLOG`):

| # | pattern | hits | what it is aimed at |
|---|---|---|---|
| 1 | `independen` | 15 \| 5 | the literal claim word, all inflections |
| 2 | `truncation-independent` | 5 \| 0 | the hyphenated adjective form |
| 3 | `truncation` | 22 \| 5 | the whole topic, to catch unhyphenated phrasings |
| 4 | `does have (one\|a floor\|a truncation)` | **0 \| 0** | the anaphoric form — a floor asserted without naming it |
| 5 | `floor` | 12 \| 3 | the claim under its other name |
| 6 | `bounded away` | 1 \| 0 | the claim's mathematical phrasing |
| 7 | `uniform` | 3 \| 0 | "uniformly in the truncation" |
| 8 | `window\|decade` | 8 \| 2 | the *quadrature-window* conflation, which is how the claim was justified |
| 9 | `invariant\|insensitive\|unaffected\|regardless of\|does not depend\|doesn't depend\|settled\|stays put\|does not (go\|drift)` | 6 \| 4 | independence stated **without** the word "independent" |
| 10 | `verdict\|outstanding\|commission\|leg 192\|leg 249\|re-deriv\|verification leg` | 19 \| 5 | clause (b), the two ceilings |
| 11 | `σ_min\|sigma_min\|T⁻¹` | 34 \| 0 | every mention of either subject quantity, read individually |
| 12 | `1.44\|1.4e\|4.026\|4.03\|0.0908` | 21 \| 4 | every occurrence of the numbers the claim was ever attached to |

Pattern 11 is the backstop: **34 + 0 = 34** occurrences of the two quantities themselves were read
one by one, so a site phrasing the claim in words none of patterns 1–10 anticipate would still have
to name its subject to make the claim.

**Post-edit verdict: 0 sites assert that `σ_min` or `‖T⁻¹‖_X` has a value independent of
truncation, window or `N`.** Every surviving `independen` hit is one of: a negation
(TECHNICAL L334/335/359/362, BLOG L68/113), a different subject ("three independent coordinates"
L81, "three independent quadratures" L184, "independent data" L303, "`p`-independent" L508,
"independently re-derived" L634, BLOG L143), or the true statement about `ℓ¹_w` (L207 left cell —
leg 127's theorem, which is exactly where a truncation-independent value genuinely does **not**
exist).

## 3. The 7 sites, before → after

Template for every replacement: leg 268's own already-corrected wording at TECHNICAL §3.2's
"no §2-class obstruction" row (L175), §3.5's ladder paragraph, and §5(3). No new numbers.

**Independence family (5 sites).**

1. **`BLOG` L65–67** *(verify_268 site A)* — "bounded away from zero and, **crucially**,
   independent of the truncation — it moves by `1.4e−04` when we widen the computational window
   by four decades." The load-bearing "crucially" is gone, the independence assertion is gone, and
   the `1.4e−04` is relabelled as the **quadrature-window** sweep it actually is, with a forward
   pointer to the correction 40 lines down. Was 42 lines above leg 268's own "is simply false".
2. **`TECHNICAL` L201** *(site B)* — §3.2's contrast table, row "truncation dependence". The cell
   put a window-sweep number opposite a truncation claim, at `1e−4` — an order of magnitude
   tighter than the ladder's own measured relative spread `1.39e−03`. Now states the flattening
   ladder (0.139 % / 16-fold) as the truncation evidence, explicitly says it is **not** a
   truncation-independent value, and carries the `1.444e−04` window spread as a separate sweep
   that "is not evidence about truncation".
3. **`TECHNICAL` L353–354** *(site C, load-bearing)* — "Here the operator does have one". Now:
   leg 127's mechanism is **measured absent** rather than proved absent, with the `ℓ¹_w` side
   marked as the theorem it is. See §5 below on why this is wording and not argument.
4. **`TECHNICAL` L200** — **not named by verify_268.** §3.2's `σ_min` row asserted "does **not**
   go to 0" as settled fact. Now "does not decay that way … a ladder that flattens … evidence of
   a positive limit, **not a proved floor**", matching L175 verbatim in strength.
5. **`BLOG` L98–99** — **not named by verify_268.** "Here the operator does have a floor" — the
   blog mirror of site 3, sitting **9 lines** above leg 268's own new "is simply false"
   paragraph. Now "evidence of a floor and not a proof of one".

**Verification-status family (2 sites, clause (b)).**

6. **`TECHNICAL` L629–631** *(site D)* — §7 said the verification leg "has returned no verdict"
   while the BLOG ceiling (leg 268's edit) said "has since reported". Resolved in the BLOG's
   favour because the BLOG is right: §7 now records that leg 192 committed only a novelty pass,
   **and** that leg 249 re-derived the construction in exact rational arithmetic, confirmed both
   conjuncts, and corrected two banked values — with the explicit non-inflation that leg 249's
   exactness "does not convert any ladder here into a proved bound".
7. **`TECHNICAL` L64–67** — **not named by verify_268.** §0's "Two limits" (2) carried the same
   stale status ("no re-derivation, no verdict … not as independently re-derived ones") 560 lines
   before §7 did. Same repair, with the branch/commit pin `leg/249-h2cv2-v2 @ e9db984` and the
   artifact link.

The distinction that leg 192 is not formally leg 249 is preserved at both sites rather than
papered over: the commissioned leg produced a novelty pass, and the re-derivation was done
elsewhere. Both statements are true and are now stated together.

## 4. Inspected and deliberately **not** changed

- **`BLOG` L88** "the quantity … stays put here — `0.0908`, moving by **0.139 %** across a
  sixteen-fold change in truncation". Pattern 9 flagged "stays put". Kept: the phrase is
  immediately quantified with the magnitude of the movement, and the same paragraph block closes
  at L116–119 with "a floating-point sequence that descends and flattens is evidence of a
  positive limit, not a proof of one, and we do not write it as one". It asserts no independence.
- **`TECHNICAL` L202** "no such floor" and L525/L531 "no `ℓ¹_w`-class floor" — a floor on `‖A‖_w`,
  a different quantity in a different direction. Untouched.
- **`TECHNICAL` L150** "uniformly in `M`" — inside §2's `ℓ¹_w` argument, where it is correct.
- **`TECHNICAL` L275–282** — the leg-176 status paragraph, explicitly marked "kept exactly as
  written" with its outcome pointer. It is about the *construction* leg, not the verification leg,
  and is honestly time-stamped. Untouched.
- **`BLOG` L146/L181, `TECHNICAL` L453/466/494/528/584/604** — "invariant" in §4's
  `σ = s + 1/p` sense. A different object entirely.
- **`TECHNICAL` L46, L175, L319–335, L376–383, L568–573** — leg 268's own corrected text, already
  in the target framing. Byte-untouched; they are the template, not the patient.

## 5. The escalation clause did not fire — the call, recorded so it can be disputed

Site 3 (and its mirror, site 5) carries what leg 176 calls "its most useful output" and propagates
into §5(1) and §6, so the question "is this wording or argument?" is real.

It is wording. The contrast has **asymmetric evidence on its two sides**, and the old text stated
both sides at the strength of the stronger one:

- `ℓ¹_w`: `σ_min(L_M) = c_s M^{−(1−s)} → 0` is a **theorem** (leg 127), giving `Z₁ ≥ 1` for every
  bounded `A`;
- origin-`H²`: a **float64 ladder**, 0.0908878 → 0.090804 over `N = 64…512` (0.0920 %), 0.139 %
  over the full 16-fold range — which leg 176's own words call "float64 evidence of a positive
  limit, not a proof of one", and which leg 268 had *already* restated as such at §3.2 and §3.5.

The repaired sentences keep the inference — leg 127's mechanism does not apply here, therefore the
`Z₁` failure is attributable to the block-diagonal **shape** of `A` — and attach to it the
qualifier "on that reading, the strongest the data support". Nothing that §§3–6 draw from the
contrast uses more than *this ladder flattens where that one decays*, which is precisely what
leg 249 independently states survives. No conclusion changes, no magnitude changes, and the `140.72`
best cell and its `~K²` growth are untouched.

The clause therefore did not fire, and PUB2's approval does not return to the user on this leg's
account. This paragraph is here so a reviewer can reject that judgement directly rather than
reconstruct it.

## 6. Clause (c) — the protected artifacts

`git diff origin/main -- writeup/data/ writeup/novelty/leg_268.md experiments/journal/leg_268.md
writeup/novelty/verify_268.md` = **0 bytes**. Leg 268's companion artifact
`p2_route_h2c_v1_construction_correction_leg268.json` and leg 176's banked
`p2_route_h2c_v1_construction.json` are both untouched, as is `verify_268.md` itself (read-only
territory). Total leg diff: **2 files** in `writeup/4_p2_lottery/` plus this journal and
`writeup/novelty/leg_271.md`.

## 7. Ceiling

No computation was run. No solver was touched. Every number appearing in the edited text was
already in the documents or already banked by leg 249; **this leg introduced none**. Nothing is
claimed about `HL_S2_nonsymmetric` and nothing about Navier–Stokes — the object is still the
`a = 0` CLM steady linearisation. PUB2 is now internally consistent on this claim family pending
leg 270's full trace; it is **not** thereby approved for submission. No ban lifted, no route
promoted, no GA compute, no link of the `L1 → L4` chain moved. Clay odds remain ~0.05 %.
