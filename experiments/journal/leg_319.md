# Leg 319 — Route-P0TCR: correct the 7 landed surfaces carrying 6.855

Repair leg, drafted by the DM directly from leg 300's journal §7 no-branch
(`experiments/journal/leg_300.md:81-134`). Leg 300 landed at `5e30bf3`: it verified leg
266's GATE-YES correction and found the width ratio quoted `6.855` re-derives from BCG's
own `(eq:rstar)` / `(eq:r:restriction)` at γ=7/5 as the closed form
`(7+3√5)/2 = 6.8541019662496845...`, i.e. `6.854` at the quoted precision — a slipped
final digit, rel. err `1.310e-04`. This leg does not re-derive that value; it cites it.

## 1. The 7 surfaces, listed before any edit (per leg 300's §7 propagation census: 8
   surfaces total, 7 already on `main`)

| # | surface | content |
|---|---|---|
| 1 | `experiments/JOURNAL.md:3730` | "6.855x wider" in leg 266's landed-history entry |
| 2 | `DIRECTION.md:11154` | "6.855× wider" in the 2026-08-07 DM update |
| 3 | `DIRECTION.md:13287` | "6.855x" in Ruling 2's claim-bearing description of 266 |
| 4 | `DIRECTION.md:13326` | "6.855x wider" inside leg 300's own **dispatched gate thesis** |
| 5 | `DIRECTION.md:13334` | "6.855x width ratio" inside leg 300's own **dispatched gate question** |
| 6 | `DIRECTION.md:13472` | "6.855x" in reserve leg 305's title |
| 7 | `DIRECTION.md:13477` | "6.855x" in reserve leg 305's thesis |

(The 8th surface, `experiments/journal/leg_266.md:70`, is explicitly out of this leg's
territory per the leg brief — "266's branch is NOT touched" — and is not counted among
the 7 here.)

## 2. Surfaces 4 and 5 resist — reported before editing anything

Surfaces 4 and 5 are the text of **leg 300's own pre-committed gate** (thesis and
question, `DIRECTION.md:13319-13344`). Leg 300's own journal (§7, drafted rework-leg spec,
`experiments/journal/leg_300.md:113-116`) states explicitly this text must NOT be edited:
"leg 300's own quoted gate text at DIRECTION.md:13328/:13336 deliberately EXCLUDED, since
a pre-committed gate records what was asked." A pre-committed gate is a historical record
of what was asked of the agent at dispatch time, not a live claim; correcting it after the
fact would misrepresent what leg 300 was actually asked to verify (269 was asked to check
"6.855x", found it wrong, and answered accordingly — rewriting the question to already say
"6.854" erases that history).

This is exactly this leg's own gate's no-branch condition: "a surface resists correction
without touching an argument." Surfaces 4/5 resist not because editing would change what
the surrounding argument claims, but because editing a pre-committed gate falsifies the
historical record of what was asked — a distinct, but equally binding, reason to stop.

**Verbatim, surface 4** (`DIRECTION.md:13326`):
> `0.0243163, vs the target window (1, 7/6], width 0.1666667 (6.855x wider). It finished`

**Verbatim, surface 5** (`DIRECTION.md:13334`):
> `branch, and (c) the window endpoints (1.1666667, 1.1909830) and the 6.855x width ratio`

Per the leg brief's own no-branch instruction ("STOP at the enumerated 7, report the
resister verbatim, escalate — never silently widen the territory"), surfaces 1/2/3/6/7 are
corrected below and surfaces 4/5 are left untouched, verbatim, with this note as the
escalation record.

## 3. Repository-wide sweep for an 8th surface (executed, not assumed)

`grep -rn "6\.855" --include="*.md" --include="*.py" --include="*.json" .` (case-sensitive,
literal decimal point) across the whole tree, cross-checked with `grep -o` context for
every JSON/MD hit outside `DIRECTION.md`/`experiments/JOURNAL.md`/leg 300's/266's own
files. Result: **no 8th surface**. Every other hit is incidental digit overlap in an
unrelated float — e.g. `writeup/data/spike1_stepA_velocity.json` contains
`6.855039399091452` and `-6.855288156608295` (unrelated radial-cut samples),
`writeup/data/p2_weight_repairs_v2.json` / `p2_route_wvr_v1_fitness.json` /
`p2_route_ngx_v1_general.json` contain `26.85575221999712` and `86.85522233596849`
(unrelated scalars) — none is the width ratio. `writeup/INDEX.md:104` and
`writeup/4_p2_lottery/BLOG_P2_ROUTEP0TCV_V1.md` / `TECHNICAL_P2_ROUTEP0TCV_V1.md` and
`experiments/p2_route_p0tcv_v1_verify*.py` / `writeup/data/p2_route_p0tcv_v1_verify.json`
quote `6.855` as leg 266's *original, wrong* claim under test — historically accurate
prose about what leg 300 found wrong, not a live assertion of the value — and are outside
this leg's declared territory besides (leg 300's own artifacts; `INDEX.md` is explicitly
orchestrator-owned).

## 4. Edits made

Surfaces 1, 2, 3, 6, 7 corrected in place: `6.855` / `6.855x` / `6.855×` →
`6.854 (closed form (7+3√5)/2, per leg 300's re-derivation)`, with the surrounding
sentence's other numbers (window endpoints, both widths) left byte-identical. See the
diff for the exact wording chosen per surface; each correction cites leg 300 by number so
the provenance is visible in place.

`writeup/CORRECTIONS.md`: one append-only entry (§13), following §12's template (leg 128's
"ceiling" convention), stating what was corrected, what resisted and why, and the sweep
result.

## 5. Diff-check performed (gate's own requirement)

`git diff --stat` and a manual read of every changed line confirm: 5 lines changed (the 5
non-resisting surfaces), 1 file appended-only (`CORRECTIONS.md`), 2 new files added (this
journal, the novelty pass). No other numeric content on any touched line changed — window
endpoints `1.1666667` / `1.1909830`, both widths `0.0243163` / `0.1666667`, and every
unrelated number on the same lines are byte-identical before/after.

## 6. Gate answer

**Gate.** Are all 7 enumerated surfaces corrected to the closed form with a citation to
300's re-derivation, with zero other numeric content changed (diff-checked)?

**NO.** 5 of 7 are corrected (1/2/3/6/7). 2 of 7 (4/5 — leg 300's own dispatched gate text)
resist correction without touching an argument, for the reason in §2: a pre-committed gate
is a historical record of what was asked, and editing it after the fact would misrepresent
that history, not merely restate a number. No 8th surface was found (§3). Per the gate's
no-branch: reported here verbatim; this leg escalates rather than silently widening the
territory to force a "yes" by editing text that should not be edited, or silently leaving
6.855 unfixed on the 5 surfaces that have no such objection.

**This leg pushes its branch only, not `main`**, and parks the finding for the DM/user:
should leg 300's own gate text (surfaces 4/5) be read as in-scope of "corrected" (in which
case a ruling that historical-gate text may be corrected is needed — it would set a
precedent for every future pre-committed gate that quotes a since-corrected number), or
should the leg's own gate wording be amended to explicitly exclude them the way leg 300's
original drafted rework-spec did? Either ruling is the user's/DM's, not this leg's to make
unilaterally.

## 7. What this leg did not do

Did not touch `leg/266-p0tc-v1` or `leg/251-p0t-v1` branch files (out of territory,
explicitly). Did not touch `experiments/journal/leg_266.md:70` (the 8th, non-`main`
surface named by leg 300, also explicitly out of territory). Did not touch `writeup/INDEX.md`
(orchestrator-owned). Did not re-derive the closed form independently — cited leg 300's.
Did not change any window endpoint, width, or any other numeric content anywhere.

## 8. Clay

No link of the L1→L4 chain moved. This is a repair to landed prose's transcription of an
already-derived scalar. Clay odds stay **~0.05%**.
