# Run report — 2026-08-19. **RUN STOPPED BY USER DIRECTIVE. NO SUCCESSOR SCHEDULED.**

Written per §9c at the close of the CONDUCTOR run. The live block is
`reports/ORCH_STATE.md`; the restart document is `CONTINUATION_PROMPT.md`. Numbers here are
cited by file and JSON field and are not restated as prose facts.

## 1. What this run did

Waves 8 and 9. Legs 409–416, plus `E-FE` (408) carried over. Nine units dispatched across the
two waves; **eight returned and were integrated, one was still running at the stop.**

**Seven `CORRECTIONS.md` sections landed: §53–§59.** That is the run's output. It is not a
flattering output and it should not be written as one.

## 2. The honest accounting of those seven sections

**Four of the six correct the Conductor's own record.** The other two correct drafts.

| § | unit | what it establishes |
|---|---|---|
| §53 | Conductor | `L-JVER`/§52 landing audit — upheld and extended. **Its `×130` was later withdrawn by §56.** |
| §54 | Conductor | `L-JVER`'s "evidence script" **re-runs and overwrites the artefact it checks**. Reverses §49. |
| §55 | `P2-DRAFT` | Per-term decomposition is at the **wrong norm**; `rho_exponent` banked against the **wrong fit window** in three fields. |
| §56 | `V-W8` | **Four defects on the Conductor.** §53's `×130` **WITHDRAWN as arithmetic**. §50 item 6's remedy had landed in one file of two. |
| §57 | `P4-DRAFT` | **§45's `32 of 49` is UNBANKED.** An `UNDER-RESOURCED` measurement was banked as a **passed control**. |
| §58 | `L5-cmod` | Literal pre-committed rule returned **`UNDER-RESOURCED`**. §53's flag discharged **only** where directly measured. |
| §59 | `L5-cmod`, after the close-out | **The unit's own evidence check `E5` FAILED** (`41b0ace`). `n_s` `6→12` moves the per-decade increment `×1.855293`; control `X4` did not fire as planted. §58's word **CONTROLLED is WITHDRAWN** and its 7-digit `relative_change` withdrawn as a quoted figure; `c_mod` is defensible to `869.288`. The one section of this run that moves the record BACKWARDS. |

## 3. The three findings a successor should read first

1. **§57 `F3` — the refutation and the verdict were in the same JSON object, four lines
   apart, unread for eleven legs** by the unit, the wave's evidence script, the Conductor and
   `V-W6`. **No instrument in this record checks a verdict field against its own object's
   sibling fields.** Parked as `P4-F3` (~2–3 h). This is the highest-value unbuilt instrument.
2. **§55 `F3` — the repair a consistency check would demand would have BROKEN a wall.** Aligning
   `rho_exponent` to the range named beside it reports summability available and breaks `W4`
   clause (b) **off a transient**. Parked as `P2-F3` with **ESCALATE FIRST** on the row.
3. **§57 `F1` — `32 of 49` cannot be re-derived.** The record's central methodological claim
   has no artefact, no classifier and no file list anywhere in `writeup/data/`.

## 4. What did NOT happen

- **No `L1 → L4` link moved.** None has moved in over 416 legs.
- **No wall moved in either direction.** `W4` clause (c) — the torus, **statement (D)** — remains
  **UNTESTED, NOT CLOSED**, and is the only surviving clause of `W4`. It is blocked on the
  `D_BUNDLING` user ruling, **not on work**.
- **No `NO` was reopened.** `L5`'s `NO`, `L6`'s, `L6-b`'s all stand.
- **Nothing was certified.** Every landed unit is **Tier 2**, and Tier 2 is never a proof.

## 5. Direction, re-ranked on what is measured now

**Lane V — rank 1 BY DEFAULT, NOT BY STRENGTH** (still owes its obligatory Q4 unit).
**Lane L — DEMOTED 2026-08-19 from PRIORITY to rank 2.** Both `W4` clauses it could reach are shut
by its own units; the rest is untouched and has no known method; and this run found four defects in
its integration record, none of them by the lane's own instruments.
**Lane T — rank 3, DEFERRED, and BLOCKED ON A USER RULING, not on work.**
**Lane R — continuous, never sets direction.**

**The cheapest unit that could move an `L1 → L4` link: NO SUCH UNIT IS KNOWN.**
`CLAY_OBLIGATIONS.md` §6 names two obligations with no known method, and nothing in `OPTIONS.md`
is priced to deliver a certified result.

## 6. In flight at the stop

**`E-FE` (leg 408)** — `[57/163]` attempts, 54 banked partials in
`experiments/programme_r4/e_fe_partials/json/`, branch `main`, last checkpoint `3176b35`.
**No gate answer, no verdict, NOT citable.** It was **not** stopped; the directive forbade
`TaskStop`. A successor must read the partials and must not assume a result.

## 7. Process findings against this run's own discipline

- An audit loop formed: three consecutive units (`PB2`, `V-W8`, `P4-DRAFT`) produced corrections
  and no movement on the mathematics. `L5-cmod` broke it by computing. Recorded, not acted on.
- The Conductor's own close-out edit **broke a landed gate** (`test_plan_of_record.py` tests 4 and
  8) and only `scripts/merge_gate.sh` caught it. Same shape as §57 `F3`.
- `test_headroom.py:46` has a **known false-negative path**; today's readings were independently
  re-measured and are true. `V-W8`'s proposed remedy is **wrong** and would punish §3j's own
  preference for verbatim retirement (§56 item 7).
- **The tool-output channel drops words, including inside `repr()`.** Active hazard; see
  `reports/ORCH_STATE.md` `## Environment notes`.

**The close-out was RE-OPENED, and that is the finding of this section.** `L5-cmod`'s journal §7–§12 landed at `41b0ace` *after* the final close-out commit `add9751` and after the final message. It reported that its own evidence check `E5` FAILED against its own result. The wind-down directive requires each returning unit to be gated and integrated *as if the run were continuing*, so it was: `CORRECTIONS.md` §59 withdraws a word and six digits from §58. **A close-out that had been declared finished was worth less than one measurement that arrived late, and the correct move was to spend the tidiness rather than the truth.** The evidence script had been committed by the Conductor marked UNVERIFIED BY ME after timing out at a two-minute budget; **that caveat was load-bearing — running the script fails.** It is left on `main`, failing and labelled.

## 8. Stop

Report magnitudes, never booleans. No summary claims more than the artefact it links to. Tier-2
ceiling in every route-4 gate until a certification route lands. §6's two no-method obligations
**OPEN**. No external outreach; reading published material is authorised, contacting anyone is not.

**Nothing in this run moved a link of the `L1 → L4` chain. Clay stays ~0.05%.**
