# Daily report — 2026-08-05

Orchestrated six-agent day, dispatched from `ORCHESTRATOR_PROMPT.md`/`ORCHESTRATION.md`.
Stage `TC` (`ASSEMBLE the bordered certificate`) was `NEXT` at Step 0. All eight PRs from
the day are merged; final `main` is `7bb5ce2`; `scripts/merge_gate.sh origin/main` passes.

## Nothing parked for the user today

All work merged same-day. No plan change outside the gate's prescribed branch, no ban lift
outside its recorded condition, no Clay-chain claim, no rewrite of banked results. One item
is flagged for attention below (§ Corrections and errata), not parked as a PR.

## Agent table

| Agent | Task | Outcome | Claim-bearing? | Status |
|---|---|---|---|---|
| **LEG** | Execute stage `TC`: assemble the bordered certificate (coupling, four-term polynomial, border defect) | Gate answered **NO** (below). PR #5, revised once after review, then merged | Claim-bearing | Merged `3ea2830` |
| **VERIFIER** | Re-measure leg 52's headline before LEG builds on it; review LEG's PR; adjudicate a literature conflict; (added mid-day) fix a pre-existing bug | Leg 52 confirmed to 2.2e-13 with an extended ablation (constant spans 33x, grows `K^0.46`); found 3 attribution gaps + 1 literature conflict in LEG's first PR; fixed a Newton-tolerance bug (user-authorized); re-derived LEG's fixes independently and signed off | Mixed (fix + report are mechanical/investigative; review verdict is claim-adjacent) | 3 PRs merged: `9eb42a9`, `43285dc`, `7bb5ce2` |
| **LIT** | Resurfacing check for arXiv:2604.01868; resolve whether BDL (arXiv:1503.06315) covers a zero-diagonal Fredholm operator | Resurfacing: confirmed absent (search-index gap, not a fetch problem), second independent confirmation of leg 52. BDL: does **not** cover it — PDF extracted this time; assumptions 4-5 require a diagonal bounded away from zero | Mechanical | Merged `d2b8254` |
| **REPRO** | Run all `test_*.py`, regenerate figures, check evidence scripts | Claimed 50/50 tests pass — **this did not reproduce** (see below). Figure fixes (missing fig42, fig20 rendering noise) were fine and merged | Mechanical | Merged `c762a15`, with a correction recorded in the merge commit |
| **DOCS** | `writeup/INDEX.md`, quartet-completeness audit, dead links, README | Created INDEX.md; found 2 real quartet gaps (Route-PORT v1/v2, below); restored a missing figure; flagged a stale `writeup/README.md` numbered index | Mechanical | Merged `89587c2` |
| **PREP** | Implement P2/P3 named repairs to `solver/weight_search.py`; re-run six-property viability gate | Gate still **FAIL, 4/6** (P2 0.775→0.875, P3 worst \|slope−1\| 0.366→0.342) — moved, did not pass. No GA compute run (ban intact) | Claim-bearing | Merged `8675603` |

An unrelated PR (#3, a root `README.md`) was merged directly by the user outside this
orchestration — noted, not part of the six-agent day, no action taken.

## LEG's gate, verbatim

> With the far-field amplitude carried as a real unknown through all four terms, does the
> radii polynomial close — on the a = 0 CLM object, in a class with s < 0.394?

**Answer: NO.** The term that ran out is `Z₁`'s block coupling for a block-diagonal
approximate inverse `A` (`Z₁[Γ←tail] ≥ 20.47` across six normalisations, verified as exact
closed forms: augmented `‖Γ⁻¹‖ = 2(K²−1)` vs. unaugmented `4(K−1)` — the augmentation's own
weight pairing creates the `K²` growth). Not the tail (leg 52's term — behaved, 2.19–10.32),
not `Y₀` (0 exactly), not `Z₂`. Plan updated: `TC` marked done with this answer; **`MM` is
now `NEXT`** — "is a non-block-diagonal approximate inverse a real lane, or is this the
lane's end?"

## What VERIFIER contested, and how it resolved

1. **LEG's PR #5, first pass — 3 attribution gaps found, none flipping the gate.**
   Mislabeled term scope (B.1), a wrong mechanism citation that didn't match LEG's own code
   (B.2, `K` cited where the real growth was `K²`), and a control that couldn't fail by
   construction (B.3). LEG fixed all three; VERIFIER **re-derived every fix independently**
   (not just re-reading LEG's claim) and signed off: no unresolved gap.
2. **LIT vs. LEG's own novelty pass — conflicting claims on the same question**, resolved in
   LIT's favor. LEG's query prepended the literal arXiv ID, which doesn't test leg 52's flag
   as posed; LIT re-ran leg 52's exact query and reproduced the null result a second time.
   LEG withdrew "the flag is cleared" in all four places it had written it, plus JOURNAL and
   LITERATURE_CHECK.md; the merge conflict on that file was resolved by keeping LIT's section
   byte-for-byte and editing LEG's down, not concatenating.
3. **VERIFIER's own errata**, self-reported in its final sign-off: three non-blocking
   overgeneralizations in the writeup (e.g., a ratio range stated as "1.90–2.00" that is
   actually 1.62–9.67 across the full 20-row sweep). One of these originated in VERIFIER's
   own earlier review text, which it explicitly flagged as its error, not LEG's. None changes
   a number, a conclusion, or the gate — left as follow-up errata.

## Corrections and errata

- **REPRO's headline claim ("50/50 tests pass") did not reproduce.** I independently ran
  `test_weight_search.py` three separate times against unmodified `main` — a fresh clone with
  system Python, the same clone with the repo's actual `.venv` Python, and directly inside
  REPRO's own worktree — and it failed deterministically all three times (the Newton
  non-convergence bug VERIFIER later fixed). REPRO's actual file changes were orthogonal and
  fine; the test-count claim was wrong and is recorded in the merge commit
  (`c762a15`) rather than silently accepted.
- **A pre-existing bug was found and fixed mid-day, outside the original six-agent brief.**
  `test_weight_search.py::test_known_answer_substrate` failed on unmodified `main` — Newton's
  fixed `tol=1e-14` was below its own float64 evaluation-noise floor at `n≥800` (lesson 86).
  Traced every consumer of the affected class (`BorderedCLM`) and confirmed no banked result
  (Route-L1's published ladder uses a different class, `BorderedHL`) depends on the broken
  combination — confirmed safe, then dispatched to VERIFIER (idle at the time) on user
  authorization as an exception to its normal report-don't-repair mandate. Fixed, verified,
  merged (`9eb42a9`); this had been blocking PREP's PR.
- **Two quartet gaps flagged by DOCS, not fixed today** (writing them up is claim-bearing
  work outside DOCS's mechanical remit): Route-PORT v1 and v2 each have a runner and curated
  data but no `*_evidence.py` and no registered figure. Both are load-bearing negative
  findings (the certificate closes around the wrong truncated object; extending reach makes
  the truncation gap worse) currently missing their full quartet.
- `writeup/README.md`'s numbered index is stale past entry #47 — flagged by DOCS, not edited
  (numeric/index content, outside its remit).

## Proposed split for tomorrow

`plan_of_record.py` now has `MM` as `NEXT`:

> **MM — THE MISMATCH.** Is a non-block-diagonal approximate inverse a real lane, or is this
> the lane's end? Deliverable MM-1: state and check the lower bound
> `Z₁ ≥ |1 − K/2|·(w_…)` as an inequality, not a mood.
> **Gate:** does an approximate inverse that is *not* block diagonal bring the assembled
> `Z₁` below 1, on the a=0 CLM object, in a class with s<0.394? **Yes** → the method and
> object are not mismatched, only the standard split was. **No** → stop building
> ℓ¹-Fourier radii-polynomial certificates for inviscid self-similar profiles this way.

| # | Codename | Lane | Model | Branch | Task |
|---|----------|------|-------|--------|------|
| 1 | **LEG** | local worktree | Opus, high effort | `leg/mm-v1` | Execute stage `MM`: state the `Z₁ ≥ \|1−K/2\|·(...)` inequality precisely, check it against MM's gate, novelty pass first |
| 2 | **VERIFIER** | local worktree | Opus, high effort | `verify/tc-headline` | Re-measure TC's own headline (Y₀=0, `Z₁[Γ←tail]≥20.47`, the two exact closed forms) before LEG builds MM on it; then review LEG's MM PR |
| 3 | **LIT** | cloud | Sonnet | `lit/mm-v1` | Any novelty check MM's own construction needs; also worth a second, more careful resurfacing check for arXiv:2604.01868 given today's false-positive from a differently-worded query — is there *any* query phrasing that legitimately surfaces it, or is the index gap total? |
| 4 | **REPRO** | cloud | Sonnet | `repro/2026-08-06` | Full test sweep — with an explicit instruction to run with the repo's actual `.venv/bin/python` and to report a raw pass/fail count per file, not a rolled-up summary, given today's discrepancy |
| 5 | **DOCS** | cloud | Sonnet | `docs/2026-08-06` | Close the two flagged gaps: write the missing `*_evidence.py` + figure for Route-PORT v1 and v2 (data/runner already exist; this is assembling the existing quartet pieces, still mechanical since no new numbers are computed); fix the stale `writeup/README.md` index |
| 6 | **PREP** | cloud | Sonnet | `prep/weight-p2-2d` | P2's own gate note: "the measured wall is a 1-D slice of what's actually a 2-D boundary" — investigate whether a 2-D wall gets P2 to 0.90 (still does not authorize GA compute either way) |

Re-derive this table from the actual `plan_of_record.py` state at the start of tomorrow's
session — MM's gate answer isn't in yet, so LEG's task is the only firm one; the rest follow
today's usual reconstruction from file ownership and the plan.

---

# Session 2 — first four-leg-parallel run (cycle 1)

New orchestration contract (`ORCHESTRATION.md` + `ORCHESTRATOR_PROMPT.md` + `DIRECTION.md`):
Sonnet orchestrates, an Opus Decision Maker steers, four legs run in parallel with paired
verifiers. Stage `MM` was `NEXT` at Step 0. All four legs plus five verifiers and the Decision
Maker completed; every PR gated, verified, and merged. Final `main` is `667e07b`;
`scripts/merge_gate.sh` passes.

## Agent table

| Agent | Task | Gate answer | Claim-bearing? | Status |
|---|---|---|---|---|
| **DM** | Produce leg queue for legs 54-60; later, the `NG` direction call after MM/B both closed off | Recommended `NG` as `NEXT`, escalation #1 | N/A (direction, not a claim) | `DIRECTION.md` committed twice |
| **LEG-A (54)** | Route-MM: spend the shape of A, or close the ℓ¹-Fourier lane | **NO** | Claim-bearing (fires `T`'s no-branch) | Merged (verified twice) |
| **LEG-B (55)** | Route-NB: does the target have finite ℓ¹_w norm? | **YES** | Claim-bearing (narrows a ban) | Merged |
| **LEG-C (56)** | Route-TN: enclose the (H,D) consistency defect | **NO** | Claim-bearing | Merged |
| **LEG-D (57)** | Route-XS: shape dichotomy vs. published certificates | **NO** | Claim-bearing | Merged |
| **VER-A** | Re-measure leg 53's headline before leg 54 built on it | N/A | Investigative | Merged; caught MM-1's K=2,3,4 vacuity pre-construction |
| **VER-A2** | Review leg 54's pushed PR | N/A | Investigative | Merged (cherry-picked); caught battery omitting K=2/K=6, refuted an over-claim |
| **VER-B** | Review leg 55's pushed PR | N/A | Investigative | Findings folded into leg 55's own commits; caught a miscalibrated systematic error |
| **VER-C** | Review leg 56's pushed PR | N/A | Investigative | Findings folded into leg 56's own commits; caught a false mechanism claim (prose-only fix) |
| **VER-D** | Review leg 57's pushed PR | N/A | Investigative | Signed off, one non-blocking citation-location note |

## What each leg found

- **Leg 54 (MM), gate NO, verified twice.** Best admissible `Z₁` over every shape × class ×
  gauge × split: **8.9591** (after a verifier caught the first draft's battery omitting
  `K=2`/`K=6` — the wrongly-reported headline was 32.75), against a block-diagonal baseline of
  **10.4584** (1.167×, needed <1). `MM-1`'s inequality verifies as an exact equality, restricted
  to `K≥6` flat / `K≥4` algebraic. A candidate "shape-independent floor" was proposed then
  refuted by an explicit counter-construction. Fires `T`'s pre-committed no-branch: stop
  building ℓ¹-Fourier radii-polynomial certificates for inviscid self-similar transport.
- **Leg 55 (NB), gate YES.** `HL_S2_nonsymmetric`'s compactified-basis coefficients decay as
  `k^-1.396` — finite `ℓ¹_w` norm at `s=0`/`0.3`, divergent at `s=1`. Narrows (does not falsify)
  a ban-list clause: "the target was never in the space" is retired as an explanation for legs
  52-54's failures; leg 54's block-coupling/shape finding is the operative reason.
- **Leg 56 (TN), gate NO.** The (H,D) consistency defect exceeds `L1` step one's admissible `τ`
  by `1.85e7×` (derivative) / `2.04e11×` (Hilbert, corrected mechanism after a verifier caught
  a false "shared interpolant" claim — prose-only fix, gate unaffected and strengthened).
- **Leg 57 (XS), gate NO.** No published radii-polynomial certificate has an off-diagonal
  unbounded part with a non-decaying tail inverse (4 papers, 15/15 executable gates). Closes
  the BDL novelty flag (does not cover the zero-diagonal case) but finds Cadiot arXiv:2505.03091
  independently pre-empts the same claim — a stronger reason to keep a related ban up.

## Corrections and errata

- Every leg's first draft had at least one real gap caught by its verifier before merge (see
  agent table). All were fixed in place, wrong versions left visible per repo discipline.
- `DIRECTION.md` was not committed promptly after the DM wrote it, so several leg worktrees
  forked from a stale (seed) version — surfaced when leg 57's agent reported the file looked
  empty. Fixed mid-cycle by committing `DIRECTION.md` as soon as the gap was noticed. Recorded
  in `reports/ORCH_STATE.md`'s sharding ledger as the reason this cycle's sharding-experiment
  comparison is void.
- Three leg branches (55, 56, 57) each had a stray `.venv` symlink accidentally committed
  during gating (created by the orchestrator to run tests inside worktrees that don't share
  the main checkout's virtualenv); removed before merge each time.
- Legs 55 and 57 each initially forgot to register a new `solver/` module in `capabilities.py`
  — caught by the mechanical merge gate, fixed in a follow-up commit each time.

## Escalations

**Escalation #1, applied.** Stage `NG` ("the no-go, stated as a theorem") entered the committed
sequence as `NEXT`, per the Decision Maker's recommendation under the user's explicit
pre-delegation ("whichever pursues our goals best" — MM answered NO and stage B was found
pre-refuted on all three of its degrees of freedom). Reversible.

**Two questions parked, not blocking.** (1) Stage `V`'s ban-lift condition ("needs `L1` first")
may now be permanently unmeetable, since `L1` is dead in both realizations (leg 54's coefficient
basis, leg 56's collocation). (2) `NG`'s yes-branch delivers a negative Tier-3 result — worth
confirming this is what the user wants to buy before more effort goes into `NG-2`.

## Session close

Session stopped by explicit user request after this cycle's work was pushed to `origin/main`
(`667e07b`), so the user could update `ORCHESTRATOR_PROMPT.md` and start a fresh session — not
a stop file. See `reports/ORCH_STATE.md` for the full resume checklist.

**NOVELTY: BDL flag closed, replaced by a stronger Cadiot-based reason for the same ban. No
link of the L1→L4 chain moved. Clay unchanged at ~0.05%.**
