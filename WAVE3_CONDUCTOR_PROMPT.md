# WAVE 3 — CONDUCTOR ASSIGNMENT, CARRYING THE USER'S RULING OF 2026-08-14

You are the **Conductor** of this repository's Clay programme, under `ORCHESTRATION.md` **§3g**
(one long-lived entity owning both direction and integration, dispatching waves of 2–4
self-terminating workers) and **§3h** (what a wall-breaking mandate does and does not license).
**This message is your assignment and it carries a user ruling. Start now.** Do not reply asking
what to work on, and do not re-litigate the ruling — transcribe it, then plan and dispatch Wave 3.

**Read, in this order, before anything else:** `STATE.md` (the read surface — read it first and
instead of `DIRECTION.md`), `WALLS.md` (whole), `OPTIONS.md` (whole),
`ORCHESTRATION.md` §3c–§3h, `writeup/escalations/ESCALATION_C1_EXEMPLAR_2026-08-14.md`, and the
output of `.venv/bin/python plan_of_record.py`. Consult everything else by pointer only.
Establish the next leg number from the landed record (the last was **395**, leg `T5`).

---

## PART 1 — THE RULING (user, 2026-08-14). Transcription is your first act.

The user has ruled all three questions of `ESCALATION_C1_EXEMPLAR_2026-08-14.md`, and has ruled
on direction. The escalation is **DISCHARGED**. The rulings:

**Question 1 — C1 needs NO replacement exemplar.** C1 is an **apparatus scoping** and its
proposition — a Zgliczyński-style Galerkin-plus-tail dynamical closure is a different apparatus
from a Newton–Kantorovich radii-polynomial contraction — is true independent of any instance in
the literature. C1 **stands, exemplar-free**. Record explicitly that the scope no longer carries
any implied "and this has been demonstrated": no unit may cite C1 as evidence the technology
closes for any object class. C1's **naming requirement is unchanged and binds every unit**
claiming the scope: name the apparatus with a citation, and show it does not construct a single
bounded approximate inverse uniform in `M`. Absent both, the ban applies in full.

**Question 2 — Lane T's priority-1 ranking does NOT survive.** Lane T is **DEMOTED to DEFERRED**
in `OPTIONS.md`, with its cost and re-open conditions recorded per that file's rule. It is kept
alive only through **T2″** (the Type-I rigidity question on `T³`), which survives the exemplar's
fall intact and stays deferred as the lane's sharp question. **Re-open conditions:** (i) a
demonstrated, genuinely-3D closure appearing in the literature — a certificate meeting W2's own
pre-committed test, its three-dimensionality supplied by the certified object itself; or (ii)
T2″ returning a rigidity picture favourable to a natively-periodic non-DSS ansatz. **Nothing
measured is superseded:** the domain-shape obstruction is still not refuted, Theorem NGX, leg
341 and the three dead realizations stand, and W2 stands strengthened. `T3` is not killed; it is
deferred with the lane.

**Question 3 — the packet's defect was material to the RANKING, not to the SCOPING.** C1 itself
needs no re-ruling and its transcription in `plan_of_record.py` stands byte-unchanged. The
2026-08-13 ruling's other items (A2, B1, the narrowed outreach hold) are untouched.

**Direction ruling — the programme's priority lanes are now V and L.** The user adopted the
2026-08-14 assessment on record: **Lane L is on every path** (leg 390 measured that the torus
does not retire `CLAY_OBLIGATIONS.md` §6(i)/(ii), so the two no-method obligations are the last
blockers on every branch, and no unit in 395 legs has ever attacked either), and **Lane V
decides whether any path exists** (W3 is the rung that determines whether Tier 3 is reachable in
principle, both branches valuable). Wave 2's own byproducts point the same way: `T5`'s genuinely
new C1-permitted item (leg 315's `O1` Taylor-model flow-map, needing no function space) points
at Lane V, and `T6`'s Chen–Hou near-miss (`arXiv:2308.01528` — computer-assisted blow-up,
unbounded domain, >1D, algebraic decay) is the record's best lead on the compact-domain
obstruction and points at Lane L and route 4's actual `ℝ³` geometry — prize statement (C).

**Transcribe before dispatching, one commit per item, `test_plan_of_record.py` passing after
each** (the precedent is the 2026-08-13 transcription):

1. Write `writeup/escalations/RULING_C1_EXEMPLAR_2026-08-14.md` as the authoritative ruling
   record (quote the three questions and the rulings above), and mark the escalation DISCHARGED.
2. Update `OPTIONS.md`: Lane T → DEFERRED with the re-open conditions above; Lane V and Lane L →
   ACTIVE, priority lanes.
3. Update `WALLS.md`'s lane priorities to match (V and L lead; T deferred; R runs continuously
   inside every unit's pre-registration, unchanged).
4. Regenerate `STATE.md` (it is only trustworthy if rewritten) and refresh
   `reports/ORCH_STATE.md` in the same commit as the wave plan, per §3g.

**No ban is lifted, narrowed, or reworded by any of this.** Ban counts unchanged.

---

## PART 2 — WAVE 3. Plan per §3g step 1: gates in FINAL WORDING and PRE-COMMITTED READINGS, committed BEFORE any worker is dispatched.

The user directs the following composition. **You write the final gate wording and the
pre-committed readings yourself** — they must name both branches, and every branch's meaning is
fixed before any number exists.

**First, collect `E`.** Wave 1's H-hard diagnostic was still running at the last integration.
Check for its return (branch or landed commit); if returned, audit it against its pre-committed
reading (all four branches E-i…E-iv are in `STATE.md`) and integrate it before or alongside the
wave-plan commit. Its result re-opens Lane R rankings in `OPTIONS.md` §A/§B but does not change
this wave's composition.

### Unit 1 — `V2`: name Lane V's target and open the apparatus feasibility. *Lane V, CONSTRUCTION. The wave's centre of gravity.*

Leg 174's Grade-A × fluid cell is empty **"for want of a target, not a method"**, and supplying
the named target is the lane's first job. Name a 1D (or lowest-dimension-admitting-fluid-
structure) dissipative fluid model whose finite-time blow-up is the certification target, with
the dissipative term inside the certified equation — then open the feasibility of attacking it
with a C1-compliant apparatus: the Galerkin-plus-tail dynamical closure and/or leg 315's `O1`
Taylor-model flow-map enclosure (both now outside the bans; see `OPTIONS.md` and ruling C1).
**C1's naming requirement binds and you may not waive it.** The gate must make both branches
landable: a named target with a feasibility verdict, or a measured statement of which candidate
targets fail and why. §3d governs: an under-resourced attempt returns `UNDER-RESOURCED` and a
cost, never a bare `no`. Pre-committed reading must state: a named target is not a certificate,
a feasibility is not a result on the Clay chain, and W3 is broken only by leg 174's own Grade-A
criterion, applied unchanged.

### Unit 2 — `L2`: attack `CLAY_OBLIGATIONS.md` §6(i). *Lane L. The first unit ever dispatched at a final blocker.*

Is certified far-field decay plus an admissible cutoff genuinely without method, or only without
an attempt? Leg 381's banked bill: critical `L³` tail **326.875 per decade**, required
`α > 1.5` against available `α = 1.0`. **A deficit of 0.5 in a decay exponent is a number, not
an impossibility — the unit's question is what would supply it.** Read the published
localisation/decay techniques against this object; state, per technique, the named hypothesis
that fails — or the one that does not. Pre-committed reading: a measured "still no method, and
here is precisely which hypothesis fails" is a real, landable result — it is the answer to
whether the Tier-2 ceiling is permanent — and is not softened; conversely, a candidate method is
a lead, not a broken wall, until leg 381's bill is actually paid.

### Unit 3 — `L3′`: the Chen–Hou reading unit. *Lane L, literature, full text.*

Read `arXiv:2308.01528` and the Chen–Hou stability line at full text against two named
questions: (a) does its unbounded-domain, algebraic-decay, computer-assisted mechanism bear on
the compact-domain obstruction leg 348 named — and on which side; (b) has its
nonlinear-stability-with-finite-unstable-spectrum technique anything that reaches §6(ii)
persistence-under-localisation for this programme's object. Reading any published document is
authorised; **contacting an author, group, maintainer or list remains HELD.** Bank
`UNREACHABLE`/`THROTTLED` as such, never as zeros; instrument every zero.

### Unit 4 — `V-W2`: the Wave-2 verifier. *Verification. Obligatory — §3g forbids a Conductor verifying a wave a Conductor planned.*

`T4`, `T6` and `T5` are `UNVERIFIED`. A fresh worker with no memory of the construction
re-derives their headline claims **from banked JSON and landed evidence scripts alone** — it is
briefed on the claims, never the reasoning, and does not read `STATE.md`, `WALLS.md`,
`DIRECTION.md` or the journals' reasoning sections. A discrepancy is the deliverable, banked as
a disagreement and not reconciled by the verifier. **Fold in `T1`'s owed machine record**
(`OPTIONS.md` §F, found by `V1`, well under an hour): bank the packet's three questions and the
fact that it ruled none, with an evidence script that exits non-zero on disagreement with the
escalation document. Warning from the record: `p2_route_p2t1_v1.json` is leg 302, route P2T1,
unrelated — do not match on filename substrings.

**Composition check:** the floor (§3g — at least one unit attacking a Clay-chain wall directly)
is met twice over by `V2` (W3) and `L2` (W4/§6). §3f rule 3 is satisfied: `V2` is construction.
Wave 3's own units land `UNVERIFIED` and **wave 4 must carry their verifier** — write that into
the plan commit so it cannot be forgotten.

---

## PART 3 — STANDING DISCIPLINE, unchanged and non-negotiable

- **The ceiling clause survives the ambition.** Tier 2 is never a proof; **Clay stays ~0.05%**
  and no output is described as movement toward Clay unless a link of `L1 → L4` actually moved.
  Scale is not evidence (§3h rule 2); the gate is the deliverable.
- **A ban is superseded by a measurement, never by a decision.** Run
  `.venv/bin/python plan_of_record.py` before any task that could touch one. A defective ban
  *wording* is a user escalation (§8) — and an entity that both raises and rules an escalation
  has defeated the mechanism.
- Pre-committed gates naming both branches; planted controls that can fire both ways; lesson 91
  (a negative names its realization/trial-space/basis); §3d (`UNDER-RESOURCED`, never a bare
  `no`, on any under-resourced null); lesson 68 (checks are executable or they decay);
  `scripts/merge_gate.sh origin/main` must PASS on everything that lands.
- Workers do **not** read `DIRECTION.md` (§3e); territories are disjoint (§5b); workers run to
  their gate and self-terminate; the Conductor audits each return against the gate committed
  before dispatch, lands it, regenerates `STATE.md`, and only then re-plans.
- `reports/ORCH_STATE.md` is refreshed in the wave-plan commit **and** the integration commit.
- Reading published material is authorised; **contacting anyone externally is HELD**.

**Begin now: transcribe the ruling (Part 1), commit the Wave 3 plan (Part 2), dispatch.**
